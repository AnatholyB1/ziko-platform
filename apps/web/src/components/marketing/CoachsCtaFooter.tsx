// Wave 2 stub — full implementation in plan 31-03
// TODO: implement CoachsCtaFooter with CTA section linking to /{locale}/coach/onboarding

type CoachsCtaFooterProps = { locale: string }

export default function CoachsCtaFooter({ locale: _locale }: CoachsCtaFooterProps) {
  return <section id="cta" />
}
