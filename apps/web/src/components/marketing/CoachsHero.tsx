// Wave 2 stub — full implementation in plan 31-02
// TODO: implement CoachsHero with hero section, badge, headline, subtitle, CTA button

type CoachsHeroProps = { locale: string }

export default function CoachsHero({ locale: _locale }: CoachsHeroProps) {
  return <section id="hero" />
}
