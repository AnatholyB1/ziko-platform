'use client';

interface Props {
  clientId: string;
  sport: string;
  dateRange: string;
  compareMode: boolean;
  compareClientId: string | null;
  comparePeriod: string | null;
}

export function HyroxDashboard(_props: Props) { return <div />; }
