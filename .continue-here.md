# Continue Here — UAT Mobile Fixes
**Paused:** 2026-05-17 | Branch: `gsd/phase-22-schema-foundation-rls-keystone`

---

## What we were doing

Running UAT for 8 mobile fixes from commits `13b7b78` (Sentry/bugs) and `aa6be56` (avatar profile).

---

## UAT Status

| # | Test | Status |
|---|------|--------|
| T1 | Photo scan nutrition — fallback Android manipulateAsync | ⏳ not confirmed |
| T2 | Timer workout chrono après background | ⏳ not tested |
| T3 | Rest timer valeur + son après background | ⏳ not tested |
| T4 | Séance workout restaurée après force-close | ⏳ not tested |
| T5 | Long-press aliment → déplacer repas | ⏳ not tested |
| T6 | Upload photo profil galerie/caméra → IdentityCard | ⏳ in progress |
| T7 | Couleur avatar persistée après Enregistrer | ⏳ blocked on T6 |
| T8 | Retirer photo → retour aux initiales | ⏳ blocked on T6 |

---

## Fixes applied this session (all committed)

### Committed in `41968ca`:
- `apps/web/src/app/api/storage/upload-url/route.ts` — NEW (was untracked → 404 on Vercel)
- `apps/web/src/app/api/photo/route.ts` — NEW (was untracked)
- `apps/web/src/components/coach/PhotoUpload.tsx` — calls `/api/storage/upload-url` (Next.js route)
- `apps/web/src/components/coach/FileUploadRow.tsx` — same fix
- `apps/web/src/actions/coach-identity.ts` — bypasses backend API, calls Supabase directly
- `apps/mobile/metro.config.js` — `resolveRequest` overrides react/react-native to fix 19.2.6 vs 19.1.0 mismatch
- `backend/api/src/routes/ai.ts` — `/ai/vision/nutrition` accepts `storage_path` (new) + `image` (legacy)
- `supabase/migrations/039_avatars_delete_policy.sql` — DELETE policy + UPDATE WITH CHECK on avatars bucket

### Committed in `ce9d252`:
- `backend/api/package.json` — `cross-env PORT=8080` for Windows CMD compat
- `apps/mobile/.env.local` — `EXPO_PUBLIC_API_URL=http://192.168.1.138:8080`

### Committed in `bb04e51` (WIP):
- `apps/mobile/app/(app)/profile/avatar.tsx` — FormData instead of fetch().blob() (Android fix)
- `backend/api/src/routes/ai.ts` — vision route uses user Bearer token for scan-photos download (RLS fix)
- `plugins/nutrition/src/screens/LogMealScreen.tsx` — useEffect+rAF to show scanImage before analyzeImage

---

## Active problem (not resolved)

**Nutrition scan image preview doesn't appear** after taking photo. Appears only after navigating to barcode tab and back.

Root cause attempts:
1. ✗ Moved `setScanImage` call to end of `pickImage` (useEffect approach)
2. ✗ Added `requestAnimationFrame` in useEffect before `analyzeImage`

Next things to try:
- Check if `analyzeImage` Step 1 (`fetch upload-url`) is failing and resetting state
- Try `setTimeout(500)` instead of rAF — maybe it's a timing issue
- Check if `showAlert` from the Step 1 failure is obscuring the image
- Verify backend is actually running at `192.168.1.138:8080`

---

## Environment

- Backend: `cd backend/api && npm run dev` → port 8080
- Mobile: `cd apps/mobile && npx expo start --clear`
- `apps/mobile/.env.local`: `EXPO_PUBLIC_API_URL=http://192.168.1.138:8080`
- Supabase prod: `slkobhavpwsubnsmuhya` — migrations 038 + 039 applied
- User `anatholyb@gmail.com` has 9999 AI credits

---

## Resume

```
git status  # should be clean
# Continue UAT — start with nutrition scan image preview bug
```
