// Phase 28 plan 01 — PDF rasterization stub (Wave 0)
// Implement body in Wave 1 after backend/api/src/coach/imports/parse/pdf.ts exists.
import { describe, it } from 'vitest';

describe('parse/pdf', () => {
  it.todo('rasterize: rasterizePdf returns array of 30 base64 PNG strings for 30-page PDF'); // IMPORT-07
});
