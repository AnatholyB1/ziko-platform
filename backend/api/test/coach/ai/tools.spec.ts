import { describe, it } from 'vitest'

describe('analyzeClientExecutor / generateCoachingProgramExecutor', () => {
  it.todo('rejects unlinked client')
  it.todo('creates program with is_template=false')
})
