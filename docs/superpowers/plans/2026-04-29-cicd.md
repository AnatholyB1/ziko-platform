# CI/CD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Set up two GitHub Actions workflows — quality gate + auto-deploy on push to `main`, and EAS build/submit on git tags.

**Architecture:** Two YAML workflow files in `.github/workflows/`. The `ci.yml` runs on every push to `main`: quality checks, then parallel Vercel deploy + Supabase migrations (conditionnel). The `release.yml` runs on tags `v*` or `beta*`: quality check, then EAS build + Play Store submit with different tracks depending on the tag prefix.

**Tech Stack:** GitHub Actions, Vercel CLI, Supabase CLI, EAS CLI (`expo/expo-github-action@v8`), Turborepo (existing `type-check` + `lint` scripts)

---

## Files

| Action | Path |
|--------|------|
| Create | `.github/workflows/ci.yml` |
| Create | `.github/workflows/release.yml` |
| Modify | `apps/mobile/eas.json` |

---

## Task 1: Update eas.json — deux profils submit ✅

**Files:**
- Modify: `apps/mobile/eas.json`

- [x] **Step 1: Modifier `apps/mobile/eas.json`**
- [x] **Step 2: Vérifier le JSON**
- [x] **Step 3: Commit** — `fix(eas): internal profile uses AAB instead of APK for Play Store submit`

---

## Task 2: Créer `.github/workflows/ci.yml` ✅

**Files:**
- Create: `.github/workflows/ci.yml`

- [x] **Step 1: Créer le dossier workflows**
- [x] **Step 2: Créer `.github/workflows/ci.yml`**
- [x] **Step 3: Valider la syntaxe YAML**
- [x] **Step 4: Commit** — `ci: add CI workflow — quality gate + Vercel deploy + Supabase migrations`

---

## Task 3: Créer `.github/workflows/release.yml` ✅

**Files:**
- Create: `.github/workflows/release.yml`

- [x] **Step 1: Créer `.github/workflows/release.yml`**
- [x] **Step 2: Commit** — `ci: add release workflow — EAS build/submit on v* and beta* tags`

---

## Task 4: Configurer les secrets GitHub ⚠️ MANUEL

**Files:** aucun fichier à modifier — configuration dans GitHub UI.

Ces étapes sont manuelles dans l'interface GitHub : **Settings → Secrets and variables → Actions → New repository secret**

- [ ] **Step 1: Obtenir `VERCEL_TOKEN`**

1. Aller sur vercel.com → Account Settings → Tokens
2. Créer un token avec scope "Full Account"
3. Ajouter comme secret GitHub : `VERCEL_TOKEN`

- [ ] **Step 2: Obtenir `VERCEL_ORG_ID` et `VERCEL_PROJECT_ID`**

Dans le terminal, depuis la racine du projet :
```bash
cd backend/api
vercel link
cat .vercel/project.json
```

Output exemple :
```json
{"orgId":"team_xxxx","projectId":"prj_yyyy"}
```

Ajouter :
- Secret `VERCEL_ORG_ID` = valeur de `orgId`
- Secret `VERCEL_PROJECT_ID` = valeur de `projectId`

Note : `.vercel/` est dans `.gitignore`, ne pas committer.

- [ ] **Step 3: Obtenir `SUPABASE_ACCESS_TOKEN` et `SUPABASE_PROJECT_ID`**

- `SUPABASE_ACCESS_TOKEN` : supabase.com → Account → Access Tokens → Generate new token
- `SUPABASE_PROJECT_ID` : Dashboard → ton projet → Settings → General → "Reference ID" (format : `abcdefghijklmnop`)

Ajouter les deux comme secrets GitHub.

- [ ] **Step 4: Obtenir `EXPO_TOKEN`**

1. Aller sur expo.dev → Account Settings → Access Tokens
2. Créer un token
3. Ajouter comme secret GitHub : `EXPO_TOKEN`

---

## Task 5: Tester le workflow `ci.yml` (après Task 4)

- [ ] **Step 1: Faire un commit de test sur `main`**

```bash
rtk git commit --allow-empty -m "ci: test CI workflow trigger"
rtk git push origin main
```

- [ ] **Step 2: Vérifier le workflow sur GitHub**

Aller sur : **github.com/<owner>/<repo>/actions**

Expected :
- Workflow "CI" déclenché
- Job `quality` : vert (type-check + lint passent)
- Job `deploy-backend` : vert (déploiement Vercel)
- Job `migrate-supabase` : vert (skipped si pas de migration, ou applied si changement détecté)

- [ ] **Step 3: En cas d'échec `type-check`**

```bash
npm run type-check 2>&1 | head -50
```

Corriger les erreurs TypeScript localement, committer, repousser.

- [ ] **Step 4: En cas d'échec Vercel**

Vérifier les secrets `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID` dans GitHub Settings → Secrets.

---

## Task 6: Tester le workflow `release.yml` avec un tag beta (après Task 4)

- [ ] **Step 1: Pousser un tag `beta*`**

```bash
rtk git tag beta0.0.1-test
rtk git push origin beta0.0.1-test
```

- [ ] **Step 2: Vérifier sur GitHub Actions**

Expected :
- Workflow "Release" déclenché
- Job `quality` : vert
- Job `eas-build-submit` :
  - Steps `v*` : skipped
  - Steps `beta*` : EAS build preview Android déclenché + submit internal

- [ ] **Step 3: Vérifier sur EAS dashboard**

Aller sur expo.dev → ton projet → Builds  
Un build Android en `preview` devrait apparaître.

- [ ] **Step 4: Nettoyer le tag de test**

```bash
rtk git tag -d beta0.0.1-test
rtk git push origin --delete beta0.0.1-test
```

---

## Résumé des secrets requis

| Secret | Valeur |
|--------|--------|
| `VERCEL_TOKEN` | Token Vercel (Account Settings → Tokens) |
| `VERCEL_ORG_ID` | `orgId` dans `backend/api/.vercel/project.json` |
| `VERCEL_PROJECT_ID` | `projectId` dans `backend/api/.vercel/project.json` |
| `SUPABASE_ACCESS_TOKEN` | Token Supabase (Account → Access Tokens) |
| `SUPABASE_PROJECT_ID` | Reference ID du projet Supabase |
| `EXPO_TOKEN` | Token EAS (expo.dev → Account → Access Tokens) |
