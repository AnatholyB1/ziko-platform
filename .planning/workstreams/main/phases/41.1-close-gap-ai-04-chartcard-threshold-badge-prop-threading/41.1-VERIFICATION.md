---
phase: 41.1-close-gap-ai-04-chartcard-threshold-badge-prop-threading
verified: 2026-05-30T10:00:00Z
status: human_needed
score: 3/3 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Ouvrir le dashboard d'un client avec un seuil configuré sur une métrique (ex. rpe_avg > 8). Sélectionner le sport correspondant dans l'onglet Sport."
    expected: "Le badge pill orange ou rouge apparaît dans la barre de titre du ChartCard dont le metricKey correspond à la métrique seuillée."
    why_human: "Le rendu du badge dépend de données live (crossedThresholds non vide renvoyé par useInsights). Impossible de tester sans backend actif et données réelles."
---

# Phase 41.1 : Rapport de vérification — Prop Threading ChartCard Threshold Badge

**Objectif de la phase :** Fermer le gap AI-04 — thread le prop `crossedThresholds` depuis `page.tsx` → 5 dashboards sport → `ChartCard` afin que le badge pill orange/rouge s'affiche quand un seuil est franchi.
**Vérifié :** 2026-05-30
**Statut :** human_needed
**Re-vérification :** Non — vérification initiale

---

## Atteinte de l'objectif

### Vérités observables

| # | Vérité | Statut | Preuves |
|---|--------|--------|---------|
| 1 | Quand `crossedThresholds` contient une entrée dont `metric_key` correspond au `metricKey` d'un ChartCard, le badge pill orange/rouge s'affiche dans la barre de titre de cette carte | ✓ VERIFIED | `ChartCard.tsx` lignes 21–45 : logique `matchingAlert = crossedThresholds.find(t => t.metric_key === metricKey)` + rendu conditionnel du badge orange/rouge selon delta. Prop `metricKey` et `crossedThresholds` présents sur chaque appel ChartCard dans les 5 dashboards. Rendu effectif à confirmer en live (voir section Vérification humaine). |
| 2 | Les 5 dashboards sport acceptent `crossedThresholds` comme prop optionnel et le passent à chaque appel ChartCard | ✓ VERIFIED | `crossedThresholds?:` déclaré dans l'interface de chaque dashboard (lignes 73 dans PowerliftingDashboard, HyroxDashboard, RunningDashboard, BodybuildingDashboard, WeightLossDashboard). Passé à `ChartCard` via `crossedThresholds={crossedThresholds}` sur tous les call sites (1 dans le map pour Powerlifting/Hyrox/Running ; 3 inline pour Bodybuilding/WeightLoss). |
| 3 | `dashboard/page.tsx` passe `insights?.crossedThresholds` aux 5 composants dashboard sport (pas seulement à AlertesModal) | ✓ VERIFIED | 6 occurrences de `crossedThresholds={insights?.crossedThresholds}` dans `page.tsx` (lignes 213, 227, 239, 252, 265 pour les 5 dashboards + ligne 362 pour AlertesModal). Compté par grep : résultat = 6. |

**Score : 3/3 vérités confirmées**

---

### Artefacts requis

| Artefact | Attendu | Statut | Détails |
|----------|---------|--------|---------|
| `apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx` | `crossedThresholds?` dans l'interface + `metricKey` + `crossedThresholds` sur ChartCard | ✓ VERIFIED | Ligne 73 : interface OK. Ligne 370 : `metricKey={card.chartKey} crossedThresholds={crossedThresholds}` dans le map. 1 occurrence de `metricKey=`. |
| `apps/web/src/components/coach/dashboard/HyroxDashboard.tsx` | Idem | ✓ VERIFIED | Ligne 70 : interface OK. Ligne 244 : props passés dans le map. 1 occurrence de `metricKey=`. |
| `apps/web/src/components/coach/dashboard/RunningDashboard.tsx` | Idem | ✓ VERIFIED | Ligne 72 : interface OK. Ligne 328 : props passés dans le map. 1 occurrence de `metricKey=`. |
| `apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx` | `crossedThresholds?` + `metricKey` + `crossedThresholds` sur 3 appels ChartCard inline | ✓ VERIFIED | Ligne 74 : interface OK. Lignes 141, 177, 211 : `metricKey="volume_total"`, `metricKey="series_hebdo"`, `metricKey="poids_corps"`. 3 occurrences de `metricKey=`. |
| `apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx` | Idem (3 appels inline) | ✓ VERIFIED | Ligne 73 : interface OK. Lignes 148, 208, 249 : `metricKey="poids_actuel"`, `metricKey="calories_avg"`, `metricKey="series_total"`. 3 occurrences de `metricKey=`. |
| `apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx` | `crossedThresholds={insights?.crossedThresholds}` sur les 5 dashboards sport | ✓ VERIFIED | 6 occurrences confirmées (5 dashboards + AlertesModal). AlertesModal préservé (ligne 362). |

---

### Vérification des liens clés (wiring)

| De | Vers | Via | Statut | Détails |
|----|------|-----|--------|---------|
| `dashboard/page.tsx` | 5 dashboards sport | `crossedThresholds={insights?.crossedThresholds}` sur chaque élément JSX | ✓ WIRED | 5 props confirmés aux lignes 213, 227, 239, 252, 265. |
| Chaque dashboard sport | `ChartCard` | `metricKey={card.chartKey}` ou `metricKey="..."` + `crossedThresholds={crossedThresholds}` | ✓ WIRED | Powerlifting/Hyrox/Running : 1 call site dans le map. Bodybuilding/WeightLoss : 3 call sites inline chacun. Tous vérifiés dans le code source. |
| `ChartCard` | Badge pill | `crossedThresholds.find(t => t.metric_key === metricKey)` → rendu conditionnel | ✓ WIRED | Logique de correspondance existante et fonctionnelle aux lignes 21–51 de `ChartCard.tsx`. |

---

### Trace de flux de données (Niveau 4)

| Artefact | Variable de données | Source | Produit des données réelles | Statut |
|----------|--------------------|---------|-----------------------------|--------|
| `ChartCard.tsx` | `crossedThresholds` prop | Injecté depuis le dashboard parent via `insights?.crossedThresholds` | Dépend de `useInsights` hook → appel API backend | ✓ FLOWING (le câblage prop est complet ; le flux réel dépend de données backend live — voir Vérification humaine) |

---

### Vérifications comportementales (Spot-checks)

| Comportement | Commande | Résultat | Statut |
|--------------|---------|---------|--------|
| Comptage des props dans page.tsx | `grep -c "crossedThresholds={insights"` | 6 | ✓ PASS |
| Prop `metricKey` dans Powerlifting | `grep -c "metricKey="` | 1 | ✓ PASS |
| Prop `metricKey` dans Bodybuilding | `grep -c "metricKey="` | 3 | ✓ PASS |
| Prop `metricKey` dans WeightLoss | `grep -c "metricKey="` | 3 | ✓ PASS |
| Absence de marqueurs de dette (TBD/FIXME/XXX) | grep dans `dashboard/` | 0 correspondance | ✓ PASS |

---

### Couverture des exigences

| Exigence | Plan source | Description | Statut | Preuves |
|----------|-------------|-------------|--------|---------|
| AI-04 | 41.1-01-PLAN.md | Le coach peut configurer des seuils numériques par métrique ; l'IA signale au coach quand un client franchit un seuil | ✓ SATISFIED | Badge pill rendu par `ChartCard.tsx` quand `crossedThresholds.find(t => t.metric_key === metricKey)` retourne une entrée. Prop chain complète page → dashboards → ChartCard. |

---

### Anti-patterns détectés

Aucun anti-pattern trouvé dans les 6 fichiers modifiés :
- Zéro marqueur `TBD`, `FIXME`, `XXX`
- Aucun stub (`return null`, `return []`, implémentation vide)
- Aucune prop hardcodée à une valeur vide

---

### Vérification humaine requise

#### 1. Rendu du badge pill sur dashboard live

**Test :** Ouvrir la page `/coach/clients/[id]/dashboard` avec un client ayant au moins un seuil configuré dans `coach_metric_thresholds`. Sélectionner le sport correspondant. Vérifier que `useInsights` retourne `crossedThresholds` non vide.

**Attendu :** Le badge pill orange (delta ≤ 20 %) ou rouge (delta > 20 %) apparaît dans la barre de titre du ChartCard dont le `metricKey` correspond à la `metric_key` de l'alerte. Les autres cartes n'affichent pas de badge.

**Raison (humain requis) :** Le rendu du badge dépend de `insights?.crossedThresholds` non nul, qui nécessite un backend actif avec des seuils configurés et des données client réelles franchissant ces seuils. Impossible à vérifier par grep ou analyse statique.

---

### Résumé

Fermeture du gap AI-04 confirmée par analyse statique complète. La chaîne prop `useInsights → page.tsx → 5 dashboards → ChartCard` est intégralement câblée. `ChartCard.tsx` contient la logique de correspondance et de rendu du badge depuis la phase 41 ; cette phase ajoute uniquement les props manquants sans introduire de nouvelle logique.

La seule vérification restante est comportementale/visuelle avec des données live — le badge ne peut pas être affiché sans un seuil franchi dans la base de données.

---

_Vérifié : 2026-05-30_
_Vérificateur : Claude (gsd-verifier)_
