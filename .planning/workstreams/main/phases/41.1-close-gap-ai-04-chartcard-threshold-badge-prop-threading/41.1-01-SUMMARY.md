---
phase: 41.1-close-gap-ai-04-chartcard-threshold-badge-prop-threading
plan: "01"
subsystem: web/coach-dashboard
tags: [gap-closure, prop-threading, chartcard, thresholds, AI-04]
dependency_graph:
  requires: []
  provides: [AI-04]
  affects: [coach-dashboard]
tech_stack:
  added: []
  patterns: [prop-threading, optional-prop]
key_files:
  created: []
  modified:
    - apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx
    - apps/web/src/components/coach/dashboard/HyroxDashboard.tsx
    - apps/web/src/components/coach/dashboard/RunningDashboard.tsx
    - apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx
    - apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx
decisions:
  - Inline type used for crossedThresholds prop (no import of ThresholdAlert) to match ChartCard.tsx pattern
metrics:
  duration: "8 minutes"
  completed: "2026-05-30"
  tasks_completed: 3
  files_modified: 6
---

# Phase 41.1 Plan 01: ChartCard Threshold Badge Prop Threading Summary

**One-liner:** Thread `crossedThresholds` from `useInsights` through `dashboard/page.tsx` → 5 sport dashboards → each `ChartCard` so the orange/red pill badge activates when a coach-configured metric threshold is crossed.

---

## What Was Built

Closed gap AI-04: the ChartCard threshold badge code already existed and worked, but `crossedThresholds` was never passed beyond `AlertesModal`. This plan adds the missing prop chain in 6 files with zero new logic.

**Prop chain completed:**
```
insights?.crossedThresholds (useInsights hook)
  └─> dashboard/page.tsx (5 sport dashboard JSX elements)
        ├─> PowerliftingDashboard → ChartCard (via CHART_CARDS map)
        ├─> HyroxDashboard → ChartCard (via CHART_CARDS map)
        ├─> RunningDashboard → ChartCard (via CHART_CARDS map)
        ├─> BodybuildingDashboard → 3 inline ChartCard calls
        └─> WeightLossDashboard → 3 inline ChartCard calls
```

---

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Add crossedThresholds to Powerlifting, Hyrox, Running (CHART_CARDS map pattern) | ab9d983 | 3 files |
| 2 | Add crossedThresholds to Bodybuilding, WeightLoss (inline ChartCard pattern) | e611bf5 | 2 files |
| 3 | Thread crossedThresholds from page.tsx to all 5 sport dashboard JSX elements | 8816579 | 1 file |

---

## Verification Results

- TypeScript: 0 errors in the 6 modified files (1 pre-existing unrelated error in `VocalReview.test.tsx`)
- `grep -c "crossedThresholds={insights"` in `page.tsx` → **6** (5 sport dashboards + 1 AlertesModal)
- All 5 dashboard files contain `crossedThresholds?` in their props interface
- `metricKey=` count in PowerliftingDashboard → **1** (one ChartCard in map)
- `metricKey=` count in BodybuildingDashboard → **3** (three inline ChartCard calls)

---

## Deviations from Plan

None — plan executed exactly as written.

---

## Known Stubs

None. The badge rendering logic already existed in ChartCard.tsx. This plan only threads the props — no stub or placeholder introduced.

---

## Threat Flags

None. Pure prop threading — no new network endpoints, auth paths, or schema changes.

---

## Self-Check: PASSED

- [x] PowerliftingDashboard.tsx modified and committed (ab9d983)
- [x] HyroxDashboard.tsx modified and committed (ab9d983)
- [x] RunningDashboard.tsx modified and committed (ab9d983)
- [x] BodybuildingDashboard.tsx modified and committed (e611bf5)
- [x] WeightLossDashboard.tsx modified and committed (e611bf5)
- [x] dashboard/page.tsx modified and committed (8816579)
- [x] 6 occurrences of `crossedThresholds={insights` in page.tsx confirmed
- [x] 5 dashboard files contain `crossedThresholds?` confirmed
- [x] TypeScript passes (0 errors in modified files)
