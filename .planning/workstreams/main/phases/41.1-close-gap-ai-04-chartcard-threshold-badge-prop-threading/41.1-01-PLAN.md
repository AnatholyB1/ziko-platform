---
phase: 41.1-close-gap-ai-04-chartcard-threshold-badge-prop-threading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx
  - apps/web/src/components/coach/dashboard/HyroxDashboard.tsx
  - apps/web/src/components/coach/dashboard/RunningDashboard.tsx
  - apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx
  - apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx
autonomous: true
gap_closure: true
requirements:
  - AI-04
must_haves:
  truths:
    - "When crossedThresholds contains an entry whose metric_key matches a ChartCard's metricKey, the orange or red pill badge renders in that card's title bar"
    - "All 5 sport dashboards accept crossedThresholds as an optional prop and pass it down to each ChartCard call site"
    - "dashboard/page.tsx passes insights?.crossedThresholds to all 5 sport dashboard components (not only to AlertesModal)"
  artifacts:
    - path: "apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx"
      provides: "crossedThresholds prop in interface + metricKey + crossedThresholds on each ChartCard"
      contains: "crossedThresholds?"
    - path: "apps/web/src/components/coach/dashboard/HyroxDashboard.tsx"
      provides: "same as Powerlifting"
      contains: "crossedThresholds?"
    - path: "apps/web/src/components/coach/dashboard/RunningDashboard.tsx"
      provides: "same as Powerlifting"
      contains: "crossedThresholds?"
    - path: "apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx"
      provides: "crossedThresholds prop + metricKey + crossedThresholds on 3 inline ChartCard calls"
      contains: "crossedThresholds?"
    - path: "apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx"
      provides: "crossedThresholds prop + metricKey + crossedThresholds on 3 inline ChartCard calls"
      contains: "crossedThresholds?"
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx"
      provides: "crossedThresholds threaded to all 5 sport dashboards"
      contains: "crossedThresholds={insights?.crossedThresholds}"
  key_links:
    - from: "dashboard/page.tsx"
      to: "PowerliftingDashboard / HyroxDashboard / RunningDashboard / BodybuildingDashboard / WeightLossDashboard"
      via: "crossedThresholds={insights?.crossedThresholds} prop on each sport dashboard JSX element"
      pattern: "crossedThresholds=\\{insights\\?\\."
    - from: "each sport dashboard"
      to: "ChartCard"
      via: "metricKey={card.chartKey} crossedThresholds={crossedThresholds} on each ChartCard call"
      pattern: "metricKey=\\{card\\.chartKey\\}"
---

<objective>
Close AI-04 gap: the ChartCard threshold badge is permanently disabled because crossedThresholds
is never passed beyond AlertesModal. The badge code exists and works — the only missing piece is
prop threading through the page → sport dashboard → ChartCard chain.

Purpose: Make the orange/red pill badge visible in ChartCard title bars when a coach-configured
metric threshold is crossed by the client's data.

Output: 6 files modified — 5 sport dashboards gain a crossedThresholds prop and thread it to each
ChartCard, and dashboard/page.tsx passes crossedThresholds to all 5 sport dashboards alongside the
existing chartInsights prop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/main/ROADMAP.md
@.planning/workstreams/main/phases/41-ai-context-injection/41-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add crossedThresholds prop to the three CHART_CARDS-map dashboards (Powerlifting, Hyrox, Running)</name>

  <read_first>
    - apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx (full file — note interface at line 63–72, CHART_CARDS map render at line 362–372)
    - apps/web/src/components/coach/dashboard/HyroxDashboard.tsx (full file — note interface at line 51–68, CHART_CARDS map render at line 234–244)
    - apps/web/src/components/coach/dashboard/RunningDashboard.tsx (full file — note interface at line 53–70, CHART_CARDS map render at line 320–328)
    - apps/web/src/components/coach/dashboard/ChartCard.tsx (lines 1–18 — confirm crossedThresholds prop is Array<{ metric_key, operator, threshold_value, current_value }> and metricKey is string)
  </read_first>

  <files>
    apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx,
    apps/web/src/components/coach/dashboard/HyroxDashboard.tsx,
    apps/web/src/components/coach/dashboard/RunningDashboard.tsx
  </files>

  <action>
    Apply the same change to all three dashboards (PowerliftingDashboard, HyroxDashboard, RunningDashboard).
    Each file uses the same CHART_CARDS map pattern — the fix is identical in structure.

    STEP A — Extend the props interface (the inline type object after the component name):
    Add `crossedThresholds?: Array<{ metric_key: string; operator: string; threshold_value: number; current_value: number }>` as a new optional field after `chartInsights?: Record<string, string>`. Do NOT import ThresholdAlert — ChartCard.tsx uses an inline type too; match it exactly.

    STEP B — Destructure the new prop:
    Add `crossedThresholds` to the destructured parameter list alongside `chartInsights`.

    STEP C — Update the CHART_CARDS map render call:
    Each dashboard has a single ChartCard call inside the map:
    - PowerliftingDashboard line ~368: `<ChartCard title={card.title} aiInsight={chartInsights?.[card.chartKey]}>`
    - HyroxDashboard line ~242: `<ChartCard title={card.title} aiInsight={chartInsights?.[card.chartKey]}>`
    - RunningDashboard line ~326: `<ChartCard title={card.title} aiInsight={chartInsights?.[card.chartKey]}>`

    Change each of these to:
    `<ChartCard title={card.title} aiInsight={chartInsights?.[card.chartKey]} metricKey={card.chartKey} crossedThresholds={crossedThresholds}>`

    No other lines change. Do not touch chart data, compare logic, or the CHART_CARDS array definitions.
  </action>

  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit --project apps/web/tsconfig.json 2>&1 | grep -i "error" | grep -E "Powerlifting|Hyrox|Running" | head -20</automated>
  </verify>

  <done>
    - PowerliftingDashboard, HyroxDashboard, and RunningDashboard each declare `crossedThresholds?` in their props interface
    - Each destructures `crossedThresholds` from props
    - Each CHART_CARDS map render passes `metricKey={card.chartKey}` and `crossedThresholds={crossedThresholds}` to ChartCard
    - TypeScript reports no errors in these three files
  </done>
</task>

<task type="auto">
  <name>Task 2: Add crossedThresholds prop to the two inline-ChartCard dashboards (Bodybuilding, WeightLoss)</name>

  <read_first>
    - apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx (full file — inline ChartCard calls at lines 139, 175, 209 with chartKeys volume_total, series_hebdo, poids_corps)
    - apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx (full file — inline ChartCard calls at lines 146, 206, 247 with chartKeys poids_actuel, calories_avg, series_total)
  </read_first>

  <files>
    apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx,
    apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx
  </files>

  <action>
    Apply the same change to BodybuildingDashboard and WeightLossDashboard.
    These two dashboards use inline ChartCard calls (not a map), each with a hardcoded chartKey string.

    STEP A — Extend the props interface (same as Task 1):
    Add `crossedThresholds?: Array<{ metric_key: string; operator: string; threshold_value: number; current_value: number }>` after `chartInsights?: Record<string, string>`.

    STEP B — Destructure the new prop alongside chartInsights.

    STEP C — Update each inline ChartCard call:

    BodybuildingDashboard has 3 ChartCard calls:
    1. `<ChartCard title="Volume par Groupe Musculaire" aiInsight={chartInsights?.['volume_total']}>` →
       add `metricKey="volume_total"` and `crossedThresholds={crossedThresholds}`
    2. `<ChartCard title="Surcharge Progressive" aiInsight={chartInsights?.['series_hebdo']}>` →
       add `metricKey="series_hebdo"` and `crossedThresholds={crossedThresholds}`
    3. `<ChartCard title="Poids Corporel" aiInsight={chartInsights?.['poids_corps']}>` →
       add `metricKey="poids_corps"` and `crossedThresholds={crossedThresholds}`

    WeightLossDashboard has 3 ChartCard calls:
    1. `<ChartCard title="Evolution du Poids" aiInsight={chartInsights?.['poids_actuel']}>` →
       add `metricKey="poids_actuel"` and `crossedThresholds={crossedThresholds}`
    2. `<ChartCard title="Conformite Calorique" aiInsight={chartInsights?.['calories_avg']}>` →
       add `metricKey="calories_avg"` and `crossedThresholds={crossedThresholds}`
    3. `<ChartCard title="Progression de Charge" aiInsight={chartInsights?.['series_total']}>` →
       add `metricKey="series_total"` and `crossedThresholds={crossedThresholds}`

    No other lines change. The metricKey string values must match the exact strings used in the
    aiInsight lookup (chartInsights?.['key']) — these are the same strings that the backend uses
    as keys in chartInsights and as metric_key values in crossedThresholds entries.
  </action>

  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit --project apps/web/tsconfig.json 2>&1 | grep -i "error" | grep -E "Bodybuilding|WeightLoss" | head -20</automated>
  </verify>

  <done>
    - BodybuildingDashboard and WeightLossDashboard each declare `crossedThresholds?` in their props interface
    - Each destructures `crossedThresholds` from props
    - All 6 inline ChartCard calls (3 per dashboard) receive `metricKey` (exact string literal) and `crossedThresholds={crossedThresholds}`
    - TypeScript reports no errors in these two files
  </done>
</task>

<task type="auto">
  <name>Task 3: Thread crossedThresholds from page.tsx to all 5 sport dashboard JSX elements</name>

  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx (lines 195–265 — the five sport dashboard JSX blocks; line 357 — existing AlertesModal crossedThresholds prop)
  </read_first>

  <files>
    apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx
  </files>

  <action>
    Add `crossedThresholds={insights?.crossedThresholds}` to each of the 5 sport dashboard JSX
    elements. Each block already has `chartInsights={insights?.chartInsights}` as the last prop —
    add `crossedThresholds={insights?.crossedThresholds}` immediately after it on a new line.

    The five locations (identified by their opening tag and last existing prop line):

    1. PowerliftingDashboard block (line ~204):
       After `chartInsights={insights?.chartInsights}`, add `crossedThresholds={insights?.crossedThresholds}`

    2. HyroxDashboard block (line ~216):
       After `chartInsights={insights?.chartInsights}`, add `crossedThresholds={insights?.crossedThresholds}`

    3. RunningDashboard block (line ~228):
       After `chartInsights={insights?.chartInsights}`, add `crossedThresholds={insights?.crossedThresholds}`

    4. BodybuildingDashboard block (line ~240):
       After `chartInsights={insights?.chartInsights}`, add `crossedThresholds={insights?.crossedThresholds}`

    5. WeightLossDashboard block (line ~252):
       After `chartInsights={insights?.chartInsights}`, add `crossedThresholds={insights?.crossedThresholds}`

    The existing `crossedThresholds={insights?.crossedThresholds}` on AlertesModal (line ~357) must
    remain untouched. This task only adds the same prop to the sport dashboard elements.

    No other changes to page.tsx. The `insights` variable (from useInsights) already contains
    `crossedThresholds` — no new state, hooks, or imports are needed.
  </action>

  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit --project apps/web/tsconfig.json 2>&1 | grep -i "error" | head -30</automated>
  </verify>

  <done>
    - All 5 sport dashboard JSX blocks in page.tsx have `crossedThresholds={insights?.crossedThresholds}` as a prop
    - The AlertesModal crossedThresholds prop at line ~357 is unchanged
    - `grep -c "crossedThresholds={insights?.crossedThresholds}" apps/web/src/app/\[locale\]/\(coach\)/coach/clients/\[id\]/dashboard/page.tsx` returns 6 (1 AlertesModal + 5 sport dashboards)
    - TypeScript reports no errors in page.tsx
    - Full type-check passes: `rtk tsc --noEmit --project apps/web/tsconfig.json` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| insights API → ChartCard | crossedThresholds values originate from backend evaluation against coach_metric_thresholds table; frontend only renders, does not evaluate thresholds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-01 | Information Disclosure | ChartCard badge label | accept | Label shows metric_key + operator + threshold_value — all coach-configured, no PII; coach already sees this in AlertesModal |
| T-41.1-SC | Tampering | npm installs | accept | No new packages installed in this plan — pure prop threading, no dependency changes |
</threat_model>

<verification>
Full verification sequence after all three tasks:

1. TypeScript: `cd /c/ziko-platform && rtk tsc --noEmit --project apps/web/tsconfig.json` exits 0

2. Prop count in page.tsx:
   `grep -c "crossedThresholds={insights" apps/web/src/app/\[locale\]/\(coach\)/coach/clients/\[id\]/dashboard/page.tsx`
   Expected: 6 (5 sport dashboards + 1 AlertesModal)

3. Interface coverage — each dashboard has the prop:
   `grep -l "crossedThresholds?" apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx apps/web/src/components/coach/dashboard/HyroxDashboard.tsx apps/web/src/components/coach/dashboard/RunningDashboard.tsx apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx`
   Expected: 5 files listed

4. ChartCard call sites receive metricKey:
   `grep -c "metricKey=" apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx`
   Expected: 1 (one ChartCard in map)
   `grep -c "metricKey=" apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx`
   Expected: 3 (three inline ChartCard calls)
</verification>

<success_criteria>
- All 5 sport dashboards declare `crossedThresholds?: Array<{ metric_key: string; operator: string; threshold_value: number; current_value: number }>` in their props interface
- All 5 sport dashboards pass `metricKey` and `crossedThresholds` to every ChartCard instance within them
- dashboard/page.tsx passes `crossedThresholds={insights?.crossedThresholds}` to all 5 sport dashboard JSX elements
- TypeScript full project check exits 0 (`tsc --noEmit`)
- ChartCard badge renders correctly when a threshold is crossed (human verification required — badge cannot be tested without live data)
</success_criteria>

<output>
Create `.planning/workstreams/main/phases/41.1-close-gap-ai-04-chartcard-threshold-badge-prop-threading/41.1-01-SUMMARY.md` when done
</output>
