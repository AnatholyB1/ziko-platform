---
phase: 38
plan: "038-01"
title: "TanStack Query install + QueryProvider setup"
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/components/coach/QueryProvider.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "useQuery from @tanstack/react-query resolves without throwing 'No QueryClient set' error inside coach client detail pages"
    - "QueryClient is created once per user session and persists across tab navigation within the client detail view"
    - "Navigating away from dashboard and back does not destroy the cache"
  artifacts:
    - path: "apps/web/src/components/coach/QueryProvider.tsx"
      provides: "'use client' QueryClientProvider wrapper with staleTime 60s"
      exports: ["QueryProvider"]
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx"
      provides: "Layout wrapping children in QueryProvider"
      contains: "QueryProvider"
  key_links:
    - from: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx"
      to: "apps/web/src/components/coach/QueryProvider.tsx"
      via: "import + JSX wrapper around {children}"
      pattern: "QueryProvider"
---

<objective>
Install @tanstack/react-query v5 in apps/web and wire a QueryClientProvider into the client detail layout so that useQuery calls in the dashboard page work without error.

Purpose: Every useQuery call on the dashboard requires a QueryClient in context. Without this, the dashboard crashes at runtime with "No QueryClient set, use QueryClientProvider to set one." This is Wave 1 because all dashboard components depend on it.

Output: @tanstack/react-query installed, QueryProvider component created, layout.tsx updated to wrap children.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/main/ROADMAP.md
@.planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-CONTEXT.md
@.planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-RESEARCH.md

<interfaces>
<!-- From apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx (verified) -->
<!-- Server component — async, imports getCachedCoachUser from server-only lib -->
<!-- Children render location (line 65): -->
<!--   <div className="flex-1 min-w-0" id="tab-panel"> -->
<!--     {children} -->
<!--   </div> -->
<!-- QueryProvider must wrap {children} here — NOT the entire layout JSX -->
<!-- Layout itself must remain a server component; only QueryProvider is 'use client' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-query in apps/web</name>
  <files>apps/web/package.json</files>
  <read_first>
    - apps/web/package.json — confirm @tanstack/react-query is absent before installing; check existing @tanstack/* entries to avoid version conflicts
  </read_first>
  <action>
    Run: npm install @tanstack/react-query@^5 --workspace=apps/web

    This installs @tanstack/react-query v5 (latest 5.x, currently 5.100.14) into apps/web/package.json dependencies. Do NOT install @tanstack/react-query-devtools — not needed.

    After install, verify the entry appears under "dependencies" in apps/web/package.json with a ^5.x.x version range. The workspace install command targets apps/web specifically — do not run npm install from the repo root without --workspace flag.
  </action>
  <verify>
    <automated>node -e "require('C:/ziko-platform/apps/web/node_modules/@tanstack/react-query/package.json')" 2>/dev/null && echo "OK" || (cd C:/ziko-platform && cat apps/web/package.json | grep react-query)</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json contains "@tanstack/react-query": "^5" under "dependencies"
    - No npm install error output
  </acceptance_criteria>
  <done>@tanstack/react-query v5 appears in apps/web/package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create QueryProvider component</name>
  <files>apps/web/src/components/coach/QueryProvider.tsx</files>
  <read_first>
    - apps/web/src/components/coach/ClientTabStrip.tsx — copy the 'use client' directive placement pattern (first line, before imports)
    - apps/web/src/components/coach/skeletons.tsx — confirm component export style (named export, no default)
    - .planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-PATTERNS.md §QueryProvider pattern
  </read_first>
  <action>
    Create apps/web/src/components/coach/QueryProvider.tsx as a new file.

    The file must:
    - Begin with 'use client'; as the first line (before imports)
    - Import QueryClient and QueryClientProvider from '@tanstack/react-query'
    - Import useState from 'react'
    - Export a named function QueryProvider({ children }: { children: React.ReactNode })
    - Create queryClient via useState(() => new QueryClient({ defaultOptions: { queries: { staleTime: 60 * 1000 } } })) — the factory function pattern prevents shared state across requests in SSR
    - Wrap children in QueryClientProvider client={queryClient}

    The staleTime of 60_000 ms means dashboard data cached for 1 minute. Switching dateRange or sport will still trigger a fresh fetch because the queryKey changes.

    Do NOT use a module-level QueryClient singleton — that would share cache across users in SSR contexts.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -i "QueryProvider" || echo "NO_TS_ERRORS_IN_QUERYPROVIDER"</automated>
  </verify>
  <acceptance_criteria>
    - File apps/web/src/components/coach/QueryProvider.tsx exists
    - First line is 'use client';
    - File contains export function QueryProvider
    - File contains useState(() => new QueryClient(
    - File contains staleTime: 60 * 1000 or staleTime: 60_000
    - File contains QueryClientProvider client={queryClient}
    - No TypeScript errors in the file
  </acceptance_criteria>
  <done>QueryProvider.tsx created with factory-function QueryClient and 60s staleTime</done>
</task>

<task type="auto">
  <name>Task 3: Wrap children in QueryProvider inside client detail layout</name>
  <files>apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx — read the full file to understand current structure before modifying; the children render at line 65 inside div#tab-panel
  </read_first>
  <action>
    Modify apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx:

    1. Add import at the top of the existing imports block: import { QueryProvider } from '@/components/coach/QueryProvider';

    2. Locate the div with id="tab-panel" (currently: <div className="flex-1 min-w-0" id="tab-panel">{children}</div>). Wrap only {children} with QueryProvider:

       <div className="flex-1 min-w-0" id="tab-panel">
         <QueryProvider>
           {children}
         </QueryProvider>
       </div>

    Do NOT add 'use client' to layout.tsx — it must remain a server component. QueryProvider is 'use client' internally and Next.js App Router handles the boundary correctly: a server component can import and render a client component as children.

    Do NOT move the import to the top of the file before other server-only imports — place it after the existing imports so the grouping is preserved.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -i "layout" | head -5 || echo "NO_TS_ERRORS_IN_LAYOUT"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx contains import { QueryProvider } from '@/components/coach/QueryProvider'
    - layout.tsx contains <QueryProvider> wrapping {children}
    - layout.tsx does NOT contain 'use client' directive
    - layout.tsx function signature still has async keyword (remains server component)
    - TypeScript compilation produces no new errors related to layout.tsx or QueryProvider.tsx
  </acceptance_criteria>
  <done>layout.tsx imports QueryProvider and wraps children — TanStack Query context available to all coach client detail tab pages</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → QueryClient cache | Cache lives in browser memory per user session; no cross-user risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-038-01 | Information Disclosure | QueryClient cache | mitigate | QueryClient created per session via useState factory — not a module singleton; no cache bleed between users |
| T-038-SC | Tampering | npm install @tanstack/react-query | mitigate | Package legitimacy audit in 038-RESEARCH.md §Package Legitimacy Audit — marked [OK]; no blocking checkpoint needed |
</threat_model>

<verification>
After all 3 tasks complete:
- apps/web/package.json has @tanstack/react-query ^5 under dependencies
- apps/web/src/components/coach/QueryProvider.tsx exists with 'use client', named export, useState factory pattern
- apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx has QueryProvider import and wraps children
- cd apps/web && npm run type-check exits 0 (no new TypeScript errors)
</verification>

<success_criteria>
- @tanstack/react-query v5 installed in apps/web
- QueryProvider component created with correct factory pattern
- layout.tsx updated — useQuery will resolve without "No QueryClient set" error on any coach client detail tab page
</success_criteria>

<output>
Create .planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-01-SUMMARY.md when done
</output>
