---
phase: 38
plan: "038-02"
title: "Dashboard route shell + tab + shared UI components"
wave: 2
depends_on: ["038-01"]
files_modified:
  - apps/web/src/components/coach/ClientTabStrip.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/loading.tsx
  - apps/web/src/components/coach/dashboard/DashboardControlBar.tsx
  - apps/web/src/components/coach/dashboard/ChartCard.tsx
  - apps/web/src/components/coach/dashboard/DashboardEmptyState.tsx
  - apps/web/src/components/coach/dashboard/DashboardLoadingState.tsx
  - apps/web/src/app/globals.css
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Dashboard tab appears as the first tab in ClientTabStrip and becomes active when navigating to /dashboard"
    - "Dashboard page renders DashboardControlBar with sport selector and date segmented control"
    - "When sport is null, DashboardEmptyState with prompt=true renders instead of charts"
    - "ChartCard wraps a ResponsiveContainer at height 240 with AI insight row at bottom"
    - "DashboardLoadingState renders 4 skeleton cards in a 2×2 grid with animate-pulse"
  artifacts:
    - path: "apps/web/src/components/coach/ClientTabStrip.tsx"
      provides: "Dashboard as first tab entry in TABS array"
      contains: "key: 'dashboard'"
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx"
      provides: "Client component page with sport/dateRange state"
      exports: ["default function DashboardPage"]
    - path: "apps/web/src/components/coach/dashboard/DashboardControlBar.tsx"
      provides: "Sport selector + date segmented control"
      exports: ["DashboardControlBar"]
    - path: "apps/web/src/components/coach/dashboard/ChartCard.tsx"
      provides: "Reusable chart wrapper with title, ResponsiveContainer h:240, AI row"
      exports: ["ChartCard"]
    - path: "apps/web/src/components/coach/dashboard/DashboardEmptyState.tsx"
      provides: "Empty state for null sport (prompt) and no-data scenarios"
      exports: ["DashboardEmptyState"]
    - path: "apps/web/src/components/coach/dashboard/DashboardLoadingState.tsx"
      provides: "4 skeleton cards in 2×2 grid"
      exports: ["DashboardLoadingState"]
    - path: "apps/web/src/app/globals.css"
      provides: "@keyframes fadeInUp for chart card mount animation"
      contains: "@keyframes fadeInUp"
  key_links:
    - from: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx"
      to: "apps/web/src/components/coach/dashboard/DashboardControlBar.tsx"
      via: "JSX props: sport, onSportChange, dateRange, onDateRangeChange"
      pattern: "DashboardControlBar"
---

<objective>
Build the complete route shell and shared UI components for the dashboard tab: ClientTabStrip modification, dashboard page route, loading.tsx, DashboardControlBar, ChartCard wrapper, DashboardEmptyState, DashboardLoadingState, and the fadeInUp CSS keyframe.

Purpose: These components form the structural skeleton that Plan 038-04 will wire to live chart data. They must exist and compile before PowerliftingDashboard can be built.

Output: 8 files created or modified, all TypeScript-clean, route accessible at /[locale]/coach/clients/[id]/dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-CONTEXT.md
@.planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-PATTERNS.md
@.planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md

<interfaces>
<!-- From apps/web/src/components/coach/ClientTabStrip.tsx (verified) -->
<!-- TABS array lines 5–14: array of { key: string, label: string } -->
<!-- Active detection: pathname.endsWith(`/${tab.key}`) -->
<!-- href pattern: `/${locale}/coach/clients/${id}/${tab.key}` -->
<!-- Adding { key: 'dashboard', label: 'Dashboard' } at position 0 auto-works with existing routing -->

<!-- From apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx (verified) -->
<!-- params is Promise<{ id: string }> in the layout (server component) -->
<!-- BUT dashboard/page.tsx is 'use client' — params.id is plain string (no await needed) -->
<!-- Layout provides px-8 py-6 via p-8 outer — dashboard page adds its own px-8 py-6 wrapper -->

<!-- From apps/web/src/app/globals.css (verified) -->
<!-- @theme block ends at line 20. Add @keyframes fadeInUp AFTER the closing } of @theme -->
<!-- File uses @import "tailwindcss" on line 1 — no @layer needed for keyframes -->

<!-- Design tokens confirmed in globals.css: -->
<!-- bg-surface-alt → --color-surface-alt: #F0EFE9 -->
<!-- text-muted → --color-muted: #6B6963 -->
<!-- border-border → --color-border: #E2E0DA -->
<!-- text-primary → --color-primary: #FF5C1A -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dashboard tab to ClientTabStrip + create route shell files + add fadeInUp keyframe</name>
  <files>
    apps/web/src/components/coach/ClientTabStrip.tsx
    apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx
    apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/loading.tsx
    apps/web/src/app/globals.css
  </files>
  <read_first>
    - apps/web/src/components/coach/ClientTabStrip.tsx — read full file; TABS array is at lines 5–14
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/sessions/page.tsx — reference for page structure; dashboard diverges to client component but use same params typing pattern
    - apps/web/src/app/globals.css — read full file; append fadeInUp after the closing brace of @theme block
    - .planning/workstreams/main/phases/038-dashboard-foundation-powerlifting/038-PATTERNS.md §DashboardPage pattern and §CSS Animation pattern
  </read_first>
  <action>
    STEP A — ClientTabStrip.tsx: Prepend { key: 'dashboard', label: 'Dashboard' } as position 0 of the TABS const array. All other entries shift down unchanged. No other modifications.

    STEP B — Create directory apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/ and create page.tsx:

    The page is a 'use client' component. Props: { params: { id: string } } — plain object, no await (client component in Next.js 15).

    Import: useState from 'react'. Import DashboardControlBar, PowerliftingDashboard (placeholder — forward reference, will be created in 038-04), DashboardEmptyState from their respective paths. Define type SportType = 'powerlifting' | 'hyrox' | 'running' | 'bodybuilding' | 'weightloss' in this file (or import from a shared types file if one exists — otherwise define locally).

    State: const [sport, setSport] = useState<SportType | null>(null) and const [dateRange, setDateRange] = useState<'week' | 'month' | '3m'>('month').

    Return JSX with className="px-8 py-6":
    - DashboardControlBar with sport, onSportChange={setSport}, dateRange, onDateRangeChange={setDateRange}
    - Conditional: if sport === null → DashboardEmptyState prompt={true}
    - Conditional: if sport === 'powerlifting' → PowerliftingDashboard clientId={params.id} sport={sport} dateRange={dateRange}

    For the PowerliftingDashboard import in this file: create a stub export in the dashboard component directory if 038-04 has not run yet. Alternatively, wrap the conditional render in a dynamic import or simply leave a TODO comment that 038-04 will fill it. The cleanest approach: import PowerliftingDashboard from './PowerliftingDashboard' and create a minimal stub component file apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx that renders null. Plan 038-04 will overwrite this stub with the real implementation.

    STEP C — Create loading.tsx in the same dashboard/ directory. Import DashboardLoadingState from '@/components/coach/dashboard/DashboardLoadingState'. Export default function DashboardLoading() returning <div className="px-8 py-6"><DashboardLoadingState /></div>.

    STEP D — globals.css: Append the following after the closing } of the @theme block (after line 20):

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(8px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    No @layer wrapper needed — top-level keyframes work in Tailwind v4 CSS.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "key: 'dashboard'" apps/web/src/components/coach/ClientTabStrip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ClientTabStrip.tsx TABS array first entry is { key: 'dashboard', label: 'Dashboard' }
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx exists with 'use client' first line
    - dashboard/page.tsx contains useState and sport === null conditional
    - dashboard/page.tsx contains sport === 'powerlifting' conditional rendering PowerliftingDashboard with sport={sport} prop
    - dashboard/loading.tsx exists and imports DashboardLoadingState
    - apps/web/src/app/globals.css contains @keyframes fadeInUp
    - globals.css fadeInUp has from { opacity: 0; transform: translateY(8px); } and to { opacity: 1; transform: translateY(0); }
  </acceptance_criteria>
  <done>Dashboard tab visible in tab strip, route shell created, CSS keyframe added</done>
</task>

<task type="auto">
  <name>Task 2: Create DashboardControlBar and ChartCard components</name>
  <files>
    apps/web/src/components/coach/dashboard/DashboardControlBar.tsx
    apps/web/src/components/coach/dashboard/ChartCard.tsx
  </files>
  <read_first>
    - apps/web/src/components/coach/ClientTabStrip.tsx — copy 'use client' pattern; active state CSS classes
    - apps/web/src/components/coach/ComparisonChart.tsx — copy card CSS classes (bg-white rounded-2xl border border-border p-6), ResponsiveContainer usage, CartesianGrid/Tooltip shared props
    - .planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-PATTERNS.md §DashboardControlBar pattern
    - .planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md §2 (Control Bar spec) and §3 (ChartCard anatomy)
  </read_first>
  <action>
    Create apps/web/src/components/coach/dashboard/DashboardControlBar.tsx:

    'use client'; first line. Props interface: { sport: SportType | null; onSportChange: (s: SportType | null) => void; dateRange: 'week' | 'month' | '3m'; onDateRangeChange: (d: 'week' | 'month' | '3m') => void; }

    Define SportType locally or import if already defined in page.tsx. Define DATE_OPTIONS array: [{ key: 'week', label: 'Semaine' }, { key: 'month', label: 'Mois' }, { key: '3m', label: '3 Mois' }].

    Container div: className="flex items-center justify-between mb-4"

    Left — sport <select>: value={sport ?? ''}, onChange handler converts e.target.value to SportType | null (empty string → null). Classes: "h-9 px-3 pr-8 text-sm font-medium bg-white border border-border rounded-lg appearance-none cursor-pointer focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-colors min-w-[200px]". Include inline style for chevron SVG background-image (data URI: a simple down-pointing chevron in #6B6963). Options: value="" Sélectionner un sport, value="powerlifting" Powerlifting, value="hyrox" Hyrox, value="running" Running / Cardio, value="bodybuilding" Bodybuilding, value="weightloss" Perte de poids.

    Right — date segmented control: outer div className="flex items-center gap-0 bg-surface-alt rounded-lg p-0.5 border border-border". Map DATE_OPTIONS to <button> elements. Active class: "bg-primary text-white shadow-sm rounded-md px-3 py-1.5 text-sm font-medium transition-colors". Inactive class: "text-muted hover:text-text rounded-md px-3 py-1.5 text-sm font-medium transition-colors". onClick: () => onDateRangeChange(key).

    Export as named export: export function DashboardControlBar.

    ---

    Create apps/web/src/components/coach/dashboard/ChartCard.tsx:

    'use client'; first line. Import ResponsiveContainer from 'recharts'.

    Props: { title: string; children: React.ReactNode; aiInsight?: string; style?: React.CSSProperties; className?: string; }

    Container: <div className={`bg-white rounded-2xl border border-border p-6 ${className ?? ''}`} style={style}>

    Inside: <h3 className="text-[15px] font-semibold text-text mb-4">{title}</h3>

    Then: <ResponsiveContainer width="100%" height={240}>{children}</ResponsiveContainer>

    Then AI row: <div className="border-t border-border mt-3 pt-3 flex items-center gap-2"><span className="text-base leading-none">🧠</span><span className="text-xs text-muted flex-1">{aiInsight ?? 'Analyse IA disponible en phase 41'}</span></div>

    Export as named export: export function ChartCard.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -E "DashboardControlBar|ChartCard" | head -10 || echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/coach/dashboard/DashboardControlBar.tsx exists with 'use client' first line
    - DashboardControlBar.tsx contains <select> with option value="" "Sélectionner un sport"
    - DashboardControlBar.tsx contains option value="powerlifting" Powerlifting
    - DashboardControlBar.tsx contains DATE_OPTIONS with week/month/3m entries
    - DashboardControlBar.tsx active date segment uses bg-primary text-white classes
    - apps/web/src/components/coach/dashboard/ChartCard.tsx exists with 'use client' first line
    - ChartCard.tsx contains ResponsiveContainer with height={240}
    - ChartCard.tsx contains AI insight row with 🧠 and aiInsight ?? 'Analyse IA disponible en phase 41'
    - ChartCard.tsx container uses bg-white rounded-2xl border border-border p-6 classes
    - No TypeScript errors in either file
  </acceptance_criteria>
  <done>DashboardControlBar and ChartCard created per UI-SPEC specs</done>
</task>

<task type="auto">
  <name>Task 3: Create DashboardEmptyState and DashboardLoadingState components</name>
  <files>
    apps/web/src/components/coach/dashboard/DashboardEmptyState.tsx
    apps/web/src/components/coach/dashboard/DashboardLoadingState.tsx
  </files>
  <read_first>
    - apps/web/src/components/coach/ComparisonChart.tsx — copy empty state pattern (lines 44–49) as reference for per-card inline empty
    - apps/web/src/components/coach/skeletons.tsx — copy animate-pulse pattern and skeleton color values (#E2E0DA, #F0EFE9)
    - .planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-PATTERNS.md §DashboardEmptyState and §DashboardLoadingState patterns
    - .planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md §9 (Empty State) and §10 (Loading State)
  </read_first>
  <action>
    Create apps/web/src/components/coach/dashboard/DashboardEmptyState.tsx:

    'use client'; first line. Import BarChart2 from 'lucide-react'.

    Props: { prompt?: boolean } — prompt=true for the "select a sport" state; prompt=false (default) for no-data state.

    Return: <div className="flex flex-col items-center justify-center py-16 px-8 text-center">

    Icon circle: <div className="w-12 h-12 rounded-full bg-[#F0EFE9] flex items-center justify-center mb-4"><BarChart2 className="w-6 h-6 text-muted" /></div>

    Heading: <p className="text-[15px] font-semibold text-text mb-2">{prompt ? 'Sélectionnez un sport' : 'Aucune donnée disponible'}</p>

    Body: <p className="text-sm text-muted max-w-[320px] leading-relaxed">{prompt ? 'Sélectionnez un sport pour afficher le dashboard.' : 'Aucune séance trouvée pour cette période. Encouragez votre client à enregistrer ses entraînements.'}</p>

    Export as named export: export function DashboardEmptyState.

    ---

    Create apps/web/src/components/coach/dashboard/DashboardLoadingState.tsx:

    No 'use client' directive needed (no hooks, no browser APIs — but add it anyway for consistency with other dashboard components since it will be rendered inside a client component tree).

    Return: <div className="grid grid-cols-2 gap-4">

    Map [0,1,2,3] to skeleton cards. Each skeleton card: <div key={i} className="bg-white rounded-2xl border border-border p-6 animate-pulse">

    Inside each card:
    - Title skeleton: <div className="h-4 bg-[#E2E0DA] rounded w-2/5 mb-4" />
    - Chart area skeleton: <div className="h-[240px] bg-[#F0EFE9] rounded-lg" />
    - AI row skeleton: <div className="border-t border-border mt-3 pt-3 flex items-center gap-2"><div className="w-4 h-4 bg-[#E2E0DA] rounded-full" /><div className="h-3 bg-[#E2E0DA] rounded w-3/5" /></div>

    Export as named export: export function DashboardLoadingState.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -E "DashboardEmptyState|DashboardLoadingState|ChartCard" | head -10 || echo NO_TS_ERRORS</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/coach/dashboard/DashboardEmptyState.tsx exists
    - DashboardEmptyState.tsx imports BarChart2 from 'lucide-react'
    - DashboardEmptyState.tsx contains prop prompt with default false
    - DashboardEmptyState.tsx contains copy string 'Sélectionner un sport' (prompt=true heading)
    - DashboardEmptyState.tsx contains copy string 'Aucune donnée disponible' (no-data heading)
    - apps/web/src/components/coach/dashboard/DashboardLoadingState.tsx exists
    - DashboardLoadingState.tsx renders 4 skeleton cards (map over [0,1,2,3])
    - DashboardLoadingState.tsx uses animate-pulse class on skeleton cards
    - DashboardLoadingState.tsx uses bg-[#E2E0DA] for title/AI row skeletons
    - DashboardLoadingState.tsx uses bg-[#F0EFE9] for chart area skeleton
    - DashboardLoadingState.tsx renders 2×2 grid (grid grid-cols-2 gap-4)
    - cd apps/web && npm run type-check exits 0
  </acceptance_criteria>
  <done>All shared dashboard UI components created and TypeScript-clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → client component | params.id from URL is passed to child components as clientId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-038-02 | Elevation of Privilege | dashboard/page.tsx params.id | mitigate | clientId passed to PowerliftingDashboard which uses it in .eq('workout_sessions.user_id', clientId) — Supabase RLS via is_coach_of() enforces at DB layer; invalid/spoofed clientId returns empty data, not another coach's data |
| T-038-03 | Tampering | DashboardControlBar select value | accept | sportType comes from a controlled select with known option values; empty string maps to null; no SQL or API call at this layer |
</threat_model>

<verification>
After all 3 tasks complete:
- ClientTabStrip shows "Dashboard" as first tab
- apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx exists and compiles
- All 4 components in apps/web/src/components/coach/dashboard/ exist and compile
- globals.css contains @keyframes fadeInUp definition
- cd apps/web && npm run type-check exits 0
</verification>

<success_criteria>
- Dashboard tab appears first in the client detail tab strip
- Navigating to /[locale]/coach/clients/[id]/dashboard renders the page without crash
- Sport selector shows "Sélectionner un sport" placeholder; selecting nothing shows DashboardEmptyState prompt
- ChartCard, DashboardControlBar, DashboardEmptyState, DashboardLoadingState all export correctly
- fadeInUp keyframe exists in globals.css for use in Plan 038-04
</success_criteria>

<output>
Create .planning/workstreams/main/phases/38-dashboard-foundation-powerlifting/038-02-SUMMARY.md when done
</output>
