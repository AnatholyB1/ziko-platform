---
phase: 037-ui-design-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md
  - .planning/workstreams/main/ROADMAP.md
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - PL-01
  - PL-02
  - PL-03
  - PL-04
  - HYR-01
  - RUN-01
  - BB-01
  - WL-01
  - AI-02
  - AI-03

must_haves:
  truths:
    - "The design contract (037-UI-SPEC.md) exists and is committed to git before any Phase 38 implementation begins"
    - "Every dashboard surface is specified with exact dimensions, color tokens, and copy so Phase 38 executor needs zero design decisions"
    - "Chart library choice (Recharts v3) is documented with import path, rationale, and every variant (line, bar, area, multi-series)"
    - "All 4 Powerlifting charts are fully specified with exact Recharts component names, data shapes, and color assignments"
    - "Hyrox, Running, Bodybuilding, and Weight Loss dashboard outlines exist so Phase 39 executor can begin without additional design discussion"
    - "All locked user decisions (D-01 through D-06) are visibly reflected in the spec"
    - "ROADMAP Phase 37 Plans field is updated to reflect the single plan"
  artifacts:
    - path: ".planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md"
      provides: "Complete visual and interaction contract for all sport dashboard surfaces"
      contains: "## Surface Specifications"
    - path: ".planning/workstreams/main/ROADMAP.md"
      provides: "Updated plan count for Phase 37"
      contains: "Plans: 1 plan"
  key_links:
    - from: "037-UI-SPEC.md §Chart Library Specification"
      to: "recharts"
      via: "documented import path and rationale"
      pattern: "from 'recharts'"
    - from: "037-UI-SPEC.md §Color"
      to: "D-04 SBD colors"
      via: "Squat=#FF5C1A, Bench=#3B82F6, Deadlift=#22C55E"
      pattern: "#FF5C1A.*#3B82F6.*#22C55E"
    - from: "037-UI-SPEC.md §Chart Card Anatomy"
      to: "D-06 locked card structure"
      via: "title → chart (h:240) → AI insight row"
      pattern: "AI insight row"
---

<objective>
Verify and finalize the Phase 37 design contract. The UI-SPEC.md has been produced by the gsd-ui-researcher agent and committed. This plan confirms the spec satisfies every Phase 37 ROADMAP success criterion, documents any gaps with their mitigations, and updates ROADMAP.md with the final plan count.

Phase 37's only deliverable is the written design contract itself — there is no implementation code. Once this plan executes, Phase 38 executors have everything they need to build without a single design decision outstanding.

Purpose: Gate Phase 38 on a verified, complete, unambiguous visual contract
Output: Confirmed 037-UI-SPEC.md coverage report + updated ROADMAP.md plan count
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/main/PROJECT.md
@.planning/workstreams/main/ROADMAP.md
@.planning/workstreams/main/STATE.md
@.planning/workstreams/main/phases/37-ui-design-contract/37-CONTEXT.md
@.planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify UI-SPEC against all ROADMAP Phase 37 success criteria</name>

  <read_first>
    - `.planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md` — the spec under review
    - `.planning/workstreams/main/ROADMAP.md` — Phase 37 success criteria (lines ~22–31)
    - `.planning/workstreams/main/phases/37-ui-design-contract/37-CONTEXT.md` — locked decisions D-01 through D-06
  </read_first>

  <action>
Read 037-UI-SPEC.md in full. Then check each of the four ROADMAP Phase 37 success criteria against the spec contents. For each criterion, record PASS or GAP with evidence.

Criterion 1 — Surface coverage: Confirm the spec contains specifications for: (a) Dashboard tab in ClientTabStrip, (b) sport type selector dropdown, (c) date range segmented filter `[Semaine] [Mois] [3 Mois]`, (d) all 4 Powerlifting chart layouts (1RM SBD, RPE Trend, Weekly Tonnage, Intensity %), (e) Hyrox layout outline (3 cards), (f) Running layout outline (4 cards), (g) Bodybuilding layout outline (3 cards), (h) Weight Loss layout outline (3 cards), (i) chart card anatomy with AI insight chip slot (D-05), (j) empty state, (k) loading skeleton state. Note: comparison mode, PDF export trigger, and narrative summary card are scoped to Phases 40–41 and are documented in 37-CONTEXT.md `<deferred>` — these are NOT gaps; their absence is expected and correct.

Criterion 2 — Figma / design tokens: Confirm the spec explicitly states color tokens #FF5C1A (primary) and #F7F6F3 (background), plus typography sizes and spacing values. Note: the spec states "Figma file access was unavailable at contract-generation time. This UI-SPEC is fully self-sufficient." Confirm the written spec contains all measurements, tokens, and layout rules that Figma annotated mockups would have provided, making Figma access non-blocking for executors.

Criterion 3 — Chart library: Confirm `## Chart Library Specification` section exists with: library name (Recharts v3), import path (`from 'recharts'`), and a rationale note (already installed, used in ComparisonChart.tsx).

Criterion 4 — Chart variants with data shapes: Confirm the spec defines data shapes (TypeScript types) for each chart variant matching Supabase table columns. Check that LineChart (`SBDDataPoint`, `RPEDataPoint`, `IntensityDataPoint`), BarChart (`TonnageDataPoint`, station splits), and AreaChart (intensity fill, bodyweight, weekly distance) are all represented in `## Surface Specifications`. Confirm `## Data Shape Reference` table maps each chart to its Supabase source table and key columns.

Also verify all 6 locked decisions are reflected:
- D-01: global date filter (one segmented control, all 4 charts update simultaneously)
- D-02: sport selector left, date filter right
- D-03: 2×2 grid, chart height h:240
- D-04: 3-line SBD chart, Squat=#FF5C1A, Bench=#3B82F6, Deadlift=#22C55E
- D-05: AI insight row in every card (🧠 icon + placeholder text)
- D-06: card structure top-to-bottom: title → chart → rule → AI row; white bg, rounded-2xl, border, p-6

After checking all criteria, produce a concise verification report in your output (not a file — embed in SUMMARY.md). Format:

```
## Phase 37 Design Contract Verification

| Criterion | Status | Evidence |
|-----------|--------|----------|
| SC-1: Surface coverage (11 surfaces) | PASS/GAP | ... |
| SC-2: Design tokens + Figma note | PASS/GAP | ... |
| SC-3: Chart library documented | PASS/GAP | ... |
| SC-4: Data shapes per chart variant | PASS/GAP | ... |

| Decision | Status | Spec Section |
|----------|--------|--------------|
| D-01: Global date filter | PASS/GAP | ... |
| D-02: Sport left / date right | PASS/GAP | ... |
| D-03: 2×2 grid, h:240 | PASS/GAP | ... |
| D-04: SBD 3-line colors | PASS/GAP | ... |
| D-05: AI insight row | PASS/GAP | ... |
| D-06: Card anatomy | PASS/GAP | ... |

Overall: VERIFIED (all PASS) / INCOMPLETE (N gaps found)
```

If any GAP is found: do not modify the spec. Instead, document the gap in the verification report with a recommended fix and a note that the human should review before proceeding to Phase 38. Do NOT block the plan on gaps — surface them and continue.
  </action>

  <verify>
    <automated>grep -c "## Chart Library Specification" /c/ziko-platform/.planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md</automated>
    Expect: 1
    Also check: grep -c "SBDDataPoint\|RPEDataPoint\|TonnageDataPoint\|IntensityDataPoint" /c/ziko-platform/.planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md — expect: >= 4
    Also check: grep -c "#FF5C1A" /c/ziko-platform/.planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md — expect: >= 5 (multiple usages across color table, SBD spec, AI insight spec)
    Also check: grep -c "AI insight" /c/ziko-platform/.planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md — expect: >= 3 (D-05 coverage across card anatomy and powerlifting specs)
  </verify>

  <acceptance_criteria>
    - `## Chart Library Specification` section exists in 037-UI-SPEC.md with `recharts ^3.8.1`, import path `from 'recharts'`, and rationale text "Already installed"
    - `SBDDataPoint`, `RPEDataPoint`, `TonnageDataPoint`, and `IntensityDataPoint` TypeScript types are all present in the spec
    - Color token `#FF5C1A` appears in the color table as "Primary / Accent" AND in the SBD chart spec as Squat color
    - Color token `#F7F6F3` appears in the color table as "Background"
    - `## Data Shape Reference` table is present and lists at least 8 rows mapping chart → Supabase table → key columns
    - Section "### 3. Chart Card Anatomy" specifies the AI insight row with `border-t border-border mt-3 pt-3` and the 🧠 placeholder
    - Sections for Hyrox, Running, Bodybuilding, and Weight Loss dashboard outlines all exist with at least one chart data shape defined
    - The spec's Figma note explicitly states Figma access was unavailable AND the spec is self-sufficient (confirming the written contract is complete without Figma dependency)
    - Verification report table (produced in task output) shows all 4 success criteria and all 6 decisions as PASS, or documents any gaps with remediation notes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 37 plan count</name>

  <read_first>
    - `.planning/workstreams/main/ROADMAP.md` — Phase 37 entry (lines ~22–32)
  </read_first>

  <action>
Read the Phase 37 entry in ROADMAP.md. Find the `**Plans**: TBD` line. Replace it with:

```
**Plans**: 1 plan
Plans:
- [x] 037-01-PLAN.md — Verify and finalize the UI design contract (037-UI-SPEC.md)
```

Mark the plan as checked `[x]` because this plan's execution constitutes the completion of the verification task. No other changes to ROADMAP.md — leave all other phases untouched.

Write the updated ROADMAP.md using the Edit tool (not a full rewrite).
  </action>

  <verify>
    <automated>grep -A 3 "Phase 37" /c/ziko-platform/.planning/workstreams/main/ROADMAP.md | grep "1 plan"</automated>
    Expect: output contains "1 plan"
    Also check: grep "037-01-PLAN.md" /c/ziko-platform/.planning/workstreams/main/ROADMAP.md — expect: 1 match
  </verify>

  <acceptance_criteria>
    - The string `**Plans**: TBD` no longer exists in ROADMAP.md
    - `**Plans**: 1 plan` exists in the Phase 37 section of ROADMAP.md
    - `037-01-PLAN.md` appears in the Phase 37 plan list in ROADMAP.md
    - No other Phase entries in ROADMAP.md have been modified (line count change is exactly +2 lines from the added plan list entry)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Commit Phase 37 artifacts to git</name>

  <read_first>
    Run `git -C /c/ziko-platform status --porcelain` before staging to confirm which files are modified/untracked.
  </read_first>

  <action>
Stage and commit the Phase 37 artifacts. The files to commit are:

1. `.planning/workstreams/main/phases/37-ui-design-contract/037-UI-SPEC.md` — the design contract (if untracked or modified)
2. `.planning/workstreams/main/phases/37-ui-design-contract/037-01-PLAN.md` — this plan file
3. `.planning/workstreams/main/ROADMAP.md` — updated plan count

Stage only these three files explicitly. Do not stage any file in `.planning/workstreams/da-coach/` or other workstreams. Use `git add` with explicit file paths, not `git add .`.

Commit message:
```
docs(37): finalize UI design contract — verified 037-UI-SPEC.md covers all dashboard surfaces
```

After committing, run `git log --oneline -1` to confirm the commit appears.
  </action>

  <verify>
    <automated>git -C /c/ziko-platform log --oneline -1</automated>
    Expect: most recent commit message contains "docs(37)"
    Also check: git -C /c/ziko-platform show --stat HEAD | grep "037-UI-SPEC.md" — expect: 1 match
  </verify>

  <acceptance_criteria>
    - `git log --oneline -1` output contains "docs(37)"
    - `git show --stat HEAD` lists `037-UI-SPEC.md` and `037-01-PLAN.md` in the changed files
    - `git status --porcelain` shows no remaining staged or modified files in `.planning/workstreams/main/phases/37-ui-design-contract/`
    - Files in `.planning/workstreams/da-coach/` are NOT included in the commit (they belong to a different workstream)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → executor | The UI-SPEC.md is read by Phase 38 executor as the source of truth — correctness of the spec directly affects implementation quality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-01 | Information Disclosure | 037-UI-SPEC.md color tokens | accept | Spec contains only design tokens (no secrets, no PII); public-safe content |
| T-037-02 | Tampering | git commit scope | mitigate | Task 3 explicitly stages only 3 named files; `da-coach` workstream files are excluded by path filter |
| T-037-SC | Tampering | npm/pip/cargo installs | accept | Phase 37 installs no packages — documentation-only phase |
</threat_model>

<verification>
Phase 37 is complete when:
- `037-UI-SPEC.md` is committed and covers all 11 dashboard surfaces (Tab shell, Control Bar, Chart Card Anatomy, Powerlifting 4-chart grid, Hyrox outline, Running outline, Bodybuilding outline, Weight Loss outline, Empty state, Loading skeleton, Dashboard page route)
- All 4 Powerlifting chart data shapes (SBDDataPoint, RPEDataPoint, TonnageDataPoint, IntensityDataPoint) are defined in the spec
- Chart library rationale is documented (Recharts v3 — already installed, import `from 'recharts'`)
- All 6 locked decisions (D-01–D-06) are traceable in the spec
- ROADMAP.md Phase 37 shows "1 plan" with plan list entry `037-01-PLAN.md`
- Verification report produced in SUMMARY.md shows all criteria PASS (or gaps surfaced with remediation notes)
</verification>

<success_criteria>
Phase 37 goal is achieved when Phase 38 executor can open 037-UI-SPEC.md and implement the entire Dashboard Foundation without asking a single design question. Specifically:
- Executor knows: exact Tailwind classes for every component (chart card, control bar, empty state, skeleton)
- Executor knows: exact Recharts component + prop values for every chart variant
- Executor knows: exact French copy strings for all UI text
- Executor knows: exact color hex codes for all chart series, states, and surfaces
- Executor knows: exact Supabase table + column names to query for each chart
- Executor knows: chart height (240px), grid layout (grid-cols-2 gap-4), card padding (p-6), border radius (rounded-2xl)
</success_criteria>

<output>
Create `.planning/workstreams/main/phases/37-ui-design-contract/037-01-SUMMARY.md` when done.

The SUMMARY.md must include the verification report table (all 4 success criteria + 6 decisions) as its primary content section. This table is the proof of phase completion that the orchestrator reads.
</output>
