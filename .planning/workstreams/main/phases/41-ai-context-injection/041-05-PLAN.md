---
phase: 41-ai-context-injection
plan: 05
type: execute
wave: 3
depends_on:
  - 041-03
  - 041-04
files_modified:
  - apps/web/src/components/coach/dashboard/DashboardControlBar.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx
  - apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx
  - apps/web/src/components/coach/dashboard/HyroxDashboard.tsx
  - apps/web/src/components/coach/dashboard/RunningDashboard.tsx
  - apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx
  - apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04

must_haves:
  truths:
    - "DashboardControlBar renders 'Alertes' ghost button and 'Demander à l'IA' primary button in the right section, in the order specified by UI-SPEC"
    - "Dashboard page calls useInsights and passes insights to NarrativeSummaryCard and ChartCard components"
    - "Dashboard page mounts DashboardChatDrawer and AlertesModal and toggles them from ControlBar callbacks"
    - "All five sport dashboards (Powerlifting, Hyrox, Running, Bodybuilding, WeightLoss) accept and call onDataReady with a compact summary metrics object"
    - "Coach can open the chat drawer, send a message, and the AI responds with dashboard context visible in the conversation"
    - "When sport and date range are selected, NarrativeSummaryCard shows loading skeleton then narrative text"
    - "When a threshold is crossed, ChartCard shows an orange or red pill badge in its title bar"
    - "Each ChartCard instance receives a real insight string from insights.chartInsights[chartKey], not the placeholder text"
  artifacts:
    - path: "apps/web/src/components/coach/dashboard/DashboardControlBar.tsx"
      provides: "Extended ControlBar with onOpenChat and onOpenAlerts callback props and two new buttons"
      exports: ["DashboardControlBar"]
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx"
      provides: "Dashboard page wiring: useInsights hook, isChatOpen/isAlertesOpen state, dashboardContextRef, NarrativeSummaryCard, DashboardChatDrawer, AlertesModal mounts"
    - path: "apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx"
      provides: "PowerliftingDashboard extended with onDataReady callback prop and chartInsights threading to ChartCards"
    - path: "apps/web/src/components/coach/dashboard/HyroxDashboard.tsx"
      provides: "HyroxDashboard extended with onDataReady callback prop and chartInsights threading to ChartCards"
    - path: "apps/web/src/components/coach/dashboard/RunningDashboard.tsx"
      provides: "RunningDashboard extended with onDataReady callback prop and chartInsights threading to ChartCards"
    - path: "apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx"
      provides: "BodybuildingDashboard extended with onDataReady callback prop and chartInsights threading to ChartCards"
    - path: "apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx"
      provides: "WeightLossDashboard extended with onDataReady callback prop and chartInsights threading to ChartCards"
  key_links:
    - from: "dashboard/page.tsx"
      to: "useInsights hook"
      via: "useInsights(clientId, sport, dateRange, chartSummary)"
      pattern: "useInsights\\("
    - from: "dashboard/page.tsx DashboardControlBar"
      to: "DashboardChatDrawer"
      via: "onOpenChat={() => setIsChatOpen(true)}"
      pattern: "setIsChatOpen\\(true\\)"
    - from: "dashboard/page.tsx DashboardControlBar"
      to: "AlertesModal"
      via: "onOpenAlerts={() => setIsAlertesOpen(true)}"
      pattern: "setIsAlertesOpen\\(true\\)"
    - from: "PowerliftingDashboard"
      to: "dashboard/page.tsx chartSummary state"
      via: "onDataReady callback"
      pattern: "onDataReady"
    - from: "HyroxDashboard"
      to: "dashboard/page.tsx chartSummary state"
      via: "onDataReady callback"
      pattern: "onDataReady"
    - from: "RunningDashboard"
      to: "dashboard/page.tsx chartSummary state"
      via: "onDataReady callback"
      pattern: "onDataReady"
    - from: "BodybuildingDashboard"
      to: "dashboard/page.tsx chartSummary state"
      via: "onDataReady callback"
      pattern: "onDataReady"
    - from: "WeightLossDashboard"
      to: "dashboard/page.tsx chartSummary state"
      via: "onDataReady callback"
      pattern: "onDataReady"
    - from: "NarrativeSummaryCard"
      to: "insights.narrative"
      via: "narrative={insights?.narrative}"
      pattern: "insights.*narrative"
    - from: "ChartCard"
      to: "insights.crossedThresholds"
      via: "crossedThresholds={insights?.crossedThresholds}"
      pattern: "crossedThresholds.*insights"
    - from: "ChartCard"
      to: "insights.chartInsights"
      via: "aiInsight={insights?.chartInsights['<chart_key>']}"
      pattern: "chartInsights\\["
---

<objective>
Wire all Phase 41 AI features into the dashboard page and ControlBar, and extend all five sport dashboards with the onDataReady callback and chartInsights prop threading.

Purpose: This plan is the integration layer. Plans 01–04 created the building blocks in isolation. This plan connects them so that the four AI features (D-01 through D-14) produce observable behavior from the coach's perspective. The human-verify checkpoint confirms the live features before this phase is closed.

Output: DashboardControlBar extended with two buttons; dashboard page wired with useInsights, state, refs, and all new component mounts; all five sport dashboards extended with onDataReady callback and chartInsights threading to their ChartCards; human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/main/ROADMAP.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-CONTEXT.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-RESEARCH.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-PATTERNS.md
@.planning/workstreams/main/phases/41-ai-context-injection/41-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DashboardControlBar with "Alertes" and "Demander à l'IA" buttons (per D-02, UI-SPEC §3/§4)</name>
  <files>apps/web/src/components/coach/dashboard/DashboardControlBar.tsx</files>
  <read_first>
    - apps/web/src/components/coach/dashboard/DashboardControlBar.tsx — read the FULL file. Identify: (1) the existing props interface (lines ~14–34) and which props already exist, (2) the right section of the ControlBar JSX where the Comparer button and Export PDF button live, (3) the exact className used for existing secondary ghost buttons (the pattern to replicate for "Alertes"), (4) which lucide-react icons are already imported.
  </read_first>
  <action>
    In apps/web/src/components/coach/dashboard/DashboardControlBar.tsx make targeted changes:

    CHANGE 1 — Props interface: add two new optional callback props: onOpenChat?: () =&gt; void and onOpenAlerts?: () =&gt; void. Both optional to avoid breaking any existing usages.

    CHANGE 2 — Lucide icons: add MessageCircle and Bell to the existing lucide-react import if not already present.

    CHANGE 3 — Right section button order (per UI-SPEC §layout constraints): the right section must contain, left to right: [existing Comparer button] | [existing date segmented control] | [new "Alertes" ghost button] | [new "Demander à l'IA" primary button] | [existing Export PDF button].

    "Alertes" button: position before "Demander à l'IA". Render only if onOpenAlerts is defined. Variant: ghost — use the same h-9 px-3 flex items-center gap-1.5 text-sm font-medium text-[#1C1A17] border border-[#E2E0DA] rounded-lg bg-white hover:bg-[#F0EFE9] transition-colors className as the Comparer button. Icon: Bell w-4 h-4 text-[#6B6963]. Label text: "Alertes". onClick=onOpenAlerts.

    "Demander à l'IA" button: position after "Alertes" and before Export PDF. Render only if onOpenChat is defined. Variant: primary — bg-[#FF5C1A] text-white, h-9 px-3, rounded-lg, flex items-center gap-1.5, text-sm font-bold, hover:opacity-90. Icon: MessageCircle w-4 h-4. Label text: "Demander à l'IA". onClick=onOpenChat.

    Do NOT modify: Comparer button, date filter control, Export PDF button, any existing props, any existing state or event handlers.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DashboardControlBar.tsx contains: onOpenChat?: () =&gt; void in props interface
    - DashboardControlBar.tsx contains: onOpenAlerts?: () =&gt; void in props interface
    - DashboardControlBar.tsx contains: MessageCircle in lucide-react import
    - DashboardControlBar.tsx contains: Bell in lucide-react import
    - DashboardControlBar.tsx contains: "Demander à l'IA" button text
    - DashboardControlBar.tsx contains: "Alertes" button text
    - DashboardControlBar.tsx contains: bg-[#FF5C1A] (primary button background for "Demander à l'IA")
    - Button order in JSX: Alertes button appears before "Demander à l'IA" button which appears before the Export PDF button
    - npm run type-check exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>DashboardControlBar renders "Alertes" ghost button then "Demander à l'IA" primary button in the right section before Export PDF; both are optional-prop guarded; existing buttons and props unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend all five sport dashboards with onDataReady callback and chartInsights prop (per D-04, AI-01, AI-02)</name>
  <files>
    apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx,
    apps/web/src/components/coach/dashboard/HyroxDashboard.tsx,
    apps/web/src/components/coach/dashboard/RunningDashboard.tsx,
    apps/web/src/components/coach/dashboard/BodybuildingDashboard.tsx,
    apps/web/src/components/coach/dashboard/WeightLossDashboard.tsx
  </files>
  <read_first>
    - Read each of the five dashboard files in full before modifying any of them. For each file identify: (1) the existing props interface, (2) the useQuery call and the shape of the returned data object (what fields are available after query succeeds), (3) where ChartCard components are rendered (they will need chartInsights and metricKey props added), (4) the chart key strings used as ChartCard identifiers — these become the keys in insights.chartInsights.
    - The onDataReady callback must be called in a useEffect when data changes, not on every render.
    - The chartInsights prop flows from the page down through the sport dashboard to each ChartCard: SportDashboard receives chartInsights?: Record&lt;string, string&gt; and passes aiInsight={chartInsights?.['&lt;chart_key&gt;']} to each ChartCard.
  </read_first>
  <action>
    Apply the same four-change pattern to ALL FIVE dashboard files. Read each file fully before modifying it.

    CHANGE 1 — Props interface (all five files): add two optional props:
      - onDataReady?: (summary: Record&lt;string, unknown&gt;) =&gt; void
      - chartInsights?: Record&lt;string, string&gt;

    CHANGE 2 — useEffect for onDataReady (all five files): after the useQuery result is available and data is non-null, call onDataReady with a compact summary of 3–5 sport-appropriate scalar values. Use the dependency array [data]. Guard: if (data && onDataReady). Build the summary as a plain object with French keys. Extract only scalars (not arrays). If a value is unavailable, omit the key.

    Sport-specific summary metrics to extract (use last/most recent value from each data array):
    - PowerliftingDashboard: { '1RM Squat (dernier)': lastSquat1RM, '1RM Bench (dernier)': lastBench1RM, '1RM Deadlift (dernier)': lastDeadlift1RM, 'RPE moyen (dernière séance)': lastRPE, 'Tonnage hebdo (dernière sem.)': lastWeekTonnage }
    - HyroxDashboard: { 'Temps total (dernière course)': lastFinishTimeSeconds, 'Volume total kg (dernière séance)': lastTotalVolumeKg, 'Allure moy. (dernière course)': lastAvgPace, 'Fréquence cardiaque moy.': lastAvgHeartRate, 'Distance totale km (période)': totalDistanceKm }
    - RunningDashboard: { 'Allure moy. (dernier run)': lastAvgPaceMinPerKm, 'Volume hebdo km': weeklyKm, 'VO2max estimé': vo2maxEstimate, 'Distance (dernier run)': lastDistanceKm, 'FC moy. (dernier run)': lastAvgHeartRate }
    - BodybuildingDashboard: { 'Volume total kg (dernière séance)': lastTotalVolumeKg, 'Séries hebdo': weeklySetCount, 'Poids de corps kg': lastBodyweightKg, 'RPE moyen (dernière séance)': lastRPE, 'Exercices distincts (période)': distinctExercises }
    - WeightLossDashboard: { 'Poids actuel kg': currentWeightKg, 'Calories moy./jour (période)': weeklyCalorieAvg, 'Séries totales (période)': totalSessionSets, 'Déficit calorique moy.': avgCaloricDeficit, 'Tendance poids (kg/sem.)': weightTrendPerWeek }

    When a field is not present in the data structure (check by reading each component's useQuery result shape), omit it from the summary rather than including undefined. Use optional chaining when accessing nested data.

    CHANGE 3 — ChartCard aiInsight prop (all five files): for each ChartCard rendered inside the dashboard, add two props:
      - aiInsight={chartInsights?.['&lt;chart_key&gt;']}   where &lt;chart_key&gt; is the stable string identifier for that chart
      - The chart_key must match exactly what the insights endpoint will receive as the key in chartData (since chartData is built from the summary returned by onDataReady, the chart_key should correspond to the French summary key or a URL-safe lowercase slug of it — use the same key in both places for consistency)

    Assign a stable chart_key slug to each ChartCard. Examples: 'squat_1rm', 'bench_1rm', 'deadlift_1rm', 'rpe_avg', 'tonnage_hebdo' for Powerlifting; 'temps_total', 'volume_total', 'allure_avg', 'fc_avg', 'distance_totale' for Hyrox; 'allure_avg', 'volume_hebdo', 'vo2max', 'distance', 'fc_avg' for Running; 'volume_total', 'series_hebdo', 'poids_corps', 'rpe_avg', 'exercices' for Bodybuilding; 'poids_actuel', 'calories_avg', 'series_total', 'deficit_calorique', 'tendance_poids' for WeightLoss. Read the actual ChartCard usages in each file and assign keys that match the visible chart title.

    CHANGE 4 — Do NOT modify: useQuery calls, chart rendering logic, existing props (besides the two additions), sport selector behavior, or any other logic.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All five dashboard files contain: onDataReady?: (summary: Record&lt;string, unknown&gt;) =&gt; void in props interface
    - All five dashboard files contain: chartInsights?: Record&lt;string, string&gt; in props interface
    - All five dashboard files contain: useEffect (for calling onDataReady when data loads)
    - All five dashboard files contain: onDataReady(summary) call inside useEffect guarded by if (data && onDataReady)
    - The summary object passed to onDataReady in each file contains scalar values (numbers), not arrays
    - All five dashboard files pass: aiInsight={chartInsights?.['...']} to each ChartCard they render
    - npm run type-check exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>All five sport dashboards call onDataReady(summary) when their chart data loads or changes; each summary contains 3–5 scalar metrics with French keys; all ChartCard instances receive aiInsight from chartInsights prop; onDataReady and chartInsights are optional so existing usages without these props compile without change.</done>
</task>

<task type="auto">
  <name>Task 3: Wire all AI features into the dashboard page and thread chartInsights to sport dashboards (per D-01 through D-14)</name>
  <files>apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx — read the FULL file. Identify: (1) existing useState and useRef declarations (lines ~29–47), (2) where DashboardControlBar is rendered and its current props, (3) the sport tab rendering — specifically the activeTab === 'sport' conditional block, including which sport dashboard component is rendered for each sport value and where NarrativeSummaryCard placeholder lives, (4) all existing import statements so new imports are placed correctly, (5) where the component return statement ends (where to append DashboardChatDrawer and AlertesModal), (6) the active sport variable name used to switch between the five sport dashboards.
    - apps/web/src/hooks/useInsights.ts — confirm the hook signature (created in Plan 03 Task 1)
    - apps/web/src/components/coach/dashboard/NarrativeSummaryCard.tsx — confirm NarrativeSummaryCard props
    - apps/web/src/components/coach/dashboard/DashboardChatDrawer.tsx — confirm DashboardChatDrawer props
    - apps/web/src/components/coach/dashboard/AlertesModal.tsx — confirm AlertesModal props
  </read_first>
  <action>
    In apps/web/src/app/[locale]/(coach)/coach/clients/[id]/dashboard/page.tsx make the following additions:

    IMPORTS — Add to existing import block:
    - import { useRef } from 'react' (add to existing react import if not already there)
    - import { useInsights } from '@/hooks/useInsights'
    - import { NarrativeSummaryCard } from '@/components/coach/dashboard/NarrativeSummaryCard'
    - import { DashboardChatDrawer } from '@/components/coach/dashboard/DashboardChatDrawer'
    - import { AlertesModal } from '@/components/coach/dashboard/AlertesModal'

    STATE — Add after existing useState declarations:
    - const [isChatOpen, setIsChatOpen] = useState(false)
    - const [isAlertesOpen, setIsAlertesOpen] = useState(false)
    - const [chartSummary, setChartSummary] = useState&lt;Record&lt;string, unknown&gt; | null&gt;(null)
    - const dashboardContextRef = useRef&lt;{ sport_type: string; metrics: Record&lt;string, string&gt; } | null&gt;(null)

    HOOK — Add after existing hooks (clientId, sport, dateRange are already in scope from the page state):
    - const { data: insights, isLoading: insightsLoading } = useInsights(clientId, sport ?? null, dateRange, chartSummary)

    DASHBOARDCONTROLBAR — In the DashboardControlBar JSX, add two new props: onOpenChat={() =&gt; setIsChatOpen(true)} and onOpenAlerts={() =&gt; setIsAlertesOpen(true)}. Do not change any existing props.

    NARRATIVESUMMARYCARD — Inside the activeTab === 'sport' block, immediately after the DashboardControlBar and before the chart grid div (or before the sport dashboard component), add: &lt;NarrativeSummaryCard narrative={insights?.narrative} sport={sport ?? ''} isLoading={insightsLoading && !!sport} /&gt;

    SPORT DASHBOARD onDataReady PROP — The onDataReady callback is shared by all five sport dashboards. Define a single handler callback:
      const handleDataReady = (summary: Record&lt;string, unknown&gt;) =&gt; {
        setChartSummary(summary);
        if (sport) {
          dashboardContextRef.current = {
            sport_type: sport,
            metrics: Object.fromEntries(Object.entries(summary).map(([k, v]) =&gt; [k, String(v)]))
          };
        }
      }
    Pass onDataReady={handleDataReady} to each of the five sport dashboard components rendered in the sport tab (PowerliftingDashboard, HyroxDashboard, RunningDashboard, BodybuildingDashboard, WeightLossDashboard). Do not change any other props on these components.

    SPORT DASHBOARD chartInsights PROP — Pass chartInsights={insights?.chartInsights} to each of the five sport dashboard components rendered in the sport tab. This threads the chartInsights record from the insights response down to each dashboard, where Task 2 has already wired it to the individual ChartCard aiInsight props.

    DASHBOARDCHATDRAWER MOUNT — At the very end of the page component's return, before the closing fragment or div, add: &lt;DashboardChatDrawer clientId={clientId} isOpen={isChatOpen} onClose={() =&gt; setIsChatOpen(false)} dashboardContextRef={dashboardContextRef} /&gt;

    ALERTESMODAL MOUNT — After the DashboardChatDrawer: {isAlertesOpen && &lt;AlertesModal clientId={clientId} sport={sport ?? null} crossedThresholds={insights?.crossedThresholds} onClose={() =&gt; setIsAlertesOpen(false)} /&gt;}

    NOTE on ChartCard props: ChartCard instances are rendered inside the sport dashboard components (not directly in the page). The chartInsights threading in this task (via chartInsights prop on each sport dashboard) combined with Task 2's changes (each sport dashboard passes aiInsight={chartInsights?.['key']} to its ChartCards) ensures every ChartCard receives the real insight string. No ChartCard modifications are needed at the page level.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/page.tsx contains: import { useInsights } from '@/hooks/useInsights'
    - dashboard/page.tsx contains: import { NarrativeSummaryCard } from '@/components/coach/dashboard/NarrativeSummaryCard'
    - dashboard/page.tsx contains: import { DashboardChatDrawer } from '@/components/coach/dashboard/DashboardChatDrawer'
    - dashboard/page.tsx contains: import { AlertesModal } from '@/components/coach/dashboard/AlertesModal'
    - dashboard/page.tsx contains: const [isChatOpen, setIsChatOpen] = useState(false)
    - dashboard/page.tsx contains: const [isAlertesOpen, setIsAlertesOpen] = useState(false)
    - dashboard/page.tsx contains: const dashboardContextRef = useRef
    - dashboard/page.tsx contains: useInsights(clientId
    - dashboard/page.tsx contains: onOpenChat={() =&gt; setIsChatOpen(true)}
    - dashboard/page.tsx contains: onOpenAlerts={() =&gt; setIsAlertesOpen(true)}
    - dashboard/page.tsx contains: &lt;NarrativeSummaryCard
    - dashboard/page.tsx contains: &lt;DashboardChatDrawer
    - dashboard/page.tsx contains: &lt;AlertesModal
    - dashboard/page.tsx contains: dashboardContextRef (passed to DashboardChatDrawer)
    - dashboard/page.tsx contains: onDataReady={handleDataReady} (or equivalent inline) on PowerliftingDashboard
    - dashboard/page.tsx contains: onDataReady={handleDataReady} (or equivalent inline) on HyroxDashboard
    - dashboard/page.tsx contains: onDataReady={handleDataReady} (or equivalent inline) on RunningDashboard
    - dashboard/page.tsx contains: onDataReady={handleDataReady} (or equivalent inline) on BodybuildingDashboard
    - dashboard/page.tsx contains: onDataReady={handleDataReady} (or equivalent inline) on WeightLossDashboard
    - dashboard/page.tsx contains: chartInsights={insights?.chartInsights} on all five sport dashboard components
    - npm run type-check exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>Dashboard page fully wired: useInsights fires when sport and chartSummary are ready; NarrativeSummaryCard receives narrative and loading state; DashboardChatDrawer and AlertesModal mounted at page root; ControlBar receives both open callbacks; all five sport dashboards receive onDataReady and chartInsights; dashboardContextRef updated when any sport's data loads; ChartCards in all five dashboards receive real aiInsight strings from insights.chartInsights.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All four AI features are now live on the dashboard page:
    - AI-01: "Demander à l'IA" button in ControlBar opens a slide-in chat drawer that sends the active sport and top-3 metrics in the system prompt. Context updates for all five sports (Powerlifting, Hyrox, Running, Bodybuilding, WeightLoss).
    - AI-02: Each chart card shows an AI insight chip ("Analyse en cours…" while loading, real insight when done) — not the "Analyse IA disponible en phase 41" placeholder
    - AI-03: NarrativeSummaryCard appears above the chart grid with a 2-line skeleton then an AI-generated paragraph
    - AI-04: "Alertes" button opens the threshold configuration modal; when thresholds are crossed, orange/red badges appear on chart card title bars
  </what-built>
  <how-to-verify>
    Start the dev server: run `npm run dev` from the repo root and navigate to the coach dashboard for a client who has workout data.

    1. NARRATIVE + INSIGHTS (AI-02/AI-03): Select a sport (e.g. Powerlifting) and a date range. Confirm:
       - NarrativeSummaryCard appears above the charts and shows a pulse skeleton briefly, then a paragraph of French text
       - Each ChartCard footer shows a one-liner insight chip (not the "Analyse IA disponible en phase 41" placeholder)
       - Changing the date range triggers a new insights fetch (skeleton reappears briefly)

    2. SPORT SWITCHING (AI-01/AI-02 for all sports): Switch to each of the five sports (Powerlifting, Hyrox, Running, Bodybuilding, WeightLoss). Confirm:
       - NarrativeSummaryCard updates with a new AI paragraph for each sport
       - ChartCard insight chips change to sport-appropriate insights
       - "Demander à l'IA" context updates (the AI should reference the switched sport's metrics when you send a message)

    3. CHAT DRAWER (AI-01): Click "Demander à l'IA". Confirm:
       - A 420px panel slides in from the right with CSS animation (no jank/flash)
       - The backdrop (20% opacity dark overlay) appears behind the drawer
       - The placeholder "Posez une question sur ce client…" is visible in the input bar
       - Type "Quelle est la tendance de RPE de ce client ?" and press Send
       - The AI responds in streaming fashion and the response mentions the sport or recent metrics without the coach having described them explicitly
       - Close with the X button — drawer slides out

    4. ALERTS (AI-04): Click "Alertes". Confirm:
       - Modal opens centered with backdrop
       - Empty state shows "Aucun seuil configuré" heading and body text
       - Click "+ Ajouter un seuil" — a new row with metric_key input, operator select, and value input appears
       - Fill in metric_key="rpe_avg", operator=">", value=8.5 and click "Enregistrer les seuils"
       - Modal closes. Reload the page and reopen Alertes — the saved threshold appears in the list
       - If rpe_avg data exceeds 8.5, confirm orange badge appears on the RPE chart card title bar

    If any feature does not work as described, note the specific failure.
  </how-to-verify>
  <resume-signal>Type "approved" if all four features work as described, or describe which feature failed and the error you see.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardControlBar callbacks | onOpenChat/onOpenAlerts are page-level state setters; no external data |
| dashboardContextRef | Contains sport_type + metric scalar values built from page state; no user-input data injected |
| chartInsights prop threading | chartInsights values originate from Claude AI response (already sanitized to 80-char strings in the backend); threaded down through sport dashboards to ChartCards as read-only display strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-13 | Information Disclosure | dashboardContextRef content in chat SSE body | accept | dashboardContextRef contains only coach's own dashboard view (sport + numeric metrics from their own Supabase query); passed to the coach's own chat session |
| T-41-14 | Tampering | chartSummary state populated from sport dashboards | accept | chartSummary is built from each sport dashboard's TanStack Query result, which is already auth-gated at the Supabase level via JWT |
| T-41-15 | Tampering | chartInsights strings rendered in ChartCard aiInsight | accept | chartInsights values are short strings (max 80 chars) returned by Claude and never exec'd; rendered as plain text in the DOM |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all tasks and the human-verify checkpoint:
1. npm run type-check passes without new errors across all modified files
2. Human has confirmed all four AI features work end-to-end:
   - NarrativeSummaryCard renders narrative paragraph (AI-03)
   - ChartCard insight chips show AI-generated text, not placeholder (AI-02)
   - Chat drawer opens, sends dashboard_context, AI responds with context awareness (AI-01)
   - AlertesModal saves thresholds; crossed thresholds produce badge on ChartCard (AI-04)
3. All five sport dashboards surface data via onDataReady when the coach switches sports
</verification>

<success_criteria>
- DashboardControlBar: "Alertes" ghost button and "Demander à l'IA" primary button appear in right section in the correct order (Alertes | Demander à l'IA | Export PDF)
- Dashboard page: useInsights fires after sport dashboard data loads (via onDataReady from any of the five sport dashboards); NarrativeSummaryCard above chart grid; DashboardChatDrawer and AlertesModal mounted and toggleable
- All five sport dashboards (Powerlifting, Hyrox, Running, Bodybuilding, WeightLoss): each calls onDataReady with compact scalar summary when data loads; each threads chartInsights down to their ChartCards via aiInsight prop
- ChatDrawer: sends dashboard_context from dashboardContextRef.current on each message; AI response references dashboard context
- AlertesModal: thresholds persist across page reloads; orange/red badge on ChartCard when threshold crossed
- TypeScript compiles without new errors across all plans
- Human checkpoint passed
</success_criteria>

<output>
Create .planning/workstreams/main/phases/41-ai-context-injection/041-05-SUMMARY.md when done
</output>
