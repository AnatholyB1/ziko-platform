---
phase: 41-ai-context-injection
plan: 03
type: execute
wave: 2
depends_on:
  - 041-01
  - 041-02
files_modified:
  - apps/web/src/hooks/useInsights.ts
  - apps/web/src/components/coach/dashboard/NarrativeSummaryCard.tsx
  - apps/web/src/components/coach/dashboard/DashboardChatDrawer.tsx
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03

must_haves:
  truths:
    - "useInsights hook only fires when sport and chartData are both non-null (enabled guard per RESEARCH.md Pitfall 5)"
    - "NarrativeSummaryCard renders above chart grid; shows 2-line skeleton while loading; returns null when sport is null or insights errored"
    - "DashboardChatDrawer slides in from the right with CSS translate-x transition (200ms ease-out); not GSAP"
    - "DashboardChatDrawer sends dashboard_context field in the POST body using dashboardContextRef.current at call time (avoids stale closure)"
    - "SSE buffer parsing in DashboardChatDrawer is copied verbatim from EditChatPanel lines 162–224"
  artifacts:
    - path: "apps/web/src/hooks/useInsights.ts"
      provides: "useInsights hook with TanStack Query, queryKey ['dashboard-insights', clientId, sport, dateRange], enabled guard"
      exports: ["useInsights"]
    - path: "apps/web/src/components/coach/dashboard/NarrativeSummaryCard.tsx"
      provides: "NarrativeSummaryCard component with skeleton loading state, ChartCard-matching anatomy"
      exports: ["NarrativeSummaryCard"]
    - path: "apps/web/src/components/coach/dashboard/DashboardChatDrawer.tsx"
      provides: "Slide-in chat drawer with SSE streaming to /api/coach/ai/chat/stream, dashboard_context injection"
      exports: ["DashboardChatDrawer"]
  key_links:
    - from: "useInsights queryFn"
      to: "POST /api/coach/dashboards/:clientId/insights"
      via: "fetch with { sport, period: dateRange, chartData } body"
      pattern: "fetch.*dashboards.*insights"
    - from: "DashboardChatDrawer streamChat"
      to: "POST /api/coach/ai/chat/stream"
      via: "SSE fetch with dashboard_context: dashboardContextRef.current"
      pattern: "dashboard_context.*dashboardContextRef.current"
---

<objective>
Create three new frontend files: the useInsights TanStack Query hook, NarrativeSummaryCard component, and DashboardChatDrawer component.

Purpose: These are the core frontend delivery units for AI-01 (chat drawer with dashboard context), AI-02 (insight chips via useInsights), and AI-03 (narrative card). The dashboard page wiring plan (Plan 05, Wave 3) depends on these exports being stable and correctly typed.

Output: Three new files. No existing files modified in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/main/ROADMAP.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-CONTEXT.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-RESEARCH.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-PATTERNS.md
@.planning/workstreams/main/phases/41-ai-context-injection/41-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useInsights hook</name>
  <files>apps/web/src/hooks/useInsights.ts</files>
  <read_first>
    - apps/web/src/components/coach/dashboard/PowerliftingDashboard.tsx — read lines 60–80 to see the exact TanStack Query useQuery pattern (queryKey, queryFn, enabled, staleTime) used in this codebase
    - apps/web/src/hooks/ directory listing — confirm the hooks directory exists and check naming convention of existing hook files
  </read_first>
  <action>
    Create apps/web/src/hooks/useInsights.ts as a new file.

    Add 'use client' directive at the top.

    Import useQuery from '@tanstack/react-query'.

    Define the InsightsResult interface locally in this file (do not import from backend types — Next.js cannot import from backend directly):
    InsightsResult: chartInsights Record&lt;string, string&gt;, narrative string, crossedThresholds array of objects each with metric_key string, operator string, threshold_value number, current_value number.

    Export the useInsights function with signature: useInsights(clientId: string, sport: string | null, dateRange: string, chartData: Record&lt;string, unknown&gt; | null).

    Inside, call useQuery&lt;InsightsResult&gt; with:
    - queryKey: ['dashboard-insights', clientId, sport, dateRange]
    - queryFn: async function that calls fetch('/api/coach/dashboards/${clientId}/insights', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ sport, period: dateRange, chartData }) }). If !res.ok, throw new Error('HTTP ' + res.status). Return res.json().
    - enabled: !!sport && !!chartData (both conditions required — per RESEARCH.md Pitfall 5)
    - staleTime: 120_000
    - gcTime: 300_000

    Return the query result object.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | grep "useInsights" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/hooks/useInsights.ts exists
    - File contains: 'use client'
    - File contains: export function useInsights(
    - File contains: queryKey: ['dashboard-insights', clientId, sport, dateRange]
    - File contains: enabled: !!sport && !!chartData
    - File contains: staleTime: 120_000
    - queryFn calls POST to /api/coach/dashboards/${clientId}/insights
    - queryFn body includes: { sport, period: dateRange, chartData }
    - InsightsResult interface includes crossedThresholds array field
  </acceptance_criteria>
  <done>useInsights hook exported; only fires when sport and chartData are non-null; queries POST /api/coach/dashboards/:clientId/insights; 2-minute cache; returns InsightsResult including crossedThresholds.</done>
</task>

<task type="auto">
  <name>Task 2: Create NarrativeSummaryCard component (per D-09, UI-SPEC §2)</name>
  <files>apps/web/src/components/coach/dashboard/NarrativeSummaryCard.tsx</files>
  <read_first>
    - apps/web/src/components/coach/dashboard/ChartCard.tsx — read the full file to extract: exact root className string (bg-white rounded-2xl border border-border p-6), header row structure, existing aiInsight slot pattern. NarrativeSummaryCard must match this anatomy exactly.
    - apps/web/DESIGN.md — confirm border-border, text-text, text-muted color tokens are defined for use in className
  </read_first>
  <action>
    Create apps/web/src/components/coach/dashboard/NarrativeSummaryCard.tsx as a new file.

    Add 'use client' directive.

    Define NarrativeSummaryCardProps interface: narrative string | undefined, sport: string, isLoading: boolean.

    Export NarrativeSummaryCard function component.

    Render logic:
    - If sport is empty string or falsy, return null (silent fail per D-09)
    - Root div: className="bg-white rounded-2xl border border-border p-6 mb-4 w-full" (matches ChartCard anatomy per D-09 and UI-SPEC §2)
    - Header row: div with className="flex items-center gap-2 mb-3". Inside: span with the 🧠 emoji (text-base), h3 with className="text-[15px] font-semibold text-text" and text "Analyse IA — {sport}"
    - Body: conditional on isLoading
      - Loading: div className="space-y-2" containing two skeleton divs — first: className="h-3 bg-[#E2E0DA] rounded animate-pulse w-full", second: className="h-3 bg-[#E2E0DA] rounded animate-pulse w-3/4"
      - Loaded: paragraph className="text-sm text-muted leading-relaxed max-w-prose" with text content: narrative ?? 'Analyse IA disponible dès la sélection d\'un sport.'

    No icon imports needed — emoji is inline text.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/coach/dashboard/NarrativeSummaryCard.tsx exists
    - File contains: export function NarrativeSummaryCard(
    - File contains: bg-white rounded-2xl border border-border p-6
    - File contains: animate-pulse (skeleton loading state)
    - File contains: return null (early return when sport is falsy)
    - File contains: max-w-prose (line length constraint per UI-SPEC)
    - Props interface includes: narrative: string | undefined, sport: string, isLoading: boolean
    - npm run type-check exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>NarrativeSummaryCard exported; renders null when sport is falsy; shows 2-line pulse skeleton while loading; renders narrative paragraph with max-w-prose; card anatomy matches ChartCard (bg-white rounded-2xl border border-border p-6).</done>
</task>

<task type="auto">
  <name>Task 3: Create DashboardChatDrawer component (per D-01/D-05, UI-SPEC §1)</name>
  <files>apps/web/src/components/coach/dashboard/DashboardChatDrawer.tsx</files>
  <read_first>
    - apps/web/src/components/coach/dashboard/EditChatPanel.tsx — read the FULL file. Extract verbatim: SSE buffer+reader loop (lines 162–224), Message/TypingIndicator/ChatInputBar imports, conversationIdRef pattern, message state shape (id, role, content, isStreaming?), the streamChat callback structure, the error state handling. DashboardChatDrawer copies this pattern with only the endpoint URL and body changed.
    - apps/web/src/components/coach/dashboard/DashboardEditOverlay.tsx — read lines 1–50 to see the outer panel and backdrop structure (reference only — do NOT copy GSAP imports; use CSS transition instead per RESEARCH.md anti-patterns)
    - apps/web/src/components/coach/MessageBubble.tsx — confirm the Message type shape and MessageBubble props
    - apps/web/src/components/coach/ChatInputBar.tsx — confirm ChatInputBar props
    - apps/web/src/components/coach/dashboard/TypingIndicator.tsx — confirm TypingIndicator props
  </read_first>
  <action>
    Create apps/web/src/components/coach/dashboard/DashboardChatDrawer.tsx as a new file.

    Add 'use client' directive.

    Import useState, useRef, useCallback, useEffect from 'react'. Import X from 'lucide-react'. Import MessageBubble and Message type from '@/components/coach/MessageBubble'. Import ChatInputBar from '@/components/coach/ChatInputBar'. Import TypingIndicator from './TypingIndicator'.

    Define DashboardContext interface locally: sport_type string, metrics Record&lt;string, string&gt;.

    Define DashboardChatDrawerProps: clientId string, isOpen boolean, onClose () =&gt; void, dashboardContextRef React.MutableRefObject&lt;DashboardContext | null&gt;.

    Export DashboardChatDrawer function component.

    State: messages Message[], isStreaming boolean, error string | null. Refs: conversationIdRef useRef&lt;string | null&gt;(null).

    streamChat callback (useCallback): reads dashboardContextRef.current at call time (not captured at definition — per RESEARCH.md Pitfall 3). Appends user message to messages state. Sets isStreaming true, error null. Calls fetch('/api/coach/ai/chat/stream', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ messages: updatedHistory, conversation_id: conversationIdRef.current ?? undefined, dashboard_context: dashboardContextRef.current }) }).

    SSE buffer+reader loop: copy the exact loop from EditChatPanel lines 162–224 verbatim. Process 'meta' events to set conversationIdRef.current. Process 'chunk' events to append text to the current assistant message bubble. Process 'error' events to setError and setIsStreaming(false). On '[DONE]' set isStreaming false. Catch errors with setError('Erreur de connexion. Réessayez dans un instant.') and setIsStreaming(false).

    JSX structure:
    - Outer wrapper div: className built with template literal — fixed right-0 top-0 h-full w-[420px] bg-white border-l border-[#E2E0DA] z-30 transform transition-transform shadow-xl, plus duration-200 ease-out when opening (translate-x-0) or translate-x-full when closed. Add role="dialog" aria-modal="true" aria-label="Chat IA — contexte dashboard".
    - Header div: height 56px, border-b border-[#E2E0DA], flex items-center justify-between px-4. Left: span "IA Coach" text-[15px] font-semibold text-[#1C1A17]. Right: button onClick=onClose aria-label="Fermer" — X icon w-5 h-5 text-[#6B6963] hover:text-[#1C1A17].
    - Message area: flex-1 overflow-y-auto bg-[#F7F6F3] p-4. Map messages to MessageBubble components. Show TypingIndicator when isStreaming and no current assistant chunk. Show error inline: div className="bg-red-50 border border-red-200 rounded-lg p-3 text-sm text-red-600" with "Impossible d'envoyer le message. Réessayer." and a retry button in text-[#FF5C1A] font-medium.
    - Input bar: fixed bottom of panel (or flex-shrink-0 at bottom), white bg, border-t border-[#E2E0DA]. Render ChatInputBar with disabled=isStreaming, placeholder="Posez une question sur ce client…", onSend=streamChat.
    - Backdrop: when isOpen render div className="fixed inset-0 z-20 bg-black/20" onClick=onClose aria-hidden="true". The backdrop uses 20% opacity (bg-black/20), not 40% — the modal backdrop (AlertesModal) uses 40%.

    Note: Do NOT import or use gsap. CSS transition-transform handles the slide animation.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/coach/dashboard/DashboardChatDrawer.tsx exists
    - File contains: export function DashboardChatDrawer(
    - File contains: dashboardContextRef (MutableRefObject pattern — not dashboardContext plain prop)
    - File contains: dashboard_context: dashboardContextRef.current (read at call time in streamChat)
    - File contains: /api/coach/ai/chat/stream (SSE endpoint URL)
    - File contains: translate-x-full (CSS animation class for closed state)
    - File contains: translate-x-0 (CSS animation class for open state)
    - File contains: transition-transform duration-200 ease-out
    - File contains: bg-black/20 (backdrop, not bg-black/40)
    - File does NOT contain: gsap or GSAP import
    - File contains: conversation_id: conversationIdRef.current (conversation persistence)
    - File contains: aria-modal="true"
    - npm run type-check exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>DashboardChatDrawer exported with CSS slide-in animation (200ms ease-out), SSE streaming to /api/coach/ai/chat/stream, dashboard_context read from ref at call time, conversation_id persisted via ref, error state with retry, backdrop with 20% opacity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardChatDrawer→/api/coach/ai/chat/stream | dashboard_context is client-side state (coach's own dashboard view); forwarded as POST body field |
| useInsights→/api/coach/dashboards/:clientId/insights | chartData is compact numeric summary built from the sport dashboard's query result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-09 | Information Disclosure | DashboardChatDrawer SSE stream | accept | Stream is fetched from same-origin /api proxy; no cross-origin leakage; conversation_id stored in memory ref (not localStorage) |
| T-41-10 | Tampering | chartData in useInsights queryFn | accept | chartData is built by the sport dashboard component from its own Supabase query result — coach-controlled, no client-injected arbitrary strings |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all three tasks:
1. npm run type-check passes without new errors
2. grep for 'dashboard_context: dashboardContextRef.current' in DashboardChatDrawer.tsx confirms ref-time read (not closure)
3. grep for 'enabled: !!sport && !!chartData' in useInsights.ts confirms dual guard
4. NarrativeSummaryCard contains 'return null' for the no-sport guard
</verification>

<success_criteria>
- useInsights.ts: queryKey includes clientId + sport + dateRange; enabled guard requires both sport and chartData; POST body is { sport, period: dateRange, chartData }
- NarrativeSummaryCard.tsx: returns null when sport is falsy; 2-line pulse skeleton while loading; card anatomy matches ChartCard (bg-white rounded-2xl border border-border p-6)
- DashboardChatDrawer.tsx: CSS translate-x slide (no GSAP); SSE loop mirrors EditChatPanel exactly; dashboard_context read from dashboardContextRef.current at call time; conversation_id persisted; backdrop bg-black/20
- All three files TypeScript-clean
</success_criteria>

<output>
Create .planning/workstreams/main/phases/41-ai-context-injection/041-03-SUMMARY.md when done
</output>
