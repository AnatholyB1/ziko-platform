---
phase: 41-ai-context-injection
plan: 04
type: execute
wave: 2
depends_on:
  - 041-01
  - 041-02
files_modified:
  - apps/web/src/components/coach/dashboard/AlertesModal.tsx
  - apps/web/src/components/coach/dashboard/ChartCard.tsx
autonomous: true
requirements:
  - AI-04

must_haves:
  truths:
    - "AlertesModal fetches thresholds for the current client+sport on mount; displays empty state when none configured"
    - "AlertesModal allows coach to add (POST), delete (DELETE) thresholds inline; save button persists all pending adds"
    - "ChartCard renders a threshold-crossed badge in the title bar when crossedThresholds prop includes a matching metric_key"
    - "Badge is orange-tint pill when delta ≤ 20% and red-tint pill when delta > 20%"
  artifacts:
    - path: "apps/web/src/components/coach/dashboard/AlertesModal.tsx"
      provides: "Threshold configuration modal with GET/POST/DELETE CRUD, sport-scoped, per D-12"
      exports: ["AlertesModal"]
    - path: "apps/web/src/components/coach/dashboard/ChartCard.tsx"
      provides: "Extended ChartCard with crossedThresholds prop rendering alert badge per D-14"
      exports: ["ChartCard"]
  key_links:
    - from: "AlertesModal"
      to: "GET /api/coach/dashboards/:clientId/thresholds?sport=..."
      via: "useEffect fetch on mount"
      pattern: "fetch.*thresholds.*sport"
    - from: "AlertesModal"
      to: "POST /api/coach/dashboards/:clientId/thresholds"
      via: "addThreshold handler"
      pattern: "method: 'POST'.*thresholds"
    - from: "ChartCard alertBadge"
      to: "crossedThresholds prop"
      via: "find on metric_key to determine badge presence and severity"
      pattern: "crossedThresholds.*metric_key"
---

<objective>
Create AlertesModal and extend ChartCard to complete the AI-04 threshold feature.

Purpose: AlertesModal gives coaches a sport-scoped UI to configure, view, and delete numeric alert thresholds. ChartCard receives the crossedThresholds array and renders orange/red badge pills on chart title bars when a threshold is crossed. Together these deliver the full AI-04 visual alerting loop.

Output: New AlertesModal.tsx file; existing ChartCard.tsx extended with crossedThresholds prop and badge rendering. No other files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/main/ROADMAP.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-CONTEXT.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-RESEARCH.md
@.planning/workstreams/main/phases/41-ai-context-injection/041-PATTERNS.md
@.planning/workstreams/main/phases/41-ai-context-injection/41-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AlertesModal component (per D-12, UI-SPEC §4)</name>
  <files>apps/web/src/components/coach/dashboard/AlertesModal.tsx</files>
  <read_first>
    - apps/web/src/components/coach/dashboard/DashboardEditOverlay.tsx — read lines 1–130 for the modal overlay structure pattern: fixed inset-0 backdrop, centered panel, header with close button. Also read the handleSave fetch pattern for reference on async state management.
    - apps/web/src/components/coach/dashboard/DashboardControlBar.tsx — read to see the button className pattern used in this file ("h-9 px-3 flex items-center...") for consistency with UI-SPEC
    - apps/web/DESIGN.md — confirm color tokens (#FF5C1A for primary, #EF4444 for critical/red, #E2E0DA for border) used in inline styles
  </read_first>
  <action>
    Create apps/web/src/components/coach/dashboard/AlertesModal.tsx as a new file.

    Add 'use client' directive.

    Import useState, useEffect from 'react'. Import X, Trash2, Plus, Loader2 from 'lucide-react'.

    Define CoachMetricThreshold interface locally (mirror of backend type): id string, coach_id string, client_id string, sport_type string, metric_key string, operator '>' | '&lt;', threshold_value number, is_active boolean, created_at string, updated_at string.

    Define NewThresholdForm type: sport_type string, metric_key string, operator '>' | '&lt;', threshold_value number, is_active boolean.

    Define AlertesModalProps: clientId string, sport: string | null, onClose: () =&gt; void, crossedThresholds?: Array&lt;{ metric_key: string; current_value: number }&gt;.

    Export AlertesModal function component.

    State: thresholds CoachMetricThreshold[], isLoading boolean (true), error string | null, isSaving boolean, newForms NewThresholdForm[] (list of threshold forms to add — starts empty). saveError string | null.

    useEffect (deps: [clientId, sport]): if sport is null, return. Fetch GET /api/coach/dashboards/${clientId}/thresholds?sport=${encodeURIComponent(sport)}. On success, setThresholds(data.thresholds ?? []); setIsLoading(false). On error, setError('Impossible de charger les seuils.'); setIsLoading(false).

    deleteThreshold async function(id: string): call DELETE /api/coach/dashboards/${clientId}/thresholds/${id}. On success, setThresholds(prev =&gt; prev.filter(t =&gt; t.id !== id)).

    addNewForm function: append a default NewThresholdForm to newForms state — default: { sport_type: sport ?? '', metric_key: '', operator: '&gt;', threshold_value: 0, is_active: true }.

    saveNewForms async function: set isSaving true. For each form in newForms where metric_key is non-empty, call POST /api/coach/dashboards/${clientId}/thresholds with body JSON of the form. Collect returned thresholds. After all saves, append returned thresholds to thresholds state, clear newForms, set isSaving false. On error, setSaveError('Erreur lors de l\'enregistrement. Réessayez.'); set isSaving false.

    JSX structure:
    - Root: fixed inset-0 z-50 flex items-center justify-center
    - Backdrop: absolute inset-0 bg-black/40, onClick=onClose, aria-hidden="true"
    - Panel: position relative, z-index 1, bg-white, border-radius 16, width 480, maxHeight '80vh', overflowY 'auto', border '1px solid #E2E0DA'. role="dialog" aria-modal="true" aria-label="Configuration des alertes"
    - Panel header (height 56, flex, px-5, borderBottom, items-center): span "Alertes seuils" (text-[15px] font-semibold text-[#1C1A17]), spacer div flex-1, close button aria-label="Fermer" with X icon
    - Body (p-5): 
      - isLoading: show single-line skeleton (animate-pulse bg-[#E2E0DA] h-8 rounded)
      - error: show error text in text-red-500 text-sm
      - !isLoading && !error:
        - If thresholds.length === 0 && newForms.length === 0: empty state div — heading "Aucun seuil configuré" (text-sm font-semibold text-[#1C1A17] mb-1), p "Définissez des seuils numériques pour être alerté quand un indicateur dépasse une limite." (text-sm text-[#6B6963])
        - Existing thresholds list: for each threshold, render a row div (flex items-center gap-3 py-2 border-b border-[#E2E0DA]):
          - If crossedThresholds?.some(ct =&gt; ct.metric_key === threshold.metric_key): show 8px orange dot (div w-2 h-2 rounded-full bg-[#FF5C1A])
          - Metric key label span (text-sm text-[#1C1A17] flex-1)
          - Operator chip span (text-xs font-bold bg-[#F7F6F3] px-2 py-0.5 rounded)
          - Threshold value span (text-sm font-medium text-[#1C1A17] w-16 text-center)
          - Delete button: aria-label="Supprimer ce seuil", onClick=deleteThreshold(threshold.id), Trash2 icon w-4 h-4 text-[#6B6963] hover:text-red-400
          - If crossedThresholds?.find(ct =&gt; ct.metric_key === threshold.metric_key): show "Valeur actuelle : {ct.current_value}" below row in text-xs text-[#6B6963]
        - New threshold forms list: for each form in newForms, render an editable row:
          - metric_key input: text input, placeholder "Clé de métrique (ex: rpe_avg)", className h-9 border border-[#E2E0DA] rounded-lg px-3 text-sm flex-1, onChange updates newForms[index].metric_key
          - operator select: options '&gt;' and '&lt;', h-9 border border-[#E2E0DA] rounded-lg px-2 text-sm w-16
          - threshold_value input: type="number" step="0.1", h-9 border border-[#E2E0DA] rounded-lg px-3 text-sm w-20
        - Add button: "+ Ajouter un seuil" — button className "text-[#FF5C1A] text-sm font-bold mt-3" onClick=addNewForm
    - Footer (px-5 pb-5 pt-3 border-t border-[#E2E0DA]):
      - If saveError: p text-red-500 text-sm mb-2 with saveError text
      - Save button: "Enregistrer les seuils", full-width h-11 bg-[#FF5C1A] text-white font-bold rounded-xl, onClick=saveNewForms, disabled=isSaving. When isSaving: show Loader2 spin icon instead of label text.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/coach/dashboard/AlertesModal.tsx exists
    - File contains: export function AlertesModal(
    - File contains: useEffect (for initial threshold fetch on mount)
    - File contains: /api/coach/dashboards/${clientId}/thresholds (fetch URL)
    - File contains: method: 'DELETE' (for deleteThreshold)
    - File contains: aria-modal="true"
    - File contains: bg-black/40 (modal backdrop opacity per UI-SPEC)
    - File contains: "Aucun seuil configuré" (empty state copy per copywriting contract)
    - File contains: "+ Ajouter un seuil" (add button copy per copywriting contract)
    - File contains: "Enregistrer les seuils" (save button copy per copywriting contract)
    - File contains: aria-label="Supprimer ce seuil" (delete button accessibility)
    - File contains: crossedThresholds (prop for showing orange dots on crossed thresholds)
    - npm run type-check exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>AlertesModal exported; fetches thresholds on mount; renders empty state when no thresholds; allows adding new threshold forms and saving via POST; allows deleting individual thresholds via DELETE; shows orange dot on crossed thresholds from crossedThresholds prop; modal backdrop uses bg-black/40.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ChartCard with crossedThresholds badge prop (per D-14, UI-SPEC §5)</name>
  <files>apps/web/src/components/coach/dashboard/ChartCard.tsx</files>
  <read_first>
    - apps/web/src/components/coach/dashboard/ChartCard.tsx — read the FULL file. Specifically identify: (1) the existing props interface and where aiInsight?: string appears, (2) the title bar JSX (the div containing the chart title text) where the badge must be inserted inline right-aligned, (3) the exact className of the title text element. Do not modify aiInsight rendering — it already works.
  </read_first>
  <action>
    In apps/web/src/components/coach/dashboard/ChartCard.tsx, make targeted changes only:

    CHANGE 1 — Props interface: add optional prop crossedThresholds?: Array&lt;{ metric_key: string; operator: string; threshold_value: number; current_value: number }&gt; to the existing ChartCardProps interface. Also add optional prop metricKey?: string to allow the chart card to declare which metric it represents (used for badge lookup).

    CHANGE 2 — Badge rendering in title bar: In the title bar JSX (the div containing the chart title), after the title text element and within the same flex row, add a conditional expression: if (metricKey and crossedThresholds?.length), find matchingAlert = crossedThresholds.find(t =&gt; t.metric_key === metricKey). If matchingAlert exists, compute delta: Math.abs((matchingAlert.current_value - matchingAlert.threshold_value) / matchingAlert.threshold_value). If delta &lt;= 0.20 (warning): render a span with className="text-xs font-bold px-2 py-0.5 rounded-full bg-[#FFF7ED] text-[#FF5C1A] border border-[#FF5C1A]/20" and text content matchingAlert.metric_key + ' ' + matchingAlert.operator + ' ' + matchingAlert.threshold_value. If delta &gt; 0.20 (critical): render span with className="text-xs font-bold px-2 py-0.5 rounded-full bg-red-50 text-red-500 border border-red-200" with same text content.

    Do NOT modify: aiInsight prop rendering, chart children rendering, card anatomy (bg-white rounded-2xl border border-border p-6), or any other existing props.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ChartCard.tsx props interface contains: crossedThresholds?: Array (optional, non-breaking for existing usage)
    - ChartCard.tsx props interface contains: metricKey?: string (optional)
    - ChartCard.tsx contains: bg-[#FFF7ED] text-[#FF5C1A] (warning badge color — per UI-SPEC)
    - ChartCard.tsx contains: bg-red-50 text-red-500 (critical badge color — per UI-SPEC)
    - ChartCard.tsx contains: rounded-full (pill badge shape per UI-SPEC §5)
    - ChartCard.tsx contains: border border-[#FF5C1A]/20 (warning badge border)
    - Badge is rendered conditionally — only when metricKey is defined AND a matching crossedThreshold exists
    - Existing callers of ChartCard that do not pass crossedThresholds continue to compile without changes (props are optional)
    - npm run type-check exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>ChartCard accepts optional crossedThresholds array and optional metricKey; when both are present and a threshold crossing is found, renders orange-tint pill badge (warning, delta ≤20%) or red-tint pill badge (critical, delta >20%) inline in the title bar; all new props are optional so existing ChartCard callers require no changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AlertesModal→/api/coach/dashboards/:clientId/thresholds | Coach-submitted threshold configuration (metric_key, operator, threshold_value) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-11 | Tampering | AlertesModal new threshold form (metric_key is free text) | accept | metric_key is an internal label stored per coach; not executed or used in SQL WHERE (backend uses it for comparison only); backend validates operator with CHECK constraint |
| T-41-12 | Information Disclosure | AlertesModal displays threshold values | accept | Thresholds are coach-configured data about their own client; no sensitive data disclosed |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks:
1. npm run type-check passes without new errors
2. grep 'crossedThresholds' in ChartCard.tsx shows the optional prop in interface and the conditional badge rendering
3. grep 'bg-[#FFF7ED]' and 'bg-red-50' in ChartCard.tsx confirms both badge severity colors exist
4. AlertesModal.tsx contains all three fetch patterns: GET (useEffect), POST (saveNewForms), DELETE (deleteThreshold)
</verification>

<success_criteria>
- AlertesModal: fetches on mount; empty state with correct copy; add/save/delete CRUD; orange dot on crossed rows; modal with bg-black/40 backdrop
- ChartCard: crossedThresholds and metricKey props optional (no breaking change); badge rendered as pill with orange (#FFF7ED / #FF5C1A) for warning or red-50 for critical; badge content is "{metric_key} {operator} {threshold_value}"
- TypeScript compiles without new errors
</success_criteria>

<output>
Create .planning/workstreams/main/phases/41-ai-context-injection/041-04-SUMMARY.md when done
</output>
