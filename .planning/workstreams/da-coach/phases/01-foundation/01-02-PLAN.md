---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/plugin-sdk/src/theme.ts
autonomous: true
requirements: []
# Note: SDK theme actions satisfy ROADMAP Phase 1 Success Criterion 4 — tracked via ROADMAP, no FOUND-XX ID

must_haves:
  truths:
    - "useThemeStore exposes a setCustomTheme(overrides: Partial<ThemePalette>) action"
    - "setCustomTheme merges overrides over DEFAULT_THEME and when primary is provided auto-derives primaryLight = primary + '15' and tabBarActive = primary"
    - "clearCoachTheme() action exists and calls resetTheme() internally"
    - "Both new actions compile without TypeScript errors in the monorepo"
  artifacts:
    - path: "packages/plugin-sdk/src/theme.ts"
      provides: "setCustomTheme + clearCoachTheme added to useThemeStore"
      exports: ["setCustomTheme", "clearCoachTheme"]
  key_links:
    - from: "setCustomTheme"
      to: "DEFAULT_THEME"
      via: "{ ...DEFAULT_THEME, ...overrides, primaryLight, tabBarActive }"
      pattern: "DEFAULT_THEME"
    - from: "clearCoachTheme"
      to: "resetTheme"
      via: "calls get().resetTheme()"
      pattern: "resetTheme"
---

<objective>
Extend `packages/plugin-sdk/src/theme.ts` — the single source of truth for the Zustand theme store — with two new actions: `setCustomTheme(overrides: Partial<ThemePalette>)` and `clearCoachTheme()`. No new files; no MMKV persistence (Phase 3 scope).

Purpose: Phase 3 (Mobile Injection) calls `setCustomTheme({ primary: brandHex })` to apply coach branding. `clearCoachTheme()` restores the default Ziko theme when a link is revoked.
Output: `packages/plugin-sdk/src/theme.ts` updated with both actions in `ThemeState` and `useThemeStore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/da-coach/phases/01-foundation/01-CONTEXT.md
</context>

<interfaces>
<!-- Current useThemeStore shape — extracted from packages/plugin-sdk/src/theme.ts -->
<!-- Executor must extend, not replace, the existing store. -->

ThemePalette (relevant tokens for new actions):
  primary: string
  primaryLight: string    -- pattern: primary + '15' (e.g. '#FF5C1A' -> '#FF5C1A15')
  tabBarActive: string    -- matches primary in all existing themes
  tabBarInactive: string  -- fixed at '#7A7670' in DEFAULT_THEME; not auto-derived from primary

Current ThemeState interface:
  theme: ThemePalette
  equippedBanner: BannerDef | null
  setTheme: (themeId: string) => void
  setBanner: (bannerId: string | null) => void
  resetTheme: () => void             -- sets { theme: DEFAULT_THEME, equippedBanner: null }

New additions required (D-10, D-11, D-12, D-13):
  setCustomTheme: (overrides: Partial<ThemePalette>) => void
  clearCoachTheme: () => void
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add setCustomTheme and clearCoachTheme to useThemeStore</name>
  <files>packages/plugin-sdk/src/theme.ts</files>

  <read_first>
    - packages/plugin-sdk/src/theme.ts — read the full file before touching it; understand ThemePalette, DEFAULT_THEME, ThemeState interface, and useThemeStore create() call
  </read_first>

  <behavior>
    - setCustomTheme({ primary: '#2563EB' }) → store.theme has primary='#2563EB', primaryLight='#2563EB15', tabBarActive='#2563EB'; all other tokens inherit from DEFAULT_THEME
    - setCustomTheme({ primary: '#2563EB', surface: '#F0F0F0' }) → surface override is also applied alongside the auto-derived primary tokens
    - setCustomTheme({}) → theme remains DEFAULT_THEME (empty overrides = no change from defaults)
    - clearCoachTheme() → theme equals DEFAULT_THEME, equippedBanner is null (same post-state as resetTheme())
    - setCustomTheme does NOT persist to MMKV — it only updates in-memory Zustand state
  </behavior>

  <action>
    Edit `packages/plugin-sdk/src/theme.ts`. Make two targeted changes:

    1. Extend the `ThemeState` interface with two new action signatures:
       - `setCustomTheme: (overrides: Partial<ThemePalette>) => void`
       - `clearCoachTheme: () => void`

    2. Add the two implementations inside the `create<ThemeState>()((set, get) => ({ ... }))` block.
       Note: if the current create call uses only `(set)`, change the signature to `(set, get)` so `clearCoachTheme` can call `get().resetTheme()`.

       `setCustomTheme` implementation (per D-10, D-11):
       - Compute `primaryLight` from the incoming `primary` override (if provided): `(overrides.primary ?? DEFAULT_THEME.primary) + '15'`
       - Compute `tabBarActive` from the incoming `primary` override (if provided): `overrides.primary ?? DEFAULT_THEME.primary`
       - These two derived values are always computed from `primary` — they must never come from `overrides` directly (they are auto-derived, per D-11)
       - Spread the new theme: `{ ...DEFAULT_THEME, ...overrides, primaryLight, tabBarActive }`
       - Call `set({ theme: <computed> })`
       - Do NOT touch `equippedBanner`

       `clearCoachTheme` implementation (per D-12):
       - Thin wrapper: calls `get().resetTheme()` — no direct `set()` call needed
       - This keeps a single reset path; clearCoachTheme is a named alias for caller clarity

    Do NOT modify any existing action (`setTheme`, `setBanner`, `resetTheme`), the ThemePalette interface, DEFAULT_THEME, THEME_REGISTRY, or BANNER_REGISTRY.
  </action>

  <verify>
    <automated>cd C:/ziko-platform && npm run type-check 2>&1 | grep -i "plugin-sdk/src/theme" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `ThemeState` interface contains `setCustomTheme: (overrides: Partial<ThemePalette>) => void`
    - `ThemeState` interface contains `clearCoachTheme: () => void`
    - `useThemeStore` create block contains `setCustomTheme` implementation with `...DEFAULT_THEME, ...overrides` spread and auto-derived `primaryLight` and `tabBarActive`
    - `primaryLight` computation is `(overrides.primary ?? DEFAULT_THEME.primary) + '15'` or functionally equivalent
    - `tabBarActive` computation is `overrides.primary ?? DEFAULT_THEME.primary` or functionally equivalent
    - `clearCoachTheme` calls `get().resetTheme()` (or equivalent — sets theme: DEFAULT_THEME, equippedBanner: null)
    - `npm run type-check` (or `tsc --noEmit` from packages/plugin-sdk) produces zero errors in theme.ts
    - Existing actions `setTheme`, `setBanner`, `resetTheme` are unchanged
  </acceptance_criteria>
  <done>Both new actions are in the store, the ThemeState interface is updated, and the monorepo type-check passes with zero errors in plugin-sdk/src/theme.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → setCustomTheme | Accepts Partial<ThemePalette> — all inputs are in-memory string values with no persistence or server I/O in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | setCustomTheme overrides | accept | In-memory only (D-13); no persistence in Phase 1. Validated hex at API layer (Plan 03) before value reaches the store. Phase 3 validates before calling setCustomTheme. |
| T-02-02 | Tampering | tabBarActive auto-derivation | accept | tabBarActive is always set to primary (not read from overrides directly) — prevents caller from setting tabBarActive to an arbitrary value inconsistent with primary |
</threat_model>

<verification>
After task completes:
- `npm run type-check` from repo root passes with no errors in `packages/plugin-sdk/src/theme.ts`
- `grep -n "setCustomTheme\|clearCoachTheme" packages/plugin-sdk/src/theme.ts` shows entries in both the interface and the store implementation
</verification>

<success_criteria>
- useThemeStore has setCustomTheme and clearCoachTheme actions
- TypeScript compile passes for plugin-sdk
- Existing theme actions are unmodified
</success_criteria>

<output>
Create `.planning/workstreams/da-coach/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
