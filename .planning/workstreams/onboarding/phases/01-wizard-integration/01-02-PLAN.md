---
phase: 01-wizard-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx
autonomous: true
requirements:
  - WIZARD-01
  - WIZARD-02
  - WIZARD-03

must_haves:
  truths:
    - "OnboardingWizard.tsx imports WizardStep4Import from @/components/coach/WizardStep4Import"
    - "OnboardingWizard.tsx line containing Math.min uses Math.min(4, ...) not Math.min(3, ...)"
    - "OnboardingWizard.tsx renders <WizardProgress totalSteps={4} not totalSteps={3}"
    - "WizardStep3Kyc onSuccess prop calls goToStep(4) not router.push to dashboard"
    - "OnboardingWizard.tsx contains {step === 4 && <WizardStep4Import"
    - "WizardStep4Import onSkip prop calls goToStep or router.push to /${locale}/coach/dashboard"
    - "WizardStep4Import receives userId={userId} apiUrl={API_URL} jwt={jwt}"
    - "npx tsc --noEmit in apps/web exits 0"
  artifacts:
    - path: "apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx"
      provides: "4-step wizard shell: Steps 1–4 mounted, KYC routes to Step 4, Step 4 skip routes to dashboard"
      exports: ["OnboardingWizard"]
---

<objective>
Wire `WizardStep4Import` into `OnboardingWizard.tsx` with four targeted edits. No logic changes beyond the four mechanical wires listed in CONTEXT.md.

Purpose: Makes Step 4 reachable (WIZARD-01: 4-step progress), reached automatically after KYC (WIZARD-02), and rendered when `step === 4` (WIZARD-03).

Output: `OnboardingWizard.tsx` updated with 4 changes — step clamp, totalSteps, Step 3 onSuccess redirect, and Step 4 mount block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/PROJECT.md
@.planning/workstreams/onboarding/ROADMAP.md
@.planning/workstreams/onboarding/REQUIREMENTS.md
@.planning/workstreams/onboarding/STATE.md
@.planning/workstreams/onboarding/phases/01-wizard-integration/01-CONTEXT.md
@.planning/workstreams/onboarding/phases/01-wizard-integration/01-UI-SPEC.md
</context>

---

## Task 1 — Add import for `WizardStep4Import`

<read_first>
- `apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx` — read the full file before editing; note all 4 import lines at the top (lines 1–9)
</read_first>

<action>
In `OnboardingWizard.tsx`, add one import line after the existing `WizardStep3Kyc` import (line 9):

```
import { WizardStep4Import } from '@/components/coach/WizardStep4Import';
```

This adds the named import consistent with the three existing step imports (`WizardStep1Role`, `WizardStep2Profile`, `WizardStep3Kyc`).
</action>

<acceptance_criteria>
- `OnboardingWizard.tsx` contains `import { WizardStep4Import } from '@/components/coach/WizardStep4Import';`
- No duplicate imports
- `npx tsc --noEmit` exits 0 after this change
</acceptance_criteria>

---

## Task 2 — Change step clamp from `Math.min(3, ...)` to `Math.min(4, ...)`

<read_first>
- `apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx` — line 17: `const step = Math.min(3, Math.max(1, parseInt(searchParams.get('step') ?? '1', 10)));`
</read_first>

<action>
On line 17 of `OnboardingWizard.tsx`, change `Math.min(3,` to `Math.min(4,`:

Before: `const step = Math.min(3, Math.max(1, parseInt(searchParams.get('step') ?? '1', 10)));`
After:  `const step = Math.min(4, Math.max(1, parseInt(searchParams.get('step') ?? '1', 10)));`

This allows `?step=4` in the URL to be a valid step value (WIZARD-01 + WIZARD-03 prerequisite).
</action>

<acceptance_criteria>
- `OnboardingWizard.tsx` contains `Math.min(4,` (not `Math.min(3,`) in the step clamp line
- `npx tsc --noEmit` exits 0
</acceptance_criteria>

---

## Task 3 — Change `totalSteps={3}` to `totalSteps={4}` on `WizardProgress`

<read_first>
- `apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx` — line 68: `<WizardProgress currentStep={step} totalSteps={3} />`
- `apps/web/src/components/coach/WizardProgress.tsx` — verify it accepts `totalSteps` as a prop (it does — no changes needed to WizardProgress)
</read_first>

<action>
On line 68 of `OnboardingWizard.tsx`, change `totalSteps={3}` to `totalSteps={4}`:

Before: `<WizardProgress currentStep={step} totalSteps={3} />`
After:  `<WizardProgress currentStep={step} totalSteps={4} />`

This satisfies WIZARD-01: the progress indicator shows 4 steps.
</action>

<acceptance_criteria>
- `OnboardingWizard.tsx` contains `<WizardProgress currentStep={step} totalSteps={4} />`
- File does NOT contain `totalSteps={3}` anywhere
- `npx tsc --noEmit` exits 0
</acceptance_criteria>

---

## Task 4 — Wire `WizardStep3Kyc.onSuccess` to `goToStep(4)`

<read_first>
- `apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx` — line 88: `onSuccess={() => router.push(`/${locale}/coach/dashboard`)}`
- `apps/web/src/components/coach/WizardStep3Kyc.tsx` — verify `onSuccess` prop is `() => void` (it is — no changes needed to WizardStep3Kyc)
</read_first>

<action>
In the `{step === 3 && <WizardStep3Kyc ...>}` block (line 83–90), change the `onSuccess` prop:

Before: `onSuccess={() => router.push(`/${locale}/coach/dashboard`)}`
After:  `onSuccess={() => goToStep(4)}`

This satisfies WIZARD-02: completing KYC navigates to `?step=4` instead of the dashboard.

The `goToStep` helper (defined at line 62: `const goToStep = (n: number) => router.push(...)`) is already available — no new code needed.

Leave `onSkip` unchanged: `onSkip={() => router.push(`/${locale}/coach/dashboard`)}` — Step 3 skip still goes to dashboard.
</action>

<acceptance_criteria>
- `OnboardingWizard.tsx` WizardStep3Kyc block contains `onSuccess={() => goToStep(4)}`
- `OnboardingWizard.tsx` WizardStep3Kyc block still contains `onSkip={() => router.push(`/${locale}/coach/dashboard`)}`
- File does NOT contain `onSuccess={() => router.push` in the WizardStep3Kyc block (the old redirect is removed)
- `npx tsc --noEmit` exits 0
</acceptance_criteria>

---

## Task 5 — Mount `WizardStep4Import` for `step === 4`

<read_first>
- `apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx` — lines 83–91: the `{step === 3 && <WizardStep3Kyc ...>}` block; new step 4 block goes immediately after its closing `)`
</read_first>

<action>
After the closing `)` of the `{step === 3 && <WizardStep3Kyc ... />}` block (after line 90), add:

```tsx
{step === 4 && (
  <WizardStep4Import
    userId={userId}
    apiUrl={API_URL}
    jwt={jwt}
    onSuccess={() => router.push(`/${locale}/coach/dashboard`)}
    onSkip={() => router.push(`/${locale}/coach/dashboard`)}
  />
)}
```

Note: `onSuccess` on Step 4 also goes to dashboard (no step 5 in Phase 1). Both onSuccess and onSkip route to the same destination for Phase 1.

This satisfies WIZARD-03: `OnboardingWizard` mounts `WizardStep4Import` when `step === 4`.
</action>

<acceptance_criteria>
- `OnboardingWizard.tsx` contains `{step === 4 && (`
- `OnboardingWizard.tsx` contains `<WizardStep4Import`
- `WizardStep4Import` mount block passes `userId={userId}`, `apiUrl={API_URL}`, `jwt={jwt}`, `onSuccess`, `onSkip`
- `npx tsc --noEmit` exits 0
- The existing `{step === 1}`, `{step === 2}`, `{step === 3}` blocks are unchanged
</acceptance_criteria>
