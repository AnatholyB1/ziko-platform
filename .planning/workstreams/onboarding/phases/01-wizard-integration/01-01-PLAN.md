---
phase: 01-wizard-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/coach/WizardStep4Import.tsx
  - apps/web/messages/fr.json
  - apps/web/messages/en.json
autonomous: true
requirements:
  - WIZARD-03

must_haves:
  truths:
    - "apps/web/src/components/coach/WizardStep4Import.tsx exists and exports named function WizardStep4Import"
    - "WizardStep4Import accepts props: userId, apiUrl, jwt (all string), onSuccess and onSkip (both () => void)"
    - "WizardStep4Import root div has classes: bg-white rounded-2xl p-8 border border-border shadow-sm"
    - "WizardStep4Import renders h2 with classes text-xl font-bold text-text mb-2"
    - "WizardStep4Import renders p with classes text-sm font-normal text-muted mb-6"
    - "WizardStep4Import skip button calls onSkip onClick and has classes h-11 px-4 text-sm font-normal text-muted hover:text-text transition-colors"
    - "WizardStep4Import skip button text renders t('step4Skip')"
    - "messages/fr.json Onboarding namespace contains keys: step4Heading, step4Subtitle, step4Skip"
    - "messages/en.json Onboarding namespace contains keys: step4Heading, step4Subtitle, step4Skip"
    - "fr.json step4Heading value is: Importer vos documents"
    - "fr.json step4Skip value is: Ignorer pour l'instant"
    - "npx tsc --noEmit in apps/web exits 0"
  artifacts:
    - path: "apps/web/src/components/coach/WizardStep4Import.tsx"
      provides: "Step 4 shell component — receives all Phase 1–4 props, wires only onSkip in Phase 1"
      exports: ["WizardStep4Import"]
---

<objective>
Create the `WizardStep4Import` component shell and add its i18n keys to both locale files.

Purpose: Delivers the new Step 4 component in isolation so `OnboardingWizard.tsx` can import it in Plan 02. The component matches the visual and prop pattern of `WizardStep3Kyc` exactly — same card DOM structure, same button style, same prop interface — but wires only `onSkip` in Phase 1.

Output: `WizardStep4Import.tsx` + 3 i18n keys in fr.json and en.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/PROJECT.md
@.planning/workstreams/onboarding/ROADMAP.md
@.planning/workstreams/onboarding/REQUIREMENTS.md
@.planning/workstreams/onboarding/STATE.md
@.planning/workstreams/onboarding/phases/01-wizard-integration/01-CONTEXT.md
@.planning/workstreams/onboarding/phases/01-wizard-integration/01-UI-SPEC.md
</context>

---

## Task 1 — Create `WizardStep4Import.tsx`

<read_first>
- `apps/web/src/components/coach/WizardStep3Kyc.tsx` — source of truth for card DOM structure, prop interface, and Tailwind classes to replicate exactly
- `apps/web/src/components/coach/WizardProgress.tsx` — optional context: no changes, just verify pattern
- `.planning/workstreams/onboarding/phases/01-wizard-integration/01-UI-SPEC.md` — exact DOM spec, prop interface, i18n key names, color/typography values
</read_first>

<action>
Create `apps/web/src/components/coach/WizardStep4Import.tsx` as a new file. It must:

1. `'use client';` directive at top
2. Import `useTranslations` from `next-intl`
3. Export named function `WizardStep4Import` with this exact prop interface:
   - `userId: string`
   - `apiUrl: string`
   - `jwt: string`
   - `onSuccess: () => void`
   - `onSkip: () => void`
4. Inside: `const t = useTranslations('Onboarding');`
5. Return a card matching WizardStep3Kyc's root structure:
   - Root div: `className="bg-white rounded-2xl p-8 border border-border shadow-sm"`
   - h2: `className="text-xl font-bold text-text mb-2"` with `{t('step4Heading')}`
   - p: `className="text-sm font-normal text-muted mb-6"` with `{t('step4Subtitle')}`
   - Empty placeholder comment `{/* Phase 2: upload UI goes here */}`
   - Button row div: `className="flex gap-3 mt-8 justify-end items-center"`
   - Skip button: `type="button"`, `onClick={onSkip}`, `className="h-11 px-4 text-sm font-normal text-muted hover:text-text transition-colors"` with `{t('step4Skip')}`
   - No primary CTA button in Phase 1
</action>

<acceptance_criteria>
- `apps/web/src/components/coach/WizardStep4Import.tsx` exists
- File contains `export function WizardStep4Import(`
- File contains `userId: string` and `apiUrl: string` and `jwt: string` in the prop destructuring block
- File contains `onSuccess: () => void` and `onSkip: () => void` in the prop destructuring block
- File contains `className="bg-white rounded-2xl p-8 border border-border shadow-sm"`
- File contains `className="text-xl font-bold text-text mb-2"`
- File contains `className="text-sm font-normal text-muted mb-6"`
- File contains `onClick={onSkip}`
- File contains `className="h-11 px-4 text-sm font-normal text-muted hover:text-text transition-colors"`
- File contains `t('step4Heading')` and `t('step4Subtitle')` and `t('step4Skip')`
- `npx tsc --noEmit` in `apps/web/` exits 0 (no TypeScript errors in the new file)
</acceptance_criteria>

---

## Task 2 — Add i18n keys to `apps/web/messages/fr.json`

<read_first>
- `apps/web/messages/fr.json` — read lines 130–140 to locate the insert point (after `"step3Cta": "Terminer la configuration"` at line 137, before the closing `}` of the `Onboarding` namespace)
</read_first>

<action>
In `apps/web/messages/fr.json`, inside the `Onboarding` namespace, add three keys immediately after `"step3Cta": "Terminer la configuration"` (line 137):

```
"step4Heading": "Importer vos documents",
"step4Subtitle": "Vous pourrez importer vos programmes et fiches clients une fois le compte créé.",
"step4Skip": "Ignorer pour l'instant"
```

Do NOT add a trailing comma on `step4Skip` if it is the last key before the closing `}` of `Onboarding`.
</action>

<acceptance_criteria>
- `apps/web/messages/fr.json` contains `"step4Heading": "Importer vos documents"`
- `apps/web/messages/fr.json` contains `"step4Subtitle": "Vous pourrez importer vos programmes et fiches clients une fois le compte créé."`
- `apps/web/messages/fr.json` contains `"step4Skip": "Ignorer pour l'instant"`
- The file remains valid JSON (no syntax error)
</acceptance_criteria>

---

## Task 3 — Add i18n keys to `apps/web/messages/en.json`

<read_first>
- `apps/web/messages/en.json` — read around the `"step3Cta"` line to locate the insert point (same pattern as fr.json)
</read_first>

<action>
In `apps/web/messages/en.json`, inside the `Onboarding` namespace, add three keys immediately after `"step3Cta": "Complete setup"`:

```
"step4Heading": "Import your documents",
"step4Subtitle": "You'll be able to import your programs and client files once your account is created.",
"step4Skip": "Skip for now"
```

Do NOT add a trailing comma on `step4Skip` if it is the last key before the closing `}` of `Onboarding`.
</action>

<acceptance_criteria>
- `apps/web/messages/en.json` contains `"step4Heading": "Import your documents"`
- `apps/web/messages/en.json` contains `"step4Skip": "Skip for now"`
- The file remains valid JSON (no syntax error)
- `npx tsc --noEmit` in `apps/web/` exits 0
</acceptance_criteria>
