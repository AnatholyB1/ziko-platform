---
phase: 04-review-commit
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - apps/web/src/components/coach/WizardStep4Import.tsx
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, COMPLETE-01, COMPLETE-02]

must_haves:
  truths:
    - "Clicking 'Continuer →' replaces the entire card with a review screen — chat, drop zone and file list are unmounted"
    - "The review screen lists every analysed doc, with da_coach rows carrying a 'no action' badge"
    - "A coach can flip any doc's type with a pill toggle and the running count updates on the same render"
    - "'Confirmer et importer' commits every template-typed doc; a failed doc gets its own inline error and Retry"
    - "'Ignorer pour l'instant' on the review screen leaves without committing anything"
    - "After all commits land, a success message shows and the coach reaches the dashboard"
    - "D-01: 'Continuer →' swaps the entire WizardStep4Import card to a dedicated review view, fully unmounting the chat/drop zone/file list rather than appending or partially replacing"
    - "D-02: the review screen is one-way — no back button is rendered"
    - "D-03: 'Ignorer pour l'instant' stays available on the review screen and redirects without committing"
    - "D-04: the review list renders both coach_template and da_coach docs together in one consolidated list"
    - "D-05: da_coach rows show the 'Enregistré comme contexte' no-action badge instead of an import indicator"
  artifacts:
    - path: "apps/web/src/components/coach/WizardStep4Import.tsx"
      provides: "Review 'editing'/'committing'/'done' render branches"
      contains: "step4ReviewHeading"
  key_links:
    - from: "import-view 'Continuer →' button"
      to: "review view"
      via: "onClick setView('review')"
      pattern: "setView\\('review'\\)"
    - from: "review row pill toggle"
      to: "setDocType"
      via: "onClick handler"
      pattern: "setDocType\\("
    - from: "review CTA"
      to: "handleConfirm"
      via: "onClick handler"
      pattern: "onClick=\\{handleConfirm\\}"
    - from: "failed row"
      to: "retryCommit"
      via: "scoped Réessayer button"
      pattern: "retryCommit\\("
---

<objective>
Render the Phase 4 review screen inside `WizardStep4Import.tsx` and wire it to the handlers plan 04-03 created. This is the plan that turns the RED test suite GREEN and closes all five phase requirements.

Purpose: Today, Phase 3's "Continuer →" button calls `onSuccess()` directly and the coach lands on the dashboard with nothing committed. This plan inserts the review + commit screen between classification and that redirect.

Output: Three new render branches (`editing`, `committing`, `done`) inside the existing card shell, one changed `onClick` on the import view, and two new icon imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/phases/04-review-commit/04-CONTEXT.md
@.planning/workstreams/onboarding/phases/04-review-commit/04-UI-SPEC.md
@.planning/workstreams/onboarding/phases/04-review-commit/04-PATTERNS.md
@apps/web/src/components/coach/WizardStep4Import.tsx
@apps/web/src/components/coach/WizardStep4Import.test.tsx

<interfaces>
<!-- Exact class strings to reuse verbatim, extracted from the current file.
     04-UI-SPEC.md introduces ZERO new spacing, typography or colour tokens. -->

Card shell (line 427):        bg-white rounded-2xl p-8 border border-border shadow-sm
Heading (line 428):           text-xl font-bold text-text mb-2
Subtitle (line 429):          text-sm font-normal text-muted mb-6
Doc row container (line 552): flex items-center gap-3 p-3 rounded-xl border border-border bg-white
Filename (line 555):          text-sm font-normal text-text truncate
File size (line 556):         text-xs text-muted
Inline error (line 558):      text-xs text-red-500 mt-0.5
Badge (line 565):             inline-flex items-center px-2 py-1 rounded-md text-xs font-bold bg-surface-alt text-text border border-border
Pill (lines 477, 484):        px-3 py-2 rounded-full border border-border text-sm font-bold text-text hover:border-primary hover:text-primary transition-colors
Spinner span (line 50):       w-3 h-3 border-2 border-current border-t-transparent rounded-full animate-spin
Footer row (line 582):        flex gap-3 mt-8 justify-end items-center
Skip button (line 586):       h-11 px-4 text-sm font-normal text-muted hover:text-text transition-colors
Primary CTA (line 594):       h-11 px-6 rounded-xl bg-primary text-white text-sm font-bold hover:opacity-90 transition-opacity

Helpers already defined in the component and reusable as-is:
  getFileIcon(filename: string): React.ReactElement
  formatBytes(bytes: number): string

State and handlers delivered by plan 04-03 (wire to these by name):
  view, setView            : 'import' | 'review'
  reviewPhase, setReviewPhase : 'editing' | 'committing' | 'done'
  reviewDocs               : FileState[]  (fileStates filtered to status === 'ready')
  committableCount         : number
  committedCount           : number
  setDocType(fileId, next) : void
  handleConfirm()          : Promise<void>
  retryCommit(fileId)      : void

i18n keys delivered by plan 04-01 (all under the Onboarding namespace):
  step4ReviewHeading, step4ReviewSubtitle, step4ReviewNoAction, step4ReviewCount (ICU plural, arg `count`),
  step4ReviewConfirm, step4CommitError, step4CommitRetry, step4CommitRetryAria,
  step4CommitSuccess (ICU plural, arg `count`)
Existing keys reused: step4Skip, step4PillTemplate ("Template programme"), step4PillDaCoach ("DA coach")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intercept "Continuer →" and render the review 'editing' view</name>
  <files>apps/web/src/components/coach/WizardStep4Import.tsx</files>
  <read_first>
    - apps/web/src/components/coach/WizardStep4Import.tsx (lines 426-602 — the entire current return block, so the new branch matches its structure exactly)
    - .planning/workstreams/onboarding/phases/04-review-commit/04-UI-SPEC.md (Screen States section, state 1 "Editing", and the Copywriting Contract)
    - .planning/workstreams/onboarding/phases/04-review-commit/04-PATTERNS.md (the exact class strings and which line each came from)
    - apps/web/src/components/coach/WizardStep4Import.test.tsx (tests 1-3 — the accessible names and text your markup must expose)
  </read_first>
  <action>
    STEP A — intercept the CTA (D-01). On the existing import-view "Continuer →" button (currently `onClick={onSuccess}` at line 593), change the handler to `onClick={() => setView('review')}`. Leave the `canAdvance &&` gate, the label key `step4Continue`, and the class string untouched. `onSuccess` is now only ever called by plan 04-03's completion effect.

    STEP B — add the review render branch. All hooks in this component are already declared above the current `return (`, so insert `if (view === 'review') { return ( ... ); }` immediately before it. This satisfies D-01's full content swap: the chat block, drop zone and file list are unmounted, not hidden.

    The review branch renders the SAME outer card shell (`bg-white rounded-2xl p-8 border border-border shadow-sm`) containing, in order:

    1. `<h2>` with the heading class and `{t('step4ReviewHeading')}`, then `<p>` with the subtitle class and `{t('step4ReviewSubtitle')}`.

    2. A `<div className="flex flex-col gap-2">` mapping over `reviewDocs` (D-04 — both doc types together, nothing hidden or grouped into sections). Give each entry an outer `<div key={fileState.id} className="flex flex-col gap-1">` wrapper so Task 2's error block can sit directly beneath the row. Inside that wrapper, the row itself uses the doc-row container class and holds:
       - `<div className="shrink-0 text-muted">{getFileIcon(fileState.file.name)}</div>`
       - `<div className="flex-1 min-w-0">` with the filename `<p>` (filename class) and the size `<p>` (file-size class, value `formatBytes(fileState.file.size)`)
       - the type-correction pill pair (D-06), inside `<div className="flex gap-2 shrink-0">`: a `Template programme` pill (`t('step4PillTemplate')`, `onClick={() => setDocType(fileState.id, 'template_programme')}`) and a `DA coach` pill (`t('step4PillDaCoach')`, `onClick={() => setDocType(fileState.id, 'da_coach')}`). Both are `type="button"`.
         Pill classes: base `px-3 py-2 rounded-full border text-sm font-bold transition-colors`; when that pill's type equals `fileState.docType` append `border-primary text-primary bg-primary/10` (the NEW selected state 04-UI-SPEC.md adds for Phase 4 — Phase 3's clarification pills were single-use-then-gone and never needed it); otherwise append `border-border text-text hover:border-primary hover:text-primary`. Add `disabled={reviewPhase !== 'editing'}` plus `disabled:opacity-50 disabled:pointer-events-none` so types are frozen once a commit is in flight.
       - When `fileState.docType === 'da_coach'` only (D-05), a `<span>` after the pills using `inline-flex items-center px-2 py-1 rounded-md text-xs font-bold bg-surface-alt text-muted border border-border` and `{t('step4ReviewNoAction')}`. Note `text-muted`, not `text-text`: 04-UI-SPEC.md de-emphasises no-action rows relative to committed ones.

    3. The live running count (D-07), directly above the footer: `<p className="text-sm font-bold text-primary text-right mt-6">{t('step4ReviewCount', { count: committableCount })}</p>`.
       DOCUMENTED DEVIATION: 04-UI-SPEC.md's Colour section asks for the count NUMERAL in `text-primary` with the rest in `text-text`. The `step4ReviewCount` copy is locked as a plain ICU plural string with no rich-text tags, so per-numeral styling would require either mutating the locked copy or a `t.rich` tag that the copy does not contain. Applying the accent to the whole one-line count is the closest achievable rendering and preserves the spec's intent (the count line is the coach's focal point in the editing state). Record this in the SUMMARY so the UI checker sees it as intentional.

    4. The footer, reusing the footer row container class `flex gap-3 mt-8 justify-end items-center`:
       - Skip button (COMPLETE-01, D-03): the Skip class string verbatim, `onClick={onSkip}`, label `{t('step4Skip')}` — the existing key, no new one. Per 04-RESEARCH.md Open Question 1, it stays VISIBLE and ENABLED in every review sub-state; it is the coach's escape hatch from a stuck retry, and neither CONTEXT.md nor UI-SPEC.md marks it disabled.
       - Confirm CTA (REVIEW-03): the primary CTA class string plus `disabled:opacity-50 disabled:pointer-events-none`, `onClick={handleConfirm}`, `disabled={reviewPhase === 'committing'}`, label `{t('step4ReviewConfirm')}`.

    Do NOT add a back button — D-02 makes the review screen one-way.
    Do NOT render an empty state — the review view is only reachable when `canAdvance` is true, which requires at least one `ready` doc.
    Do NOT use hex colours or introduce any Tailwind token not listed in the `<interfaces>` block; 04-UI-SPEC.md confirms Phase 4 adds zero new tokens.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/coach/WizardStep4Import.test.tsx -t "consolidated review list" 2>&1 | tail -12 && npx vitest run src/components/coach/WizardStep4Import.test.tsx -t "type correction updates count" 2>&1 | tail -12 && npx vitest run src/components/coach/WizardStep4Import.test.tsx -t "skip on review screen" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/coach/WizardStep4Import.test.tsx -t "consolidated review list"` passes (REVIEW-01).
    - `npx vitest run src/components/coach/WizardStep4Import.test.tsx -t "type correction updates count"` passes (REVIEW-02).
    - `npx vitest run src/components/coach/WizardStep4Import.test.tsx -t "skip on review screen"` passes (COMPLETE-01).
    - `grep -c "onClick={onSuccess}" src/components/coach/WizardStep4Import.tsx` returns `0` — the direct redirect is gone.
    - `grep -c "setView('review')" src/components/coach/WizardStep4Import.tsx` returns `1`.
    - `grep -Ec "#[0-9A-Fa-f]{6}" src/components/coach/WizardStep4Import.tsx` returns `0` — no hex colours.
    - `grep -c "step4ReviewSkip\|step4ReviewBack" src/components/coach/WizardStep4Import.tsx` returns `0` — no new skip key, no back button.
    - `npx tsc --noEmit 2>&1 | grep -c "WizardStep4Import.tsx"` returns `0`.
    - The review branch is a genuine early `return` placed before the existing import-view `return (`, so the drop zone input and chat greeting are absent from the DOM on the review screen (test 1 asserts this).
  </acceptance_criteria>
  <done>The coach reaches a full-card review screen listing every analysed doc, can flip any doc's type with a persistent pill toggle, sees a live count, and can skip out without committing.</done>
</task>

<task type="auto">
  <name>Task 2: Render the 'committing' feedback, per-doc retry, and 'done' success state</name>
  <files>apps/web/src/components/coach/WizardStep4Import.tsx</files>
  <read_first>
    - apps/web/src/components/coach/WizardStep4Import.tsx (the review branch from Task 1; plus lines 40-54 for the `StatusPill` spinner span and lines 557-561 for the per-file error block pattern)
    - .planning/workstreams/onboarding/phases/04-review-commit/04-UI-SPEC.md (Screen States 2 "Committing" and 3 "Done")
    - apps/web/src/components/coach/WizardStep4Import.test.tsx (tests 4-6 — the exact accessible names and texts asserted)
  </read_first>
  <action>
    STEP A — icon imports. Extend the existing `react-icons/io5` import line (line 4) with `IoCheckmarkCircleOutline` and `IoRefreshOutline`. No new icon package; 04-UI-SPEC.md names exactly these two.

    STEP B — 'done' sub-state (D-10, COMPLETE-02). Inside the review branch, before rendering the editing markup, short-circuit on `reviewPhase === 'done'` and return the same card shell containing ONLY a centred success block: `<div className="flex flex-col items-center justify-center gap-3 py-8">` holding `<IoCheckmarkCircleOutline className="w-6 h-6 text-primary" />` and `<p className="text-xl font-bold text-text">{t('step4CommitSuccess', { count: committedCount })}</p>`. No buttons, no rows, no footer — 04-UI-SPEC.md makes this the sole focal point for the 1500 ms hold. The redirect itself is already scheduled by plan 04-03's completion effect; do not add another `setTimeout` or call `onSuccess` from JSX.

    STEP C — 'committing' per-row feedback (D-08). Inside the doc row, after the pills and badge, render the spinner span (`w-3 h-3 border-2 border-current border-t-transparent rounded-full animate-spin`, wrapped in a `shrink-0 text-muted` container) when `fileState.docType === 'template_programme' && fileState.commitStatus === 'pending'`. Add NO "committed" badge for successful rows — 04-UI-SPEC.md deliberately leaves committed rows visually unchanged to avoid clutter; the absence of the spinner is the signal.

    STEP D — per-doc failure + scoped retry (D-09). When `fileState.commitStatus === 'failed'`, render directly BELOW that row (inside the `flex flex-col gap-1` wrapper from Task 1, so it is visually attached to its own row and no other) a `<div className="flex items-center gap-2 pl-3">` containing:
       - `<p className="text-xs text-red-500">{t('step4CommitError')}</p>`
       - a retry `<button type="button">` with class `h-8 px-3 rounded-lg border border-border text-xs font-bold text-text hover:border-primary hover:text-primary transition-colors inline-flex items-center gap-1`, `onClick={() => retryCommit(fileState.id)}`, `aria-label={t('step4CommitRetryAria')}`, containing `<IoRefreshOutline className="w-3 h-3" />` and the label `{t('step4CommitRetry')}`.
       CRITICAL for the test's accessible-name query: the button's accessible name must be exactly `Réessayer`. An `aria-label` OVERRIDES the text content for accessible-name computation, so `aria-label={t('step4CommitRetryAria')}` would make the name "Réessayer l'import de ce document" and break `getAllByRole('button', { name: 'Réessayer' })`. Resolve this by putting the long form on `title` instead of `aria-label`, i.e. use `title={t('step4CommitRetryAria')}` and let the visible text `Réessayer` supply the accessible name. This keeps 04-UI-SPEC.md's intent (fuller context available to assistive tech and on hover) while keeping the accessible name short and matching the test. Note this resolution in the SUMMARY.
       There must be NO page-level "retry all" control — retry is per-doc only.

    STEP E — CTA in-flight state. On the Confirm CTA from Task 1, when `reviewPhase === 'committing'`, render the same spinner span inline before the label. The button must remain VISIBLE and merely disabled (`disabled:opacity-50 disabled:pointer-events-none`), never hidden — 04-UI-SPEC.md is explicit about this.

    Do NOT change any handler logic from plan 04-03; this task is rendering and wiring only.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/coach/WizardStep4Import.test.tsx 2>&1 | tail -20 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run src/components/coach/WizardStep4Import.test.tsx` exits 0 with ALL tests passing — including `parallel commit fires only for template docs` (REVIEW-03), `auto-redirect after commit` (COMPLETE-02), and `per-doc retry isolation` (D-09).
    - `cd apps/web && npm run test` exits 0 — the full suite is green, all 7 test files.
    - `npx tsc --noEmit 2>&1 | grep -c "WizardStep4Import.tsx"` returns `0`.
    - `grep -c "IoCheckmarkCircleOutline" src/components/coach/WizardStep4Import.tsx` returns `2` (import + usage); same for `IoRefreshOutline`.
    - `grep -c "aria-label={t('step4CommitRetryAria')}" src/components/coach/WizardStep4Import.tsx` returns `0` and `grep -c "title={t('step4CommitRetryAria')}" src/components/coach/WizardStep4Import.tsx` returns `1`.
    - `grep -Ec "#[0-9A-Fa-f]{6}" src/components/coach/WizardStep4Import.tsx` returns `0`.
    - `grep -c "setTimeout" src/components/coach/WizardStep4Import.tsx` returns `1` (only plan 04-03's completion effect; no second redirect timer in JSX).
    - The failed-row error block sits inside the same per-doc `flex flex-col gap-1` wrapper as its row, so exactly one row can show an error while siblings stay clean (the retry-isolation test asserts a length of exactly 1 for both the error text and the Retry button).
  </acceptance_criteria>
  <done>Commit progress, per-doc failure with scoped retry, and the 1500 ms success hold all render; the whole Phase 4 suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach interaction → commit trigger | A UI action initiates a server write |
| server error responses → rendered DOM | Failure states are surfaced to the coach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | commit failure rendering | mitigate | The failed row renders the fixed i18n string `step4CommitError`, never the raw server `error` field. Backend 500 responses echo `err.message`, which can leak internal detail; the review screen never displays it. |
| T-04-05 | Repudiation / duplicate side-effect | double-click on Confirm or Réessayer | mitigate | Confirm is `disabled` while `reviewPhase === 'committing'`; the Retry button disappears the instant `commitStatus` flips to `'pending'` (plan 04-03 sets it synchronously). Server-side 409 remains the authoritative guard. |
| T-04-08 | Tampering | type-correction pills during commit | mitigate | Pills are `disabled` unless `reviewPhase === 'editing'`, so the commit set cannot mutate mid-batch and desynchronise the completion effect's `allDone` derivation. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `IoCheckmarkCircleOutline` and `IoRefreshOutline` come from the already-installed `react-icons@^5.6.0`. If any install appears necessary, STOP — a legitimacy gate is required and is not authorized by this plan. |
</threat_model>

<verification>
1. `cd apps/web && npm run test` → all 7 test files pass, exit 0.
2. `cd apps/web && npx tsc --noEmit 2>&1 | grep "WizardStep4Import.tsx"` → no output.
3. `cd apps/web && npx eslint src/components/coach/WizardStep4Import.tsx` → no errors.
4. `grep -E "#[0-9A-Fa-f]{6}" apps/web/src/components/coach/WizardStep4Import.tsx` → no output (semantic tokens only).
</verification>

<success_criteria>
- REVIEW-01: the review screen lists every analysed doc, `da_coach` rows badged "Enregistré comme contexte".
- REVIEW-02: pill toggles change any doc's type and the running count updates on the same render.
- REVIEW-03: Confirm fires `PUT /coach/imports/:id/commit` in parallel for template-typed docs only.
- COMPLETE-01: "Ignorer pour l'instant" is present on the review screen and exits without any commit call.
- COMPLETE-02: a success message renders and `onSuccess` fires 1500 ms later, reaching `/coach/dashboard`.
- Full `apps/web` test suite green; no hex colours; no new i18n keys beyond plan 04-01's nine.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/04-review-commit/04-04-SUMMARY.md` when done.
Record the two documented deviations from 04-UI-SPEC.md — (1) the count line uses `text-primary` on the full line rather than the numeral alone, (2) the retry button's long-form copy is on `title` rather than `aria-label` — so the UI checker treats them as decisions, not defects.
</output>
