---
phase: 04-review-commit
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - apps/web/src/components/coach/WizardStep4Import.test.tsx
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, COMPLETE-01, COMPLETE-02]

must_haves:
  truths:
    - "A test suite drives WizardStep4Import from empty state to the review screen without touching the network"
    - "Each of the 5 phase requirements has at least one named test asserting its observable behavior"
    - "D-09 per-doc retry isolation has a dedicated test"
    - "The commit request body assertion proves the FULL parsed_data object was persisted (D-11)"
  artifacts:
    - path: "apps/web/src/components/coach/WizardStep4Import.test.tsx"
      provides: "6 RED tests covering REVIEW-01/02/03, COMPLETE-01/02, and D-09"
      min_lines: 200
      contains: "vi.stubGlobal('fetch'"
  key_links:
    - from: "apps/web/src/components/coach/WizardStep4Import.test.tsx"
      to: "apps/web/messages/fr.json"
      via: "NextIntlClientProvider messages prop"
      pattern: "NextIntlClientProvider"
    - from: "apps/web/src/components/coach/WizardStep4Import.test.tsx"
      to: "apps/web/src/components/coach/WizardStep4Import.tsx"
      via: "named import + RTL render"
      pattern: "WizardStep4Import"
---

<objective>
Create the Wave 0 test file mandated by `04-VALIDATION.md`: `apps/web/src/components/coach/WizardStep4Import.test.tsx`, covering all five Phase 4 requirement IDs plus D-09's per-doc retry isolation.

Purpose: There is no existing test for this component, and no existing test anywhere in the repo mocks `fetch` (confirmed in `04-PATTERNS.md` "No Analog Found"). This plan establishes that harness so the implementation plans (04-03, 04-04) have a real feedback signal.

Output: One test file with 6-7 named `it()` blocks. **These tests are expected to FAIL (RED) when this plan completes** — the review screen does not exist yet. They go GREEN at the end of plan 04-04. Do NOT weaken assertions to make them pass early, and do NOT implement any part of the review screen from this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/phases/04-review-commit/04-CONTEXT.md
@.planning/workstreams/onboarding/phases/04-review-commit/04-RESEARCH.md
@.planning/workstreams/onboarding/phases/04-review-commit/04-UI-SPEC.md
@.planning/workstreams/onboarding/phases/04-review-commit/04-VALIDATION.md
@apps/web/src/components/coach/WizardStep4Import.tsx
@apps/web/src/components/coach/vocal/VocalReview.test.tsx

<interfaces>
<!-- Load-bearing contracts the test harness must satisfy. Extracted from source;
     the executor should not need to go hunting for these. -->

Component under test (props unchanged by Phase 4):
```typescript
export function WizardStep4Import(props: {
  userId: string;
  apiUrl: string;
  jwt: string;
  onSuccess: () => void;
  onSkip: () => void;
}): JSX.Element
```

Network calls the component makes, in order, per selected file
(source: WizardStep4Import.tsx `runPipeline`, lines 99-336):

| # | Method | URL | Body sent | Response the mock must return |
|---|--------|-----|-----------|-------------------------------|
| 1 | POST | `{apiUrl}/coach/imports` | `{filename, mime_type, size_bytes, mode:'coach_template'}` | `{ import_id, signed_upload_url }` |
| 2 | PUT  | the `signed_upload_url` from #1 | raw File | 200, empty body |
| 3 | PUT  | `{apiUrl}/coach/imports/{id}/status` | `{status:'uploaded'}` | 200, empty body |
| 4 | POST | `{apiUrl}/coach/imports/{id}/parse` | none | 202, empty body |
| 5 | GET  | `{apiUrl}/coach/imports/{id}` | none | `{ import: { status, error_message, parsed_data } }` — polled every 3000ms via setInterval |
| 6 | PUT  | `{apiUrl}/coach/imports/{id}/commit` | `{ parsed_data }` | added by plan 04-03: `{ program_id }` on 200; `{ error, program_id }` on 409 |

Classification thresholds already implemented (Phase 3, lines 268-317):
  parsed_data.overall_confidence >= 0.6  -> docType 'template_programme'
  parsed_data.overall_confidence <  0.4  -> docType 'da_coach'
  otherwise / null                       -> clarificationPending (blocks canAdvance)

Input filter (lines 24-25, 377-380): extension must be pdf/xlsx/xls/docx and size <= 5 MB.

Existing button accessible names on the import view: "Ignorer pour l'instant" (step4Skip),
"Continuer →" (step4Continue, rendered only when canAdvance is true).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the test harness and the three render-level tests (REVIEW-01, REVIEW-02, COMPLETE-01)</name>
  <files>apps/web/src/components/coach/WizardStep4Import.test.tsx</files>
  <read_first>
    - apps/web/src/components/coach/WizardStep4Import.tsx (read in full — exact pipeline call order, classification thresholds, the hidden file input, existing button labels)
    - apps/web/src/components/coach/vocal/VocalReview.test.tsx (repo RTL conventions: plain describe/it, `expect(...).toBeDefined()`, NO jest-dom matchers — jest-dom is not installed)
    - apps/web/src/components/coach/vocal/useVocalRecorder.test.ts (repo convention for stubbing a global and resetting it in beforeEach)
    - apps/web/messages/fr.json (the exact French strings the assertions match against, including the nine keys added by plan 04-01)
    - apps/web/vitest.config.ts (confirms `.test.tsx` runs under happy-dom, `globals: true`, `@/` alias)
  </read_first>
  <action>
    Create `apps/web/src/components/coach/WizardStep4Import.test.tsx`.

    IMPORTS — `React` from 'react'; `describe, it, expect, vi, beforeEach, afterEach` from 'vitest'; `render, screen, fireEvent, act` from '@testing-library/react'; `NextIntlClientProvider` from 'next-intl'; `WizardStep4Import` from './WizardStep4Import'; and the message catalogue via `import frMessages from '../../../messages/fr.json'` (tsconfig sets `resolveJsonModule: true`; the path resolves to `apps/web/messages/fr.json`).

    RENDER HELPER — `renderStep4(onSuccess, onSkip)` renders `<WizardStep4Import userId="coach-1" apiUrl="https://api.test" jwt="jwt-token" onSuccess={onSuccess} onSkip={onSkip} />` wrapped in `<NextIntlClientProvider locale="fr" messages={frMessages}>`, and returns RTL's `container`. Use the REAL `fr.json` catalogue rather than mocking `next-intl`: this makes the suite double as a guard that every `t()` key exists and that ICU plural forms render.

    FIXTURES — two `File` objects: `programme.pdf` and `da-coach.pdf`, both `type: 'application/pdf'`, tiny content so they pass the extension + 5 MB filter.

    FETCH ROUTER — install with `vi.stubGlobal('fetch', vi.fn(routerImpl))` inside `beforeEach`. `routerImpl(input, init)` dispatches on `String(input)` plus `init?.method` and must be ORDER-INDEPENDENT (never a call-sequence array — the two files' pipelines interleave):
    - POST `https://api.test/coach/imports` → parse `init.body` as JSON and derive a deterministic id from `filename`: `programme.pdf` → `imp-template`, `da-coach.pdf` → `imp-da`. Respond 200 `{ import_id: id, signed_upload_url: 'https://storage.test/upload/' + id }`.
    - PUT to any URL starting `https://storage.test/upload/` → 200, `{}`.
    - PUT to a URL ending `/status` → 200, `{}`.
    - POST to a URL ending `/parse` → 202, `{}`.
    - GET `https://api.test/coach/imports/imp-template` → 200 `{ import: { status: 'ready', error_message: null, parsed_data: TEMPLATE_PARSED } }` where `TEMPLATE_PARSED` is the object `{ overall_confidence: 0.9, name: 'Programme Force', weeks: [{ sessions: [{}, {}, {}] }] }`.
    - GET `https://api.test/coach/imports/imp-da` → 200 `{ import: { status: 'ready', error_message: null, parsed_data: DA_PARSED } }` where `DA_PARSED` is `{ overall_confidence: 0.2 }`.
    - PUT to a URL ending `/commit` → delegate to a per-test mutable `commitHandlers` record keyed by import id, so Task 2 can vary the outcome per doc. Default entry for both ids: 200 `{ program_id: 'prog-' + importId }`.
    - Anything else → `throw new Error('unmocked fetch: ' + method + ' ' + url)` so a missed route fails loudly instead of hanging.
    Each response value needs `ok`, `status`, and an async `json()`; a small `jsonResponse(status, body)` factory suffices — the component never reads response headers.

    TIMER STRATEGY — `vi.useFakeTimers()` in `beforeEach`; `vi.useRealTimers()` and `vi.unstubAllGlobals()` in `afterEach`. Do NOT use RTL `waitFor` anywhere in this file: `waitFor`'s fake-timer support keys off a global `jest` object that Vitest does not define, so it would silently fall back to real timers and hang. Drive every async boundary explicitly with `await act(async () => { ... })`, and advance the poll clock with `await vi.advanceTimersByTimeAsync(ms)` (the async variant — it flushes microtasks between ticks; the sync `advanceTimersByTime` does not).

    CRYPTO GUARD — the component calls `crypto.randomUUID()` in `handleFiles`. In `beforeEach`, if `globalThis.crypto?.randomUUID` is not a function, `vi.stubGlobal('crypto', { randomUUID: () => 'test-uuid-' + (++uuidCounter) })`.

    FILE SELECTION HELPER — `selectFiles(container, files)`: get the hidden input via `container.querySelector('input[type="file"]')`, attach files with `Object.defineProperty(input, 'files', { value: files, configurable: true })`, then `fireEvent.change(input)`. Do NOT use `userEvent.upload` — it requires an `advanceTimers` bridge under fake timers and adds a failure mode for no benefit. The component sets `e.target.value = ''` after reading; that is legal on a file input, do not work around it.

    DRIVE HELPER — `driveToReview(container)`:
    (1) `await act(async () => { selectFiles(container, [templateFile, daFile]); })` — flushes the four sequential pipeline fetches for both files, since every mocked response resolves as a microtask.
    (2) `await act(async () => { await vi.advanceTimersByTimeAsync(3000); })` — fires the 3000 ms polling interval for both files, transitioning them to `ready` with `docType` assigned.
    (3) `await act(async () => { fireEvent.click(screen.getByRole('button', { name: 'Continuer →' })); })` — the Phase 3 CTA, which plan 04-04 re-points at the review view.

    TESTS IN THIS TASK — three `it()` blocks. Their names MUST contain the exact substrings that `04-VALIDATION.md` filters on, so `vitest -t` selection keeps working:

    1. Name: `renders a consolidated review list of every analysed doc` (REVIEW-01; contains "consolidated review list"). After `driveToReview`: assert `screen.getByText("Vérifie tes documents avant l'import")` is defined; assert the subtitle `Confirme le type de chaque document, puis lance l'import.` is defined; assert both `programme.pdf` and `da-coach.pdf` render; assert `screen.getAllByText('Enregistré comme contexte')` has length exactly 1 (only the `da_coach` row carries the no-action badge, D-05); and prove the D-01 full swap by asserting `screen.queryByText("Envoie-moi tes docs et je m'occupe du reste.")` is null AND `container.querySelector('input[type="file"]')` is null.

    2. Name: `type correction updates count and the live commit set` (REVIEW-02; contains "type correction updates count"). After `driveToReview`: assert `screen.getByText('1 programme sera importé')` is defined. Then click the `Template programme` pill on the SECOND row — `screen.getAllByRole('button', { name: 'Template programme' })[1]` — inside `act`; rows render in `fileStates` order so index 1 is `da-coach.pdf`. Assert `screen.getByText('2 programmes seront importés')` is defined and `screen.queryByText('1 programme sera importé')` is null. Then click the `DA coach` pill on the FIRST row and assert the count text returns to `1 programme sera importé` — this proves the count is derived live from `docType` on every render rather than snapshotted (D-07).

    3. Name: `skip on review screen exits without committing` (COMPLETE-01; contains "skip on review screen"). After `driveToReview`: click `screen.getByRole('button', { name: "Ignorer pour l'instant" })` inside `act`. Assert `onSkip` was called exactly once, `onSuccess` was never called, and that NO recorded fetch call has a URL ending in `/commit` — derive this from `(globalThis.fetch as unknown as { mock: { calls: unknown[][] } }).mock.calls`.

    Leave the default `commitHandlers` entries at 200; Task 2 overrides them per test.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/coach/WizardStep4Import.test.tsx --reporter=verbose 2>&1 | tee /tmp/p4t1.log; grep -Ec "consolidated review list|type correction updates count|skip on review screen" /tmp/p4t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/coach/WizardStep4Import.test.tsx` exists and contains the literal `vi.stubGlobal('fetch'`.
    - The verify command's `grep -Ec` prints `3` or greater — all three test names were COLLECTED AND EXECUTED. A module-load crash (which prints no test names at all) is a hard failure of this task.
    - The runner output contains NO occurrence of `Cannot find module` and NO occurrence of `unmocked fetch:`.
    - `grep -c "waitFor" src/components/coach/WizardStep4Import.test.tsx` returns `0`.
    - `grep -c "userEvent" src/components/coach/WizardStep4Import.test.tsx` returns `0`.
    - Tests are EXPECTED to report as failing at this point. Every failure must be an assertion failure that names review-screen copy (e.g. `Unable to find an element with the text: Vérifie tes documents avant l'import`). Timeouts, `TypeError`, and unhandled promise rejections are NOT acceptable failure modes and must be fixed before the task is done.
  </acceptance_criteria>
  <done>The harness renders the component with real fr.json messages, drives it through the full mocked pipeline up to the "Continuer →" click, and three named tests execute and fail cleanly on review-screen assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add the commit-flow tests (REVIEW-03, COMPLETE-02, D-09)</name>
  <files>apps/web/src/components/coach/WizardStep4Import.test.tsx</files>
  <read_first>
    - apps/web/src/components/coach/WizardStep4Import.test.tsx (the harness from Task 1)
    - .planning/workstreams/onboarding/phases/04-review-commit/04-RESEARCH.md (Pattern 2 for the exact commit request shape, Pitfall 2 for 409 idempotency, Pattern 3 for the 1500 ms hold)
    - backend/api/src/coach/imports/service.ts (lines 372-414 — the real `PUT /:id/commit` contract the mock imitates: 200 `{program_id}`, and 409 `{error:'Import already committed', program_id}` when already committed)
  </read_first>
  <action>
    Append three more `it()` blocks (plus one optional seventh) to the same file, reusing the Task 1 harness.

    4. Name: `parallel commit fires only for template docs` (REVIEW-03). Drive to review. Correct `da-coach.pdf` to `Template programme` so there are two committable docs. For this test, make BOTH `/commit` responses DEFERRED: build two `{ promise, resolve }` deferreds keyed by import id and have `commitHandlers` return those promises. Click `Confirmer et importer` inside `await act(async () => { ... })`. BEFORE resolving either deferred, assert exactly 2 recorded fetch calls have a URL ending `/commit` — this is the D-08 parallelism assertion (a sequential implementation would show 1). Then assert on the recorded `imp-template` commit call: `method === 'PUT'`, `headers.Authorization === 'Bearer jwt-token'`, `headers['Content-Type'] === 'application/json'`, and `JSON.parse(body)` deep-equals `{ parsed_data: { overall_confidence: 0.9, name: 'Programme Force', weeks: [{ sessions: [{}, {}, {}] }] } }`. That deep-equal is the D-11 assertion: it proves the FULL `parsed_data` object was persisted onto `FileState`, not just the narrowed `name`/`weeks`/`sessions` display fields Phase 3 extracted. Resolve both deferreds and flush inside `act`.
       Then, in a SECOND `renderStep4` within the same test, drive to review and leave `da-coach.pdf` as `DA coach`. Confirm, and assert exactly 1 `/commit` call was recorded for that render, that its URL contains `imp-template`, and that no recorded `/commit` URL contains `imp-da` — `da_coach` docs are never sent to `/commit` (D-08).

    5. Name: `auto-redirect after commit` (COMPLETE-02). Drive to review with the default one-template/one-da state. Click `Confirmer et importer` inside `act` (default handlers return 200 `{ program_id }` immediately). Assert `screen.getByText('1 programme importé !')` is defined — the ICU `one` branch of `step4CommitSuccess`. Assert `onSuccess` has NOT been called yet. Then `await act(async () => { await vi.advanceTimersByTimeAsync(1400); })` and assert `onSuccess` still has zero calls; then advance a further 100 ms and assert `onSuccess` was called exactly once. This pins the exact 1500 ms hold from D-10 / 04-UI-SPEC.md rather than accepting any delay.

    6. Name: `per-doc retry isolation` (D-09). Drive to review and correct `da-coach.pdf` to `Template programme` so both docs are committable. Configure `commitHandlers` so `imp-template` returns 200 `{ program_id: 'prog-1' }` and `imp-da` returns 500 `{ error: 'boom' }`. Click `Confirmer et importer` inside `act`. Assert: `screen.getAllByText("L'import de ce document a échoué.")` has length exactly 1, and `screen.getAllByRole('button', { name: 'Réessayer' })` has length exactly 1 — the successful row shows neither. Assert `onSuccess` was NOT called even after `await vi.advanceTimersByTimeAsync(1500)` inside `act` (a failed row blocks the redirect per D-10). Snapshot the per-import-id `/commit` call counts. Flip `imp-da`'s handler to 200 `{ program_id: 'prog-2' }`, click the single `Réessayer` button inside `act`, then assert: the `/commit` call count for `imp-template` is UNCHANGED at 1 (retry must not re-fire an already-committed sibling), the count for `imp-da` incremented to 2, the error text and retry button are gone, `screen.getByText('2 programmes importés !')` is defined, and after `await vi.advanceTimersByTimeAsync(1500)` inside `act` `onSuccess` was called exactly once.

    7. OPTIONAL — Name: `treats a 409 already-committed response as success`. Configure `imp-template`'s handler to return status 409 with body `{ error: 'Import already committed', program_id: 'prog-existing' }`. Confirm, and assert no `L'import de ce document a échoué.` text appears and `screen.getByText('1 programme importé !')` is defined. This encodes 04-RESEARCH.md Pitfall 2 / assumption A1. Include it only if the file stays under ~350 lines; skip it rather than overrunning.

    Keep every async boundary on `await act(async () => { ... })`. Do not introduce `waitFor`. Do not modify any file other than this test file.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/coach/WizardStep4Import.test.tsx --reporter=verbose 2>&1 | tee /tmp/p4t2.log; grep -Ec "consolidated review list|type correction updates count|parallel commit fires only for template docs|skip on review screen|auto-redirect after commit|per-doc retry isolation" /tmp/p4t2.log</automated>
  </verify>
  <acceptance_criteria>
    - The verify command's `grep -Ec` prints `6` or greater — all six required test names were collected and executed.
    - `grep -c "advanceTimersByTimeAsync(1500)\|advanceTimersByTimeAsync(1400)" src/components/coach/WizardStep4Import.test.tsx` returns a value `>= 2` (the 1500 ms hold is asserted, not assumed).
    - The file contains a deep-equality assertion on the commit request body that includes the literal `overall_confidence` — proving D-11's full-object persistence is under test.
    - The runner output contains NO `unmocked fetch:`, NO `Cannot find module`, and NO test that fails via timeout or unhandled rejection.
    - `cd apps/web && npx vitest run` still reports the other 6 test files passing (this plan must not break `VocalReview.test.tsx` or any other suite).
    - Tests in this file are EXPECTED to fail (RED) — the review screen ships in 04-04.
  </acceptance_criteria>
  <done>Six named tests execute; each fails with an assertion error naming missing review/commit UI, never with an infrastructure error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → network | Tests must never reach a real host; all egress is stubbed |
| test fixtures → assertions | Fixture data stands in for server-issued `parsed_data` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | test harness credentials | mitigate | The `jwt` prop is the literal string `jwt-token` and `apiUrl` is `https://api.test`; no real credential or real host appears in the test file. The unmocked-route `throw` guarantees no request escapes to a real network. |
| T-04-03 | Spoofing | fetch stub | mitigate | Test asserts `Authorization: Bearer jwt-token` is present on the commit call, locking in that the implementation forwards the caller's JWT rather than issuing an unauthenticated commit. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed by this plan. Plan 04-01 already materialized `@testing-library/dom`; if any install appears necessary here, STOP — a legitimacy gate is required and is not authorized by this plan. |
</threat_model>

<verification>
1. `cd apps/web && npx vitest run src/components/coach/WizardStep4Import.test.tsx --reporter=verbose` prints all six required test names.
2. Every failure in that file is an assertion failure naming review-screen copy — no timeouts, no `TypeError`, no `unmocked fetch:`.
3. `cd apps/web && npx vitest run` shows the other 6 test files still passing.
</verification>

<success_criteria>
- One test file exists with 6 (optionally 7) named tests mapping 1:1 to `04-VALIDATION.md`'s Per-Task Verification Map.
- The suite runs against `NextIntlClientProvider` + real `fr.json`, so missing i18n keys surface as test failures.
- The commit-body deep-equal assertion pins D-11 full-`parsed_data` persistence.
- The 1500 ms redirect hold is asserted at both 1400 ms and 1500 ms.
- Tests are RED, and RED for the right reason.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/04-review-commit/04-02-SUMMARY.md` when done.
Record the exact test names and the current failure message of each — plan 04-04 uses those messages as its GREEN target.
</output>
