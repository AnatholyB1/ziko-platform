---
phase: 04-review-commit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/messages/fr.json
  - apps/web/messages/en.json
  - package-lock.json
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, COMPLETE-02]

must_haves:
  truths:
    - "A .test.tsx file using @testing-library/react `render` + `screen` executes without a module-resolution crash"
    - "Every Phase 4 i18n key resolves to real French copy and real English copy"
    - "The two ICU plural keys render correct singular/plural/zero forms"
  artifacts:
    - path: "apps/web/messages/fr.json"
      provides: "9 new Onboarding-namespace step4Review*/step4Commit* keys with locked French copy"
      contains: "step4ReviewCount"
    - path: "apps/web/messages/en.json"
      provides: "Same 9 keys with English values"
      contains: "step4CommitSuccess"
    - path: "apps/web/node_modules/@testing-library/dom"
      provides: "Peer dependency required at runtime by @testing-library/react v16"
  key_links:
    - from: "apps/web/src/components/coach/vocal/VocalReview.test.tsx"
      to: "@testing-library/dom"
      via: "@testing-library/react/dist/pure.js require"
      pattern: "screen"
    - from: "apps/web/messages/fr.json"
      to: "apps/web/messages/en.json"
      via: "identical Onboarding key sets"
      pattern: "step4Review|step4Commit"
---

<objective>
Unblock Phase 4's Wave 0 test file by (a) repairing the broken RTL test infrastructure and (b) adding the nine new i18n keys that both the test suite and the implementation depend on.

Purpose: `04-VALIDATION.md` and `04-RESEARCH.md` both assert "no framework install needed — Vitest, RTL, and happy-dom are already devDependencies." **This is incorrect.** `@testing-library/dom` is declared in `apps/web/package.json` devDependencies at `^10.0.0` but is NOT present in `node_modules` anywhere in the monorepo. As a result `apps/web/src/components/coach/vocal/VocalReview.test.tsx` currently fails at module load with `Error: Cannot find module '@testing-library/dom'` (verified by running `npx vitest run` from `apps/web/`: 6 files pass, 1 file fails to load). Any new `.test.tsx` that calls `render()` will fail the same way. This plan fixes that before the Phase 4 test file is written.

Output: A green `VocalReview.test.tsx`, and 9 new i18n keys in both locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/ROADMAP.md
@.planning/workstreams/onboarding/REQUIREMENTS.md
@.planning/workstreams/onboarding/phases/04-review-commit/04-UI-SPEC.md
@.planning/workstreams/onboarding/phases/04-review-commit/04-PATTERNS.md
</context>

<interfaces>
<!-- Insertion point in both message files. Both are 4-space-indented flat string maps
     inside a top-level "Onboarding" object. The last existing key is "step4Continue"
     (fr.json line 163, en.json line 163), immediately followed by the closing `},`
     of the Onboarding namespace and then `"Dashboard": {`. New keys go after
     "step4Continue", which must gain a trailing comma. -->

Existing tail of the `Onboarding` namespace (fr.json, lines 161-164):
  "step4DocTypeTemplate", "step4DocTypeDaCoach", "step4Continue", then `},`

next-intl ICU plural call shape already used elsewhere in the component:
  t('key', { count: someNumber })
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the missing @testing-library/dom peer dependency</name>
  <files>package-lock.json</files>
  <read_first>
    - apps/web/package.json (confirm `@testing-library/dom": "^10.0.0"` is already declared under devDependencies — it is, at line 39)
    - apps/web/vitest.config.ts (confirm `.test.tsx` maps to the happy-dom environment via environmentMatchGlobs)
  </read_first>
  <action>
    `@testing-library/react@^16` lists `@testing-library/dom` as a peer dependency and `require`s it at runtime from `node_modules/@testing-library/react/dist/pure.js`. It is declared in `apps/web/package.json` devDependencies but was never materialized into `node_modules` (neither `apps/web/node_modules/@testing-library/` nor the hoisted root `node_modules/@testing-library/` contains a `dom` directory).

    Step 1: From the repository root, run `npm install`. This is a manifest-reconciliation install, not a new-package install — it materializes an already-declared dependency at the already-declared `^10.0.0` range.

    Step 2: Confirm resolution from `apps/web/`. If `@testing-library/dom` still does not resolve after step 1, run `npm install --save-dev --workspace apps/web @testing-library/dom@^10.0.0` — this uses the exact range already present in the manifest, so `apps/web/package.json` must remain byte-identical afterwards.

    SUPPLY-CHAIN CONSTRAINT (threat T-04-SC): No new package may enter `apps/web/package.json`. If completing this task would require adding a dependency entry that is not already declared in the committed manifest, STOP and report it instead of installing — that would be a new supply-chain surface requiring a legitimacy gate that this plan does not authorize.

    Do NOT edit `apps/web/package.json`. Do NOT change the `^10.0.0` range. Do NOT add `@testing-library/jest-dom` (this codebase deliberately asserts with `expect(...).toBeDefined()` / `toBe()` rather than jest-dom matchers — see `VocalReview.test.tsx`).
  </action>
  <verify>
    <automated>cd apps/web && node -e "console.log(require.resolve('@testing-library/dom'))" && npx vitest run src/components/coach/vocal/VocalReview.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && node -e "console.log(require.resolve('@testing-library/dom'))"` prints a path and exits 0.
    - `cd apps/web && npx vitest run src/components/coach/vocal/VocalReview.test.tsx` reports `2 passed` and exits 0 (it currently fails with `Cannot find module '@testing-library/dom'`).
    - `git diff --stat apps/web/package.json` produces NO output (empty diff) — proving no new dependency entry was added to the manifest.
    - `cd apps/web && npx vitest run` reports `7 passed (7)` test files, `0 failed`.
  </acceptance_criteria>
  <done>RTL `render` + `screen` work at runtime in `.test.tsx` files; the pre-existing VocalReview suite is green; `apps/web/package.json` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the nine Phase 4 i18n keys to fr.json and en.json</name>
  <files>apps/web/messages/fr.json, apps/web/messages/en.json</files>
  <read_first>
    - apps/web/messages/fr.json (lines 130-165 — the `Onboarding` namespace tail and its closing brace)
    - apps/web/messages/en.json (lines 130-165 — same region)
    - .planning/workstreams/onboarding/phases/04-review-commit/04-UI-SPEC.md (Copywriting Contract table — French copy is LOCKED, reproduce it verbatim)
  </read_first>
  <action>
    In BOTH `apps/web/messages/fr.json` and `apps/web/messages/en.json`, inside the top-level `"Onboarding"` object, append the following nine keys immediately after the existing `"step4Continue"` entry (add a trailing comma to `"step4Continue"`). Use the same 4-space indentation and flat-string style as the surrounding keys — no nested objects.

    French values (LOCKED by 04-UI-SPEC.md Copywriting Contract — reproduce exactly, including the accented characters and the space before `!`):
    - `step4ReviewHeading` = Vérifie tes documents avant l'import
    - `step4ReviewSubtitle` = Confirme le type de chaque document, puis lance l'import.
    - `step4ReviewNoAction` = Enregistré comme contexte
    - `step4ReviewCount` = the ICU plural string with `count` as the argument, `=0` branch reading "Aucun programme ne sera importé", `one` branch reading "1 programme sera importé", and `other` branch reading "# programmes seront importés"
    - `step4ReviewConfirm` = Confirmer et importer
    - `step4CommitError` = L'import de ce document a échoué.
    - `step4CommitRetry` = Réessayer
    - `step4CommitRetryAria` = Réessayer l'import de ce document
    - `step4CommitSuccess` = the ICU plural string with `count` as the argument, `one` branch reading "1 programme importé !" and `other` branch reading "# programmes importés !"

    English values (Claude's discretion per CONTEXT.md — use exactly these to keep the plan deterministic):
    - `step4ReviewHeading` = Check your documents before importing
    - `step4ReviewSubtitle` = Confirm each document's type, then start the import.
    - `step4ReviewNoAction` = Saved as context
    - `step4ReviewCount` = ICU plural with `=0` "No program will be imported", `one` "1 program will be imported", `other` "# programs will be imported"
    - `step4ReviewConfirm` = Confirm and import
    - `step4CommitError` = Importing this document failed.
    - `step4CommitRetry` = Retry
    - `step4CommitRetryAria` = Retry importing this document
    - `step4CommitSuccess` = ICU plural with `one` "1 program imported!" and `other` "# programs imported!"

    Notes:
    - `step4CommitRetryAria` is a ninth key beyond 04-PATTERNS.md's eight-key table; 04-UI-SPEC.md's Copywriting Contract specifies `aria-label="Réessayer l'import de ce document"` on the retry button, and this project routes every user-facing string through `t()`, so it needs its own key.
    - These two ICU plural keys are the FIRST use of `{count, plural, ...}` in these message files. All existing `step4*` interpolations are simple named placeholders. ICU plural is native to next-intl v4.8.3 — no new dependency.
    - Do NOT add a `step4ReviewSkip` key: 04-UI-SPEC.md explicitly reuses the existing `step4Skip` key on the review screen.
    - Do NOT escape the accented characters as `\uXXXX`. Existing keys in these files use literal accented characters (e.g. `"Erreur serveur. Veuillez réessayer."`); only `…` and `✓` are escaped, and neither appears in the new copy.
  </action>
  <verify>
    <automated>cd apps/web && node -e "const fr=require('./messages/fr.json'),en=require('./messages/en.json');const k=['step4ReviewHeading','step4ReviewSubtitle','step4ReviewNoAction','step4ReviewCount','step4ReviewConfirm','step4CommitError','step4CommitRetry','step4CommitRetryAria','step4CommitSuccess'];const miss=k.filter(x=>!fr.Onboarding[x]||!en.Onboarding[x]);if(miss.length)throw new Error('missing: '+miss);if(!/\{count, ?plural/.test(fr.Onboarding.step4ReviewCount))throw new Error('fr count not ICU plural');if(!/\{count, ?plural/.test(en.Onboarding.step4CommitSuccess))throw new Error('en success not ICU plural');const fk=Object.keys(fr.Onboarding).sort().join(),ek=Object.keys(en.Onboarding).sort().join();if(fk!==ek)throw new Error('fr/en Onboarding key sets differ');console.log('OK 9 keys, parity confirmed')"</automated>
  </verify>
  <acceptance_criteria>
    - Both files still parse as valid JSON (`require()` in the verify command succeeds).
    - All nine keys exist under `Onboarding` in both files with non-empty values.
    - `fr.Onboarding.step4ReviewCount` and `en.Onboarding.step4ReviewCount` both match `/\{count, ?plural/`; same for `step4CommitSuccess`.
    - The sorted key list of `fr.Onboarding` is identical to that of `en.Onboarding` (no drift).
    - `fr.Onboarding.step4ReviewHeading === "Vérifie tes documents avant l'import"` exactly.
    - `fr.Onboarding.step4CommitError === "L'import de ce document a échoué."` exactly.
    - `git diff apps/web/messages/fr.json` shows only additions (no modified or deleted existing keys), except the added comma on the `step4Continue` line.
  </acceptance_criteria>
  <done>All nine Phase 4 keys resolve in both locales; ICU plural syntax is present on the two count keys; fr/en key parity holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party code enters the build via package installation |
| translation files → rendered DOM | Message strings are interpolated into React output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | `npm install` in Task 1 | mitigate | No new package may enter `apps/web/package.json`. `@testing-library/dom@^10.0.0` is already declared in the committed manifest and is the required peer of the already-installed `@testing-library/react@^16`; this task only materializes it. Acceptance criterion enforces an EMPTY `git diff apps/web/package.json`, which mechanically proves no new supply-chain surface was introduced. If a manifest edit proves necessary, the executor must STOP rather than install. |
| T-04-01 | Injection (XSS via i18n) | ICU plural messages | accept | Values are interpolated by next-intl as text nodes, not `dangerouslySetInnerHTML`; the only interpolated argument is `count`, a locally-derived integer from `fileStates.filter().length`. No user-controlled string enters a message. |
</threat_model>

<verification>
1. `cd apps/web && npx vitest run` → 7 test files, all passing, exit 0.
2. `git diff --stat apps/web/package.json` → empty.
3. The i18n parity/ICU node script in Task 2 prints `OK 9 keys, parity confirmed`.
</verification>

<success_criteria>
- `@testing-library/dom` resolves from `apps/web/`; RTL `render`/`screen` work in `.test.tsx` files.
- `VocalReview.test.tsx` passes (was failing before this plan).
- `apps/web/package.json` is unmodified.
- Nine Phase 4 i18n keys exist in both `fr.json` and `en.json` with fr/en key parity.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/04-review-commit/04-01-SUMMARY.md` when done.
Record in the summary: the RESEARCH.md/VALIDATION.md claim "no framework install needed" was wrong, and what the actual fix was — downstream verification agents need this.
</output>
