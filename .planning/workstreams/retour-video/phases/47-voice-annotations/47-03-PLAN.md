---
phase: 47-voice-annotations
plan: 03
type: execute
wave: 3
depends_on:
  - 47-02
files_modified:
  - apps/web/src/components/coach/videos/AnnotationPanel.tsx
  - apps/web/src/components/coach/videos/VideoPlayerClient.tsx
autonomous: true
requirements:
  - VOICE-01
  - VOICE-04
must_haves:
  truths:
    - "AnnotationPanel composing state has a [Texte][Voix] mode toggle above the timestamp chip"
    - "When mode is 'voice', VoiceComposer renders inside the composing body replacing the textarea"
    - "When VoiceComposer calls onVoiceReady, parent saves annotation via POST /:videoId/annotations with type: 'voice' and audio_path"
    - "Voice annotation list items show mic badge + timestamp chip + transcript text + native <audio> player"
    - "Signed audio URLs are fetched at panel mount for all voice annotations and stored in local state"
    - "Audio player shows skeleton pulse while URL is loading; <audio controls> when ready"
    - "Annotation type is exported from VideoPlayerClient.tsx and includes type and audio_path fields"
  artifacts:
    - path: "apps/web/src/components/coach/videos/AnnotationPanel.tsx"
      provides: "Extended AnnotationPanel: mode toggle, VoiceComposer mount, voice annotation list rendering, signed URL fetch"
      contains: "SET_MODE"
    - path: "apps/web/src/components/coach/videos/VideoPlayerClient.tsx"
      provides: "Annotation interface updated with type and audio_path fields"
      contains: "audio_path"
  key_links:
    - from: "AnnotationPanel.tsx"
      to: "VoiceComposer.tsx"
      via: "import { VoiceComposer } from './VoiceComposer'"
      pattern: "VoiceComposer"
    - from: "AnnotationPanel.tsx"
      to: "GET /coach/videos/annotations/:annotationId/audio-url"
      via: "fetch in useEffect at panel mount for voice annotations"
      pattern: "audio-url"
---

<objective>
Extend AnnotationPanel with the mode toggle, VoiceComposer mounting, VOICE_READY dispatch, voice annotation list rendering, and signed audio URL fetch. Also update the Annotation interface in VideoPlayerClient.tsx to carry type and audio_path.

Purpose: The web annotation panel becomes the full voice + text annotation hub. Coaches can record a voice annotation, review the transcript, and save it — all within the existing panel flow.

Output:
- `apps/web/src/components/coach/videos/AnnotationPanel.tsx` — extended with mode toggle + voice annotation rendering
- `apps/web/src/components/coach/videos/VideoPlayerClient.tsx` — Annotation interface gains type + audio_path
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/retour-video/phases/47-voice-annotations/47-CONTEXT.md
@C:/ziko-platform/.planning/workstreams/retour-video/phases/47-voice-annotations/047-UI-SPEC.md
@C:/ziko-platform/.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-CONTEXT.md

<interfaces>
From apps/web/src/components/coach/videos/AnnotationPanel.tsx (current state to extend):
- AnnotationState union: `status: 'list' | 'composing' | 'editing' | 'sending' | 'sent'`
- composing state: `{ status: 'composing'; annotations: Annotation[]; timestamp_s: number }`
- editing state: `{ status: 'editing'; annotations: Annotation[]; annotationId: string; content: string; timestamp_s: number }`
- AnnotationAction union includes START_COMPOSE, CANCEL_COMPOSE, SAVE_SUCCESS, etc.
- AnnotationPanelProps: `{ videoId, annotations, accessToken, apiUrl, onAnnotationsChange, status? }`
- handleSave(): POSTs to `${apiUrl}/coach/videos/${videoId}/annotations` with `{ timestamp_s, content }`

From apps/web/src/components/coach/videos/VideoPlayerClient.tsx (Annotation interface to extend):
```typescript
export interface Annotation {
  id: string;
  timestamp_s: number;
  content: string;
  coach_id: string;
  // NEEDS: type?: 'text' | 'voice'; audio_path?: string | null;
}
```

From apps/web/src/components/coach/videos/VoiceComposer.tsx (just built):
```typescript
export function VoiceComposer(props: VoiceComposerProps): React.ReactElement
// Props: { timestampSeconds, onVoiceReady, onCancel, accessToken, apiUrl, videoId }
```

New AnnotationState additions per 047-UI-SPEC.md:
- composing state gains `mode: 'text' | 'voice'` (default 'text')
- editing state gains `mode: 'text' | 'voice'` (default 'text')
- New actions: SET_MODE, VOICE_READY

New action types:
  | { type: 'SET_MODE'; mode: 'text' | 'voice' }
  | { type: 'VOICE_READY'; transcript: string; audioPath: string }

VOICE_READY behavior: parent saves annotation via existing fetch pattern with body
  { timestamp_s, content: transcript, type: 'voice', audio_path: audioPath }
then dispatches SAVE_SUCCESS.

Signed URL fetch:
- `useState<Map<string, string>>(new Map())` for audioUrlMap
- `useEffect` fires when panel enters `list` status and annotations array changes
- For each annotation where `annotation.type === 'voice'` and `annotation.audio_path`:
  fetch `GET ${apiUrl}/coach/videos/annotations/${annotation.id}/audio-url` with Bearer token
  on success: update Map with annotation.id → signedUrl
  on failure: log error, leave entry absent (skeleton shows permanently for that item)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Annotation interface in VideoPlayerClient.tsx to include type and audio_path</name>
  <files>apps/web/src/components/coach/videos/VideoPlayerClient.tsx</files>
  <read_first>
    - apps/web/src/components/coach/videos/VideoPlayerClient.tsx (full — find Annotation interface and understand what else imports it)
  </read_first>
  <action>
    Locate `export interface Annotation` in `VideoPlayerClient.tsx` (currently has id, timestamp_s, content, coach_id).

    Add two optional fields:
    - `type?: 'text' | 'voice'`
    - `audio_path?: string | null`

    These are optional to maintain backward compatibility with existing text annotation objects that may not carry these fields (they default to text behavior when absent).

    No other changes to VideoPlayerClient.tsx. Only the interface definition changes.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -A 8 "export interface Annotation" apps/web/src/components/coach/videos/VideoPlayerClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `export interface Annotation` in VideoPlayerClient.tsx contains `type?: 'text' | 'voice'`
    - `export interface Annotation` contains `audio_path?: string | null`
    - Existing fields `id, timestamp_s, content, coach_id` unchanged
    - No other lines in the file changed
    - TypeScript compiles without errors on this file
  </acceptance_criteria>
  <done>Annotation interface extended; VideoPlayerClient.tsx otherwise untouched</done>
</task>

<task type="auto">
  <name>Task 2: Extend AnnotationPanel with mode toggle, VoiceComposer mount, voice save, voice list rendering, and audio URL fetch</name>
  <files>apps/web/src/components/coach/videos/AnnotationPanel.tsx</files>
  <read_first>
    - apps/web/src/components/coach/videos/AnnotationPanel.tsx (full — current state is the base to extend)
    - apps/web/src/components/coach/videos/047-UI-SPEC.md (.planning/workstreams/retour-video/phases/47-voice-annotations/047-UI-SPEC.md) — PRIMARY reference: mode toggle spec, VoiceComposer mount position, voice annotation list item spec
    - apps/web/src/components/coach/videos/VoiceComposer.tsx (just built — import from here)
    - apps/web/src/components/coach/videos/VideoPlayerClient.tsx (Annotation interface — check updated fields)
  </read_first>
  <action>
    Extend `AnnotationPanel.tsx` with the following changes. Read the full current file before editing to avoid regressions.

    STEP A — Imports:
    - Add: `import { VoiceComposer } from './VoiceComposer';`
    - Add Lucide icons: `Type, Mic` (to existing Lucide import line)

    STEP B — Type extensions:
    Extend AnnotationState union's composing and editing states to add `mode: 'text' | 'voice'`:
    ```
    | { status: 'composing'; annotations: Annotation[]; timestamp_s: number; mode: 'text' | 'voice' }
    | { status: 'editing'; annotations: Annotation[]; annotationId: string; content: string; timestamp_s: number; mode: 'text' | 'voice' }
    ```

    Add to AnnotationAction union:
    ```
    | { type: 'SET_MODE'; mode: 'text' | 'voice' }
    | { type: 'VOICE_READY'; transcript: string; audioPath: string }
    ```

    Note: VOICE_READY is fired by the `onVoiceReady` callback — it triggers the save API call directly (not via reducer). The reducer does not handle VOICE_READY; the parent handler calls the API and then dispatches SAVE_SUCCESS.

    STEP C — Reducer updates:
    In annotationReducer:
    - `START_COMPOSE` case: add `mode: 'text'` to returned composing state (default)
    - `START_EDIT` case: add `mode: 'text'` to returned editing state (default)
    - Add `SET_MODE` case: only applies when `state.status === 'composing' || state.status === 'editing'`; returns same state with `mode: action.mode`; for all other statuses return state unchanged

    STEP D — New state in component:
    - Add `const [audioUrlMap, setAudioUrlMap] = useState<Map<string, string>>(new Map())`

    STEP E — Signed URL fetch effect:
    Add a `useEffect` that runs when `state.status === 'list'` and `state.annotations` changes:
    - Filter annotations where `a.type === 'voice' && a.audio_path`
    - For each, if not already in audioUrlMap, fetch `GET ${apiUrl}/coach/videos/annotations/${a.id}/audio-url` with `Authorization: Bearer ${accessToken}`
    - On success: `setAudioUrlMap(prev => new Map(prev).set(a.id, data.signedUrl))`
    - On error: `console.error('[AnnotationPanel] audio-url fetch failed:', a.id, err)` — no UI error; skeleton persists

    STEP F — handleVoiceReady handler:
    Add `async function handleVoiceReady({ transcript, audioPath }: { transcript: string; audioPath: string })`:
    - Guard: state must be composing or editing
    - Extract timestamp_s from state
    - Call `fetch POST ${apiUrl}/coach/videos/${videoId}/annotations` with body `{ timestamp_s, content: transcript, type: 'voice', audio_path: audioPath }`
    - On success: `const annotation: Annotation = await res.json(); dispatch({ type: 'SAVE_SUCCESS', annotation })`
    - On failure: `setErrorMessage(data.error ?? 'Impossible de sauvegarder. Réessayez.')`

    STEP G — Mode toggle (per 047-UI-SPEC.md Component: Mode Toggle [Text] [Mic]):
    In the composer body (where `isComposerVisible === true`), BEFORE the existing timestamp chip, add the mode toggle:
    ```
    Container: flex rounded-md border border-[#E2E0DA] overflow-hidden w-full mb-4
    ```
    Two tab buttons (each `w-1/2 py-1.5 flex items-center justify-center gap-1.5 text-sm font-medium transition-colors duration-100`):
    - Texte tab: active when `currentMode === 'text'` → `bg-white text-[#1C1A17]`; inactive → `bg-[#F0EFE9] text-[#6B6963] hover:text-[#1C1A17]`
      - Icon: `<Type size={14} className="flex-shrink-0" />`, copy: "Texte"
      - onClick: `dispatch({ type: 'SET_MODE', mode: 'text' })`
    - Voix tab: active when `currentMode === 'voice'` → `bg-[#FF5C1A] text-white`; inactive → `bg-[#F0EFE9] text-[#6B6963] hover:text-[#1C1A17]`
      - Icon: `<Mic size={14} className="flex-shrink-0" />`, copy: "Voix"
      - onClick: `dispatch({ type: 'SET_MODE', mode: 'voice' })`

    Extract `currentMode` as a derived const: when `isComposerVisible` → `(state as { mode: 'text' | 'voice' }).mode ?? 'text'`

    STEP H — VoiceComposer mount:
    In the composer body, after the timestamp chip section, conditionally render:
    - When `currentMode === 'text'`: render the existing textarea block (unchanged)
    - When `currentMode === 'voice'`: render `<VoiceComposer timestampSeconds={...} onVoiceReady={handleVoiceReady} onCancel={() => dispatch({ type: 'SET_MODE', mode: 'text' })} accessToken={accessToken} apiUrl={apiUrl} videoId={videoId} />`

    STEP I — Composer footer when mode is 'voice':
    When `isComposerVisible && currentMode === 'voice'`: the footer content from VoiceComposer owns its own footer. Hide the existing [Annuler]/[Enregistrer] buttons when mode is 'voice' (return null or empty div for the footer buttons area). VoiceComposer renders its own footer internally.

    STEP J — Voice annotation list item rendering:
    In the annotation list (`annotations.map(...)`), branch on `a.type === 'voice'`:

    Voice annotation item JSX structure (per 047-UI-SPEC.md Component: Voice Annotation List Item):
    ```
    Container: same as text annotation — px-4 py-3 border-b border-[#E2E0DA] last:border-0 group flex flex-col gap-2 cursor-pointer hover:bg-[#F0EFE9] transition-colors duration-100 relative
    ```

    Line 1 (header row): `flex items-center gap-2`
    - Mic badge: `flex-shrink-0 w-5 h-5 rounded-full bg-[#FFF0E8] flex items-center justify-center`
      - `<Mic size={10} className="text-[#FF5C1A]" />`
    - Timestamp chip: same as text annotation
    - Transcript text: `flex-1 text-sm text-[#1C1A17] line-clamp-2`
    - Action icons (pencil shows disabled tooltip "Ré-enregistrement disponible dans une prochaine version" on hover, trash-2 for delete — same delete logic as text)

    Line 2 (audio player): `mt-1`
    - If `audioUrlMap.get(a.id)` is undefined → skeleton: `<div className="w-full h-8 rounded bg-[#E2E0DA] animate-pulse" />`
    - If URL present → `<audio controls preload="none" src={audioUrlMap.get(a.id)} className="w-full h-8 rounded" onClick={(e) => e.stopPropagation()} />`
      - `onClick` on audio element calls `e.stopPropagation()` to prevent row click from firing

    Left accent strip (same as text annotation, visible on group-hover):
    - `absolute left-0 top-0 bottom-0 w-1 bg-[#FF5C1A] rounded-r opacity-0 group-hover:opacity-100 transition-opacity duration-100`

    For text annotations (default / when `!a.type || a.type === 'text'`): keep existing rendering unchanged.

    IMPORTANT: Do not introduce a separate AnnotationListItem component in this plan — refactor is noted in the spec but out of scope for Phase 47 (both text and voice renderings live inline in the map for now).
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `import { VoiceComposer } from './VoiceComposer'` present in AnnotationPanel.tsx
    - `import { ..., Type, Mic } from 'lucide-react'` updated with Type and Mic
    - `SET_MODE` case in `annotationReducer` switch
    - `mode: 'text' | 'voice'` in composing and editing AnnotationState union types
    - `audioUrlMap` state: `useState<Map<string, string>>(new Map())`
    - `useEffect` that fetches audio-url for all voice annotations on panel mount
    - `handleVoiceReady` async function present that POSTs to `/${videoId}/annotations` with `type: 'voice'` and `audio_path`
    - Mode toggle renders `[Texte][Voix]` above timestamp chip in composer view
    - `<VoiceComposer ... />` rendered when `currentMode === 'voice'`
    - Voice annotation list item has `bg-[#FFF0E8]` mic badge
    - Voice annotation list item has `<audio controls` or skeleton based on audioUrlMap
    - Existing text annotation list rendering unchanged
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>AnnotationPanel extended with mode toggle, voice save flow, voice annotation list with audio player; TypeScript clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AnnotationPanel → GET /annotations/:id/audio-url | Fetches signed URLs per annotation; backend enforces coach/athlete guard |
| handleVoiceReady → POST /:videoId/annotations | Saves annotation with type:voice; backend enforces coach-only write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-P3-01 | Information Disclosure | audioUrlMap stored in component state | accept | Signed URLs are short-lived (15 min) and scoped to this component instance; no persistence |
| T-47-P3-02 | Tampering | type field sent from client in POST body | accept | Backend validates coach_id ownership; type field is informational metadata only |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `grep "SET_MODE\|VOICE_READY\|audioUrlMap\|VoiceComposer" apps/web/src/components/coach/videos/AnnotationPanel.tsx` returns matches for all four
- `grep "audio-url" apps/web/src/components/coach/videos/AnnotationPanel.tsx` returns match (signed URL fetch)
- `grep "type: 'voice'" apps/web/src/components/coach/videos/AnnotationPanel.tsx` returns match (in handleVoiceReady)
- `grep "animate-pulse" apps/web/src/components/coach/videos/AnnotationPanel.tsx` returns match (audio skeleton)
- `grep "audio controls" apps/web/src/components/coach/videos/AnnotationPanel.tsx` returns match
- `grep "type.*voice.*audio_path\|audio_path.*type.*voice" apps/web/src/components/coach/videos/VideoPlayerClient.tsx` returns match
- `rtk tsc --noEmit` from apps/web passes
</verification>

<success_criteria>
- AnnotationPanel composing view has [Texte][Voix] toggle per 047-UI-SPEC.md
- Selecting Voix mode mounts VoiceComposer; saving transcript creates voice annotation
- Voice annotation list items show mic badge + transcript + audio player (or skeleton)
- Signed audio URLs fetched at panel mount from GET /annotations/:id/audio-url
- Annotation interface in VideoPlayerClient.tsx updated with type and audio_path
- TypeScript clean
</success_criteria>

<output>
Create `.planning/workstreams/retour-video/phases/47-voice-annotations/47-03-SUMMARY.md` when done
</output>
