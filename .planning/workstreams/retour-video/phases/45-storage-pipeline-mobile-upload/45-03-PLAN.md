---
phase: "45-storage-pipeline-mobile-upload"
plan: 03
type: execute
wave: 2
depends_on:
  - "45-01"
  - "45-02"
files_modified:
  - plugins/coach/src/screens/VideoListScreen.tsx
  - plugins/coach/src/screens/VideoUploadSheet.tsx
  - apps/mobile/app/(app)/(plugins)/coach/videos.tsx
autonomous: true
requirements:
  - UPLOAD-01
  - UPLOAD-02
  - UPLOAD-03
  - INFRA-02

must_haves:
  truths:
    - "Athlete opens Mon coach plugin (State C) and sees a Vidéos tab listing previously uploaded videos with title, date, and status badge"
    - "Athlete taps Uploader une vidéo FAB, selects or records a video from camera/gallery with H.264 export preset on iOS"
    - "Confirmation bottom sheet appears with auto-suggested title Exercice YYYY-MM-DD, duration preview, and Upload button disabled if title is empty"
    - "During upload, a progress bar shows percent complete (0–100%) via XHR onprogress events"
    - "After upload completes, the video list refreshes and the new video appears with status ready"
  artifacts:
    - path: "plugins/coach/src/screens/VideoListScreen.tsx"
      provides: "Videos tab — list of athlete's uploaded videos + upload FAB"
      exports: ["VideoListScreen"]
    - path: "plugins/coach/src/screens/VideoUploadSheet.tsx"
      provides: "Confirmation bottom sheet — title input + duration + upload progress"
      exports: ["VideoUploadSheet"]
    - path: "apps/mobile/app/(app)/(plugins)/coach/videos.tsx"
      provides: "Expo Router route wrapper for VideoListScreen"
      contains: "VideoListScreen"
  key_links:
    - from: "plugins/coach/src/screens/VideoListScreen.tsx"
      to: "POST /coach/videos/upload-url"
      via: "fetch in handleUpload, then XHR PUT"
      pattern: "coach/videos/upload-url"
    - from: "plugins/coach/src/screens/VideoListScreen.tsx"
      to: "POST /coach/videos/:videoId/complete"
      via: "fetch after XHR resolves"
      pattern: "coach/videos"
    - from: "plugins/coach/src/screens/VideoListScreen.tsx"
      to: "supabase.from('coach_client_videos')"
      via: "useQuery queryFn"
      pattern: "coach_client_videos"
---

<objective>
Build the mobile upload screens inside the coach plugin: VideoListScreen (Vidéos tab with list + FAB) and VideoUploadSheet (confirmation bottom sheet with title input, duration, and XHR upload with progress bar). Add the Expo Router route wrapper. The tab appears only in State C (athlete linked to a coach).

Purpose: Delivers UPLOAD-01 (video picker), UPLOAD-02 (XHR progress bar), UPLOAD-03 (title input), and INFRA-02 (H.264 enforcement via videoExportPreset). This plan is the main user-facing deliverable of Phase 45.
Output: Athlete can pick a video, add a title, and upload it — with real progress feedback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-CONTEXT.md
@.planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-RESEARCH.md

<interfaces>
<!-- Key patterns the executor must replicate. Extracted from codebase. -->

From apps/mobile/app/(app)/(plugins)/coach/dashboard.tsx (route wrapper pattern):
  import CoachScreen from '@ziko/plugin-coach/screens/CoachScreen';
  import { supabase } from '../../../../src/lib/supabase';
  export default function CoachDashboardRoute() {
    return <CoachScreen supabase={supabase} />;
  }
  → videos.tsx must follow this exact pattern with VideoListScreen

From apps/mobile/app/(app)/profile/avatar.tsx (expo-image-picker pattern):
  import * as ImagePicker from 'expo-image-picker';
  await ImagePicker.requestMediaLibraryPermissionsAsync()  — gallery permission
  await ImagePicker.requestCameraPermissionsAsync()        — camera permission
  launchImageLibraryAsync({ mediaTypes: ['videos'], ... }) — string array, not enum
  launchCameraAsync({ ... })                               — camera recording
  result.canceled, result.assets?.[0].uri, result.assets?.[0].duration

From RESEARCH.md Pattern 2 (XHR PUT with progress):
  xhr.open('PUT', signedUrl)
  xhr.setRequestHeader('Content-Type', 'video/mp4')
  xhr.upload.onprogress = (e) => if (e.lengthComputable) onProgress((e.loaded / e.total) * 100)
  xhr.send({ uri: localUri, type: 'video/mp4', name: 'upload.mp4' } as any)
  — React Native: send file URI directly, do NOT use fetch().blob()

From RESEARCH.md Pattern 3 (H.264 export preset — INFRA-02 locked):
  mediaTypes: ['videos']        ← string array (not enum)
  allowsEditing: false          ← MUST be false for videoExportPreset to apply (Pitfall 3)
  videoExportPreset: ImagePicker.VideoExportPreset.H264_1920x1080  ← iOS only, value 7
  videoMaxDuration: 0           ← no limit

From plugins/coach/src/screens/CoachScreen.tsx (plugin screen conventions):
  import { useThemeStore, useTranslation, showAlert } from '@ziko/plugin-sdk'
  import { useAuthStore } from '../../../../apps/mobile/src/stores/authStore'  ← path from plugin
  import { useQuery, useQueryClient } from '@tanstack/react-query'
  theme.background, theme.surface, theme.border, theme.text, theme.muted, theme.primary
  paddingBottom: 100  ← ALL plugin screens use this for tab bar clearance
  showAlert(...)  ← NEVER use Alert from react-native (CLAUDE.md rule)

Design tokens:
  Background: #F7F6F3, Surface: #FFFFFF, Border: #E2E0DA
  Primary: #FF5C1A, Text: #1C1A17, Muted: #6B6963
  Status badge: uploading=spinner, ready=green dot (#2E9E5B), annotated=orange dot (#FF5C1A)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build VideoUploadSheet — confirmation bottom sheet</name>
  <files>plugins/coach/src/screens/VideoUploadSheet.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/avatar.tsx (expo-image-picker permission + launch patterns)
    - plugins/coach/src/screens/CoachScreen.tsx (theme + showAlert usage, React Native Modal pattern lines 1-60)
    - .planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-CONTEXT.md (D-08, D-09, D-10 locked decisions)
    - .planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-RESEARCH.md (Pattern 2 XHR, Pattern 3 H.264 picker, Pitfall 1 Android note, Pitfall 2 SDK 54 note, Pitfall 3 allowsEditing)
  </read_first>
  <action>
    Create plugins/coach/src/screens/VideoUploadSheet.tsx as a React Native component:

    Props interface:
      VideoUploadSheetProps {
        visible: boolean
        onClose: () => void
        onUploadComplete: () => void
        supabase: any
        apiUrl: string
      }

    Internal state: title (string), uploadProgress (number 0–100), isUploading (boolean), pickedAsset ({ uri, durationMs } | null)

    Video picker logic — call this in a useEffect or on mount when visible:
    - Request library permission via ImagePicker.requestMediaLibraryPermissionsAsync()
    - Call launchImageLibraryAsync with: mediaTypes: ['videos'], allowsEditing: false, videoExportPreset: ImagePicker.VideoExportPreset.H264_1920x1080, videoMaxDuration: 0
    - On cancel: call onClose()
    - On success: set pickedAsset = { uri: result.assets[0].uri, durationMs: result.assets[0].duration ?? 0 }

    Camera recording — offer as alternative via ActionSheet-style showAlert:
    - showAlert('Source vidéo', 'Choisir une source', [{ text: 'Annuler', style: 'cancel', onPress: onClose }, { text: 'Galerie', onPress: pickFromLibrary }, { text: 'Caméra', onPress: recordWithCamera }])
    - recordWithCamera: requestCameraPermissionsAsync → launchCameraAsync({ mediaTypes: ['videos'], allowsEditing: false, videoExportPreset: ImagePicker.VideoExportPreset.H264_1920x1080, videoMaxDuration: 0 })

    Auto-suggested title (D-09): format today's date as "Exercice YYYY-MM-DD" using new Date().toISOString().slice(0, 10)
    Initialize title state to this value when component mounts.

    Bottom sheet Modal (using React Native Modal, animationType="slide", presentationStyle="pageSheet"):
    - Title input: TextInput bound to title state, placeholder "Titre de la vidéo", required (D-10)
    - Duration display: show formatted duration from pickedAsset.durationMs ÷ 1000 → "MM:SS"
    - Upload button: disabled when title.trim() === '' OR isUploading (D-10)
    - Progress bar: visible when isUploading — View with width: `${uploadProgress}%`, backgroundColor: theme.primary

    handleUpload function (D-01 flow per CONTEXT.md):
    1. GET auth token: await supabase.auth.getSession() → session.access_token
    2. POST `${apiUrl}/coach/videos/upload-url` with Bearer token, no body needed
    3. Destructure { signedUrl, videoId } from response JSON
    4. XHR PUT: new XMLHttpRequest() → xhr.open('PUT', signedUrl) → xhr.setRequestHeader('Content-Type', 'video/mp4') → xhr.upload.onprogress = (e) => { if (e.lengthComputable) setUploadProgress(Math.round((e.loaded / e.total) * 100)) } → xhr.onload = checkStatus → xhr.onerror = rejectHandler → xhr.send({ uri: localUri, type: 'video/mp4', name: 'upload.mp4' } as any)
    5. After XHR resolves: POST `${apiUrl}/coach/videos/${videoId}/complete` with { title: title.trim(), duration_s: Math.round(durationMs / 1000) }
    6. On success: call onUploadComplete() then onClose()
    7. On any error: showAlert('Erreur', "L'upload a échoué. Réessaie.") — reset isUploading to false

    Follow design system: paddingBottom: 100, showAlert instead of Alert.alert, theme colors from useThemeStore.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p C:/ziko-platform/apps/mobile/tsconfig.json 2>&1 | grep -i "VideoUploadSheet" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - plugins/coach/src/screens/VideoUploadSheet.tsx exports VideoUploadSheet as named export
    - Component uses ImagePicker.VideoExportPreset.H264_1920x1080 (INFRA-02)
    - allowsEditing: false is set in both launchImageLibraryAsync and launchCameraAsync calls
    - mediaTypes: ['videos'] used (not ImagePicker.MediaTypeOptions.Videos — deprecated)
    - XHR PUT uses xhr.send({ uri, type: 'video/mp4', name: 'upload.mp4' } as any) — not fetch().blob()
    - Upload button is disabled when title.trim() === '' (D-10)
    - Auto-title initialized to "Exercice YYYY-MM-DD" format (D-09)
    - showAlert used for all alerts — no Alert.alert import
    - Progress bar View visible when isUploading === true with width tied to uploadProgress
    - TypeScript: no errors for VideoUploadSheet in tsc output
  </acceptance_criteria>
  <done>VideoUploadSheet built with H.264 picker, XHR progress upload, title validation, auto-suggested title</done>
</task>

<task type="auto">
  <name>Task 2: Build VideoListScreen and videos.tsx route wrapper</name>
  <files>plugins/coach/src/screens/VideoListScreen.tsx, apps/mobile/app/(app)/(plugins)/coach/videos.tsx</files>
  <read_first>
    - plugins/coach/src/screens/CoachScreen.tsx (full file — State C structure, theme, query patterns, padding conventions)
    - plugins/coach/src/screens/VideoUploadSheet.tsx (just created — import path, props interface)
    - apps/mobile/app/(app)/(plugins)/coach/dashboard.tsx (route wrapper pattern to mirror)
    - .planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-CONTEXT.md (D-06, D-07 locked decisions — Vidéos tab placement, list content)
    - .planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-RESEARCH.md (Pattern 5 Videos Tab as State C content)
  </read_first>
  <action>
    Create plugins/coach/src/screens/VideoListScreen.tsx:

    Props: VideoListScreenProps { supabase: any }

    Query using useQuery with queryKey ['coach-videos', userId]:
      SELECT id, title, created_at, status FROM coach_client_videos WHERE athlete_id = userId ORDER BY created_at DESC
      via supabase.from('coach_client_videos').select('id, title, created_at, status').eq('athlete_id', userId).order('created_at', { ascending: false })

    VideoRow type: { id: string; title: string; created_at: string; status: 'uploading' | 'ready' | 'annotated' }

    State: showUploadSheet (boolean)

    Status badge render function:
      'uploading' → ActivityIndicator size="small" color={theme.muted} + Text "En cours..."
      'ready'     → View with 8px circle backgroundColor="#2E9E5B" + Text "Prête"
      'annotated' → View with 8px circle backgroundColor={theme.primary} + Text "Annotée"

    List render:
      Each video as a TouchableOpacity card (borderRadius 12, backgroundColor theme.surface, borderWidth 1, borderColor theme.border, marginBottom 8, padding 14, flexDirection row, alignItems center):
        Left: Ionicons name="videocam-outline" size=24 color={theme.muted} with marginRight 12
        Middle (flex 1): title (fontWeight 700, color theme.text), created_at formatted as "DD MMMM YYYY" using date-fns format + fr locale
        Right: status badge

    Empty state: when videos.length === 0 and not loading — centered View with Ionicons "cloud-upload-outline" size=48 color={theme.muted}, Text "Aucune vidéo uploadée", Text "Appuie sur le bouton + pour uploader ta première vidéo"

    FAB (Floating Action Button) for upload:
      Position: absolute, bottom 24, right 20 (on top of the list, NOT inside ScrollView)
      Style: width 56, height 56, borderRadius 28, backgroundColor theme.primary, alignItems center, justifyContent center, elevation 4
      Icon: Ionicons name="add" size=28 color="#fff"
      onPress: setShowUploadSheet(true)

    Render VideoUploadSheet when showUploadSheet === true:
      Pass: visible={showUploadSheet}, onClose={() => setShowUploadSheet(false)}, supabase={supabase}, apiUrl={process.env.EXPO_PUBLIC_API_URL ?? ''}, onUploadComplete={() => { queryClient.invalidateQueries({ queryKey: ['coach-videos'] }); setShowUploadSheet(false); }}

    Screen wrapper:
      SafeAreaView + ScrollView with contentContainerStyle={{ paddingHorizontal: 16, paddingBottom: 100 }}
      Header: uppercase label "MES VIDÉOS" (fontSize 10, fontWeight 800, letterSpacing 1.2, color theme.primary)

    Create apps/mobile/app/(app)/(plugins)/coach/videos.tsx following dashboard.tsx pattern:
      import VideoListScreen from '@ziko/plugin-coach/screens/VideoListScreen';
      import { supabase } from '../../../../src/lib/supabase';
      export default function CoachVideosRoute() {
        return <VideoListScreen supabase={supabase} />;
      }
  </action>
  <verify>
    <automated>npx tsc --noEmit -p C:/ziko-platform/apps/mobile/tsconfig.json 2>&1 | grep -iE "VideoListScreen|CoachVideosRoute" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - plugins/coach/src/screens/VideoListScreen.tsx exports VideoListScreen as default export
    - apps/mobile/app/(app)/(plugins)/coach/videos.tsx exists with CoachVideosRoute as default export
    - VideoListScreen queries supabase.from('coach_client_videos') with athlete_id filter and created_at DESC ordering
    - Status badges render: 'uploading' shows ActivityIndicator, 'ready' shows green dot (#2E9E5B), 'annotated' shows orange dot (theme.primary)
    - FAB is positioned absolutely at bottom 24 / right 20 with theme.primary background
    - VideoUploadSheet rendered with correct props when showUploadSheet === true
    - queryClient.invalidateQueries({ queryKey: ['coach-videos'] }) called onUploadComplete
    - paddingBottom: 100 applied (tab bar clearance)
    - showAlert used — no Alert.alert
    - TypeScript: no errors for VideoListScreen or CoachVideosRoute in tsc output
  </acceptance_criteria>
  <done>VideoListScreen and route wrapper built, video list queries coach_client_videos, upload sheet wired, FAB visible</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile→/coach/videos/upload-url | JWT Bearer sent from mobile — auth middleware validates on Hono side |
| XHR PUT to signedUrl | Video bytes go directly to Supabase signed URL — mobile client sends, Hono not involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09 | Information Disclosure | signedUrl exposure in mobile | accept | Signed URL expires in 900s (D-03); limited window; no PII in URL |
| T-45-10 | Denial of Service | Very large video upload blocking UI | accept | No size cap in v1.13 (per CONTEXT.md deferred); Supabase Pro handles up to 5 GB; progress bar gives user feedback |
| T-45-11 | Spoofing | Android HEVC format bypass | accept | videoExportPreset is iOS-only; modern Android defaults to H.264; HEVC format error surfaced at Phase 46 player — acceptable risk for v1.13 (Pitfall 1) |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — all deps already present |
</threat_model>

<verification>
After both tasks complete:
1. npx tsc --noEmit -p apps/mobile/tsconfig.json — zero errors
2. Confirm apps/mobile/app/(app)/(plugins)/coach/videos.tsx exists
3. Confirm plugins/coach/src/screens/VideoListScreen.tsx exists
4. Confirm plugins/coach/src/screens/VideoUploadSheet.tsx exists
5. grep "coach_client_videos" plugins/coach/src/screens/VideoListScreen.tsx — returns query line
6. grep "H264_1920x1080" plugins/coach/src/screens/VideoUploadSheet.tsx — returns picker call
</verification>

<success_criteria>
- VideoListScreen renders video list from coach_client_videos table with title, date, status badge
- VideoUploadSheet uses H.264 export preset (INFRA-02), XHR progress upload (UPLOAD-02), required title (UPLOAD-03), auto-suggested title (D-09)
- FAB triggers upload sheet (D-07)
- Route wrapper videos.tsx follows dashboard.tsx pattern
- TypeScript clean
- Both screens use showAlert — no Alert.alert
- paddingBottom: 100 applied
</success_criteria>

<output>
Create `.planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-03-SUMMARY.md` when done
</output>
