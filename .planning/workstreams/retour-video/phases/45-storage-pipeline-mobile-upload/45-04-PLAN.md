---
phase: "45-storage-pipeline-mobile-upload"
plan: 04
type: execute
wave: 2
depends_on:
  - "45-02"
files_modified:
  - plugins/coach/src/screens/CoachScreen.tsx
autonomous: false
requirements:
  - UPLOAD-04

must_haves:
  truths:
    - "When the coach opens the Mon coach plugin in State C, their Expo push token is registered via POST /notifications/token"
    - "The push token registration uses the existing POST /notifications/token endpoint — no new endpoint created"
    - "Coach receives a push notification on their device when an athlete completes an upload"
  artifacts:
    - path: "plugins/coach/src/screens/CoachScreen.tsx"
      provides: "Push token registration side-effect in State C"
      contains: "notifications/token"
  key_links:
    - from: "plugins/coach/src/screens/CoachScreen.tsx"
      to: "POST /notifications/token"
      via: "useEffect when link is active (State C)"
      pattern: "notifications/token"
    - from: "backend/api/src/services/notificationService.ts"
      to: "notification_tokens table"
      via: "notificationService.send() → looks up recipientUserId's active tokens"
      pattern: "notification_tokens"
---

<objective>
Wire push token registration into CoachScreen (State C). When a coach opens the plugin in linked state, their device registers its Expo push token via POST /notifications/token. This enables the push notification sent by /coach/videos/:videoId/complete to reach the coach's device.

Purpose: Completes UPLOAD-04 — the coach receives a push notification when an athlete uploads. The backend push pipeline already exists (notificationService + notification_tokens table). This plan adds only the mobile side: registering the token at the point where the coach enters State C.
Output: CoachScreen registers push token in a useEffect, leveraging the existing /notifications/token endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-CONTEXT.md
@.planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-RESEARCH.md

<interfaces>
<!-- Key contracts for executor. Extracted from codebase. -->

From backend/api/src/routes/notifications.ts (POST /notifications/token):
  Body: { token: string, platform: 'ios' | 'android', deviceId: string }
  token must match regex: /^ExponentPushToken\[.+\]$/
  Returns: 200 { registered: true }
  Auth: Bearer JWT required

From RESEARCH.md Pattern: Push token registration (mobile side):
  import * as Notifications from 'expo-notifications'
  import Constants from 'expo-constants'
  import * as Device from 'expo-device'
  const { status } = await Notifications.requestPermissionsAsync()
  if (status !== 'granted') return — do not block UI
  const projectId = Constants?.expoConfig?.extra?.eas?.projectId ?? Constants?.easConfig?.projectId
    — value "9b672c1a-10c4-4d66-882c-b9a08294650f" (from RESEARCH.md)
  const pushToken = (await Notifications.getExpoPushTokenAsync({ projectId })).data
  deviceId: stable UUID from MMKV — create once with MMKV.set('deviceId', uuid) if not present

From RESEARCH.md State of the Art:
  D-04/D-05 in CONTEXT.md are SUPERSEDED — do NOT add expo_push_token column to user_profiles
  Use POST /notifications/token (existing endpoint) instead
  Use notification_tokens table (migration 054) — already production-ready

From plugins/coach/src/screens/CoachScreen.tsx:
  - State C (linked state) is when data?.link is not null
  - useEffect with [link?.id] as dependency is the correct pattern to trigger once on link activation
  - supabase.auth.getSession() for auth token
  - process.env.EXPO_PUBLIC_API_URL for base URL
  - showAlert for any user-facing error — never Alert.alert

MMKV pattern (from apps/mobile/src/lib/ or stores):
  import { MMKV } from 'react-native-mmkv'
  const storage = new MMKV()
  — use key 'deviceId' to store/retrieve stable UUID
  — if not present: const id = randomUUID(); storage.set('deviceId', id); return id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register push token when coach enters State C</name>
  <files>plugins/coach/src/screens/CoachScreen.tsx</files>
  <read_first>
    - plugins/coach/src/screens/CoachScreen.tsx (full file — understand State C detection via data?.link, existing useEffect patterns, imports, supabase session access, EXPO_PUBLIC_API_URL usage)
    - backend/api/src/routes/notifications.ts (POST /notifications/token body shape, 400 error conditions)
    - .planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-RESEARCH.md (Push token registration code example, Environment Availability section — MMKV already installed, expo-notifications already installed)
    - .planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-CONTEXT.md (D-04, D-05 — note that RESEARCH.md supersedes these with existing notificationService infrastructure)
  </read_first>
  <action>
    Add a push token registration useEffect to CoachScreen.tsx that fires when the coach is in State C (link is active).

    New imports to add (only if not already present):
      import * as Notifications from 'expo-notifications'
      import * as Device from 'expo-device'
      import Constants from 'expo-constants'
      import { MMKV } from 'react-native-mmkv'
      import { Platform, randomUUID } } — Platform already imported; randomUUID from 'crypto' is not available in RN — use expo-crypto OR use a simple UUID v4 implementation: `${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`

    Helper function getOrCreateDeviceId():
      const storage = new MMKV()
      const existing = storage.getString('deviceId')
      if (existing) return existing
      const id = [Date.now().toString(36), Math.random().toString(36).slice(2), Math.random().toString(36).slice(2)].join('-')
      storage.set('deviceId', id)
      return id

    New useEffect (dependency: [link?.id]):
      if (!link) return  — only run when linked (State C)
      if (!Device.isDevice) return  — push not supported on simulator; silently skip

      async function registerToken() {
        try {
          const { status } = await Notifications.requestPermissionsAsync()
          if (status !== 'granted') return  — silently skip if denied; do not prompt user

          const projectId = Constants?.expoConfig?.extra?.eas?.projectId
            ?? Constants?.easConfig?.projectId
            ?? '9b672c1a-10c4-4d66-882c-b9a08294650f'

          const expoPushToken = (await Notifications.getExpoPushTokenAsync({ projectId })).data
          const deviceId = getOrCreateDeviceId()
          const { data: { session } } = await supabase.auth.getSession()
          if (!session?.access_token) return

          await fetch(`${process.env.EXPO_PUBLIC_API_URL}/notifications/token`, {
            method: 'POST',
            headers: { Authorization: `Bearer ${session.access_token}`, 'Content-Type': 'application/json' },
            body: JSON.stringify({ token: expoPushToken, platform: Platform.OS, deviceId }),
          })
          — do NOT await aggressively or throw: push registration is best-effort; failure must not block UI
        } catch {
          — silently swallow: push token registration failure must not surface to user
        }
      }
      registerToken()

    Place the useEffect AFTER the existing queries and handlers, before the return statement.
    Do NOT modify any existing rendering, queries, or handlers.
  </action>
  <verify>
    <automated>grep -n "notifications/token" C:/ziko-platform/plugins/coach/src/screens/CoachScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CoachScreen.tsx contains import for expo-notifications (Notifications)
    - CoachScreen.tsx contains import for expo-device (Device)
    - CoachScreen.tsx contains "notifications/token" in a fetch call
    - useEffect dependency includes link?.id (fires on State C activation)
    - if (!Device.isDevice) return guard is present (simulator safety)
    - if (!link) return guard is present (State C check)
    - Push registration errors are caught and silently swallowed — not surfaced to user
    - No Alert.alert calls added — showAlert used for any user-facing messages
    - cd apps/mobile && npx tsc --noEmit exits 0 (or: npx tsc --noEmit -p apps/mobile/tsconfig.json)
    - Existing CoachScreen functionality unchanged (no regression to query or render logic)
  </acceptance_criteria>
  <done>Push token registration useEffect added to CoachScreen, fires on State C activation, errors swallowed silently</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end push notification verification</name>
  <read_first>
    - .planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-CONTEXT.md (prerequisite: Expo Dev Build required)
  </read_first>
  <what-built>
    All four plans of Phase 45 are now implemented:
    - Migration 057: coach_client_videos + coach_video_annotations tables + RLS + storage object policies
    - Hono endpoints: POST /coach/videos/upload-url + POST /coach/videos/:videoId/complete
    - Mobile screens: VideoListScreen + VideoUploadSheet (H.264, XHR progress, title input)
    - Push token registration in CoachScreen (State C)
  </what-built>
  <how-to-verify>
    Requires: Expo Dev Build (EAS) running on physical device + Supabase Pro plan active

    1. Open the Mon coach plugin on a physical iOS device as an athlete (State C — linked to a coach)
    2. Navigate to the Vidéos tab (should appear in plugin navigation)
    3. Tap the orange FAB (+) button
    4. Expected: action sheet appears asking "Galerie" or "Caméra"
    5. Select "Galerie" — pick a short video from camera roll
    6. Expected: confirmation bottom sheet appears with title pre-filled as "Exercice YYYY-MM-DD"
    7. Title field is editable — try clearing it and confirming Upload button becomes disabled
    8. Restore title and tap "Uploader"
    9. Expected: progress bar appears and fills to 100%
    10. Expected: video appears in the list with status "Prête" (green dot)
    11. On the coach's device — verify a push notification arrives: "📹 Nouvelle vidéo — [athleteName] a uploadé une nouvelle vidéo : [title]"

    If Expo Dev Build is not available yet:
    - Confirm Hono endpoints respond correctly via curl (replace JWT with a valid athlete token):
      curl -X POST http://localhost:3000/coach/videos/upload-url -H "Authorization: Bearer JWT" → should return { signedUrl, videoId, path }
    - Confirm TypeScript clean: npx tsc --noEmit -p apps/mobile/tsconfig.json
    - Confirm backend tests pass: cd backend/api && npx vitest run
  </how-to-verify>
  <resume-signal>
    Type "approved" if upload flow works end-to-end OR if Expo Dev Build is not yet available and you want to defer the physical device test to a later QA session.
    Type "issues: [describe]" to report problems for debugging.
  </resume-signal>
  <acceptance_criteria>
    - Vidéos tab appears in Mon coach plugin when athlete is in State C
    - FAB triggers video picker
    - Confirmation sheet shows auto-suggested title and Upload button disabled when title empty
    - XHR progress bar reaches 100%
    - Uploaded video appears in list with status "Prête"
    - Coach push notification delivered (or deferred to Dev Build QA session)
    - Hono endpoints return correct shapes
  </acceptance_criteria>
  <done>Phase 45 end-to-end flow verified (or physical device test explicitly deferred)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile→/notifications/token | Coach device sends Expo push token — validated by regex on Hono side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-12 | Spoofing | Push token from non-device (simulator) | accept | Device.isDevice guard added — token not registered from simulator; low-value target |
| T-45-13 | Denial of Service | Push token spam on every render | mitigate | useEffect depends on [link?.id] — fires once on State C activation, not on every re-render |
| T-45-14 | Information Disclosure | ExpoPushToken leaked in logs | accept | Push token is ephemeral, device-bound; exposed only in fetch body to authenticated Hono endpoint; not logged in service.ts |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new packages — expo-notifications, expo-device, expo-constants, react-native-mmkv all already installed |
</threat_model>

<verification>
After both tasks complete:
1. grep "notifications/token" plugins/coach/src/screens/CoachScreen.tsx — returns fetch line
2. grep "Device.isDevice" plugins/coach/src/screens/CoachScreen.tsx — returns guard line
3. npx tsc --noEmit -p apps/mobile/tsconfig.json — zero errors
4. cd backend/api && npx vitest run — full suite green (no regressions)
</verification>

<success_criteria>
- CoachScreen registers push token on State C activation via POST /notifications/token
- Existing notification_tokens infrastructure used — no user_profiles.expo_push_token column added (RESEARCH.md override of D-04)
- Device.isDevice guard prevents simulator registration
- Token registration errors swallowed silently — UX not disrupted
- TypeScript clean across mobile app
- E2E verified or explicitly deferred to Dev Build QA session
</success_criteria>

<output>
Create `.planning/workstreams/retour-video/phases/45-storage-pipeline-mobile-upload/45-04-SUMMARY.md` when done
</output>
