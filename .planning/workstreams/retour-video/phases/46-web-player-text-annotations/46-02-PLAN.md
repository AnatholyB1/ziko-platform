---
phase: 46
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - apps/web/src/components/coach/ClientTabStrip.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx
  - apps/web/src/components/coach/videos/ClientNotesPanelConditional.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/videos/page.tsx
  - apps/web/src/components/coach/videos/VideoListClient.tsx
autonomous: true
requirements: [PLAYER-01]

must_haves:
  truths:
    - "A 'Vidéos' tab appears in ClientTabStrip and stays active on both /videos and /videos/[videoId]"
    - "The notes panel (w-72) is hidden on any /videos/* sub-page"
    - "VideoListPage renders loading skeletons, empty state, error state, and populated list"
    - "Clicking a ready or annotated video row navigates to /videos/[videoId]"
    - "Uploading rows have no chevron and are not clickable"
    - "GSAP list-item stagger fires after data loads"
  artifacts:
    - path: "apps/web/src/components/coach/ClientTabStrip.tsx"
      provides: "11-tab strip with Vidéos tab; active detection uses pathname.includes('/videos') for that tab"
    - path: "apps/web/src/components/coach/videos/ClientNotesPanelConditional.tsx"
      provides: "Client component wrapping ClientNotesPanel that returns null when pathname.includes('/videos')"
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/videos/page.tsx"
      provides: "Server route rendering VideoListClient"
    - path: "apps/web/src/components/coach/videos/VideoListClient.tsx"
      provides: "Client component: fetch + loading/empty/error/populated states + status badges + GSAP stagger"
  key_links:
    - from: "layout.tsx"
      to: "ClientNotesPanelConditional"
      via: "import and render in place of ClientNotesPanel"
      pattern: "ClientNotesPanelConditional"
    - from: "VideoListClient"
      to: "/coach/clients/:clientId/videos"
      via: "fetch with Authorization Bearer accessToken header"
      pattern: "fetch.*coach/clients.*videos"
---

<objective>
Wire the "Vidéos" tab into the existing client detail navigation, conditionally hide the notes panel on /videos/* pages, and build the VideoListPage with all required states (loading, empty, error, populated) per the UI-SPEC.

Purpose: Establishes the navigation entry point and list page that coaches use to browse athlete videos. Unblocks 46-03 (player page).
Output: Updated ClientTabStrip, new ClientNotesPanelConditional, videos/page.tsx, VideoListClient.tsx.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-video/ROADMAP.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-CONTEXT.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/046-UI-SPEC.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClientTabStrip + layout notes panel fix</name>
  <read_first>
    - apps/web/src/components/coach/ClientTabStrip.tsx (read full file — modify TABS array and isActive detection)
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/layout.tsx (read full file — locate notes panel div around line 71)
    - apps/web/src/components/coach/ClientNotesPanel.tsx (read props interface — needed for ClientNotesPanelConditional)
  </read_first>
  <action>
    In ClientTabStrip.tsx:
    1. Append `{ key: 'videos', label: 'Vidéos' }` to the TABS array (after vocal entry, making it tab 11).
    2. Change the isActive detection: replace `const isActive = pathname.endsWith(\`/\${tab.key}\`)` with:
       `const isActive = tab.key === 'videos' ? pathname.includes('/videos') : pathname.endsWith(\`/\${tab.key}\`);`
    The strip already has `overflow-x-auto scrollbar-none` — no layout change needed.

    Create apps/web/src/components/coach/videos/ClientNotesPanelConditional.tsx as a 'use client' component:
    - Import usePathname from 'next/navigation' and ClientNotesPanel from '@/components/coach/ClientNotesPanel'
    - Accept same props as ClientNotesPanel (clientId: string, initialNote: string, initialTags: array, apiUrl: string)
    - Return null when pathname.includes('/videos'), otherwise return <ClientNotesPanel {...props} />

    In layout.tsx:
    - Replace the import of ClientNotesPanel with ClientNotesPanelConditional from '@/components/coach/videos/ClientNotesPanelConditional'
    - Replace <ClientNotesPanel ...props> with <ClientNotesPanelConditional ...props> — pass all existing props unchanged
    - The outer `<div className="hidden lg:block w-72 shrink-0">` stays; only the inner component changes
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ClientTabStrip.tsx TABS array has exactly 11 entries ending with `{ key: 'videos', label: 'Vidéos' }`
    - ClientTabStrip.tsx isActive logic contains `pathname.includes('/videos')` for the videos tab
    - ClientNotesPanelConditional.tsx exists with 'use client' directive and usePathname import
    - ClientNotesPanelConditional returns null when pathname includes '/videos'
    - layout.tsx imports ClientNotesPanelConditional (no longer imports ClientNotesPanel directly)
    - tsc --noEmit exits 0 for apps/web
  </acceptance_criteria>
  <done>Type check passes; Vidéos tab appears in strip; notes panel hidden on /videos/* routes</done>
</task>

<task type="auto">
  <name>Task 2: videos/page.tsx + VideoListClient.tsx</name>
  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx (server route pattern to copy)
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/ClientProgramsContent.tsx (client component skeleton, fetch pattern, card + empty state pattern)
    - .planning/workstreams/retour-video/phases/46-web-player-text-annotations/046-UI-SPEC.md (Screen 1 full spec — loading skeletons, empty, error, populated, status badges, GSAP stagger)
  </read_first>
  <action>
    Create apps/web/src/app/[locale]/(coach)/coach/clients/[id]/videos/page.tsx:
    - Thin server component — async function VideosPage({ params }) { const { id } = await params; return <VideoListClient clientId={id} />; }
    - Import VideoListClient from '@/components/coach/videos/VideoListClient'
    - No SSR data fetch needed — the list is fetched client-side for better loading UX (avoids blocking page render)
    - No force-dynamic needed — parent layout.tsx already sets it

    Create apps/web/src/components/coach/videos/VideoListClient.tsx as a 'use client' component:
    Props: { clientId: string }
    Behavior:
    - On mount, fetch `${apiUrl}/coach/clients/${clientId}/videos` with Authorization Bearer (get accessToken via useSession or pass through supabase client — follow programs/ClientProgramsContent.tsx pattern for getting the token)
    - Loading state: render 3 skeleton rows (video icon placeholder w-6 h-6 bg-[#E2E0DA] animate-pulse, title line w-48 h-[14px] bg-[#E2E0DA] animate-pulse, date line w-24 h-[12px] bg-[#E2E0DA] animate-pulse, badge placeholder w-20 h-6 bg-[#E2E0DA] animate-pulse) — no orange on skeletons
    - Error state: alert-circle icon (Lucide) 24px text-[#EF4444], heading "Impossible de charger les vidéos" 14px/600, body "Vérifiez votre connexion et réessayez." 12px muted, "Réessayer" button that re-triggers fetch
    - Empty state: video icon 40px text-muted opacity-40, H2 "Aucune vidéo pour l'instant" 18px/600, body "L'athlète peut envoyer ses vidéos depuis l'application mobile — elles apparaîtront ici." 14px muted max-w-[320px]
    - Populated: card container bg-white rounded-lg border border-[#E2E0DA] shadow-sm overflow-hidden; page H1 "Vidéos de [clientName]" 22px/600; caption "[N] vidéo(s)" 12px text-muted; each row px-4 py-3 flex items-center gap-4 min-h-[56px] border-b last:border-0; film icon (Lucide) 24px text-muted; title 14px/600 text-[#1C1A17]; date 12px text-muted; status badge (see below); chevron-right (Lucide) 16px text-muted for ready/annotated rows
    - Status badges — uploading: bg-[#FEF3C7] text-[#92400E] "En cours d'envoi"; ready: bg-[#DCFCE7] text-[#166534] "Prête à annoter"; annotated: bg-[#FFF0E8] text-[#C2410C] "Annotée"
    - Row click: ready/annotated rows navigate to `/coach/clients/${clientId}/videos/${video.id}` via router.push; uploading rows: cursor-default, no chevron, not clickable
    - Row hover (clickable rows only): hover:bg-[#F0EFE9] transition-colors duration-100
    - GSAP stagger after data loads: gsap.from('.video-list-item', { y: 8, opacity: 0, duration: 0.15, stagger: 0.04, ease: 'power2.out' })
    - Page entrance: gsap.from(listRef.current, { y: 16, opacity: 0, duration: 0.2, ease: 'power2.out' }) on mount
    - Use useRouter from 'next/navigation' for navigation; use Lucide icons (film, chevron-right, alert-circle, video) — these are already in apps/web
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: apps/web/src/app/[locale]/(coach)/coach/clients/[id]/videos/page.tsx
    - File exists: apps/web/src/components/coach/videos/VideoListClient.tsx
    - VideoListClient.tsx has 'use client' directive
    - VideoListClient.tsx contains gsap.from('.video-list-item' string (GSAP stagger)
    - VideoListClient.tsx contains bg-[#FEF3C7] (uploading badge), bg-[#DCFCE7] (ready badge), bg-[#FFF0E8] (annotated badge)
    - VideoListClient.tsx contains animate-pulse (skeleton loading)
    - No skeleton uses bg-primary or #FF5C1A — skeletons use bg-[#E2E0DA] only
    - tsc --noEmit exits 0 for apps/web
  </acceptance_criteria>
  <done>Type check passes; VideoListPage renders all states correctly; tab navigation works; notes panel hides on /videos/*</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Hono GET /coach/clients/:clientId/videos | Coach JWT required; server validates coach_id ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05 | Information Disclosure | VideoListClient fetch | mitigate | Authorization Bearer header sent on every fetch; token from Supabase session |
| T-46-06 | Tampering | ClientNotesPanelConditional | accept | pathname check is UI-only; notes panel data is separately auth-gated by the API |
| T-46-SC | Tampering | npm installs | mitigate | no new packages — Lucide and gsap already in apps/web |
</threat_model>

<verification>
npx tsc --noEmit -p apps/web/tsconfig.json exits 0.
VideoListClient.tsx file contains: 'use client', animate-pulse, gsap.from, status badge classes, hover:bg-[#F0EFE9].
ClientTabStrip.tsx contains: 'Vidéos' in TABS array, pathname.includes('/videos') for active detection.
</verification>

<success_criteria>
- tsc --noEmit for apps/web exits 0
- ClientTabStrip has 11 tabs with Vidéos last
- ClientNotesPanelConditional returns null on /videos/* paths
- VideoListClient renders 3 skeleton rows while loading, empty state with French copy, error state with retry, populated list with status badges and GSAP stagger
- Clicking ready/annotated row navigates to /videos/[videoId]
- No orange (#FF5C1A) used in skeleton fill colors
</success_criteria>

<output>
Create `.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-02-SUMMARY.md` when done.
</output>
