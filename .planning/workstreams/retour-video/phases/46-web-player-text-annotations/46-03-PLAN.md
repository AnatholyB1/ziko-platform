---
phase: 46
plan: 03
type: execute
wave: 2
depends_on: [46-01, 46-02]
files_modified:
  - apps/web/package.json
  - apps/web/src/app/globals.css
  - apps/web/src/components/coach/videos/VideoPlayerClient.tsx
  - apps/web/src/components/coach/videos/VideoPlayer.tsx
  - apps/web/src/components/coach/videos/AnnotatedTimeSlider.tsx
  - apps/web/src/components/coach/videos/AnnotationPanel.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/videos/[videoId]/page.tsx
autonomous: true
requirements: [PLAYER-02, ANNOT-01, ANNOT-02, ANNOT-03]

must_haves:
  truths:
    - "Coach can open a video and use the Vidstack player to play, pause, and scrub"
    - "Orange annotation dots appear on the scrub bar at correct timecode positions (gated on duration > 0)"
    - "Hovering a dot shows a tooltip with [MM:SS] — [first 60 chars of text]"
    - "Clicking a dot or an annotation list item seeks the player to that timecode"
    - "Coach can pause → click 'Annoter à ce moment' → type text → save (creates annotation via POST)"
    - "Coach can edit an existing annotation (PATCH) or delete it (DELETE) from the panel"
    - "Coach clicks 'Envoyer le retour' → panel shows 'Retour envoyé' (green chip, disabled)"
    - "Notes panel is hidden on the player page; 2/3 player / 1/3 annotation panel layout"
    - "'Annoter à ce moment' is disabled when the video is playing; enabled only when paused"
  artifacts:
    - path: "apps/web/src/components/coach/videos/VideoPlayerClient.tsx"
      provides: "2/3+1/3 layout wrapping MediaPlayer; passes signedUrl, annotations, accessToken, apiUrl to children"
    - path: "apps/web/src/components/coach/videos/VideoPlayer.tsx"
      provides: "@vidstack/react MediaPlayer + MediaProvider + DefaultVideoLayout wrapper"
    - path: "apps/web/src/components/coach/videos/AnnotatedTimeSlider.tsx"
      provides: "TimeSlider.Root + absolute orange dot overlay; gated on duration > 0"
    - path: "apps/web/src/components/coach/videos/AnnotationPanel.tsx"
      provides: "useReducer state machine (list/composing/editing/sending/sent) + GSAP entrance + all 5 states per UI-SPEC"
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/videos/[videoId]/page.tsx"
      provides: "Server component SSR-fetching signedUrl + annotations + video; passes as props to VideoPlayerClient"
  key_links:
    - from: "VideoPlayerClient.tsx"
      to: "MediaPlayer (Vidstack)"
      via: "<MediaPlayer src={signedUrl}> wrapping both player column and AnnotationPanel"
      pattern: "MediaPlayer"
    - from: "AnnotationPanel.tsx"
      to: "useMediaRemote / useMediaState"
      via: "called inside MediaPlayer subtree (Risk 2 mitigation)"
      pattern: "useMediaRemote|useMediaState"
    - from: "AnnotatedTimeSlider.tsx"
      to: "duration"
      via: "useMediaState('duration') gating dot render"
      pattern: "useMediaState.*duration"
---

<objective>
Install @vidstack/react@next, build the video player page with the 2/3/1/3 layout, implement AnnotatedTimeSlider with orange dot overlays, and build the full AnnotationPanel state machine with all 5 states. Both player and panel are wrapped in a single MediaPlayer root so Vidstack hooks work in the panel.

Purpose: Delivers the core coach annotation workflow — watching video + adding/editing/deleting timecoded text annotations + sending feedback.
Output: VideoPlayerPage with fully functional player and annotation panel.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-CONTEXT.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-RESEARCH.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/046-UI-SPEC.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vidstack/react@next + CSS + server page + layout scaffold</name>
  <read_first>
    - apps/web/package.json (verify @vidstack/react is NOT already installed; confirm react version is 19.x)
    - apps/web/src/app/globals.css (read full file — add Vidstack CSS after existing imports)
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/page.tsx (SSR fetch + pass-props pattern to copy for VideoPlayerPage)
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx (thin server page pattern)
  </read_first>
  <action>
    Install package: cd apps/web && npm install @vidstack/react@next
    This installs version 1.15.1 (next tag). Do NOT install @vidstack/react (latest=0.6.15 requires React 18 — project uses React 19.2.6).
    Verify package.json shows "@vidstack/react": "^1.15.1" after install.

    Add to apps/web/src/app/globals.css (immediately after the first @import or @plugin line, before any custom vars):
    ```
    @import '@vidstack/react/player/styles/default/theme.css';
    @import '@vidstack/react/player/styles/default/layouts/video.css';
    @plugin '@vidstack/react/tailwind.cjs';
    ```

    Create apps/web/src/app/[locale]/(coach)/coach/clients/[id]/videos/[videoId]/page.tsx as a server component:
    - async VideoPlayerPage({ params: Promise<{ locale: string; id: string; videoId: string }> })
    - await params to get clientId (id) and videoId
    - Get Supabase session to obtain JWT: createServerSupabase() → session?.access_token
    - apiUrl from process.env.NEXT_PUBLIC_API_URL ?? ''
    - Parallel SSR fetch (Promise.all) with cache: 'no-store' and Authorization Bearer:
      * GET ${apiUrl}/coach/videos/${videoId}/signed-url → signedUrl string
      * GET ${apiUrl}/coach/videos/${videoId}/annotations → annotations array
      * GET ${apiUrl}/coach/clients/${clientId}/videos → find the specific videoId entry for title/duration/status
    - Handle fetch errors with try/catch (console.error, fallback to empty values)
    - Return <VideoPlayerClient clientId={clientId} videoId={videoId} signedUrl={signedUrl} annotations={annotations} video={video} accessToken={jwt} apiUrl={apiUrl} />
    - Import VideoPlayerClient from '@/components/coach/videos/VideoPlayerClient'
    - Import createServerSupabase from '@/lib/supabase/server' (same pattern as programs/page.tsx)
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -r "@vidstack/react" apps/web/package.json && grep "@import '@vidstack" apps/web/src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json contains "@vidstack/react" with version matching "^1" (not "^0")
    - apps/web/src/app/globals.css contains `@import '@vidstack/react/player/styles/default/theme.css'`
    - apps/web/src/app/globals.css contains `@plugin '@vidstack/react/tailwind.cjs'`
    - File exists: apps/web/src/app/[locale]/(coach)/coach/clients/[id]/videos/[videoId]/page.tsx
    - VideoPlayerPage performs Promise.all with 3 fetch calls using Authorization Bearer header
    - npx tsc --noEmit -p apps/web/tsconfig.json exits 0 after stubs are in place
  </acceptance_criteria>
  <done>@vidstack/react@next installed; CSS imported in globals.css; VideoPlayerPage server component created</done>
</task>

<task type="auto">
  <name>Task 2: VideoPlayer + AnnotatedTimeSlider + VideoPlayerClient layout</name>
  <read_first>
    - apps/web/src/components/coach/vocal/VocalRetourPanel.tsx (GSAP entrance pattern: panelRef + gsap.from y:16 opacity:0)
    - .planning/workstreams/retour-video/phases/46-web-player-text-annotations/046-UI-SPEC.md (Component 2a VideoPlayer + AnnotatedTimeSlider full spec)
    - .planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-RESEARCH.md (Risk 2: both player + panel must be inside MediaPlayer; Risk 3: duration > 0 gate)
  </read_first>
  <action>
    Create apps/web/src/components/coach/videos/VideoPlayer.tsx as 'use client':
    - Imports: MediaPlayer, MediaProvider, useMediaState, useMediaRemote from '@vidstack/react'; DefaultVideoLayout, defaultLayoutIcons from '@vidstack/react/player/layouts/default'; AnnotatedTimeSlider from './AnnotatedTimeSlider'
    - Props: { src: string; annotations: Annotation[]; children: React.ReactNode } — children slot allows AnnotationPanel to live inside MediaPlayer tree
    - Returns <MediaPlayer src={src} className="w-full"> wrapping a flex layout: left column (flex-[2] min-w-0) containing <MediaProvider className="aspect-video bg-black rounded-lg" /> + <DefaultVideoLayout icons={defaultLayoutIcons} /> + <AnnotatedTimeSlider annotations={annotations} />; right column is {children}
    - CRITICAL: do NOT put AnnotationPanel here — it is passed as children so the parent (VideoPlayerClient) controls layout
    - GSAP entrance on the player container: gsap.from(containerRef.current, { y: 16, opacity: 0, duration: 0.2, ease: 'power2.out' }) in useEffect on mount
    - Error state (signedUrl empty string): render aspect-video div with bg-[#F7F6F3] border border-[#E2E0DA] rounded-lg, video-off (Lucide) icon 32px text-[#6B6963], text "Impossible de charger la vidéo" 14px/600, subtext "L'URL a peut-être expiré. Rechargez la page." 12px muted

    Create apps/web/src/components/coach/videos/AnnotatedTimeSlider.tsx as 'use client':
    - Imports: TimeSlider, useMediaState, useMediaRemote from '@vidstack/react'
    - Props: { annotations: Annotation[] }
    - const duration = useMediaState('duration') ?? 0; const remote = useMediaRemote();
    - Returns relative container div with TimeSlider.Root inside; TimeSlider.Track h-[3px] bg-[#E2E0DA] rounded-full with TrackFill bg-[#FF5C1A] and Progress bg-[#E2E0DA] opacity-50; TimeSlider.Thumb w-4 h-4 bg-[#FF5C1A] rounded-full border-2 border-white opacity-0 group-hocus:opacity-100; TimeSlider.Preview with TimeSlider.Value showing time text
    - Orange dot overlay: {duration > 0 && annotations.map(a => <button key={a.id} title={`${formatMmSs(a.timestamp_s)} — ${a.content.slice(0,60)}${a.content.length>60?'...':''}`} onClick={() => remote.seek(a.timestamp_s)} style={{ left: \`${(a.timestamp_s / duration) * 100}%\` }} className="absolute top-1/2 -translate-y-1/2 -translate-x-1/2 w-3 h-3 rounded-full bg-[#FF5C1A] border-2 border-white shadow z-10 hover:scale-[1.4] transition-transform duration-100 cursor-pointer" />)}
    - Helper formatMmSs(s: number): string — `${String(Math.floor(s/60)).padStart(2,'0')}:${String(Math.floor(s%60)).padStart(2,'0')}`
    - Duration gate comment: // CRITICAL: do not render dots when duration === 0 (Risk 3 — all dots cluster at left:0%)

    Create apps/web/src/components/coach/videos/VideoPlayerClient.tsx as 'use client':
    - Props: { clientId, videoId, signedUrl, annotations: Annotation[], video, accessToken, apiUrl }
    - Annotation interface: { id: string; timestamp_s: number; content: string; coach_id: string; created_at: string }
    - Local state: annotations (useState initialized from props); manages live updates after CRUD
    - Layout: page header (← back link, H1 video title, caption date · duration); then flex gap-6 items-start container
    - Back link: arrow-left (Lucide) 14px + "← Retour aux vidéos" 12px text-[#6B6963] hover:text-[#1C1A17]; href /coach/clients/${clientId}/videos
    - Render <VideoPlayer src={signedUrl} annotations={annotations}> with AnnotationPanel inside as children: <div className="flex-1 min-w-0 max-w-sm min-w-[280px] sticky top-6"><AnnotationPanel videoId={videoId} annotations={annotations} accessToken={accessToken} apiUrl={apiUrl} onAnnotationsChange={setAnnotations} status={video?.status} /></div>
    - Pass setAnnotations callback to AnnotationPanel as onAnnotationsChange so panel updates parent after CRUD
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AnnotatedTimeSlider.tsx contains `useMediaState('duration')` and `duration > 0` gate
    - AnnotatedTimeSlider.tsx contains `remote.seek(a.timestamp_s)` on dot click
    - AnnotatedTimeSlider.tsx dot style contains `bg-[#FF5C1A]` and `border-2 border-white`
    - VideoPlayer.tsx imports from '@vidstack/react' and '@vidstack/react/player/layouts/default'
    - VideoPlayerClient.tsx contains `sticky top-6` on the annotation panel column
    - VideoPlayerClient.tsx contains back link "Retour aux vidéos"
    - tsc --noEmit exits 0 for apps/web
  </acceptance_criteria>
  <done>Type check passes; VideoPlayer renders player + scrub bar with orange dots; VideoPlayerClient has correct 2/3+1/3 layout</done>
</task>

<task type="auto">
  <name>Task 3: AnnotationPanel — full useReducer state machine (list/composing/editing/sending/sent)</name>
  <read_first>
    - apps/web/src/components/coach/vocal/vocalReducer.ts (state union type pattern to replicate exactly)
    - apps/web/src/components/coach/vocal/VocalRetourPanel.tsx (useReducer hook, panelRef GSAP entrance, async fetch with try/catch, conditional state rendering)
    - .planning/workstreams/retour-video/phases/46-web-player-text-annotations/046-UI-SPEC.md (Component 2b AnnotationPanel — all states, all copy, all transitions, motion contracts)
  </read_first>
  <action>
    Create apps/web/src/components/coach/videos/AnnotationPanel.tsx as 'use client':

    State machine (useReducer — replicate vocalReducer pattern):
    Type AnnotationState: { status: 'list'; annotations: Annotation[] } | { status: 'composing'; annotations: Annotation[]; timestamp_s: number } | { status: 'editing'; annotations: Annotation[]; annotationId: string; content: string; timestamp_s: number } | { status: 'sending'; annotations: Annotation[] } | { status: 'sent'; annotations: Annotation[] }
    Actions: START_COMPOSE (timestamp_s), START_EDIT (annotationId, content, timestamp_s), CANCEL_COMPOSE, SAVE_SUCCESS (annotation: Annotation), UPDATE_SUCCESS (annotation: Annotation), DELETE_ANNOTATION (annotationId), START_SEND, SEND_SUCCESS, SEND_ERROR, SET_ANNOTATIONS (annotations)
    Initial state: { status: 'list', annotations: initialAnnotations }

    Props: { videoId: string; annotations: Annotation[]; accessToken: string; apiUrl: string; onAnnotationsChange: (a: Annotation[]) => void; status?: string }

    Hooks: const paused = useMediaState('paused'); const remote = useMediaRemote(); — MUST be called inside MediaPlayer subtree (guaranteed by VideoPlayerClient wrapping)

    Panel container: bg-white rounded-lg border border-[#E2E0DA] shadow-sm flex flex-col h-[calc(100vh-220px)] min-h-[400px] overflow-hidden; GSAP entrance gsap.from(panelRef.current, { y: 16, opacity: 0, duration: 0.2, ease: 'power2.out' }) on mount (exact VocalRetourPanel pattern)

    STATE list rendering:
    - Header px-4 py-3 border-b flex items-center justify-between: left "Annotations" H2 18px/600; right count badge bg-[#F0EFE9] text-[#C2410C] text-xs font-medium px-2 py-0.5 rounded when annotations.length > 0
    - Scrollable list flex-1 overflow-y-auto: each item px-4 py-3 border-b last:border-0 group flex items-start gap-3 cursor-pointer hover:bg-[#F0EFE9] relative; left accent strip absolute left-0 top-0 bottom-0 w-1 bg-[#FF5C1A] rounded-r (active item only); timestamp chip font-mono 12px bg-[#FFF0E8] text-[#C2410C] px-1.5 py-0.5 rounded; annotation text 14px text-[#1C1A17] flex-1 line-clamp-3; action icons pencil+trash-2 (Lucide) 16px opacity-0 group-hover:opacity-100; row click: remote.seek(a.timestamp_s); delete: inline "Supprimer ?" / "Annuler" confirmation row (no modal); edit: dispatch START_EDIT
    - Empty list state: flex-1 flex flex-col items-center justify-center gap-3 px-4 py-8; message-square icon 32px text-[#E2E0DA]; "Aucune annotation" 14px/600; subtext 12px muted
    - Footer flex-shrink-0 px-4 py-3 border-t flex flex-col gap-2:
      * "Annoter à ce moment" button — outline — w-full border border-[#FF5C1A] text-[#FF5C1A] when paused; opacity-40 cursor-not-allowed border-[#E2E0DA] text-[#6B6963] when playing; title="Mettez la vidéo en pause d'abord" when disabled; plus (Lucide) icon 14px; onClick: dispatch START_COMPOSE with remote's current time via useMediaState('currentTime')
      * "Envoyer le retour" button — hidden when annotations.length === 0 — w-full bg-[#FF5C1A] text-white; send icon 14px; GSAP press: gsap.to(btnRef.current, { scale: 0.97, duration: 0.1, yoyo: true, repeat: 1 })

    STATE composing rendering:
    - Header: "Nouvelle annotation"
    - Body: timestamp chip read-only bg-[#FFF0E8] text-[#C2410C] border border-[#FF5C1A] font-mono + label "Moment"; textarea rows=6 bg-[#F0EFE9] border-[#E2E0DA] focus:border-[#FF5C1A] outline-none resize-none; char counter [N]/2000 text-right 12px muted; GSAP composer slide-in: gsap.from(composerRef.current, { x: 16, opacity: 0, duration: 0.2, ease: 'power2.out' })
    - Footer: "Annuler" w-1/2 outline + "Enregistrer" w-1/2 bg-[#FF5C1A] text-white; save disabled when textarea empty/whitespace
    - Save action: POST ${apiUrl}/coach/videos/${videoId}/annotations { timestamp_s, content } with Authorization Bearer accessToken; on 201: dispatch SAVE_SUCCESS; call onAnnotationsChange; show item entry animation gsap.from(newItemRef.current, { height: 0, opacity: 0, duration: 0.25, ease: 'power2.out' })

    STATE editing: same as composing but header "Modifier l'annotation"; save button "Mettre à jour"; PATCH ${apiUrl}/coach/videos/${videoId}/annotations/${annotationId}; on 200: dispatch UPDATE_SUCCESS

    STATE sending: footer buttons dimmed opacity-50 pointer-events-none; "Envoi en cours..." spinner on send button; POST ${apiUrl}/coach/videos/${videoId}/send-feedback; on 200: dispatch SEND_SUCCESS + call onAnnotationsChange

    STATE sent: "Retour envoyé" green chip bg-[#DCFCE7] text-[#166534] check-circle icon pointer-events-none; GSAP: gsap.from(sentRef.current, { scale: 0.95, opacity: 0, duration: 0.25, ease: 'back.out(1.4)' }); "Annoter à ce moment" remains functional (can still add annotations after send)

    DELETE handler: DELETE ${apiUrl}/coach/videos/${videoId}/annotations/${annotId} with Authorization Bearer; on 200: dispatch DELETE_ANNOTATION; call onAnnotationsChange

    Error handling: failed saves/updates/deletes/sends show inline toast using showAlert equivalent or a local error banner — use a transient `errorMessage` state string rendered as text-[#EF4444] 12px below the footer buttons
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AnnotationPanel.tsx exists with 'use client' directive
    - AnnotationPanel.tsx contains useReducer (state machine)
    - AnnotationPanel.tsx contains useMediaState and useMediaRemote imports from '@vidstack/react'
    - AnnotationPanel.tsx contains gsap.from(panelRef.current (GSAP entrance)
    - AnnotationPanel.tsx contains 'START_COMPOSE', 'SEND_SUCCESS', 'SEND_ERROR' action type strings
    - AnnotationPanel.tsx contains `paused === true` or `!paused` condition gating the "Annoter" button
    - AnnotationPanel.tsx contains POST fetch to `/annotations` endpoint and POST fetch to `/send-feedback`
    - AnnotationPanel.tsx contains bg-[#DCFCE7] text-[#166534] (sent confirmation chip colors)
    - AnnotationPanel.tsx contains content.slice(0,60) or similar (for annotation list item display)
    - tsc --noEmit exits 0 for apps/web
  </acceptance_criteria>
  <done>Type check passes; AnnotationPanel implements all 5 states with correct GSAP animations, French copy, and API integration; "Annoter" button is gated on paused state; "Envoyer" sends push and shows sent confirmation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Hono annotation write | Authorization Bearer required on every fetch in AnnotationPanel |
| browser→Vidstack MediaPlayer | Client-side only; signed URL never stored in localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-07 | Information Disclosure | signedUrl exposed in DOM | accept | signed URL is 15-min TTL; no PII — it is a Supabase storage URL; acceptable for v1.13 |
| T-46-08 | Elevation of Privilege | AnnotationPanel fetch | mitigate | Authorization Bearer accessToken sent on every annotation write; server validates coach_id |
| T-46-09 | Tampering | textarea content client-side | mitigate | maxLength={2000} on textarea + server validates content.length <= 2000 (double enforcement) |
| T-46-SC | Tampering | npm installs | mitigate | @vidstack/react@next — Approved in Package Legitimacy Audit (RESEARCH.md); official vidstack org |
</threat_model>

<verification>
npx tsc --noEmit -p apps/web/tsconfig.json exits 0.
Manual smoke test (after EAS build or local next dev):
1. Navigate to /coach/clients/[id]/videos — "Vidéos" tab active, list loads
2. Click a ready video — player opens, back link works, notes panel absent
3. Play video, verify orange scrub bar fill
4. Pause video — "Annoter à ce moment" button becomes active (orange border)
5. Click "Annoter" — composer view slides in with MM:SS timestamp chip
6. Type text + "Enregistrer" — annotation appears in list; orange dot appears on timeline at correct position
7. Click dot on timeline — player seeks to that timecode
8. Click annotation list item — player seeks
9. Click "Envoyer le retour" — "Retour envoyé" green chip appears; button disabled
</verification>

<success_criteria>
- tsc --noEmit exits 0 for apps/web
- @vidstack/react@next (1.15.1) in apps/web/package.json — not @vidstack/react@0.6.x
- globals.css has all 3 Vidstack CSS lines
- AnnotationPanel implements all 5 states (list, composing, editing, sending, sent)
- Annotation dots gated on duration > 0 (no dots at left:0% on load)
- "Annoter à ce moment" disabled when video is playing; enabled when paused
- "Envoyer le retour" hidden when annotations.length === 0
- POST /send-feedback called once on "Envoyer"; button permanently disabled after send
</success_criteria>

<output>
Create `.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-03-SUMMARY.md` when done.
</output>
