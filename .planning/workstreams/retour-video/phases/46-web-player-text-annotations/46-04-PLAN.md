---
phase: 46
plan: 04
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app/(app)/(plugins)/coach/video-player.tsx
  - plugins/coach/src/screens/VideoPlayerScreen.tsx
  - plugins/coach/src/screens/VideoListScreen.tsx
autonomous: true
requirements: [REVIEW-01, REVIEW-02]

must_haves:
  truths:
    - "Tapping an annotated video in VideoListScreen navigates to VideoPlayerScreen"
    - "VideoPlayerScreen fetches signed URL on mount and shows ActivityIndicator while pending"
    - "expo-video renders the video with nativeControls"
    - "Orange annotation dots appear below the player at correct percentage positions (gated on duration > 0)"
    - "Tapping a dot seeks the video to that timecode and shows the active annotation card"
    - "Annotation list shows all annotations sorted by timestamp_s asc with MM:SS chips"
    - "Touch target for each dot is 44×44px (accessibility)"
  artifacts:
    - path: "apps/mobile/app/(app)/(plugins)/coach/video-player.tsx"
      provides: "Expo Router thin wrapper reading videoId from query params"
    - path: "plugins/coach/src/screens/VideoPlayerScreen.tsx"
      provides: "Full mobile player screen: expo-video + signed URL fetch + annotation timeline strip + annotation list"
    - path: "plugins/coach/src/screens/VideoListScreen.tsx"
      provides: "Extended with onPress for annotated rows navigating to video-player route"
  key_links:
    - from: "VideoListScreen.tsx"
      to: "video-player route"
      via: "router.push('/(plugins)/coach/video-player?videoId=${video.id}')"
      pattern: "video-player.*videoId"
    - from: "VideoPlayerScreen.tsx"
      to: "Hono GET /coach/videos/:videoId/signed-url"
      via: "useQuery with Authorization Bearer jwt"
      pattern: "coach/videos.*signed-url"
    - from: "VideoPlayerScreen.tsx"
      to: "player.currentTime"
      via: "assignment on dot tap (expo-video seek)"
      pattern: "player\\.currentTime"
---

<objective>
Install expo-video, build the VideoPlayerScreen in the Mon coach plugin, and wire VideoListScreen to navigate to it for annotated videos. The athlete can review all annotations with seek-to-timecode using a custom annotation timeline strip overlaid below the player.

Purpose: Delivers the athlete-side annotation review loop — completing the Phase 46 milestone.
Output: video-player.tsx route, VideoPlayerScreen.tsx plugin screen, VideoListScreen.tsx extended.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-CONTEXT.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-RESEARCH.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/046-UI-SPEC.md
@.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-video + route wrapper + VideoListScreen extension</name>
  <read_first>
    - apps/mobile/package.json (verify expo-video is NOT installed; confirm expo-av version; confirm SDK version is 54)
    - apps/mobile/app/(app)/(plugins)/coach/videos.tsx (route wrapper pattern to replicate exactly)
    - plugins/coach/src/screens/VideoListScreen.tsx (lines 150–230 — read the TouchableOpacity row render to find where to add onPress)
  </read_first>
  <action>
    Install expo-video: run `cd apps/mobile && npx expo install expo-video` — use npx expo install (not npm install) so the Expo resolver pins the correct version for SDK 54 (~2.0.6).
    expo-video is a native module and requires Dev Build (EAS). It does NOT work in Expo Go. Confirm in plan comment.

    Create apps/mobile/app/(app)/(plugins)/coach/video-player.tsx (new file):
    - Exact same pattern as videos.tsx: import React; import { useLocalSearchParams } from 'expo-router'; import VideoPlayerScreen from '@ziko/plugin-coach/screens/VideoPlayerScreen'; import { supabase } from '../../../../src/lib/supabase'
    - export default function CoachVideoPlayerRoute() { const { videoId } = useLocalSearchParams<{ videoId: string }>(); return <VideoPlayerScreen supabase={supabase} videoId={videoId ?? ''} /> }
    - Note: useLocalSearchParams picks up ?videoId= from the navigation push call

    Extend plugins/coach/src/screens/VideoListScreen.tsx:
    - Add useRouter import from 'expo-router' if not already present
    - On the TouchableOpacity wrapping each video row, add onPress:
      if (video.status === 'annotated' || video.status === 'ready') { router.push(`/(plugins)/coach/video-player?videoId=${video.id}` as any); }
    - Videos with status 'uploading' remain non-navigable (no onPress change needed — they already have none)
    - Note: navigate for both 'annotated' and 'ready' status (coach may have already added annotations but not sent yet — athlete still needs to be able to open the video after annotations are sent)
    - Do not change any other part of VideoListScreen.tsx
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep "expo-video" apps/mobile/package.json && ls apps/mobile/app/(app)/\(plugins\)/coach/video-player.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/package.json contains "expo-video" (version ~2.x or as resolved by expo install)
    - File exists: apps/mobile/app/(app)/(plugins)/coach/video-player.tsx
    - video-player.tsx imports useLocalSearchParams and VideoPlayerScreen
    - video-player.tsx passes videoId from query params to VideoPlayerScreen
    - VideoListScreen.tsx contains router.push string including 'video-player?videoId='
    - VideoListScreen.tsx onPress only fires for status === 'annotated' or 'ready' rows (not 'uploading')
  </acceptance_criteria>
  <done>expo-video installed; route wrapper created; VideoListScreen extended with navigation to player</done>
</task>

<task type="auto">
  <name>Task 2: VideoPlayerScreen — expo-video + annotation timeline strip + annotation list</name>
  <read_first>
    - plugins/coach/src/screens/VideoListScreen.tsx (full imports, useQuery pattern, SafeAreaView + ScrollView + paddingBottom:100 pattern, theme usage, showAlert import)
    - .planning/workstreams/retour-video/phases/46-web-player-text-annotations/046-UI-SPEC.md (Screen 3 VideoPlayerScreen — full layout spec including annotation timeline strip pixel values, active annotation card, annotation list, motion contracts)
    - .planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-RESEARCH.md (expo-video API: useVideoPlayer, VideoView, player.currentTime, useEvent timeUpdate, player.duration, statusChange event)
  </read_first>
  <action>
    Create plugins/coach/src/screens/VideoPlayerScreen.tsx:

    Imports:
    - React, { useState, useEffect, useRef } from 'react'
    - View, Text, ScrollView, TouchableOpacity, ActivityIndicator, Dimensions from 'react-native'
    - SafeAreaView from 'react-native-safe-area-context'
    - { VideoView, useVideoPlayer } from 'expo-video'
    - { useEvent } from 'expo'
    - Ionicons from '@expo/vector-icons'
    - { useQuery } from '@tanstack/react-query'
    - { useThemeStore, showAlert } from '@ziko/plugin-sdk'
    - { useAuthStore } from '../../../../apps/mobile/src/stores/authStore' (follow VideoListScreen pattern for import path)
    - { useRouter } from 'expo-router'
    - Animated, withSpring from 'react-native-reanimated'

    Props: { supabase: any; videoId: string }

    Signed URL fetch (useQuery):
    - queryKey: ['coach-video-signed-url', videoId]
    - queryFn: GET ${apiUrl}/coach/videos/${videoId}/signed-url with Authorization Bearer jwt; return (await res.json()).signedUrl
    - staleTime: 10 * 60 * 1000 (10 min — URL expires in 15, this ensures refresh before expiry on remount)
    - enabled: !!videoId

    Annotation fetch (useQuery):
    - queryKey: ['coach-video-annotations', videoId]
    - queryFn: GET ${apiUrl}/coach/videos/${videoId}/annotations → returns Annotation[] sorted by timestamp_s asc
    - enabled: !!videoId

    Player setup (useVideoPlayer):
    - const player = useVideoPlayer(signedUrl ?? '', (p) => { p.loop = false; p.timeUpdateEventInterval = 0.25; })
    - const { currentTime } = useEvent(player, 'timeUpdate', { currentTime: player.currentTime })
    - Duration: listen for statusChange via useEvent(player, 'statusChange', { status: 'idle' }) — when status === 'readyForDisplay', read player.duration to set local duration state
    - const [duration, setDuration] = useState(0)
    - const [activeAnnotationId, setActiveAnnotationId] = useState<string | null>(null)

    Loading state (while signedUrl is null or URL loading):
    - In place of VideoView: View style={{ width:'100%', aspectRatio:16/9, backgroundColor:'#000000', justifyContent:'center', alignItems:'center' }}
    - ActivityIndicator color='#FF5C1A' size='large'
    - Text "Chargement de la vidéo..." 12px/400 color='#FFFFFF' marginTop:8

    Error state (fetch fails):
    - Ionicons alert-circle-outline 32px color='#EF4444'
    - Text "Impossible de charger la vidéo" 14px/600 color='#FFFFFF'
    - Text "Revenez plus tard ou contactez votre coach." 12px/400 color='#6B6963'

    VideoView (once signedUrl loaded):
    - style={{ width:'100%', aspectRatio:16/9, backgroundColor:'#000000' }}
    - contentFit='contain', nativeControls={true}, allowsPictureInPicture={false}

    Header (height 48, paddingHorizontal:16, flexDirection:row, alignItems:center, gap:12):
    - Ionicons chevron-back 24px color='#1C1A17' onPress={() => router.back()}
    - Text video title (from annotations query or passed metadata) 18px/600 flex:1 numberOfLines:1

    Annotation Timeline Strip (between VideoView and ScrollView):
    - Container: height:36, paddingHorizontal:16, paddingVertical:10, backgroundColor:'#FFFFFF', borderTopWidth:1, borderTopColor:'#E2E0DA', borderBottomWidth:1, borderBottomColor:'#E2E0DA', position:'relative'
    - Inner track bar: position:'absolute', left:16, right:16, top:'50%', transform:[{translateY:-1.5}], height:3, backgroundColor:'#E2E0DA', borderRadius:2
    - const { width: screenWidth } = Dimensions.get('window')
    - const trackWidth = screenWidth - 32
    - Render dots only when duration > 0 (gate): annotations.map(a => { const leftOffset = (a.timestamp_s / duration) * trackWidth + 16; return <TouchableOpacity key={a.id} onPress={() => { player.currentTime = a.timestamp_s; setActiveAnnotationId(a.id); }} style={{ position:'absolute', top:'50%', transform:[{translateY:-22},{translateX:-22}], width:44, height:44, justifyContent:'center', alignItems:'center' }} activeOpacity={0.7}><View style={{ width:12, height:12, borderRadius:6, backgroundColor:'#FF5C1A', borderWidth:2, borderColor:'#FFFFFF', position:'absolute', left:leftOffset-22, top:11 }} /></TouchableOpacity> })
    - Note: 44×44 TouchableOpacity is the accessible touch target; the 12px dot is the visual indicator centered within it. The dot's left position within the TouchableOpacity is always centered at (leftOffset, 18) relative to the strip container.

    Active Annotation Card (show when activeAnnotationId is set):
    - marginHorizontal:16, marginTop:12, padding:12, backgroundColor:'#FFFFFF', borderRadius:8, borderWidth:1, borderColor:'#FF5C1A', shadow (shadowOpacity:0.08, shadowRadius:8, elevation:3)
    - Row: timestamp chip [MM:SS] bg-[#FFF0E8] text-[#C2410C] rounded px-8 py-4 font-mono 12px
    - mt-8: annotation text 14px color='#1C1A17' lineHeight:21
    - Close button: Ionicons close 18px color='#6B6963' position absolute top:8 right:8 onPress={() => setActiveAnnotationId(null)}
    - activeAnnotation = annotations.find(a => a.id === activeAnnotationId)

    Annotation List (in ScrollView, below card):
    - Section header: "Tous les commentaires ([N])" 14px/600 color='#1C1A17' paddingVertical:12
    - Each row: paddingVertical:10 borderBottom:1 borderColor:'#E2E0DA' flexDirection:row alignItems:flex-start gap:10; timestamp chip + text 14px flex:1 lineHeight:21; onPress seeks + activates card
    - Empty list: Ionicons chatbubble-ellipses-outline 28px color='#E2E0DA'; "Aucun commentaire" 14px/600; "Votre coach n'a pas encore ajouté d'annotations." 12px/400 color='#6B6963' textAlign:center

    Helper formatMmSs(s: number): string — same as web: String(Math.floor(s/60)).padStart(2,'0') + ':' + String(Math.floor(s%60)).padStart(2,'0')

    Never use Alert.alert — use showAlert from @ziko/plugin-sdk for any error alerts.
    All plugin screens must have paddingBottom: 100 in contentContainerStyle.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit 2>&1 | grep "plugin-coach\|VideoPlayerScreen\|video-player" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: plugins/coach/src/screens/VideoPlayerScreen.tsx
    - VideoPlayerScreen.tsx imports VideoView and useVideoPlayer from 'expo-video'
    - VideoPlayerScreen.tsx imports useEvent from 'expo'
    - VideoPlayerScreen.tsx contains player.currentTime = a.timestamp_s (seek on dot tap)
    - VideoPlayerScreen.tsx contains duration > 0 gate before rendering dots
    - VideoPlayerScreen.tsx contains width: 44, height: 44 touch target on dot TouchableOpacity
    - VideoPlayerScreen.tsx contains paddingBottom: 100 in contentContainerStyle
    - VideoPlayerScreen.tsx does NOT import Alert from 'react-native' — uses showAlert
    - tsc --noEmit exits 0 (no TypeScript errors in VideoPlayerScreen)
  </acceptance_criteria>
  <done>VideoPlayerScreen builds with no TS errors; expo-video renders video; annotation timeline strip shows orange dots at correct positions; tapping dot seeks player and shows comment card; annotation list renders all annotations with MM:SS chips</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile→Hono GET /signed-url | Athlete JWT required; server verifies caller is athlete_id of the video |
| mobile→Hono GET /annotations | Athlete JWT required; server verifies caller is athlete_id OR coach_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-10 | Information Disclosure | signed URL in memory | accept | 15-min TTL; no caching to disk; acceptable for v1.13 |
| T-46-11 | Elevation of Privilege | athlete reads another athlete's annotations | mitigate | Hono GET /annotations checks caller is athlete_id of that specific video (enforced in 46-01) |
| T-46-12 | Elevation of Privilege | athlete attempts to POST annotations | mitigate | Hono POST /annotations verifies caller === video.coach_id (enforced in 46-01) |
| T-46-SC | Tampering | expo-video npm install | mitigate | expo-video is official Expo SDK package — Approved in Package Legitimacy Audit (RESEARCH.md). Use `npx expo install` (not npm install) to respect SDK version resolver |
</threat_model>

<verification>
Manual smoke test (requires EAS Dev Build — expo-video is a native module, does not run in Expo Go):
1. Open VideoListScreen for a linked athlete — rows appear
2. Tap a video with status 'annotated' — navigates to VideoPlayerScreen
3. Loading state shows ActivityIndicator with orange spinner while URL fetches
4. Video plays with native controls (play/pause/scrub)
5. Orange dots appear on timeline strip at correct positions once duration > 0
6. Tap a dot — video seeks to that timecode + active annotation card appears
7. Tap card close button — card dismisses
8. Tap annotation list row — video seeks to that timecode
9. If no annotations: empty state shows "Aucun commentaire" with chatbubble icon
</verification>

<success_criteria>
- expo-video in apps/mobile/package.json (version ~2.x)
- video-player.tsx route exists and passes videoId from query params
- VideoListScreen.tsx navigates to video-player for annotated/ready rows
- VideoPlayerScreen.tsx renders loading state, error state, player, timeline strip, annotation list
- Orange dots only rendered when duration > 0
- player.currentTime = timestamp_s on dot or list item tap
- 44×44px touch targets on timeline dots (WCAG minimum)
- paddingBottom: 100 in contentContainerStyle (tab bar clearance)
- No Alert.alert — only showAlert from @ziko/plugin-sdk
</success_criteria>

<output>
Create `.planning/workstreams/retour-video/phases/46-web-player-text-annotations/46-04-SUMMARY.md` when done.
</output>
