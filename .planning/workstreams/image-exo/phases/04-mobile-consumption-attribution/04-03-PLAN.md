---
phase: 04-mobile-consumption-attribution
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx
autonomous: true
requirements: [MOBILE-01, MOBILE-04, MOBILE-05, MOBILE-06]

must_haves:
  truths:
    - "The exercise detail hero is a full-width square (1:1) card showing the real animated GIF, autoplaying and looping, from the exercise-media bucket (D-01, D-03)"
    - "No play button, HD badge, or 'Demo 0:42' duration text exists anywhere on the screen (D-02)"
    - "The Gym visual attribution badge renders exactly once on the screen, on the hero media (D-05)"
    - "Exercises with no gif show a neutral barbell placeholder in the same square hero slot, with no badge (D-07)"
    - "The Consignes tab renders numbered steps sourced directly from instruction_steps JSONB"
    - "No JSON.parse or split('\\n') remains anywhere in the file"
    - "Exercise name and instruction steps follow the user's fr/en locale"
    - "The exercise detail query uses a versioned cache key"
  artifacts:
    - path: "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"
      provides: "Square attributed GIF hero, structured bilingual instructions, versioned query key"
      contains: "AttributedMedia"
  key_links:
    - from: "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"
      to: "@ziko/ui AttributedMedia"
      via: "named import"
      pattern: "import \\{ AttributedMedia"
    - from: "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"
      to: "exercise-media Storage bucket"
      via: "supabase.storage.getPublicUrl on exercise.gif"
      pattern: "getPublicUrl"
    - from: "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"
      to: "@ziko/plugin-sdk useTranslation"
      via: "tExercise + locale for bilingual name and steps"
      pattern: "useTranslation"
---

<objective>
Replace the fake video placeholder on the exercise detail screen with the real attributed GIF hero (MOBILE-01), rewire the Consignes tab onto `instruction_steps` JSONB (MOBILE-04), render name and steps per locale (MOBILE-05), and version the query key (MOBILE-06).

Purpose: This screen is the milestone's headline deliverable — it currently shows a `Démo · 0:42` / `HD` chrome over a black rectangle with no asset behind it, and parses instructions through a `JSON.parse`/`.split('\n')` chain. Phase 3 put real media and structured bilingual instructions in production for all 1,318 non-custom rows; this plan consumes them.
Output: `apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx` — hero block replaced, instructions block replaced, header title localized, query key versioned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/image-exo/REQUIREMENTS.md
@.planning/workstreams/image-exo/ROADMAP.md
@.planning/workstreams/image-exo/STATE.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-CONTEXT.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-UI-SPEC.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-PATTERNS.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-RESEARCH.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

From @ziko/ui (created in plan 04-01, already merged when this plan runs):
  import { AttributedMedia, EmptyState } from '@ziko/ui';
  AttributedMedia props: { uri: string | null; size?: number; borderRadius?: number; showBadge?: boolean; credit?: string; fallbackIconSize?: number }
    - renders at Math.min(size ?? 180, 180) square, always
    - mounts the '© Gym visual — https://gymvisual.com/' badge only when showBadge !== false AND uri is truthy
    - renders a centered barbell-outline placeholder + t('exercise.mediaUnavailable') caption when uri is falsy, with no badge
  EmptyState props: { variant: 'no-data' | 'error' | 'offline' | 'no-results'; title: string; message?: string; ctaLabel?: string; onCta?: () => void }

From @ziko/plugin-sdk (pre-existing):
  const { t, tExercise, locale } = useTranslation();   // locale is typed 'fr' | 'en'
  tExercise(name: string, nameFr?: string | null): string
  i18n keys added in plan 04-01: 'exercise.mediaUnavailable', 'exercise.instructionsEmptyTitle', 'exercise.instructionsEmptyBody'

Live DB shape for the row returned by this screen's `.select('*')`:
  exercises.gif           text | null   -- relative Storage path '{exercise_uuid}/animation.gif'
  exercises.image         text | null   -- relative Storage path '{exercise_uuid}/thumb.png'
  exercises.name          text          -- English canonical
  exercises.name_fr       text | null
  exercises.instructions      text | null   -- legacy English prose
  exercises.instructions_fr   text | null   -- French prose (populated for all 1,318 non-custom rows)
  exercises.instruction_steps jsonb | null  -- { en: string[]; fr: string[] } verbatim from the dataset
The supabase client (apps/mobile/src/lib/supabase.ts) is untyped, so `exercise` is `any` — declare a local type for the instruction_steps shape rather than relying on inference.

Production data facts (verified against .planning/workstreams/image-exo/reports/merge-run.md + match-report.md):
  - All 1,318 non-custom rows have gif, image, instructions_fr and instruction_steps populated (100% coverage of processed rows).
  - The 6 unmatched-new dataset rows were NEVER inserted (deterministic category CHECK failure) — they do not exist in production.
  - `is_custom = true` rows (user/coach-created) have gif/image/instruction_steps NULL and may have legacy `instructions` prose. These are the live rows the fallback paths actually serve.

Bucket: `exercise-media` (public-read, supabase/migrations/20260814_exercise_media_schema.sql).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the fake 16:9 video placeholder with the real attributed square GIF hero</name>

  <files>apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx</files>

  <read_first>
    - apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx — read the whole file. The import block is lines 1-19, the exercise `useQuery` is lines 28-45, and the hero block being deleted is lines 200-297. The stat tiles at lines 302-316 carry the exact card style the new hero must match.
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-UI-SPEC.md §1, §2 and §4 — the locked hero container style, the AttributedMedia contract, and the hero-size placeholder spec.
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-CONTEXT.md — D-01 (square hero), D-02 (all fake chrome stripped), D-03 (GIF autoplays/loops, thumb.png never used here), D-05 (badge once per screen), D-07 (placeholder, no badge).
    - packages/ui/src/components/AttributedMedia.tsx — the component built in plan 04-01; confirm the actual prop names and clamp behavior before wiring it.
    - apps/mobile/app/(app)/profile/avatar.tsx around line 109 — the `getPublicUrl` precedent (this plan omits its `?t=` suffix, see action).
  </read_first>

  <action>
Imports: add `import { AttributedMedia } from '@ziko/ui';`. Do NOT import `Image` from `expo-image` in this file — the hero renders media only through `AttributedMedia`, which owns the `expo-image` call. Remove the `Ionicons` import only if nothing else in the file still uses it (the Muscles tab and the AI suggestion block do, so it stays).

Query key (line 34): change `['exercise', exerciseId]` to exactly `['exercises', 'v2', exerciseId]` (MOBILE-06). Leave `.select('*')`, `.eq('id', exerciseId)`, `.single()` and `enabled: !!exerciseId` untouched — `'*'` already returns `gif`, `image`, `instructions_fr` and `instruction_steps`.

Public URL: after the query, derive `publicGifUrl` as `supabase.storage.from('exercise-media').getPublicUrl(exercise.gif).data.publicUrl` when `exercise?.gif` is truthy, else `null`. Use `exercise.gif` (the animated `animation.gif`), NOT `exercise.image` — D-03 states `thumb.png` is reserved for list rows and the detail hero always shows the animated asset directly, with no static-then-tap state. Do NOT append `?t=${Date.now()}`: `exercise-media` assets are immutable, so cache-busting only destroys image-cache hit rates (04-RESEARCH.md Pattern 1).

Delete lines 200-297 in full — the `{/* 16:9 Video Placeholder */}` View and everything nested in it: the `aspectRatio: 16 / 9` container, the `#1C1A17` background, the `rgba(255,92,26,0.04)` diagonal overlay, the 60x60 play button with `Ionicons name="play"`, the `Démo · 0:42` top-left badge, and the `HD` bottom-right badge. None of this is true for a GIF (no duration, no resolution tier, no play state) — D-02 strips all of it. Nothing from this block is preserved or commented out.

Replace it with a hero card `View` styled exactly like the stat tiles below it (04-UI-SPEC.md §1): `borderRadius: 14`, `aspectRatio: 1`, `overflow: 'hidden'`, `backgroundColor: theme.surface`, `borderWidth: 1`, `borderColor: theme.border`, `shadowColor: theme.text`, `shadowOpacity: 0.08`, `shadowRadius: 12`, `shadowOffset: { width: 0, height: 2 }`, `elevation: 3`, plus `alignItems: 'center'` and `justifyContent: 'center'`. It fills the ScrollView's existing 16px horizontal padding — do not set an explicit width.

Inside that card render exactly one element: `<AttributedMedia uri={publicGifUrl} showBadge />`. Pass NO `size` prop.

Locked interpretation of the UI-SPEC's one internal tension, so the executor does not have to adjudicate it: 04-UI-SPEC.md §1 describes a full-width square hero, while §2 makes `AttributedMedia` clamp its rendered box to 180x180. Both hold simultaneously — the CARD is full-width square (D-01, honest to the asset's shape and to the card convention), and the MEDIA inside it renders at its native 180x180, centered on the card's `theme.surface` background. Never upscale the GIF to fill the card; that is exactly the "upscale-cropping" MEDIA-03 and D-01's own rationale forbid. Because no `size` prop is passed, `AttributedMedia` uses its 180 default and no clamping surprise occurs.

Missing-media path needs no branch in this file: `AttributedMedia` already renders the barbell placeholder plus the `t('exercise.mediaUnavailable')` caption and suppresses the badge when `uri` is null (D-07). Do not add a second placeholder here.

This is the phase's one and only badge instance (D-05) — do not add `AttributedMedia`, a credit string, or any second media element anywhere else on this screen.
  </action>

  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | grep -cF "[exerciseId].tsx"</automated>
    <automated>grep -cF "AttributedMedia" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"</automated>
    <automated>grep -cF "0:42" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "0:42" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0`.
    - `grep -cF "aspectRatio: 16 / 9" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0`, and `grep -cF "aspectRatio: 1" ...` prints `1`.
    - `grep -cF 'name="play"' "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0`.
    - `grep -cF "#1C1A17" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0` — the fake video background is gone.
    - `grep -c "^\s*HD$" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0`.
    - `grep -cF "AttributedMedia" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints exactly `2` (one import, one usage) — D-05: a single badge instance on the screen.
    - `grep -cF "size=" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx" ` shows no `size=` on the `AttributedMedia` element (source assertion: the JSX reads `<AttributedMedia uri={publicGifUrl} showBadge />`).
    - `grep -c "queryKey: \['exercises', 'v2', exerciseId\]" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `1`, and `grep -cF "queryKey: ['exercise', exerciseId]" ...` prints `0`.
    - `grep -cF "getPublicUrl" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `1`, and `grep -cF "exercise.image" ...` prints `0` — the hero uses `gif`, never `image`.
    - `grep -cF "Date.now()" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0`.
    - `grep -cF "from 'expo-image'" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0` — media rendering is delegated to AttributedMedia.
    - `cd apps/mobile && npx tsc --noEmit 2>&1 | grep -cF "[exerciseId].tsx"` prints `0`, and `npx tsc --noEmit 2>&1 | wc -l` still prints `6` (unchanged pre-existing baseline in `settings.tsx` / `useNotificationSetup.ts`).
  </acceptance_criteria>

  <done>The hero is a full-width square card containing a 180x180 autoplaying GIF with the Gym visual badge (or a barbell placeholder with no badge when `gif` is null), all fake video chrome is deleted, and the query key is versioned.</done>
</task>

<task type="auto">
  <name>Task 2: Source the Consignes tab from instruction_steps and localize the exercise name</name>

  <files>apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx</files>

  <read_first>
    - apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx — lines 125-136 hold the `cues` fallback chain being deleted; line 182 holds the `WSHeader title`; lines 490-513 hold the numbered-steps render loop that must be preserved structurally.
    - packages/plugin-sdk/src/i18n.ts lines 1712-1734 — the `useTranslation()` return shape and the `tExercise(name, nameFr)` implementation this task mirrors, plus the `exercise.instructionsEmpty*` keys added in plan 04-01.
    - packages/ui/src/components/EmptyState.tsx — the props signature (`variant`, `title`, `message`) for the empty-instructions state.
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-UI-SPEC.md §5 and the Copywriting Contract — the numbered-step visual is unchanged; the empty state uses `EmptyState` variant `'no-data'` with the locked copy.
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-RESEARCH.md — "Pitfall 3" (instruction_steps is real JSONB, already deserialized, never JSON.parse it) and "Open Questions" #1 (the legacy-TEXT fallback question this task resolves).
  </read_first>

  <action>
Imports: add `import { useTranslation } from '@ziko/plugin-sdk';` and add `EmptyState` to the `@ziko/ui` import introduced in Task 1. Inside the component, destructure `const { t, tExercise, locale } = useTranslation();`.

Delete the entire `cues` IIFE at lines 125-136. Every branch of it goes: the `'Pas de consignes disponibles.'` hardcoded string, the `Array.isArray` check, the `JSON.parse` try block, and the `.split('\n').filter(Boolean)` catch. `instruction_steps` is a genuine `jsonb` column — the Supabase client already deserializes it into a JS object, so parsing it again is the exact fragility MOBILE-04 removes. No `try`/`catch` may be introduced around `instruction_steps` access.

Declare a local type for the JSONB shape (e.g. `type InstructionSteps = { en?: string[]; fr?: string[] }`) and read it as `exercise?.instruction_steps as InstructionSteps | null | undefined`, because the supabase client in this app is untyped.

Replace `cues` with `steps`, computed as `instructionSteps?.[locale] ?? instructionSteps?.en ?? []`. This is the MOBILE-05 locale selection and it mirrors `tExercise`'s own "preferred language, else canonical English" fallback shape. `locale` is typed `'fr' | 'en'`, matching the JSONB keys exactly — no mapping table is needed.

Add the legacy-prose fallback. This resolves the Claude's-Discretion item CONTEXT.md left open and 04-RESEARCH.md Open Question #1. Compute `legacyInstructions` as `exercise?.instructions_fr` when `locale === 'fr'` (falling back to `exercise?.instructions`), and `exercise?.instructions` otherwise (falling back to `exercise?.instructions_fr`), trimmed. Then the Consignes tab renders one of exactly three states, in this order:
  1. `steps.length > 0` -> the existing numbered list (see below).
  2. `steps.length === 0` and `legacyInstructions` is non-empty -> a single unstructured paragraph `Text` with `fontSize: 12`, `lineHeight: 18`, `color: theme.text`, rendering `legacyInstructions` verbatim. No numbering, no splitting, no parsing — this is a plain render of the string, which is why it does not reintroduce the fragile chain. Rationale to record in the summary: all 1,318 non-custom production rows have `instruction_steps`, so this branch exists solely for `is_custom = true` user/coach-created exercises, which carry free-text `instructions` and no structured steps. Dropping straight to an empty state for them would regress live content to zero.
  3. Both empty -> `<EmptyState variant="no-data" title={t('exercise.instructionsEmptyTitle')} message={t('exercise.instructionsEmptyBody')} />`.

The numbered-steps render loop at lines 490-513 keeps its exact existing visuals — 22x22 circle, `borderRadius: 999`, `rgba(255,92,26,0.14)` fill, number `Text` at fontSize 10 / fontWeight '700' / color `#FF5C1A`, body `Text` at fontSize 12 / lineHeight 18 / `theme.text`, wrapper `gap: 10`. The only change is that it maps over `steps` instead of `cues`. Do not restyle it. Keep the `Points clés d'exécution` section label and the AI suggestion block below it exactly as they are.

Localize the header (MOBILE-05): change `WSHeader`'s `title` at line 182 from `exercise?.name ?? 'Exercice'` to `tExercise(exercise.name, exercise.name_fr)` guarded so it still falls back to `'Exercice'` when `exercise` is absent. Leave the `sub` prop and the loading/error branches (lines 144-177) untouched.

Do not change the Muscles tab, the Historique tab, the stat tiles, the action buttons, or the `ExercisePicker` modal at the bottom of the file.
  </action>

  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | grep -cF "[exerciseId].tsx"</automated>
    <automated>grep -cF "JSON.parse" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"</automated>
    <automated>grep -cF "instruction_steps" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "JSON.parse" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0`.
    - `grep -cF "split('\\n')" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0`.
    - `grep -cF "Pas de consignes disponibles" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0`.
    - `grep -cF "const cues" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `0` and `grep -cF "cues.map" ...` prints `0`.
    - `grep -cF "instruction_steps" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` is at least `1`.
    - `grep -cF "steps.map" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `1`.
    - `grep -cF "useTranslation" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `2` (import + call).
    - `grep -cF "tExercise(" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` is at least `1`.
    - `grep -cF "exercise.instructionsEmptyTitle" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `1` and `grep -cF "exercise.instructionsEmptyBody" ...` prints `1`.
    - `grep -cF "EmptyState" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `2` (import + usage).
    - `grep -cF "rgba(255,92,26,0.14)" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` prints `1` — the numbered-circle visual survived unchanged.
    - Source assertion: no `try {` block appears anywhere within 20 lines of an `instruction_steps` reference.
    - Source assertion: the Consignes tab body contains three mutually exclusive branches keyed on `steps.length > 0`, then a non-empty legacy string, then `EmptyState`.
    - `cd apps/mobile && npx tsc --noEmit 2>&1 | grep -cF "[exerciseId].tsx"` prints `0`, and `npx tsc --noEmit 2>&1 | wc -l` still prints `6`.
  </acceptance_criteria>

  <done>The Consignes tab renders numbered steps straight from `instruction_steps[locale]` with an English fallback, degrades to a plain legacy-prose paragraph for custom exercises, shows the localized EmptyState when there is nothing at all, and the header title is locale-aware — with zero parsing code left in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Postgres (`public.exercises`, RLS-protected) → mobile client | Row data including `gif`, `instructions`, `instructions_fr`, `instruction_steps` crosses here. The pre-existing SELECT policy (`001_initial_schema.sql:293`) is unchanged. |
| Supabase Storage (`exercise-media`, public-read) → mobile client | GIF bytes cross here. Public-read by design (MEDIA-02, Phase 1); no client write policy. |

No new boundary is introduced — this plan reads and renders columns that already ship.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `exercise.gif` passed to `getPublicUrl()` in [exerciseId].tsx | accept | Not exploitable: `exercises.gif` is written exclusively by the service-role Phase 3 merge script, whose `storagePaths()` derives the path from the exercise UUID only. The `exercise-media` bucket has no client INSERT/UPDATE/DELETE policy, so no user can plant a traversal path. Matches 04-RESEARCH.md Security Domain (ASVS V5: "marginal / not exploitable" for this phase). |
| T-04-04 | Tampering | Rendering `instruction_steps` / legacy `instructions` strings into `<Text>` | accept | React Native `<Text>` renders strings as text, not markup — there is no HTML/JS injection sink. For `is_custom = true` rows the prose is the owning user's own input, visible only to that user under the existing RLS SELECT policy. Removing `JSON.parse` (MOBILE-04) strictly reduces attack surface by deleting an untrusted-string deserialization path. |
| T-04-02 | Information Disclosure | Public-read `exercise-media` bucket | accept | Public-read is the shipped, intentional design (MEDIA-02). Assets are licensed third-party exercise GIFs with no PII. |
| T-04-03 | Repudiation (licence attribution) | Attribution badge on the hero | mitigate | Media is rendered only via `AttributedMedia`, which owns the badge and cannot be told to drop it while showing media. Acceptance criteria assert `AttributedMedia` appears exactly twice (import + single usage) and `expo-image` is never imported directly in this file, so no unattributed second media path can be introduced. |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed (04-RESEARCH.md "Package Legitimacy Audit: Not applicable"). No `package.json` edit is permitted in this plan. |

ASVS L1 categories V2/V3/V4/V6 do not apply — no auth, session, access-control, or cryptography surface is touched.
</threat_model>

<verification>
- `cd apps/mobile && npx tsc --noEmit 2>&1 | wc -l` prints `6` (unchanged baseline) and `| grep -cF "[exerciseId].tsx"` prints `0`
- All grep assertions in both tasks' acceptance criteria return their stated counts
- `git diff --name-only` shows only `apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx`
</verification>

<success_criteria>
- MOBILE-01: the hero shows the real animated GIF; `0:42`, `HD`, the play button and the `#1C1A17` frame are gone from the source
- MOBILE-03 (screen-level): the Gym visual badge renders exactly once, on the hero, via `AttributedMedia`
- MOBILE-04: `instruction_steps` drives the numbered list; `JSON.parse` and `split('\n')` appear nowhere in the file
- MOBILE-05: `tExercise` drives the header title and `locale` drives step selection
- MOBILE-06: the detail query uses `['exercises', 'v2', exerciseId]`
</success_criteria>

<output>
Create `.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-03-SUMMARY.md` when done
</output>
