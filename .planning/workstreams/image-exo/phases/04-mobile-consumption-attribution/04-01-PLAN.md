---
phase: 04-mobile-consumption-attribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/plugin-sdk/src/i18n.ts
  - packages/ui/src/components/AttributedMedia.tsx
  - packages/ui/src/index.ts
autonomous: true
requirements: [MOBILE-03]

must_haves:
  truths:
    - "A shared AttributedMedia component is importable from @ziko/ui by any app screen"
    - "AttributedMedia never renders media larger than 180x180 regardless of the size prop a caller passes"
    - "AttributedMedia renders the Gym visual credit badge whenever it renders media"
    - "AttributedMedia renders a neutral barbell placeholder and no badge when uri is null"
    - "FR and EN copy for the missing-media caption and the empty-instructions state resolves through t()"
  artifacts:
    - path: "packages/ui/src/components/AttributedMedia.tsx"
      provides: "AttributedMedia shared component enforcing badge + 180 cap + fallback"
      exports: ["AttributedMedia"]
      min_lines: 60
    - path: "packages/ui/src/index.ts"
      provides: "Package-root export of AttributedMedia"
      contains: "AttributedMedia"
    - path: "packages/plugin-sdk/src/i18n.ts"
      provides: "exercise.mediaUnavailable / exercise.instructionsEmptyTitle / exercise.instructionsEmptyBody keys in fr and en"
      contains: "exercise.mediaUnavailable"
  key_links:
    - from: "packages/ui/src/index.ts"
      to: "packages/ui/src/components/AttributedMedia.tsx"
      via: "named re-export"
      pattern: "export \\{ AttributedMedia \\} from './components/AttributedMedia'"
    - from: "packages/ui/src/components/AttributedMedia.tsx"
      to: "expo-image"
      via: "Image import"
      pattern: "from 'expo-image'"
    - from: "packages/ui/src/components/AttributedMedia.tsx"
      to: "@ziko/plugin-sdk"
      via: "useTranslation for the fallback caption"
      pattern: "useTranslation"
---

<objective>
Build the shared `<AttributedMedia>` component in `packages/ui` that structurally enforces MOBILE-03 (Gym visual credit badge + 180x180 display cap + neutral missing-media fallback), and add the three FR/EN i18n keys the media/instructions surfaces need.

Purpose: This is the contract both consuming screens depend on. Wave 2's detail screen imports it; the i18n keys back the copy the UI-SPEC locks. Nothing downstream can be built without these.
Output: `packages/ui/src/components/AttributedMedia.tsx`, its export in `packages/ui/src/index.ts`, and 3 new `exercise.*` keys in each of the `fr` and `en` dictionaries in `packages/plugin-sdk/src/i18n.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/image-exo/REQUIREMENTS.md
@.planning/workstreams/image-exo/ROADMAP.md
@.planning/workstreams/image-exo/STATE.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-CONTEXT.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-UI-SPEC.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-PATTERNS.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

Locked prop interface for the new component (04-UI-SPEC.md §2 — not open to reinterpretation):
  uri: string | null
  size?: number                 // default 180, clamped via Math.min(size ?? 180, 180)
  borderRadius?: number         // default 14
  showBadge?: boolean           // default true
  credit?: string               // default '© Gym visual — https://gymvisual.com/'
  fallbackIconSize?: number     // default 48

Existing @ziko/plugin-sdk exports available to packages/ui (packages/plugin-sdk/src/index.ts line 7 does `export * from './i18n'`):
  export type Locale = 'fr' | 'en'
  export function useTranslation(): { t, tExercise, tMuscle, tCategory, tMeal, locale }
  export const useThemeStore

Existing packages/ui export style (packages/ui/src/index.ts lines 32-33):
  export { EmptyState } from './components/EmptyState';
  export { ErrorScreen } from './components/ErrorScreen';

Module resolution note (verified): `expo-image` is hoisted to the repo-root `node_modules`, and `packages/ui/src/components/ProfileHero.tsx` already imports `expo-linear-gradient` the same way without declaring it in `packages/ui/package.json`. Do NOT add a dependency entry to `packages/ui/package.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exercise.mediaUnavailable / instructionsEmptyTitle / instructionsEmptyBody keys to the fr and en dictionaries</name>

  <files>packages/plugin-sdk/src/i18n.ts</files>

  <read_first>
    - packages/plugin-sdk/src/i18n.ts — read the `fr` dictionary block around lines 278-283 (`'exercise.category.strength'` … `'exercise.category.stretching'`) and the mirrored `en` block around lines 1102-1107, plus the `t()` implementation around lines 1712-1724, so the new keys land in the same namespace and both dictionaries stay symmetric.
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-UI-SPEC.md — the "Copywriting Contract" table holds the exact literal strings; they are locked copy, do not paraphrase or re-translate.
  </read_first>

  <action>
Add exactly three new keys to the `fr` dictionary object (immediately after `'exercise.category.stretching'`, ~line 283) and the same three keys to the `en` dictionary object (immediately after its `'exercise.category.stretching'`, ~line 1107). Keep both dictionaries in the same key order.

FR values:
  'exercise.mediaUnavailable' -> Aperçu indisponible
  'exercise.instructionsEmptyTitle' -> Consignes à venir
  'exercise.instructionsEmptyBody' -> Les instructions détaillées n'ont pas encore été ajoutées pour cet exercice.

EN values:
  'exercise.mediaUnavailable' -> Preview unavailable
  'exercise.instructionsEmptyTitle' -> Instructions coming soon
  'exercise.instructionsEmptyBody' -> Detailed instructions haven't been added for this exercise yet.

Do NOT add a key for the attribution credit string `© Gym visual — https://gymvisual.com/`. Per 04-UI-SPEC.md's Copywriting Contract it is locked, non-localized legal text and lives only as the `credit` default prop on `AttributedMedia` (Task 2). Adding it to i18n.ts would create a second, drift-prone source of truth for legally mandated text.

Do not touch `t()`, `tExercise`, `useI18nStore`, or any other existing key — the lookup mechanism already picks up new dictionary entries with no code change.
  </action>

  <verify>
    <automated>cd packages/plugin-sdk && npx tsc --noEmit</automated>
    <automated>grep -c "exercise.mediaUnavailable" packages/plugin-sdk/src/i18n.ts</automated>
    <automated>grep -c "exercise.instructionsEmptyTitle" packages/plugin-sdk/src/i18n.ts</automated>
    <automated>grep -c "exercise.instructionsEmptyBody" packages/plugin-sdk/src/i18n.ts</automated>
  </verify>

  <acceptance_criteria>
    - `cd packages/plugin-sdk && npx tsc --noEmit` exits 0 and prints no output (verified pre-existing baseline for this package is 0 errors — any output is a regression introduced by this task).
    - `grep -c "exercise.mediaUnavailable" packages/plugin-sdk/src/i18n.ts` prints exactly `2` (one fr entry, one en entry).
    - `grep -c "exercise.instructionsEmptyTitle" packages/plugin-sdk/src/i18n.ts` prints exactly `2`.
    - `grep -c "exercise.instructionsEmptyBody" packages/plugin-sdk/src/i18n.ts` prints exactly `2`.
    - `grep -c "gymvisual.com" packages/plugin-sdk/src/i18n.ts` prints `0` — the attribution credit string must NOT be added to the dictionaries.
    - `grep -c "Aperçu indisponible" packages/plugin-sdk/src/i18n.ts` prints `1` and `grep -c "Preview unavailable" packages/plugin-sdk/src/i18n.ts` prints `1`.
  </acceptance_criteria>

  <done>Three `exercise.*` keys exist in both the fr and en dictionaries with the exact UI-SPEC copy, plugin-sdk type-checks clean, and the attribution credit string is absent from i18n.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Create the AttributedMedia shared component and export it from @ziko/ui</name>

  <files>packages/ui/src/components/AttributedMedia.tsx, packages/ui/src/index.ts</files>

  <read_first>
    - packages/ui/src/components/EmptyState.tsx — the direct structural template for a new file in this package: props interface -> config -> functional component -> named export + default export, inline style objects only, no StyleSheet.
    - packages/ui/src/index.ts — the export-registration convention (named re-exports from './components/X').
    - packages/ui/src/components.tsx — how shared components in this package consume `useThemeStore` from `@ziko/plugin-sdk` (`Card`, `Badge`), and the `spacing` / `radius` token exports.
    - packages/ui/src/components/ProfileHero.tsx — precedent for importing a hoisted Expo package (`expo-linear-gradient`) from inside packages/ui without a package.json entry.
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-UI-SPEC.md §2 and §4 — the locked prop interface, badge pixel values, and the hero-size fallback placeholder spec.
    - apps/mobile/app/(app)/ai/index.tsx lines 14 and 322-327 — the in-repo `expo-image` usage precedent (`contentFit`, `transition`).
  </read_first>

  <action>
Create `packages/ui/src/components/AttributedMedia.tsx` implementing the locked `AttributedMediaProps` interface from `<interfaces>` above. Export it as a named export `AttributedMedia` plus a default export, matching `EmptyState.tsx`'s shape. Use inline style objects only — no `StyleSheet` (CLAUDE.md constraint).

Behaviour contract (all three clauses are what MOBILE-03's "structurally enforces" means — a caller must not be able to opt out):

1. Size clamp: compute the rendered box edge as `Math.min(size ?? 180, 180)`. The component renders its outer `View` at exactly that width and height. A caller passing 343 gets 180. This is the structural enforcement of MEDIA-03 / MOBILE-03's 180x180 cap — do not make it conditional, do not expose an escape hatch prop.

2. Media path (`uri` truthy): render `Image` from `'expo-image'` with `source={{ uri }}`, `style={{ width: '100%', height: '100%' }}`, `contentFit="cover"`, `transition={150}`. Do not add any play button, duration text, or resolution badge — that chrome is being deleted this phase (D-02). Do not add a `?t=` cache-busting suffix; `exercise-media` assets are immutable (04-RESEARCH.md Pattern 1).

3. Fallback path (`uri` falsy): render a centered `Ionicons` named `barbell-outline` at `fallbackIconSize ?? 48` in color `#6B6963`, with the caption `t('exercise.mediaUnavailable')` below it at fontSize 11, fontWeight '700', color `#6B6963`, marginTop 8. Get `t` from `useTranslation()` imported from `@ziko/plugin-sdk` (this is why Task 1 runs first). In this path the badge must NOT mount — there is no Gym visual asset to credit (D-07).

Outer container style: width/height = the clamped edge, `borderRadius: borderRadius ?? 14`, `overflow: 'hidden'`, `position: 'relative'`, `alignItems: 'center'`, `justifyContent: 'center'`, `backgroundColor: '#FFFFFF'`.

Badge (mounts only when `showBadge !== false` AND `uri` is truthy): absolutely positioned `View` at `bottom: 8`, `right: 8`, `paddingHorizontal: 8`, `paddingVertical: 4`, `borderRadius: 6`, `backgroundColor: 'rgba(28,26,23,0.55)'`, containing a `Text` with `fontSize: 9`, `fontWeight: '700'`, `color: '#FFFAF6'`, `letterSpacing: 0`, `numberOfLines={1}`, rendering the `credit` prop. The `credit` default value is the exact string `© Gym visual — https://gymvisual.com/` (em dash, trailing slash, no shortening, no line wrap, not localized).

Use literal hex/rgba values as given above rather than `theme.*` lookups — `EmptyState.tsx` sets this precedent, and 04-PATTERNS.md explicitly prefers literals here so the legally-mandated attribution styling cannot drift if the theme palette changes. `useThemeStore` is therefore not needed in this file.

Then register the export in `packages/ui/src/index.ts` by appending a line after the existing `ErrorScreen` export (line 33): `export { AttributedMedia } from './components/AttributedMedia';`

Do NOT add `expo-image` or `@expo/vector-icons` to `packages/ui/package.json` — both are hoisted to the repo-root `node_modules` and other files in this package already import them without declarations.
  </action>

  <verify>
    <automated>cd packages/ui && npx tsc --noEmit</automated>
    <automated>grep -c "export { AttributedMedia } from './components/AttributedMedia';" packages/ui/src/index.ts</automated>
    <automated>grep -c "Math.min(size ?? 180, 180)" packages/ui/src/components/AttributedMedia.tsx</automated>
    <automated>grep -c "gymvisual.com" packages/ui/src/components/AttributedMedia.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `packages/ui/src/components/AttributedMedia.tsx` exists and is at least 60 lines.
    - `cd packages/ui && npx tsc --noEmit` exits 0 and prints no output (verified pre-existing baseline for this package is 0 errors).
    - `grep -c "export { AttributedMedia } from './components/AttributedMedia';" packages/ui/src/index.ts` prints `1`.
    - `grep -c "Math.min(size ?? 180, 180)" packages/ui/src/components/AttributedMedia.tsx` prints `1` — the clamp is present verbatim and unconditional.
    - `grep -c "gymvisual.com" packages/ui/src/components/AttributedMedia.tsx` prints `1` and the surrounding string is exactly `© Gym visual — https://gymvisual.com/`.
    - `grep -c "barbell-outline" packages/ui/src/components/AttributedMedia.tsx` prints `1`.
    - `grep -c "exercise.mediaUnavailable" packages/ui/src/components/AttributedMedia.tsx` prints `1` — the fallback caption goes through `t()`, not a hardcoded ternary.
    - `grep -c "from 'expo-image'" packages/ui/src/components/AttributedMedia.tsx` prints `1`.
    - `grep -c "StyleSheet" packages/ui/src/components/AttributedMedia.tsx` prints `0`.
    - `grep -c "expo-image" packages/ui/package.json` prints `0` — no dependency entry added.
    - Behavior assertion (source-readable): the JSX subtree containing `rgba(28,26,23,0.55)` is rendered inside a condition that requires both `showBadge` and `uri`; the `barbell-outline` subtree is rendered only when `uri` is falsy, and no badge markup appears inside it.
  </acceptance_criteria>

  <done>`AttributedMedia` is importable as `import { AttributedMedia } from '@ziko/ui'`, clamps any size to 180, mounts the exact Gym visual badge only alongside real media, and falls back to a barbell placeholder with a localized caption and no badge when `uri` is null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Storage (`exercise-media`, public-read) → mobile client | Media bytes cross into the app. Bucket is public-read by design (MEDIA-02, shipped in Phase 1); it has no client INSERT/UPDATE/DELETE policy (`supabase/migrations/20260814_exercise_media_schema.sql`). |

No new trust boundary is introduced by this plan — it adds a presentational component and dictionary strings only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `uri` prop passed into `AttributedMedia`'s `<Image source>` | accept | Not exploitable: callers build the `uri` from `exercises.image`/`.gif`, columns written exclusively by the service-role Phase 3 merge script (`storagePaths()` derives paths from the exercise UUID only, never from dataset text). No user-controlled input reaches this component. Matches 04-RESEARCH.md Security Domain, which rates ASVS V5 "marginal / not exploitable" for this phase. |
| T-04-02 | Information Disclosure | Public-read `exercise-media` bucket | accept | Public-read is the shipped, intentional design (MEDIA-02). Assets are licensed third-party exercise GIFs containing no PII or user data. |
| T-04-03 | Repudiation (licence attribution) | Attribution badge omission | mitigate | The `credit` default and the `showBadge && uri` mount condition live inside `AttributedMedia` itself, so a consuming screen cannot render Gym visual media through this component without the credit. Grep gate on `gymvisual.com` in the acceptance criteria enforces the exact locked string. |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | This phase installs zero packages (04-RESEARCH.md "Package Legitimacy Audit: Not applicable"). Executors must not add dependency entries; acceptance criteria assert `packages/ui/package.json` is unchanged w.r.t. expo-image. |

ASVS L1 categories V2/V3/V4/V6 do not apply — no auth, session, access-control, or cryptography surface is touched.
</threat_model>

<verification>
- `cd packages/plugin-sdk && npx tsc --noEmit` → clean (baseline 0 errors)
- `cd packages/ui && npx tsc --noEmit` → clean (baseline 0 errors)
- All grep assertions in both tasks' acceptance criteria return their stated counts
</verification>

<success_criteria>
- `AttributedMedia` is exported from `@ziko/ui` and satisfies the locked 04-UI-SPEC.md §2 prop interface
- The 180x180 cap and the credit badge cannot be bypassed by a caller
- `t('exercise.mediaUnavailable')`, `t('exercise.instructionsEmptyTitle')`, `t('exercise.instructionsEmptyBody')` resolve in both fr and en
- No new npm dependency added anywhere
</success_criteria>

<output>
Create `.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-01-SUMMARY.md` when done
</output>
