---
phase: 04-mobile-consumption-attribution
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04, MOBILE-05, MOBILE-06]

must_haves:
  truths:
    - "No source file in apps/mobile still references an unversioned exercises query key or the deleted fake-video chrome"
    - "A human has seen the real GIF hero with the attribution badge on a device or simulator"
    - "A human has seen thumbnails in ExercisePicker and confirmed no badge appears on list rows"
    - "A human has seen numbered instructions render in both fr and en"
    - "A human has seen the barbell placeholder path for an exercise with no media"
  artifacts:
    - path: ".planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-04-SUMMARY.md"
      provides: "Recorded manual verification outcome for all six MOBILE requirements"
      contains: "MOBILE-06"
  key_links:
    - from: "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"
      to: "packages/ui/src/components/AttributedMedia.tsx"
      via: "@ziko/ui import resolved at runtime on device"
      pattern: "AttributedMedia"
---

<objective>
Close the phase with a repo-wide static sweep plus the manual device verification that `04-VALIDATION.md` designates as this phase's only real gate.

Purpose: `apps/mobile` has no test runner (zero `*.test.tsx` files, no `jest.config.*`, no `test` script) — `04-VALIDATION.md` was approved as manual-only. Every MOBILE requirement in this phase is visual. Static greps can prove the old code is gone and the new wiring exists; only a human looking at a screen can prove the GIF actually animates, the badge is legible, and the locale switch works.
Output: a static-sweep result plus a signed-off manual verification record in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/image-exo/REQUIREMENTS.md
@.planning/workstreams/image-exo/ROADMAP.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-CONTEXT.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-UI-SPEC.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-VALIDATION.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-01-SUMMARY.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-02-SUMMARY.md
@.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the phase-wide static verification sweep</name>

  <files>none (read-only verification)</files>

  <read_first>
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-VALIDATION.md — the "Manual-Only Verifications" table; this task covers the statically-provable half of it, Task 2 covers the rest.
    - apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx — the post-plan-04-03 state.
    - apps/mobile/src/components/ExercisePicker.tsx — the post-plan-04-02 state.
    - packages/ui/src/components/AttributedMedia.tsx and packages/ui/src/index.ts — the post-plan-04-01 state.
  </read_first>

  <action>
Run the sweep below and record every command's actual output in the plan summary as a table. Do not edit any source file in this task — if an assertion fails, report it as a gap for `/gsd:verify-work` rather than patching it here.

Type gates (three workspaces):
  - `cd packages/ui && npx tsc --noEmit` — must exit 0 with no output.
  - `cd packages/plugin-sdk && npx tsc --noEmit` — must exit 0 with no output.
  - `cd apps/mobile && npx tsc --noEmit 2>&1 | wc -l` — must print `6`, and the 6 lines must all name `app/(app)/profile/settings.tsx` or `src/hooks/useNotificationSetup.ts`. These are the documented pre-existing `NotificationPermissionsStatus` errors and are out of this phase's scope; any seventh line, or any line naming a file this phase touched, is a regression.

Dead-code gates (the fake chrome and the parse chain must be absent repo-wide in `apps/mobile`, not just in the touched file):
  - `grep -rn "0:42" apps/mobile/app apps/mobile/src` — expect no matches.
  - `grep -rn "aspectRatio: 16 / 9" apps/mobile/app/\(app\)/workout` — expect no matches.
  - `grep -rn "JSON.parse" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` — expect no matches.

Query-key gates (MOBILE-06):
  - `grep -rn "exercises-picker" apps/mobile` — expect no matches.
  - `grep -rn "queryKey: \['exercise', exerciseId\]" apps/mobile` — expect no matches.
  - `grep -rn "'exercises', 'v2'" apps/mobile` — expect exactly 2 matches: `[exerciseId].tsx` and `ExercisePicker.tsx`.
  - Confirm the documented exclusions are still intact and unmodified: `apps/mobile/src/components/SearchOverlay.tsx` still uses `queryKey: ['search_exercises', debouncedQuery]` and still selects only `id, name, category` (no media, so out of MOBILE-06 scope), and `apps/mobile/src/stores/workoutStore.ts` line ~284 still uses a direct non-TanStack fetch. Neither should have been edited by this phase. Record this explicitly so a future auditor does not have to re-derive it.

Attribution gates (MOBILE-03 and the D-06 narrowing):
  - `grep -rn "gymvisual.com" packages apps` — expect exactly 1 match, in `packages/ui/src/components/AttributedMedia.tsx`. A second occurrence means the legally-locked credit string was duplicated and can drift.
  - `grep -c "AttributedMedia" "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"` — expect `2` (import + single usage; D-05 one badge per screen).
  - `grep -c "AttributedMedia" apps/mobile/src/components/ExercisePicker.tsx` — expect `0` (D-06: list thumbnails are deliberately unattributed).

Dependency gate:
  - `git diff --stat HEAD~<phase-start> -- '**/package.json' 'package-lock.json'` (or equivalent range covering this phase's commits) — expect zero changes. This phase installs no packages (04-RESEARCH.md Package Legitimacy Audit: "Not applicable").
  </action>

  <verify>
    <automated>cd packages/ui && npx tsc --noEmit && cd ../plugin-sdk && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | wc -l</automated>
    <automated>grep -rn "'exercises', 'v2'" apps/mobile | wc -l</automated>
    <automated>grep -rn "gymvisual.com" packages apps | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `cd packages/ui && npx tsc --noEmit` and `cd packages/plugin-sdk && npx tsc --noEmit` both exit 0 with empty output.
    - `cd apps/mobile && npx tsc --noEmit 2>&1 | wc -l` prints `6`, and no line names `[exerciseId].tsx`, `ExercisePicker.tsx`, or `AttributedMedia.tsx`.
    - `grep -rn "0:42" apps/mobile/app apps/mobile/src` returns nothing.
    - `grep -rn "exercises-picker" apps/mobile` returns nothing.
    - `grep -rn "'exercises', 'v2'" apps/mobile | wc -l` prints `2`.
    - `grep -rn "gymvisual.com" packages apps | wc -l` prints `1`, and the single hit is in `packages/ui/src/components/AttributedMedia.tsx`.
    - `grep -c "AttributedMedia" apps/mobile/src/components/ExercisePicker.tsx` prints `0`.
    - `git status --porcelain -- '**/package.json' package-lock.json` shows no modification attributable to this phase.
    - The plan summary contains a table with one row per command above and its actual observed output.
  </acceptance_criteria>

  <done>Every statically-provable assertion for MOBILE-01 through MOBILE-06 has been executed, its real output recorded, and any failures written up as gaps rather than silently patched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual device verification of all six MOBILE requirements</name>

  <files>none (human verification)</files>

  <read_first>
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-VALIDATION.md — the "Manual-Only Verifications" table is the source of these steps.
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-UI-SPEC.md — the visual contract each observation is checked against.
    - .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-CONTEXT.md — D-06, whose consequence (no badge on list rows) is intentional and must not be reported as a defect.
  </read_first>

  <action>
Halt autonomous execution and hand control to the developer. Start the Expo dev server (`npm run mobile`, or `cd apps/mobile && npx expo start`) so a device or simulator build is reachable, then present the `<how-to-verify>` checklist below verbatim and wait for the resume signal. Do not self-approve, do not infer PASS from Task 1's static sweep, and do not modify source files in this task. Record each of the 8 checks as PASS or FAIL with the developer's own observation in the plan summary. If any check FAILs, capture the screen, the exercise used, and the observed-vs-expected difference, and carry it forward as a gap for `/gsd:verify-work` rather than patching it here.
  </action>

  <what-built>
    Plan 04-01 added `<AttributedMedia>` to `@ziko/ui` (180x180 clamp + Gym visual badge + barbell fallback) and three FR/EN i18n keys.
    Plan 04-02 gave `ExercisePicker` rows a 40x40 thumbnail slot and moved its query to `['exercises', 'v2', 'picker']`.
    Plan 04-03 replaced the fake 16:9 `Démo · 0:42` / `HD` video placeholder with a full-width square card containing the real 180x180 animated GIF plus the attribution badge, rewired the Consignes tab onto `instruction_steps` JSONB with a locale-aware selection and a legacy-prose fallback, localized the header title via `tExercise`, and moved the detail query to `['exercises', 'v2', exerciseId]`.
    Task 1 above already confirmed the static side; what remains cannot be proved without eyes on a screen.
  </what-built>

  <how-to-verify>
    Start the app: `npm run mobile` (or `cd apps/mobile && npx expo start`), then open it on a device or simulator.

    1. MOBILE-01 — hero media. Navigate to any standard (non-custom) exercise detail screen. Confirm: the hero is a full-width SQUARE card with the app's usual white surface, 1px border and soft shadow; a real exercise GIF plays inside it at roughly 180x180, centered; it animates continuously with no tap required. Confirm there is NO play button, NO `HD` badge, NO `Démo · 0:42` text, and no black video frame anywhere.

    2. MOBILE-03 — attribution. On that same screen, confirm a small dark semi-transparent badge reading exactly `© Gym visual — https://gymvisual.com/` sits in the bottom-right corner of the GIF, is legible, and fits on one line without truncation. Confirm it appears exactly ONCE on the whole screen.

    3. MOBILE-04 — instructions. Open the `Consignes` tab. Confirm the execution steps render as a numbered list with the orange circled numbers, one step per row, with real sentence content (not a single blob of JSON and not a wall of text). Check the Metro/console output: there must be no JSON parse errors or `undefined` warnings.

    4. MOBILE-05 — locale. Switch the app language to English, reopen the same exercise. Confirm the screen title switches to the English exercise name and the numbered steps switch to English text. Switch back to French and confirm both revert.

    5. MOBILE-02 — picker thumbnails. Tap `Ajouter à ma séance` to open `ExercisePicker`. Confirm every row shows a 40x40 rounded-square image to the LEFT of the checkbox, that images are static (not animating), and that row alignment is uniform down the whole list.

    6. D-06 — deliberate non-attribution. In that same picker, confirm NO attribution badge appears on any row thumbnail. This is intentional (CONTEXT.md D-06: attribution is once per screen on the primary media, not per media instance). Do not report it as a defect.

    7. D-07 / D-08 — missing media. Find or create a custom exercise (one you added yourself, `is_custom = true`; these are the only rows in production with no media — the 6 unmatched-new dataset rows were never inserted). Confirm its detail hero shows a centered grey `barbell-outline` icon with the caption `Aperçu indisponible` (or `Preview unavailable` in EN) and NO attribution badge, in the same square slot a real GIF would occupy — the hero block must not collapse. Confirm its picker row shows the same barbell icon in a bordered 40x40 slot with the row still perfectly aligned.

    8. MOBILE-04 fallback — for that same custom exercise, if you gave it free-text instructions, confirm the `Consignes` tab shows them as a plain paragraph. If it has none at all, confirm the empty state reads `Consignes à venir` / `Les instructions détaillées n'ont pas encore été ajoutées pour cet exercice.`
  </how-to-verify>

  <acceptance_criteria>
    - Each of the 8 checks above is recorded in the plan summary as PASS or FAIL with a one-line observation; no check is left unrecorded.
    - Any FAIL is written up with the screen, the exercise used, and the observed-vs-expected difference, and is carried into `/gsd:verify-work` as a gap.
    - The summary explicitly records that check 6 (no badge on picker rows) passed as intended per D-06, so a later code review or licence audit finds the decision trail.
    - The user has responded with an explicit approval or a list of issues — the phase does not advance on silence.
  </acceptance_criteria>

  <verify>
    <human-check>All 8 checks in `<how-to-verify>` observed on a device or simulator and each recorded PASS or FAIL in the plan summary.</human-check>
    <automated>test -f .planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-04-SUMMARY.md</automated>
  </verify>

  <done>The developer has walked all 8 checks on a real build, every check is recorded PASS or FAIL with an observation, check 6 is recorded as intentionally-no-badge per D-06, and the developer has explicitly approved or listed issues.</done>

  <resume-signal>Type "approved" to close the phase, or describe the issues you saw (screen + exercise + what looked wrong).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Storage (`exercise-media`, public-read) → mobile client | Exercised during manual verification only; unchanged, public-read by design (MEDIA-02, Phase 1). |

This plan writes no code and introduces no boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Repudiation (licence attribution) | Gym visual credit rendering on device | mitigate | Manual check 2 confirms the exact locked string renders legibly and unshortened on real hardware, and Task 1's `gymvisual.com` grep confirms exactly one source of truth for it. Check 6 records the D-06 narrowing as an approved decision rather than an undetected omission. |
| T-04-05 | Information Disclosure | Verification performed against live production data | accept | Verification is read-only on a public-read bucket and RLS-protected rows the verifying user already owns; no data is written, exported, or screenshotted into the repo. |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed this phase; Task 1 asserts no `package.json` / lockfile change occurred across the phase's commits. |

ASVS L1 categories V2/V3/V4/V5/V6 do not apply — this plan changes no code and adds no input, auth, access-control, or cryptography surface.
</threat_model>

<verification>
- Task 1's four automated commands return their stated values
- Task 2's 8 manual checks are each recorded PASS/FAIL in the summary
- User has explicitly approved or listed issues
</verification>

<success_criteria>
- All six MOBILE requirements have been observed working on a real device or simulator
- The static sweep confirms no stale query key, no fake-video chrome, no duplicated credit string, and no dependency drift
- Any gap found is documented for `/gsd:verify-work` rather than silently patched
</success_criteria>

<output>
Create `.planning/workstreams/image-exo/phases/04-mobile-consumption-attribution/04-04-SUMMARY.md` when done
</output>
