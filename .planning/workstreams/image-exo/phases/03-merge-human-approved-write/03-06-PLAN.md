---
phase: 03-merge-human-approved-write
plan: 06
type: execute
wave: 4
depends_on: ["03-01", "03-05"]
files_modified:
  - .planning/workstreams/image-exo/reports/merge-run.md
autonomous: false
requirements: [IMPORT-03, IMPORT-04, IMPORT-05, MEDIA-03, MEDIA-04]

must_haves:
  truths:
    - "The dataset clone on disk is at the exact commit the human approved (7455efae41b330c265e7cd4b78dfa848e7ce5ebd)"
    - "A human typed the confirmation at the merge prompt before any production row changed"
    - "1318 matched exercises retain their original UUIDs and now carry exercise-media storage paths in image and gif"
    - "6 new exercises exist that did not exist before the run"
    - "exercises_merge_backup holds one snapshot row per successfully-updated exercise"
    - "No exercise row was deleted — the non-custom exercises row count went up, never down"
    - "Every uploaded object in exercise-media is at most 180x180"
  artifacts:
    - path: ".planning/workstreams/image-exo/reports/merge-run.md"
      provides: "The recorded outcome of the real supervised merge run — tallies, errored rows, post-run verification numbers"
      min_lines: 30
  key_links:
    - from: "public.exercises.image"
      to: "exercise-media storage bucket"
      via: "relative path {exercise_id}/thumb.png written by merge.ts"
      pattern: "thumb\\.png"
---

<objective>
Run the merge for real against production, under human supervision, and record what actually happened.

Purpose: 03-VALIDATION.md is explicit that automated tests cannot verify Supabase Storage and Postgres write behaviour without live credentials, and that real-DB integration is a manual supervised step matching Phase 2's 02-06 precedent. Every unit test in plans 03-02 through 03-05 proves the shape of the writes; only this plan proves they land. The phase is not functionally complete until this run is done and verified.
Output: A committed `merge-run.md` recording tallies, errors, and post-run verification numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/image-exo/ROADMAP.md
@.planning/workstreams/image-exo/phases/03-merge-human-approved-write/03-CONTEXT.md
@.planning/workstreams/image-exo/phases/03-merge-human-approved-write/03-RESEARCH.md
@.planning/workstreams/image-exo/phases/03-merge-human-approved-write/03-VALIDATION.md
@.planning/workstreams/image-exo/phases/02-download-match-dry-run/02-06-SUMMARY.md
@scripts/exercise-import/README.md

<interfaces>
<!-- The approved report this run consumes — already committed, do NOT regenerate it -->
.planning/workstreams/image-exo/reports/match-report.json
  dataset_commit    = "7455efae41b330c265e7cd4b78dfa848e7ce5ebd"
  dataset_count     = 1324
  production_count  = 1318
  counts            = { matched: 1318, unmatched_new: 6, unmatched_legacy: 0, ambiguous: 0 }

<!-- Commands (all run from the repo root) -->
npx tsx --env-file=backend/api/.env.local scripts/exercise-import/fetch.ts
npx tsx --env-file=backend/api/.env.local scripts/exercise-import/merge.ts

<!-- Expected post-run state -->
public.exercises              non-custom rows: 1318 -> 1324 (never fewer)
public.exercises_merge_backup rows: 1318 (one per successful UPDATE)
public.exercise_import_log    rows: 1324 new (status matched=1318, inserted=6)
exercise-media bucket         objects: 2648 (thumb.png + animation.gif per exercise)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore the dataset clone at the approved commit</name>
  <files>.planning/workstreams/image-exo/reports/merge-run.md</files>
  <read_first>
    - scripts/exercise-import/README.md §"Invocation" and §"Flags" (the `fetch.ts` command and the `--refetch` flag semantics)
    - scripts/exercise-import/fetch.ts (what the clone actually does and what it verifies)
    - .planning/workstreams/image-exo/reports/match-report.json (only the header fields: `dataset_commit`, `dataset_count`, `counts` — do not read the 1,324-row body into context)
    - .planning/workstreams/image-exo/phases/03-merge-human-approved-write/03-RESEARCH.md §"Environment Availability" (`.dataset-cache/` is gitignored and confirmed absent; `SUPABASE_SERVICE_KEY` provisioning status)
  </read_first>
  <action>
`scripts/exercise-import/.dataset-cache/` is gitignored and confirmed absent from this working tree — `merge.ts` cannot run without it, and will hard-exit rather than re-clone implicitly.

Run `npx tsx --env-file=backend/api/.env.local scripts/exercise-import/fetch.ts` from the repo root to restore the clone into `scripts/exercise-import/.dataset-cache/exercises-dataset`.

Then run `git -C scripts/exercise-import/.dataset-cache/exercises-dataset rev-parse HEAD` and compare the result character-for-character against `dataset_commit` in `.planning/workstreams/image-exo/reports/match-report.json`, which is `7455efae41b330c265e7cd4b78dfa848e7ce5ebd`.

If the SHAs match: create `.planning/workstreams/image-exo/reports/merge-run.md` with a "Preflight" section recording the fetch date, the resolved SHA, the record count reported by `fetch.ts`, and confirmation that it equals the approved `dataset_commit`.

If the SHAs DIFFER: STOP. `fetch.ts` clones with `--depth 1`, so it lands on upstream HEAD, which may have moved since the report was approved on 2026-08-15. Do not attempt to check out the old SHA, do not re-run `match.ts`, and do not proceed to Task 2. Report the two SHAs to the user and state the two available paths: (a) fetch the specific approved commit so the already-approved report stays valid, or (b) re-run `match.ts` against the new upstream HEAD and re-approve the regenerated report before merging. This is a scope decision for the user, not an executor choice — the entire write authorization derives from a report a human reviewed against specific dataset content.

Also re-check before finishing — this is a re-verification, not a new request. The credential was provisioned by the operator back in plan 03-01 Task 2 (see 03-01's `user_setup`), and the `fetch.ts` call above already loaded the same file via `--env-file`. Confirm `SUPABASE_SERVICE_KEY` is still non-empty in `backend/api/.env.local` (presence only — never print the value). If it has since gone missing or blank, stop: report that Task 2 is blocked on restoring it from the Supabase dashboard.
  </action>
  <verify>
    <automated>test -f scripts/exercise-import/.dataset-cache/exercises-dataset/data/exercises.json && SHA=$(git -C scripts/exercise-import/.dataset-cache/exercises-dataset rev-parse HEAD) && EXPECTED=$(node -e "console.log(require('./.planning/workstreams/image-exo/reports/match-report.json').dataset_commit)") && [ "$SHA" = "$EXPECTED" ] && echo "DATASET_AT_APPROVED_COMMIT $SHA"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/exercise-import/.dataset-cache/exercises-dataset/data/exercises.json` exists.
    - `git -C scripts/exercise-import/.dataset-cache/exercises-dataset rev-parse HEAD` prints exactly `7455efae41b330c265e7cd4b78dfa848e7ce5ebd`, or execution stopped and escalated the mismatch to the user without proceeding.
    - `scripts/exercise-import/.dataset-cache/exercises-dataset/images` and `.../videos` each contain 1324 files.
    - `.planning/workstreams/image-exo/reports/merge-run.md` exists with a "Preflight" section naming the resolved SHA.
    - `.dataset-cache/` is still untracked by git (`git status --porcelain scripts/exercise-import/.dataset-cache` prints nothing).
    - `SUPABASE_SERVICE_KEY` is re-confirmed non-empty in `backend/api/.env.local` (re-check of the 03-01 Task 2 provisioning), with the value never printed.
  </acceptance_criteria>
  <done>The dataset clone exists at the exact approved commit, the report's authorization is still valid, and the service-role credential provisioned in 03-01 is still in place.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>[BLOCKING] Task 2: Supervised interactive merge run against production</name>
  <files>.planning/workstreams/image-exo/reports/merge-run.md</files>
  <read_first>
    - scripts/exercise-import/README.md §"Invocation" and §"Resuming a merge run"
    - scripts/exercise-import/merge.ts (the summary the prompt prints and the exact confirmation string it accepts)
    - .planning/workstreams/image-exo/phases/03-merge-human-approved-write/03-CONTEXT.md (D-04, D-05, D-06, D-08 — what the prompt is asking you to authorize)
  </read_first>
  <action>
Hand this run to the operator — it cannot be executed from an agent shell. `merge.ts` calls `process.exit(1)` when `process.stdin.isTTY` is falsy and exposes no `--yes`/`--force` bypass, so a non-interactive invocation writes zero rows by design.

Present the command `npx tsx --env-file=backend/api/.env.local scripts/exercise-import/merge.ts`, the expected pre-confirmation summary (1318 UPDATE / 6 INSERT / 0 needs_review), the accepted confirmation string (`yes`), and the roughly 20-45 minute expected duration. Do NOT attempt to pipe `yes` into the command, do not use `script`/`unbuffer`/pty emulation to fake a TTY, and do not edit `merge.ts` to relax the gate.

Block until the operator pastes back the `=== Merge Complete ===` summary block. Append it verbatim to `.planning/workstreams/image-exo/reports/merge-run.md` under a "Run output" section, along with any unmappable-category warning text from the pre-confirmation summary. If the operator reports a non-zero errored tally, ask them to re-run the same command once (errored rows are picked up automatically per D-08) and record that second summary too.
  </action>
  <what-built>
Everything the merge needs is in place: the migration is live (plan 03-01), the media cap and retry helpers are unit-proven (03-02), the write client, resume reducer and category guard are unit-proven (03-03), the per-row ordering guarantees are unit-proven (03-04), and `merge.ts` is written and refuses non-TTY invocation (03-05). Task 1 restored the dataset clone at the approved commit.

This step cannot be automated — by design. `merge.ts` hard-exits when stdin is not a TTY and has no `--yes` bypass, which is the mechanism enforcing roadmap success criterion #1. An agent shell is not an interactive terminal, so a human must run this one.

Expected duration: roughly 20-45 minutes for 1,324 sequential rows (two media uploads, one backup insert, and one write per row). Progress prints every 50 rows.
  </what-built>
  <how-to-verify>
1. Open a real terminal at the repo root (`C:\ziko-platform`).
2. Run: `npx tsx --env-file=backend/api/.env.local scripts/exercise-import/merge.ts`
3. Read the pre-confirmation summary carefully before answering. Check specifically:
   - the counts line reads 1318 UPDATE / 6 INSERT / 0 flagged needs_review
   - the resume breakdown shows 1324 unprocessed on a first run (or a mix of skipped/retry on a re-run)
   - whether an "unmappable category" warning appears. If it does, note which values and how many dataset ids are affected. Matched rows with an unmappable category keep their existing category; unmatched-new rows with one will fail and be logged. If the count is large or surprising, answer anything other than `yes` to abort with zero writes and report back before continuing.
4. Type `yes` to proceed, or anything else to abort.
5. Let the run finish. Copy the entire `=== Merge Complete ===` summary block, including any listed errored rows.
6. If the run reports errored rows, re-run the exact same command once — errored rows are picked up automatically (D-08). Copy that second summary too.
7. Paste both summaries back here.
  </how-to-verify>
  <acceptance_criteria>
    - The merge command was run from an interactive terminal and the operator typed the confirmation.
    - The `=== Merge Complete ===` summary block is captured verbatim and appended to `.planning/workstreams/image-exo/reports/merge-run.md`.
    - If the first run reported a non-zero errored tally, a second run was performed and its summary is also recorded.
    - Any "unmappable category" warning text from the pre-confirmation summary is recorded verbatim.
    - No `SUPABASE_SERVICE_KEY` value appears in the pasted output or in `merge-run.md`.
  </acceptance_criteria>
  <verify>
    <automated>set -a; . ./backend/api/.env.local; set +a; curl -s -I -H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" -H "Prefer: count=exact" -H "Range: 0-0" "$SUPABASE_URL/rest/v1/exercise_import_log?select=id" | grep -i 'content-range'</automated>
  </verify>
  <done>A human ran `merge.ts` interactively to completion, the `=== Merge Complete ===` summary is recorded verbatim in `merge-run.md`, and `exercise_import_log` reports a non-zero row count.</done>
  <resume-signal>Paste the `=== Merge Complete ===` summary block(s), or "aborted" plus the reason.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify the production result and record the run</name>
  <files>.planning/workstreams/image-exo/reports/merge-run.md</files>
  <read_first>
    - .planning/workstreams/image-exo/reports/merge-run.md (the preflight section and the pasted run summaries from Tasks 1-2)
    - .planning/workstreams/image-exo/phases/01-schema-storage-foundation/01-01-SUMMARY.md §"Task 3 Verification (live production checks)" (the PostgREST-via-curl verification style to reuse, including `Prefer: count=exact` for row counts)
    - .planning/workstreams/image-exo/ROADMAP.md §"Phase 3" success criteria (the five statements this verification must map onto)
  </read_first>
  <action>
Verify the live result with read-only PostgREST and Storage queries, sourcing `SUPABASE_URL` and `SUPABASE_SERVICE_KEY` from `backend/api/.env.local`. Never print the key.

Run these checks, using the `Prefer: count=exact` header with `Range: 0-0` to read counts from the `Content-Range` response header rather than downloading rows:
1. Non-custom exercise count: `GET /rest/v1/exercises?select=id&is_custom=eq.false` — must be 1324 (was 1318). It must never be below 1318, which is the "no row was ever DELETEd" proof (IMPORT-03).
2. Exercises carrying a new media path: `GET /rest/v1/exercises?select=id&image=like.*thumb.png&gif=like.*animation.gif` — must be 1324.
3. Backup snapshots: `GET /rest/v1/exercises_merge_backup?select=backup_id` — must be 1318, one per UPDATE (MEDIA-04).
4. Import log outcomes: `GET /rest/v1/exercise_import_log?select=status` grouped manually, or one counted request per status value — expect `matched` 1318, `inserted` 6, `needs_review` 0 for this report.
5. Errored rows: `GET /rest/v1/exercise_import_log?select=source_id,status,error_message&error_message=not.is.null&order=processed_at.desc&limit=25` — record every distinct `error_message` found. A non-empty result is not automatically a failure (D-06 log-and-continue is the designed behaviour), but each distinct message must be recorded and triaged in the report.
6. Bilingual instructions landed: `GET /rest/v1/exercises?select=id&instructions_fr=not.is.null` and `...&instruction_steps=not.is.null` — both should be at or near 1324; record the actual numbers.
7. UUID preservation spot-check (IMPORT-03): pick 3 `exercise_id` values from `match-report.json`'s `matched` array and confirm each still exists via `GET /rest/v1/exercises?select=id,name,image&id=eq.<uuid>`, returning one row each with a populated `image`. This proves the FK targets used by `program_exercises`/`session_sets` were preserved rather than replaced.
8. FK integrity: `GET /rest/v1/program_exercises?select=id&limit=1` and `GET /rest/v1/session_sets?select=id&limit=1` must both return 200 — no orphaned-FK breakage.
9. Media cap spot-check (MEDIA-03): download 3 objects from the public bucket (`GET /storage/v1/object/public/exercise-media/<exercise_id>/thumb.png` and one `animation.gif`) and confirm with `sharp(buffer).metadata()` that width and height are each at most 180, and that the GIF's `pages` is greater than 1.

Write all results into `.planning/workstreams/image-exo/reports/merge-run.md` under a "Post-run verification" section: one line per check with the expected value, the observed value, and pass/fail. Add a "Roadmap success criteria" subsection mapping each of the five Phase 3 criteria to the specific check(s) that evidence it. Add an "Errors and triage" subsection listing every distinct `error_message` with its row count and a one-line assessment.

If any check fails, do NOT re-run the merge automatically — record the failure and escalate to the user with the specific numbers.
  </action>
  <verify>
    <automated>set -a; . ./backend/api/.env.local; set +a; curl -s -I -H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" -H "Prefer: count=exact" -H "Range: 0-0" "$SUPABASE_URL/rest/v1/exercises?select=id&is_custom=eq.false" | grep -i 'content-range'; curl -s -I -H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" -H "Prefer: count=exact" -H "Range: 0-0" "$SUPABASE_URL/rest/v1/exercises_merge_backup?select=backup_id" | grep -i 'content-range'</automated>
  </verify>
  <acceptance_criteria>
    - Non-custom `exercises` count is exactly 1324 and is greater than the pre-run 1318 (no deletions).
    - `exercises_merge_backup` count equals the number of successful UPDATEs reported by the run summary (expected 1318).
    - `exercise_import_log` contains one row per report row for this run, with statuses drawn only from `matched`/`inserted`/`skipped`/`needs_review`.
    - Three sampled matched `exercise_id` UUIDs from `match-report.json` still resolve to existing rows with a populated `image` path.
    - `program_exercises` and `session_sets` both still return 200.
    - Three sampled storage objects have both dimensions at most 180, and the sampled GIF reports `pages` greater than 1.
    - `.planning/workstreams/image-exo/reports/merge-run.md` contains a "Post-run verification" table, a "Roadmap success criteria" mapping covering all five Phase 3 criteria, and an "Errors and triage" subsection.
    - No `SUPABASE_SERVICE_KEY` value appears anywhere in `merge-run.md`.
  </acceptance_criteria>
  <done>Production reflects the merge, no row was deleted, backups and audit-log rows reconcile with the run summary, sampled media respects the 180×180 cap, and it is all recorded in `merge-run.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator terminal → 1,324 production writes | The live human gate is exercised for real here for the first time |
| upstream GitHub repo → local dataset clone | Freshly re-cloned content that must match what was reviewed |
| verification queries → production | Read-only service-role reads over a live table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | Upstream dataset moved since approval | mitigate | Task 1 compares the freshly-cloned `rev-parse HEAD` against `report.dataset_commit` and STOPS on mismatch, escalating the re-approve-or-refetch decision to the user rather than merging content no human reviewed |
| T-03-13 | Tampering | Approval gate exercised by an automation instead of a person | mitigate | Task 2 is a blocking human-action checkpoint; the agent shell cannot satisfy `process.stdin.isTTY`, so the gate is enforced by mechanism as well as by process |
| T-03-12 | Destruction | Irreversible loss of production exercise data | mitigate | Verified after the fact: `exercises_merge_backup` count reconciles 1:1 with the UPDATE tally, non-custom row count went UP not down, and three sampled original UUIDs still resolve — the three independent signals that nothing was deleted or re-keyed |
| T-03-02 | Information Disclosure | Service-role key leaking into a committed report | mitigate | Key is sourced from gitignored `backend/api/.env.local`, referenced only as a shell variable, and grep-asserted absent from `merge-run.md`; curl verification reads only headers and counts |
| T-03-16 | Information Disclosure | Committing the 17 MB dataset clone or its media | mitigate | `.dataset-cache/` is gitignored (Phase 2); Task 1 acceptance criteria assert `git status --porcelain` on that path prints nothing |
</threat_model>

<verification>
- Dataset clone SHA equals `7455efae41b330c265e7cd4b78dfa848e7ce5ebd`
- The `=== Merge Complete ===` block was produced by a human-supervised interactive run and is recorded verbatim
- Non-custom `exercises` count moved 1318 → 1324, never below 1318
- `exercises_merge_backup` count reconciles with the UPDATE tally
- Sampled storage objects are at most 180×180 with GIF animation preserved
- `program_exercises` and `session_sets` still return 200
</verification>

<success_criteria>
- All five Phase 3 roadmap success criteria are evidenced by a named check in `merge-run.md`
- `merge-run.md` is committed with preflight, run summaries, post-run verification, and error triage
- Any errored rows are enumerated with their `error_message` and an assessment
</success_criteria>

<output>
Create `.planning/workstreams/image-exo/phases/03-merge-human-approved-write/03-06-SUMMARY.md` when done
</output>
