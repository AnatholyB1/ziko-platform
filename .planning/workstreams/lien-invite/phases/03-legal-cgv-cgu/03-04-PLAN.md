---
phase: 03-legal-cgv-cgu
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - .planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-APPROVAL.md
  - apps/web/src/app/[locale]/(marketing)/cgv/page.tsx
  - apps/web/src/app/[locale]/(marketing)/cgu/page.tsx
  - apps/web/src/content/legal/founder-offer.ts
  - apps/web/src/content/legal/cgv.ts
  - apps/web/test/legal/cgv-content.test.ts
  - apps/web/test/legal/cgv-cgu-consistency.test.ts
  - .planning/workstreams/lien-invite/REQUIREMENTS.md
autonomous: false
requirements: [LEGAL-01, LEGAL-02, LEGAL-03, LEGAL-04, LEGAL-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: Phase 3 does not close on drafting alone — the terminal task is a blocking gate that only clears on the user's explicit confirmation that real outside counsel has reviewed and approved the CGV and CGU text. It is never auto-approved, and workflow.auto_advance does not apply to it."
    - "LEGAL-05: a written approval record exists naming the approving counsel, the approval date, and the git SHA of the exact text approved — so Phase 4 and Phase 6 can verify the ordering constraint rather than assume it."
    - "LEGAL-05: the ROADMAP records Phase 4 as depending on Phase 3, and the approval record restates that the credit-gate change must not activate before this text is live."
    - "D-01 close-out: the draft-pending-review banner is removed from both the CGV and the CGU only after approval, in the same commit that removes the test assertions guarding it — so the banner can never be silently dropped while review is still open."
    - "Counsel's revisions, if any, are applied to the shipped constants before the banner comes down, and the resulting text still passes every LEGAL-02/03/04 assertion from plans 01 and 02."
  artifacts:
    - .planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-APPROVAL.md
  key_links:
    - "03-COUNSEL-BRIEFING.md (plan 03) → the user's lawyer → this plan's blocking gate: the briefing is what makes the gate answerable rather than an indefinite stall."
    - "This gate → Phase 4's start (LEGAL-05) and Phase 6's activation checklist criterion 2: the credit-gate flag must not flip before this text is live."
    - "DRAFT_REVIEW_BANNER + its assertions in cgv-content.test.ts and cgv-cgu-consistency.test.ts → removed together in one commit: the coupling is what prevents a silent early removal."
  prohibitions:
    - "The counsel-approval gate is not auto-approved, auto-advanced, self-answered, or satisfied by Claude's own assessment that the drafting looks sound."
    - "The draft-pending-review banner is not removed, hidden, or weakened before the gate clears."
    - "The phase is not marked complete on the strength of drafting alone."
    - "Counsel's revisions are not paraphrased or partially applied — if a returned clause is ambiguous, the gate reopens rather than the executor guessing."
    - "The [TBD] notice-period placeholder is not resolved by anyone other than counsel."
  edge_probe_note: >
    The deterministic edge-probe engine classified all 9 phase requirements as
    category=unclassified / status=unresolved — expected for content-and-policy
    requirements outside its data-shape taxonomy. Surfaced as flagged assumptions,
    not auto-resolved and not dismissed. LEGAL-05 in particular has no automated
    test by design (03-VALIDATION.md marks it a process gate); its verification is
    the human gate below plus the written approval record. Prohibitions are
    descriptor-less (flagged-unverified).
---

<objective>
Close the phase the way D-01 requires: a blocking human gate that only clears when real outside
counsel has reviewed and approved the CGV and CGU text, followed by the clean-up that removes the
draft-pending marking and records the LEGAL-05 ordering guarantee Phase 4 and Phase 6 depend on.

Purpose: LEGAL-05's "reviewed by counsel before publish" is binding on this phase's own completion,
not merely on Phase 4's start. Everything plans 01 through 03 produced is a high-quality draft; this
plan is the only place it becomes approved text.

Output: `03-COUNSEL-APPROVAL.md`, counsel's revisions applied to the shipped constants, and the
draft-pending banner removed from both pages together with the assertions that guarded it.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/REQUIREMENTS.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-CONTEXT.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-01-SUMMARY.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-02-SUMMARY.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-03-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Owner plan |
|----------|------|-----------|
| `apps/web/src/content/legal/founder-offer.ts` — `LocalizedText`, `WAITLIST_RETENTION_YEARS`, `DRAFT_REVIEW_BANNER`, `AI_CREDIT_CAP_SENTENCE`, `LIFETIME_SCOPE_SENTENCE`, `SHUTDOWN_MODIFICATION_CLAUSE`, `RETENTION_STATEMENT`, `LANGUAGE_PRECEDENCE_CLAUSE` | new module | 03-01 |
| `apps/web/src/content/legal/founder-offer.ts` — appended `ERASURE_REQUEST_STATEMENT`, `CONSENT_CHECKBOX_LABEL`, `COLLECTION_POINT_NOTICE`, `CONSENT_VERSION` | module additions | 03-02 |
| `apps/web/src/content/legal/cgv.ts` — `LegalSection`, `CGV_SECTIONS_FR`, `CGV_SECTIONS_EN`, `CGV_LAST_UPDATED`, `cgvSections(locale)` | new module | 03-01 |
| `apps/web/src/app/[locale]/(marketing)/cgv/page.tsx` — `generateMetadata`, `CgvPage` | new route | 03-01 |
| `apps/web/src/components/layout/Footer.tsx` / `FooterClient.tsx` — `cgv` prop + `/cgv` nav link | modified | 03-01 |
| `apps/web/messages/fr.json` / `en.json` — `Metadata.cgvTitle`, `Metadata.cgvDescription`, `Footer.cgv` | modified | 03-01 |
| `apps/web/src/app/[locale]/(marketing)/cgu/page.tsx` — draft banner + AI-credit-cap section | modified | 03-02 |
| `apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx` — waitlist retention + erasure section | modified | 03-02 |
| `supabase/migrations/20260815_waitlist_retention_config.sql` — `app_config` row `waitlist_retention_years` | new migration | 03-03 |
| `backend/api/test/rls/waitlist-config-rpc.spec.ts` — retention-row coverage | modified | 03-03 |
| `scripts/waitlist-erasure/erase.mjs` — `parseErasureArgs`, `runErasure`, `writeErasureLog` | new script | 03-03 |
| `scripts/waitlist-erasure/RUNBOOK.md` | new doc | 03-03 |
| `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md` | new doc | 03-03 |
| `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-APPROVAL.md` | new doc | 03-04 |
| `apps/web/test/legal/cgv-locale.test.ts` | new test | 03-01 |
| `apps/web/test/legal/cgv-content.test.ts` | new test | 03-01 |
| `apps/web/test/legal/cgv-cgu-consistency.test.ts` | new test | 03-02 |
| `apps/web/test/legal/consent-copy.test.ts` | new test | 03-02 |
| `apps/web/test/legal/retention-config.test.ts` | new test | 03-03 |
| `apps/web/test/legal/erasure-script.test.ts` | new test | 03-03 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-review walkthrough — the draft is honest, bilingual, and complete</name>
  <read_first>
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-01-SUMMARY.md`, `03-02-SUMMARY.md`, `03-03-SUMMARY.md`.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-VALIDATION.md` — the Manual-Only Verifications table, which names exactly these three judgement calls as untestable.
  </read_first>
  <what-built>
Plans 01 through 03 shipped: a bilingual CGV route reachable from the footer, a revised CGU stating
the identical AI-credit cap by shared import, a privacy-policy section carrying the retention period
and the erasure channel, the consent-checkbox and collection-notice copy for Phase 5, an additive
migration seeding `waitlist_retention_years`, a credential-gated erasure script with a runbook, and
the counsel-briefing package. Every automated suite under `apps/web/test/legal` is green. All of it
is marked draft pending legal review.
  </what-built>
  <how-to-verify>
Run `cd apps/web && npm run dev` and check five things in a browser, then one on disk.

1. Open `http://localhost:3000/fr/cgv`. The draft-pending-review banner should be visible without
   scrolling, immediately under the page title, in an amber-bordered box — obvious to a stranger,
   not something only a developer would notice.
2. Open `http://localhost:3000/en/cgv`. The banner and the whole contract body should be in English.
   If any paragraph is still French, stop — that is the exact defect `/en/cgu` has today.
3. Open `http://localhost:3000/fr/cgu` and `/en/cgu`. The new Premium/AI-credits section should read
   word-for-word the same as the CGV's corresponding paragraph, and the CGU should carry the same
   banner.
4. Scroll to the footer on any page in each locale and confirm the CGV link is present and lands on
   the right page.
5. Open `/fr/politique-de-confidentialite` and confirm the waitlist retention and erasure paragraphs
   appear near the existing retention section.
6. Read `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md` end to
   end and judge whether you could hand it to a lawyer with no further explanation.

This checkpoint is about presentation and completeness only. It is explicitly not a legal review —
that is the next task.
  </how-to-verify>
  <acceptance_criteria>
    - The user confirms the banner is visible without scrolling on both `/fr/cgv` and `/en/cgv`.
    - The user confirms `/en/cgv` renders English prose throughout, with no French paragraph remaining.
    - The user confirms the CGU's AI-credit paragraph matches the CGV's word for word in the same locale.
    - The user confirms the footer CGV link resolves in both locales.
    - The user confirms the counsel-briefing package is usable as-is, or names what is missing.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to the counsel gate, or describe what needs fixing first.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: BLOCKING — outside counsel has reviewed and approved the CGV and CGU (D-01)</name>
  <read_first>
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md` — the seven questions counsel was asked.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-CONTEXT.md` D-01 — the decision this gate implements.
    - `.planning/workstreams/lien-invite/research/PITFALLS.md` Pitfalls 7 and 8 — the highest-severity items in the milestone, and the reason this gate exists at all.
  </read_first>
  <decision>
Has a real outside lawyer read the drafted CGV and CGU and approved the text for publication?

This gate implements D-01 and is the phase's completion criterion. Phase 3 is not complete on
drafting alone. It cannot be cleared by Claude's own assessment that the drafting looks sound, by
`workflow.auto_advance`, or by any automated check — 03-VALIDATION.md's Manual-Only Verifications
table records that no test suite can verify legal validity, and 03-RESEARCH.md's Assumptions Log A4
marks the entire clause synthesis as unconfirmed until exactly this review happens.

Downstream consequence of clearing it: Phase 4 (Credit-Gate Alignment) becomes unblocked per
LEGAL-05, and Phase 6's activation checklist gains its "legal text live before the flag flips"
precondition. Clearing it prematurely means the credit-gate behaviour change can ship ahead of the
text describing it, which is the single sequencing failure this milestone was structured to prevent.
  </decision>
  <context>
The `[TBD]` advance-notice day count in the shutdown clause is still unresolved by design — it is
Q6 in the briefing and only counsel supplies it. If counsel returns everything else approved but
does not answer Q6, that is `approved-with-revisions`, not `approved-as-drafted`, because a `[TBD]`
placeholder must not stay in published contractual text.
  </context>
  <options>
    <option id="approved-as-drafted">
      <name>Counsel reviewed and approved the text as drafted</name>
      <pros>Phase closes; Phase 4 unblocks; task 3 records the approval and removes the draft banner unchanged.</pros>
      <cons>Requires that counsel also supplied the Q6 notice period, since no placeholder may remain.</cons>
    </option>
    <option id="approved-with-revisions">
      <name>Counsel approved subject to specific revisions</name>
      <pros>The common outcome; task 3 applies the returned wording verbatim to the shipped constants, re-runs every assertion, then removes the banner.</pros>
      <cons>Revisions may touch clauses the LEGAL-02/03/04 assertions guard, so those assertions may need to be re-derived from the revised text rather than the drafted text.</cons>
    </option>
    <option id="not-yet-reviewed">
      <name>Counsel has not reviewed the text yet</name>
      <pros>Honest state; the draft stays live and honestly marked, and nothing downstream is falsely unblocked.</pros>
      <cons>Phase 3 stays open and Phase 4 stays blocked — which is the intended behaviour, not a failure.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Execution halts here and does not proceed to task 3 on any signal other than an explicit user response naming one of the three options.
    - If `not-yet-reviewed` is selected, the phase remains incomplete, the banner stays on both pages, and no ROADMAP or REQUIREMENTS status is advanced.
    - If `approved-with-revisions` is selected, the user supplies counsel's returned wording before task 3 runs; the executor does not infer, paraphrase, or partially apply it.
    - The approving counsel's name or firm and the approval date are captured in the response, since task 3 records them.
  </acceptance_criteria>
  <resume-signal>Select: approved-as-drafted, approved-with-revisions (and paste counsel's returned wording plus the Q6 notice period), or not-yet-reviewed. Include the counsel name or firm and the approval date.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the approval, apply revisions, and clear the draft state</name>
  <files>.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-APPROVAL.md, apps/web/src/content/legal/founder-offer.ts, apps/web/src/content/legal/cgv.ts, apps/web/src/app/[locale]/(marketing)/cgv/page.tsx, apps/web/src/app/[locale]/(marketing)/cgu/page.tsx, apps/web/test/legal/cgv-content.test.ts, apps/web/test/legal/cgv-cgu-consistency.test.ts, .planning/workstreams/lien-invite/REQUIREMENTS.md</files>
  <read_first>
    - The task 2 checkpoint response — counsel's identity, date, selected option, and any returned wording. This is the only source for what to apply.
    - `apps/web/src/content/legal/founder-offer.ts` and `cgv.ts` — the constants revisions land in.
    - `apps/web/test/legal/cgv-content.test.ts` and `cgv-cgu-consistency.test.ts` — the banner assertions removed here, and the LEGAL-02/03/04 assertions that must still pass against revised text.
    - `.planning/workstreams/lien-invite/ROADMAP.md` Phase 4 "Depends on" line — the LEGAL-05 ordering guarantee this task verifies rather than creates.
    - `.planning/workstreams/lien-invite/REQUIREMENTS.md` Traceability table — the LEGAL-01 through LEGAL-09 rows.
  </read_first>
  <precondition>Task 2's checkpoint resolved to `approved-as-drafted` or `approved-with-revisions`; this task must not run under `not-yet-reviewed`.</precondition>
  <action>
Run in three steps, in this order, so the draft marking never comes down before the text it marks is final.

First, apply counsel's revisions if the checkpoint returned any. Edit the affected constants in `founder-offer.ts` and the affected sections in `cgv.ts` using counsel's returned wording verbatim for the French text, then update the English translation to match — French governs per D-08, so English follows the revised French rather than being revised independently. Replace the `[TBD]` placeholders in `SHUTDOWN_MODIFICATION_CLAUSE` with the notice period counsel supplied, in both locales. If any returned wording is ambiguous about which clause it replaces, stop and re-raise the question rather than choosing an interpretation. Re-run `cd apps/web && npx vitest run test/legal` before continuing; if a LEGAL-02, LEGAL-03, or LEGAL-04 assertion now fails against revised text, the assertion is re-derived from the approved wording, never the wording bent back to fit the assertion.

Second, write `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-APPROVAL.md`. Record: the approving lawyer's name or firm, the approval date, which of the three checkpoint options was selected, the full text of any revisions applied, per-question resolutions for the seven briefing questions including the Q6 notice period now in force, and the git SHA of the commit holding the approved text — obtain it with `git rev-parse HEAD` after the revision commit. Add an explicit LEGAL-05 statement: this text is approved and, once merged, live; Phase 4's credit-gate change and Phase 6's flag flip must not activate before that, and Phase 6's activation checklist criterion 2 is the place that gets verified in production. Note that ROADMAP Phase 4's "Depends on" line already names Phase 3, and confirm by reading it that this is still true.

Third, clear the draft state in a single commit that removes the marking and the assertions guarding it together — this coupling is what makes an early, silent banner removal impossible, since dropping the banner alone leaves a red suite. Remove the banner block from `cgv/page.tsx` and `cgu/page.tsx`, remove the now-unused `DRAFT_REVIEW_BANNER` export and the `IoWarningOutline` imports, remove the banner assertions from `cgv-content.test.ts` and `cgv-cgu-consistency.test.ts`, and update `CGV_LAST_UPDATED` and the CGU's trailing last-updated line to the approval month. Then mark the LEGAL-01 through LEGAL-09 rows Complete in the REQUIREMENTS.md Traceability table.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run test/legal &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run test/legal` exits 0 with all six suites passing after the revisions and the banner removal.
    - `cd apps/web && npm run test` exits 0 — no regression in `test/purge`, `test/actions`, or `safe-next`.
    - `grep -r "DRAFT_REVIEW_BANNER" apps/web/src apps/web/test` returns no match.
    - `grep -rn "\[TBD" apps/web/src/content/legal/` returns no match — no placeholder survives into approved text.
    - `03-COUNSEL-APPROVAL.md` exists and contains the approving counsel's name or firm, an ISO-format approval date, a 40-character git SHA, resolutions for all seven briefing questions, and the string `LEGAL-05`.
    - The ROADMAP Phase 4 "Depends on" line still names Phase 3, confirmed by reading it and recorded in the approval document.
    - The REQUIREMENTS.md Traceability rows for LEGAL-01 through LEGAL-09 read `Complete`.
    - `cd apps/web && npx tsc --noEmit -p tsconfig.json` reports no error.
  </acceptance_criteria>
  <done>The CGV and CGU carry counsel-approved text with no draft marking and no unresolved placeholder, the approval is recorded against a specific commit, and Phase 4's LEGAL-05 precondition is documented as satisfied. Committed.</done>
  <reversibility rating="costly">Reverting to a publish-now-review-later posture after this plan closes means re-opening a phase already marked complete and re-litigating whatever went live in the meantime — the cost D-01 records. The revisions themselves are ordinary edits; the process position is what is expensive to undo.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted text → published contractual representation | Clearing the gate converts a marked draft into a representation Ziko is bound by; the boundary is crossed by a human decision, not by code |
| this phase's completion state → Phase 4 and Phase 6 | Downstream phases read Phase 3's completion as permission to change credit-gate behaviour; a falsely-cleared gate propagates silently |
| counsel's returned wording → shipped constants | Third-party text enters the codebase through a human paste, applied verbatim rather than interpreted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Elevation of Privilege | the counsel-approval gate | high | mitigate | Modelled as `checkpoint:decision` with `gate="blocking-human"`, never auto-approvable; the acceptance criteria require an explicit named option plus counsel identity and date, and `not-yet-reviewed` leaves the phase open rather than advancing anything |
| T-03-18 | Repudiation | no record of what counsel actually approved | high | mitigate | `03-COUNSEL-APPROVAL.md` records the approving party, the date, the selected option, the applied revisions, per-question resolutions, and the git SHA of the exact approved text — so a later dispute resolves against a commit, not a memory |
| T-03-19 | Tampering | draft-pending banner removed before approval | high | mitigate | The banner and the assertions asserting its presence are removed in the same commit and only under this task's precondition; removing the banner alone leaves `cgv-content.test.ts` and `cgv-cgu-consistency.test.ts` red |
| T-03-20 | Information Disclosure | an unresolved `[TBD]` placeholder published as binding contractual text | high | mitigate | An acceptance criterion greps `apps/web/src/content/legal/` for `[TBD` and must return no match before the phase closes; Q6 of the briefing exists specifically to obtain the figure from counsel |
| T-03-21 | Tampering | counsel's revision partially or loosely applied | medium | mitigate | Revisions are applied verbatim to the French text with English re-translated from it per D-08; ambiguity re-raises the question rather than being resolved by the executor, and a failing LEGAL assertion is re-derived from approved wording rather than the wording bent to fit |
| T-03-22 | Spoofing | Phase 4 starting on an unverified assumption that Phase 3 is done | medium | mitigate | The approval record states the LEGAL-05 constraint explicitly and confirms the ROADMAP Phase 4 dependency line; Phase 6's activation checklist criterion 2 verifies live-ness in production before the flag flips |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this plan; 03-RESEARCH.md's Package Legitimacy Audit records zero new packages for the phase. Any install command appearing during execution is out of contract and requires a blocking human checkpoint |
</threat_model>

<verification>
- Task 2's checkpoint resolved with a named option, counsel identity, and date recorded.
- `cd apps/web && npx vitest run test/legal` — six suites green after revisions and banner removal.
- `cd apps/web && npm run test` and `cd backend/api && npm run test:rls` — no regression.
- `grep -r "DRAFT_REVIEW_BANNER" apps/web/src apps/web/test` and `grep -rn "\[TBD" apps/web/src/content/legal/` — both empty.
- `03-COUNSEL-APPROVAL.md` present with counsel, date, SHA, seven resolutions, and the LEGAL-05 statement.
</verification>

<success_criteria>
- Outside counsel has reviewed and approved the CGV and CGU, confirmed by the user at a gate that cannot auto-clear (D-01, LEGAL-05).
- Counsel's revisions, including the notice-period figure, are applied to the shipped text and every LEGAL-02/03/04 assertion passes against the approved wording.
- The draft-pending marking is gone from both pages, removed together with the assertions that guarded it.
- A written approval record ties the approval to a specific commit and restates the LEGAL-05 ordering constraint for Phase 4 and Phase 6.
- LEGAL-01 through LEGAL-09 are marked Complete in the Traceability table.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-04-SUMMARY.md` when done.
</output>
