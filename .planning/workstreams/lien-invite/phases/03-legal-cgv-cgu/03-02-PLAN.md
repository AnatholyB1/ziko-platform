---
phase: 03-legal-cgv-cgu
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/content/legal/founder-offer.ts
  - apps/web/src/app/[locale]/(marketing)/cgu/page.tsx
  - apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx
  - apps/web/test/legal/cgv-cgu-consistency.test.ts
  - apps/web/test/legal/consent-copy.test.ts
autonomous: true
requirements: [LEGAL-04, LEGAL-06, LEGAL-07, LEGAL-08, LEGAL-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LEGAL-04: the CGU and the CGV state the AI-credit cap in byte-identical text per locale, because both import the same AI_CREDIT_CAP_SENTENCE constant rather than each holding its own paraphrase — a word-diff between the two documents' credit-cap paragraphs is empty."
    - "LEGAL-04: the CGU's new AI-credit section renders in the reader's locale (FR and EN branches both real), and the CGU carries the same draft-pending-review banner as the CGV while review is outstanding (D-01)."
    - "LEGAL-08: the privacy policy states the waitlist retention period as 3 years from last contact, citing CNIL NS-048, in both locales (D-05)."
    - "LEGAL-09: the privacy policy states that a registrant may request erasure of their waitlist address by writing to support@ziko-app.com and that the request is handled within one month at most per GDPR Article 12 (D-06)."
    - "LEGAL-06: a final, reviewable consent-checkbox label exists in FR and EN as an exported constant, worded for an unchecked, standalone opt-in separate from the submit action (D-07 — copy only; Phase 5 renders the checkbox)."
    - "LEGAL-07: a final, reviewable point-of-collection RGPD notice exists in FR and EN carrying all six CNIL Article 13 minimum fields — controller, purposes, legal basis, recipients, retention duration, and data-subject rights (D-07 — copy only)."
    - "Phase 5 has a concrete literal for waitlist_signups.consent_version — the exported CONSENT_VERSION string — tying a stored signup to the exact copy version it agreed to."
  artifacts:
    - "apps/web/src/content/legal/founder-offer.ts (ERASURE_REQUEST_STATEMENT, CONSENT_CHECKBOX_LABEL, COLLECTION_POINT_NOTICE, CONSENT_VERSION)"
    - "apps/web/src/app/[locale]/(marketing)/cgu/page.tsx"
    - "apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx"
    - apps/web/test/legal/cgv-cgu-consistency.test.ts
    - apps/web/test/legal/consent-copy.test.ts
  key_links:
    - "AI_CREDIT_CAP_SENTENCE (plan 01) imported by both cgv.ts and cgu/page.tsx: the single import is the LEGAL-04 mechanism — drift becomes impossible rather than merely tested-for."
    - "RETENTION_STATEMENT + WAITLIST_RETENTION_YEARS (plan 01) → privacy page copy → plan 03's app_config seed value: the number stated to users and the number stored in the database come from one constant."
    - "CONSENT_CHECKBOX_LABEL / COLLECTION_POINT_NOTICE / CONSENT_VERSION → Phase 5's waitlist form: this phase's only deliverable to Phase 5, per D-07."
  prohibitions:
    - "The CGU's AI-credit paragraph is not paraphrased, re-translated, or re-typed — it is the imported constant, so the two documents cannot drift."
    - "No shipped string implies Premium includes uncapped or unmetered AI usage (D-03)."
    - "The existing CGU section 9 modification clause is not silently rewritten this phase — it is flagged to counsel by plan 03 and left byte-identical here."
    - "The consent-checkbox copy does not bundle consent with the submit action, pre-tick language, or any wording implying consent is a condition of a service the registrant is otherwise entitled to."
    - "The point-of-collection notice is not reduced to a link to the privacy policy; the six Article 13 fields are stated at the point of collection."
    - "The draft-pending-review banner is not omitted from the CGU while the counsel checkpoint is open."
  edge_probe_note: >
    The deterministic edge-probe engine classified all 9 phase requirements as
    category=unclassified / status=unresolved — expected for content-and-policy
    requirements outside its data-shape taxonomy. Surfaced here as flagged
    assumptions rather than auto-resolved or dismissed; the truths above are
    planner-authored from 03-VALIDATION.md's per-requirement test map and
    03-CONTEXT.md's decisions. Prohibitions are descriptor-less (flagged-unverified).
---

<objective>
Make the CGU say exactly what the CGV says about the AI-credit cap (LEGAL-04), give the privacy
policy a waitlist-specific retention and erasure section (LEGAL-08, LEGAL-09), and freeze the final
FR/EN consent-checkbox label and point-of-collection RGPD notice as reviewable copy for Phase 5
(LEGAL-06, LEGAL-07, per D-07).

Purpose: LEGAL-04's real failure mode is drift — two documents drafted in separate passes describing
the same cap in subtly different words. This plan removes the possibility rather than testing for it,
by having the CGU import plan 01's `AI_CREDIT_CAP_SENTENCE` instead of restating it.

Output: revised `cgu` and `politique-de-confidentialite` routes, four new constants in the shared
clause module including the `consent_version` literal Phase 1 left waiting, and two Vitest suites.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-CONTEXT.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-UI-SPEC.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-PATTERNS.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Owner plan |
|----------|------|-----------|
| `apps/web/src/content/legal/founder-offer.ts` — `LocalizedText`, `WAITLIST_RETENTION_YEARS`, `DRAFT_REVIEW_BANNER`, `AI_CREDIT_CAP_SENTENCE`, `LIFETIME_SCOPE_SENTENCE`, `SHUTDOWN_MODIFICATION_CLAUSE`, `RETENTION_STATEMENT`, `LANGUAGE_PRECEDENCE_CLAUSE` | new module | 03-01 |
| `apps/web/src/content/legal/founder-offer.ts` — appended `ERASURE_REQUEST_STATEMENT`, `CONSENT_CHECKBOX_LABEL`, `COLLECTION_POINT_NOTICE`, `CONSENT_VERSION` | module additions | 03-02 |
| `apps/web/src/content/legal/cgv.ts` — `LegalSection`, `CGV_SECTIONS_FR`, `CGV_SECTIONS_EN`, `CGV_LAST_UPDATED`, `cgvSections(locale)` | new module | 03-01 |
| `apps/web/src/app/[locale]/(marketing)/cgv/page.tsx` — `generateMetadata`, `CgvPage` | new route | 03-01 |
| `apps/web/src/components/layout/Footer.tsx` / `FooterClient.tsx` — `cgv` prop + `/cgv` nav link | modified | 03-01 |
| `apps/web/messages/fr.json` / `en.json` — `Metadata.cgvTitle`, `Metadata.cgvDescription`, `Footer.cgv` | modified | 03-01 |
| `apps/web/src/app/[locale]/(marketing)/cgu/page.tsx` — draft banner + AI-credit-cap section | modified | 03-02 |
| `apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx` — waitlist retention + erasure section | modified | 03-02 |
| `supabase/migrations/20260815_waitlist_retention_config.sql` — `app_config` row `waitlist_retention_years` | new migration | 03-03 |
| `backend/api/test/rls/waitlist-config-rpc.spec.ts` — retention-row coverage | modified | 03-03 |
| `scripts/waitlist-erasure/erase.mjs` — `parseErasureArgs`, `runErasure`, `writeErasureLog` | new script | 03-03 |
| `scripts/waitlist-erasure/RUNBOOK.md` | new doc | 03-03 |
| `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md` | new doc | 03-03 |
| `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-APPROVAL.md` | new doc | 03-04 |
| `apps/web/test/legal/cgv-locale.test.ts` | new test | 03-01 |
| `apps/web/test/legal/cgv-content.test.ts` | new test | 03-01 |
| `apps/web/test/legal/cgv-cgu-consistency.test.ts` | new test | 03-02 |
| `apps/web/test/legal/consent-copy.test.ts` | new test | 03-02 |
| `apps/web/test/legal/retention-config.test.ts` | new test | 03-03 |
| `apps/web/test/legal/erasure-script.test.ts` | new test | 03-03 |

<!-- planner-discipline-allow: illimité, unlimited, locale === 'en' -->

<tasks>

<task type="auto">
  <name>Task 1: CGU states the identical AI-credit cap, by import not by paraphrase</name>
  <files>apps/web/src/app/[locale]/(marketing)/cgu/page.tsx, apps/web/test/legal/cgv-cgu-consistency.test.ts</files>
  <read_first>
    - `apps/web/src/app/[locale]/(marketing)/cgu/page.tsx` — the full 172-line file; you are revising it in place, and the section numbering (1 through 10) must stay coherent after insertion.
    - `apps/web/src/content/legal/founder-offer.ts` — plan 01's module; `AI_CREDIT_CAP_SENTENCE` and `DRAFT_REVIEW_BANNER` are imported from here, never retyped.
    - `apps/web/src/app/[locale]/(marketing)/cgv/page.tsx` — plan 01's banner markup and locale-branch idiom, to be mirrored exactly.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-UI-SPEC.md` — banner copy, warning-token override, and the 600-weight rule for touched headings.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-PATTERNS.md` — the CGU modified-file assignment, including the instruction to leave section 9 alone.
  </read_first>
  <action>
Revise `cgu/page.tsx` in three targeted places. This is a surgical edit of a live page, not a rewrite: sections 1 through 4 and 7 through 10 are untouched, keep their existing `font-bold` `<h1>` and their French-only prose, and are explicitly out of scope for retrofitting.

First, insert a new section covering Premium access and AI credits immediately after the existing section 5 medical-disclaimer block and before the existing section 6 on personal data. Number it 6 and renumber the four following sections to 7 through 11, updating their `<h2>` text only. Its heading is "6. Accès Premium et crédits IA" in French and "6. Premium access and AI credits" in English. Its body paragraph is `AI_CREDIT_CAP_SENTENCE` imported from `@/content/legal/founder-offer`, selected by locale — rendering `AI_CREDIT_CAP_SENTENCE[locale === 'en' ? 'en' : 'fr']` — so the string in the CGU and the string in the CGV are the same object, not two copies. Add a second sentence of your own pointing the reader to the CGV for the founder offer's own terms, using the `Link` from `@/i18n/navigation` with `className="text-primary underline"` and `href="/cgv"`, matching the cross-document link idiom already at lines 88-93. That pointer sentence is locale-branched like the rest of the section.

Second, add the draft-pending-review banner immediately after the CGU's `<h1>` and before the first `<section>`, using the identical markup plan 01 established on the CGV — `rounded-lg border-2 border-warning bg-warning-subtle p-6 space-y-3`, `IoWarningOutline` with `aria-hidden="true"`, `font-semibold text-warning text-xl` headline, `text-text leading-relaxed` body — sourced from `DRAFT_REVIEW_BANNER` in the reader's locale. The CGU is being materially revised this phase, so it enters review alongside the CGV and carries the same honest marking.

Third, leave the existing section 9 modification clause byte-identical. It is a general terms-amendment reservation predating this milestone, materially different from a founder-benefit revocation clause, and 03-RESEARCH.md's Open Question 3 routes it to counsel rather than to a silent rewrite. The same applies to the unresolved court-designation placeholder in the existing final section: flag, do not fix. Plan 03's counsel briefing carries both.

Because only the new section and the banner are locale-aware, the page's existing French-only sections stay as they are — this is the scoped-retrofit rule from 03-PATTERNS.md's Shared Patterns, deliberately narrow so this phase does not turn into a full CGU translation project.

Create `apps/web/test/legal/cgv-cgu-consistency.test.ts` proving LEGAL-04 structurally: it imports `AI_CREDIT_CAP_SENTENCE` and the CGV section arrays, and reads `cgu/page.tsx` as raw text to confirm the CGU obtains the sentence by import rather than by literal. All value comparisons run over imported constants.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run test/legal/cgv-cgu-consistency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run test/legal/cgv-cgu-consistency.test.ts` exits 0.
    - The suite asserts the joined text of `CGV_SECTIONS_FR` contains `AI_CREDIT_CAP_SENTENCE.fr` and the joined text of `CGV_SECTIONS_EN` contains `AI_CREDIT_CAP_SENTENCE.en` (the CGV side of the identity).
    - The suite asserts the raw text of `cgu/page.tsx` contains `AI_CREDIT_CAP_SENTENCE` and contains `@/content/legal/founder-offer` — proving the CGU renders the shared constant.
    - The suite asserts the raw text of `cgu/page.tsx` does NOT contain the first 40 characters of `AI_CREDIT_CAP_SENTENCE.fr` as a literal — proving the sentence was not additionally re-typed into the page.
    - The suite asserts the raw text of `cgu/page.tsx` contains `DRAFT_REVIEW_BANNER` and `border-warning`.
    - The suite asserts the raw text of `cgu/page.tsx` still contains the existing section-9 clause fragment `Ziko se r&eacute;serve le droit de modifier` — proving it was left untouched.
    - The suite asserts `cgu/page.tsx` contains `locale === 'en'` at least once — proving the new section branches for real.
    - `cd apps/web && npx tsc --noEmit -p tsconfig.json` reports no error in the `cgu` route.
  </acceptance_criteria>
  <done>The CGU carries a Premium/AI-credits section whose text is the same constant the CGV renders, branches on locale, links to the CGV, and shows the draft-pending banner; section 9 and the court placeholder are untouched and routed to counsel. Committed.</done>
  <reversibility rating="reversible">A section insertion, a renumber, and a banner on an existing page — revertable in one commit; the shared-constant import is the structural choice and it is equally easy to undo.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Privacy policy gains the waitlist retention and erasure section</name>
  <files>apps/web/src/content/legal/founder-offer.ts, apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx</files>
  <read_first>
    - `apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx` — the full file; sections 6 (Durée de conservation, around line 145) and 7 (Vos droits, around line 163) are where the new waitlist paragraphs must sit so a reader finds them where they expect.
    - `apps/web/src/content/legal/founder-offer.ts` — `RETENTION_STATEMENT` and `WAITLIST_RETENTION_YEARS` already exist from plan 01; `ERASURE_REQUEST_STATEMENT` is added here.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-UI-SPEC.md` Copywriting Contract — the verbatim retention and erasure-request strings for both locales.
    - `.planning/workstreams/lien-invite/phases/01-data-foundation/01-CONTEXT.md` — D-15/D-16, the consent columns and the `anonymize_waitlist_signup()` RPC this text promises against.
  </read_first>
  <action>
Append `ERASURE_REQUEST_STATEMENT` to `apps/web/src/content/legal/founder-offer.ts` as a `LocalizedText` holding the verbatim FR and EN erasure-request copy from 03-UI-SPEC.md's Copywriting Contract — the support@ziko-app.com channel plus the one-month Article 12 GDPR maximum. Do not soften the deadline and do not invent a shorter one; one month is the settled statutory ceiling, not a drafting choice.

Add one new `<section>` to `politique-de-confidentialite/page.tsx` covering the founder-waitlist data specifically, placed immediately after the existing section 6 on retention duration so it reads as a specialisation of it rather than a contradiction. Give it a heading numbered to follow the existing sequence, "Liste d'attente fondateurs — conservation et effacement" in French and "Founder waitlist — retention and erasure" in English, and two paragraphs: `RETENTION_STATEMENT` and `ERASURE_REQUEST_STATEMENT`, each selected by locale exactly as the CGU section does. Follow the page's existing section markup — `<h2 className="text-xl font-semibold mt-8 mb-3">` and `<p className="text-text leading-relaxed mb-4">` — so it is visually indistinguishable from its neighbours.

This page is one of the three carrying the French-only-body defect, and the scoped-retrofit rule applies here too: branch this new section on locale, leave every pre-existing section as it is. Do not add the draft-pending-review banner to this page — the banner marks documents whose substantive terms are under counsel review, and this addition restates a retention period and a statutory right rather than drafting new contractual terms; keeping the banner to the CGV and CGU preserves its signal.

Note explicitly in the section's French paragraph ordering that the retention clock runs from last contact, not from signup, since that is the CNIL NS-048 shape and the distinction is what makes the three-year figure defensible.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run test/legal</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run test/legal` exits 0 with every existing suite still passing.
    - `apps/web/src/content/legal/founder-offer.ts` exports `ERASURE_REQUEST_STATEMENT` with non-empty `fr` and `en` values; `ERASURE_REQUEST_STATEMENT.fr` contains `support@ziko-app.com` and `un mois`, and `.en` contains `support@ziko-app.com` and `one month`.
    - The raw text of `politique-de-confidentialite/page.tsx` contains `RETENTION_STATEMENT`, `ERASURE_REQUEST_STATEMENT`, and `locale === 'en'`.
    - `RETENTION_STATEMENT.fr` contains `String(WAITLIST_RETENTION_YEARS)` and `NS-048`; `.en` contains `String(WAITLIST_RETENTION_YEARS)` and `NS-048`.
    - The raw text of `politique-de-confidentialite/page.tsx` does NOT contain `DRAFT_REVIEW_BANNER`.
    - `cd apps/web && npx tsc --noEmit -p tsconfig.json` reports no error in the privacy route or the content module.
  </acceptance_criteria>
  <done>A visitor reading the privacy policy in either locale finds the waitlist retention period and the erasure channel stated where the retention section already lives, with the same number the database will store. Committed.</done>
  <reversibility rating="reversible">A documented policy value and a section addition; D-05 already records the three-year figure as changeable with a new decision record.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Freeze the consent-checkbox label, the collection notice, and the consent version for Phase 5</name>
  <files>apps/web/src/content/legal/founder-offer.ts, apps/web/test/legal/consent-copy.test.ts</files>
  <read_first>
    - `apps/web/src/content/legal/founder-offer.ts` — the module as it stands after task 2.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-UI-SPEC.md` — the verbatim consent-checkbox label, the point-of-collection notice with all six CNIL fields, and the `consent_version` literal.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-RESEARCH.md` — Pattern 4 (the waiting `consent_given_at`/`consent_version` columns) and the "Don't Hand-Roll" row on the CNIL Article 13 minimum field set.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-CONTEXT.md` — D-07, the copy-only boundary; the checkbox UI belongs to Phase 5 and must not be built here.
  </read_first>
  <action>
Append the three remaining Phase-5 deliverables to `apps/web/src/content/legal/founder-offer.ts`, all as exported constants with no React and no component code — D-07 scopes this phase to reviewable copy, and building any part of the form here would cross into Phase 5's domain.

`CONSENT_CHECKBOX_LABEL` is a `LocalizedText` holding the verbatim FR and EN checkbox label from 03-UI-SPEC.md. The wording must stand alone as an affirmative opt-in that a reader could decline without losing anything else, since Phase 5 renders it unchecked and separate from the submit control per Planet49 (CJEU C-673/17) and GDPR Recital 32. Do not phrase it as an acknowledgement of terms and do not bundle it with any acceptance of the CGV or CGU.

`COLLECTION_POINT_NOTICE` is a `LocalizedText` holding the verbatim FR and EN point-of-collection notice. It must carry all six CNIL Article 13 minimum fields in a single readable block: the controller, the purposes, the legal basis, the recipients, the retention duration, and the data-subject rights with a contact channel. This is a regulatory checklist, not a style choice — a missing field is a compliance gap. The retention duration it states must be the same number `WAITLIST_RETENTION_YEARS` holds.

`CONSENT_VERSION` is the exported string literal `waitlist-consent-v1`. This is the value Phase 5 writes into `waitlist_signups.consent_version` alongside `consent_given_at`, the columns Phase 1 shipped empty for exactly this purpose (D-15/D-16). Document in a short comment above it that the suffix increments whenever `CONSENT_CHECKBOX_LABEL` or `COLLECTION_POINT_NOTICE` changes, so a stored signup stays traceable to the copy it actually agreed to.

Create `apps/web/test/legal/consent-copy.test.ts` asserting the structural properties of these three constants. The Article 13 field coverage is checked by substring presence of the field markers each locale's notice uses, listed in the acceptance criteria below — all assertions run over the imported constant values, never over raw file text.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run test/legal/consent-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run test/legal/consent-copy.test.ts` exits 0.
    - The suite asserts `CONSENT_CHECKBOX_LABEL.fr` and `.en` are both non-empty, and that `.fr` contains `désinscrire` and `.en` contains `unsubscribe`.
    - The suite asserts `CONSENT_CHECKBOX_LABEL.fr` and `.en` do not contain `CGV`, `CGU`, `Terms`, or `conditions générales` — the label is a marketing opt-in, not a terms acceptance.
    - The suite asserts `COLLECTION_POINT_NOTICE.fr` contains all six Article 13 markers: `responsable du traitement`, `pour vous informer`, `consentement`, `aucun tiers`, `3`, and `support@ziko-app.com`.
    - The suite asserts `COLLECTION_POINT_NOTICE.en` contains all six counterparts: `data controller`, `to notify you`, `consent`, `third parties`, `3`, and `support@ziko-app.com`.
    - The suite asserts both notice locales contain `String(WAITLIST_RETENTION_YEARS)`.
    - The suite asserts `CONSENT_VERSION === 'waitlist-consent-v1'` and that it matches `/^waitlist-consent-v\d+$/`.
    - `cd apps/web && npx vitest run test/legal` — all five suites in the directory pass together.
  </acceptance_criteria>
  <done>Phase 5 can wire the waitlist form's checkbox, notice, and stored consent version from three exported constants without re-opening this phase's artifacts or inventing wording. Committed.</done>
  <reversibility rating="reversible">Copy constants with a versioned identifier; the version suffix is the designed mechanism for changing them later without losing traceability.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → SSR legal routes | Untrusted readers consume `/[locale]/cgu` and `/[locale]/politique-de-confidentialite`; neither accepts input beyond the locale segment |
| this phase's copy constants → Phase 5's form | The consent label and collection notice cross a phase boundary as data; Phase 5 renders them into a real form that writes `consent_given_at`/`consent_version` |
| stated policy → stored data | The retention figure shown to users and the `app_config` value plan 03 seeds must agree, or the published policy misdescribes actual practice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | CGU/CGV credit-cap wording drift | high | mitigate | The CGU imports `AI_CREDIT_CAP_SENTENCE` rather than restating it; `cgv-cgu-consistency.test.ts` asserts the import exists and that no re-typed literal shadows it, so drift requires deleting a passing test rather than merely editing prose |
| T-03-06 | Information Disclosure | point-of-collection notice missing a mandatory Article 13 field | medium | mitigate | `consent-copy.test.ts` asserts all six CNIL field markers per locale; the field list comes from CNIL guidance cited in 03-RESEARCH.md, not from drafting judgement |
| T-03-07 | Repudiation | a signup's stored consent cannot be traced to the copy it agreed to | medium | mitigate | `CONSENT_VERSION` is a versioned literal validated against `/^waitlist-consent-v\d+$/`, written by Phase 5 into the `consent_version` column Phase 1 reserved; the increment rule is documented at the constant |
| T-03-08 | Repudiation | published retention figure diverges from the stored `app_config` value | medium | mitigate | Both the notice and the retention statement assert against `WAITLIST_RETENTION_YEARS`; plan 03's `retention-config.test.ts` asserts the migration's seeded value against the same constant |
| T-03-09 | Elevation of Privilege | consent framed as a precondition, making it non-freely-given | medium | mitigate | The label is asserted to contain no reference to the CGV, CGU, or general terms, keeping it a standalone marketing opt-in per Planet49 / Recital 32; unchecked-by-default rendering is Phase 5's contract |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this plan; 03-RESEARCH.md's Package Legitimacy Audit records zero new packages. Any install command appearing during execution is out of contract and requires a blocking human checkpoint |
</threat_model>

<verification>
- `cd apps/web && npx vitest run test/legal` — all five suites green.
- `cd apps/web && npm run test` — full web suite, no regression.
- `cd apps/web && npx tsc --noEmit -p tsconfig.json` — clean.
- `git diff` on `cgu/page.tsx` shows the section-9 clause and the court-designation placeholder unchanged.
</verification>

<success_criteria>
- The CGU and CGV describe the AI-credit cap in identical words per locale, guaranteed by a shared import (LEGAL-04).
- The privacy policy states the waitlist retention period and the erasure channel in both locales (LEGAL-08, LEGAL-09).
- Final FR/EN consent-checkbox and point-of-collection notice copy exist as reviewable constants carrying all six Article 13 fields (LEGAL-06, LEGAL-07).
- Phase 5 has a concrete `consent_version` literal to store.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-02-SUMMARY.md` when done.
</output>
