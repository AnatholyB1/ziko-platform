---
phase: 03-legal-cgv-cgu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/content/legal/founder-offer.ts
  - apps/web/src/content/legal/cgv.ts
  - apps/web/src/app/[locale]/(marketing)/cgv/page.tsx
  - apps/web/src/components/layout/Footer.tsx
  - apps/web/src/components/layout/FooterClient.tsx
  - apps/web/messages/fr.json
  - apps/web/messages/en.json
  - apps/web/test/legal/cgv-locale.test.ts
  - apps/web/test/legal/cgv-content.test.ts
autonomous: true
requirements: [LEGAL-01, LEGAL-02, LEGAL-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "LEGAL-01: /fr/cgv and /en/cgv both resolve and render genuinely divergent bodies — CGV_SECTIONS_EN is a real English translation, not the French text under an English <title> (guards the existing broken precedent where /en/cgu serves French prose)."
    - "LEGAL-01: a visitor on any page reaches the CGV from the footer nav in both locales (Footer.cgv key present in fr.json and en.json, AnimatedLink href=\"/cgv\" rendered)."
    - "LEGAL-02: the CGV states that lifetime Premium unlocks all features while AI usage stays subject to a monthly credit allowance identical to any other Premium user; no shipped string claims uncapped AI (D-03)."
    - "LEGAL-03: the CGV scopes the lifetime benefit to the Service's operating lifetime with a frozen feature set and discretionary-only future additions (D-03)."
    - "LEGAL-03: the CGV's only benefit-ending clause is the narrow, notice-bearing, service-wide-shutdown clause with a data-export commitment (D-04) — no unconditional discretionary revocation right."
    - "D-01: the draft-pending-review banner renders on the CGV page above the first section, visible to a real visitor, in the locale being viewed."
    - "D-08: the CGV carries an explicit language-precedence clause stating the French version governs."
  artifacts:
    - apps/web/src/content/legal/founder-offer.ts
    - apps/web/src/content/legal/cgv.ts
    - "apps/web/src/app/[locale]/(marketing)/cgv/page.tsx"
    - apps/web/test/legal/cgv-locale.test.ts
    - apps/web/test/legal/cgv-content.test.ts
  key_links:
    - "founder-offer.ts constants → cgv.ts sections → cgv/page.tsx render: one source of truth for every clause that must also appear in the CGU (plan 02 imports the same constants, which is what makes LEGAL-04 structural rather than a copy-paste promise)."
    - "messages/{fr,en}.json Metadata.cgvTitle/cgvDescription → generateMetadata → alternates.languages: the hreflang pair that makes /en/cgv a real locale, not a redirect."
    - "Footer.tsx cgv prop → FooterClient AnimatedLink href=\"/cgv\": the only discovery path to the page."
  prohibitions:
    - "No shipped page, content constant, message-file value, or marketing string implies that Premium includes uncapped or unmetered AI usage (D-03)."
    - "No clause grants Ziko an unconditional, notice-free or purely discretionary right to revoke, reduce, or re-price the founder Premium benefit while the Service continues to operate (D-04)."
    - "The draft-pending-review banner is not omitted, hidden behind a flag, downgraded to an HTML comment, or styled to be visually inconspicuous before the D-01 counsel checkpoint clears."
    - "The [TBD] advance-notice day count in the shutdown clause is not replaced with an invented number; it stays a visible placeholder routed to counsel."
    - "The English branch is not the French text, a machine-mangled near-copy, or an English heading over French prose."
  edge_probe_note: >
    The deterministic edge-probe engine classified all 9 phase requirements as
    category=unclassified / status=unresolved — expected for content-and-policy
    requirements that match none of its data-shape taxonomy (numeric bounds, string
    format, enum, list-op). These are surfaced here as flagged assumptions, not
    auto-resolved and not dismissed: the concrete truths above are planner-authored
    from 03-VALIDATION.md's per-requirement test map and 03-CONTEXT.md's decisions.
    Prohibitions are descriptor-less (flagged-unverified) per the spec-less fallback.
    Canon-referral drops (breadcrumbs only): generic GDPR data-minimisation and
    generic secret-handling prohibitions dropped as covered by GDPR/OWASP ASVS canon.
---

<objective>
Ship the CGV as a real, reachable, genuinely bilingual page route — French authoritative, English a
courtesy translation (D-08) — carrying the two highest-severity clauses in the whole milestone: the
AI-credit-cap parity sentence (LEGAL-02, D-03) and the "à vie" scope + narrow shutdown-only
modification clause (LEGAL-03, D-03/D-04). The page ships marked as a draft pending legal review
(D-01) and stays that way until the phase's terminal counsel checkpoint clears in plan 04.

Purpose: this is the milestone's longest-lead-time artifact and the hard precondition of Phase 4
(LEGAL-05). It also establishes the single-source clause module every other legal surface in this
phase imports, which is what makes CGU/CGV consistency (LEGAL-04) structural instead of a promise.

Output: `apps/web/src/content/legal/founder-offer.ts` (shared clause constants),
`apps/web/src/content/legal/cgv.ts` (locale-branched section data), the `cgv` route, footer +
i18n wiring, and two Vitest suites under `apps/web/test/legal/`.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/STATE.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-CONTEXT.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-UI-SPEC.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-PATTERNS.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Owner plan |
|----------|------|-----------|
| `apps/web/src/content/legal/founder-offer.ts` — `LocalizedText`, `WAITLIST_RETENTION_YEARS`, `DRAFT_REVIEW_BANNER`, `AI_CREDIT_CAP_SENTENCE`, `LIFETIME_SCOPE_SENTENCE`, `SHUTDOWN_MODIFICATION_CLAUSE`, `RETENTION_STATEMENT`, `LANGUAGE_PRECEDENCE_CLAUSE` | new module | 03-01 |
| `apps/web/src/content/legal/founder-offer.ts` — appended `ERASURE_REQUEST_STATEMENT`, `CONSENT_CHECKBOX_LABEL`, `COLLECTION_POINT_NOTICE`, `CONSENT_VERSION` | module additions | 03-02 |
| `apps/web/src/content/legal/cgv.ts` — `LegalSection`, `CGV_SECTIONS_FR`, `CGV_SECTIONS_EN`, `CGV_LAST_UPDATED`, `cgvSections(locale)` | new module | 03-01 |
| `apps/web/src/app/[locale]/(marketing)/cgv/page.tsx` — `generateMetadata`, `CgvPage` | new route | 03-01 |
| `apps/web/src/components/layout/Footer.tsx` / `FooterClient.tsx` — `cgv` prop + `/cgv` nav link | modified | 03-01 |
| `apps/web/messages/fr.json` / `en.json` — `Metadata.cgvTitle`, `Metadata.cgvDescription`, `Footer.cgv` | modified | 03-01 |
| `apps/web/src/app/[locale]/(marketing)/cgu/page.tsx` — draft banner + AI-credit-cap section | modified | 03-02 |
| `apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx` — waitlist retention + erasure section | modified | 03-02 |
| `supabase/migrations/20260815_waitlist_retention_config.sql` — `app_config` row `waitlist_retention_years` | new migration | 03-03 |
| `backend/api/test/rls/waitlist-config-rpc.spec.ts` — retention-row coverage | modified | 03-03 |
| `scripts/waitlist-erasure/erase.mjs` — `parseErasureArgs`, `runErasure`, `writeErasureLog` | new script | 03-03 |
| `scripts/waitlist-erasure/RUNBOOK.md` | new doc | 03-03 |
| `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md` | new doc | 03-03 |
| `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-APPROVAL.md` | new doc | 03-04 |
| `apps/web/test/legal/cgv-locale.test.ts` | new test | 03-01 |
| `apps/web/test/legal/cgv-content.test.ts` | new test | 03-01 |
| `apps/web/test/legal/cgv-cgu-consistency.test.ts` | new test | 03-02 |
| `apps/web/test/legal/consent-copy.test.ts` | new test | 03-02 |
| `apps/web/test/legal/retention-config.test.ts` | new test | 03-03 |
| `apps/web/test/legal/erasure-script.test.ts` | new test | 03-03 |

<!-- planner-discipline-allow: illimité, unlimited, à tout moment, sans préavis, locale === 'en' -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end bilingual CGV — footer link to rendered locale-branched clause, one path only</name>
  <files>apps/web/src/content/legal/founder-offer.ts, apps/web/src/content/legal/cgv.ts, apps/web/src/app/[locale]/(marketing)/cgv/page.tsx, apps/web/src/components/layout/Footer.tsx, apps/web/src/components/layout/FooterClient.tsx, apps/web/messages/fr.json, apps/web/messages/en.json, apps/web/test/legal/cgv-locale.test.ts</files>
  <read_first>
    - `apps/web/src/app/[locale]/(marketing)/cgu/page.tsx` — the metadata/shell analog to copy (lines 1-49) AND the concrete instance of the bug this task must not reproduce: its entire body (lines 42-169) is French JSX with zero locale branch.
    - `apps/web/src/components/layout/Footer.tsx` and `FooterClient.tsx` — full files, 15 and 48 lines; the exact prop-threading and `AnimatedLink` shape.
    - `apps/web/messages/fr.json` and `apps/web/messages/en.json` — the `Metadata` and `Footer` namespace blocks only.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-UI-SPEC.md` — "Copywriting Contract" (every FR/EN string below is verbatim there), "Color" (banner token override), "Typography" (600-weight rule for new surfaces), "Spacing Scale".
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-PATTERNS.md` — CGV pattern assignment and Shared Patterns.
    - `apps/web/vitest.config.ts` — node environment, `{src,test}/**/*.{spec,test}.{ts,tsx}` include glob, `@` alias.
  </read_first>
  <action>
Wire one CGV clause from the footer entry point through every layer this phase touches, end to end, production quality.

Create `apps/web/src/content/legal/founder-offer.ts` as the single source of truth for every clause that appears in more than one legal document. Export a `LocalizedText` type of `{ fr: string; en: string }`, the numeric constant `WAITLIST_RETENTION_YEARS` set to `3` (D-05; plan 03's migration asserts against this exact constant), and these `LocalizedText` (or `{ headline, body }`-of-`LocalizedText`) constants, each holding the **verbatim** string from the 03-UI-SPEC.md Copywriting Contract table with no rewording: `DRAFT_REVIEW_BANNER` (headline + body, "Document en cours de relecture juridique" / "Document under legal review"), `AI_CREDIT_CAP_SENTENCE` (the three-sentence parity paragraph, D-03/LEGAL-02), `LIFETIME_SCOPE_SENTENCE` (the service-operating-lifetime paragraph, D-03/LEGAL-03), `SHUTDOWN_MODIFICATION_CLAUSE` (the shutdown-only, notice-bearing, data-export clause, D-04/LEGAL-03 — keep the `[TBD — nombre de jours à confirmer par le conseil]` / `[TBD — day count to be confirmed by counsel]` placeholder exactly as written; do not resolve it), and `RETENTION_STATEMENT` (D-05). Add `LANGUAGE_PRECEDENCE_CLAUSE` yourself within D-08's guardrail: French governs legally, English is a courtesy translation, French controls on any discrepancy — FR authoritative, EN a translation of it. Use real typographic characters in the string values (apostrophes, accents, ` ` where UI-SPEC shows `&nbsp;`); the HTML-entity style in the existing pages is a JSX artifact, not a requirement for TS string literals.

Create `apps/web/src/content/legal/cgv.ts` exporting a `LegalSection` type of `{ heading: string; paragraphs: string[] }`, the arrays `CGV_SECTIONS_FR` and `CGV_SECTIONS_EN`, a `CGV_LAST_UPDATED: LocalizedText`, and a `cgvSections(locale: string): LegalSection[]` selector that returns the EN array when `locale === 'en'` and the FR array otherwise. In this tracer task populate only the two load-bearing sections, in this order and same order in both arrays: (1) "Portée de l'engagement « à vie »" / "Scope of the lifetime commitment" whose paragraphs include `LIFETIME_SCOPE_SENTENCE`, and (2) "Fonctionnalités Premium et crédits IA" / "Premium features and AI credits" whose paragraphs include `AI_CREDIT_CAP_SENTENCE`. Task 2 fills in the remaining eight sections around them. The section content is plain strings — no JSX, no React import — so the test suite can import and assert on it in the node environment without rendering a React Server Component.

Create the route `apps/web/src/app/[locale]/(marketing)/cgv/page.tsx` mirroring `cgu/page.tsx`'s import list, `Props` type, `generateMetadata` shape, `setRequestLocale(locale)` call, and `<main className="max-w-screen-xl mx-auto px-8 py-16 space-y-8">` shell. Swap the metadata keys to `cgvTitle`/`cgvDescription`, `canonical` to `/${locale}/cgv`, and `languages` to the `/fr/cgv` + `/en/cgv` pair. The body must call `cgvSections(locale)` and map over the result — this is the whole point of the tracer and the one place the existing precedent is deliberately broken. `<h1>` uses `text-3xl font-semibold` (600, per UI-SPEC Typography, not the `font-bold` the existing pages use), section headings `text-xl font-semibold mt-8 mb-3`, paragraphs `text-text leading-relaxed mb-4`, and the trailing "last updated" line `text-muted text-sm mt-12`. Immediately after `<h1>` and before the first `<section>`, render the draft-pending-review banner as `rounded-lg border-2 border-warning bg-warning-subtle p-6 space-y-3` with `IoWarningOutline` from `react-icons/io5` (`aria-hidden="true"`) beside a `font-semibold text-warning text-xl` headline and a `text-text leading-relaxed` body, both pulled from `DRAFT_REVIEW_BANNER` in the locale being viewed. Do not reuse `border-primary`/`bg-orange-50` from the CGU medical box — UI-SPEC's Color section overrides that deliberately so the brand accent stays reserved for links.

Wire discovery: add `cgv={t('cgv')}` to the `FooterClient` call in `Footer.tsx`, add `cgv: string` to `FooterClient`'s `Props` and destructuring, and insert `<AnimatedLink href="/cgv">{cgv}</AnimatedLink>` directly after the existing `/cgu` link. Add `Metadata.cgvTitle`, `Metadata.cgvDescription`, and `Footer.cgv` to both `apps/web/messages/fr.json` and `apps/web/messages/en.json` using the exact values in 03-UI-SPEC.md's Metadata namespace table (FR "CGV" / EN "Terms of Sale"), placed alongside the existing `cguTitle`/`terms` keys so the namespace ordering stays readable.

Create `apps/web/test/legal/cgv-locale.test.ts` as the tracer's real end-to-end proof, not a per-layer unit test. It imports `CGV_SECTIONS_FR`, `CGV_SECTIONS_EN`, `cgvSections` from the content module, reads `cgv/page.tsx` and `FooterClient.tsx` as text with `readFileSync` + `resolve(__dirname, ...)` (the cross-workspace-path idiom `apps/web/test/purge/purge-delete.test.ts` already uses), and imports both message JSON files. All content assertions run against the imported constant values — never against raw file text — so a source comment can never invalidate them; the only raw-source assertions are positive substring matches on the page and footer files.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run test/legal/cgv-locale.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run test/legal/cgv-locale.test.ts` exits 0 with at least 8 passing assertions.
    - The suite asserts `CGV_SECTIONS_FR.length === CGV_SECTIONS_EN.length` and that the length is at least 2.
    - For every index, the suite asserts `CGV_SECTIONS_EN[i].heading !== CGV_SECTIONS_FR[i].heading` and `CGV_SECTIONS_EN[i].paragraphs.join(' ') !== CGV_SECTIONS_FR[i].paragraphs.join(' ')`.
    - The suite asserts the joined EN text contains none of the French markers `'conformément'`, `'présentes'`, `' les '`, `' une '`, and that it does contain `'Lifetime Premium'`; and that the joined FR text contains `'à vie'`.
    - The suite asserts `cgvSections('en')` returns `CGV_SECTIONS_EN`, and that both `cgvSections('fr')` and `cgvSections('de')` return `CGV_SECTIONS_FR`.
    - The suite asserts the raw text of `apps/web/src/app/[locale]/(marketing)/cgv/page.tsx` contains `cgvSections(locale)` and the raw text of `FooterClient.tsx` contains `href="/cgv"`.
    - The suite asserts `Metadata.cgvTitle`, `Metadata.cgvDescription`, and `Footer.cgv` exist and are non-empty in both `messages/fr.json` and `messages/en.json`, and that `Metadata.cgvDescription` differs between the two files.
    - `cd apps/web && npx tsc --noEmit -p tsconfig.json` reports no error in any file this task created or modified.
  </acceptance_criteria>
  <done>A visitor clicking the footer's CGV link lands on a page that shows the lifetime-scope and AI-credit-cap clauses in their own locale, under a visible draft-pending-review banner, and the same path works at `/fr/cgv` and `/en/cgv` with genuinely different prose. Committed.</done>
  <reversibility rating="reversible">Page routes, content modules, and a footer link are all deletable in one commit with no data migration and no external dependency; only the drafted wording carries residual cost, and that is exactly what plan 04's counsel gate exists to settle.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Complete the CGV body — all ten sections, FR authoritative + EN translation</name>
  <files>apps/web/src/content/legal/cgv.ts, apps/web/src/app/[locale]/(marketing)/cgv/page.tsx, apps/web/test/legal/cgv-content.test.ts</files>
  <read_first>
    - `apps/web/src/content/legal/cgv.ts` and `apps/web/src/content/legal/founder-offer.ts` — the modules Task 1 created; reuse the constants, do not restate their text inline.
    - `apps/web/src/app/[locale]/(marketing)/cgu/page.tsx` lines 44-169 — section numbering, tone, and paragraph rhythm of this codebase's existing French legal prose, so the CGV reads as the same document family.
    - `.planning/workstreams/lien-invite/research/PITFALLS.md` Pitfalls 7 and 8 — the substantive constraints on the "à vie" and modification language; these are the highest-severity items in the milestone.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-UI-SPEC.md` Copywriting Contract — the verbatim strings; anything not in that table is yours to draft inside the D-03/D-04 guardrails.
    - `apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx` lines 145-196 — the existing retention and rights sections the CGV cross-references rather than duplicates.
  </read_first>
  <action>
Grow `CGV_SECTIONS_FR` to the full ten-section contract, then produce `CGV_SECTIONS_EN` as a faithful translation of it — French first, English second (D-08), never two independent drafts. Keep both arrays at identical length and identical section order, since the locale test asserts index-for-index correspondence.

Final section order: (1) Objet et champ d'application — what the CGV governs and its relationship to the CGU, which stays the general terms of use; (2) Identité de l'éditeur — a short pointer to the Mentions légales page rather than a duplicated company block; (3) L'offre fondateurs — the first 200 registrants receive lifetime Premium, the offer is granted free of charge with no payment and no subscription, and how a place is claimed; (4) Portée de l'engagement « à vie » — the Task 1 section, unchanged, carrying `LIFETIME_SCOPE_SENTENCE`; (5) Fonctionnalités Premium et crédits IA — the Task 1 section, unchanged, carrying `AI_CREDIT_CAP_SENTENCE`; (6) Évolution et cessation du Service — carrying `SHUTDOWN_MODIFICATION_CLAUSE` with its `[TBD]` placeholder intact, framed as the only circumstance in which the founder benefit ends; (7) Données personnelles — carrying `RETENTION_STATEMENT` and deferring to the Politique de confidentialité for the full treatment, without restating it; (8) Absence de paiement et droit de rétractation — state plainly that the founder offer involves no payment, so the fourteen-day withdrawal right for distance contracts has no monetary object here, and that the registrant may unsubscribe or request erasure at any point; (9) Langue du contrat — carrying `LANGUAGE_PRECEDENCE_CLAUSE`; (10) Droit applicable et litiges — French law governs, amicable resolution and consumer mediation come before any court, and the competent courts are those designated by ordinary French law. In section 10, do not name a specific tribunal and do not import the `[A COMPLÉTER]` placeholder that sits unresolved in the existing CGU section 10 — that pre-existing gap is flagged to counsel by plan 03, not copied forward here.

Section 6 is where LEGAL-03 is won or lost. The only clause permitting the founder benefit to end must be the service-wide-shutdown clause carrying advance notice and a data-export commitment. Do not add any sentence, anywhere in the document, that reserves a broad discretionary right to alter or withdraw the benefit while the Service keeps running — that is the Code de la consommation Article R.212-1 black-list trap D-04 exists to avoid. Section 5's parity framing is the only place the credit allowance is described as changeable, and only as a platform-wide policy that never singles out founders.

Render the completed body from `cgv/page.tsx` with no structural change beyond one addition: a locale-branched JSX paragraph after the last mapped section holding the cross-document links to `/cgu` and `/politique-de-confidentialite` via the `Link` from `@/i18n/navigation` with `className="text-primary underline"`, matching the existing pattern at `cgu/page.tsx` lines 88-93. Those two links cannot live in the plain-string section arrays, which is why they are page-level. Close with the `CGV_LAST_UPDATED` line in the `text-muted text-sm mt-12` style.

Create `apps/web/test/legal/cgv-content.test.ts` covering LEGAL-02 and LEGAL-03 as content assertions over the imported constants and the joined section text — never over raw file text, so comments and prose in this plan cannot invalidate the negative checks.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run test/legal</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run test/legal` exits 0; both `cgv-locale.test.ts` and `cgv-content.test.ts` pass.
    - `cgv-content.test.ts` asserts `CGV_SECTIONS_FR.length === 10` and `CGV_SECTIONS_EN.length === 10`.
    - LEGAL-02 positive: the joined FR section text contains `AI_CREDIT_CAP_SENTENCE.fr` as an exact substring, and the joined EN text contains `AI_CREDIT_CAP_SENTENCE.en`.
    - LEGAL-02 negative: for each locale, the joined section text matched case-insensitively against `/illimit|unlimited|sans limite|uncapped/` returns no match.
    - LEGAL-03 positive: the joined FR text contains `LIFETIME_SCOPE_SENTENCE.fr` and `SHUTDOWN_MODIFICATION_CLAUSE.fr`; same for the EN counterparts.
    - LEGAL-03 negative: no section's joined paragraphs contain both `'à tout moment'` and `'sans préavis'`, and the joined text matched against `/à sa seule discrétion|at its sole discretion/i` returns no match.
    - D-04 placeholder integrity: `SHUTDOWN_MODIFICATION_CLAUSE.fr` and `.en` each still contain the substring `'[TBD'`.
    - D-08: the joined FR text contains `LANGUAGE_PRECEDENCE_CLAUSE.fr` and the joined EN text contains `LANGUAGE_PRECEDENCE_CLAUSE.en`.
    - `cd apps/web && npx tsc --noEmit -p tsconfig.json` reports no error in `src/content/legal/` or the `cgv` route.
  </acceptance_criteria>
  <done>`/fr/cgv` renders a complete ten-section French contract and `/en/cgv` renders its English translation; the credit-cap and lifetime-scope clauses assert green and the black-list-pattern negatives assert absent. Committed.</done>
  <reversibility rating="costly">The drafted wording is what outside counsel reviews in plan 04; a structural re-frame after review (for instance switching the lifetime benefit from service-scoped to person-scoped) means re-verifying both language versions say the same thing with no gaps, which is effectively a re-draft — the same cost D-08 records.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → SSR marketing route | Untrusted readers consume `/[locale]/cgv`; the route accepts no input beyond the `locale` path segment, which `next-intl` middleware already constrains to the configured locale set |
| drafted legal text → the public | Text published before counsel review is a representation to consumers; the draft-pending banner is the control that keeps it honest |
| repository → CI build | Message-file and content-module edits reach production through the existing Vercel + GitHub integration with no new secret or credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Repudiation | `DRAFT_REVIEW_BANNER` rendering in `cgv/page.tsx` | high | mitigate | Banner is a rendered `border-2 border-warning bg-warning-subtle` block above the first section, sourced from a constant, not a build flag or comment; its presence in the page source is asserted by `cgv-content.test.ts`, and removal is scoped exclusively to plan 04's post-checkpoint task |
| T-03-02 | Information Disclosure | drafted CGV clauses published pre-review | high | mitigate | Every legally load-bearing sentence is the verbatim UI-SPEC string derived from PITFALLS.md Pitfalls 7/8; the `[TBD]` notice-period placeholder is preserved rather than invented, and plan 03's counsel briefing routes all residual uncertainty to a lawyer |
| T-03-03 | Tampering | `apps/web/messages/{fr,en}.json` edits | low | accept | JSON message files carry only short display labels with no interpolation of user input; malformed edits fail the build, and `cgv-locale.test.ts` asserts the three new keys exist and are non-empty in both files |
| T-03-04 | Spoofing | `/en/cgv` serving French contract text under an English title | medium | mitigate | `cgvSections(locale)` selects a real EN array; `cgv-locale.test.ts` asserts index-for-index divergence, absence of French markers in the EN branch, and that the page source calls the selector — this is the concrete defect present today at `/en/cgu` |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this plan. 03-RESEARCH.md's Package Legitimacy Audit records zero new packages: `next-intl`, `react-icons`, and `framer-motion` are all already in `apps/web/package.json`. Any install command appearing during execution is out of contract and requires a blocking human checkpoint before proceeding |
</threat_model>

<verification>
- `cd apps/web && npx vitest run test/legal` — both suites green.
- `cd apps/web && npm run test` — no regression in the existing `test/purge`, `test/actions`, or `safe-next` suites.
- `cd apps/web && npx tsc --noEmit -p tsconfig.json` — clean for every file this plan touches.
- Manual (carried to plan 04's checkpoint, not blocking here): load `/fr/cgv` and `/en/cgv` and confirm the draft-pending banner is visible without scrolling and the prose is in the requested language.
</verification>

<success_criteria>
- A public CGV route exists and is reachable from the footer in both locales (LEGAL-01).
- `/en/cgv` serves English prose, not French prose under an English title (LEGAL-01, the codebase's existing defect not reproduced).
- The CGV states that Premium unlocks all features while AI usage stays under a monthly allowance at parity with any other Premium user, and never claims otherwise (LEGAL-02).
- The lifetime commitment is scoped to the Service's operating lifetime, and the only benefit-ending clause is the narrow, notice-bearing shutdown clause (LEGAL-03).
- The draft-pending-review banner is live and visible on the page (D-01).
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-01-SUMMARY.md` when done.
</output>
