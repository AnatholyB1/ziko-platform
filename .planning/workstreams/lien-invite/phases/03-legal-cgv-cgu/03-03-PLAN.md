---
phase: 03-legal-cgv-cgu
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - supabase/migrations/20260815_waitlist_retention_config.sql
  - backend/api/test/rls/waitlist-config-rpc.spec.ts
  - scripts/waitlist-erasure/erase.mjs
  - scripts/waitlist-erasure/RUNBOOK.md
  - apps/web/test/legal/retention-config.test.ts
  - apps/web/test/legal/erasure-script.test.ts
  - .planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md
autonomous: true
requirements: [LEGAL-08, LEGAL-09]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LEGAL-08: a new dated migration seeds app_config with key waitlist_retention_years and value 3, using ON CONFLICT (key) DO NOTHING so a CI re-run of the migration set never fails on a duplicate insert (D-05, D-16)."
    - "LEGAL-08: the seeded value and the number published in the privacy notice come from the same source — the migration's literal is asserted against WAITLIST_RETENTION_YEARS by an automated test that needs no database credentials."
    - "LEGAL-09: a registrant who emails support@ziko-app.com gets erasure through a written, repeatable procedure — a human runs scripts/waitlist-erasure/erase.mjs, which calls Phase 1's anonymize_waitlist_signup RPC with a service-role client and an explicit --confirm flag (D-06)."
    - "LEGAL-09: the RPC stays service_role-only — the new migration adds no GRANT and no HTTP surface, so anon and authenticated remain unable to anonymize anyone's row."
    - "D-02: a counsel-briefing package exists that a lawyer can act on cold, organised around the seven open questions quoted verbatim from research/PITFALLS.md, REQUIREMENTS.md's Lawyer Review Required section, 03-RESEARCH.md Open Question 3, and 03-UI-SPEC.md's [TBD] notice period — each paired with the drafted clause it applies to."
  artifacts:
    - supabase/migrations/20260815_waitlist_retention_config.sql
    - scripts/waitlist-erasure/erase.mjs
    - scripts/waitlist-erasure/RUNBOOK.md
    - apps/web/test/legal/retention-config.test.ts
    - apps/web/test/legal/erasure-script.test.ts
    - .planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md
  key_links:
    - "WAITLIST_RETENTION_YEARS (plan 01) → migration literal → retention-config.test.ts: the published figure and the stored figure cannot diverge without a red test."
    - "erase.mjs → supabase.rpc('anonymize_waitlist_signup', { p_email }) → Phase 1 migration 20260812_waitlist_founder_offer.sql lines 217-241: the only erasure path, already service_role-gated at the database layer."
    - "03-COUNSEL-BRIEFING.md → plan 04's blocking checkpoint: the briefing is what the user hands to a lawyer to make that checkpoint resolvable."
  prohibitions:
    - "The new migration does not edit, re-run, or re-create anything in 20260812_waitlist_founder_offer.sql — it adds one seed row and nothing else."
    - "The new migration adds no GRANT EXECUTE on anonymize_waitlist_signup to anon or authenticated, and creates no HTTP route, server action, or API surface that reaches the RPC."
    - "The erasure script never falls back to a publishable or anon key, and never proceeds without an explicit confirmation flag."
    - "The erasure audit log is not written into the repository and no real registrant address is committed as a fixture."
    - "The counsel briefing does not paraphrase away the open questions or answer them on counsel's behalf — each is quoted verbatim from its source with the source cited."
    - "The briefing does not present Claude's clause drafting as settled legal conclusion."
  edge_probe_note: >
    The deterministic edge-probe engine classified all 9 phase requirements as
    category=unclassified / status=unresolved — expected for content-and-policy
    requirements outside its data-shape taxonomy. Surfaced as flagged assumptions,
    not auto-resolved and not dismissed; the truths above are planner-authored from
    03-VALIDATION.md's per-requirement test map and 03-CONTEXT.md's decisions.
    Prohibitions are descriptor-less (flagged-unverified).
---

<objective>
Make the retention period and the erasure right real rather than merely stated: seed the retention
value into `app_config` through a new additive migration (LEGAL-08, D-05/D-16), give support a
written, credential-gated procedure for running Phase 1's `anonymize_waitlist_signup()` RPC on
request (LEGAL-09, D-06), and assemble the counsel-briefing package that makes plan 04's blocking
checkpoint answerable (D-02).

Purpose: 03-RESEARCH.md flags "retention duration stated in copy but never written to `app_config`"
as this phase's most likely silent miss, and D-02 exists so the user can hand a lawyer a document
instead of re-deriving seven open questions from research files.

Output: one new migration, one operational script plus runbook, two Vitest suites that run without
database credentials, an extension to the existing RLS suite for CI, and `03-COUNSEL-BRIEFING.md`.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/REQUIREMENTS.md
@.planning/workstreams/lien-invite/research/PITFALLS.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-CONTEXT.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-RESEARCH.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-PATTERNS.md
@.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Owner plan |
|----------|------|-----------|
| `apps/web/src/content/legal/founder-offer.ts` — `LocalizedText`, `WAITLIST_RETENTION_YEARS`, `DRAFT_REVIEW_BANNER`, `AI_CREDIT_CAP_SENTENCE`, `LIFETIME_SCOPE_SENTENCE`, `SHUTDOWN_MODIFICATION_CLAUSE`, `RETENTION_STATEMENT`, `LANGUAGE_PRECEDENCE_CLAUSE` | new module | 03-01 |
| `apps/web/src/content/legal/founder-offer.ts` — appended `ERASURE_REQUEST_STATEMENT`, `CONSENT_CHECKBOX_LABEL`, `COLLECTION_POINT_NOTICE`, `CONSENT_VERSION` | module additions | 03-02 |
| `apps/web/src/content/legal/cgv.ts` — `LegalSection`, `CGV_SECTIONS_FR`, `CGV_SECTIONS_EN`, `CGV_LAST_UPDATED`, `cgvSections(locale)` | new module | 03-01 |
| `apps/web/src/app/[locale]/(marketing)/cgv/page.tsx` — `generateMetadata`, `CgvPage` | new route | 03-01 |
| `apps/web/src/components/layout/Footer.tsx` / `FooterClient.tsx` — `cgv` prop + `/cgv` nav link | modified | 03-01 |
| `apps/web/messages/fr.json` / `en.json` — `Metadata.cgvTitle`, `Metadata.cgvDescription`, `Footer.cgv` | modified | 03-01 |
| `apps/web/src/app/[locale]/(marketing)/cgu/page.tsx` — draft banner + AI-credit-cap section | modified | 03-02 |
| `apps/web/src/app/[locale]/(marketing)/politique-de-confidentialite/page.tsx` — waitlist retention + erasure section | modified | 03-02 |
| `supabase/migrations/20260815_waitlist_retention_config.sql` — `app_config` row `waitlist_retention_years` | new migration | 03-03 |
| `backend/api/test/rls/waitlist-config-rpc.spec.ts` — retention-row coverage | modified | 03-03 |
| `scripts/waitlist-erasure/erase.mjs` — `parseErasureArgs`, `runErasure`, `writeErasureLog` | new script | 03-03 |
| `scripts/waitlist-erasure/RUNBOOK.md` | new doc | 03-03 |
| `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md` | new doc | 03-03 |
| `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-APPROVAL.md` | new doc | 03-04 |
| `apps/web/test/legal/cgv-locale.test.ts` | new test | 03-01 |
| `apps/web/test/legal/cgv-content.test.ts` | new test | 03-01 |
| `apps/web/test/legal/cgv-cgu-consistency.test.ts` | new test | 03-02 |
| `apps/web/test/legal/consent-copy.test.ts` | new test | 03-02 |
| `apps/web/test/legal/retention-config.test.ts` | new test | 03-03 |
| `apps/web/test/legal/erasure-script.test.ts` | new test | 03-03 |

<tasks>

<task type="auto">
  <name>Task 1: Additive migration seeding the retention period into app_config</name>
  <files>supabase/migrations/20260815_waitlist_retention_config.sql, apps/web/test/legal/retention-config.test.ts, backend/api/test/rls/waitlist-config-rpc.spec.ts</files>
  <read_first>
    - `supabase/migrations/20260812_waitlist_founder_offer.sql` lines 155-184 — the `app_config` DDL, its deny-all RLS posture (RLS enabled, deliberately zero policies), the `JSONB` value column, and the `waitlist_reveal_threshold` seed idiom this migration replicates.
    - `backend/api/test/rls/waitlist-config-rpc.spec.ts` — the full file; note the `RUN_DB` guard pattern at the top that skips the suite unless `SUPABASE_TEST_URL` is set and equal to `SUPABASE_URL`, and the `beforeAll`/`afterAll` restore discipline.
    - `apps/web/src/content/legal/founder-offer.ts` — `WAITLIST_RETENTION_YEARS`, the constant the migration's literal must match.
    - `apps/web/test/purge/purge-delete.test.ts` lines 1-30 — the cross-workspace `resolve(__dirname, ...)` idiom for reading files outside `apps/web` from a Vitest suite.
  </read_first>
  <precondition>The `app_config` table already exists in every environment this migration runs against, created by `20260812_waitlist_founder_offer.sql`, which CI applies on push to `main` when `supabase/migrations/` changes.</precondition>
  <action>
Create `supabase/migrations/20260815_waitlist_retention_config.sql` as a new, additive, dated migration. Never edit `20260812_waitlist_founder_offer.sql` — that file is applied in production and the project's migration rule is strictly append-only.

The migration does exactly one thing: insert the row keyed `waitlist_retention_years` with the JSONB value `3` into `public.app_config`, guarded by `ON CONFLICT (key) DO NOTHING`. That guard is the idempotency contract — CI re-applies the full migration set, and a bare insert would fail the second time. Do not re-declare the table, do not add or alter any RLS policy, and do not add a `GRANT` of any kind. `app_config` is deliberately deny-all with zero policies, and the retention row inherits that posture for free.

Open the file with a short comment block explaining that the value is the CNIL NS-048 prospect-data ceiling recorded by D-05, that it is measured from last contact rather than from signup, and that no v1 code reads this key back programmatically — it exists so a future cleanup job and any auditor have a machine-readable source of truth matching the published policy. Keep the whole file under about twenty lines; anything more means scope crept.

Create `apps/web/test/legal/retention-config.test.ts` as the credential-free structural proof. It reads the migration file as text with `readFileSync` and `resolve(__dirname, ...)` reaching up to the repo root, imports `WAITLIST_RETENTION_YEARS` from the content module, and asserts the SQL and the constant agree. This is the suite that actually runs in this environment; the RLS extension below is the CI-side complement.

Extend `backend/api/test/rls/waitlist-config-rpc.spec.ts` with the live-database assertions, inside its existing `describe.skipIf(!RUN_DB)` block so it stays skipped where credentials are absent: the admin client reading `app_config` for key `waitlist_retention_years` returns exactly one row whose value equals 3, and the anon client reading the same key returns zero rows — the deny-all posture holding for the new row exactly as it does for the threshold row. Follow the file's existing restore discipline: this suite must not mutate the retention row, so no `afterAll` cleanup is needed for it, unlike the threshold row the file already saves and restores.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run test/legal/retention-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run test/legal/retention-config.test.ts` exits 0.
    - The suite asserts the migration file exists at `supabase/migrations/20260815_waitlist_retention_config.sql`.
    - With SQL line comments stripped (drop every line whose trimmed form starts with `--`) before asserting, the remaining SQL contains `INSERT INTO public.app_config`, `waitlist_retention_years`, and `ON CONFLICT (key) DO NOTHING`.
    - With comments stripped, the remaining SQL matches `/'waitlist_retention_years'\s*,\s*'3'/` and the extracted numeric literal equals `String(WAITLIST_RETENTION_YEARS)`.
    - With comments stripped, the remaining SQL contains none of `GRANT`, `CREATE POLICY`, `DROP`, or `ALTER TABLE`.
    - The suite asserts the byte length and sha256 of `supabase/migrations/20260812_waitlist_founder_offer.sql` are unchanged from the values recorded when the test was written — the append-only guarantee, expressed as a fixture the test carries.
    - `cd backend/api && npm run test:rls` exits 0 (the new assertions skip cleanly without `SUPABASE_TEST_URL`, per the file's existing `RUN_DB` guard).
  </acceptance_criteria>
  <done>The retention period a visitor reads on the privacy page and the value stored in `app_config` are the same number, provable without a database connection, and re-running the migration set is safe. Committed.</done>
  <reversibility rating="reversible">A single seed row behind `ON CONFLICT DO NOTHING`; D-05 already records the three-year figure as changeable with a new decision record and a follow-up migration.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Human-triggered erasure procedure — script plus runbook over the existing RPC</name>
  <files>scripts/waitlist-erasure/erase.mjs, scripts/waitlist-erasure/RUNBOOK.md, apps/web/test/legal/erasure-script.test.ts</files>
  <read_first>
    - `scripts/purge-test-accounts/lib.mjs` — the standalone admin-client idiom: own `createClient`, fail-fast env check, `auth: { persistSession: false, autoRefreshToken: false }`; note it deliberately does not import `apps/web/src/lib/supabase/admin.ts`, which carries `import 'server-only'` and cannot run in plain Node.
    - `scripts/purge-test-accounts/delete.mjs` — the `parseArgs`/`--confirm` gate and `writeDeleteLog` audit-output shape to mirror at lighter weight.
    - `scripts/purge-test-accounts/RUNBOOK.md` — the written-procedure format this codebase already uses for human-run operations.
    - `apps/web/test/purge/purge-delete.test.ts` — the hand-rolled-fake testing style: pure functions receive an injected fake client, so no network and no credentials are needed to test the logic.
    - `supabase/migrations/20260812_waitlist_founder_offer.sql` lines 217-241 — the `anonymize_waitlist_signup(p_email TEXT) RETURNS BOOLEAN` signature, its internal `normalize_waitlist_email` call, and the `REVOKE ... FROM PUBLIC, anon, authenticated` / `GRANT ... TO service_role` posture.
  </read_first>
  <precondition>`SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are available in the operator's shell at the moment a real erasure is run; this environment does not have them, so this task builds and unit-tests the procedure without executing it against a live database (the same documented gap Phase 1 and Phase 2 carried).</precondition>
  <action>
Resolve 03-RESEARCH.md's Open Question 2 in favour of a thin script plus a runbook, rather than a bare SQL-editor runbook. The deciding factor is testability: a script gives `parseErasureArgs` and `runErasure` as pure functions with an automated verify that runs today with no credentials, which a raw SQL procedure cannot offer, and it matches the operational convention Phase 2 already established in this repo. It deliberately does not carry Phase 2's full ceremony — no dry-run export, no hashed manifest, no two-person rule — because this operation is a single-row, non-destructive, anonymising UPDATE with no cascade, and that rigor would be disproportionate.

Create `scripts/waitlist-erasure/erase.mjs` exporting three pure-ish functions plus a small main guarded by the standard direct-invocation check. `parseErasureArgs(argv)` accepts `--email <address>`, the required `--confirm` flag, and an optional `--log <path>`; it returns a normalised object and throws a descriptive error when the address is missing, when it fails a basic single-`@` shape check, or when `--confirm` is absent. `runErasure(client, email)` calls `client.rpc('anonymize_waitlist_signup', { p_email: email })` and maps the result into `{ ok, anonymized, error }` where `anonymized` reflects the RPC's boolean return — `false` meaning no matching un-anonymised row was found, which is a legitimate outcome to report to the requester, not a failure. Do not normalise or lowercase the address in JavaScript: the RPC calls `normalize_waitlist_email` internally, and a second client-side normalisation would silently diverge if the SQL definition ever changes. `writeErasureLog(entry, logPath)` appends one JSON line recording the timestamp, the address acted on, the boolean outcome, and the operator identifier passed through the environment.

The admin client follows `purge-test-accounts/lib.mjs` exactly: read `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` from the environment, print a clear message and exit non-zero when either is missing, and never fall back to a publishable or anon key — the RPC has no grant for those roles anyway, so a fallback would produce a confusing permission error instead of an honest missing-credential message. Default the log path to a file under the OS temp directory rather than anywhere inside the repository, so a real registrant's address is never committed.

Create `scripts/waitlist-erasure/RUNBOOK.md` as the procedure a support agent follows. It must state, in order: that the request arrives at support@ziko-app.com; that the operator must confirm the requesting address matches the address to be erased before running anything, since nothing in the tooling can verify ownership and this is the one control against a third party erasing someone else's entry; the exact invocation with its required flags; how to read the three possible outcomes (erased, no matching active row, error); the GDPR Article 12 one-month response deadline the published copy commits to; and the note that anonymisation intentionally preserves `founder_rank` and `is_founder` per Phase 1's D-07, so an erasure never renumbers other registrants — with the caveat that what happens to a claimed founder spot after erasure is an open legal question routed to counsel by the briefing package, not settled here.

Create `apps/web/test/legal/erasure-script.test.ts` in the hand-rolled-fake style: import the three functions across the workspace boundary by relative path exactly as `test/purge/purge-delete.test.ts` does, and drive `runErasure` with a fake client object whose `rpc` records its arguments and returns canned shapes. No network, no credentials, no real address in any fixture.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run test/legal/erasure-script.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npx vitest run test/legal/erasure-script.test.ts` exits 0.
    - `parseErasureArgs` throws when `--confirm` is absent, throws when `--email` is absent, throws on an address with no `@`, and returns the address unchanged (no lowercasing, no trimming beyond surrounding whitespace) on a well-formed invocation.
    - `parseErasureArgs` defaults `logPath` to a path under `os.tmpdir()` when `--log` is omitted, and the resolved default path does not contain the repository root.
    - `runErasure(fakeClient, 'a@b.test')` calls `fakeClient.rpc` exactly once with `'anonymize_waitlist_signup'` and `{ p_email: 'a@b.test' }` — asserting the address is passed through verbatim, unnormalised.
    - `runErasure` returns `{ ok: true, anonymized: true }` for an RPC returning `{ data: true, error: null }`, `{ ok: true, anonymized: false }` for `{ data: false, error: null }`, and `ok: false` with the error surfaced for an RPC error.
    - `writeErasureLog` writes one parseable JSON line per call to a temp path created by the test, and the test cleans it up in `afterEach`.
    - The raw text of `erase.mjs` contains `SUPABASE_SERVICE_ROLE_KEY` and contains neither `SUPABASE_PUBLISHABLE_KEY` nor `ANON_KEY`.
    - `scripts/waitlist-erasure/RUNBOOK.md` contains `support@ziko-app.com`, `--confirm`, `anonymize_waitlist_signup`, and a requester-identity verification step.
    - `git status --porcelain scripts/waitlist-erasure/` shows no `.log`, `.jsonl`, or `.csv` file staged.
  </acceptance_criteria>
  <done>A support agent receiving an erasure request has a written procedure and a credential-gated one-command tool that calls the already-built RPC, and its argument parsing and result mapping are proven by tests that need no database. Committed.</done>
  <reversibility rating="reversible">D-06 records that a self-service erasure link can be added later without touching this phase's text or the anonymisation mechanism; the script is an operational convenience over an RPC that already exists.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Counsel-briefing package organised around the seven open questions</name>
  <files>.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md</files>
  <read_first>
    - `.planning/workstreams/lien-invite/research/PITFALLS.md` — the closing "Uncertainty flagged for lawyer review" list (four items) and the body of Pitfalls 4, 7, 8, and 9 they point at; quote the flags verbatim.
    - `.planning/workstreams/lien-invite/REQUIREMENTS.md` — the "Lawyer Review Required" section (three items) and the LEGAL-01 through LEGAL-09 statements.
    - `apps/web/src/content/legal/founder-offer.ts` and `apps/web/src/content/legal/cgv.ts` — the drafted clauses counsel reacts to; excerpt them into the briefing rather than describing them.
    - `apps/web/src/app/[locale]/(marketing)/cgu/page.tsx` lines 153-165 — the pre-existing section 9 modification clause and the unresolved court-designation placeholder, both flagged rather than fixed.
    - `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-RESEARCH.md` — Open Question 3 and the Assumptions Log, in particular A4 which marks the whole clause synthesis as unconfirmed until counsel signs off.
  </read_first>
  <action>
Write `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md` as a document a French lawyer can act on cold, with no access to this repository's planning history and no prior conversation. D-02's whole point is that the user should not have to re-derive the open questions before handing it over.

Open with a one-page orientation: who Ziko is, what the founder offer is in three sentences (the first 200 registrants receive lifetime Premium, granted free of charge, on a French-law consumer-facing service), which documents are enclosed and where they are live (the CGV and CGU routes), that the French text governs with English as a courtesy translation, and an explicit statement that everything enclosed is a draft prepared without legal advice and that its drafting choices are proposals for counsel to accept, amend, or reject.

Then one numbered section per open question — seven in total, each in the same four-part shape: the question stated plainly; the source quoted verbatim with its citation; the clause as currently drafted, excerpted from the shipped constants; and what specifically is being asked of counsel. The seven, in this order:

Q1, the enforceability boundary of "à vie" under `pratique commerciale trompeuse` for this fact pattern, quoting PITFALLS.md's Pitfall 7 flag and REQUIREMENTS.md's "Rédaction de la clause « à vie » au regard de l'article R.212-1 du Code de la consommation", paired with `LIFETIME_SCOPE_SENTENCE`. Q2, whether the `clause abusive` black-list and grey-list framework — developed around paid consumer contracts — applies identically to a free lifetime benefit attached to an otherwise free tier, quoting PITFALLS.md's Pitfall 8 flag and REQUIREMENTS.md's "Applicabilité du régime des clauses abusives à un avantage gratuit à vie", paired with `SHUTDOWN_MODIFICATION_CLAUSE`. Q3, whether a claimed founder spot survives an erasure request as an accepted contractual offer, quoting PITFALLS.md's Pitfall 4 flag and REQUIREMENTS.md's "Sort d'une place fondateur revendiquée face à une demande d'effacement RGPD", paired with the CGV's personal-data section, the privacy page's waitlist section, and the fact that anonymisation preserves `founder_rank`. Q4, whether advance notice plus continued use suffices as acceptance of new capped terms for any existing Premium user, or whether affirmative re-consent is required, quoting PITFALLS.md's Pitfall 9 flag, and noting that Phase 4 audits the live count of such users before changing any code. Q5, whether the pre-existing CGU section 9 general amendment clause needs the same narrowing D-04 applies to the founder benefit, quoting the clause verbatim from the live page and citing 03-RESEARCH.md Open Question 3 — flagged proactively because counsel reviewing consistency will read it regardless. Q6, the advance-notice period in the shutdown clause, currently a visible `[TBD]` placeholder that was deliberately not invented; ask counsel for the figure French law and practice support. Q7, the court-designation placeholder still unresolved in the live CGU's final section, and whether a jurisdiction clause is worth carrying at all in a consumer contract governed by French law.

Close with a short section naming what is explicitly not being asked — the milestone's engineering choices, the retention figure (already aligned to CNIL NS-048), and the Article 13 field set (already a regulatory checklist) — so counsel's time goes to the seven questions, plus a note that the AI-credit-cap and consent copy are enclosed as context because they must reconcile with the clauses under review.

State plainly, near the top and again at the close, that the enclosed drafting is Claude-generated within the guardrails recorded in this phase's context and research, and that 03-RESEARCH.md's Assumptions Log entry A4 marks the clause synthesis as unconfirmed pending exactly this review.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-COUNSEL-BRIEFING.md','utf8'); const q=(t.match(/^## Q[1-7][.:) ]/gm)||[]).length; if(q!==7) throw new Error('expected 7 question sections, found '+q); for (const s of ['R.212-1','Pitfall 4','Pitfall 7','Pitfall 8','Pitfall 9','[TBD','anonymize_waitlist_signup','A4']) if(!t.includes(s)) throw new Error('missing citation: '+s); console.log('counsel briefing OK, '+q+' questions');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints `counsel briefing OK, 7 questions`.
    - The briefing contains exactly seven `## Q1.` through `## Q7.` sections, in the order specified in the action.
    - Each of the seven sections contains a verbatim block quote of its source flag with the source file named — `research/PITFALLS.md`, `REQUIREMENTS.md`, `03-RESEARCH.md`, or the live `cgu/page.tsx` — and a distinct "What we are asking" line.
    - Q1 excerpts `LIFETIME_SCOPE_SENTENCE`, Q2 excerpts `SHUTDOWN_MODIFICATION_CLAUSE`, and Q6 shows the `[TBD` placeholder still unresolved.
    - Q5 quotes the pre-existing CGU section 9 clause verbatim and states it was deliberately left unmodified this phase.
    - The document states, at the top and at the close, that the enclosed drafting is Claude-generated and unconfirmed, citing 03-RESEARCH.md Assumptions Log entry A4.
    - The document contains no sentence asserting a legal conclusion on any of the seven questions.
  </acceptance_criteria>
  <done>The user can send this one file to any French lawyer and get back answers to the seven questions that block plan 04's checkpoint, without first re-reading PITFALLS.md, REQUIREMENTS.md, or the drafted clauses. Committed.</done>
  <reversibility rating="reversible">A planning-directory document consumed by a human; regenerating or extending it costs nothing and changes no shipped surface.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| support agent (human) → `erase.mjs` → `service_role` → Postgres RPC | The only path that mutates a registrant's row; the operator holds a service-role credential and the tooling cannot verify that the requester owns the address |
| repository → CI → Supabase migration apply | The new migration reaches production through the existing push-to-`main` workflow; it must be safe to re-apply |
| `anonymize_waitlist_signup()` RPC → anon / authenticated roles | Phase 1 revoked execute from `PUBLIC`, `anon`, and `authenticated`; this phase must not widen that |
| drafted clauses → outside counsel | The briefing leaves the repository as a document handed to a third party |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Elevation of Privilege | `anonymize_waitlist_signup()` exposure | high | mitigate | The new migration adds no `GRANT` and creates no HTTP route, server action, or API surface reaching the RPC; `retention-config.test.ts` asserts the comment-stripped SQL contains no `GRANT`, and the extended RLS spec asserts anon still reads zero rows from `app_config` |
| T-03-11 | Spoofing | erasure requested by someone who does not own the address | medium | mitigate | Process control, not a code control: the RUNBOOK requires the operator to confirm the requesting address matches the erasure target before invoking, and `--confirm` prevents an accidental run; the acceptance criteria assert that step is present in the runbook |
| T-03-12 | Tampering | migration re-applied by CI on a later push | medium | mitigate | `ON CONFLICT (key) DO NOTHING` makes the seed idempotent, and the migration performs no DDL — asserted by the comment-stripped SQL containing none of `DROP`, `ALTER TABLE`, or `CREATE POLICY` |
| T-03-13 | Tampering | an existing applied migration edited in place | high | mitigate | `retention-config.test.ts` carries a recorded byte length and sha256 of `20260812_waitlist_founder_offer.sql` and fails if either changes, turning the append-only rule into an enforced check rather than a convention |
| T-03-14 | Information Disclosure | a real registrant's address committed in an erasure audit log | medium | mitigate | `writeErasureLog` defaults its path to the OS temp directory, no repository path; the acceptance criteria assert no log artefact is staged under `scripts/waitlist-erasure/` and that no fixture carries a real address |
| T-03-15 | Information Disclosure | service-role credential mishandling in a new script | high | mitigate | The script reads `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` from the environment only, fails fast when absent, never falls back to a publishable or anon key, and never prints the key; asserted by a source check for the absence of `SUPABASE_PUBLISHABLE_KEY` and `ANON_KEY` |
| T-03-16 | Repudiation | drafted clauses presented to counsel as settled conclusions | medium | mitigate | The briefing states its provisional status at the top and close and cites 03-RESEARCH.md Assumptions Log A4; the acceptance criteria forbid any sentence asserting a legal conclusion on the seven questions |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this plan; `@supabase/supabase-js` is already a workspace dependency used by `scripts/purge-test-accounts/lib.mjs`, and 03-RESEARCH.md's Package Legitimacy Audit records zero new packages. Any install command appearing during execution is out of contract and requires a blocking human checkpoint |
</threat_model>

<verification>
- `cd apps/web && npx vitest run test/legal` — retention and erasure suites green alongside plan 01's and plan 02's.
- `cd backend/api && npm run test:rls` — exits 0; the new live-database assertions skip cleanly without `SUPABASE_TEST_URL`, and run in CI where it is set.
- `git diff --stat supabase/migrations/` — shows exactly one added file and zero modified files.
- The counsel-briefing verify command prints seven questions.
</verification>

<success_criteria>
- A new additive migration seeds `waitlist_retention_years` = 3 idempotently, and the stored value provably matches the published figure (LEGAL-08).
- A support agent has a written, credential-gated, tested procedure for granting an erasure request through the existing RPC (LEGAL-09).
- The RPC's `service_role`-only posture is unchanged and asserted.
- A counsel-briefing package covering all seven open questions exists and is ready to hand to a lawyer (D-02), which is what makes plan 04's checkpoint answerable.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/03-legal-cgv-cgu/03-03-SUMMARY.md` when done.
</output>