---
phase: 05-waitlist-page-entry-points
plan: 06
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - .planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-VALIDATION.md
autonomous: false
requirements: [WAIT-01, WAIT-02, WAIT-03, WAIT-04, WAIT-05, WAIT-06, WAIT-07, WAIT-08, FOND-01, FOND-02, FOND-03, FOND-04, FOND-05, FOND-06, ENTRY-01, ENTRY-02, ENTRY-03, ENTRY-04, ENTRY-05, ENTRY-06]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every automated gate the phase declared runs green together, in one pass, on the merged result of all five preceding plans — not merely one plan at a time in isolation."
    - "`05-VALIDATION.md` records the real command and the real file for every requirement in this phase, with no Wave 0 gap left open and no row still pointing at a test file that does not exist."
    - "WAIT-08 confirmed on the merged tree: the production build still prerenders both locales of the founders route, the homepage, and `/coachs`, after every client component this phase added."
    - "WAIT-07 and the whole UI-SPEC design contract are confirmed by a human looking at the rendered page, because a colour, a weight and a spacing rhythm are not assertable by grep and this project's UI safety gate exists for exactly that."
    - "All four entry points — homepage section, both `/coachs` calls to action, header, footer — are confirmed to actually land a real browser on the founders page, in both locales."
    - "FOND-06 is confirmed by observation, not by inference: changing the configured reveal value in the database changes what the page shows, with no redeploy, and the value is restored afterwards."
    - "The three counter states are each seen at least once, including the completion state, which is the one no visitor will see until the offer is genuinely full and therefore the one most likely to ship broken."
    - "Any gate that genuinely cannot run in this environment is recorded as an explicit, named gap in the phase summary rather than silently reported as passing — the same discipline Phase 1 applied to its database-gated suites."
  artifacts:
    - ".planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-VALIDATION.md (modified — per-task map filled, sign-off completed)"
  key_links:
    - "the merged tree → the full-suite run: five plans across three waves each proved themselves in isolation. This is the first and only point at which they are proven together, and interference between them — a shared component edited by two plans, a message key one plan expected another to write — surfaces only here."
    - "`05-VALIDATION.md`'s per-task map → the phase's verification step: whatever is written there is what a later audit will treat as this phase's evidence. A row pointing at a file that does not exist is worse than an empty row."
    - "the human checkpoint → the UI safety gate: this project sets `ui_phase` and `ui_safety_gate` both on, and `05-UI-SPEC.md` is a contract with no automated enforcement. This is where it is actually checked."
  prohibitions:
    - "Do not write production code in this plan. If a gate fails, the fix belongs in the plan that owns the file, and that plan's summary must record it — repairing it here hides which slice was wrong."
    - "Do not mark a gate green that did not run. An unrunnable gate is a recorded gap, exactly as Phase 1 recorded its database-gated suites."
    - "Do not run the database-mutating waitlist suites against production credentials. The existing guard requires an explicit test-project URL matching the configured URL; do not weaken, bypass or work around it."
    - "Do not leave the configured reveal value changed after the checkpoint's FOND-06 observation. Restoring it is part of that step, and Phase 6's go-live checklist assumes it holds the value the founder intended."
    - "Do not treat the human checkpoint as auto-approvable. This project's `ui_safety_gate` is on and the approval must come from the developer."
---

<objective>
Prove the phase as a whole rather than as five separately-passing slices: run every automated gate
once on the merged tree, write down what was actually verified and what genuinely could not be, and
put the rendered page in front of a human — because the design contract, the four entry points and
the counter's three states are not things a grep can sign off.

Purpose: this phase's five plans ran across three waves, several of them editing the same components
in sequence and one of them adding a package that the others' suites must tolerate. Every one of them
was green when it finished. That is not the same as the phase being green, and the difference is
where integration bugs live.

Output: one full-suite, typecheck, lint and production-build pass on the merged result; a completed
`05-VALIDATION.md` whose every row names a command that exists; and a developer's explicit approval of
the page they are about to point their entire homepage at.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-CONTEXT.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-UI-SPEC.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-VALIDATION.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-01-SUMMARY.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-02-SUMMARY.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-03-SUMMARY.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-04-SUMMARY.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>T-05-13: Run every gate once on the merged tree and complete the validation record</name>
  <files>.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-VALIDATION.md</files>
  <precondition>All five preceding plans have written their summaries into the phase directory. If any of `05-01-SUMMARY.md` through `05-05-SUMMARY.md` is missing, halt — a convergence gate over an incomplete phase reports a green that means nothing.</precondition>
  <read_first>
    - `.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-VALIDATION.md` — the whole file, in particular the per-task map with its Wave 0 markers and the sign-off checklist at the bottom, both of which you are completing.
    - All five plan summaries in the phase directory — specifically each one's list of test files created and any deviation or gap it recorded.
    - `apps/web/vitest.config.ts` — the include glob and the environment matching rule, so you can confirm every new suite is actually being collected rather than silently skipped by a path that falls outside the glob.
    - `apps/web/test/actions/waitlist.concurrency.test.ts` lines 14-19 — the guard that keeps the database-mutating suites from running against production credentials, and why it stays exactly as it is.
    - `.planning/workstreams/lien-invite/STATE.md` "Blockers/Concerns" — Phase 1's recorded gap about database-gated suites never having run with real credentials, which this phase either resolves or inherits.
  </read_first>
  <action>
Run the four gates in order and record the real outcome of each.

First, the full workspace suite from `apps/web`. Before trusting the result, confirm the run actually
collected every suite this phase produced — the config passes with no tests, so a suite in a path the
include glob misses reports as absence rather than as failure. Count the collected files against the
list in the five summaries and reconcile any difference before moving on.

Second, the TypeScript check. Third, the linter. Fourth, the production build, and read its route
table rather than only its exit code: confirm both locales of the founders route, the homepage and
`/coachs` are all listed as prerendered static content. That table is the only continuously-run proof
of WAIT-08 across the merged tree, and this phase added a fetching client component, a script mount
and a bot-detection wrapper since the last time any single plan checked it.

If a gate fails, do not fix it here. Identify which plan owns the file, report the failure with the
owning plan named, and stop. Repairing another slice's mistake inside the convergence plan is how a
phase ends up green with no record of which part of it was wrong.

Then complete `05-VALIDATION.md`. Replace every Wave 0 marker in the per-task map with the real test
file and the real command that now covers that requirement, using the file names the summaries
record rather than the names this document guessed before the work existed. Several rows will have
moved: the entry-point coverage is split across two suites rather than one, the counter has both a
route suite and a component suite, and the consent evidence is proven by a mocked-client unit case
rather than only by the database-gated integration suite. Write what is true now.

Update the Wave 0 Requirements checklist to reflect completion, set `wave_0_complete` to true in the
front matter, and complete the Validation Sign-Off checklist — including `nyquist_compliant`, which
you may only set true if every task in this phase genuinely carried an automated verify command that
runs in under the declared latency. Set the `status` field to `validated`.

In the Manual-Only Verifications table, keep the visual-fidelity row and update it to name the human
checkpoint in T-05-14 as its resolution. For the database-gated row, record honestly whether those
suites were executed against a real test project in this session. If they were not — the same
condition Phase 1 recorded and `STATE.md` still carries — say so plainly, name the environment
variables that would be needed, and leave it as a known gap. A validation document that claims a
database proof that never ran is worse than one that admits the gap, because the gap is what a later
reader needs to know.

Commit the completed document on its own.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npm run test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npx next build</automated>
  </verify>
  <acceptance_criteria>
    - The command above exits 0 in full — suite, typecheck, lint and build.
    - The build's route table lists both locales of the founders route, the homepage and `/coachs` as prerendered static content.
    - The vitest run collected every test file named across the five plan summaries; the count is reconciled and recorded in this plan's summary.
    - `grep -qE '^status: validated$' .planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-VALIDATION.md` succeeds.
    - `grep -qE '^wave_0_complete: true$' .planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-VALIDATION.md` succeeds.
    - `! grep -q 'Wave 0' .planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-VALIDATION.md` succeeds outside the completed checklist heading — no row still defers to a test that does not exist.
    - Every command string appearing in the per-task map names a file that exists: verified by extracting the paths from the table and testing each with `test -f`.
    - The Manual-Only Verifications table states explicitly whether the database-gated suites ran with real credentials in this session.
    - `git status --porcelain apps/web/` is empty at the moment this task completes — the convergence gate changed no production code.
  </acceptance_criteria>
  <done>Every automated gate this phase declared has run green together on the merged tree, the validation record names only commands and files that exist, and any gate that could not run is written down as a named gap rather than reported as a pass. Committed.</done>
  <reversibility rating="reversible">A documentation update and a set of read-only gate runs.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-05-14: Look at the page the whole site is about to point at</name>
  <what-built>
The complete Phase 5 surface, merged and green on every automated gate:

- `/fr/fondateurs` and `/en/fondateurs` — a statically rendered founders page with a hero, a two-card
  profile picker that reveals the email field only after a choice, an unchecked consent box carrying
  Phase 3's frozen wording, the point-of-collection RGPD notice, and an inline confirmation that
  shows a founder rank only when one was genuinely assigned.
- A live counter widget with three states: the offer stated as a plain fact below the reveal point, a
  descending remaining count above it, and a distinct completion panel once all two hundred spots are
  gone.
- A hardened submission path: honeypot, bot check, per-address and per-source rate limiting,
  syntax validation, disposable-domain rejection, consent recorded against the frozen version
  identifier, and campaign attribution stored on the row.
- Four entry points: a dedicated homepage section between the hero and the how-it-works block, both
  `/coachs` calls to action redirected with the coach profile pre-selected, and plain links in the
  header and the footer.
- Social preview images, an explicit large-image Twitter card, a sitemap entry, and one cookieless
  conversion event per completed signup.

Everything above is verified by automated tests. What is not, and what this gate is for: whether it
looks right, whether the four entry points actually land where they claim, and whether the two
counter states no visitor can trigger on demand render correctly.
  </what-built>
  <how-to-verify>
Run `cd apps/web && npm run dev` and work through these in order.

**1 — The page itself, both locales.** Open `http://localhost:3000/fr/fondateurs`, then the `/en/`
equivalent. Against `05-UI-SPEC.md`, check: the background is the site's warm off-white and the cards
are white; the accent orange appears only on the selected card, the offer badge, the submit button
and focus rings — nowhere else; the headline is bold rather than the site's heaviest weight; there
are no more than four distinct text sizes on the page; and the two role cards, not the headline, are
what your eye lands on first. Confirm the English page is fully English — no French string left in
the interface — and that neither language's layout breaks at a phone width.

**2 — The progressive disclosure and the consent gate.** With no role selected, confirm there is no
email field at all. Click Athlète: the field appears. Click Coach: the selection moves and the field
stays. Confirm the consent box starts unchecked and the submit button is disabled until you check it.
Read the consent sentence and the RGPD block and confirm they are the same text as on `/cgu` and
`/cgv` — this is Phase 3's wording and it must not have been paraphrased.

**3 — A real submission.** Submit a fresh address you control. Confirm the inline confirmation
appears in place of the form, in the right language, and that it does not show your email address
back to you. Submit the same address again and confirm you get a confirmation that gives you no hint
you were already registered — no "already signed up", no different wording, no missing rank that was
there the first time in a way that reads as a downgrade.

**4 — A rejection.** Submit something malformed, then submit an address on an obvious throwaway
domain. Both should be refused with the same clear sentence telling you to use a personal address.

**5 — The counter's three states.** Below the reveal point you should currently see the static offer
sentence with no number. Then, against the **test** Supabase project only, raise the configured
reveal value in `app_config` (the `waitlist_reveal_threshold` row) high enough that the count reveals,
reload the page, and confirm a real descending count appears — and that you did not redeploy
anything to make that happen. That is FOND-06 observed rather than assumed. Then restore the original
value and confirm the static sentence returns. If you can also reach a full state on the test
project, confirm the completion panel is a visibly different shape with its own heading rather than
the same badge showing zero — this is the one state no visitor will see until the offer genuinely
fills, so it is the one most likely to have shipped broken.

**6 — All four entry points, both locales.** From `http://localhost:3000/fr` scroll to the founders
section between the hero and the how-it-works block and click through. From `http://localhost:3000/fr/coachs`
click the hero button, then the footer button — both should land on the founders page with the coach
card already selected, and both button labels should describe joining the waitlist rather than
joining a beta. Then use the header link and the footer link from any page. Repeat the two that
matter most in English.

**7 — The header at a phone width.** Narrow the window until the header cannot fit on one line.
Confirm it wraps onto a second line rather than clipping, overlapping, or producing a horizontal
scrollbar, and that the coach-dashboard button is still there and still leads to the dashboard.

**8 — The share preview.** Run `npx next build && npx next start` and check the founders page's
`og:image`, `twitter:card`, canonical and alternate-language tags in the page source, or paste the
URL into a link-preview inspector. The card should carry the ZIKO wordmark and the offer sentence in
the matching language.

Approve only if every step behaves as described. If something is off, say which step and what you
saw — the fix belongs in the plan that owns that file, and naming the step is what makes that
routing possible.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5, or describe which step failed and what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| five independently-verified slices → one merged deployable | The first point at which the phase exists as a whole. Interference between slices is invisible to every gate that ran before this one. |
| validation record → future audit | `05-VALIDATION.md` is what a later reader, a milestone audit, or Phase 6's go-live checklist will treat as this phase's evidence. |
| operator → live test project configuration | The FOND-06 observation temporarily changes a configuration row in a real database. |
| human judgement → the UI safety gate | The design contract has no automated enforcement; this checkpoint is its only enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-R5 | Repudiation | a validation record claiming coverage that does not exist | high | mitigate | Every command string in the per-task map is checked to name a file that exists, and the database-gated row must state plainly whether those suites actually ran with real credentials — inheriting Phase 1's recorded gap honestly rather than quietly closing it. |
| T-05-T8 | Tampering | the reveal configuration left changed after the FOND-06 observation | high | mitigate | Restoring the original value is an explicit numbered step inside the checkpoint, and a prohibition names it. Phase 6's go-live checklist confirms this value independently, so a change left behind here would surface as a launch-day surprise. |
| T-05-T9 | Tampering | verification suites run against production data | critical | mitigate | The existing guard requires an explicit test-project URL matching the configured URL, and a prohibition forbids weakening or bypassing it. The checkpoint's counter step names the test project specifically. |
| T-05-D8 | Denial of Service | an integration failure between slices reaching production | high | mitigate | The full suite, typecheck, lint and production build all run once on the merged tree, and the build's route table is read rather than only its exit code — which is the only continuously-run confirmation that the client components added across three waves did not make a static route dynamic. |
| T-05-R6 | Repudiation | the design contract signed off without being looked at | medium | mitigate | The checkpoint is `blocking` and this project's UI safety gate is enabled, so approval must come from the developer. Its steps are specific and ordered rather than a general request to have a look. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing and modifies no production file. The two `SUS`-verdict packages were gated by a blocking human checkpoint in Plan 05-02 and the one `OK`-verdict package in Plan 05-05; any install command appearing during this plan is out of contract. |
</threat_model>

<verification>
- `cd apps/web && npm run test && npx tsc --noEmit && npm run lint && npx next build` exits 0 in full.
- The build's route table lists the founders route, the homepage and `/coachs` as prerendered static content for both locales.
- `05-VALIDATION.md` has `status: validated`, `wave_0_complete: true`, a completed sign-off checklist, and no row naming a file that does not exist.
- The human checkpoint is approved by the developer.
- `git status --porcelain apps/web/` is empty after this plan's commits — no production code changed here.
</verification>

<success_criteria>
- Every automated gate the phase declared passes together on the merged tree.
- The production build still prerenders every marketing route this phase touched.
- `05-VALIDATION.md` records the real coverage, and any gate that could not run is named as a gap rather than reported as a pass.
- A developer has seen the founders page, exercised all four entry points, observed the counter change state without a redeploy, and approved.
- The configured reveal value is back to what it was before the observation.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-06-SUMMARY.md` when done.
</output>
