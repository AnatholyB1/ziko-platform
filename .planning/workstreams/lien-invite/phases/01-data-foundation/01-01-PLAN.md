---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260812_waitlist_founder_offer.sql
  - apps/web/src/actions/waitlist.ts
  - apps/web/test/actions/waitlist.concurrency.test.ts
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A single new email submitted through claimWaitlistSpot creates exactly one waitlist_signups row carrying email, audience, created_at and a founder_rank (ROADMAP SC-4, DATA-01)"
    - "The founder rank is issued by nextval on public.waitlist_founder_seq, never derived from a count of existing rows (ROADMAP SC-1, DATA-02)"
    - "A rank above 200 is stored with is_founder false and the signup is still accepted (ROADMAP SC-1, DATA-03)"
    - "claimWaitlistSpot returns a rank only when the RPC reports the signup was genuinely new; every other outcome returns the same neutral success shape (ROADMAP SC-5, DATA-07, D-03/D-04)"
    - "Every function created in the migration carries its own REVOKE EXECUTE FROM PUBLIC line immediately followed by a GRANT EXECUTE to service_role, and to no other role (ROADMAP SC-4, DATA-06, D-02)"
    - "waitlist_signups has row level security enabled and no policy of any kind (ROADMAP SC-3, DATA-05)"
    - "Re-running the whole migration file against an already-migrated database completes without error (enables mid-phase re-application to the test project)"
    - statement: "The phase migration is applied to the SUPABASE_TEST_* Supabase project before any DB-touching spec is trusted"
      verification: backstop
  artifacts:
    - path: "supabase/migrations/20260812_waitlist_founder_offer.sql"
      provides: "waitlist_signups table, waitlist_founder_seq sequence, normalize_waitlist_email(), claim_waitlist_signup(), deny-all RLS, REVOKE/GRANT pairs"
      contains: "CREATE OR REPLACE FUNCTION public.claim_waitlist_signup"
    - path: "apps/web/src/actions/waitlist.ts"
      provides: "claimWaitlistSpot Server Action holding the D-03/D-04 non-disclosure filter"
      exports: ["claimWaitlistSpot", "WaitlistState"]
    - path: "apps/web/test/actions/waitlist.concurrency.test.ts"
      provides: "End-to-end happy-path proof that one submission reaches the database through the production call path"
  key_links:
    - from: "apps/web/src/actions/waitlist.ts"
      to: "supabase/migrations/20260812_waitlist_founder_offer.sql"
      via: "service-role rpc call to claim_waitlist_signup"
      pattern: "rpc\\(['\"]claim_waitlist_signup"
    - from: "apps/web/src/actions/waitlist.ts"
      to: "apps/web/src/lib/supabase/admin.ts"
      via: "createAdminClient() service-role client (D-01)"
      pattern: "createAdminClient"
    - from: "supabase/migrations/20260812_waitlist_founder_offer.sql"
      to: "public.waitlist_founder_seq"
      via: "nextval() issues the founder rank inside claim_waitlist_signup"
      pattern: "nextval\\('public\\.waitlist_founder_seq'\\)"
  # canon dropped (not minted as phase prohibitions): OWASP injection/XSS/CSRF families,
  # RGPD/GDPR compliance statements, secrets-in-repo rules — covered project-wide by
  # CLAUDE.md and the existing CI guards, not phase-specific regressions.
  prohibitions:
    - "No row-level-security policy is created on waitlist_signups or app_config — deny-all plus a SECURITY DEFINER door is the entire access control (DATA-05)"
    - "No GRANT of any kind targets the anon or authenticated role on the new tables, sequence or functions (D-01)"
    - "No SECURITY DEFINER function ships without its own REVOKE EXECUTE ... FROM PUBLIC line — deduct_ai_credits' missing grant block is a known gap, not a precedent (D-02, 01-RESEARCH.md Pitfall 1)"
    - "No import of next/headers in apps/web/src/actions/waitlist.ts — rate limiting is deferred to phase 5 and the import breaks direct-import testability (01-RESEARCH.md Pitfall 3)"
    - "No second implementation of the email normalization rules in TypeScript — the plpgsql helper is the only one (D-12)"
    - "No dot-stripping outside gmail.com and googlemail.com (D-10, 01-RESEARCH.md Pitfall 5)"
    - "No read-then-decide cap check — the 200 cap is derived from the sequence value returned by nextval, never from a row count (research/ARCHITECTURE.md Section 1)"
    - "No grant_premium_credits function and no user_profiles.is_lifetime_premium column in this phase — ROADMAP Phase 4's Depends-on line is stale against 01-CONTEXT.md's domain boundary"
    - "No speculative abuse-signal column such as an IP hash (01-RESEARCH.md Open Question 2)"
    - "No edit to any migration file already present on main — new migrations only (CLAUDE.md)"
    - "No service-role key reference under backend/api/src/** (CLAUDE.md, enforced by the CI guard in test-rls.yml)"
    - "No DB-mutating spec runs against the production Supabase project — the waitlist specs require SUPABASE_TEST_URL to equal SUPABASE_URL and skip otherwise"
    - "No assumption that a generic SQL-execution RPC exists in this codebase — none does (01-RESEARCH.md concurrency-harness note)"
---

<objective>
Prove the whole waitlist capture path end to end with one production-quality vertical slice: a
visitor's email travels from the Next.js Server Action, through the service-role client, into a
`SECURITY DEFINER` RPC that atomically issues a founder rank from a Postgres sequence, and lands in
an RLS-locked table no client can touch — and the response that comes back is already filtered for
non-disclosure.

Purpose: the phase's headline promise (an exact 200-founder cap under any concurrency, with zero
direct client surface) is only meaningful through the path production will actually take. Proving
that path first, on one email, exposes any architectural dead end after one commit rather than after
the whole schema has shipped.

Output: `supabase/migrations/20260812_waitlist_founder_offer.sql` (core objects),
`apps/web/src/actions/waitlist.ts`, and the first end-to-end spec in
`apps/web/test/actions/waitlist.concurrency.test.ts`.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/STATE.md
@.planning/workstreams/lien-invite/phases/01-data-foundation/01-CONTEXT.md
@.planning/workstreams/lien-invite/phases/01-data-foundation/01-RESEARCH.md
@.planning/workstreams/lien-invite/phases/01-data-foundation/01-PATTERNS.md
@.planning/workstreams/lien-invite/phases/01-data-foundation/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase this session. No exploration required. -->

From `apps/web/src/lib/supabase/admin.ts` (do not modify):
```typescript
import 'server-only';
export function createAdminClient(): SupabaseClient; // reads process.env.SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY at call time
```

From `apps/web/src/actions/account.ts` — the Server Action state/signature shape to mirror:
```typescript
export type DeleteAccountState = { status: 'idle' | 'success' | 'error'; message: string };
export async function deleteAccount(prevState: DeleteAccountState, formData: FormData): Promise<DeleteAccountState>;
```

New contract this plan publishes (phase 5 renders against it, plan 01-04 tests it):
```typescript
export type WaitlistState = {
  status: 'idle' | 'success' | 'error';
  isFounder: boolean;
  founderRank: number | null;
  message: string;
};
export async function claimWaitlistSpot(prevState: WaitlistState, formData: FormData): Promise<WaitlistState>;
```

New RPC contract this plan publishes:
```
public.claim_waitlist_signup(p_email TEXT, p_audience TEXT, p_locale TEXT DEFAULT NULL,
                             p_utm_source TEXT DEFAULT NULL, p_utm_campaign TEXT DEFAULT NULL)
  RETURNS TABLE(is_new BOOLEAN, is_founder BOOLEAN, founder_rank INTEGER)
public.normalize_waitlist_email(p_email TEXT) RETURNS TEXT
```

Vitest environment facts verified this session:
- `apps/web/vitest.config.ts`: `environment: 'node'`, `globals: true`, `@` aliased to `apps/web/src`,
  include glob `{src,test}/**/*.{spec,test}.{ts,tsx}`, `passWithNoTests: true`. No setup file.
- `apps/web/package.json` has no `pg` dependency and no dotenv loader for tests — env comes from the shell.
- `turbo.json` `test` task already passes `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`,
  `SUPABASE_SERVICE_ROLE_KEY` through to every package.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the one-way schema contract before it is authored</name>
  <decision>
    Approve the shape of `supabase/migrations/20260812_waitlist_founder_offer.sql` — specifically
    (a) the two-column email design with the unique index on `email_normalized`, (b) the
    normalization rule that strips `+suffix` everywhere and dots on Gmail/Googlemail only, and
    (c) how the concurrency test will fast-forward the founder sequence.
  </decision>
  <context>
    Merging this migration to `main` runs it against the production Supabase project automatically
    (`.github/workflows/ci.yml`, `migrate-supabase` job). Undoing any of it afterwards is a second
    migration, and two of the three items cannot be cleanly undone at all once rows exist:
    changing the normalization scheme requires a data migration that can surface two stored rows
    collapsing to one key with no rule for which keeps its founder rank (D-10), and the founder rank
    is a public promise once announced.

    Item (c) is genuinely unresolved and is the only new question here: 01-RESEARCH.md's concurrency
    harness needs `setval` on `public.waitlist_founder_seq` to reach the 200 boundary without
    inserting 195 rows, and the researcher confirmed this codebase contains no generic
    SQL-execution RPC and refused to invent one. Items (a) and (b) are already locked by D-10/D-11
    and are listed here only so the whole one-way surface is visible in one place before it ships —
    they are not being re-opened.
  </context>
  <options>
    <option id="option-a">
      <name>Ship a narrowly-scoped admin sequence-reset function in the same migration (recommended)</name>
      <pros>
        No new npm dependency and no new secret. The function takes one BIGINT, targets the single
        hardcoded sequence `public.waitlist_founder_seq`, builds no dynamic SQL, and is granted to
        `service_role` only — a role that can already write `waitlist_signups` rows directly, so it
        adds no privilege that did not already exist. It is also a genuine production need, not a
        test backdoor: ROADMAP Phase 6 criterion 1 requires the waitlist table truncated and the
        sequence reset before the page opens to the public.
      </pros>
      <cons>
        One more function in the public schema whose only phase-1 caller is a test.
      </cons>
    </option>
    <option id="option-b">
      <name>Seed the sequence over a direct Postgres connection in the test's beforeAll</name>
      <pros>
        Nothing test-shaped ships in the migration.
      </pros>
      <cons>
        Adds a `pg` devDependency to `apps/web` (which has none today, so the package-legitimacy gate
        applies), plus a new Postgres connection-string secret in CI that no existing workflow holds.
        Phase 6 still needs a reset mechanism afterwards, so this defers the same decision rather
        than avoiding it.
      </cons>
    </option>
    <option id="option-c">
      <name>Revise the schema contract before authoring</name>
      <pros>Any concern with the column set, the cap constant, or the normalization asymmetry is far cheaper now than after the migration is applied.</pros>
      <cons>Blocks the phase until the revision is specified.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c (with the revision described)</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: One signup, end to end — migration core, Server Action, and the round-trip proof</name>
  <reversibility rating="one-way">Once merged to main this migration runs against the production project; the normalization scheme and the founder ranks it issues cannot be changed afterwards without a data migration that can leave two rows contending for one rank.</reversibility>
  <precondition>`SUPABASE_TEST_URL` is set in the executing shell and its value equals `SUPABASE_URL` (compare the two env vars read-only) — every DB call in this task must land on the dedicated test project, never production.</precondition>
  <read_first>
    - `supabase/migrations/035_coach_invitations_links_rls.sql` — the canonical shape for this phase: `SECURITY DEFINER`, `SET search_path`, and the mandatory `REVOKE ... FROM PUBLIC` / `GRANT ... TO <role>` pairing at lines 100-101 and 176-177
    - `supabase/migrations/040_peek_invitation_function.sql` line 73 — third confirmation of the same grant idiom
    - `supabase/migrations/026_ai_credits.sql` lines 89-134 — plpgsql declare/lock/decide/return skeleton ONLY; this function ships with no grant statements at all, which is the gap this phase must not repeat
    - `supabase/migrations/034_coach_role_profiles.sql` lines 16-19 — `TEXT` + `CHECK (... IN (...))` for enumerated columns; this codebase has no native enum anywhere
    - `apps/web/src/actions/account.ts` — full file: the `'use server'` + state-type + `(prevState, formData)` structure, and the neutral-success anti-enumeration idiom at lines 73-81
    - `apps/web/src/lib/supabase/admin.ts` — `createAdminClient()`, and note its `server-only` import
    - `apps/web/test/safe-next.spec.ts` — the repo's precedent for importing server-side modules under plain Vitest
    - `.planning/workstreams/lien-invite/phases/01-data-foundation/01-RESEARCH.md` § Code Examples — the DDL, the normalization helper, the claim RPC and the Server Action filter, all written out
    - `.planning/workstreams/lien-invite/phases/01-data-foundation/01-PATTERNS.md` — analog map and the filename convention evidence
  </read_first>
  <files>supabase/migrations/20260812_waitlist_founder_offer.sql, apps/web/src/actions/waitlist.ts, apps/web/test/actions/waitlist.concurrency.test.ts</files>
  <behavior>
    - Submitting a never-seen email returns `status: 'success'` with `isFounder: true` and a numeric `founderRank`, and exactly one row exists for it.
    - The row read back with the service-role client carries the same `founder_rank` the action reported, plus the submitted `audience` and a non-null `created_at`.
    - `normalize_waitlist_email` collapses `Test.User+promo@GMAIL.com` and `testuser@gmail.com` to one key, and keeps `first.last@outlook.com` distinct from `firstlast@outlook.com`.
  </behavior>
  <action>
    Create `supabase/migrations/20260812_waitlist_founder_offer.sql` as a NEW file. The `NNN_` series
    is frozen at `064_unaccent_user_search.sql`; every migration since uses the dated form. Do not
    touch any migration that already exists.

    Author these objects, in this order, fully qualifying every reference with `public.`:

    1. `public.waitlist_signups` — `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`;
       `email TEXT NOT NULL` (the address as typed, used for delivery, D-11);
       `email_normalized TEXT NOT NULL` (the identity key, D-10/D-11);
       `audience TEXT NOT NULL` restricted to `'athlete'` and `'coach'` by a `CHECK ... IN (...)`
       list; `locale TEXT` restricted the same way to `'fr'` and `'en'`; `utm_source TEXT`;
       `utm_campaign TEXT` (D-14); `is_founder BOOLEAN NOT NULL DEFAULT false`;
       `founder_rank INTEGER`; `consent_given_at TIMESTAMPTZ` and `consent_version TEXT`
       (D-15 — this phase defines the columns, phases 3 and 5 fill them);
       `anonymized_at TIMESTAMPTZ` (D-16); `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`; and a
       named table constraint `waitlist_signups_email_format_chk` carrying the minimal format regex
       from 01-RESEARCH.md § Code Examples (D-13 — an `@` and a plausible domain, nothing more;
       richer validation is phase 5's). Use `TEXT` + `CHECK` for both enumerated columns.
    2. Indexes: a unique `idx_waitlist_signups_email_normalized` on `(email_normalized)`; a unique
       partial `idx_waitlist_signups_founder_rank` on `(founder_rank)` restricted to rows where it
       is not null; and `idx_waitlist_signups_audience_created` on `(audience, created_at DESC)`.
    3. `ALTER TABLE public.waitlist_signups ENABLE ROW LEVEL SECURITY;` — and nothing after it. This
       table gets zero policy statements of any kind; the RPCs are the only door (DATA-05). This
       mirrors `user_ai_credits` and `coach_client_links`.
    4. `CREATE SEQUENCE IF NOT EXISTS public.waitlist_founder_seq;` (DATA-02).
    5. `public.normalize_waitlist_email(p_email TEXT) RETURNS TEXT LANGUAGE plpgsql IMMUTABLE` —
       lower and trim, locate the first `@`, return the input unchanged when there is none, then
       drop everything from the first `+` onward in the local part for every domain, and drop dots
       from the local part only when the domain is exactly `gmail.com` or `googlemail.com` (D-10).
       The asymmetry is deliberate: a dot is significant at most other providers.
    6. `public.claim_waitlist_signup(p_email TEXT, p_audience TEXT, p_locale TEXT DEFAULT NULL,
       p_utm_source TEXT DEFAULT NULL, p_utm_campaign TEXT DEFAULT NULL)
       RETURNS TABLE(is_new BOOLEAN, is_founder BOOLEAN, founder_rank INTEGER)
       LANGUAGE plpgsql SECURITY DEFINER SET search_path = public, pg_temp` — normalize the address
       with the helper, insert with `ON CONFLICT (email_normalized) DO NOTHING ... RETURNING id`;
       when no id comes back the address is already known, so read the stored row's status and
       return it with `is_new` false (this branch is server-to-server only — the Server Action, not
       this function, performs the non-disclosure filtering); when an id comes back, take
       `nextval('public.waitlist_founder_seq')`, set the founder flag from `rank <= 200`, update the
       row with both values and return them with `is_new` true. The literal 200 is the locked
       business constant from REQUIREMENTS.md ("Plafond fondateurs: 200 au total"), not a
       configurable value. Never compute the cap from a row count — that race is exactly what the
       sequence exists to eliminate.
    7. Immediately after each of the two functions, write its own
       `REVOKE EXECUTE ON FUNCTION public.<name>(<argtypes>) FROM PUBLIC;` followed by
       `GRANT EXECUTE ON FUNCTION public.<name>(<argtypes>) TO service_role;`. Both functions, no
       exceptions, and no grant to any other role. PostgreSQL grants execute to PUBLIC by default,
       so omitting the revoke leaves the function reachable with the anonymous key — that is a real
       existing gap in `026_ai_credits.sql` and must not be reproduced here.
    8. Write every statement so re-running the whole file is a no-op: `CREATE TABLE IF NOT EXISTS`,
       `CREATE SEQUENCE IF NOT EXISTS`, `CREATE UNIQUE INDEX IF NOT EXISTS`,
       `CREATE OR REPLACE FUNCTION`, and guard the named constraint so a second run does not error.
       Later tasks in this phase append to this same file and re-apply it.

    Apply the file to the test project. Use whichever of these works in this environment, in order:
    the Supabase MCP `apply_migration` tool pointed at the test project; `supabase link --project-ref
    <test project ref>` then `supabase db push`; or `psql` against the test project's connection
    string. If none is available, halt and report — do not proceed to a verification that cannot run.

    Then create `apps/web/src/actions/waitlist.ts`: `'use server'` at the top, importing
    `createAdminClient` from `@/lib/supabase/admin` and nothing from the Next.js request-header
    module — rate limiting is phase 5's, and importing that module here would make this file
    unimportable in a plain Vitest process, breaking the concurrency proof this phase exists to
    deliver. Export the `WaitlistState` type and `claimWaitlistSpot(prevState, formData)` exactly as
    given in the `<interfaces>` block. Read `email`, `audience` and `locale` from the FormData,
    return the error state when email or audience is missing, call
    `admin.rpc('claim_waitlist_signup', { p_email, p_audience, p_locale })`, and return the error
    state when the call errors or returns no row. Then apply the non-disclosure filter as the very
    first thing done with the result (D-03/D-04): when the returned `is_new` is false, return
    success with `isFounder: false` and `founderRank: null`; only when it is true may the real
    founder flag and rank be forwarded, and the rank is only included when the flag is true. The
    message string is identical in both success branches. This ordering is the whole requirement —
    reading any other field before the `is_new` check reintroduces the founder-status oracle.

    Finally create `apps/web/test/actions/waitlist.concurrency.test.ts` with the harness that plan
    01-04 will extend. Call `vi.mock('server-only', () => ({}))` at the top of the file: the
    service-role client module imports that package, whose default export throws outside a React
    Server Component, and Vitest hoists the mock above the static imports. Compute
    `const RUN_DB = Boolean(process.env.SUPABASE_TEST_URL) && process.env.SUPABASE_TEST_URL ===
    process.env.SUPABASE_URL;` and wrap every describe block in `describe.skipIf(!RUN_DB)`. This
    guard is load-bearing: the root CI `verify` job runs `npx turbo run test` with the production
    Supabase secrets, and a DB-mutating waitlist spec running there would consume real founder ranks
    and destroy the guarantee this phase ships. Build a service-role client with
    `createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!, { auth: {
    autoRefreshToken: false, persistSession: false } })`. In `beforeAll`, delete only the
    `waitlist_signups` rows whose email starts with this spec's own `tracer-` prefix — every
    waitlist spec in this phase cleans its own prefix and never a shared wildcard, so two specs can
    never delete each other's fixtures. Write one test that imports
    `claimWaitlistSpot` from `@/actions/waitlist`, submits a unique `tracer-<uuid>@example.com` with
    audience `athlete` through a real FormData, asserts `status` is success and `founderRank` is a
    number, then reads the row back with the service-role client and asserts its `founder_rank`
    equals the reported rank, its `audience` is `athlete` and its `created_at` is not null. Add two
    direct assertions on `normalize_waitlist_email` through the service-role client covering the
    Gmail collapse and the Outlook non-collapse. Clean up the created rows in `afterAll`.
  </action>
  <verify>
    <automated>cd apps/web && SUPABASE_TEST_URL="$SUPABASE_URL" npx vitest run test/actions/waitlist.concurrency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260812_waitlist_founder_offer.sql` exists, and `git diff --name-only main -- supabase/migrations/ | grep -v '20260812_waitlist_founder_offer.sql'` prints nothing (no pre-existing migration was edited)
    - `grep -c 'REVOKE EXECUTE ON FUNCTION' supabase/migrations/20260812_waitlist_founder_offer.sql` outputs `2`
    - `grep -Ec 'GRANT EXECUTE ON FUNCTION public\.[a-z_]+\(.*\) TO service_role' supabase/migrations/20260812_waitlist_founder_offer.sql` outputs `2`
    - `test "$(grep -v '^[[:space:]]*--' supabase/migrations/20260812_waitlist_founder_offer.sql | grep -Ec 'GRANT .* TO (anon|authenticated)')" -eq 0`
    - `test "$(grep -v '^[[:space:]]*--' supabase/migrations/20260812_waitlist_founder_offer.sql | grep -c 'CREATE POLICY')" -eq 0`
    - `grep -c 'ALTER TABLE public.waitlist_signups ENABLE ROW LEVEL SECURITY' supabase/migrations/20260812_waitlist_founder_offer.sql` outputs `1`
    - `grep -c "nextval('public.waitlist_founder_seq')" supabase/migrations/20260812_waitlist_founder_offer.sql` outputs `1`
    - `grep -c 'gmail.com' supabase/migrations/20260812_waitlist_founder_offer.sql` outputs at least `1` and the same file contains `googlemail.com`
    - `grep -c 'waitlist_signups_email_format_chk' supabase/migrations/20260812_waitlist_founder_offer.sql` outputs at least `1`
    - `test "$(grep -v '^[[:space:]]*//' apps/web/src/actions/waitlist.ts | grep -c 'next/headers')" -eq 0`
    - `grep -c 'export async function claimWaitlistSpot' apps/web/src/actions/waitlist.ts` outputs `1`
    - `grep -c 'is_new' apps/web/src/actions/waitlist.ts` outputs at least `1`, and the `is_new` check appears on an earlier line than the first read of `is_founder` in that file
    - `grep -c "describe.skipIf" apps/web/test/actions/waitlist.concurrency.test.ts` outputs at least `1`
    - `cd apps/web && npx vitest run test/actions/waitlist.concurrency.test.ts` exits 0 with at least 3 passing tests when `SUPABASE_TEST_URL` equals `SUPABASE_URL`, and reports skipped (exit 0) when `SUPABASE_TEST_URL` is unset
    - Applying the migration file a second time against the test project completes without error
  </acceptance_criteria>
  <done>One never-seen email submitted through `claimWaitlistSpot` produces exactly one `waitlist_signups` row whose stored `founder_rank` matches the rank the action returned, the rank came from `nextval`, and the table is unreadable to every role but the definer.</done>
</task>

</tasks>

<assumption_delta_decision>
The `assumption-delta scan` detector fired (`detected: true`, kind `pluralization`) on the phrases
"alongside Phase 2 and Phase 3" and "a second founder rank". Both matches are prose artefacts, but
re-reading the phase scope surfaces one real singular-to-plural transition worth recording: D-11
splits what `research/ARCHITECTURE.md` §1 modelled as a single `email` column into two.

- **Primary noun:** `email_normalized` — the normalized address is the identity of a signup.
- **Decision:** `promote`. The normalized form carries the unique index, the `ON CONFLICT` target
  and the anonymization lookup; the address as typed is demoted to a delivery attribute with no
  uniqueness meaning. This is the promote-not-add-alongside move: no constraint keys off the
  as-typed column.
- **Invariant test (accepted, implemented in plan 01-03):** for every non-anonymized row,
  `email_normalized` equals `normalize_waitlist_email(email)`. It goes red the instant any future
  caller writes an unnormalized key, which is precisely what D-12 exists to prevent.
</assumption_delta_decision>

<flagged_assumptions>
## Spec-less edge probe — all 7 rows surfaced, none dropped

This phase has no SPEC.md, so the deterministic edge probe ran over DATA-01..07 and returned
`{"applicable":7,"resolved":0,"unresolved":7}` with every row `category: "unclassified"`. A
controlled A/B during planning showed the probe's classifier keys on English text: the same
requirement classified into `boundary` and `precision` edges in English and returned `unclassified`
in French. REQUIREMENTS.md is French. **The 7/7 unclassified result is a classifier false negative,
not evidence that these requirements have no edges.** All 7 are therefore carried here as explicit
flagged assumptions, each with the authored disposition that replaces the probe's silence.

| Probe row | Probe result | Flagged assumption carried forward | Where it is discharged |
|---|---|---|---|
| DATA-01 | unclassified | Assumed edges: the stored field set is exactly email / audience / created_at / founder_rank plus the D-14/D-15/D-16 additions; nothing else is stored | plan 01-01 Task 2 DDL; asserted in plan 01-03 |
| DATA-02 | unclassified | Assumed edge: rank uniqueness is guaranteed by `nextval` alone, and the unique partial index on `founder_rank` is a second net, not the mechanism | plan 01-01 Task 2; raced in plan 01-04 |
| DATA-03 | unclassified | Assumed boundary: ranks 1..200 inclusive are founders, 201 is not; the 201st signup is still accepted and stored | plan 01-04 concurrency test straddling 196..215 |
| DATA-04 | unclassified | Assumed edges: case, surrounding whitespace, `+suffix` on every domain, and dots on Gmail/Googlemail only; dots stay significant elsewhere | plan 01-03 dedupe spec |
| DATA-05 | unclassified | Assumed edge: deny-all must hold for anon and authenticated, on select and on write, for both new tables — not select alone | plan 01-03 deny-all spec |
| DATA-06 | unclassified | Assumed edge: "through a SECURITY DEFINER RPC" also means the RPC is not executable by anon, which requires the explicit revoke PostgreSQL does not give for free | plan 01-01 Task 2 grants; asserted in plan 01-03 |
| DATA-07 | unclassified | Assumed edge (revised by D-04): membership is disclosable, founder status is not; the oracle is closed at the Server Action, so the test must go through it and not through the raw RPC | plan 01-04 non-disclosure test |

Equality holds: 7 probe-surfaced rows, 7 authored and flagged, 0 silently dropped.
</flagged_assumptions>

<source_coverage_audit>
## Multi-source coverage audit (phase-level)

```
SOURCE    | ID      | Feature/Requirement                                   | Plan     | Status  | Notes
--------- | ------- | ----------------------------------------------------- | -------- | ------- | ------
GOAL      | —       | Atomic, secure, dedupe-safe capture backend; 200-cap   | 01/02/03/04 | COVERED | Split: capture path 01, config+erasure 02, RLS/dedupe proof 03, cap/non-disclosure proof 04
REQ       | DATA-01 | email, audience, timestamp, founder rank stored        | 01, 03   | COVERED |
REQ       | DATA-02 | rank from a Postgres SEQUENCE, never shared            | 01, 04   | COVERED |
REQ       | DATA-03 | 200-cap exact under any concurrency                    | 01, 04   | COVERED |
REQ       | DATA-04 | duplicate email creates no second row or rank          | 01, 03   | COVERED |
REQ       | DATA-05 | RLS on, zero policies, no role reads or writes         | 01, 02, 03 | COVERED | 01 waitlist_signups, 02 app_config, 03 proves both
REQ       | DATA-06 | all access via SECURITY DEFINER RPCs, house idiom      | 01, 02, 03 | COVERED |
REQ       | DATA-07 | third party's founder status never disclosed           | 01, 04   | COVERED | Filter in 01, proof in 04
RESEARCH  | —       | Single dated migration 20260812_waitlist_founder_offer | 01       | COVERED |
RESEARCH  | —       | Explicit REVOKE/GRANT on every RPC (Pitfall 1)         | 01, 02   | COVERED |
RESEARCH  | —       | Server Action free of the request-header module (Pitfall 3) | 01  | COVERED |
RESEARCH  | —       | Gmail-only dot rule (Pitfall 5)                        | 01, 03   | COVERED |
RESEARCH  | —       | test-rls.yml has no migration-apply step (Pitfall 4)   | 02, 04   | COVERED | Blocking checkpoint in 02, workflow step in 04
RESEARCH  | —       | Sequence fast-forward mechanism unresolved             | 01, 02   | COVERED | Decided at 01 Task 1, shipped in 02
RESEARCH  | —       | ROADMAP Phase 4 drift (grant_premium_credits, is_lifetime_premium) | 01, 04 | COVERED | Prohibited here; drift note raised for phase 4
CONTEXT   | D-01    | Server Action + service-role client, no anon grant      | 01       | COVERED |
CONTEXT   | D-02    | REVOKE FROM PUBLIC then GRANT idiom                     | 01, 02   | COVERED |
CONTEXT   | D-03    | real founder status only for a genuinely new signup     | 01, 04   | COVERED |
CONTEXT   | D-04    | DATA-07 revised — membership disclosable, status not    | 01, 04   | COVERED |
CONTEXT   | D-05    | phase 1 delivers the Server Action, not only SQL        | 01       | COVERED |
CONTEXT   | D-06    | read RPC returns threshold-arbitrated object            | 02       | COVERED |
CONTEXT   | D-07    | erasure anonymizes, never deletes                       | 02       | COVERED |
CONTEXT   | D-08    | shared app_config table, deny-all, RPC-read             | 02       | COVERED |
CONTEXT   | D-09    | counter cache with monotonic floor (Upstash)            | NONE     | DEFERRED | Serving-layer concern with no phase-1 consumer; ROADMAP maps FOND-04 to phase 5. Recorded, not silently dropped.
CONTEXT   | D-10    | +suffix everywhere, dots Gmail/Googlemail only          | 01, 03   | COVERED |
CONTEXT   | D-11    | two columns: as-typed and normalized                    | 01       | COVERED |
CONTEXT   | D-12    | normalization inside the RPC, in plpgsql                | 01, 03   | COVERED | Invariant test in 03
CONTEXT   | D-13    | minimal format CHECK on the table                       | 01       | COVERED |
CONTEXT   | D-14    | locale, utm_source, utm_campaign stored                 | 01       | COVERED |
CONTEXT   | D-15    | consent-proof columns ship now, filled later            | 01       | COVERED |
CONTEXT   | D-16    | anonymization RPC and anonymized_at column              | 01, 02   | COVERED | Column in 01, RPC in 02
```

**D-09 disposition.** D-09 describes how the counter is *served* — a short cache with a monotonic
floor over Upstash Redis. Phase 1 ships no counter consumer: `01-CONTEXT.md`'s domain boundary lists
"the counter read RPC" as in scope and every pixel of UI as out of scope, and ROADMAP maps FOND-04
(the never-increasing display) to phase 5 along with the page that renders it. Plan 01-02 therefore
delivers the RPC D-09 caches, and the cache with its monotonic floor belongs with its consumer in
phase 5. This is recorded here rather than dropped so phase 5 planning inherits it explicitly.
</source_coverage_audit>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is new in phase 1. Nothing here exists on `main` before this phase.

**Database — all in `supabase/migrations/20260812_waitlist_founder_offer.sql` (new file)**
- Tables: `public.waitlist_signups`, `public.app_config`
- Sequence: `public.waitlist_founder_seq`
- `waitlist_signups` columns: `id`, `email`, `email_normalized`, `audience`, `locale`,
  `utm_source`, `utm_campaign`, `is_founder`, `founder_rank`, `consent_given_at`,
  `consent_version`, `anonymized_at`, `created_at`
- `app_config` columns: `key`, `value`, `updated_at`
- Indexes: `idx_waitlist_signups_email_normalized`, `idx_waitlist_signups_founder_rank`,
  `idx_waitlist_signups_audience_created`
- Named constraint: `waitlist_signups_email_format_chk`
- Functions: `public.normalize_waitlist_email(TEXT)`,
  `public.claim_waitlist_signup(TEXT, TEXT, TEXT, TEXT, TEXT)`,
  `public.get_waitlist_founder_status()`, `public.anonymize_waitlist_signup(TEXT)`,
  `public.reset_waitlist_founder_sequence(BIGINT)`
- Seeded config key: `waitlist_reveal_threshold`

**Web**
- `apps/web/src/actions/waitlist.ts` — exports `claimWaitlistSpot`, type `WaitlistState`

**Tests**
- `apps/web/test/actions/waitlist.concurrency.test.ts`
- `backend/api/test/rls/waitlist-rls.spec.ts`

**CI**
- `.github/workflows/test-rls.yml` — modified (path filters, migration-apply step, web waitlist job)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Action | Unauthenticated visitor input crosses into server code here; the response that crosses back is the only thing an attacker observes |
| Server Action → Postgres | Service-role credential is used here and never leaves the server |
| any client key → PostgREST | The anon and authenticated keys reach the database directly; deny-all RLS plus function grants are the only control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | `normalize_waitlist_email`, `claim_waitlist_signup` | high | mitigate | Explicit `REVOKE EXECUTE ... FROM PUBLIC` then `GRANT EXECUTE ... TO service_role` on each function; PostgreSQL's PUBLIC default is what makes this mandatory, and `deduct_ai_credits` is the in-repo counter-example |
| T-01-02 | Information Disclosure | `claimWaitlistSpot` response shape | high | mitigate | The `is_new` gate is the first thing read from the RPC result; duplicates return `isFounder: false` and `founderRank: null` with an identical message (D-03/D-04). Proven in plan 01-04 |
| T-01-03 | Information Disclosure | `waitlist_signups` via PostgREST | high | mitigate | Row level security enabled with no policy; anon and authenticated selects return zero rows and writes error. Proven in plan 01-03 |
| T-01-04 | Tampering | DB-mutating specs pointed at production | high | mitigate | Both new specs require `SUPABASE_TEST_URL` to be set and equal to `SUPABASE_URL`, and skip otherwise; the root CI `verify` job runs with production secrets and therefore skips them |
| T-01-05 | Tampering | SQL injection through RPC arguments | low | mitigate | Every RPC takes typed plpgsql parameters; no dynamic SQL string is built anywhere in the migration |
| T-01-06 | Spoofing | Third party's waitlist membership inferred by submitting their address | low | accept | Explicitly accepted in D-04 with the residual risk recorded: a new signup shows a rank and a duplicate does not, so membership stays inferable. Founder status — the property DATA-07 protects — does not leak |
| T-01-07 | Denial of Service | Scripted signups consuming all 200 founder spots | medium | transfer | Out of phase-1 scope by decision: D-01 ships the server-side chokepoint that phase 5's Upstash rate limiter attaches to. Flagged so it is not mistaken for already-handled |
| T-01-08 | Repudiation | Consent cannot be demonstrated for early founders | low | accept | Phase 1 ships `consent_given_at` and `consent_version` so the earliest cohort is not retrofit-blocked; population is phase 3/5 (D-15) |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase. 01-RESEARCH.md § Package Legitimacy Audit records "Not applicable — this phase installs zero new packages"; every component is already a workspace dependency |
</threat_model>

<verification>
- The migration file is new, and no migration already on `main` was modified.
- Both functions in this plan carry their own revoke-then-grant pair, targeting `service_role` only.
- `waitlist_signups` has row level security on and no policy.
- The founder rank comes from `nextval`, and the cap is compared against the value it returns.
- `apps/web/src/actions/waitlist.ts` does not import the Next.js request-header module.
- The non-disclosure filter reads `is_new` before any other field of the RPC result.
- `cd apps/web && npx vitest run test/actions/waitlist.concurrency.test.ts` is green against the
  test project and cleanly skipped when `SUPABASE_TEST_URL` is unset.
</verification>

<success_criteria>
- One submission through the production call path creates one row whose stored `founder_rank`
  equals the rank the Server Action reported.
- The Gmail collapse and the Outlook non-collapse both hold when `normalize_waitlist_email` is
  called directly.
- Re-applying the whole migration file to the test project completes without error.
- Every acceptance criterion in Task 2 passes.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/01-data-foundation/01-01-SUMMARY.md` when done.
</output>
