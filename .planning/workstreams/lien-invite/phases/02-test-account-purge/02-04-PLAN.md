---
phase: 02-test-account-purge
plan: 04
type: execute
wave: 4
depends_on: ['02-01', '02-02', '02-03']
files_modified:
  - scripts/purge-test-accounts/RUNBOOK.md
  - apps/web/test/purge/purge-rehearsal.test.ts
autonomous: false
requirements: [PURGE-01, PURGE-02, PURGE-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ROADMAP Phase 2 success criteria 1, 2 and 3 — this plan is where the written, human-reviewed
    # form of each is delivered (resolved edge coverage, PURGE-01/02/03)
    - "A written, human-reviewed list of exact test-account IDs/emails exists before any deletion runs — no deletion criterion is a bare pattern match used as the executed filter (PURGE-01)"
    - "A dry-run export lists precisely the accounts that would be deleted, including any cross-links to real (non-candidate) accounts flagged for manual review, with zero rows actually removed at that point (PURGE-02)"
    - "A restorable backup/PITR checkpoint exists and its timestamp is recorded before the real deletion executes (PURGE-03)"
    # In-phase evidence
    - "The criterion is written down in the repository as prose a second reviewer can read without running anything: the account email domain is exactly ziko-app.com, every other domain is a real account, no exceptions (D-01)"
    - "The runbook states the ordered procedure — dry-run, human review of the concrete row set, export, PITR check, confirmed delete, post-purge verification — with the exact command and flags for each step"
    - "The runbook names the two-person rule explicitly: one person runs the purge, a second reviews the dry-run output first"
    - "The runbook states the recovery path from the export CSV, and states PITR as the secondary backstop behind it (D-04)"
    - "The runbook states that the real production deletion is deliberately outside this phase and is a separate, explicitly human-triggered step (D-03)"
    - "The four scripts compose end to end against one fixture dataset: the dry-run partition feeds the export, the export manifest feeds the delete guard, and the post-check reconciles the result"
    - "The human has reviewed the criterion, the runbook and the rehearsal evidence and confirmed the phase is rehearsed rather than fired (D-02, D-03)"
  artifacts:
    - path: "scripts/purge-test-accounts/RUNBOOK.md"
      provides: "The written criterion (PURGE-01) and the ordered, reviewable two-person procedure"
      contains: "ziko-app.com"
    - path: "apps/web/test/purge/purge-rehearsal.test.ts"
      provides: "End-to-end composition proof across all four purge scripts on one fixture dataset"
  key_links:
    - from: "scripts/purge-test-accounts/RUNBOOK.md"
      to: "scripts/purge-test-accounts/delete.mjs"
      via: "documents the exact invocation and every guard flag"
      pattern: "accept-unknown-pitr"
    - from: "apps/web/test/purge/purge-rehearsal.test.ts"
      to: "scripts/purge-test-accounts/lib.mjs"
      via: "drives runDryRun, then feeds its report into the export and delete modules"
      pattern: "runDryRun"
  prohibitions:
    - "The runbook never instructs anyone to issue a raw bulk statement against the auth schema in the SQL editor (research/ARCHITECTURE.md §6 step 5)"
    - "The runbook never presents the criterion as a fuzzy or substring pattern — it states exact domain equality (D-01, research/ARCHITECTURE.md §6)"
    - "The runbook never presents the export as optional or conditional on PITR being enabled (D-04)"
    - "The rehearsal never issues a network call and never requires a Supabase credential — it runs entirely on injected fixtures (D-03)"
    - "No real account email, user id, or credential appears in the runbook or in any test fixture"
    - "The review checkpoint is never auto-approved — it is the human gate the two-person rule requires"
---

<objective>
Deliver the part of PURGE-01 that is not code: the criterion written down where a second person can
read it, the ordered procedure with real commands, and a proof that the four scripts actually
compose into that procedure. Then stop and let the human confirm.

Purpose: `research/PITFALLS.md` Pitfall 13 step 6 is a two-person rule, and a two-person rule needs a
document the second person can read. `research/ARCHITECTURE.md` §6 step 2 requires the criterion to
be written and agreed by a human rather than inferred by a query — this plan is where that written
form lands in the repository, alongside the evidence that the tooling behaves as the document claims.

Output: `scripts/purge-test-accounts/RUNBOOK.md`, an end-to-end rehearsal suite, and a blocking
review checkpoint.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/phases/02-test-account-purge/02-CONTEXT.md
@.planning/workstreams/lien-invite/phases/02-test-account-purge/02-01-SUMMARY.md
@.planning/workstreams/lien-invite/phases/02-test-account-purge/02-02-SUMMARY.md
@.planning/workstreams/lien-invite/phases/02-test-account-purge/02-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The complete CLI surface this runbook documents and this rehearsal drives. -->

```
node scripts/purge-test-accounts/dry-run.mjs [--out <dir>]
node scripts/purge-test-accounts/export.mjs --report <path> [--out <dir>]
node scripts/purge-test-accounts/delete.mjs --manifest <path> [--confirm]
                                            [--accept-unknown-pitr]
                                            [--max-manifest-age-minutes <n>] [--out <dir>]
node scripts/purge-test-accounts/verify-purge.mjs --manifest <path> --report <path>
```

Environment, per `apps/web/.env.local` naming:
```
SUPABASE_URL                 required by every script
SUPABASE_SERVICE_ROLE_KEY    required by every script except pitr.mjs
SUPABASE_ACCESS_TOKEN        optional; absence downgrades the PITR result to unknown
```
Node 22 is in use, so `node --env-file=apps/web/.env.local <script>` loads them without extra tooling.

Module surfaces the rehearsal composes (all pure or injection-based, no client required):
```
lib.mjs          runDryRun({ listUsers, fetchCrossLinks, now }), writeReport(report, outDir),
                 classifyAccounts({ users, crossLinks }), isTestAccountEmail(email)
export.mjs       collectExportRows({ report, fetchProfiles, fetchWaitlistRows }),
                 buildManifest({ report, reportPath, csvPath, rows, pitr, now }),
                 writeExport({ rows, manifest, outDir })
pitr.mjs         checkPitrStatus({ supabaseUrl, accessToken, fetchImpl, now })
delete.mjs       assertManifestIntegrity({ manifest, now, maxAgeMinutes, acceptUnknownPitr, fileExists }),
                 runDelete({ manifest, deleteAccount, confirm, now })
verify-purge.mjs checkResidualMatches({ users, manifest, report }),
                 checkAccountConservation({ users, manifest, report }), summarizeOrphans(rows)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the criterion and the ordered two-person procedure</name>
  <read_first>
    - `scripts/purge-test-accounts/dry-run.mjs`, `export.mjs`, `delete.mjs`, `verify-purge.mjs` — the actual flag names and defaults, so every command in the runbook is copy-pasteable rather than approximate
    - `scripts/purge-test-accounts/lib.mjs` — the criterion as implemented, so the prose and the code agree
    - `.planning/workstreams/lien-invite/research/PITFALLS.md` lines 256-278 — the seven-step "how to avoid" procedure and the warning signs this document has to close off
    - `.planning/workstreams/lien-invite/research/ARCHITECTURE.md` lines 419-484 — the criterion discussion, the FK cascade map, and the five-step recommended procedure
    - `.planning/workstreams/lien-invite/phases/02-test-account-purge/02-CONTEXT.md` — D-01 through D-05 in full
    - `CLAUDE.md` — the environment-variable names and the Supabase key conventions
  </read_first>
  <files>scripts/purge-test-accounts/RUNBOOK.md</files>
  <action>
    Create `scripts/purge-test-accounts/RUNBOOK.md`. Write it for a second person who has not read any
    planning document and who is about to authorize an irreversible action.

    Open with the criterion as prose, stated once and unambiguously: an `auth.users` account whose
    email domain is exactly `ziko-app.com` is test or dev data; every other domain is a real account,
    with no exception list. State that the comparison is case-insensitive, whitespace-trimmed, and an
    exact equality on the domain after the final at-sign — so `notziko-app.com`, `sub.ziko-app.com`,
    `ziko-app.co` and `ziko-app.com.attacker.tld` are all real accounts. Include that list; the
    lookalikes are the entire reason the criterion is an equality rather than a pattern. State that
    this rule is the human-provided criterion of record for PURGE-01 and that changing it requires a
    new written decision, not an edit to a script.

    Document the FK reality plainly: every domain table roots at `auth.users.id` with
    `ON DELETE CASCADE` across 31 migration files, so deleting one account removes that account's
    workouts, nutrition logs, AI conversations, credit ledger and coach links. Name the cross-user
    hazard from `research/ARCHITECTURE.md` §6 — a test coach entangled with a real athlete — and
    state that the dry-run withholds those candidates automatically and lists them for manual
    handling outside this procedure.

    Write the procedure as numbered steps, each with its literal command and what to look at in the
    output:
    1. Load the environment with `node --env-file=apps/web/.env.local`, naming `SUPABASE_URL`,
       `SUPABASE_SERVICE_ROLE_KEY` and the optional `SUPABASE_ACCESS_TOKEN`.
    2. Run the dry-run. Read the four totals. This step removes nothing.
    3. **Second-person review.** A person who did not run step 2 opens the CSV and reads every row.
       This is the two-person rule from Pitfall 13 step 6 and it is the gate PURGE-01 and PURGE-02
       exist for — the reviewer is checking the concrete row set, not the rule. Call out what to look
       for: an unfamiliar address, a recent `last_sign_in_at`, a non-zero flagged count.
    4. Run the export immediately before the deletion, passing the reviewed report. State that this
       step is unconditional and is the primary safety net, and that a manifest older than 60 minutes
       is refused by the next step precisely so this stays true.
    5. Read the PITR line the export prints. State that PITR is the secondary backstop behind the
       export, that an `unknown` result is a real outcome rather than an error, and that proceeding on
       `unknown` requires the acknowledgement flag and is a decision the operator is making on the
       record.
    6. Run the delete with the manifest. Show the inert invocation first and the confirmed invocation
       second, and note that the confirmation flag is the only difference between them.
    7. Run the post-purge verification with both the manifest and the original report. State what a
       pass looks like: no manifest id survives, every surviving account matching the criterion was
       already named as withheld, the surviving account count is at or above the scanned-minus-purged
       floor, and the orphan total is zero. Spell out that the conservation line is the one that
       would catch a real account destroyed by an over-broad criterion, since such an account leaves
       no residual match and no orphan behind — it is the check to read most carefully.

    Add a recovery section: restoring from the export CSV is the fast path and restores only the
    affected rows; PITR is the slow, project-wide fallback behind it. Note that the Admin API path is
    what keeps Supabase's own sessions, refresh tokens and identities consistent, and that issuing a
    raw bulk statement against the auth schema in the SQL editor is not an alternative to this
    procedure — `research/ARCHITECTURE.md` §6 step 5 recommends explicitly against it.

    Close with a scope section stating that the real production run is deliberately outside phase 2
    per D-03: the tooling here is built, tested and rehearsed, and firing it is a separate, explicitly
    human-triggered step taken once production credentials are in hand and the review in step 3 has
    happened. State that no phase-2 task executed the deletion against production.

    Use no real account email, no real user id and no credential value anywhere in the document.
  </action>
  <verify>
    <automated>test -f scripts/purge-test-accounts/RUNBOOK.md && for d in notziko-app.com sub.ziko-app.com ziko-app.co ziko-app.com.attacker.tld; do grep -q "$d" scripts/purge-test-accounts/RUNBOOK.md || { echo "missing lookalike $d"; exit 1; }; done && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Every script is documented with its command: `for s in dry-run export delete verify-purge; do grep -q "purge-test-accounts/$s.mjs" scripts/purge-test-accounts/RUNBOOK.md || exit 1; done`
    - Every guard flag is documented: `for f in -- confirm accept-unknown-pitr max-manifest-age-minutes manifest report; do grep -q -- "$f" scripts/purge-test-accounts/RUNBOOK.md || exit 1; done`
    - All three environment variables are named: `for v in SUPABASE_URL SUPABASE_SERVICE_ROLE_KEY SUPABASE_ACCESS_TOKEN; do grep -q "$v" scripts/purge-test-accounts/RUNBOOK.md || exit 1; done`
    - All three PITR states are described: `for s in enabled disabled unknown; do grep -qi "$s" scripts/purge-test-accounts/RUNBOOK.md || exit 1; done`
    - `grep -ci 'two-person\|second person\|deuxième' scripts/purge-test-accounts/RUNBOOK.md` outputs at least `1`
    - `grep -c 'ON DELETE CASCADE' scripts/purge-test-accounts/RUNBOOK.md` outputs at least `1`
    - `grep -ci 'D-03' scripts/purge-test-accounts/RUNBOOK.md` outputs at least `1` (the out-of-scope boundary is stated)
    - `test "$(grep -c 'DELETE FROM' scripts/purge-test-accounts/RUNBOOK.md)" -eq 0` (the document never spells out a bulk statement someone could copy)
    - `test "$(grep -Ec "LIKE '%|email LIKE" scripts/purge-test-accounts/RUNBOOK.md)" -eq 0` (the criterion is never presented as a pattern match)
    - `test "$(grep -Eic 'eyJ[A-Za-z0-9_-]{10}|sbp_[A-Za-z0-9]{10}' scripts/purge-test-accounts/RUNBOOK.md)" -eq 0` (no credential value pasted into the document)
    - The number of numbered procedure steps is at least 7: `test "$(grep -Ec '^[0-9]+\.' scripts/purge-test-accounts/RUNBOOK.md)" -ge 7`
  </acceptance_criteria>
  <done>A second reviewer can read one file and know the exact criterion, the ordered commands with their real flags, what a pass looks like at each step, how to recover, and that the production run has not happened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rehearse the whole procedure end to end on a fixture dataset</name>
  <read_first>
    - `apps/web/test/purge/purge-lib.test.ts`, `purge-export.test.ts`, `purge-delete.test.ts` — the fixture and injection conventions to reuse rather than reinvent
    - `scripts/purge-test-accounts/lib.mjs`, `export.mjs`, `pitr.mjs`, `delete.mjs`, `verify-purge.mjs` — the exact exported signatures being chained
    - `scripts/purge-test-accounts/RUNBOOK.md` — the step order this rehearsal must mirror, so the document and the proof cannot drift
    - `.planning/workstreams/lien-invite/phases/02-test-account-purge/02-CONTEXT.md` — D-03's "built and rehearsed" framing
  </read_first>
  <files>apps/web/test/purge/purge-rehearsal.test.ts</files>
  <behavior>
    - A fixture project of eight accounts — five at the test domain, one at each of two lookalike domains, one real consumer address — plus one cross-link from a test coach to a real athlete, yields a dry-run report with five candidates, one flagged, and four in `to_delete`.
    - Feeding that report into the export produces four CSV rows and a manifest whose `candidate_ids` are exactly those four ids.
    - The manifest passes `assertManifestIntegrity` when fresh, its PITR status resolved through `checkPitrStatus` with a fake fetch reporting enabled.
    - Mutating one character of the manifest's id list and re-checking integrity fails, without any deletion having been attempted.
    - Running `runDelete` unconfirmed against the valid manifest attempts zero deletions.
    - Running it confirmed deletes exactly those four ids from the fixture project's account list.
    - Re-running the criterion over the post-delete account list leaves exactly the flagged cross-linked account, `checkResidualMatches` reports it as expected rather than unexpected, and the run passes.
    - `checkAccountConservation` over the post-delete fixture passes, and deleting one extra real account from the fixture by hand makes it fail with a shortfall of one.
    - `summarizeOrphans` over a post-delete scan of the fixture's linked tables reports a total of zero.
  </behavior>
  <action>
    Create `apps/web/test/purge/purge-rehearsal.test.ts` as a single ordered composition test that
    walks the runbook's steps 2 through 7 against one in-memory fixture project.

    Build the fixture as a small mutable object holding an account array and arrays standing in for
    `coach_client_links`, `coach_vocal_feedbacks`, `workout_programs`, `user_profiles` and
    `waitlist_signups`. Include the eight accounts described in the behaviors, using addresses that
    are obviously synthetic — no real user's address belongs in a committed fixture. Include one
    `coach_client_links` row pairing a test-domain account with the real consumer account; that single
    row is what makes the D-05 exclusion observable end to end rather than only in a unit test.

    Chain the real exported functions in runbook order, never reimplementing any of their logic:
    `runDryRun` with `listUsers` and `fetchCrossLinks` reading the fixture, then `writeReport` into a
    `mkdtempSync` directory; `collectExportRows` and `buildManifest` and `writeExport` with fixture
    fetchers and a `pitr` value obtained from a real `checkPitrStatus` call driven by a fake
    `fetchImpl`; `assertManifestIntegrity` with `fileExists` bound to `node:fs.existsSync` against the
    real written CSV; `runDelete` twice, first unconfirmed then confirmed, with `deleteAccount`
    removing the id from the fixture's account array so the effect is observable; then
    `checkResidualMatches`, `checkAccountConservation` and `summarizeOrphans` over the mutated
    fixture. Then add the over-deletion case: remove one real account from the fixture array by hand,
    re-run the conservation check, and assert it fails reporting a shortfall of one — that proves the
    safety net catches the failure mode it exists for, rather than merely passing on a clean run.

    Assert at each step against the values listed in the behaviors, and include the negative case:
    flip one character in `manifest.candidate_ids[0]`, re-run `assertManifestIntegrity`, assert it
    reports not-ok, and assert the fixture's account count is unchanged so it is visible that nothing
    was deleted on that path.

    This test issues no network call and reads no environment variable, which is the property that
    makes it a rehearsal rather than a run: it proves the composition without ever being able to
    touch production. Remove the temporary directory afterwards.
  </action>
  <verify>
    <automated>cd apps/web && npm run test -- test/purge/purge-rehearsal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && npm run test -- test/purge/purge-rehearsal.test.ts` exits 0 with at least 15 passing assertions
    - All five modules are exercised: `for m in lib export pitr delete verify-purge; do grep -q "purge-test-accounts/$m.mjs" apps/web/test/purge/purge-rehearsal.test.ts || exit 1; done`
    - The chained entry points are called by name: `for f in runDryRun writeExport assertManifestIntegrity runDelete checkResidualMatches checkAccountConservation summarizeOrphans; do grep -q "$f" apps/web/test/purge/purge-rehearsal.test.ts || exit 1; done`
    - `grep -c 'coach_client_links' apps/web/test/purge/purge-rehearsal.test.ts` outputs at least `1` (the cross-link exclusion is observed end to end)
    - `test "$(grep -Ec 'process\.env' apps/web/test/purge/purge-rehearsal.test.ts)" -eq 0` (the rehearsal reads no environment variable)
    - `test "$(grep -Eic 'eyJ[A-Za-z0-9_-]{10}|sbp_[A-Za-z0-9]{10}' apps/web/test/purge/purge-rehearsal.test.ts)" -eq 0` (no credential in the fixture)
    - `cd apps/web && npm run test` exits 0 and `cd apps/web && npm run lint` exits 0
    - `npx turbo run test` exits 0 — the whole monorepo suite is green with the new purge tests in it
  </acceptance_criteria>
  <done>The four scripts provably compose into the runbook's procedure: the dry-run partition survives into the export, the manifest hash gates the delete, the confirmed delete removes exactly the reviewed set, and the post-check reconciles the withheld cross-linked account as expected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the criterion, the rehearsal, and the "rehearsed, not fired" boundary</name>
  <what-built>
    The complete test-account purge toolkit, built and rehearsed but deliberately not fired (D-03):

    - `scripts/purge-test-accounts/dry-run.mjs` — read-only enumeration applying the locked
      `@ziko-app.com` criterion, with automatic exclusion of candidates cross-linked to real accounts
    - `scripts/purge-test-accounts/export.mjs` + `pitr.mjs` — the unconditional pre-delete row export
      and the live PITR status read, both recorded in a hashed manifest
    - `scripts/purge-test-accounts/delete.mjs` — Admin API deletion of exactly the hashed manifest set,
      inert without an explicit confirmation flag
    - `scripts/purge-test-accounts/verify-purge.mjs` — residual reconciliation and a seven-column
      orphan scan
    - `scripts/purge-test-accounts/RUNBOOK.md` — the written criterion and the ordered two-person
      procedure
    - Four Vitest suites, including an end-to-end rehearsal, all passing with no Supabase credential
      present

    No account was deleted. No script was run against production.
  </what-built>
  <how-to-verify>
    1. Read `scripts/purge-test-accounts/RUNBOOK.md` top to bottom. Confirm the criterion in its
       opening section is the rule you intended: an account whose email domain is exactly
       `ziko-app.com` is test data, every other domain is real, no exceptions. This is the PURGE-01
       written criterion of record — if it is wrong, say so now rather than after a production run.
    2. Confirm the lookalike list reads correctly to you: `notziko-app.com`, `sub.ziko-app.com`,
       `ziko-app.co` and `ziko-app.com.attacker.tld` are all treated as real accounts.
    3. Run `cd apps/web && npm run test -- test/purge/` and confirm all four suites pass. These run
       without any Supabase credential — that is deliberate, and it is why nothing here could have
       touched production.
    4. Open `apps/web/test/purge/purge-rehearsal.test.ts` and read the fixture at the top. Confirm the
       scenario matches your expectation: five test-domain accounts, one of them cross-linked to a
       real athlete, and that one is withheld from the delete set rather than deleted.
    5. Confirm you accept the scope boundary: this phase ends here. The real production run —
       obtaining `SUPABASE_SERVICE_ROLE_KEY` for the production project, running the dry-run against
       it, reviewing the actual CSV of real rows, and running the confirmed delete — is a separate,
       explicitly human-triggered step outside phase 2, per D-03. Two of ROADMAP.md's five success
       criteria for this phase (a backup checkpoint taken before the real deletion, and the
       post-deletion zero-row state) can only complete during that run.
    6. Note the open item carried forward: `SUPABASE_SERVICE_ROLE_KEY` for the **production** project
       has not been available in any session so far, and neither has `SUPABASE_ACCESS_TOKEN` for the
       PITR read. Both are needed before the out-of-phase run, and the runbook names them.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the criterion and the rehearsal, or describe what should change in the criterion, the runbook, or the scripts.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is new in phase 2. Files marked *(plan NN)* are created by that plan.

**Scripts — `scripts/purge-test-accounts/` (new directory)**
- `lib.mjs` *(02-01)* — `TEST_ACCOUNT_DOMAIN`, `isTestAccountEmail`, `createPurgeAdminClient`,
  `listAllUsers`, `fetchCrossLinks`, `classifyAccounts`, `runDryRun`, `writeReport`
- `dry-run.mjs` *(02-01)* — CLI, flag `--out <dir>`
- `pitr.mjs` *(02-02)* — `checkPitrStatus`, `resolveProjectRef`
- `export.mjs` *(02-02)* — `collectExportRows`, `buildManifest`, `writeExport`; flags
  `--report <path>`, `--out <dir>`
- `delete.mjs` *(02-03)* — `parsePurgeArgs`, `assertManifestIntegrity`, `runDelete`,
  `writeDeleteLog`; flags `--manifest <path>`, `--confirm`, `--accept-unknown-pitr`,
  `--max-manifest-age-minutes <n>`, `--out <dir>`
- `verify-purge.mjs` *(02-03)* — `checkResidualMatches`, `checkAccountConservation`,
  `fetchOrphanRows`, `summarizeOrphans`; flags `--manifest <path>`, `--report <path>`
- `RUNBOOK.md` *(02-04)* — the written criterion (PURGE-01) and the ordered two-person procedure
- `exports/` *(02-01)* — git-ignored output directory

**Tests — `apps/web/test/purge/` (new directory)**
- `purge-lib.test.ts` *(02-01)* · `purge-export.test.ts` *(02-02)* ·
  `purge-delete.test.ts` *(02-03)* · `purge-rehearsal.test.ts` *(02-04)*

**Modified**
- `.gitignore` *(02-01)* — adds `scripts/purge-test-accounts/exports/`

**On-disk data contracts (produced at run time, never committed)**
- `dry-run-<ISO>.json` / `.csv` *(02-01)*
- `export-<ISO>.csv` and `export-<ISO>.manifest.json` carrying `candidate_ids`,
  `candidate_ids_sha256` and `pitr` *(02-02)*
- `delete-log-<ISO>.json` *(02-03)*

**Not produced by this phase (deliberately, per D-03)**
- No production deletion is executed, and no `supabase/migrations/*.sql` file is added — the purge is
  operational rather than schema, so the schema-push gate does not fire.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| written criterion → the operator's understanding | The runbook is what the second reviewer authorizes against; if the prose and the code disagree, the review approves something that will not happen |
| runbook prose → a copy-pasted command | Anything spelled out as a command in this document may be run verbatim by someone in a hurry |
| committed fixture → the repository | A fixture is committed forever; a real address in one is a permanent disclosure |
| phase boundary → production | The only thing preventing a production run here is that no task performs one (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-24 | Spoofing | Runbook prose drifting from the implemented criterion, so the review approves a different rule than the one that runs | high | mitigate | The runbook is written from `lib.mjs`, the lookalike list is gated by name in both the runbook and the criterion test table, and the rehearsal chains the real exported functions rather than restating their logic |
| T-02-25 | Elevation of Privilege | A runbook reader copying a bulk statement out of the document and running it in the SQL editor | high | mitigate | The document never spells one out; a gate asserts the bulk-statement literal is absent, and the recovery section names the Admin API as the only deletion path |
| T-02-26 | Information Disclosure | A real account email or credential committed inside the runbook or the rehearsal fixture | high | mitigate | Fixtures use obviously synthetic addresses; gates assert no JWT-shaped or Supabase-token-shaped string appears in either file |
| T-02-27 | Tampering | The rehearsal reaching a real project through a stray environment variable | high | mitigate | A gate asserts the rehearsal file contains no environment-variable read at all, and every data source is an injected fixture |
| T-02-28 | Repudiation | The phase sealing as "purge complete" when it was only rehearsed | high | mitigate | The blocking checkpoint states the boundary explicitly and names the two ROADMAP criteria that can only complete during the out-of-phase run; the runbook repeats it in its scope section |
| T-02-29 | Spoofing | The criterion silently presented as a fuzzy pattern, reintroducing the exact hazard Pitfall 13 names | high | mitigate | A gate asserts no `LIKE`-shaped pattern appears in the runbook, and the criterion is stated as exact domain equality with its lookalike counterexamples |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; the runbook is prose and the rehearsal composes modules already written in plans 02-01 through 02-03 |
</threat_model>

<verification>
- The runbook names every script, every guard flag, all three environment variables and all three
  PITR states, and contains no bulk-statement literal and no pattern-match presentation of the
  criterion.
- The runbook lists all four lookalike domains as real accounts.
- The rehearsal chains all five modules on one fixture, reads no environment variable, and issues no
  network call.
- A tampered manifest fails integrity in the rehearsal with the fixture account count unchanged.
- The confirmed delete removes exactly the four reviewed ids and leaves the cross-linked account,
  which the residual check classifies as expected.
- `npx turbo run test` is green across the monorepo.
- The human has reviewed the criterion and the scope boundary and responded at the checkpoint.
</verification>

<success_criteria>
- `scripts/purge-test-accounts/RUNBOOK.md` states the PURGE-01 criterion in prose a second reviewer
  can act on, with the full ordered procedure and its real commands.
- `apps/web/test/purge/purge-rehearsal.test.ts` passes at least 15 assertions with no credential and
  no network access.
- The human has explicitly confirmed the criterion and accepted that the production run is a separate
  step outside this phase.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/02-test-account-purge/02-04-SUMMARY.md` when done.
</output>
