---
phase: 06-founder-offer-go-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/founder-offer-go-live/RUNBOOK.md
  - .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md
autonomous: false
# Phase 6 owns no net-new requirement. The IDs below are the ones this plan
# *verifies the production-activation state of*, per ROADMAP Phase 6 and
# REQUIREMENTS.md's Traceability table — not requirements this phase introduces.
requirements: [CRED-05, LEGAL-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The milestone branch is merged into `main` through a PR that lands as a single merge or squash commit, and all four files under `supabase/migrations/` that `git diff main --name-status` currently reports as added are present on `main` afterwards."
    - "CI's `migrate-supabase` job is observed running on the merge push to `main` in `.github/workflows/ci.yml`, and its `Push migrations` step is observed to have actually executed rather than been skipped by its `changed == 'true'` guard (D-02)."
    - "`SELECT key, value FROM public.app_config ORDER BY key;` run against production project `slkobhavpwsubnsmuhya` returns a `premium_credit_cap_enabled` row — one migration artifact that provably did not exist before this plan ran (D-02)."
    - "`GET https://ziko-api-lilac.vercel.app/health` returns HTTP 200 from the post-merge deploy, and the deployed commit SHA is confirmed to be the merge commit — proving Vercel picked up Phase 4's credit-gate code, not just that the API is up (D-02)."
    - "Every real command in this plan was fired by a human holding production credentials; the executing session fired none of them, because no session in this milestone has held `SUPABASE_ACCESS_TOKEN` or `SUPABASE_SERVICE_ROLE_KEY` for production (D-01)."
    - "`scripts/founder-offer-go-live/RUNBOOK.md` is usable by a human with no memory of this planning discussion: every step names a literal command and the literal output to read back."
  artifacts:
    - "scripts/founder-offer-go-live/RUNBOOK.md (created — sections 0 through 3: preconditions, merge, CI migration apply, deploy confirmation)"
    - ".planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md (created — the dated evidence ledger every later plan appends to)"
  key_links:
    - "the PR merge strategy → CI's `HEAD~1 HEAD` migration detector: the detector diffs only the top commit against its immediate parent, so a raw multi-commit fast-forward push of this branch makes it examine one of 157 commits, resolve `changed=false`, skip the entire apply block, and report the job green having applied nothing (06-RESEARCH.md Pitfall 2)."
    - "a green CI badge → the actual production schema: `.github/workflows/test-rls.yml` contains a near-identical `supabase link` / `db push` block aimed at a *different* project ref, so only a direct read against `slkobhavpwsubnsmuhya` distinguishes 'migrations applied to production' from 'migrations applied to the test project' (Pitfall 5)."
    - "the secrets precondition → every downstream step in the phase: if `SUPABASE_PROJECT_ID` or `SUPABASE_ACCESS_TOKEN` is absent or stale, `migration repair`'s trailing `|| true` swallows the failure and the job can still look green (06-RESEARCH.md assumption A3)."
    - "the merge landing → every other plan in this phase: nothing in plans 06-02, 06-03 or 06-04 has any meaning until the code and schema are actually live, which is why this is wave 1 and blocking."
  prohibitions:
    - "Do not run any of this plan's real commands from the executing session. No session in this milestone has held production credentials (D-01); a result that appears to have been produced here is fabricated, not verified."
    - "Do not edit, rename, or reorder any file under `supabase/migrations/`. The Supabase CLI compares each file against the recorded version in `supabase_migrations.schema_migrations`; editing an already-applied file is how a clean apply turns into a repair loop."
    - "Do not treat a green CI badge, or a green `test-rls.yml` run, as proof that production migrations applied. Only a direct query against the production project ref proves it."
    - "Do not land the branch on `main` as a raw multi-commit fast-forward push."
    - "Do not paste any secret value into the go-live log or a summary. The log records command names and result values only."
---

<objective>
Prove the deploy pipeline itself works end to end, on the smallest observable slice, before any of
this phase's remaining verification is written around it.

Purpose: `06-RESEARCH.md`'s load-bearing finding is that this milestone's entire branch has never
been merged. Verified again at plan time: `git rev-list --count main..HEAD` is 157 and
`git rev-list --count HEAD..main` is 0, and `git diff main --name-status -- supabase/migrations/`
reports all four migration files as additions. Production therefore holds only Phase 1's schema, and
it got there out of band during Phase 1's own testing — not through this repo's CI. Every remaining
activation check in this phase reads production state, so every one of them is meaningless until the
merge lands and the pipeline is proven to have actually applied something. Proving that on one
migration artifact and one deploy artifact costs one PR; discovering it failed after writing the rest
of the runbook costs the launch window.

Output: `scripts/founder-offer-go-live/RUNBOOK.md` sections 0 through 3, a blocking human execution
of them, and `06-GO-LIVE-LOG.md` holding the literal evidence that both ends of the pipeline moved.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/STATE.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-CONTEXT.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-RESEARCH.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md
@scripts/purge-test-accounts/RUNBOOK.md
@.github/workflows/ci.yml
</context>

<assumption_delta_decision>
**Scanner signal:** `detected: true`, `kind: "chosen"`, matched on the word "chosen" in ROADMAP
Phase 6 success criterion 3 — "The founder's **chosen** reveal-threshold state (static offer vs.
decreasing count)".

**Decision: no-change.**

Rationale: this is a false-positive trigger on prose phrasing, not an identity-model shift. The
reveal threshold has been a configurable `app_config` parameter since Phase 1 (FOND-06,
`supabase/migrations/20260812_waitlist_founder_offer.sql:170`), and `06-CONTEXT.md` D-05 already
resolved the only live question — keep `waitlist_reveal_threshold` at 30, no config change. Nothing
is promoted, nothing is demoted, and the threshold value is not re-opened. Criterion 3 is satisfied
by a confirm-not-assume read, planned as T-06-06 in plan 06-02.
</assumption_delta_decision>

<probe_disclosure>
**No edge-probe / prohibition-probe run — phase owns no net-new requirement IDs.**

Recorded plainly rather than silently skipped. Phase 6's `phase_req_ids` is descriptive prose
("None net-new — verifies the activation conditions of CRED-01, CRED-05, LEGAL-05…"), not a list of
requirement IDs owned by this phase with their own REQUIREMENTS.md text. There is no
phase-local SPEC.md and none was generated. Running the deterministic edge-probe engine against
that prose would be a category error — there is no requirement text unique to this phase to probe.

`must_haves.truths` across all four plans are therefore derived directly from ROADMAP.md's four
literal Phase 6 success criteria, each of which is already a precise, verifiable predicate.
</probe_disclosure>

<source_audit>
## Multi-Source Coverage Audit (phase-level, covers all four plans)

**GOAL** — ROADMAP Phase 6 goal ("safely and legally activated in production only once every upstream
gate is confirmed satisfied") and its four success criteria:

| Item | Covered by | Status |
|---|---|---|
| Criterion 1 — counter reflects only genuine post-launch signups (truncate + sequence reset before public) | 06-03 (T-06-07, T-06-08, T-06-09) | COVERED |
| Criterion 2 — flag flipped only after CGU/CGV are live | 06-03 (T-06-07, T-06-09) | COVERED |
| Criterion 3 — reveal-threshold state matches what was intended | 06-02 (T-06-04, T-06-05, T-06-06) | COVERED |
| Criterion 4 — entry points route real traffic, conversions record from first live visit | 06-04 (T-06-10, T-06-11, T-06-12) | COVERED |

**REQ** — requirement IDs whose production-activation state this phase verifies (none net-new; all
already Complete under their owning phases per REQUIREMENTS.md Traceability):

| ID | Covered by | Status |
|---|---|---|
| CRED-01 (production premium count re-audit) | 06-02 (T-06-05) | COVERED |
| CRED-05 (flag-gated credit behavior, flipped deliberately) | 06-01 (deploy of the flag row), 06-03 (T-06-09 flip) | COVERED |
| LEGAL-05 (legal text live before/with the code change) | 06-03 (T-06-07, T-06-09 four-URL gate) | COVERED |
| PURGE-01–05 (test-account purge complete before the counter is trusted) | 06-02 (T-06-04, T-06-05) | COVERED |
| FOND-06 (reveal threshold configurable without redeploy) | 06-02 (value confirmed), 06-04 (Phase 5's carried-forward live-reveal gap) | COVERED |

**RESEARCH** — `06-RESEARCH.md` patterns, pitfalls and open questions:

| Item | Covered by | Status |
|---|---|---|
| Pattern 1 merge-first (branch never merged) | 06-01 T-06-01, T-06-02 | COVERED |
| Pattern 2 CI's migrate-supabase mechanism | 06-01 T-06-01, T-06-02 | COVERED |
| Pattern 3 post-deploy confirmation query set | 06-01 (one artifact, tracer), 06-02 (all three) | COVERED |
| Pattern 4 purge re-verification via Phase 2's toolkit | 06-02 T-06-04, T-06-05 | COVERED |
| Pattern 5 smoke test + cleanup pair | 06-03 T-06-07, T-06-09 | COVERED |
| Pattern 6 CGU/CGV liveness gate | 06-03 T-06-07, T-06-09 | COVERED |
| Pattern 7 the flag flip | 06-03 T-06-07, T-06-09 | COVERED |
| Pattern 8 entry-point / conversion spot-check | 06-04 T-06-10, T-06-11 | COVERED |
| Pitfall 1 (merge assumed), 2 (`HEAD~1` blind spot), 5 (wrong project) | 06-01 T-06-01, T-06-02 | COVERED |
| Pitfall 3 (JSONB boolean vs string on the flip) | 06-03 T-06-07, T-06-09 | COVERED |
| Pitfall 4 (no CLI/credentials in any session) | every plan — `autonomous: false`, blocking checkpoints | COVERED |
| Open Question 1 (are the two GitHub secrets configured?) | 06-01 T-06-01 section 0, T-06-02 step 1 | COVERED |
| Open Question 2 (merge mechanism) | 06-01 T-06-01 section 1, T-06-02 step 2 | COVERED |
| Assumption A1 (production domain unconfirmed) | 06-01 T-06-01 section 0 records it as an operator-confirmed precondition | COVERED |
| Rollback note (discretionary) | 06-04 T-06-10 section 11 — kept to a short note, no scope expansion | COVERED |

**CONTEXT** — `06-CONTEXT.md` locked decisions:

| ID | Covered by | Status |
|---|---|---|
| D-01 rehearse-then-human-fires | every plan; all four are `autonomous: false` with blocking checkpoints | COVERED |
| D-02 merge + CI apply + Vercel deploy in scope | 06-01 (all three tasks), 06-02 T-06-04 | COVERED |
| D-03 purge dry-run re-run live | 06-02 T-06-04, T-06-05 | COVERED |
| D-04 smoke test + `TRUNCATE` + sequence reset | 06-03 T-06-07, T-06-08, T-06-09 | COVERED |
| D-05 threshold stays 30, confirm not assume | 06-02 T-06-04, T-06-05, T-06-06 | COVERED |
| D-06 flag flips only after CGU/CGV confirmed live | 06-03 T-06-07, T-06-09 | COVERED |

**Excluded (not gaps):** the rollback/kill-switch procedure beyond a short note, the pre-existing
`anon`-executable RPC gap, and the founder-to-`tier='premium'` redemption flow are all Deferred
Ideas in `06-CONTEXT.md`. No item is MISSING.
</source_audit>

<artifacts_this_phase_produces>
This phase ships zero application code and zero UI. It produces exactly these files:

| File | Created/modified by | What it is |
|---|---|---|
| `scripts/founder-offer-go-live/RUNBOOK.md` | 06-01 (§0–§3), 06-02 (§4–§6), 06-03 (§7–§9), 06-04 (§10–§11) | The ordered go-live procedure, written for a human operator with no memory of this planning discussion — mirrors `scripts/purge-test-accounts/RUNBOOK.md`'s audience, tone and structure |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` | 06-01 (created), 06-02, 06-03, 06-04 (appended) | The dated evidence ledger — one row per RUNBOOK step holding the literal output the operator read back |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md` | 06-04 (completed) | The per-criterion verification map, signed off once all four ROADMAP criteria are green |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-0N-SUMMARY.md` | each plan | Standard per-plan summary |

No source file, no migration, no route, no test file is created or modified by this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>T-06-01: Write the merge-and-deploy slice of the RUNBOOK and open the evidence ledger</name>
  <files>scripts/founder-offer-go-live/RUNBOOK.md, .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md</files>
  <read_first>
    - `scripts/purge-test-accounts/RUNBOOK.md` — the whole file. This is the structural and tonal precedent: a numbered-section document addressed to a person who has not read any planning artifact, where each step names its literal command and what to read in the output. Copy that shape, not its content.
    - `.github/workflows/ci.yml` lines 1-80 — the `push: branches: [main]` trigger, the `verify` job the migration job depends on, the `Check for migration changes` step's `git diff --name-only HEAD~1 HEAD` heuristic, and the `Push migrations` step's `supabase link` / `migration repair --status applied` / `db push --include-all` sequence with its `SUPABASE_PROJECT_ID` and `SUPABASE_ACCESS_TOKEN` secret references.
    - `.github/workflows/test-rls.yml` — enough of it to see that it carries a near-identical migration-apply block aimed at a separate test project, which is the confusion the RUNBOOK must warn against.
    - `06-RESEARCH.md` Patterns 1, 2 and 3, Pitfalls 1, 2, 4 and 5, the Assumptions Log, the Open Questions, and the Environment Availability table — these supply every literal command, project ref and failure mode this section set encodes.
    - `06-CONTEXT.md` D-01 and D-02 — the execution model and the deploy scope this section set implements.
    - `supabase/migrations/20260816_premium_credit_flag.sql` line 44 — the `app_config` row this plan's tracer confirmation reads back.
    - `CLAUDE.md` — the production API host and the note that Vercel deploys API and web via the GitHub integration.
  </read_first>
  <action>
Create `scripts/founder-offer-go-live/RUNBOOK.md`. Open it with an audience line in the same register
as the purge runbook's: a person who has not read any planning document and is about to change
production. State up front that this document is the whole procedure, that its section order is a
safety property rather than a convenience, and that sections 4 through 11 are added by later plans in
this phase — so an operator who finds them missing must not improvise them.

Write section 0, Preconditions. It records four things the operator confirms before touching
anything, each with its literal check and the fallback if it fails. First, that they hold production
credentials — `SUPABASE_ACCESS_TOKEN` for the CLI path and `SUPABASE_SERVICE_ROLE_KEY` for the direct
query path — and that no Claude session in this milestone has ever held either, so nothing in this
document was rehearsed against the real thing (D-01). Second, that the two GitHub Actions secrets
`SUPABASE_PROJECT_ID` and `SUPABASE_ACCESS_TOKEN` are actually configured on the repository, checked
with `gh secret list --repo AnatholyB1/ziko-platform` or the repository Settings page; record that
their absence is not hypothetical — `STATE.md` already carries an unresolved analogue for
`test-rls.yml`'s own secret — and that the fallback if either is missing is to apply the three pending
migration files by hand through the Supabase SQL Editor rather than to merge and hope. Third, that
the production project ref is `slkobhavpwsubnsmuhya` and every verification query in this document is
run against that ref specifically, never against whichever project a terminal happens to be linked
to. Fourth, that the operator confirms the real production web domain from `apps/web/.env.local`'s
`NEXT_PUBLIC_SITE_URL` before running any URL check later in this document — the research could not
read that env value, so the domain used in the examples is an unconfirmed convenience.

Write section 1, Merge the milestone branch to `main`. State the branch state as of planning:
`git rev-list --count main..HEAD` is 157 and `git rev-list --count HEAD..main` is 0, and
`git diff main --name-status -- supabase/migrations/` shows all four migration files as additions —
then instruct the operator to re-run those three commands themselves rather than trust the numbers,
because the branch moves. Instruct them to open a pull request from the milestone branch to `main`
and to land it as a single merge commit or a single squash commit. Explain why in one paragraph, not
as a preference: CI's migration detector diffs only the top commit against its immediate parent, and
the checkout uses a shallow fetch depth that reaches exactly that far, so a raw multi-commit
fast-forward push makes the detector examine one commit out of the whole push, resolve its output to
false, skip the entire apply block behind its guard, and finish green having applied nothing.

Write section 2, Confirm CI applied the migrations. Instruct the operator to open the Actions run
triggered by the merge push on `main` — not a pull-request run, since the migration job's condition
restricts it to push events on that ref — and to expand the `Apply Supabase migrations` job. Tell
them what a real success looks like versus a hollow one: the `Setup Supabase CLI` and `Push
migrations` steps must show as executed, not skipped; a skipped pair means the change detector
resolved false and nothing was applied regardless of the job's colour. Then state the rule this whole
document turns on: the badge is not the evidence. Point forward to the confirmation query in the next
section and note that `test-rls.yml` runs a near-identical apply block against a different project,
so a green migration workflow somewhere is not a green migration on production.

Write section 3, Confirm the pipeline moved, on one artifact at each end. This is the tracer
confirmation and it is deliberately narrow — two checks, one per end of the pipeline, both fired
immediately after section 2. For the database end, run `SELECT key, value FROM public.app_config
ORDER BY key;` against production and read whether a `premium_credit_cap_enabled` row is present at
all; state that before this merge production held only `waitlist_reveal_threshold`, so this row's
existence is unambiguous proof that a previously-unapplied migration landed. For the deploy end, run
`curl -s https://ziko-api-lilac.vercel.app/health` and confirm HTTP 200, then confirm in the Vercel
dashboard that the production deployment's commit matches the merge commit SHA — note explicitly that
a 200 alone only proves the API is up, and the SHA match is what proves Phase 4's credit-gate code is
the code now running. Close the section by stating that the full three-migration confirmation, the
purge re-check, the smoke test, the legal gate and the flag flip all live in later sections and must
not be attempted from memory if those sections are not yet present in this file.

Add a short standing note near the top of the file that no secret value is ever written into this
runbook, into the evidence log, or into a chat transcript when results are pasted back — only command
names and result values.

Then create `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md`.
Give it front matter with `phase: 06`, `slug: founder-offer-go-live`, `status: open`, and a
`started:` date. Under a heading, put a table with columns for the RUNBOOK section, the check, the
expected result, the literal observed result, the operator, and the timestamp — with one seeded row
per section 0 through 3 check, each marked pending. Below the table, add an Escalations heading
holding nothing yet, and a short note that a step which could not be run is written down as a named
gap with its reason, never left blank and never marked green — the same discipline
`05-06-SUMMARY.md` applied to Phase 5's two disclosed gaps.

Commit both files together.
  </action>
  <verify>
    <automated>test -f scripts/founder-offer-go-live/RUNBOOK.md &amp;&amp; test -f .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md &amp;&amp; for s in 'slkobhavpwsubnsmuhya' 'SUPABASE_PROJECT_ID' 'SUPABASE_ACCESS_TOKEN' 'migrate-supabase' 'squash' 'premium_credit_cap_enabled' 'app_config' 'ziko-api-lilac.vercel.app' 'NEXT_PUBLIC_SITE_URL' 'test-rls.yml'; do grep -qF "$s" scripts/founder-offer-go-live/RUNBOOK.md || { echo "MISSING: $s"; exit 1; }; done; grep -qE '^status: open$' .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/founder-offer-go-live/RUNBOOK.md` exists and contains four numbered sections 0 through 3, in that order, each ending with a stated expected output the operator reads before moving on.
    - The command in `<verify>` exits 0: every one of the ten grounding literals above is present in the runbook, and the log file carries `status: open`.
    - Section 0 names all four preconditions — production credentials, the two GitHub Actions secret names with the `gh secret list --repo AnatholyB1/ziko-platform` check, the production project ref, and the operator-confirmed site domain — and states the manual SQL Editor fallback for a missing secret.
    - Section 1 instructs a single merge or squash commit and gives the one-paragraph reason grounded in the change detector's single-commit diff, not as a style preference.
    - Section 2 distinguishes an executed `Push migrations` step from a skipped one and states that the job's colour is not the evidence.
    - Section 3 contains exactly two confirmations — the `app_config` read against the production ref, and the `/health` 200 plus a deployed-SHA match against the merge commit — and says why a bare 200 is insufficient.
    - `06-GO-LIVE-LOG.md` has a table whose rows cover every check in sections 0 through 3, all marked pending, plus an Escalations heading and the named-gap discipline note.
    - `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty — this task changed no source, schema or application file.
  </acceptance_criteria>
  <done>The merge-and-deploy slice of the runbook exists, reads as an executable procedure rather than a description of one, and the evidence ledger is open with a pending row for every check it declares. Both committed.</done>
  <reversibility rating="reversible">Two new documentation files; nothing executed, nothing deployed.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-06-02: [BLOCKING] Merge to main, let CI apply the migrations, and confirm both ends of the pipeline moved</name>
  <precondition>The operator holds production `SUPABASE_ACCESS_TOKEN` (or Supabase SQL Editor access to project `slkobhavpwsubnsmuhya`) and write access to `main` on `AnatholyB1/ziko-platform`. No session in this milestone has held either — `06-RESEARCH.md` Environment Availability confirms both as unavailable to any Claude session, which is why this step is a human checkpoint rather than an automated task.</precondition>
  <read_first>
    - `scripts/founder-offer-go-live/RUNBOOK.md` sections 0 through 3 — the document created by T-06-01 is the authority for this step; follow it, do not improvise from this checkpoint's summary.
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` — the rows you will be filling.
  </read_first>
  <what-built>
`scripts/founder-offer-go-live/RUNBOOK.md` sections 0 through 3: the preconditions, the merge, the CI
migration-apply confirmation, and the two-artifact proof that the pipeline actually moved.

Nothing has been executed. This milestone's branch has never been merged — verified at plan time,
157 commits ahead of `main` and 0 behind, with all four migration files showing as additions.
Production currently holds only Phase 1's schema, and it got there out of band during Phase 1's own
testing rather than through this repository's CI. Phase 3's `20260815_waitlist_retention_config`
and Phase 4's `20260816_premium_credit_flag` and `20260816_premium_credit_grant` have never been
applied, and the live Vercel-deployed API still runs the old unconditional premium bypass.

This is the phase's blocking gate. Every remaining check in Phase 6 reads production state, so none
of them means anything until this step lands.
  </what-built>
  <how-to-verify>
Work through `scripts/founder-offer-go-live/RUNBOOK.md` sections 0 through 3 in order, and copy each
literal result back here as you go.

**1 — Section 0, preconditions.** Confirm you hold production credentials. Run
`gh secret list --repo AnatholyB1/ziko-platform` (or open the repository Settings, Secrets and
variables, Actions) and confirm both `SUPABASE_PROJECT_ID` and `SUPABASE_ACCESS_TOKEN` are listed.
If either is missing, stop here and say so — the fallback is a manual apply through the Supabase SQL
Editor, and that is a different path with different evidence, not a footnote to this one. Confirm the
production project ref is `slkobhavpwsubnsmuhya` and read your real site domain out of
`apps/web/.env.local`'s `NEXT_PUBLIC_SITE_URL`; the research could not read that value, so say what
it actually is.

**2 — Section 1, the merge.** Re-run `git rev-list --count main..HEAD`,
`git rev-list --count HEAD..main` and `git diff main --name-status -- supabase/migrations/` yourself
and report the three outputs — the plan-time numbers were 157, 0, and four added files, and they will
have moved. Open a pull request from the milestone branch to `main` and land it as a **single merge
commit or a single squash commit**. Do not fast-forward-push the raw commit series: CI's migration
detector diffs only the top commit against its immediate parent, so a multi-commit push makes it look
at one commit, decide nothing changed, skip the whole apply block, and finish green having applied
nothing. Report the PR number and the merge commit SHA.

**3 — Section 2, the CI run.** Open the Actions run triggered by the merge push on `main` — not a
pull-request run; the migration job only fires on a push to that ref. Expand the
`Apply Supabase migrations` job and report, for each of `Check for migration changes`,
`Setup Supabase CLI` and `Push migrations`, whether it ran or was skipped. A green job whose last two
steps are skipped applied nothing. Paste the tail of the `Push migrations` step's log.

**4 — Section 3, the database end.** Against project `slkobhavpwsubnsmuhya` specifically, run
`SELECT key, value FROM public.app_config ORDER BY key;` and paste the full result. Before this merge
that table held a single row, `waitlist_reveal_threshold`. A `premium_credit_cap_enabled` row now
being present is the proof that a previously-unapplied migration landed. If the result is still one
row, the apply did not happen — say so and stop; do not retry blind.

**5 — Section 3, the deploy end.** Run `curl -s -o /dev/null -w '%{http_code}' https://ziko-api-lilac.vercel.app/health`
and report the status code. Then open the Vercel dashboard and report the commit SHA of the current
production deployment for both the API and the web project. A 200 on its own only says the API is
up; the SHA matching your merge commit from step 2 is what proves Phase 4's credit-gate code is what
is now running.

Approve only if steps 4 and 5 both produced their expected result. If anything failed, say which step
and paste what you actually saw — a partial or skipped apply is escalated, never worked around.
  </how-to-verify>
  <acceptance_criteria>
    - `git rev-list --count HEAD..main` reported by the operator, and the PR number plus merge commit SHA recorded.
    - Both `SUPABASE_PROJECT_ID` and `SUPABASE_ACCESS_TOKEN` reported present, or the manual SQL Editor fallback explicitly chosen on the record.
    - The `Apply Supabase migrations` job's `Setup Supabase CLI` and `Push migrations` steps are both reported as executed, not skipped.
    - `SELECT key, value FROM public.app_config ORDER BY key;` against `slkobhavpwsubnsmuhya` returns a row whose key is `premium_credit_cap_enabled`.
    - `curl -s -o /dev/null -w '%{http_code}' https://ziko-api-lilac.vercel.app/health` returns `200`, and the reported production deployment SHA equals the merge commit SHA.
    - Every result above is supplied as literal pasted output, not as a summary judgement that it worked.
  </acceptance_criteria>
  <resume-signal>Paste the results of steps 1 through 5, then type "approved" — or name the step that failed and paste what you saw.</resume-signal>
</task>

<task type="auto">
  <name>T-06-03: Record the tracer evidence and gate the phase on it</name>
  <files>.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md</files>
  <precondition>T-06-02's checkpoint has been answered with literal pasted output. If it was approved without results — a bare approval with no `app_config` result and no deployment SHA — halt: an evidence ledger filled from an assumption is worse than an empty one, and every later plan in this phase reads these rows as settled fact.</precondition>
  <read_first>
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` — the table seeded by T-06-01, whose pending rows you are filling.
    - The operator's pasted output from T-06-02 — the only source for these values. Do not re-derive, infer, or round any of them.
    - `.planning/workstreams/lien-invite/phases/02-test-account-purge/02-04-SUMMARY.md` — the written, dated confirmation pattern this ledger follows.
    - `.planning/workstreams/lien-invite/STATE.md` "Blockers/Concerns" — the two Phase 5 gaps carried into this phase, which must not be quietly closed by this plan.
  </read_first>
  <action>
Transcribe the operator's pasted output from T-06-02 into `06-GO-LIVE-LOG.md`, one row per seeded
check. Write the observed value verbatim: the three git counts, the PR number, the merge commit SHA,
the presence or absence of each of the two GitHub Actions secrets, the site domain the operator read
out of their environment, the run-or-skip state of each of the three CI steps, the full `app_config`
result set, the health status code, and both deployment SHAs. Where the operator reported a value the
plan expected to differ — the git counts in particular, which will have moved past 157 — record what
they reported, not what this plan predicted.

Then apply the gate. If the `app_config` result does not contain a `premium_credit_cap_enabled` row,
or the reported production deployment SHA does not match the merge commit, do not mark the tracer
green: write the failure into the Escalations section with the literal evidence, set the log's front
matter `status` to `blocked`, and halt the phase rather than proceeding to plan 06-02. Distinguish
the two failure shapes in what you write, because they have different remedies — an unchanged
`app_config` means the apply did not run (check whether the CI steps were skipped, which points at
the change detector and the merge strategy), while a mismatched deployment SHA means the migration
landed but the code did not, which points at the Vercel integration rather than at CI.

If both confirmations passed, set the tracer rows to their observed values, leave the log's `status`
as `open` — the phase is not finished, only unblocked — and add a one-line note recording that
sections 4 through 11 of the runbook do not yet exist and that plans 06-02 through 06-04 add them.

Also record, in the Escalations section as an inherited open item rather than a new failure, the two
verification gaps Phase 5 disclosed and carried forward per `STATE.md`: a real submission's
success and duplicate confirmation states were never observed live, and FOND-06's live counter-reveal
was never observed. Note that plan 06-04 is where they are either closed or restated as still open.

Update `06-VALIDATION.md`'s per-criterion map only insofar as its status column for rows this plan
touched — do not complete its sign-off, which belongs to plan 06-04.

Commit the log on its own.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md';const t=fs.readFileSync(p,'utf8');const body=t.split('\n').filter(l=>!l.trim().startsWith('#')).join('\n');const st=(t.match(/^status:\s*(\S+)/m)||[])[1];if(!['open','blocked'].includes(st)){console.error('bad status: '+st);process.exit(1)}if(/pending/i.test(body.split('## Escalations')[0])){console.error('a seeded row is still pending');process.exit(1)}for(const s of ['app_config','/health','Escalations']){if(!t.includes(s)){console.error('MISSING: '+s);process.exit(1)}}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - No row seeded by T-06-01 for sections 0 through 3 is still marked pending; each holds the operator's literal reported value.
    - The `app_config` result set is recorded verbatim, including the number of rows returned.
    - The merge commit SHA and both reported production deployment SHAs are recorded, and the log states explicitly whether they match.
    - If either tracer confirmation failed, the log's front matter reads `status: blocked`, the Escalations section names the failure with its evidence, and the two failure shapes are distinguished by remedy; otherwise `status: open`.
    - The Escalations section carries the two inherited Phase 5 gaps as open items pointing at plan 06-04.
    - `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty — this task changed no source, schema or application file.
  </acceptance_criteria>
  <done>The tracer's evidence is on disk in the operator's own words, the phase is explicitly either unblocked or halted on that evidence, and the two gaps Phase 5 handed forward are carried visibly rather than lost. Committed.</done>
  <reversibility rating="reversible">A documentation update recording results that already happened.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature branch → `main` | 157 commits of unreviewed-against-`main` work cross into the branch that auto-applies migrations to production. |
| GitHub Actions runner → production Supabase | `ci.yml`'s `migrate-supabase` job holds `SUPABASE_ACCESS_TOKEN` and writes DDL to `slkobhavpwsubnsmuhya`. This is the only automated write path to production schema in the repository. |
| GitHub push event → Vercel | The GitHub integration redeploys `apps/web` and `backend/api` with no gate of its own beyond the push. |
| human operator → planning record | Results are pasted back by a person; the ledger's truthfulness is a process control, not a technical one. |

## STRIDE Threat Register

Threat IDs use the `T-06-S<n>` namespace to stay out of the `T-06-<nn>` task-ID namespace, matching
`04-VALIDATION.md`'s convention in this workstream.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-S1 | Tampering | `supabase db push --include-all` landing one migration file and failing the next, leaving production in a mixed schema | high | mitigate | The tracer confirms one artifact here; plan 06-02's T-06-05 checks all three artifacts independently rather than as one aggregate signal, and halts on any single miss instead of proceeding. |
| T-06-S2 | Tampering | `ci.yml`'s `git diff --name-only HEAD~1 HEAD` change detector resolving false on a multi-commit push, skipping the apply block while the job reports success | high | mitigate | RUNBOOK section 1 constrains the merge to a single merge or squash commit with the reason stated; section 2 requires reading the run-or-skip state of `Setup Supabase CLI` and `Push migrations`; section 3's direct `app_config` read is required regardless of job colour. |
| T-06-S3 | Spoofing | A verification query answered by the test project rather than production, because `test-rls.yml` runs a near-identical apply block against `SUPABASE_TEST_PROJECT_ID` | high | mitigate | Every verification step in this runbook names project ref `slkobhavpwsubnsmuhya` explicitly, and section 0 makes confirming the connection target a precondition rather than an assumption. |
| T-06-S6 | Information Disclosure | A production `SUPABASE_SERVICE_ROLE_KEY` or `SUPABASE_ACCESS_TOKEN` value pasted into the runbook, the evidence log, or a chat transcript when results are returned | medium | mitigate | A standing note near the top of the runbook, and the ledger's own column set, record command names and result values only — never env values. Secret *presence* is confirmed via `gh secret list`, which by GitHub's design never prints a value. |
| T-06-S7 | Repudiation | The evidence ledger recording a step as green that was never fired | high | mitigate | T-06-03 carries a precondition that halts on a bare approval with no pasted output, and its verify fails while any seeded row is still pending — the ledger cannot be completed by assertion. |
| T-06-S8 | Elevation of Privilege | The pre-existing `anon`-executable `is_coach_of()` / `redeem_invitation_code()` / `peek_invitation()` gap coexisting in production with the RPCs this merge newly deploys | high | transfer | Transferred to a future, separate security-response phase, per `STATE.md`'s own note and `06-CONTEXT.md`'s Deferred Ideas. Not remediated here. The migrations this merge deploys each use the three-role `REVOKE EXECUTE … FROM PUBLIC, anon, authenticated` form (verified in all four files), so this merge does not reproduce the gap on any new function — it only declines to fix the existing one. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs zero packages — `06-RESEARCH.md`'s Package Legitimacy Audit records the gate as not applicable because no package-manager install step exists anywhere in this phase's scope. Any install command appearing during execution is out of contract and halts the task. |
</threat_model>

<verification>
- `scripts/founder-offer-go-live/RUNBOOK.md` exists with sections 0 through 3 and passes T-06-01's literal-presence check.
- `06-GO-LIVE-LOG.md` exists, has no pending row for sections 0 through 3, and its status is `open` (unblocked) or `blocked` (halted on evidence).
- The production `app_config` table returns a `premium_credit_cap_enabled` row when queried against project ref `slkobhavpwsubnsmuhya`.
- The production API deployment SHA equals the merge commit SHA.
- `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty across all three tasks.
</verification>

<success_criteria>
- The milestone branch is on `main`, landed as a single merge or squash commit.
- CI's `migrate-supabase` job ran on that push and its apply steps executed rather than being skipped.
- One migration artifact and one deploy artifact are both independently confirmed present in production, by direct read rather than by badge.
- Every real command was fired by a human operator, and their literal output is on disk in `06-GO-LIVE-LOG.md`.
- Plans 06-02, 06-03 and 06-04 are unblocked, or the phase is explicitly halted with the failure written down.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-01-SUMMARY.md` when done.
</output>
