---
phase: 06-founder-offer-go-live
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - scripts/founder-offer-go-live/RUNBOOK.md
  - .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md
  - .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md
autonomous: false
# Phase 6 owns no net-new requirement. The IDs below are the ones this plan
# *verifies the production-activation state of*, per ROADMAP Phase 6 and
# REQUIREMENTS.md's Traceability table — not requirements this phase introduces.
requirements: [ENTRY-01, ENTRY-02, ENTRY-03, ENTRY-04, ENTRY-06, FOND-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real browser reaches `/fondateurs` from all four mapped entry points — the homepage founders section, both `/coachs` calls to action, the header link and the footer link — in both locales, on the production domain rather than a dev server (ROADMAP criterion 4, ENTRY-01 through ENTRY-04)."
    - "The `/coachs` calls to action land on the founders page with the coach profile already selected, not on an unanswered role picker (ENTRY-02)."
    - statement: "A completed signup on the live page produces a `waitlist_signup` event visible in the production Vercel Analytics dashboard, confirming conversions record from the first live visit (ENTRY-06, ROADMAP criterion 4)."
      verification: backstop
    - "The two verification gaps Phase 5 disclosed and carried forward — a real submission's success and duplicate confirmation states, and FOND-06's live counter-reveal observation — are each either closed with real evidence or restated as still open with the reason, never silently dropped (STATE.md Blockers/Concerns, 05-06-SUMMARY.md)."
    - "`06-GO-LIVE-LOG.md` holds a dated, literal result line for every step of runbook sections 0 through 10, with any step that could not be run written down as a named gap rather than left blank or marked green."
    - "`06-VALIDATION.md`'s per-criterion map shows all four ROADMAP Phase 6 success criteria with the task ID that closed each and the evidence that closed it, and its sign-off block reflects what actually ran."
  artifacts:
    - "scripts/founder-offer-go-live/RUNBOOK.md (modified — sections 10 and 11 appended: entry-point and conversion spot-check, short rollback note)"
    - ".planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md (modified — activation evidence transcribed, ledger closed)"
    - ".planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md (modified — per-criterion map completed, sign-off recorded)"
  key_links:
    - "the go-live log → any later audit: this file is the only durable evidence that any of these production steps happened. Nothing in git, in CI, or in the database records who ran what and read back which value."
    - "the conversion check → the analytics mount: the tracking call fires client-side from the role form, so a deploy that shipped without the analytics component still lets every entry point work perfectly while the conversion silently never records — the click-through and the dashboard check are two different facts and neither implies the other."
    - "the `/coachs` calls to action → the role pre-selection parameter: the buttons import the parameter name rather than retyping it, so a broken pre-selection means a link was rewritten by hand, and the visitor lands on a picker they already answered."
    - "Phase 5's two carried-forward gaps → this phase's close: this is the last planned opportunity in the milestone to close them with real credentials, and letting them lapse silently is how a disclosed gap becomes an undocumented one."
  prohibitions:
    - "Do not mark a criterion green in `06-VALIDATION.md` on the strength of a step that was not actually fired. Phase 5 recorded its two gaps honestly and this phase inherits that discipline."
    - "Do not write production code in this phase. If the entry-point walkthrough finds a bug, it is reported with the file named and fixed in a plan that owns that file — repairing it here hides which slice was wrong."
    - "Do not expand the rollback note into a rollback procedure. It was explicitly a deferred idea in `06-CONTEXT.md`; a short note is the discretion granted, a full kill-switch design is scope this phase does not have."
    - "Do not close Phase 5's two carried-forward gaps by assertion. Either the evidence is pasted, or the gap is restated as open with the reason."
    - "Do not run any real command in this plan from the executing session. No session in this milestone holds production credentials (D-01)."
---

<objective>
Confirm the public surface actually works for a real visitor, then close the phase with an evidence
record that stands on its own.

Purpose: the offer is live as of plan 06-03. What has not been confirmed is the thing the whole
milestone was built to produce — that a person arriving from any of the four entry points reaches the
page, and that their signup is measurable. Those are two independent facts: the tracking call fires
client-side, so a deploy missing the analytics mount leaves every link working perfectly while no
conversion ever records. Neither is checkable from a repository. And this is the last planned point
in the milestone at which Phase 5's two disclosed gaps — a real submission's confirmation states, and
FOND-06's live counter reveal — can be closed with real credentials rather than lapsing quietly from
"disclosed and accepted" into "nobody remembers".

Output: `scripts/founder-offer-go-live/RUNBOOK.md` sections 10 and 11, a blocking human walkthrough of
the live site, a closed `06-GO-LIVE-LOG.md`, and a `06-VALIDATION.md` whose four criterion rows each
name the task that closed them and the evidence that did it.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/STATE.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-CONTEXT.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-RESEARCH.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-03-SUMMARY.md
@.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This phase ships zero application code and zero UI. It produces exactly these files:

| File | Created/modified by | What it is |
|---|---|---|
| `scripts/founder-offer-go-live/RUNBOOK.md` | 06-01 (§0–§3), 06-02 (§4–§6), 06-03 (§7–§9), **06-04 (§10–§11)** | The ordered go-live procedure, written for a human operator with no memory of this planning discussion |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` | 06-01 (created), 06-02, **06-04 (activation evidence transcribed, ledger closed)** | The dated evidence ledger — one row per RUNBOOK step holding the literal output the operator read back |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md` | **06-04 (completed)** | The per-criterion verification map, signed off once all four ROADMAP criteria are green |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-0N-SUMMARY.md` | each plan | Standard per-plan summary |

No source file, no migration, no route, no test file is created or modified by this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>T-06-10: Transcribe the activation evidence and write the public-surface sections of the RUNBOOK</name>
  <files>scripts/founder-offer-go-live/RUNBOOK.md, .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md</files>
  <precondition>T-06-09's checkpoint was answered with literal pasted output for all five of its steps — the pre-state count, the claim result row, the post-cleanup count with its wall-clock time, the four legal status lines, and the flag readback with its `jsonb_typeof` result. A bare approval means the ledger would be filled from expectation, which is the one thing this record exists to prevent: halt instead.</precondition>
  <read_first>
    - The operator's pasted output from T-06-09 — the only source for the activation rows.
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` — the table and the Escalations section as left by T-06-06, including the two inherited Phase 5 gaps.
    - `scripts/founder-offer-go-live/RUNBOOK.md` sections 0 through 9 — so sections 10 and 11 continue the numbering and register.
    - `apps/web/src/components/marketing/WaitlistRoleForm.tsx` lines 1-30 and around line 100 — the event-name constant and the tracking call, so section 10 names the event the operator is looking for in the dashboard rather than describing it.
    - `apps/web/src/app/sitemap.ts` — the founders entry, so section 10's indexability check names the real path.
    - `.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-06-SUMMARY.md` — the two gaps disclosed and accepted at that phase's human checkpoint, in their own words, so section 10 restates them accurately rather than from memory.
    - `.planning/workstreams/lien-invite/STATE.md` "Blockers/Concerns" — the same two gaps as recorded at workstream level.
    - `06-CONTEXT.md` Deferred Ideas — the rollback note's scope limit, so section 11 stays a note.
  </read_first>
  <action>
First, transcribe. Append rows to `06-GO-LIVE-LOG.md` for every check in runbook sections 7 through 9,
filled with the operator's literal output from T-06-09: the pre-smoke-test row count, the full claim
result row with all three returned columns, which cleanup mechanism T-06-08 selected and who selected
it, the post-cleanup count with its recorded wall-clock time, all four legal-page status lines, and
the flag value together with its reported JSONB type. Record the ordering fact explicitly as its own
line — that the four 200s were obtained before the flag update was issued, and that the cleanup ran
before any entry point was publicly linked. Those two orderings are what criteria 1 and 2 actually
assert, and they are only checkable later if the record states them rather than implying them from
row order.

Then write section 10, Confirm the public surface. Frame it as two independent facts rather than one
check, because they fail independently: whether a visitor arriving from each entry point reaches the
page, and whether their signup is recorded as a conversion. Give the four entry points as they exist —
the founders section on the homepage between the hero and the how-it-works block, both calls to
action on `/coachs`, the header link, and the footer link — and instruct the operator to click each
one on the production domain in a real browser, in both locales, and to confirm the two `/coachs`
routes land with the coach profile already selected rather than on an unanswered picker. Note that a
picker appearing unanswered from those two means a link was rewritten by hand instead of using the
shared parameter constant the buttons import, and that this is a bug to report with the file named,
not to work around. Add the sitemap and indexability check: confirm the founders path is present in
the served sitemap and that the page is not excluded from indexing.

For the conversion half, be explicit that it is not automatable and why: the tracking call fires from
the client on a completed submission, and it is visible only in the Vercel Analytics dashboard for the
production project — there is no repository-local or command-line path to it, so this is the one check
in the whole runbook confirmed by looking at an external dashboard. Name the event the form emits so
the operator is searching for a specific string rather than scanning. Add the failure mode worth
knowing: every link can work perfectly and the conversion still never record, if the deploy shipped
without the analytics component mounted — so a successful click-through says nothing about whether the
event fired.

Then, still in section 10, add the two carried-forward Phase 5 gaps as explicit named steps rather
than as prose. The first: observe a real submission's confirmation state, and then submit the same
address a second time and confirm the response is indistinguishable from the first — no wording that
reveals a prior registration. The second: FOND-06's live counter reveal, observed by changing the
configured reveal value and confirming the page's display changes with no redeploy, then restoring
the value. State clearly that these were disclosed and accepted at Phase 5's checkpoint for want of
credentials, that this is the last planned opportunity to close them, and that restoring the reveal
value afterwards is part of the step rather than a follow-up — the launch depends on it holding 30.

Write section 11, Rollback note. Keep it to a short note, not a procedure: the flag can be set back
with the same statement inverted and the readback repeated, which returns premium AI access to its
prior unconditional behaviour without a deploy; the founders page can be delinked from the four entry
points; and the one action in this runbook that cannot be undone is the cleanup in section 7. Say
plainly that a full kill-switch procedure was deliberately not designed for this phase and that
anything beyond the above is a decision for the project owner, not an improvisation at the terminal.

Commit the runbook and the log together.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const R='scripts/founder-offer-go-live/RUNBOOK.md';const L='.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md';const r=fs.readFileSync(R,'utf8');for(const s of ['waitlist_signup','/fondateurs','/coachs','sitemap','Vercel Analytics']){if(!r.includes(s)){console.error('MISSING in runbook: '+s);process.exit(1)}}for(const h of [/^#+ *10[.\s]/m,/^#+ *11[.\s]/m]){if(!h.test(r)){console.error('MISSING heading: '+h);process.exit(1)}}const l=fs.readFileSync(L,'utf8');for(const s of ['claim_waitlist_signup','jsonb_typeof','reset_waitlist_founder_sequence']){if(!l.includes(s)){console.error('MISSING in log: '+s);process.exit(1)}}const pre=l.split('## Escalations')[0];if(/pending/i.test(pre)){console.error('a ledger row is still pending');process.exit(1)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0: sections 10 and 11 exist as numbered headings, the runbook names the tracked event, both `/fondateurs` and `/coachs`, the sitemap and the analytics dashboard, and the log carries the three activation identifiers with no row still pending above Escalations.
    - Every check in runbook sections 7 through 9 has a ledger row holding the operator's literal output, including all three columns of the claim result and the reported JSONB type of the flag value.
    - The ledger states as its own explicit line that the four legal 200s preceded the flag update, and that the cleanup preceded any entry point being publicly linked.
    - The selected cleanup mechanism and the person who selected it are recorded.
    - Section 10 treats the click-through and the conversion recording as two independent facts and states the failure mode where every link works and no event fires.
    - Section 10 states that the conversion check is confirmed only in an external dashboard with no repository-local or command-line alternative.
    - Section 10 carries both Phase 5 carried-forward gaps as named steps, and requires the reveal value to be restored as part of the second one.
    - Section 11 is a short note that names the flag set-back, the delinking, and the cleanup as the one thing that cannot be undone — and states that no full kill-switch procedure was designed for this phase.
    - `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty — this task changed no source, schema or application file.
  </acceptance_criteria>
  <done>The activation is on the record in the operator's own words with its two ordering facts stated outright, and the runbook's last two sections are written — one an executable walkthrough of the public surface, the other a note that knows its own limits. Committed.</done>
  <reversibility rating="reversible">A documentation append and a transcription of results that already happened.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-06-11: [BLOCKING] Walk the live site from every entry point and confirm a conversion records</name>
  <precondition>The founders page is reachable on the production domain and the cleanup in T-06-09 has already run — this walkthrough creates real rows and must not be followed by any further cleanup. The operator can reach the Vercel Analytics dashboard for the production project; no Claude session can, which is why the conversion half of this check is a backstop verification rather than an automated one.</precondition>
  <read_first>
    - `scripts/founder-offer-go-live/RUNBOOK.md` section 10 — the authority for this step. Follow the document, not this checkpoint's summary.
    - `.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-06-SUMMARY.md` — the two gaps you are being asked to close, in the words they were disclosed in.
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` — where these results are recorded by T-06-12.
  </read_first>
  <what-built>
The founder offer is live. Migrations applied, code deployed, purge precondition confirmed fresh,
waitlist table cleared with the sequence reset to issue rank 1, CGU and CGV confirmed served in both
locales, and the capped credit behaviour enabled with its flag confirmed stored as a JSONB boolean.

`scripts/founder-offer-go-live/RUNBOOK.md` section 10 is the last verification step: whether a real
visitor arriving from each of the four mapped entry points actually reaches the page, and whether
their signup records as a conversion. Those two fail independently — the tracking call fires
client-side, so every link can work perfectly while nothing is ever recorded.

Section 10 also carries the two verification gaps Phase 5 disclosed and you accepted at its own
checkpoint: a real submission's confirmation states were never observed live, and FOND-06's counter
reveal was never observed changing without a redeploy. This is the last planned opportunity in the
milestone to close them with real credentials.

**This walkthrough creates real rows. No cleanup follows it — the table was cleared before the page
opened and it stays that way.**
  </what-built>
  <how-to-verify>
Work through `scripts/founder-offer-go-live/RUNBOOK.md` section 10, on the production domain in a real
browser. Report what you actually see at each step.

**1 — The four entry points, French.** From the homepage, scroll to the founders section between the
hero and the how-it-works block and click through. From `/coachs`, click the hero call to action, then
the footer one — both must land on the founders page with the **coach** profile already selected, not
on an unanswered picker. Then use the header link and the footer link from any page. Report which of
the five clicks landed correctly.

**2 — The four entry points, English.** Repeat at minimum the homepage section and the two `/coachs`
calls to action in English, and confirm the page you land on is fully English with no French string
left in the interface.

**3 — Sitemap and indexability.** Fetch the served sitemap on the production domain and confirm the
founders path is present. Confirm the page is not excluded from indexing.

**4 — A real submission, and a repeat of it.** Submit one address you control. Confirm the inline
confirmation replaces the form, in the right language, and does not echo your address back. Then
submit the **same** address again and confirm the response gives you no hint you were already
registered — no different wording, no missing rank that was there the first time. This closes the
first of Phase 5's two carried-forward gaps.

**5 — The conversion.** Open the Vercel Analytics dashboard for the production project and confirm a
`waitlist_signup` event appears for the submission you just made. Report whether you saw it and how
long after the submission. If no event appears, say so plainly — every link working tells you nothing
about whether the event fired, because the tracking call is client-side and a deploy missing the
analytics mount produces exactly this: a perfect click-through and no data.

**6 — FOND-06 observed, not assumed.** Raise `app_config.waitlist_reveal_threshold` high enough that
the counter reveals, reload the page, and confirm a real descending count appears with **no
redeploy**. Then restore the value to 30 and confirm the static offer sentence returns. Report both
observations and confirm the restore. This closes the second carried-forward gap, and restoring the
value is part of the step — the launch depends on it holding 30.

**7 — What you did not do.** If any step above could not be completed, say which and why. It will be
written into the record as a named open gap, which is what Phase 5 did with these same two items and
why they were still findable today.

Approve only if steps 1 through 4 behaved as described and steps 5 and 6 were either completed or
explicitly reported as not completed with the reason. If a link went to the wrong place or a state
rendered wrong, name the step and what you saw — the fix belongs in a plan that owns the file.
  </how-to-verify>
  <acceptance_criteria>
    - All five French entry-point clicks and at least three English ones are reported as landing on `/fondateurs`, with the two `/coachs` routes reported as arriving with the coach profile already selected.
    - The served production sitemap is confirmed to contain the founders path, and the page is confirmed not excluded from indexing.
    - A real submission's confirmation state is reported as observed, and a repeat submission of the same address is reported as producing a response indistinguishable from the first — closing Phase 5's first carried-forward gap, or explicitly reported as not completed with the reason.
    - A `waitlist_signup` event is reported as visible in the production Vercel Analytics dashboard for that submission, with the observed delay — or explicitly reported as not visible, which is a finding, not an approval.
    - The reveal threshold is reported as observed changing the page's display with no redeploy, and confirmed restored to 30 — closing Phase 5's second carried-forward gap, or explicitly reported as not completed with the reason.
    - Any step not completed is named with its reason rather than left unmentioned.
  </acceptance_criteria>
  <resume-signal>Report the results of steps 1 through 7, then type "approved" — or name the step that failed and what you saw.</resume-signal>
</task>

<task type="auto">
  <name>T-06-12: Close the evidence ledger and complete the validation record</name>
  <files>.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md, .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md</files>
  <precondition>T-06-11 was answered with per-step results, including an explicit statement for any step not completed. A bare approval leaves no basis for deciding whether criterion 4 is green or open, and guessing at it is exactly the failure this record exists to prevent — halt instead.</precondition>
  <read_first>
    - The operator's reported results from T-06-11 — the only source for criterion 4's rows and for the disposition of Phase 5's two carried-forward gaps.
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` — the ledger as left by T-06-10, including everything in Escalations.
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md` — the per-criterion map and the sign-off block you are completing; note its front matter fields `status`, `nyquist_compliant` and `wave_0_complete`.
    - `.planning/workstreams/lien-invite/phases/04-credit-gate-alignment/04-VALIDATION.md` — the completed-map and sign-off format this workstream uses, including the `plans_created` front-matter field and the status legend.
    - `.planning/workstreams/lien-invite/phases/05-waitlist-page-entry-points/05-06-SUMMARY.md` — the precedent for recording a gap as accepted-and-open rather than closing it by assertion.
    - `.planning/workstreams/lien-invite/ROADMAP.md` Phase 6 section — the four success criteria in their literal wording, which the validation map's rows must match.
  </read_first>
  <action>
Fill the remaining ledger rows in `06-GO-LIVE-LOG.md` from the operator's T-06-11 report: which entry
points landed correctly in each locale, the sitemap and indexability result, the submission and
repeat-submission observations, whether the conversion event appeared and after how long, and the
reveal-threshold observation together with its restoration. Then resolve Escalations. For each of the
two inherited Phase 5 gaps, either mark it closed with the evidence from T-06-11 quoted, or restate it
as still open with the operator's reason in their words — do not summarise a "not completed" into a
"completed with caveats". Add any new gap the walkthrough surfaced, with the file it points at named
if the operator named one.

Set the ledger's front matter `status` to `complete` if every step of runbook sections 0 through 10
has a recorded result and no halt condition fired, or to `complete-with-gaps` if any step is recorded
as an open gap. Add a closing dated line stating who ran the go-live and on what date.

Then complete `06-VALIDATION.md`. Its per-criterion map must have one row per ROADMAP Phase 6 success
criterion, in the criteria's own wording, each carrying the task ID that closed it and the literal
evidence: criterion 1 closed by T-06-09 with the post-cleanup count of 0 and its wall-clock time;
criterion 2 closed by T-06-09 with the four legal status lines and the flag's confirmed JSONB boolean
type; criterion 3 closed by T-06-05 with the observed threshold value; criterion 4 closed by T-06-11
with the entry-point results and the conversion-event observation. Mark criterion 4's conversion half
according to what the operator actually reported — if the event was not seen, the row is not green,
and the map says so.

Set the front matter `status` to `validated` if all four criteria are green, or leave it `ready` with
the open criterion named if not. Set `wave_0_complete` to true — the wave-0 equivalent for this phase
was writing the runbook itself, and it exists. For `nyquist_compliant`, set it true only if every
criterion in the map has a recorded verification result; where a row's verification is the external
analytics dashboard, record it as a backstop verification with that reason rather than as an
automated one, because this phase genuinely has no automatable path to it and claiming otherwise
would be the same dishonesty the ledger is designed against. Complete the Validation Sign-Off
checklist against what actually happened, and update the Manual-Only Verifications table so each row
names the task that resolved it.

Commit the ledger and the validation record together.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const L='.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md';const V='.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md';const l=fs.readFileSync(L,'utf8');const ls=(l.match(/^status:\s*(\S+)/m)||[])[1];if(!['complete','complete-with-gaps'].includes(ls)){console.error('ledger status not closed: '+ls);process.exit(1)}if(/pending/i.test(l.split('## Escalations')[0])){console.error('a ledger row is still pending');process.exit(1)}const v=fs.readFileSync(V,'utf8');const vs=(v.match(/^status:\s*(\S+)/m)||[])[1];if(!['validated','ready'].includes(vs)){console.error('validation status invalid: '+vs);process.exit(1)}if(!/^plans_created:/m.test(v)){console.error('plans_created missing');process.exit(1)}if(!/^wave_0_complete: true$/m.test(v)){console.error('wave_0_complete not true');process.exit(1)}for(const id of ['T-06-05','T-06-09','T-06-11']){if(!v.includes(id)){console.error('validation map does not cite '+id);process.exit(1)}}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Every step of runbook sections 0 through 10 has a recorded result in `06-GO-LIVE-LOG.md`; no row above Escalations is pending.
    - Each of Phase 5's two carried-forward gaps is resolved in Escalations as either closed with quoted evidence or still open with the operator's own stated reason — never converted from a not-completed into a completed-with-caveats.
    - The ledger's front matter is `complete` or `complete-with-gaps`, and it carries a closing dated line naming who ran the go-live.
    - `06-VALIDATION.md`'s map has one row per ROADMAP Phase 6 success criterion in the criteria's own wording, each citing the closing task ID and its literal evidence, and it cites T-06-05, T-06-09 and T-06-11.
    - Criterion 4's conversion row reflects what the operator reported; if the event was not observed, the row is not marked green.
    - `06-VALIDATION.md` front matter carries `plans_created`, `wave_0_complete: true`, and a `status` of `validated` (all four criteria green) or `ready` (with the open criterion named).
    - The row whose verification is the external analytics dashboard is recorded as a backstop verification with that reason, not as an automated one.
    - The Validation Sign-Off checklist and the Manual-Only Verifications table both reflect what actually ran, with each manual row naming its resolving task.
    - `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty — this task changed no source, schema or application file.
  </acceptance_criteria>
  <done>The go-live has a durable, dated record in the operator's own words; all four ROADMAP criteria are answered with the evidence and the task that produced it; and anything that did not run is written down as a named gap rather than absorbed into a green checkmark. Committed.</done>
  <reversibility rating="reversible">Two documentation updates recording results that already happened.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live public page → real visitor | The first point at which people outside the project interact with the offer; every prior check was operator-driven. |
| client-side tracking call → external analytics dashboard | The conversion evidence leaves the system entirely. No repository-local or command-line path to it exists, so its absence is invisible from inside. |
| operator's walkthrough report → the phase's permanent record | The validation map and the ledger are what a later audit reads as this phase's evidence. |
| a disclosed gap → an undocumented one | Phase 5's two carried-forward gaps lapse into invisibility if this plan closes them by assertion rather than by evidence. |

## STRIDE Threat Register

Threat IDs use the `T-06-S<n>` namespace to stay out of the `T-06-<nn>` task-ID namespace, matching
`04-VALIDATION.md`'s convention in this workstream.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-S7 | Repudiation | The validation record claiming coverage that was never produced — a criterion marked green on a step that did not run | high | mitigate | T-06-12 carries a precondition that halts on a bare approval; its verify rejects any pending ledger row and requires the map to cite T-06-05, T-06-09 and T-06-11; and the criterion-4 conversion row is required to reflect the operator's actual report rather than the expected one. |
| T-06-S14 | Repudiation | Conversions silently never recording while every entry point works, because the deploy shipped without the analytics mount | high | mitigate | Section 10 treats the click-through and the conversion as two independent facts and names the failure mode outright; the checkpoint requires the operator to report whether the event appeared and after how long, and an absent event is recorded as a finding rather than absorbed into an approval. |
| T-06-S15 | Tampering | The reveal threshold left changed after the FOND-06 observation, so the live page reveals its counter at the wrong point | high | mitigate | Restoring the value to 30 is written into section 10 as part of the step rather than as a follow-up, is an explicit acceptance criterion of T-06-11, and is a recorded ledger row in T-06-12. Phase 5's plan 05-06 already treated an unrestored value as a launch-day surprise for exactly this reason. |
| T-06-S16 | Repudiation | Phase 5's two carried-forward gaps quietly converted from not-completed into completed-with-caveats | medium | mitigate | T-06-12's action and acceptance criteria require each to be either closed with quoted evidence or restated as open in the operator's own words, and forbid the summarising conversion explicitly. |
| T-06-S8 | Elevation of Privilege | The pre-existing `anon`-executable `is_coach_of()` / `redeem_invitation_code()` / `peek_invitation()` gap in production, now coexisting with a publicly-launched marketing surface that increases traffic to the site | high | transfer | Transferred to a future, separate security-response phase per `STATE.md` and `06-CONTEXT.md`'s Deferred Ideas. Not remediated here. It should be carried forward into the milestone close-out rather than dropped when this phase's ledger closes; T-06-12's Escalations resolution keeps it visible. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs zero packages — `06-RESEARCH.md`'s Package Legitimacy Audit records the gate as not applicable because no package-manager install step exists anywhere in this phase's scope. Any install command appearing during execution is out of contract and halts the task. |
</threat_model>

<verification>
- `scripts/founder-offer-go-live/RUNBOOK.md` carries numbered sections 10 and 11 and passes T-06-10's literal-presence check.
- `06-GO-LIVE-LOG.md` has a recorded result for every step of sections 0 through 10, no pending row above Escalations, a `status` of `complete` or `complete-with-gaps`, and a closing dated line.
- `06-VALIDATION.md` has one row per ROADMAP Phase 6 success criterion citing its closing task ID and evidence, carries `plans_created` and `wave_0_complete: true`, and its `status` is `validated` or `ready` with the open criterion named.
- Both Phase 5 carried-forward gaps are resolved in Escalations as closed-with-evidence or open-with-reason.
- `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty across all three tasks.
</verification>

<success_criteria>
- A real browser reaches the founders page from all four mapped entry points in both locales, with the coach routes arriving pre-selected — ROADMAP criterion 4, first half.
- A completed live signup is confirmed to record as a `waitlist_signup` conversion in the production analytics dashboard, or its absence is recorded as a finding — ROADMAP criterion 4, second half.
- Phase 5's two carried-forward gaps are each closed with evidence or restated as open with a reason.
- The reveal threshold is confirmed back at 30 after the FOND-06 observation.
- `06-GO-LIVE-LOG.md` and `06-VALIDATION.md` together form a record a later reader can audit without this conversation.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-04-SUMMARY.md` when done.
</output>
