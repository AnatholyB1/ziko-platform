---
phase: 06-founder-offer-go-live
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/founder-offer-go-live/RUNBOOK.md
  - .planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md
autonomous: false
# Phase 6 owns no net-new requirement. The IDs below are the ones this plan
# *verifies the production-activation state of*, per ROADMAP Phase 6 and
# REQUIREMENTS.md's Traceability table — not requirements this phase introduces.
requirements: [PURGE-01, PURGE-02, PURGE-03, PURGE-04, PURGE-05, FOND-06, CRED-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All three previously-unapplied migrations are confirmed present in production by their own separate artifacts — `app_config` returns three rows including `waitlist_retention_years` and `premium_credit_cap_enabled`, `user_profiles.is_lifetime_premium` appears in `information_schema.columns`, and `grant_premium_credits` appears in `pg_proc` — each checked independently rather than as one aggregate success signal (D-02)."
    - "`SELECT value FROM public.app_config WHERE key = 'waitlist_reveal_threshold';` against production returns 30, confirmed by reading it rather than by trusting that Phase 1's migration seeded it (D-05, ROADMAP criterion 3)."
    - "A fresh run of `scripts/purge-test-accounts/dry-run.mjs` against production, dated within the launch window rather than carried over from Phase 2's rehearsal, records its four totals — and if `to_delete` is anything other than 0, the launch stops and the reviewed two-person delete procedure in `scripts/purge-test-accounts/RUNBOOK.md` runs before any later plan proceeds (D-03, PURGE-01 through PURGE-05)."
    - "`SELECT count(*) FROM public.user_profiles WHERE tier = 'premium';` is re-run against production within the launch window and recorded, closing `04-CONTEXT.md` D-01's own staleness note rather than carrying its 2026-08-15 count forward — a nonzero result halts the phase and escalates the grandfather question (CRED-01)."
    - "Nothing in this plan writes to production. Every step is a read, and the one branch that would write — a nonzero purge candidate set — hands off to an existing reviewed procedure rather than executing here."
  artifacts:
    - "scripts/founder-offer-go-live/RUNBOOK.md (modified — sections 4, 5 and 6 appended: full schema confirmation, purge re-verification, threshold and premium-count confirmation)"
    - ".planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md (modified — pre-launch evidence rows filled)"
  key_links:
    - "the purge dry-run's `to_delete` total → ROADMAP criterion 1's credibility: a founder counter can only be trusted if the accounts it counts are real, which is the entire reason PURGE-01 through PURGE-05 gate this phase rather than standing alone in Phase 2."
    - "CRED-01's re-audit → plan 06-03's flag flip: a nonzero premium count means flipping the flag silently downgrades a real paying user, which is the exact failure Phase 4 criterion 1 exists to prevent — so the re-audit belongs immediately before the flip, not once at phase start."
    - "each migration artifact checked separately → the partial-apply failure mode: `supabase db push --include-all` can land one file and fail the next, and a single aggregate check calls that green while `creditGate.ts` reads a flag row that exists next to a `grant_premium_credits` RPC that does not."
    - "`waitlist_retention_years` present in `app_config` → LEGAL-08's published retention promise: the CGU and privacy policy already state a retention period computed from this row, so a missing row means the site is publishing a number the database cannot honour."
  prohibitions:
    - "Do not delete any account in this plan. If the dry-run reports candidates, the deletion runs through `scripts/purge-test-accounts/`'s own reviewed, two-person, export-and-PITR-gated procedure — referenced, never re-derived and never abbreviated here (D-03)."
    - "Do not change `waitlist_reveal_threshold`. D-05 locked the value at 30; this plan confirms it, it does not set it, and Phase 5's plan 05-06 already required any temporary change made during its own checkpoint to have been restored."
    - "Do not proceed to plan 06-03 if any one of the three migration artifacts is absent. A partial apply is escalated with its evidence, not worked around by applying the missing piece by hand mid-launch."
    - "Do not run any real command in this plan from the executing session. No session in this milestone holds production credentials (D-01)."
    - "Do not carry `04-CONTEXT.md`'s 2026-08-15 premium count forward as if it were current. That file's own note asks for a re-verification if meaningful time has passed, and it has."
---

<objective>
Establish, by direct read against production and immediately before activation, that every
precondition the founder offer depends on is true right now rather than true when it was built.

Purpose: plan 06-01 proved the pipeline moved on one artifact. That is deliberately not enough to
launch on. `supabase db push --include-all` applies files in sequence and can land one and fail the
next, so each of the three previously-unapplied migrations needs its own artifact checked. The purge
that makes the founder counter credible was rehearsed in Phase 2 and explicitly never fired, and its
"zero test accounts" result is now days old. CRED-01's production premium count carries its own
staleness note from `04-CONTEXT.md`. And FOND-06's threshold has never actually been read back off
production by anyone — it has only been assumed correct because Phase 1's migration seeded it. Every
one of those is a cheap read and an expensive assumption.

Output: `scripts/founder-offer-go-live/RUNBOOK.md` sections 4 through 6, a blocking human execution
of them against production, and the results recorded in `06-GO-LIVE-LOG.md` with the two halt
conditions — a partial apply, and a nonzero premium count — encoded as stops rather than notes.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-CONTEXT.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-RESEARCH.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-01-SUMMARY.md
@scripts/purge-test-accounts/RUNBOOK.md
</context>

<assumption_delta_note>
The phase-level `<assumption_delta_decision>` is recorded in `06-01-PLAN.md`: **no-change**. The
reveal threshold has been an `app_config` parameter since Phase 1 (FOND-06) and `06-CONTEXT.md` D-05
already fixed the value at 30. T-06-04 through T-06-06 below implement criterion 3 as a
confirm-not-assume read of that value. The threshold is not re-opened and is not changed.
</assumption_delta_note>

<artifacts_this_phase_produces>
This phase ships zero application code and zero UI. It produces exactly these files:

| File | Created/modified by | What it is |
|---|---|---|
| `scripts/founder-offer-go-live/RUNBOOK.md` | 06-01 (§0–§3), **06-02 (§4–§6)**, 06-03 (§7–§9), 06-04 (§10–§11) | The ordered go-live procedure, written for a human operator with no memory of this planning discussion |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` | 06-01 (created), **06-02 (appended)**, 06-03, 06-04 | The dated evidence ledger — one row per RUNBOOK step holding the literal output the operator read back |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md` | 06-04 (completed) | The per-criterion verification map, signed off once all four ROADMAP criteria are green |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-0N-SUMMARY.md` | each plan | Standard per-plan summary |

No source file, no migration, no route, no test file is created or modified by this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>T-06-04: Write the pre-launch verification sections of the RUNBOOK</name>
  <files>scripts/founder-offer-go-live/RUNBOOK.md</files>
  <precondition>`06-GO-LIVE-LOG.md`'s front matter reads `status: open` and its section 3 rows record a `premium_credit_cap_enabled` row present in production. If the log reads `status: blocked`, halt — the migrations never landed and sections 4 through 6 would be written to check a schema that does not exist.</precondition>
  <read_first>
    - `scripts/founder-offer-go-live/RUNBOOK.md` — sections 0 through 3 as written by T-06-01, so the sections you append match their register, numbering and expected-output discipline rather than starting a second document inside the first.
    - `scripts/purge-test-accounts/RUNBOOK.md` — the whole file, in particular section 4 (the `node --env-file=apps/web/.env.local` invocation form and the three environment variables), section 5 step 2 (the dry-run command and its four printed totals), and sections 5 steps 3 through 7 (the reviewed delete procedure you reference rather than restate).
    - `supabase/migrations/20260815_waitlist_retention_config.sql` lines 13-15 — the `waitlist_retention_years` row and its value.
    - `supabase/migrations/20260816_premium_credit_flag.sql` lines 29 and 43-44 — the `is_lifetime_premium` column definition and the `premium_credit_cap_enabled` row.
    - `supabase/migrations/20260816_premium_credit_grant.sql` lines 25 and 100-101 — the `grant_premium_credits` signature and its `service_role`-only grant.
    - `supabase/migrations/20260812_waitlist_founder_offer.sql` lines 159-195 — the `app_config` table, the seeded `waitlist_reveal_threshold` row, and `get_waitlist_founder_status`'s read of it.
    - `06-RESEARCH.md` Patterns 3 and 4, and the Code Examples section — the literal query set and the dry-run invocation.
    - `06-CONTEXT.md` D-02, D-03 and D-05, and `.planning/workstreams/lien-invite/phases/04-credit-gate-alignment/04-CONTEXT.md` D-01 — the four checks these sections encode and the staleness note behind the premium re-count.
  </read_first>
  <action>
Append sections 4, 5 and 6 to `scripts/founder-offer-go-live/RUNBOOK.md`, continuing the numbering and
the register already established.

Section 4, Confirm every migration landed. Open with the reason this is separate from section 3's
single-artifact tracer check: the apply command runs files in sequence and can land one and fail the
next, so a mixed schema is a real outcome that one aggregate signal calls green. Give three
independent checks, each with its own expected result and its own halt condition. First,
`SELECT key, value, updated_at FROM public.app_config ORDER BY key;` — expect three rows,
`premium_credit_cap_enabled` at false, `waitlist_retention_years` at 3, `waitlist_reveal_threshold`
at 30. Note what each one implies if missing: no retention row means the published CGU and privacy
policy are stating a retention period the database cannot compute, and no flag row means
`creditGate.ts` reads nothing and degrades to its flag-off branch. Second,
`SELECT column_name, data_type, column_default FROM information_schema.columns WHERE table_name =
'user_profiles' AND column_name = 'is_lifetime_premium';` — expect one row, boolean, defaulting to
false. Third, `SELECT proname FROM pg_proc WHERE proname IN ('grant_premium_credits',
'reset_waitlist_founder_sequence');` — expect both names, since the second is what section 7 will
call. State plainly that any one of the three missing halts the launch and is escalated with its
evidence, rather than patched by hand mid-procedure — a hand-applied fragment leaves the CLI's
recorded migration state disagreeing with the actual schema, which is a worse problem than a delayed
launch.

Section 5, Re-verify the test-account purge. Explain in two sentences why this runs again rather than
being inherited: Phase 2 built and rehearsed the full purge but deliberately never fired it against
production, and the "zero test accounts" figure recorded during the phase-6 discussion is a
point-in-time reading that goes stale the moment anyone creates an account. Give the literal command,
`node --env-file=apps/web/.env.local scripts/purge-test-accounts/dry-run.mjs`, name the environment
variables it needs, and state that it removes nothing — it enumerates and writes a report plus a CSV
to a git-ignored directory. Tell the operator to read and record all four printed totals:
`users_scanned`, `candidates`, `flagged_cross_linked`, `to_delete`. Then give the branch. If
`to_delete` is 0, record that as a fresh, dated confirmation of the purge precondition and continue.
If it is anything else, the launch stops here and the operator runs the full reviewed procedure in
`scripts/purge-test-accounts/RUNBOOK.md` sections 3 through 7 — second-person review of the CSV, the
unconditional export, the PITR reading, the confirmed delete, the post-purge verification — before
returning to this document. Reference that procedure by section; do not restate it, because it
already encodes the two-person rule, the exact-domain-equality criterion and the cross-link safety
check, and a paraphrase of it here would be a second, subtly different authority.

Section 6, Confirm the launch parameters. Two reads, both cheap, both currently assumed rather than
known. First, `SELECT value FROM public.app_config WHERE key = 'waitlist_reveal_threshold';` — expect
30. State what the value means so the operator can sanity-check it rather than pattern-match: the
counter stays hidden and the page states the offer as a plain fact until fewer than that many spots
remain, so 30 means the count first appears at 170 of 200 claimed. State that this value is fixed by
decision and is confirmed here, not set — if it reads anything other than 30, that is a finding to
escalate, not a number to correct in passing. Second,
`SELECT count(*) FROM public.user_profiles WHERE tier = 'premium';` — expect 0. Explain the
consequence rather than just the expectation: this is the audit the entire credit-gate change was
gated on, the count was last taken on 2026-08-15 and that record asks for a re-verification if
meaningful time has passed, and a nonzero result means flipping the flag in section 9 would silently
put a real paying user onto a finite balance. A nonzero result halts the launch and escalates the
grandfather question to the project owner; it is not a judgement call for the operator at the
terminal.

Close section 6 by stating that sections 7 through 9 — the first steps in this document that write to
production — are added by the next plan, and that an operator who reaches this point with those
sections absent stops here.

Commit the runbook on its own.
  </action>
  <verify>
    <automated>for s in 'information_schema.columns' 'is_lifetime_premium' 'grant_premium_credits' 'reset_waitlist_founder_sequence' 'waitlist_retention_years' 'waitlist_reveal_threshold' 'dry-run.mjs' 'users_scanned' 'flagged_cross_linked' 'to_delete' 'scripts/purge-test-accounts/RUNBOOK.md' "tier = 'premium'"; do grep -qF "$s" scripts/founder-offer-go-live/RUNBOOK.md || { echo "MISSING: $s"; exit 1; }; done; grep -qE '^#+ *4[.[:space:]]' scripts/founder-offer-go-live/RUNBOOK.md &amp;&amp; grep -qE '^#+ *5[.[:space:]]' scripts/founder-offer-go-live/RUNBOOK.md &amp;&amp; grep -qE '^#+ *6[.[:space:]]' scripts/founder-offer-go-live/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0: sections 4, 5 and 6 exist as numbered headings and every one of the twelve grounding literals is present in the runbook.
    - Section 4 gives three separate checks with three separate expected results, and states that any one failing halts the launch rather than being patched by hand.
    - Section 4 names the consequence of a missing `waitlist_retention_years` row and of a missing `premium_credit_cap_enabled` row, distinctly.
    - Section 5 gives the literal `node --env-file=apps/web/.env.local scripts/purge-test-accounts/dry-run.mjs` invocation, names all four printed totals, and states the command removes nothing.
    - Section 5's nonzero branch points at `scripts/purge-test-accounts/RUNBOOK.md` sections 3 through 7 by reference and does not restate the delete procedure.
    - Section 6 states that 30 means the counter first appears at 170 of 200 claimed, and that the value is confirmed here rather than set.
    - Section 6 states that a nonzero premium count halts the launch and escalates to the project owner rather than being resolved at the terminal.
    - `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty — this task changed no source, schema or application file.
  </acceptance_criteria>
  <done>The runbook can now be handed to an operator who will confirm, by direct read, that all three migrations landed, that the purge precondition is fresh rather than inherited, and that both launch parameters are what they are supposed to be — with both halt conditions written as stops. Committed.</done>
  <reversibility rating="reversible">A documentation append; nothing executed.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-06-05: [BLOCKING] Run the pre-launch verification reads against production</name>
  <precondition>The operator holds production `SUPABASE_SERVICE_ROLE_KEY` and `SUPABASE_URL` for project `slkobhavpwsubnsmuhya` in `apps/web/.env.local`, and Node 22 for the `--env-file` invocation. No Claude session in this milestone has held those credentials — `06-RESEARCH.md` Environment Availability confirms it — which is why this is a human checkpoint (D-01).</precondition>
  <read_first>
    - `scripts/founder-offer-go-live/RUNBOOK.md` sections 4, 5 and 6 — the authority for this step. Follow the document, not this checkpoint's summary.
    - `scripts/purge-test-accounts/RUNBOOK.md` sections 1 through 7 — read section 1's criterion and section 3's review gate before running the dry-run, so a nonzero result finds you already briefed rather than improvising.
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` — the rows you will be filling.
  </read_first>
  <what-built>
`scripts/founder-offer-go-live/RUNBOOK.md` sections 4, 5 and 6 — the pre-launch verification set.

Plan 06-01 confirmed the pipeline moved by checking one artifact at each end. These sections check
the rest: that all three previously-unapplied migrations landed rather than one, that the
test-account purge precondition is true today rather than at Phase 2's rehearsal, and that the two
launch parameters nobody has actually read off production are what the decisions say they are.

Every step in this checkpoint is a read. Nothing here writes to production. The one branch that would
write — a purge dry-run that finds real candidates — hands off to the existing reviewed procedure
rather than executing anything from this document.
  </what-built>
  <how-to-verify>
Work through `scripts/founder-offer-go-live/RUNBOOK.md` sections 4, 5 and 6 in order, against project
`slkobhavpwsubnsmuhya`, and paste each literal result back.

**1 — Section 4, all three migrations.** Run the three checks and paste all three result sets in full:
`SELECT key, value, updated_at FROM public.app_config ORDER BY key;` (expect three rows —
`premium_credit_cap_enabled` false, `waitlist_retention_years` 3, `waitlist_reveal_threshold` 30);
`SELECT column_name, data_type, column_default FROM information_schema.columns WHERE table_name =
'user_profiles' AND column_name = 'is_lifetime_premium';` (expect one boolean column defaulting to
false); and `SELECT proname FROM pg_proc WHERE proname IN ('grant_premium_credits',
'reset_waitlist_founder_sequence');` (expect both names). If any one of the three comes back short,
stop and say which — do not apply the missing piece by hand, because a hand-applied fragment leaves
the CLI's recorded migration state disagreeing with the real schema and turns a delayed launch into a
repair job.

**2 — Section 5, the purge re-check.** With `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` pointing
at production in `apps/web/.env.local`, run
`node --env-file=apps/web/.env.local scripts/purge-test-accounts/dry-run.mjs` and paste all four
printed totals: `users_scanned`, `candidates`, `flagged_cross_linked`, `to_delete`. This removes
nothing. If `to_delete` is 0, say so — that is a fresh, dated confirmation and the launch continues.
If it is anything else, stop the launch and say so: the reviewed procedure in
`scripts/purge-test-accounts/RUNBOOK.md` sections 3 through 7 runs first, including the second-person
review of the CSV, the unconditional export, and the post-purge verification. Do not delete anything
from this checkpoint.

**3 — Section 6, the threshold.** Run
`SELECT value FROM public.app_config WHERE key = 'waitlist_reveal_threshold';` and paste the result.
Expect 30, which means the visible counter first appears once 170 of the 200 spots are claimed and
the page states the offer as a plain fact before that. This is confirmed here, not set — if it reads
anything else, say so rather than correcting it.

**4 — Section 6, the premium count.** Run
`SELECT count(*) FROM public.user_profiles WHERE tier = 'premium';` and paste the result. Expect 0.
This count was last taken on 2026-08-15 and the record that took it asks for a re-verification if
meaningful time has passed. If it is nonzero, stop: flipping the flag in a later step would put a
real paying user onto a finite balance, and the grandfather question goes back to the project owner
rather than being decided at the terminal.

Approve only if steps 1, 3 and 4 produced their expected results and step 2 reported `to_delete` as 0
or the reviewed delete procedure has been completed. If anything failed, name the step and paste what
you saw.
  </how-to-verify>
  <acceptance_criteria>
    - `SELECT key, value, updated_at FROM public.app_config ORDER BY key;` returns exactly three rows with the keys `premium_credit_cap_enabled`, `waitlist_retention_years` and `waitlist_reveal_threshold`.
    - The `information_schema.columns` query returns exactly one row for `is_lifetime_premium`, of boolean type, defaulting to false.
    - The `pg_proc` query returns both `grant_premium_credits` and `reset_waitlist_founder_sequence`.
    - The dry-run's four totals are pasted verbatim, and `to_delete` is 0 — or the reviewed procedure in `scripts/purge-test-accounts/RUNBOOK.md` sections 3 through 7 has been completed and its post-purge verification output is pasted too.
    - `SELECT value FROM public.app_config WHERE key = 'waitlist_reveal_threshold';` returns 30.
    - `SELECT count(*) FROM public.user_profiles WHERE tier = 'premium';` returns 0.
    - Every result is supplied as literal pasted output, not as a summary judgement.
  </acceptance_criteria>
  <resume-signal>Paste the results of steps 1 through 4, then type "approved" — or name the step that failed and paste what you saw.</resume-signal>
</task>

<task type="auto">
  <name>T-06-06: Record the pre-launch evidence and gate plan 06-03 on it</name>
  <files>.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md</files>
  <precondition>T-06-05's checkpoint has been answered with literal pasted output for all four steps. A bare approval with no result sets means the reads were not performed, and this ledger's rows are read as settled fact by plan 06-03 — halt rather than fill them from expectation.</precondition>
  <read_first>
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` — the table and Escalations section as left by T-06-03, including the two inherited Phase 5 gaps you must not disturb.
    - The operator's pasted output from T-06-05 — the only source for these values.
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md` — the per-criterion map whose criterion 3 row this task marks.
    - `.planning/workstreams/lien-invite/phases/02-test-account-purge/02-04-SUMMARY.md` — the written, dated purge confirmation format this ledger's section 5 row follows.
  </read_first>
  <action>
Append rows to `06-GO-LIVE-LOG.md` for every check in runbook sections 4 through 6, filling each with
the operator's literal reported output. Record the full three-row `app_config` result set including
each value and its `updated_at`, the `is_lifetime_premium` column row with its type and default, both
function names returned by the `pg_proc` query, all four dry-run totals, the threshold value, and the
premium count. Where the operator's output differs from what this plan expected, record what they
reported.

Give the purge row the shape of a dated confirmation rather than a checkmark: the date, the four
totals, and an explicit sentence stating that this is a fresh reading taken within the launch window
and not Phase 2's rehearsal result inherited. That sentence is the artifact a later audit will read
as satisfying the purge precondition, so it needs to stand on its own.

Then apply the two gates. If any one of section 4's three artifacts came back short, write the
specific missing artifact into Escalations with its evidence, set the log's `status` to `blocked`,
and halt the phase before plan 06-03 — name in the escalation which migration file owns the missing
artifact so whoever picks it up is not searching. If the premium count came back nonzero, write it
into Escalations as a halt with the count and the affected user identifiers withheld, set `status`
to `blocked`, and state that the grandfather question returns to the project owner per ROADMAP Phase
4 criterion 1 — this is the one finding in this phase that is not the operator's to resolve.

If everything passed, leave `status: open` and add a line noting that runbook sections 7 through 9,
the first steps that write to production, are added by plan 06-03.

Finally, update `06-VALIDATION.md`'s per-criterion map: mark criterion 3's row green with T-06-05 as
the task that closed it and the observed threshold value as the evidence. Leave criteria 1, 2 and 4
pending and leave the sign-off block alone — it belongs to plan 06-04.

Commit the log and the validation record together.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const L='.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md';const V='.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md';const t=fs.readFileSync(L,'utf8');const st=(t.match(/^status:\s*(\S+)/m)||[])[1];if(!['open','blocked'].includes(st)){console.error('bad status: '+st);process.exit(1)}const pre=t.split('## Escalations')[0];if(/pending/i.test(pre)){console.error('a ledger row is still pending');process.exit(1)}for(const s of ['users_scanned','flagged_cross_linked','to_delete','is_lifetime_premium','grant_premium_credits','waitlist_reveal_threshold']){if(!t.includes(s)){console.error('MISSING in log: '+s);process.exit(1)}}const v=fs.readFileSync(V,'utf8');if(!/T-06-05/.test(v)){console.error('validation map does not cite T-06-05');process.exit(1)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - No ledger row above the Escalations heading is still marked pending; each holds the operator's literal reported value.
    - The three-row `app_config` result set, the `is_lifetime_premium` column row, both `pg_proc` names, all four dry-run totals, the threshold value and the premium count are each recorded verbatim.
    - The purge row reads as a dated, standalone confirmation stating explicitly that it is a fresh reading within the launch window, not Phase 2's rehearsal inherited.
    - If any section 4 artifact was missing or the premium count was nonzero, the log reads `status: blocked` and Escalations names the finding, its evidence, and — for a missing artifact — the migration file that owns it; otherwise `status: open`.
    - The two inherited Phase 5 gaps recorded by T-06-03 are still present in Escalations, unmodified.
    - `06-VALIDATION.md`'s criterion 3 row is green and cites T-06-05 and the observed threshold value; criteria 1, 2 and 4 remain pending and the sign-off block is untouched.
    - `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty — this task changed no source, schema or application file.
  </acceptance_criteria>
  <done>The pre-launch state of production is on disk in the operator's own words, ROADMAP criterion 3 is closed with evidence, and the two conditions that must stop a launch — a partial apply and a nonzero premium count — are encoded as halts rather than as notes. Committed.</done>
  <reversibility rating="reversible">A documentation update recording reads that already happened.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → production Supabase | `dry-run.mjs` runs with `SUPABASE_SERVICE_ROLE_KEY` loaded from `apps/web/.env.local`; the same file is what points every query in this plan at a project. |
| Phase 2's rehearsed purge → Phase 6's live precondition | The toolkit was proven against fixture data and never fired at production; this plan is the first time it reads real accounts. |
| a point-in-time production count → a launch decision | Both CRED-01's premium count and the purge's zero result are readings, not invariants, and both are consumed as gates. |

## STRIDE Threat Register

Threat IDs use the `T-06-S<n>` namespace to stay out of the `T-06-<nn>` task-ID namespace, matching
`04-VALIDATION.md`'s convention in this workstream.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-S1 | Tampering | A partial `supabase db push --include-all` leaving production with some of the three migrations applied and some not | high | mitigate | Runbook section 4 checks three artifacts independently — an `app_config` row set, an `information_schema.columns` row, and a `pg_proc` row set — and T-06-06 halts the phase with `status: blocked` on any single miss, naming the owning migration file rather than patching it by hand. |
| T-06-S3 | Spoofing | The verification reads answered by the wrong Supabase project, since `test-rls.yml` maintains a near-identical schema on a separate test project | high | mitigate | Every step in this plan names project ref `slkobhavpwsubnsmuhya`, and the runbook's section 0 precondition requires confirming the connection target before any read. |
| T-06-S9 | Tampering | `dry-run.mjs` pointed at production by an `apps/web/.env.local` that still holds test-project values, producing a falsely clean `to_delete` of 0 | high | mitigate | The runbook names the two environment variables the script reads and requires the operator to confirm they point at production before running; the dry-run's `users_scanned` total is recorded alongside `to_delete`, so a scan of the wrong project shows up as an implausible account count rather than as a silent zero. |
| T-06-S10 | Repudiation | The purge precondition satisfied by inheriting Phase 2's rehearsal result rather than a fresh reading | medium | mitigate | The ledger's purge row is required to be a dated, standalone confirmation that states in its own words that it is a fresh reading within the launch window — a checkmark is not accepted (D-03). |
| T-06-S7 | Repudiation | The ledger recording a read as performed that was not | high | mitigate | T-06-06 carries a precondition that halts on a bare approval with no pasted output, and its verify fails while any row above the Escalations heading is still pending. |
| T-06-S8 | Elevation of Privilege | The pre-existing `anon`-executable `is_coach_of()` / `redeem_invitation_code()` / `peek_invitation()` gap in production | high | transfer | Transferred to a future, separate security-response phase per `STATE.md` and `06-CONTEXT.md`'s Deferred Ideas. Not remediated here. The `pg_proc` check in section 4 confirms only that this phase's own functions exist; it makes no claim about the pre-existing gap. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs zero packages — `06-RESEARCH.md`'s Package Legitimacy Audit records the gate as not applicable because no package-manager install step exists anywhere in this phase's scope. `dry-run.mjs` runs on Node's built-in `--env-file` with no install step. Any install command appearing during execution is out of contract and halts the task. |
</threat_model>

<verification>
- `scripts/founder-offer-go-live/RUNBOOK.md` carries numbered sections 4, 5 and 6 and passes T-06-04's literal-presence check.
- `06-GO-LIVE-LOG.md` has no pending row above the Escalations heading and records all four dry-run totals, the three-row `app_config` set, the `is_lifetime_premium` column, both `pg_proc` names, the threshold and the premium count.
- `06-VALIDATION.md`'s criterion 3 row is green and cites T-06-05.
- The log's status is `open` (launch may proceed to plan 06-03) or `blocked` (halted with the finding named).
- `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty across all three tasks.
</verification>

<success_criteria>
- All three previously-unapplied migrations are confirmed present in production by three independent artifact checks.
- A fresh, dated purge dry-run against production records its four totals, and `to_delete` is 0 — or the reviewed two-person delete procedure has run and its post-purge verification is recorded.
- `waitlist_reveal_threshold` is confirmed at 30 by direct read, closing ROADMAP criterion 3.
- The production `tier='premium'` count is re-confirmed at 0 within the launch window.
- Both halt conditions are encoded in the ledger as stops, and plan 06-03 is either unblocked or the phase is explicitly halted with the finding written down.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-02-SUMMARY.md` when done.
</output>
