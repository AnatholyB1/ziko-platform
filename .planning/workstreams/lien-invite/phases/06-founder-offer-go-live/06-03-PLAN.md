---
phase: 06-founder-offer-go-live
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - scripts/founder-offer-go-live/RUNBOOK.md
autonomous: false
# Phase 6 owns no net-new requirement. The IDs below are the ones this plan
# *verifies the production-activation state of*, per ROADMAP Phase 6 and
# REQUIREMENTS.md's Traceability table — not requirements this phase introduces.
requirements: [CRED-05, LEGAL-05]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`claim_waitlist_signup()` is proven to work against the live, post-deploy production RPC by a real call returning `is_new` true, `is_founder` true and `founder_rank` 1 — the round trip Phase 5 disclosed it could never observe live (D-04)."
    - "`SELECT count(*) FROM public.waitlist_signups;` returns 0 after `TRUNCATE public.waitlist_signups;` and `SELECT public.reset_waitlist_founder_sequence(1);` have both run, and the next founder rank the sequence will issue is 1 (ROADMAP criterion 1, D-04)."
    - "The cleanup pair runs strictly before any real visitor can reach the page. After that moment the same two statements destroy genuine signups rather than a smoke-test row, and the runbook states this as a prominent warning rather than relying on step order to convey it (D-04's one-way rating)."
    - "`https://<site>/fr/cgv`, `/fr/cgu`, `/en/cgv` and `/en/cgu` each return HTTP 200 from the production domain, checked by URL, and all four checks precede the flag `UPDATE` in the runbook and in time (LEGAL-05, D-06, ROADMAP criterion 2)."
    - "`premium_credit_cap_enabled` reads back with `jsonb_typeof(value)` of `boolean` and a value of true — not the JSON string form, which `creditGate.ts`'s strict comparison against a JavaScript boolean would never match, leaving a flag that looks flipped and behaves off (CRED-05)."
    - "Every statement in this plan is fired by a human holding production credentials; the executing session fires none of them (D-01)."
  artifacts:
    - "scripts/founder-offer-go-live/RUNBOOK.md (modified — sections 7, 8 and 9 appended: waitlist smoke test and one-way cleanup, legal-text liveness gate, flag flip)"
  key_links:
    - "the four CGU/CGV 200s → the flag `UPDATE`: this is LEGAL-05's ordering constraint turned into a gate instead of an inference. ROADMAP and REQUIREMENTS.md track build completion, not production liveness, so 'Phase 3 is marked Complete' is precisely the shortcut D-06 rejects."
    - "the smoke-test row → the `TRUNCATE`: the smoke test is the only reason a row exists to clean up, so the two are one indivisible step. Separating them in time is exactly what makes the second one dangerous."
    - "`app_config.value`'s JSONB scalar type → `creditGate.ts`'s behaviour: an `UPDATE` that runs successfully with the wrong literal form leaves the flag reading as set while the gate keeps taking its flag-off branch, and nothing errors."
    - "`reset_waitlist_founder_sequence(1)`'s third `setval` argument → the first real founder's rank: it is called with false, so the next `nextval` returns exactly 1 rather than 2, which is why the argument is 1 and not 0."
  prohibitions:
    - "Do not run the `TRUNCATE` at any moment after the founders page can receive real visitor traffic. This is the phase's one irreversible step and there is no application-level lock preventing it from running at the wrong time (D-04)."
    - "Do not flip the flag before all four legal-page checks return 200. Inferring liveness from Phase 3's completion status is the exact shortcut D-06 rejects."
    - "Do not write the flag value as a quoted JSON string. The runbook's readback check exists to catch this and it must not be softened into an equality test on the rendered text."
    - "Do not substitute a raw sequence-restart statement for `reset_waitlist_founder_sequence(1)`. Phase 1 shipped that RPC naming Phase 6 as its consumer; a parallel mechanism creates two resets with different edge-case behaviour."
    - "Do not call `claim_waitlist_signup()` with the anon or authenticated key. It is `service_role`-only by its own grant, and an anon call fails in a way that looks like the RPC is broken rather than like the caller is wrong."
    - "Do not run any real command in this plan from the executing session. No session in this milestone holds production credentials (D-01)."
---

<objective>
Fire the three production-writing steps of the go-live, in the one order that is safe, each behind a
gate that cannot be satisfied by inference.

Purpose: this is the plan where the phase stops reading and starts writing. It contains the only
irreversible action in the milestone — `TRUNCATE public.waitlist_signups` — which is correct the
minute before the page is public and destroys real customer data the minute after, with nothing in
the system to tell the difference. It also contains the flag flip, whose entire legal justification
is that the CGU and CGV describing the capped offer are already live, a fact that is cheap to check
by URL and catastrophic to assume from a roadmap checkbox. And it closes the round trip Phase 5
disclosed it could never observe: nobody has ever seen `claim_waitlist_signup()` succeed against
production.

Output: `scripts/founder-offer-go-live/RUNBOOK.md` sections 7 through 9, an explicit recorded
decision on the irreversible step, and a blocking human execution of all three in order.
</objective>

<execution_context>
@/home/user/ziko-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ziko-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/lien-invite/ROADMAP.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-CONTEXT.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-RESEARCH.md
@.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-02-SUMMARY.md
@.planning/workstreams/lien-invite/phases/04-credit-gate-alignment/04-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This phase ships zero application code and zero UI. It produces exactly these files:

| File | Created/modified by | What it is |
|---|---|---|
| `scripts/founder-offer-go-live/RUNBOOK.md` | 06-01 (§0–§3), 06-02 (§4–§6), **06-03 (§7–§9)**, 06-04 (§10–§11) | The ordered go-live procedure, written for a human operator with no memory of this planning discussion |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` | 06-01 (created), 06-02, **06-03's evidence transcribed by 06-04's T-06-10**, 06-04 | The dated evidence ledger — one row per RUNBOOK step holding the literal output the operator read back |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-VALIDATION.md` | 06-04 (completed) | The per-criterion verification map, signed off once all four ROADMAP criteria are green |
| `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-0N-SUMMARY.md` | each plan | Standard per-plan summary |

No source file, no migration, no route, no test file is created or modified by this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>T-06-07: Write the activation sections of the RUNBOOK — smoke test, cleanup, legal gate, flag flip</name>
  <files>scripts/founder-offer-go-live/RUNBOOK.md</files>
  <precondition>`06-GO-LIVE-LOG.md`'s front matter reads `status: open` with plan 06-02's rows filled — the three migration artifacts present, `to_delete` at 0, and the premium count at 0. If it reads `status: blocked`, halt: sections 7 through 9 write to production and must not be authored against a schema or a data state that was never confirmed.</precondition>
  <read_first>
    - `scripts/founder-offer-go-live/RUNBOOK.md` sections 0 through 6 — so the sections you append continue the numbering, register and expected-output discipline rather than starting a second document.
    - `supabase/migrations/20260812_waitlist_founder_offer.sql` lines 96-155 — `claim_waitlist_signup`'s full parameter list, its `RETURNS TABLE(is_new, is_founder, founder_rank)` shape, and the `REVOKE`/`GRANT` pair at lines 151-152 that makes it `service_role`-only.
    - `supabase/migrations/20260812_waitlist_founder_offer.sql` lines 244-268 — `reset_waitlist_founder_sequence`'s own comment naming this phase as its consumer, its `setval` call with the third argument false, and its `service_role`-only grant.
    - `supabase/migrations/20260816_premium_credit_flag.sql` lines 29-44 — the flag row as seeded, so the `UPDATE` matches the stored representation rather than replacing it with a different one.
    - `backend/api/src/middleware/creditGate.ts` lines 40-80 — the `app_config` read and the strict comparison the flag value must satisfy; this is why the literal form of the `UPDATE` matters more than whether the statement succeeds.
    - `apps/web/src/lib/supabase/admin.ts` — the service-role client the smoke test calls through, if the operator uses a script rather than the SQL editor.
    - `apps/web/src/app/[locale]/(marketing)/cgv/page.tsx` and `.../cgu/page.tsx` — confirm the route segments so the four URLs in section 8 are the real ones.
    - `06-RESEARCH.md` Patterns 5, 6 and 7 and Pitfall 3 — the literal statements, the four-URL check and the JSONB trap.
    - `06-CONTEXT.md` D-04 and D-06 — the two decisions these sections implement.
  </read_first>
  <action>
Append sections 7, 8 and 9 to `scripts/founder-offer-go-live/RUNBOOK.md`.

Section 7, Prove the claim path and clear the table. Open it with a boxed warning, set apart
visually from the prose so an operator skimming cannot miss it, stating that this section contains
the only irreversible action in the procedure and that its two halves run back to back in one
sitting: the cleanup statements are correct while the page is still private and destroy real customer
signups the moment it is not, and no lock, guard or policy in the system distinguishes those two
situations. Say explicitly that if the page has already been linked publicly — from the homepage, the
header, the footer or `/coachs` — this section is not run at all and the operator escalates instead.

Then give the smoke test. State why it exists rather than only what it does: this is the first time
anyone will have seen the claim path succeed against production, because Phase 5's own verification
checkpoint recorded a real submission as an accepted, disclosed gap for want of credentials. Give the
call as `SELECT * FROM public.claim_waitlist_signup('go-live-smoketest@ziko-app.com', 'athlete');`,
note that the function takes three further optional text parameters for locale and campaign
attribution that this test does not need, and state that it must be called as `service_role` — from
the Supabase SQL editor, or through the admin client in `apps/web/src/lib/supabase/admin.ts` — because
its own grant closes it to anon and authenticated, and a call with the wrong key fails in a way that
reads like a broken RPC rather than a wrong caller. Give the expected result exactly: one row,
`is_new` true, `is_founder` true, `founder_rank` 1, on the basis that the table was confirmed empty in
section 4's environment. Tell the operator that a `founder_rank` above 1 means rows already exist and
they should stop and look at the table before continuing, because the assumption underneath the
cleanup step has just failed.

Then give the cleanup pair, as the two literal statements
`TRUNCATE public.waitlist_signups;` followed by `SELECT public.reset_waitlist_founder_sequence(1);`,
and explain the argument rather than leaving it as a magic number: the RPC calls `setval` with its
third argument false, so the next rank issued is exactly the value passed, which is why it is 1 and
the first real founder gets rank 1 rather than rank 2. Note that this RPC is what Phase 1 shipped for
this exact purpose — its own migration comment names this phase as its consumer — and that a raw
sequence-restart statement is not an equivalent substitute because the two have different edge-case
behaviour. Close the section with the confirmation read: `SELECT count(*) FROM public.waitlist_signups;`
must return 0, and the operator records the wall-clock time of that reading, because criterion 1's
claim is that the table was empty *before* the page opened and the timestamp is what makes that
claim checkable later.

Section 8, Confirm the legal text is live. State the constraint in one sentence and its consequence
in the next: the credit-cap behaviour may not be enabled before the published terms describing it are
reachable, and the roadmap marking the legal phase complete is evidence that the text was written, not
that it is being served. Give four literal checks against the operator's confirmed production domain
from section 0 — `curl -sI <site>/fr/cgv`, `/fr/cgu`, `/en/cgv`, `/en/cgu` — each expecting a 200,
and state that all four must pass before section 9 is opened, both locales included, because the
bilingual requirement is part of what counsel approved. Add that a redirect chain resolving to 200 is
acceptable but a 3xx that lands anywhere other than the same document is not, and that the operator
should open one of the four in a browser and confirm it renders the terms rather than an error
boundary or an empty shell — a 200 from a framework error page is still a 200.

Section 9, Enable the capped behaviour. Give the statement as
`UPDATE public.app_config SET value = 'true', updated_at = NOW() WHERE key = 'premium_credit_cap_enabled';`
and then, immediately, the readback that is the real check:
`SELECT value, jsonb_typeof(value) FROM public.app_config WHERE key = 'premium_credit_cap_enabled';`
must report the type as boolean. Explain the failure mode this catches, because it is silent and it
has already been documented once for this same column in Phase 4's research: storing the value as a
JSON string rather than a JSON boolean leaves a row that reads as set to a human eye while the
gate's comparison against a JavaScript boolean never matches, so the flag looks flipped and the old
behaviour continues indefinitely with nothing logged. State that the readback is not optional and
that a type other than boolean means the update used the wrong literal form and must be reissued.
Close by noting that this section is the activation itself — after it, premium AI access is finite —
and that section 10 confirms the public surface.

Commit the runbook on its own.
  </action>
  <verify>
    <automated>for s in 'claim_waitlist_signup' 'go-live-smoketest@ziko-app.com' 'founder_rank' 'TRUNCATE public.waitlist_signups' 'reset_waitlist_founder_sequence(1)' 'service_role' '/fr/cgv' '/fr/cgu' '/en/cgv' '/en/cgu' 'premium_credit_cap_enabled' 'jsonb_typeof'; do grep -qF "$s" scripts/founder-offer-go-live/RUNBOOK.md || { echo "MISSING: $s"; exit 1; }; done; grep -qE '^#+ *7[.[:space:]]' scripts/founder-offer-go-live/RUNBOOK.md &amp;&amp; grep -qE '^#+ *8[.[:space:]]' scripts/founder-offer-go-live/RUNBOOK.md &amp;&amp; grep -qE '^#+ *9[.[:space:]]' scripts/founder-offer-go-live/RUNBOOK.md &amp;&amp; awk '/^#+ *7[.[:space:]]/{s7=NR} /^#+ *8[.[:space:]]/{s8=NR} /^#+ *9[.[:space:]]/{s9=NR} END{exit !(s7&amp;&amp;s8&amp;&amp;s9&amp;&amp;s7&lt;s8&amp;&amp;s8&lt;s9)}' scripts/founder-offer-go-live/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0: sections 7, 8 and 9 exist as numbered headings in that order, and every one of the twelve grounding literals is present.
    - Section 7 opens with a visually set-apart warning that names the cleanup as the only irreversible action, states the two halves run back to back, and states that the section is not run at all if the page is already publicly linked.
    - Section 7 gives the smoke-test call, states it must be made as `service_role` and why a wrong-key call misleads, and gives the exact expected row shape including `founder_rank` 1.
    - Section 7 explains the argument to `reset_waitlist_founder_sequence` in terms of the sequence's next issued value rather than presenting it as a magic number.
    - Section 7 ends with the count-zero confirmation read and an instruction to record the wall-clock time of that reading.
    - Section 8 gives four literal URL checks covering both documents in both locales, requires all four before section 9, and distinguishes a rendered terms page from a 200 served by an error boundary.
    - Section 9 gives the `UPDATE` and, immediately after it, the `jsonb_typeof` readback, and explains the silent failure mode the readback catches.
    - `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty — this task changed no source, schema or application file.
  </acceptance_criteria>
  <done>The three production-writing steps are written down in the only order that is safe, each with its own gate, its own expected output, and — for the irreversible one — a warning an operator cannot skim past. Committed.</done>
  <reversibility rating="reversible">A documentation append; nothing executed.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>T-06-08: [BLOCKING] Decide the waitlist cleanup mechanism before it is fired</name>
  <read_first>
    - `scripts/founder-offer-go-live/RUNBOOK.md` section 7 — the procedure this decision authorises.
    - `06-CONTEXT.md` D-04 — the decision as taken during discuss-phase, including its one-way reversibility rating.
    - `supabase/migrations/20260812_waitlist_founder_offer.sql` lines 244-268 — what the sequence-reset RPC actually does.
  </read_first>
  <decision>
Which mechanism clears the smoke-test row from `waitlist_signups` before the founders page opens to
real visitors, and is it fired now?
  </decision>
  <context>
`06-CONTEXT.md` D-04 already settled this during discuss-phase: a plain SQL pair,
`TRUNCATE public.waitlist_signups;` followed by `SELECT public.reset_waitlist_founder_sequence(1);`,
run by the operator immediately after the smoke test and before the page goes public. That decision
carries an explicit **one-way** reversibility rating — the only one in this milestone. This gate
exists because a one-way action gets confirmed at the moment of firing, by the person firing it, not
inherited from a decision recorded days earlier by someone who was not looking at the current state
of the table.

What makes it one-way: `TRUNCATE` removes every row with no undo and no per-row trace. It is correct
while the page is private and the only row is the smoke test. It is destructive of real customer data
the moment a genuine visitor has signed up, and nothing in the schema, the RLS posture or the
application distinguishes those two situations — the same statement, same output, entirely different
consequence. Production was confirmed at 0 rows during discuss-phase and again in plan 06-02's
environment reads, so the expected blast radius is exactly one smoke-test row.
  </context>
  <options>
    <option id="option-a">
      <name>Fire D-04 as written: TRUNCATE plus sequence reset to 1</name>
      <pros>The decision of record, taken deliberately. Guarantees an empty table regardless of what else is in it, so the founder counter's first real signup is rank 1 with no possibility of a leftover row inflating it. Uses the RPC Phase 1 shipped naming this phase as its consumer. Two statements, no tooling, nothing new to review.</pros>
      <cons>Irreversible and total. If the page is already reachable and a real visitor has signed up in the interval, their signup is destroyed with no record that it existed.</cons>
    </option>
    <option id="option-b">
      <name>Delete the smoke-test row by address, then reset the sequence to 1</name>
      <pros>Blast radius bounded to the one address the operator created. A genuine signup that arrived unexpectedly survives and is visible afterwards.</pros>
      <cons>Leaves any other pre-existing row in place — and the sequence reset then hands rank 1 to a second row while the survivor keeps its own rank, which is precisely the inflated-counter state criterion 1 exists to prevent. Diverges from the decision of record without a written amendment.</cons>
    </option>
    <option id="option-c">
      <name>Halt: the page is already publicly reachable, or the table is not in the state section 7 assumes</name>
      <pros>The correct answer if the smoke test returned a `founder_rank` above 1, if any entry point has already been linked, or if the count before the smoke test was not 0. Costs a delay and nothing else.</pros>
      <cons>Delays the launch and requires re-establishing the table's state before this section can be attempted again.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The selected option is recorded, with the person selecting it named and the timestamp of the selection.
    - The operator has stated, at the moment of selection, the observed row count of `waitlist_signups` immediately before the smoke test and whether any founders-page entry point is already publicly linked.
    - If option-c is selected, T-06-09 does not run and the phase halts with the reason recorded.
    - If option-b is selected, the divergence from D-04 is written down as an amendment with its reason, not applied silently.
  </acceptance_criteria>
  <resume-signal>Select: option-a, option-b, or option-c — and state the observed row count before the smoke test plus whether any entry point is already publicly linked.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-06-09: [BLOCKING] Fire the smoke test, the cleanup, the legal gate and the flag flip — in that order</name>
  <precondition>T-06-08 selected option-a or option-b. If it selected option-c, this task does not run. The operator holds production `SUPABASE_SERVICE_ROLE_KEY` or Supabase SQL editor access to project `slkobhavpwsubnsmuhya`, and knows the confirmed production web domain recorded in runbook section 0.</precondition>
  <read_first>
    - `scripts/founder-offer-go-live/RUNBOOK.md` sections 7, 8 and 9 — the authority for this step. Follow the document, not this checkpoint's summary.
    - The decision recorded in T-06-08 — which cleanup mechanism you authorised and on what observed state.
    - `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-GO-LIVE-LOG.md` — where these results are transcribed by T-06-10 in the next plan.
  </read_first>
  <what-built>
`scripts/founder-offer-go-live/RUNBOOK.md` sections 7, 8 and 9 — the three steps that write to
production, in the only safe order.

Everything before this point has been a read. Section 7 proves `claim_waitlist_signup()` works
against the live post-deploy RPC — a round trip nobody in this milestone has ever observed, because
Phase 5's verification checkpoint recorded it as an accepted, disclosed gap for want of credentials —
and then clears the row it creates. Section 8 confirms by URL that the CGU and CGV describing the
capped AI offer are actually being served, in both languages. Section 9 enables the capped behaviour.

The cleanup in section 7 is the phase's one irreversible action and you have just authorised it in
T-06-08 against the current state of the table. Section 9's flip is what actually changes what
premium users experience.
  </what-built>
  <how-to-verify>
Work through `scripts/founder-offer-go-live/RUNBOOK.md` sections 7, 8 and 9 in order, in one sitting,
against project `slkobhavpwsubnsmuhya`. Paste every literal result back.

**1 — Before anything: the pre-state.** Run `SELECT count(*) FROM public.waitlist_signups;` and paste
it. Confirm and state that no founders-page entry point is publicly linked yet. If the count is not
what you told T-06-08 it was, stop.

**2 — Section 7, the smoke test.** As `service_role` — from the SQL editor, or through the admin
client — run `SELECT * FROM public.claim_waitlist_signup('go-live-smoketest@ziko-app.com',
'athlete');` and paste the whole returned row. Expect `is_new` true, `is_founder` true, `founder_rank`
1. A rank above 1 means rows already existed: stop and look at the table rather than continuing into
the cleanup. Do not call this with the anon or publishable key — the function's own grant closes it to
anon and authenticated, and that failure looks like a broken RPC rather than a wrong caller.

**3 — Section 7, the cleanup.** Immediately, in the same sitting, run the mechanism you selected in
T-06-08 — for option-a that is `TRUNCATE public.waitlist_signups;` followed by
`SELECT public.reset_waitlist_founder_sequence(1);`. Then run
`SELECT count(*) FROM public.waitlist_signups;` and paste the result, which must be 0, together with
the wall-clock time you read it. That timestamp is what makes criterion 1's claim — that the table was
empty before the page opened — checkable by anyone later. **Do not run this step if any entry point
has already been linked publicly.**

**4 — Section 8, the legal gate.** Against the production domain you confirmed in section 0, run
`curl -sI <site>/fr/cgv`, `<site>/fr/cgu`, `<site>/en/cgv` and `<site>/en/cgu`, and paste all four
status lines. All four must be 200. Then open one of the four in a browser and confirm it renders the
actual terms rather than an error boundary or an empty shell — a framework error page returns 200 too.
If any of the four is not 200, stop here: the flag does not flip, because the entire legal basis for
the capped behaviour is that the text describing it is already published.

**5 — Section 9, the flip.** Only after step 4 passed in full, run
`UPDATE public.app_config SET value = 'true', updated_at = NOW() WHERE key = 'premium_credit_cap_enabled';`
and then, immediately,
`SELECT value, jsonb_typeof(value) FROM public.app_config WHERE key = 'premium_credit_cap_enabled';`
and paste both results. The readback must report the type as boolean. If it reports a string type, the
update stored the wrong literal form: the row will look set to you and the gate will keep taking its
old branch with nothing logged. Reissue it in the form the runbook gives and paste the readback again.

Approve only if steps 2, 3, 4 and 5 all produced their expected results. If anything failed, name the
step and paste what you saw.
  </how-to-verify>
  <acceptance_criteria>
    - `SELECT count(*) FROM public.waitlist_signups;` before the smoke test matches the count stated in T-06-08.
    - `SELECT * FROM public.claim_waitlist_signup('go-live-smoketest@ziko-app.com', 'athlete');` returns one row with `is_new` true, `is_founder` true and `founder_rank` 1.
    - After the cleanup, `SELECT count(*) FROM public.waitlist_signups;` returns 0, and the wall-clock time of that reading is recorded.
    - The cleanup was fired before any founders-page entry point was publicly linked, stated explicitly by the operator.
    - All four of `<site>/fr/cgv`, `<site>/fr/cgu`, `<site>/en/cgv` and `<site>/en/cgu` returned HTTP 200, with all four status lines pasted, and one was confirmed in a browser to render the terms.
    - All four 200s were obtained before the flag `UPDATE` was issued.
    - `SELECT value, jsonb_typeof(value) FROM public.app_config WHERE key = 'premium_credit_cap_enabled';` reports the value as true with type `boolean`.
    - Every result is supplied as literal pasted output, not as a summary judgement.
  </acceptance_criteria>
  <reversibility rating="one-way">Step 3's cleanup removes every row of `waitlist_signups` with no undo and no per-row trace. Its correctness depends entirely on the page not yet being publicly reachable — a condition nothing in the system enforces. T-06-08 is the gate that confirms it against the table's current state at the moment of firing. Steps 4 and 5 are reversible: the flag can be set back.</reversibility>
  <resume-signal>Paste the results of steps 1 through 5, then type "approved" — or name the step that failed and paste what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator with `service_role` → `waitlist_signups` | The only door into the table is two `SECURITY DEFINER` RPCs plus, in this section, a direct `TRUNCATE` run as a privileged operator — the one write in the milestone that bypasses the RPC surface entirely. |
| published legal text → enabled code behaviour | LEGAL-05's ordering constraint. The gate between them is four HTTP checks and the operator's discipline, not a technical interlock. |
| `app_config` row → `creditGate.ts`'s per-request read | The gate compares a stored JSONB scalar against a JavaScript boolean; a type mismatch is silent on both sides. |
| private page → public page | The moment that makes the cleanup statement change from correct to destructive. Nothing observes or records this transition. |

## STRIDE Threat Register

Threat IDs use the `T-06-S<n>` namespace to stay out of the `T-06-<nn>` task-ID namespace, matching
`04-VALIDATION.md`'s convention in this workstream.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-S5 | Repudiation | `TRUNCATE public.waitlist_signups` run after real visitor traffic has begun, destroying genuine signups with no per-row trace and no undo | critical | mitigate | A `checkpoint:decision` (T-06-08) immediately before the firing task, requiring the operator to state the observed pre-state and whether any entry point is publicly linked; a visually set-apart warning at the head of runbook section 7; a stated instruction not to run the section at all if the page is already linked; and a recorded wall-clock timestamp on the count-zero read so the ordering claim is checkable afterwards. |
| T-06-S4 | Tampering | The credit-cap flag enabled before the CGU and CGV describing it are actually served, violating LEGAL-05's ordering constraint | high | mitigate | Four literal URL checks covering both documents in both locales immediately precede the `UPDATE` in the runbook and in time; a browser confirmation distinguishes a rendered terms page from a 200 served by a framework error boundary; a failure at any of the four stops the flip rather than being noted. |
| T-06-S11 | Tampering | The flag stored as a JSON string rather than a JSON boolean, leaving a row that reads as set while `creditGate.ts`'s strict comparison never matches and the old behaviour continues with nothing logged | high | mitigate | A mandatory `jsonb_typeof` readback immediately after the `UPDATE`, with the boolean type as the acceptance criterion rather than the rendered text; the failure mode is written out in the runbook so the operator knows what a wrong result means rather than just that it is wrong. |
| T-06-S12 | Spoofing | `claim_waitlist_signup()` called with the anon or publishable key, failing in a way that reads as a broken RPC and prompting a well-meant loosening of its grant | medium | mitigate | The runbook states the function is `service_role`-only by its own grant, names the two correct call paths, and describes the wrong-key failure so it is recognised as a caller error rather than an RPC defect. A prohibition forbids the anon call. |
| T-06-S13 | Tampering | A raw sequence-restart statement substituted for `reset_waitlist_founder_sequence(1)`, producing a different next-value than the RPC's `setval` with its third argument false | medium | mitigate | The runbook explains the argument in terms of the next issued rank rather than presenting it as a magic number, states that Phase 1 shipped the RPC naming this phase as its consumer, and a prohibition forbids the substitution. |
| T-06-S7 | Repudiation | The activation recorded as performed without the literal outputs that prove it | high | mitigate | T-06-09's acceptance criteria require pasted output for every one of the five steps, and plan 06-04's T-06-10 carries a precondition that halts on a bare approval. |
| T-06-S8 | Elevation of Privilege | The pre-existing `anon`-executable `is_coach_of()` / `redeem_invitation_code()` / `peek_invitation()` gap in production | high | transfer | Transferred to a future, separate security-response phase per `STATE.md` and `06-CONTEXT.md`'s Deferred Ideas. Not remediated here. The two RPCs this plan calls both carry the three-role `REVOKE EXECUTE … FROM PUBLIC, anon, authenticated` form, so this plan neither reproduces nor widens the gap. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This phase installs zero packages — `06-RESEARCH.md`'s Package Legitimacy Audit records the gate as not applicable because no package-manager install step exists anywhere in this phase's scope. Any install command appearing during execution is out of contract and halts the task. |
</threat_model>

<verification>
- `scripts/founder-offer-go-live/RUNBOOK.md` carries numbered sections 7, 8 and 9 in that order and passes T-06-07's literal-presence and ordering check.
- The cleanup decision is recorded with the selecting person, the timestamp, the observed pre-state row count, and whether any entry point was publicly linked.
- `claim_waitlist_signup()` returned `founder_rank` 1 against production.
- `SELECT count(*) FROM public.waitlist_signups;` returned 0 with a recorded wall-clock time, before any entry point was publicly linked.
- All four legal-page URL checks returned 200, all four before the flag `UPDATE`.
- `jsonb_typeof` on the flag value reports `boolean`.
- `git status --porcelain supabase/ backend/ apps/ plugins/ packages/` is empty across all three tasks.
</verification>

<success_criteria>
- The live claim path is proven working against production for the first time in this milestone.
- The waitlist table is empty and the founder sequence will issue rank 1 next, confirmed with a timestamp that precedes the page becoming public — ROADMAP criterion 1.
- The CGU and CGV are confirmed served, in both locales, before the flag flips — ROADMAP criterion 2 and LEGAL-05.
- The capped credit behaviour is enabled and the flag is confirmed stored as a JSONB boolean — CRED-05.
- The one irreversible step was authorised at the moment of firing, against the observed state, by a named person.
</success_criteria>

<output>
Create `.planning/workstreams/lien-invite/phases/06-founder-offer-go-live/06-03-SUMMARY.md` when done.
</output>
