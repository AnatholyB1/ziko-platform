---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/coach/vocal/vocalReducer.ts
  - apps/web/src/components/coach/vocal/vocalReducer.test.ts
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03

must_haves:
  truths:
    - "vocalReducer VALIDATE action transitions review → structuring (not idle)"
    - "vocalReducer STRUCTURE_SUCCESS transitions structuring → card-ready with card and editedCard payloads"
    - "vocalReducer STRUCTURE_ERROR transitions structuring → structuring-error with message"
    - "vocalReducer SECTION_EDIT updates editedCard for the named section key"
    - "vocalReducer TAG_TOGGLE adds an absent tag and removes a present tag from editedCard.tags"
    - "vocalReducer START_SAVING transitions card-ready and card-editing → card-saving"
    - "vocalReducer SAVE_COMPLETE transitions card-saving → card-saved"
    - "vocalReducer RESET transitions card-saved → idle"
    - "All 8 reducer tests pass; the old 'review → idle on VALIDATE' test is updated to assert structuring"
  artifacts:
    - path: apps/web/src/components/coach/vocal/vocalReducer.ts
      provides: "Extended VocalState union (5 new statuses) + VocalAction union (6 new action types) + reducer cases"
      exports: ["VocalState", "VocalAction", "vocalReducer", "StructuredCard", "CardSection", "TagKey"]
    - path: apps/web/src/components/coach/vocal/vocalReducer.test.ts
      provides: "8 state-transition unit tests (1 updated + 7 new)"
      contains: "VALIDATE → structuring"
  key_links:
    - from: vocalReducer.test.ts
      to: vocalReducer.ts
      via: "named import"
      pattern: "from './vocalReducer'"
---

<objective>
Extend the vocalReducer state machine and its unit tests with all Phase 02 types and transitions. This is the Wave 1 foundation — Plans 02-03 and 02-04 depend on these types being exported.

Purpose: Phase 01 left VALIDATE wired to return idle. Phase 02 reroutes it to structuring and adds 5 new states (structuring, card-ready, card-editing, card-saving, card-saved, structuring-error) with 6 new action types. Tests must be RED before implementation (TDD discipline), then GREEN after.

Output: Extended vocalReducer.ts with all types exported; vocalReducer.test.ts with all 8 tests green.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-vocal/ROADMAP.md
@.planning/workstreams/retour-vocal/REQUIREMENTS.md
@.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-RESEARCH.md
@.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend vocalReducer types + transitions, update tests RED then GREEN</name>
  <files>
    apps/web/src/components/coach/vocal/vocalReducer.ts,
    apps/web/src/components/coach/vocal/vocalReducer.test.ts
  </files>

  <read_first>
    - apps/web/src/components/coach/vocal/vocalReducer.ts — current 65-line state machine to extend (DO NOT discard Phase 01 cases)
    - apps/web/src/components/coach/vocal/vocalReducer.test.ts — existing 7 tests; line 56-63 is the VALIDATE test that must change
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — "Implementation Notes" section: exact VocalState union, StructuredCard interface, CardSection type, TagKey type
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-RESEARCH.md — "Pattern 3: vocalReducer extension" section for full action list
  </read_first>

  <behavior>
    Step A — Add 7 new failing tests to vocalReducer.test.ts BEFORE touching the reducer (RED phase):
    - Update existing test at line 56: change `expect(next.status).toBe('idle')` → `expect(next.status).toBe('structuring')`. Also assert `(next as any).transcript === 'Hello'`.
    - New test: structuring + STRUCTURE_SUCCESS → card-ready; assert next.status === 'card-ready', next.card.context === 'ctx', next.editedCard.context === 'ctx'.
    - New test: structuring + STRUCTURE_ERROR → structuring-error; assert next.status === 'structuring-error', next.message === 'err msg', next.transcript === 'Hello'.
    - New test: card-ready + SECTION_EDIT(section: 'context', value: 'edited') → card-ready; assert next.editedCard.context === 'edited', next.card.context unchanged.
    - New test: card-ready + TAG_TOGGLE('force') when 'force' absent → card-ready; editedCard.tags includes 'force'.
    - New test: card-ready + TAG_TOGGLE('force') when 'force' present → card-ready; editedCard.tags does NOT include 'force'.
    - New test: card-ready + START_SAVING → card-saving; assert next.status === 'card-saving', next.editedCard present.
    - New test: card-saving + SAVE_COMPLETE → card-saved; assert next.status === 'card-saved'.
    - New test: card-saved + RESET → idle; assert next.status === 'idle'.

    Run tests — all new tests and the updated VALIDATE test must be RED (cannot satisfy the new assertions yet).

    Step B — Extend vocalReducer.ts (GREEN phase):

    Types to ADD to VocalState union (keep all Phase 01 states unchanged):
    - `| { status: 'structuring'; transcript: string }`
    - `| { status: 'card-ready'; card: StructuredCard; editedCard: StructuredCard }`
    - `| { status: 'card-editing'; card: StructuredCard; editedCard: StructuredCard; activeSection: CardSection }`
    - `| { status: 'card-saving'; editedCard: StructuredCard }`
    - `| { status: 'card-saved' }`
    - `| { status: 'structuring-error'; transcript: string; message: string }`

    Types to ADD to VocalAction union (keep all Phase 01 actions unchanged):
    - `| { type: 'STRUCTURE_SUCCESS'; card: StructuredCard }`
    - `| { type: 'STRUCTURE_ERROR'; message: string }`
    - `| { type: 'SECTION_EDIT'; section: CardSection; value: string }`
    - `| { type: 'TAG_TOGGLE'; tag: TagKey }`
    - `| { type: 'START_SAVING' }`
    - `| { type: 'SAVE_COMPLETE' }`
    - `| { type: 'RESET' }`

    Supporting types to EXPORT from the same file:
    - `export type CardSection = 'context' | 'strengths' | 'corrections' | 'next_steps'`
    - `export type TagKey = 'force' | 'technique' | 'mental' | 'cardio' | 'recuperation'`
    - `export interface StructuredCard { context: string; strengths: string; corrections: string; next_steps: string; tags: TagKey[]; }`

    Reducer cases to ADD or CHANGE:
    - VALIDATE case: change `return { status: 'idle' }` → `return { status: 'structuring', transcript: state.transcript }` (state guard `state.status !== 'review'` stays)
    - STRUCTURE_SUCCESS: guard `state.status !== 'structuring'`; return `{ status: 'card-ready', card: action.card, editedCard: action.card }`
    - STRUCTURE_ERROR: guard `state.status !== 'structuring'`; return `{ status: 'structuring-error', transcript: state.transcript, message: action.message }`
    - SECTION_EDIT: guard `state.status !== 'card-ready' && state.status !== 'card-editing'`; return spread state with `editedCard: { ...state.editedCard, [action.section]: action.value }`, keep status as-is
    - TAG_TOGGLE: same guard as SECTION_EDIT; toggle tag in `editedCard.tags` (add if absent, remove if present); keep status
    - START_SAVING: guard `state.status !== 'card-ready' && state.status !== 'card-editing'`; return `{ status: 'card-saving', editedCard: state.editedCard }`
    - SAVE_COMPLETE: guard `state.status !== 'card-saving'`; return `{ status: 'card-saved' }`
    - RESET: no status guard; return `{ status: 'idle' }`
  </behavior>

  <action>
    TDD sequence:

    1. Update vocalReducer.test.ts — add 8 test cases (update 1 existing + 7 new) covering all Phase 02 transitions listed in behavior above. Use a minimal mockCard fixture: `const mockCard: StructuredCard = { context: 'ctx', strengths: 'str', corrections: 'cor', next_steps: 'ns', tags: [] }`. Import StructuredCard, CardSection, TagKey from './vocalReducer' (they will not exist yet — test file will compile-error until Step B).

    2. Run tests to confirm RED: `cd C:/ziko-platform/apps/web && npx vitest run src/components/coach/vocal/vocalReducer.test.ts --passWithNoTests 2>&1 | tail -20`

    3. Extend vocalReducer.ts with new exports, union type additions, and reducer cases per behavior above. Do not remove Phase 01 cases (START_RECORDING, STOP_RECORDING, TRANSCRIPTION_SUCCESS, TRANSCRIPTION_ERROR, RETRY, RELAUNCH). Do not remove the `default: return state` fallthrough.

    4. Run tests to confirm GREEN: same command. All tests including Phase 01 tests must pass.
  </action>

  <verify>
    <automated>cd C:/ziko-platform/apps/web && npx vitest run src/components/coach/vocal/vocalReducer.test.ts --passWithNoTests 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - Test output shows 0 failures, at minimum 15 tests passed (8 original Phase 01 tests + 1 updated + 7 new — the original 7th test is now updated so counts still as the same file's test, result is 8+7=15)
    - `grep -c "structuring" apps/web/src/components/coach/vocal/vocalReducer.ts` returns at least 4
    - `grep "export.*StructuredCard" apps/web/src/components/coach/vocal/vocalReducer.ts` returns a match
    - `grep "export.*CardSection" apps/web/src/components/coach/vocal/vocalReducer.ts` returns a match
    - `grep "export.*TagKey" apps/web/src/components/coach/vocal/vocalReducer.ts` returns a match
    - `grep "VALIDATE" apps/web/src/components/coach/vocal/vocalReducer.test.ts | grep structuring` returns a match (updated assertion)
    - Phase 01 RELAUNCH and RETRY cases still present: `grep -c "case 'RELAUNCH'" apps/web/src/components/coach/vocal/vocalReducer.ts` returns 1
  </acceptance_criteria>

  <done>All 15 vocalReducer tests green. StructuredCard, CardSection, TagKey exported. VALIDATE → structuring. All 6 new action cases handled in reducer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → reducer | Pure function, no I/O, no trust boundary — fully controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | npm/vitest install | accept | No new packages — vitest already installed; no slopcheck required |
</threat_model>

<verification>
Run full suite after plan:
`cd C:/ziko-platform/apps/web && npx vitest run src/components/coach/vocal/ --passWithNoTests`

All tests in the vocal/ directory must pass.
</verification>

<success_criteria>
- vocalReducer.ts exports StructuredCard, CardSection, TagKey as named types
- VocalState union contains all 6 Phase 02 status variants
- VocalAction union contains all 7 Phase 02 action types (6 new + RESET)
- VALIDATE case returns { status: 'structuring', transcript: state.transcript }
- All 15 tests in vocalReducer.test.ts pass
- Phase 01 tests unchanged in behavior (except the VALIDATE test updated to expect 'structuring')
</success_criteria>

<output>
Create `.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-01-SUMMARY.md` when done.
</output>
