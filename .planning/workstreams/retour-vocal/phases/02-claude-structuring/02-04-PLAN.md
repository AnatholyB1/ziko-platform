---
phase: "02"
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - apps/web/src/components/coach/vocal/FeedbackCard.tsx
  - apps/web/src/components/coach/vocal/CardSection.tsx
  - apps/web/src/components/coach/vocal/TagChip.tsx
  - apps/web/src/components/coach/vocal/VocalCardReady.tsx
autonomous: true
requirements:
  - STRUCT-02
  - STRUCT-03

must_haves:
  truths:
    - "FeedbackCard renders the card header (sparkles icon + 'Retour structuré'), 4 section blocks, tags row, and footer action row"
    - "CardSection renders read-only block (bg #F0EFE9, pointer cursor) and switches to a focused textarea on click; blur or another section click deactivates it"
    - "TagChip renders all 5 tags; selected chips are bg #1C1A17 white text; deselected chips are bg #F0EFE9 muted text; click dispatches TAG_TOGGLE"
    - "Sauvegarder button dispatches START_SAVING then triggers a 500ms fake save then dispatches SAVE_COMPLETE (Phase 03 will replace with real endpoint)"
    - "card-saved state shows success block with check-circle icon + 'Retour sauvegardé.' + 'Nouveau retour' ghost button"
    - "GSAP card entrance animation runs on mount: y:16 opacity:0 → default over 250ms power2.out"
    - "GSAP sections stagger on mount: y:8 opacity:0, stagger 0.04, 200ms power2.out"
    - "GSAP tags pop on mount: scale:0.85 opacity:0, stagger 0.03, 150ms back.out(1.4)"
    - "VocalCardReady replaced from stub to real FeedbackCard wrapper"
  artifacts:
    - path: apps/web/src/components/coach/vocal/FeedbackCard.tsx
      provides: "Full 5-section card with header, sections, tags, and footer"
      contains: "Retour structuré"
      exports: ["FeedbackCard"]
    - path: apps/web/src/components/coach/vocal/CardSection.tsx
      provides: "Read/edit toggle section block"
      contains: "activeSection"
      exports: ["CardSection"]
    - path: apps/web/src/components/coach/vocal/TagChip.tsx
      provides: "Toggleable pill chip for each of 5 tags"
      contains: "TAG_LABELS"
      exports: ["TagChip"]
    - path: apps/web/src/components/coach/vocal/VocalCardReady.tsx
      provides: "Full card-ready/editing/saving/saved state machine shell"
      contains: "FeedbackCard"
  key_links:
    - from: VocalCardReady.tsx
      to: FeedbackCard.tsx
      via: "import and render"
      pattern: "FeedbackCard"
    - from: FeedbackCard.tsx
      to: CardSection.tsx
      via: "import and render ×4"
      pattern: "CardSection"
    - from: FeedbackCard.tsx
      to: TagChip.tsx
      via: "import and render ×5"
      pattern: "TagChip"
    - from: FeedbackCard.tsx
      to: vocalReducer.ts
      via: "dispatch VocalAction"
      pattern: "SECTION_EDIT|TAG_TOGGLE|START_SAVING|SAVE_COMPLETE"
---

<objective>
Build FeedbackCard, CardSection, and TagChip components, and replace the VocalCardReady stub from Plan 02-03 with the full implementation. This delivers the complete 5-section editable card UI with GSAP animations and the fake-save flow (SAVE_COMPLETE dispatched after 500ms timeout).

Purpose: Completes STRUCT-02 (rendered card) and STRUCT-03 (inline editing before save). Plan 02-03 already wired the VocalCardReady shell to VocalRetourPanel — this plan fills in the shell with real content.

Output: FeedbackCard renders 5 sections and tags. CardSection handles read/edit toggling. TagChip handles toggle state. VocalCardReady covers all 4 card states (card-ready, card-editing, card-saving, card-saved). Fake save via setTimeout 500ms.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-vocal/ROADMAP.md
@.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md
@.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-RESEARCH.md
</context>

<interfaces>
From apps/web/src/components/coach/vocal/vocalReducer.ts (Plan 02-01):

```typescript
export type CardSection = 'context' | 'strengths' | 'corrections' | 'next_steps';
export type TagKey = 'force' | 'technique' | 'mental' | 'cardio' | 'recuperation';
export interface StructuredCard {
  context: string;
  strengths: string;
  corrections: string;
  next_steps: string;
  tags: TagKey[];
}
```

VocalCardReady props (from Plan 02-03 shell):
```typescript
type CardState = Extract<VocalState,
  { status: 'card-ready' | 'card-editing' | 'card-saving' | 'card-saved' }>;
{ state: CardState; dispatch: React.Dispatch<VocalAction> }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TagChip + CardSection components</name>
  <files>
    apps/web/src/components/coach/vocal/TagChip.tsx,
    apps/web/src/components/coach/vocal/CardSection.tsx
  </files>

  <read_first>
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — "Tags section" pixel spec (chip dimensions, selected/deselected colors, transition), "Section block" pixel spec (read-only bg, border, radius, padding, min-height, hover border, cursor pointer), "Active textarea" spec (border 1.5px #1C1A17, bg white, box-shadow, resize vertical, outline none)
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — Motion Design table: "Tag toggle (on)" and "Tag toggle (off)" GSAP patterns; "Section click to edit" animation
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — Accessibility section: role="checkbox" aria-checked on chips; role="button" aria-label on read-only section; aria-label on textarea
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — Copywriting: TAG_LABELS mapping (recuperation → 'récupération')
  </read_first>

  <action>
    TagChip.tsx:
    - 'use client'
    - Imports: React, useRef from 'react'; gsap from 'gsap'; TagKey from './vocalReducer'
    - Props: `{ tag: TagKey; selected: boolean; onToggle: () => void }`
    - TAG_LABELS constant inside file: `{ force: 'force', technique: 'technique', mental: 'mental', cardio: 'cardio', recuperation: 'récupération' }`
    - Render: button element (not div) so keyboard accessible
      - role="checkbox" aria-checked={selected}
      - aria-label={TAG_LABELS[tag]}
      - onClick: call GSAP animation then onToggle
      - Height: 28px, paddingLeft/paddingRight: 12px, borderRadius: 9999px (pill — exception per MOODBOARD)
      - fontSize: 12, fontWeight: 400
      - Selected styles: backgroundColor '#1C1A17', color '#FFFFFF', border: 'none'
      - Deselected styles: backgroundColor '#F0EFE9', color '#6B6963', border: '1px solid #E2E0DA'
      - CSS transition: `backgroundColor 150ms ease, color 150ms ease` (via inline style — add `transition: 'background-color 150ms ease, color 150ms ease'`)
      - GSAP on toggle ON: `gsap.fromTo(chipRef.current, { scale: 0.85 }, { scale: 1, duration: 0.15, ease: 'back.out(1.4)' })`
      - GSAP on toggle OFF: `gsap.to(chipRef.current, { scale: 0.95, duration: 0.05, yoyo: true, repeat: 1 })`
      - Run animation in the click handler before calling onToggle

    CardSection.tsx:
    - 'use client'
    - Imports: React, useRef, useEffect from 'react'; gsap from 'gsap'; CardSection as CardSectionKey from './vocalReducer'
    - Props: `{ sectionKey: CardSectionKey; label: string; value: string; isEditing: boolean; disabled: boolean; onEdit: (sectionKey: CardSectionKey) => void; onChange: (value: string) => void }`
    - Container: div with className "card-section", marginBottom 0 (gap handled by parent FeedbackCard)
    - Section label: p, fontSize 12, fontWeight 400, color '#6B6963', marginBottom 8
    - Read-only block (when !isEditing):
      - div with role="button" aria-label={`Modifier : ${label}`} tabIndex={0}
      - onClick: if !disabled → onEdit(sectionKey)
      - onKeyDown: Enter/Space triggers onEdit
      - Style: backgroundColor '#F0EFE9', border '1px solid #E2E0DA', borderRadius 6, padding 12, minHeight 60, fontSize 16, fontWeight 400, color '#1C1A17', lineHeight 1.6, cursor: disabled ? 'default' : 'pointer'
      - Hover (via onMouseEnter/Leave): border-color '#1C1A17' (when !disabled); transition 150ms
    - Edit block (when isEditing):
      - textarea with aria-label={label}
      - value={value} onChange={(e) => onChange(e.target.value)}
      - autoFocus={true}
      - Style: width '100%', backgroundColor '#FFFFFF', border '1.5px solid #1C1A17', borderRadius 6, padding 12, minHeight 80, fontSize 16, fontWeight 400, color '#1C1A17', lineHeight 1.6, resize 'vertical', outline 'none', boxShadow '0 0 0 3px rgba(28,26,23,0.06)', boxSizing 'border-box'
      - No onBlur dismiss — editing is dismissed by clicking another section or [Sauvegarder], not blur (per UI-SPEC "No explicit 'Done' or 'Cancel' per-section")
      - GSAP entrance when isEditing becomes true: use useEffect([isEditing]) + `gsap.from(textareaRef.current, { opacity: 0, y: 4, duration: 0.15, ease: 'power2.out' })`
  </action>

  <verify>
    <automated>cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -i "TagChip\|CardSection" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "role=\"checkbox\"" apps/web/src/components/coach/vocal/TagChip.tsx` returns a match
    - `grep "récupération" apps/web/src/components/coach/vocal/TagChip.tsx` returns a match (accented char in TAG_LABELS)
    - `grep "9999px\|9999" apps/web/src/components/coach/vocal/TagChip.tsx` returns a match (pill shape)
    - `grep "role=\"button\"" apps/web/src/components/coach/vocal/CardSection.tsx` returns a match
    - `grep "aria-label.*Modifier" apps/web/src/components/coach/vocal/CardSection.tsx` returns a match
    - `grep "1.5px solid" apps/web/src/components/coach/vocal/CardSection.tsx` returns a match (focus border)
    - `grep "resize.*vertical" apps/web/src/components/coach/vocal/CardSection.tsx` returns a match
    - TypeScript: `cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -c "TagChip\|CardSection"` returns 0
    - `grep "use client" apps/web/src/components/coach/vocal/TagChip.tsx` returns a match
    - `grep "use client" apps/web/src/components/coach/vocal/CardSection.tsx` returns a match
  </acceptance_criteria>

  <done>TagChip renders 5 toggle chips with accessible role + GSAP bounce. CardSection toggles between read-only (clickable div) and edit (textarea with focus ring) per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: FeedbackCard + VocalCardReady full implementation</name>
  <files>
    apps/web/src/components/coach/vocal/FeedbackCard.tsx,
    apps/web/src/components/coach/vocal/VocalCardReady.tsx
  </files>

  <read_first>
    - apps/web/src/components/coach/vocal/VocalCardReady.tsx — current stub from Plan 02-03; this task replaces its JSX content
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — "State 2: card-ready" full pixel spec (card container, header row, section block, tags section, footer action row); "State 3: card-editing" spec; "State 4: card-saving" spec (button disabled + spinner); "State 5: card-saved" spec (success block, 'Nouveau retour' ghost button, auto-reset 3s)
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — Motion Design table: "structuring → card-ready" card entrance gsap.from(.feedback-card), sections stagger gsap.from(.card-section), tags pop gsap.from(.tag-chip)
    - apps/web/src/components/coach/vocal/CardSection.tsx — props interface (just created)
    - apps/web/src/components/coach/vocal/TagChip.tsx — props interface (just created)
  </read_first>

  <action>
    FeedbackCard.tsx:
    - 'use client'
    - Imports: React, useRef, useEffect from 'react'; gsap from 'gsap'; Sparkles, Loader2 from 'lucide-react'; CardSection component from './CardSection'; TagChip component from './TagChip'; StructuredCard, CardSection as CardSectionKey, TagKey, VocalAction from './vocalReducer'
    - Props: `{ card: StructuredCard; editedCard: StructuredCard; activeSection: CardSectionKey | null; isSaving: boolean; dispatch: React.Dispatch<VocalAction> }`
    - Sections config (array, in order): `[{ key: 'context', label: 'Contexte séance' }, { key: 'strengths', label: 'Points forts' }, { key: 'corrections', label: 'Corrections' }, { key: 'next_steps', label: 'Prochaines étapes' }]` typed as `Array<{ key: CardSectionKey; label: string }>`
    - ALL_TAGS: `['force', 'technique', 'mental', 'cardio', 'recuperation'] as const satisfies TagKey[]`
    - cardRef: useRef for the card container div
    - sectionsRef: useRef<HTMLDivElement>(null) for the sections container
    - tagsRef: useRef<HTMLDivElement>(null) for the tags row

    GSAP entrance useEffect (runs once on mount):
    - `gsap.from(cardRef.current, { y: 16, opacity: 0, duration: 0.25, ease: 'power2.out' })`
    - `gsap.from(cardRef.current.querySelectorAll('.card-section'), { y: 8, opacity: 0, duration: 0.2, stagger: 0.04, ease: 'power2.out' })` — delay slightly after card entrance
    - `gsap.from(cardRef.current.querySelectorAll('.tag-chip'), { scale: 0.85, opacity: 0, duration: 0.15, stagger: 0.03, ease: 'back.out(1.4)', delay: 0.15 })`

    Card container div (ref=cardRef, className="feedback-card"):
    - backgroundColor '#FFFFFF', border '1px solid #E2E0DA', borderRadius 8, boxShadow '0 4px 8px rgba(28,26,23,0.08)', padding 24, display 'flex', flexDirection 'column', gap 16

    Card header row (before sections):
    - flex, alignItems center, gap 8, borderBottom '1px solid #E2E0DA', paddingBottom 16, marginBottom 4
    - Sparkles icon: size 14, color '#6B6963'
    - span: "Retour structuré", fontSize 12, fontWeight 600, color '#6B6963', textTransform 'uppercase', letterSpacing '0.05em'

    Sections: map over sections config; render CardSection for each:
    - key={s.key}
    - sectionKey={s.key}
    - label={s.label}
    - value={editedCard[s.key]}
    - isEditing={activeSection === s.key}
    - disabled={isSaving}
    - onEdit={(key) => dispatch({ type: 'SECTION_EDIT', section: key, value: editedCard[key] })}  — Wait: onEdit should dispatch to make section active in state machine. But SECTION_EDIT changes the value, not the active section. Correct approach: the CardSection's onEdit fires a state change to 'card-editing' with activeSection. The reducer needs a SET_ACTIVE_SECTION action, or... Actually, re-read UI-SPEC "Implementation Notes: Section Click-to-Edit Pattern": `const [isEditing, setIsEditing] = useState(false)` — local state. The activeSection in 'card-editing' state IS the reducer state, but the UI-SPEC also shows a local useState. Resolution: Use LOCAL state in FeedbackCard for which section is active (activeEditSection: CardSectionKey | null), not the reducer. The reducer's card-editing state with activeSection is used for persistence of which section is open. But for simplicity in Phase 02, track active section locally in FeedbackCard with useState, and dispatch SECTION_EDIT when value changes. Do NOT dispatch to change the active section — just use local state. The reducer's 'card-editing' status is set when any section is being edited: onEdit dispatches nothing for section activation; instead, FeedbackCard maintains local `activeEditSection` state. When any section becomes active, FeedbackCard can optionally stay in 'card-ready' status or... the UI-SPEC "Trigger: Coach clicks on any section content block in card-ready state → card-editing". For Phase 02 this UX detail can be handled via local state only — the reducer's card-editing/card-ready distinction matters for the save button behavior (START_SAVING works from both). So: use local useState `[activeEditSection, setActiveEditSection]` in FeedbackCard. The props simplify: no activeSection prop needed from outside — remove from FeedbackCard props. VocalCardReady passes card, editedCard, isSaving, dispatch only.
    - onEdit={(key) => setActiveEditSection(key)}
    - onChange={(value) => dispatch({ type: 'SECTION_EDIT', section: s.key, value })}
    - Add className="card-section" wrapper div around each CardSection for GSAP targeting

    Tags section:
    - Section label: p, "Tags", fontSize 12, fontWeight 400, color '#6B6963', marginBottom 8
    - Tags row div (className="tag-chip-row"): display flex, flexWrap 'wrap', gap 8
    - Map ALL_TAGS: TagChip for each
      - key={tag}
      - className="tag-chip"
      - tag={tag}
      - selected={editedCard.tags.includes(tag)}
      - onToggle={() => dispatch({ type: 'TAG_TOGGLE', tag })}
      - disabled={isSaving}

    Card footer (action row):
    - borderTop '1px solid #E2E0DA', paddingTop 16, marginTop 4
    - div: display flex, justifyContent flex-end
    - [Sauvegarder] button:
      - disabled when isSaving
      - onClick: dispatch START_SAVING (isSaving becomes true via VocalCardReady handleSave)
      - backgroundColor '#FF5C1A', color '#FFFFFF', height 40, paddingLeft 24, paddingRight 24, borderRadius 8, fontSize 14, fontWeight 600, border 'none'
      - Hover: backgroundColor '#E5521A' (via onMouseEnter/Leave when !isSaving)
      - Disabled: opacity 0.5, cursor 'not-allowed'
      - Content when isSaving: flex row with Loader2 size=16 color="white" className="animate-spin" + " Sauvegarde…" (the animate-spin class should be defined — check if Tailwind is configured; if not, use GSAP: `gsap.to(loaderRef.current, { rotation: 360, duration: 0.8, ease: 'linear', repeat: -1 })`)
      - Content when !isSaving: "Sauvegarder"
      - GSAP scale feedback on click (when !isSaving): `gsap.to(saveRef.current, { scale: 0.97, duration: 0.1, yoyo: true, repeat: 1 })`

    VocalCardReady.tsx (replace stub content):
    - Read the stub file from Plan 02-03
    - Props: `{ state: CardState; dispatch: React.Dispatch<VocalAction> }`
    - Import FeedbackCard from './FeedbackCard'; CheckCircle from 'lucide-react'
    - Determine isSaving: `state.status === 'card-saving'`
    - Determine editedCard: `state.status === 'card-saved' ? undefined : state.editedCard` (card-saved has no editedCard)
    - handleSave: calls dispatch START_SAVING then sets a 500ms timeout → dispatch SAVE_COMPLETE. This is the FAKE SAVE per key constraint 5 — Phase 03 wires the real endpoint.

    Conditional render in VocalCardReady:
    - When state.status === 'card-saved': render success block
      - Container: max-w 640px, mx-auto, mt-48px, display flex, flexDirection column, gap 16
      - Success block: div role="status", bg '#F0FDF4', border '1px solid #BBF7D0', radius 8, padding 16
        - Icon row: flex, alignItems center, gap 8: CheckCircle size=18 color="#22C55E" + p "Retour sauvegardé." fontSize 14, fontWeight 600, color '#1C1A17'
        - Body: p "La card structurée a été enregistrée avec succès pour cet athlète.", fontSize 12, fontWeight 400, color '#6B6963', marginTop 4
      - Action row: div display flex, justifyContent flex-end
        - [Nouveau retour] ghost button: border '1px solid #E2E0DA', bg '#FFFFFF', color '#1C1A17', h 40, px 24, radius 8, fontSize 14, fontWeight 400
          - onClick: dispatch({ type: 'RESET' })
          - Hover: bg '#F0EFE9'
      - GSAP entrance: gsap.from(successBlockRef.current, { y: 12, opacity: 0, duration: 0.2, ease: 'power2.out' })
    - When state.status is card-ready, card-editing, or card-saving: render
      - Container: max-w 640px, mx-auto
      - FeedbackCard with card={state.card}, editedCard={state.editedCard}, isSaving={isSaving}, dispatch={dispatch}
      - handleSave is passed to FeedbackCard via onSave prop, or: [Sauvegarder] button's onClick in FeedbackCard calls dispatch({ type: 'START_SAVING' }) directly, and VocalCardReady has a useEffect watching state.status === 'card-saving' to fire the fake save timeout.

    Fake save useEffect in VocalCardReady:
    ```
    useEffect(() => {
      if (state.status !== 'card-saving') return;
      const timer = setTimeout(() => dispatch({ type: 'SAVE_COMPLETE' }), 500);
      return () => clearTimeout(timer);
    }, [state.status]);
    ```
  </action>

  <verify>
    <automated>cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -i "FeedbackCard\|VocalCardReady\|TagChip\|CardSection" | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep "Retour structuré" apps/web/src/components/coach/vocal/FeedbackCard.tsx` returns a match
    - `grep "Sauvegarder" apps/web/src/components/coach/vocal/FeedbackCard.tsx` returns at least 1 match
    - `grep "feedback-card" apps/web/src/components/coach/vocal/FeedbackCard.tsx` returns a match (className for GSAP targeting)
    - `grep "card-section" apps/web/src/components/coach/vocal/FeedbackCard.tsx` returns a match (className for GSAP targeting)
    - `grep "tag-chip" apps/web/src/components/coach/vocal/FeedbackCard.tsx` returns a match (className for GSAP targeting)
    - `grep "gsap.from.*feedback-card\|gsap.from.*card-section\|gsap.from.*tag-chip" apps/web/src/components/coach/vocal/FeedbackCard.tsx` returns at least 1 match (may span multiple lines — check each separately)
    - `grep "SAVE_COMPLETE" apps/web/src/components/coach/vocal/VocalCardReady.tsx` returns a match (fake save dispatch)
    - `grep "setTimeout" apps/web/src/components/coach/vocal/VocalCardReady.tsx` returns a match (500ms fake save)
    - `grep "Retour sauvegardé" apps/web/src/components/coach/vocal/VocalCardReady.tsx` returns a match
    - `grep "Nouveau retour" apps/web/src/components/coach/vocal/VocalCardReady.tsx` returns a match
    - `grep "data-testid.*vocal-card-ready" apps/web/src/components/coach/vocal/VocalCardReady.tsx` returns no match — stub placeholder div removed
    - TypeScript: `cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -v "^$" | grep -c "vocal/"` returns 0
    - Full vocal suite: `cd C:/ziko-platform/apps/web && npx vitest run src/components/coach/vocal/ --passWithNoTests 2>&1 | grep -c "failed"` returns 0
  </acceptance_criteria>

  <done>FeedbackCard renders 5 sections + tags + footer with GSAP animations. CardSection + TagChip wired. VocalCardReady handles all 4 card states including 500ms fake save and card-saved success block. TypeScript clean. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StructuredCard data → DOM | Claude-generated content rendered as text — not as HTML (React renders text nodes, no dangerouslySetInnerHTML) |
| Dispatch → reducer | Pure function, fully controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Claude output in section textarea | accept | Content displayed as text values in textarea; React escapes all content; no innerHTML usage |
| T-02-04-02 | Denial of Service | GSAP animation on large card | accept | All animations are one-shot (not loops); card content is bounded by API schema (5 fields) |
| T-02-04-SC | Tampering | npm installs | accept | No new packages — gsap, lucide-react, React already installed |
</threat_model>

<verification>
Full vocal directory test suite after plan:
`cd C:/ziko-platform/apps/web && npx vitest run src/components/coach/vocal/ --passWithNoTests`

TypeScript clean check:
`cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20`

Manual smoke test:
1. Open coach CRM → client sheet → Retour vocal tab
2. Record audio, transcribe, click [Valider]
3. Spinner "Structuration en cours…" must appear
4. After Claude returns, card with 5 sections must animate in (y:16 entrance)
5. Click any section — textarea with dark border must appear
6. Toggle tag chips — selected chip goes dark
7. Click [Sauvegarder] — button shows spinner for ~500ms then success block appears
8. "Nouveau retour" button resets to idle
</verification>

<success_criteria>
- FeedbackCard renders card header (sparkles + "Retour structuré"), 4 sections via CardSection, 5 tag chips via TagChip, footer with Sauvegarder button
- GSAP: card entrance y:16 250ms, sections stagger y:8 0.04s, tags pop scale:0.85 0.03s stagger
- CardSection click-to-edit: read-only div → textarea; no blur dismiss; clicking another section switches focus via local state
- TagChip dispatches TAG_TOGGLE on click; selected = bg #1C1A17 white text; deselected = bg #F0EFE9 muted
- VocalCardReady fake save: START_SAVING → 500ms → SAVE_COMPLETE
- card-saved shows success block with check-circle + "Retour sauvegardé." + "Nouveau retour" ghost button
- "Nouveau retour" dispatches RESET → state returns to idle
- All 4 card states (card-ready, card-editing, card-saving, card-saved) render correctly
- TypeScript clean across all new files
- All vocalReducer tests still green
</success_criteria>

<output>
Create `.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-04-SUMMARY.md` when done.
</output>
