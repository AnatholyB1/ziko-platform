---
phase: "02"
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/web/src/components/coach/vocal/vocalReducer.ts
  - apps/web/src/components/coach/vocal/VocalRetourPanel.tsx
  - apps/web/src/components/coach/vocal/VocalStructuring.tsx
  - apps/web/src/components/coach/vocal/VocalStructuringError.tsx
  - apps/web/src/components/coach/vocal/VocalCardReady.tsx
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03

must_haves:
  truths:
    - "When status transitions to 'structuring', VocalRetourPanel fires POST /api/coach/voice/structure with clientId as athlete_id"
    - "VocalStructuring renders spinner + heading + body copy exactly per UI-SPEC (32px spinner, accent border-top, no interactive elements)"
    - "VocalStructuringError renders error block with Réessayer and Retour buttons; Réessayer re-fires the structure call; Retour dispatches RELAUNCH back to review"
    - "VocalCardReady is a shell component that receives the state and dispatch and delegates to FeedbackCard (Plan 02-04)"
    - "void clientId line removed from VocalRetourPanel; clientId used in POST body"
    - "GSAP entrance animations run in useEffect on component mount per UI-SPEC motion contract"
  artifacts:
    - path: apps/web/src/components/coach/vocal/VocalRetourPanel.tsx
      provides: "useEffect on status=structuring firing POST; renders VocalStructuring, VocalStructuringError, VocalCardReady stubs"
      contains: "handleStructure"
    - path: apps/web/src/components/coach/vocal/VocalStructuring.tsx
      provides: "Loading spinner UI — structuring state"
      contains: "Structuration en cours"
    - path: apps/web/src/components/coach/vocal/VocalStructuringError.tsx
      provides: "Error UI — structuring-error state"
      contains: "La structuration a échoué"
    - path: apps/web/src/components/coach/vocal/VocalCardReady.tsx
      provides: "Shell wrapper for card states — delegates to FeedbackCard (Plan 02-04)"
      contains: "card-ready"
  key_links:
    - from: VocalRetourPanel.tsx
      to: /api/coach/voice/structure
      via: "fetch in handleStructure called from useEffect on status=structuring"
      pattern: "handleStructure"
    - from: VocalRetourPanel.tsx
      to: VocalStructuring.tsx
      via: "conditional render on state.status === 'structuring'"
      pattern: "VocalStructuring"
    - from: VocalRetourPanel.tsx
      to: VocalStructuringError.tsx
      via: "conditional render on state.status === 'structuring-error'"
      pattern: "VocalStructuringError"
    - from: VocalRetourPanel.tsx
      to: VocalCardReady.tsx
      via: "conditional render on card-ready, card-editing, card-saving, card-saved"
      pattern: "VocalCardReady"
---

<objective>
Wire VocalRetourPanel to fire the structure API call on the structuring state, and create the VocalStructuring, VocalStructuringError, and VocalCardReady (shell) components. VocalCardReady is a stub in this plan — its internals (FeedbackCard, CardSection, TagChip) are built in Plan 02-04.

Purpose: After Plan 02-01 (reducer) and 02-02 (backend), this plan closes the state machine → UI loop. The coach sees the spinner immediately after pressing [Valider], sees errors if Claude fails, and will see the card shell once Plan 02-04 completes.

Output: 3 new component files + updated VocalRetourPanel. The `void clientId` line is removed and clientId is wired into the fetch body.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-vocal/ROADMAP.md
@.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-RESEARCH.md
@.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md
</context>

<interfaces>
From apps/web/src/components/coach/vocal/vocalReducer.ts (extended by Plan 02-01):

```typescript
export type CardSection = 'context' | 'strengths' | 'corrections' | 'next_steps';
export type TagKey = 'force' | 'technique' | 'mental' | 'cardio' | 'recuperation';
export interface StructuredCard {
  context: string;
  strengths: string;
  corrections: string;
  next_steps: string;
  tags: TagKey[];
}
export type VocalState =
  | { status: 'idle' }
  | { status: 'recording'; startedAt: number }
  | { status: 'transcribing'; blob: Blob; mimeType: string }
  | { status: 'review'; transcript: string }
  | { status: 'error'; blob: Blob; mimeType: string; message: string }
  | { status: 'structuring'; transcript: string }
  | { status: 'card-ready'; card: StructuredCard; editedCard: StructuredCard }
  | { status: 'card-editing'; card: StructuredCard; editedCard: StructuredCard; activeSection: CardSection }
  | { status: 'card-saving'; editedCard: StructuredCard }
  | { status: 'card-saved' }
  | { status: 'structuring-error'; transcript: string; message: string };
export type VocalAction =
  | { type: 'START_RECORDING' }
  | { type: 'STOP_RECORDING'; blob: Blob; mimeType: string }
  | { type: 'TRANSCRIPTION_SUCCESS'; transcript: string }
  | { type: 'TRANSCRIPTION_ERROR'; message: string }
  | { type: 'RETRY' }
  | { type: 'RELAUNCH' }
  | { type: 'VALIDATE' }
  | { type: 'STRUCTURE_SUCCESS'; card: StructuredCard }
  | { type: 'STRUCTURE_ERROR'; message: string }
  | { type: 'SECTION_EDIT'; section: CardSection; value: string }
  | { type: 'TAG_TOGGLE'; tag: TagKey }
  | { type: 'START_SAVING' }
  | { type: 'SAVE_COMPLETE' }
  | { type: 'RESET' };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: VocalStructuring + VocalStructuringError components</name>
  <files>
    apps/web/src/components/coach/vocal/VocalStructuring.tsx,
    apps/web/src/components/coach/vocal/VocalStructuringError.tsx
  </files>

  <read_first>
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — "State 1: structuring" and "State 6: structuring-error" pixel specs and copywriting contract
    - apps/web/src/components/coach/vocal/VocalTranscribing.tsx — existing spinner component as GSAP pattern reference
    - apps/web/src/components/coach/vocal/VocalReview.tsx — existing GSAP shake pattern for error block reference
  </read_first>

  <action>
    VocalStructuring.tsx:
    - 'use client' directive at top
    - Imports: React, useRef, useEffect from 'react'; gsap from 'gsap'
    - Props: none (stateless display)
    - Container: div, max-width 640px, mx-auto, px-24px, mt-48px (matches `paddingTop: 48`), display flex, flexDirection column, alignItems center, gap 0
    - Spinner: div 32x32px, border `4px solid #E2E0DA`, borderTop `4px solid #FF5C1A`, borderRadius 9999px, CSS animation spin 0.8s linear infinite. Use a `<style>` tag with `@keyframes spin { to { transform: rotate(360deg) } }` inside a wrapper fragment, OR use a className with global CSS already defined in Phase 01 — check if a global spin keyframe already exists in globals.css first before adding a duplicate.
    - Heading: p, "Structuration en cours…", fontSize 14, fontWeight 600, color '#1C1A17', textAlign center, marginTop 16
    - Body: p, "Claude analyse le retour vocal et le contexte de l'athlète. Cela prend généralement 5 à 10 secondes.", fontSize 12, fontWeight 400, color '#6B6963', textAlign center, maxWidth 300, lineHeight 1.5, marginTop 8
    - GSAP entrance on mount in useEffect: `gsap.from(containerRef.current, { opacity: 0, duration: 0.2, ease: 'power2.out' })`

    VocalStructuringError.tsx:
    - 'use client' directive
    - Imports: React, useRef, useEffect from 'react'; gsap from 'gsap'; AlertTriangle from 'lucide-react'
    - Props: `{ message: string; onRetry: () => void; onBack: () => void }`
    - Container: div, max-width 640px, mx-auto, px-0, marginTop 48, display flex, flexDirection column, gap 16
    - Error block: div with role="alert", bg '#FEF2F2', border '1px solid #FECACA', radius 8, padding 16
      - Icon row: flex, alignItems center, gap 8: AlertTriangle size=18 color='#EF4444' + p "La structuration a échoué." fontSize 14 fontWeight 600 color '#1C1A17'
      - Body: p "{message || 'Claude n\'a pas pu analyser le retour vocal. Vérifiez votre connexion ou réessayez.'}", fontSize 12, fontWeight 400, color '#6B6963', marginTop 4
    - Button row: div, flex, justifyContent flex-end, gap 8
      - [Retour] ghost button: h 40, px 24, radius 8, fontSize 14, fontWeight 400, border '1px solid #E2E0DA', bg '#FFFFFF', color '#1C1A17'; onClick: onBack; hover bg '#F0EFE9'
      - [Réessayer] primary button: h 40, px 24, radius 8, fontSize 14, fontWeight 600, bg '#FF5C1A', color '#FFFFFF', border none; onClick: onRetry; hover bg '#E5521A'
    - GSAP entrance on mount: `gsap.from(blockRef.current, { y: 8, opacity: 0, duration: 0.2, ease: 'power2.out' })` then shake: `gsap.to(blockRef.current, { keyframes: { x: [-6, 6, -4, 4, -2, 2, 0] }, duration: 0.3, delay: 0.2 })`
    - GSAP [Réessayer] scale on click: `gsap.to(retryBtnRef.current, { scale: 0.97, duration: 0.1, yoyo: true, repeat: 1 })`
  </action>

  <verify>
    <automated>cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -i "VocalStructuring" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "Structuration en cours" apps/web/src/components/coach/vocal/VocalStructuring.tsx` returns a match
    - `grep "La structuration a échoué" apps/web/src/components/coach/vocal/VocalStructuringError.tsx` returns a match
    - `grep "role=\"alert\"" apps/web/src/components/coach/vocal/VocalStructuringError.tsx` returns a match
    - `grep "Réessayer" apps/web/src/components/coach/vocal/VocalStructuringError.tsx` returns a match
    - `grep "onRetry\|onBack" apps/web/src/components/coach/vocal/VocalStructuringError.tsx` returns at least 2 matches
    - TypeScript: `cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -c "VocalStructuring"` returns 0
    - `grep "use client" apps/web/src/components/coach/vocal/VocalStructuring.tsx` returns a match
    - `grep "use client" apps/web/src/components/coach/vocal/VocalStructuringError.tsx` returns a match
  </acceptance_criteria>

  <done>VocalStructuring renders spinner with accent border-top and copy. VocalStructuringError renders error block with role=alert, Réessayer + Retour buttons, GSAP entrance + shake.</done>
</task>

<task type="auto">
  <name>Task 2: Extend VocalRetourPanel + create VocalCardReady shell</name>
  <files>
    apps/web/src/components/coach/vocal/VocalRetourPanel.tsx,
    apps/web/src/components/coach/vocal/VocalCardReady.tsx
  </files>

  <read_first>
    - apps/web/src/components/coach/vocal/VocalRetourPanel.tsx — current file; read the full file before editing; remove the `void clientId` line; add handleStructure; add useEffect; add conditional renders
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-RESEARCH.md — "Frontend: Triggering the structure call" code example for handleStructure function and the useEffect pattern
    - .planning/workstreams/retour-vocal/phases/02-claude-structuring/02-UI-SPEC.md — "Auto-reset After Save" useEffect pattern for card-saved state; "Transition: review → structuring" section
  </read_first>

  <action>
    VocalRetourPanel.tsx modifications (read the current file first — all existing code stays):

    1. Remove `void clientId;` line (the comment about Phase 02 reservation). clientId is now used.

    2. Add imports at top: VocalStructuring, VocalStructuringError, VocalCardReady from their respective files.

    3. Add handleStructure async function inside the component:
       `async function handleStructure(transcript: string)` — fires POST '/api/coach/voice/structure' with `{ athlete_id: clientId, transcript }` as JSON body. On error response: dispatch STRUCTURE_ERROR. On success: dispatch STRUCTURE_SUCCESS with `data.card`. On network catch: dispatch STRUCTURE_ERROR with 'Erreur réseau. Vérifiez votre connexion.' Per RESEARCH.md Pattern example.

    4. Add useEffect that fires handleStructure when state.status === 'structuring':
       ```
       useEffect(() => {
         if (state.status !== 'structuring') return;
         handleStructure(state.transcript);
       }, [state.status]);
       ```
       Dependency array is [state.status] only — handleStructure is defined inside the component so it captures current clientId and dispatch without needing to be in deps (same pattern as uploadBlob).

    5. Add handleRetryStructure function: if state.status === 'structuring-error', dispatch STRUCTURE_ERROR first to reset message? No — just call handleStructure(state.transcript) directly since the state is already 'structuring-error' and we need to retrigger. Actually: dispatch nothing; call handleStructure(state.transcript) directly. But handleStructure dispatches STRUCTURE_ERROR on fail or STRUCTURE_SUCCESS on success — the state will remain structuring-error if we call from there. Correct approach: dispatch a synthetic transition back to 'structuring' first by... there is no RETRY_STRUCTURE action. Instead, the error component's onRetry should dispatch { type: 'VALIDATE' } (which from 'structuring-error' state will fail the guard) — no. Correct: add a `RETRY_STRUCTURE` action to the reducer? No — RESEARCH.md says Réessayer "retries the Claude API call with same transcript + context (no re-recording needed)". The simplest approach that avoids a new action: handleRetryStructure calls handleStructure(state.transcript) directly — the dispatch STRUCTURE_SUCCESS/ERROR will transition from structuring-error fine because the reducer cases for those are guarded on 'structuring' only. So for retry, temporarily set status to 'structuring' by dispatching... use the existing VALIDATE action only works from 'review'. Resolution per RESEARCH.md spirit: add a `RETRY_STRUCTURE` action to vocalReducer that transitions 'structuring-error' → 'structuring' (keep transcript). Add it to VocalAction and the reducer in the same edit. The test for it is not strictly required in Wave 0 but add it anyway for Nyquist compliance.

    6. Add auto-reset useEffect for card-saved state (per UI-SPEC "Auto-reset behavior"):
       ```
       useEffect(() => {
         if (state.status !== 'card-saved') return;
         const timer = setTimeout(() => dispatch({ type: 'RESET' }), 3000);
         return () => clearTimeout(timer);
       }, [state.status]);
       ```

    7. Add conditional renders inside the JSX return (after existing error and review renders):
       - `{state.status === 'structuring' && <VocalStructuring />}`
       - `{state.status === 'structuring-error' && <VocalStructuringError message={state.message} onRetry={handleRetryStructure} onBack={() => dispatch({ type: 'RELAUNCH' })} />}`
         Note: RELAUNCH guard in reducer accepts 'structuring-error' — verify in Plan 02-01 reducer; if RELAUNCH only guards 'error' and 'review', update it to also accept 'structuring-error'. If the guard rejects the transition, add 'structuring-error' to the RELAUNCH guard condition.
       - `{(state.status === 'card-ready' || state.status === 'card-editing' || state.status === 'card-saving' || state.status === 'card-saved') && <VocalCardReady state={state} dispatch={dispatch} />}`

    Note on RELAUNCH from structuring-error: The Phase 01 reducer has `if (state.status !== 'error' && state.status !== 'review') return state`. 'structuring-error' is not in that guard. Update the guard in vocalReducer.ts (also already an edited file) to also allow 'structuring-error': `if (state.status !== 'error' && state.status !== 'review' && state.status !== 'structuring-error') return state`.

    VocalCardReady.tsx (shell for Plan 02-04):
    - 'use client' directive
    - Imports: React from 'react'; VocalState, VocalAction, StructuredCard from './vocalReducer'
    - Props: `{ state: Extract<VocalState, { status: 'card-ready' | 'card-editing' | 'card-saving' | 'card-saved' }>; dispatch: React.Dispatch<VocalAction> }`
    - For now render a placeholder div: `<div data-testid="vocal-card-ready" style={{ padding: 24 }}>FeedbackCard — Plan 02-04</div>` — Plan 02-04 will replace this with the real FeedbackCard component.
    - Export as named export `VocalCardReady`

    Also update vocalReducer.ts to:
    - Add `| { type: 'RETRY_STRUCTURE' }` to VocalAction
    - Add RETRY_STRUCTURE case: guard `state.status !== 'structuring-error'`; return `{ status: 'structuring', transcript: state.transcript }`
    - Update RELAUNCH guard to also accept 'structuring-error'
  </action>

  <verify>
    <automated>cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -i "VocalRetourPanel\|VocalCardReady" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "void clientId" apps/web/src/components/coach/vocal/VocalRetourPanel.tsx` returns no match (line removed)
    - `grep "handleStructure" apps/web/src/components/coach/vocal/VocalRetourPanel.tsx | grep "clientId"` returns a match (clientId in POST body)
    - `grep "state.status.*structuring" apps/web/src/components/coach/vocal/VocalRetourPanel.tsx` returns at least 1 match (useEffect trigger)
    - `grep "card-saved" apps/web/src/components/coach/vocal/VocalRetourPanel.tsx` returns a match (auto-reset useEffect)
    - `grep "VocalStructuring\b" apps/web/src/components/coach/vocal/VocalRetourPanel.tsx` returns at least 2 matches (import + usage)
    - `grep "VocalStructuringError" apps/web/src/components/coach/vocal/VocalRetourPanel.tsx` returns at least 2 matches (import + usage)
    - `grep "VocalCardReady" apps/web/src/components/coach/vocal/VocalRetourPanel.tsx` returns at least 2 matches (import + usage)
    - `grep "data-testid.*vocal-card-ready" apps/web/src/components/coach/vocal/VocalCardReady.tsx` returns a match
    - `grep "RETRY_STRUCTURE" apps/web/src/components/coach/vocal/vocalReducer.ts` returns at least 1 match
    - TypeScript: `cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -c "vocal/"` returns 0
    - All reducer tests still pass: `cd C:/ziko-platform/apps/web && npx vitest run src/components/coach/vocal/vocalReducer.test.ts --passWithNoTests 2>&1 | grep -c "passed"` returns a number > 0
  </acceptance_criteria>

  <done>VocalRetourPanel wired: void clientId removed, handleStructure fires on structuring state, VocalStructuring + VocalStructuringError + VocalCardReady shell conditionally rendered, auto-reset after card-saved. RELAUNCH guard extended to structuring-error. RETRY_STRUCTURE action added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser fetch → /api/coach/voice/structure | Frontend sends athlete_id + transcript; response carries StructuredCard |
| GSAP useEffect → DOM | Client-only — 'use client' prevents SSR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | StructuredCard data in browser | accept | Card is the coach-facing output; not sensitive to the athlete; no PII beyond what coach already inputs |
| T-02-03-02 | Denial of Service | Retry loop on structuring-error | accept | RETRY_STRUCTURE requires explicit user click; no auto-retry; no loop without user action |
| T-02-03-SC | Tampering | npm installs | accept | No new packages — gsap, lucide-react already installed |
</threat_model>

<verification>
Full vocal suite must pass after this plan:
`cd C:/ziko-platform/apps/web && npx vitest run src/components/coach/vocal/ --passWithNoTests`

TypeScript clean:
`cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -v "^$" | head -30`
</verification>

<success_criteria>
- VocalRetourPanel fires POST /api/coach/voice/structure when state.status === 'structuring'
- clientId included in POST body as athlete_id (void clientId line removed)
- VocalStructuring renders correctly per UI-SPEC State 1 spec
- VocalStructuringError renders error block, Réessayer retries, Retour goes back to review
- VocalCardReady shell exists and renders a placeholder (FeedbackCard wired in Plan 02-04)
- RELAUNCH works from structuring-error state
- RETRY_STRUCTURE action added to reducer
- TypeScript clean in apps/web
</success_criteria>

<output>
Create `.planning/workstreams/retour-vocal/phases/02-claude-structuring/02-03-SUMMARY.md` when done.
</output>
