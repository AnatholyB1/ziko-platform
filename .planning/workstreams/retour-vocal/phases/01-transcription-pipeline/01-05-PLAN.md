---
phase: 01-transcription-pipeline
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - apps/web/src/components/coach/vocal/VocalIdle.tsx
  - apps/web/src/components/coach/vocal/VocalRecording.tsx
  - apps/web/src/components/coach/vocal/VocalTranscribing.tsx
  - apps/web/src/components/coach/vocal/VocalReview.tsx
  - apps/web/src/components/coach/vocal/VocalRetourPanel.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx
  - apps/web/src/components/coach/ClientTabStrip.tsx
autonomous: false
requirements:
  - VOICE-01
  - VOICE-03

must_haves:
  truths:
    - "ClientTabStrip has a 9th tab 'Retour vocal' linking to /[locale]/coach/clients/[id]/vocal"
    - "VocalReview.test.tsx goes GREEN after this plan"
    - "VocalIdle renders mic icon circle, heading, body copy, and 'Nouveau retour' button per UI-SPEC"
    - "VocalRecording shows red pulsing stop button, mm:ss timer, red color at 4:00+"
    - "VocalTranscribing shows CSS spinner with orange border-t and 'Transcription en cours…'"
    - "VocalReview renders transcript in muted box (min-h 160px, max-h 320px, scrollable) with Valider + Relancer buttons"
    - "VocalRetourPanel replaces placeholder divs with the 4 styled sub-components"
    - "GSAP entrance animations wired per UI-SPEC motion contract"
    - "Page accessible at /[locale]/coach/clients/[id]/vocal"
  artifacts:
    - path: "apps/web/src/components/coach/vocal/VocalIdle.tsx"
      provides: "Idle state view"
    - path: "apps/web/src/components/coach/vocal/VocalRecording.tsx"
      provides: "Recording state view with pulse animation"
    - path: "apps/web/src/components/coach/vocal/VocalTranscribing.tsx"
      provides: "Transcribing state spinner view"
    - path: "apps/web/src/components/coach/vocal/VocalReview.tsx"
      provides: "Review and error sub-state view"
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx"
      provides: "Thin server wrapper mounting VocalRetourPanel"
    - path: "apps/web/src/components/coach/ClientTabStrip.tsx"
      provides: "9th tab: Retour vocal"
  key_links:
    - from: "apps/web/src/components/coach/ClientTabStrip.tsx"
      to: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx"
      via: "{ key: 'vocal', label: 'Retour vocal' } in TABS array"
      pattern: "vocal"
    - from: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx"
      to: "apps/web/src/components/coach/vocal/VocalRetourPanel.tsx"
      via: "import VocalRetourPanel + render <VocalRetourPanel clientId={id} />"
      pattern: "VocalRetourPanel"
    - from: "apps/web/src/components/coach/vocal/VocalRetourPanel.tsx"
      to: "VocalIdle/VocalRecording/VocalTranscribing/VocalReview"
      via: "conditional render based on state.status"
      pattern: "state\\.status"
---

<objective>
Build all four styled UI sub-components (VocalIdle, VocalRecording, VocalTranscribing, VocalReview), wire them into VocalRetourPanel replacing the placeholder divs, create the page.tsx server wrapper, and add the "Retour vocal" tab to ClientTabStrip.

All visual specs come from UI-SPEC.md (approved, pixel-perfect implementation required). GSAP animations follow the motion contract in UI-SPEC.md Motion Design section.

After this plan, the full flow is end-to-end: coach clicks "Retour vocal" tab → idle state → records → transcribes → sees transcript → validates or relaunches. VocalReview.test.tsx goes GREEN.

Purpose: Completes VOICE-01 (recording UI), VOICE-03 (transcript display), and wires the feature into the client sheet navigation.
Output: 4 styled sub-components + page.tsx + ClientTabStrip update + VocalRetourPanel updated.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-UI-SPEC.md
@.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-CONTEXT.md

<interfaces>
<!-- VocalIdle props -->
```typescript
interface VocalIdleProps {
  onStart: () => void;
}
```

<!-- VocalRecording props -->
```typescript
interface VocalRecordingProps {
  formatted: string;       // mm:ss from useVocalTimer
  elapsedSeconds: number;  // raw seconds for color threshold (>= 240 → red)
  onStop: () => void;
}
```

<!-- VocalTranscribing props — no interactivity -->
```typescript
// No props needed
function VocalTranscribing(): JSX.Element
```

<!-- VocalReview props — handles both review and error states -->
```typescript
interface VocalReviewProps {
  // review state
  transcript?: string;
  // error state
  error?: string;
  onValidate?: () => void;  // undefined in error state
  onRelaunch: () => void;
  onRetry?: () => void;     // undefined in review state
}
```

<!-- page.tsx pattern (from programs/page.tsx) -->
```typescript
// Thin server wrapper: extract params, render client component
export default async function VocalPage({ params }: { params: Promise<{ locale: string; id: string }> }) {
  const { id } = await params;
  return <VocalRetourPanel clientId={id} />;
}
```

<!-- ClientTabStrip.tsx TABS array (current — 8 tabs) -->
```typescript
const TABS = [
  { key: 'sessions', label: 'Séances' },
  // ... 7 more tabs
  // ADD: { key: 'vocal', label: 'Retour vocal' }  ← 9th tab per D-01
];
```

<!-- GSAP pattern from apps/web/src/components/coach/ChatInputBar.tsx (already in codebase) -->
```typescript
import gsap from 'gsap';
// Usage: gsap.from(ref.current, { y: 16, opacity: 0, duration: 0.2, ease: 'power2.out' })
```

<!-- Color tokens (from UI-SPEC.md Design System) -->
```
Background: #F7F6F3
Surface: #FFFFFF
Border: #E2E0DA
Primary (Valider only): #FF5C1A
Destructive (recording, error): #EF4444
Muted surface: #F0EFE9
Text primary: #1C1A17
Text muted: #6B6963
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build VocalIdle, VocalRecording, VocalTranscribing, VocalReview sub-components</name>
  <files>
    apps/web/src/components/coach/vocal/VocalIdle.tsx
    apps/web/src/components/coach/vocal/VocalRecording.tsx
    apps/web/src/components/coach/vocal/VocalTranscribing.tsx
    apps/web/src/components/coach/vocal/VocalReview.tsx
  </files>
  <behavior>
    VocalIdle:
    - Renders: 64×64px circle (bg #F0EFE9, border 1px solid #E2E0DA, radius 9999px) containing 28px mic icon (Lucide `Mic`, color #6B6963)
    - Heading: "Enregistrer un retour" (18px/600/#1C1A17, mt-24px / mt-6 in Tailwind)
    - Body: "Appuyez pour enregistrer un retour vocal pour cet athlète. Durée max : 5 min." (14px/400/#6B6963, text-center, max-w-[300px])
    - Button: "Nouveau retour" (bg #FF5C1A, text white, h-11, px-6, rounded-lg, 14px/600) — calls onStart

    VocalRecording:
    - Status badge: red dot 8×8px (bg #EF4444, rounded-full) + "Enregistrement" (12px/400/#EF4444)
    - Stop button: 72×72px circle (bg #EF4444, radius 9999px, shadow-md) with Lucide `Square` icon (24px, white fill) + CSS pulse ring animation class `animate-pulse-ring`
    - "Arrêter l'enregistrement" hint (14px/400/#6B6963, mt-2)
    - Timer: formatted prop, 18px/600, color #EF4444 when elapsedSeconds >= 240 else #6B6963, CSS transition color 500ms
    - "Durée max : 5 min" (12px/400/#6B6963)
    - Stop button calls onStop

    VocalTranscribing:
    - 32×32px CSS spinner: `border-4 border-[#E2E0DA] border-t-[#FF5C1A] rounded-full animate-spin` (Tailwind + CSS)
    - "Transcription en cours…" (14px/600/#1C1A17, mt-4)
    - "L'audio est en cours de traitement. Cela prend généralement 5 à 15 secondes." (12px/400/#6B6963, text-center, max-w-[280px], mt-2)
    - No interactive elements (D-04: no cancel during upload)

    VocalReview:
    - If transcript prop: label "Transcript" (12px/400/#6B6963, mb-2), transcript block (bg #F0EFE9, border 1px #E2E0DA, radius 8px, p-4, min-h-[160px], max-h-[320px], overflow-y-auto, 16px/400/#1C1A17, pointer-events none on text), buttons: [Valider] (primary orange, calls onValidate) + [Relancer] (ghost, calls onRelaunch)
    - If error prop: error block (bg #FEF2F2, border 1px #FECACA, radius 8px, p-4) with Lucide `AlertTriangle` (18px/#EF4444) + "La transcription a échoué." (14px/600/#1C1A17) + error body text (12px/400/#6B6963), buttons: [Ressayer] (primary orange, calls onRetry) + [Relancer] (ghost, calls onRelaunch)
    - Button row: mt-4, flex, gap-2, justify-end
    - Valider/Ressayer: h-10, px-6, rounded-lg, 14px/600, bg #FF5C1A, text white
    - Relancer: h-10, px-6, rounded-lg, 14px/400, border 1px #E2E0DA, bg white, text #1C1A17, hover:bg-[#F0EFE9]
  </behavior>
  <read_first>
    - .planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-UI-SPEC.md — read ALL screen specs (State 1 through State 5) and Motion Design section before writing any component
    - apps/web/src/components/coach/vocal/VocalReview.test.tsx — read test assertions; component props and text must match exactly what the tests assert
    - apps/web/src/components/coach/ChatInputBar.tsx — scan for GSAP import pattern used in this codebase
  </read_first>
  <action>
    Each component starts with `'use client'` directive.

    GSAP animations: use `useRef` + `useEffect` pattern for entrance animations:
    - VocalIdle: on mount, `gsap.from(panelRef.current, { y: 16, opacity: 0, duration: 0.2, ease: 'power2.out' })`
    - VocalRecording: on mount, same crossfade entrance
    - VocalTranscribing: on mount, same crossfade entrance. The CSS spinner uses `animate-spin` (Tailwind built-in)
    - VocalReview (review state): transcript block entrance `gsap.from(blockRef.current, { y: 12, opacity: 0, duration: 0.25, ease: 'power2.out' })`
    - VocalReview (error state): block entrance then shake — `gsap.from(blockRef.current, { y: 8, opacity: 0, duration: 0.2, ease: 'power2.out' })` then `gsap.to(blockRef.current, { keyframes: { x: [-6, 6, -4, 4, -2, 2, 0] }, duration: 0.3 })`

    Recording pulse ring: add a global CSS keyframe in the component file via a `<style>` tag or add to `apps/web/src/app/globals.css`. Keyframe:
    `@keyframes pulse-ring { 0% { box-shadow: 0 0 0 0px rgba(239,68,68,0.4) } 100% { box-shadow: 0 0 0 20px rgba(239,68,68,0) } }` at 1.5s infinite. Apply as `style={{ animation: 'pulse-ring 1.5s ease-in-out infinite' }}` on the stop button circle.

    Use inline styles for all color/size tokens (no CSS classes for non-Tailwind values like #FF5C1A, #6B6963 etc.) — consistent with CLAUDE.md project conventions (no StyleSheet, use inline style objects).

    Wait — this is a Next.js web app (not React Native). Use Tailwind classes where available. For exact hex values not in Tailwind config, use the `[]` arbitrary value syntax: `text-[#6B6963]`, `bg-[#F0EFE9]`, `border-[#E2E0DA]` etc. Do NOT use inline `style` prop for colors/sizes that can be expressed in Tailwind arbitrary values.

    All French copy must exactly match the Copywriting Contract in UI-SPEC.md — use those exact strings.

    Container for all sub-components: `max-w-[640px] mx-auto flex flex-col items-center` with appropriate top padding to center vertically in the content area.
  </action>
  <verify>
    <automated>
      cd C:/ziko-platform && npx vitest run apps/web/src/components/coach/vocal/VocalReview.test.tsx 2>&1 | tail -20
    </automated>
    VocalReview.test.tsx must be GREEN (all tests pass). Also run type-check:
    `cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep vocal | head -10` → must return empty.
  </verify>
  <done>
    - VocalReview.test.tsx passes GREEN
    - All 4 sub-component files exist
    - French copy matches UI-SPEC.md Copywriting Contract exactly (spot-check: "Enregistrement en cours…" in VocalTranscribing, "Valider" in VocalReview, "Ressayer" in error state)
    - Tailwind arbitrary values used for brand colors (e.g., `bg-[#FF5C1A]`, `text-[#1C1A17]`)
    - TypeScript type-check passes for all vocal component files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 created all 4 styled sub-components. Task 2 below will wire them into VocalRetourPanel and add the tab. Before wiring, verify the component visual output matches UI-SPEC.md.
  </what-built>
  <how-to-verify>
    1. A temporary test page can be created at `apps/web/src/app/[locale]/(coach)/vocal-test/page.tsx` that renders each sub-component in isolation:
       - `<VocalIdle onStart={() => {}} />`
       - `<VocalRecording formatted="02:34" elapsedSeconds={154} onStop={() => {}} />`
       - `<VocalTranscribing />`
       - `<VocalReview transcript="Joaquim a bien poussé sur le squat aujourd'hui." onValidate={() => {}} onRelaunch={() => {}} />`
       - `<VocalReview error="Transcription failed" onRelaunch={() => {}} onRetry={() => {}} />`
    2. Start dev server: `npm run dev` from project root
    3. Navigate to: `http://localhost:3000/fr/coach/vocal-test`
    4. Verify each state matches the screen specs from UI-SPEC.md:
       - idle: mic icon in circle, orange "Nouveau retour" button
       - recording: red pulse button, green/red timer
       - transcribing: spinning orange-topped spinner
       - review: muted box with transcript, Valider/Relancer buttons
       - error: red-border block, Ressayer/Relancer buttons
    5. Check pulse animation on stop button (should pulse continuously)
    6. Delete the temp test page after verification
  </how-to-verify>
  <resume-signal>Type "visual-approved" to continue to Task 2, or describe visual issues to fix first</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire VocalRetourPanel, create page.tsx, update ClientTabStrip</name>
  <files>
    apps/web/src/components/coach/vocal/VocalRetourPanel.tsx
    apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx
    apps/web/src/components/coach/ClientTabStrip.tsx
  </files>
  <read_first>
    - apps/web/src/components/coach/vocal/VocalRetourPanel.tsx — read current file; replace placeholder divs with sub-component imports
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/page.tsx — read for the thin server wrapper pattern (async params, no Supabase needed for vocal page)
    - apps/web/src/components/coach/ClientTabStrip.tsx — read TABS array; add vocal as 9th item per D-01
  </read_first>
  <action>
    VocalRetourPanel.tsx — replace placeholder divs with sub-component renders:
    - Add imports: `import { VocalIdle } from './VocalIdle'`, same for VocalRecording, VocalTranscribing, VocalReview
    - Keep the outer wrapper div with `className="vocal-panel"` for GSAP targeting
    - Add page-level entrance GSAP: `gsap.from('.vocal-panel', { y: 16, opacity: 0, duration: 0.2, ease: 'power2.out' })` in a useEffect on mount
    - State routing:
      - `idle`: `<VocalIdle onStart={handleStart} />`
      - `recording`: `<VocalRecording formatted={formatted} elapsedSeconds={elapsedSeconds} onStop={handleStop} />`
      - `transcribing`: `<VocalTranscribing />`
      - `review`: `<VocalReview transcript={state.transcript} onValidate={handleValidate} onRelaunch={handleRelaunch} />`
      - `error`: `<VocalReview error={state.message} onRetry={handleRetry} onRelaunch={handleRelaunch} />`
    - Remove the placeholder `data-testid` divs that are now replaced by sub-components
    - Preserve the `data-testid="vocal-panel"` on the outer wrapper for integration testing

    page.tsx — create `apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx`:
    - Server component (no 'use client')
    - `export default async function VocalPage({ params }: { params: Promise<{ locale: string; id: string }> }) { const { id } = await params; return <VocalRetourPanel clientId={id} />; }`
    - Import VocalRetourPanel from the component path

    ClientTabStrip.tsx — add 9th tab to TABS array (D-01):
    - `{ key: 'vocal', label: 'Retour vocal' }` appended after `{ key: 'programs', label: 'Programmes' }`
    - No other changes to the component
  </action>
  <verify>
    <automated>
      cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | grep -i "vocal\|error" | head -10
    </automated>
    Must return no TypeScript errors. Also:
    `grep -c "Retour vocal" C:/ziko-platform/apps/web/src/components/coach/ClientTabStrip.tsx` → 1
    `grep -c "VocalRetourPanel" C:/ziko-platform/apps/web/src/app/[locale]/\(coach\)/coach/clients/\[id\]/vocal/page.tsx` → 1
    `grep -c "vocal-panel" C:/ziko-platform/apps/web/src/components/coach/vocal/VocalRetourPanel.tsx` → 1
  </verify>
  <done>
    - ClientTabStrip.tsx TABS array has 9 entries; 9th is `{ key: 'vocal', label: 'Retour vocal' }`
    - `apps/web/src/app/[locale]/(coach)/coach/clients/[id]/vocal/page.tsx` exists and renders VocalRetourPanel
    - VocalRetourPanel.tsx renders 4 styled sub-components (no more placeholder divs for states)
    - TypeScript type-check clean for all vocal-related files
    - Full frontend test suite passes: `cd C:/ziko-platform && npm run test --workspace=apps/web`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser page→coach route | vocal page lives under (coach) route group which requires authenticated session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Elevation of Privilege | /coach/clients/[id]/vocal page access | accept | Route group (coach) already enforces auth via middleware.ts; vocal page inherits this without additional guard |
| T-01-10 | Information Disclosure | Transcript displayed in browser | accept | Transcript is the coach's own dictation; no PII beyond what the coach intentionally spoke; no persistence in Phase 01 |
</threat_model>

<verification>
Full phase gate (run before /gsd:verify-work):

1. Tab exists: `grep -c "Retour vocal" C:/ziko-platform/apps/web/src/components/coach/ClientTabStrip.tsx` → 1
2. Page exists: `ls C:/ziko-platform/apps/web/src/app/[locale]/\(coach\)/coach/clients/\[id\]/vocal/page.tsx`
3. All 7 vocal source files present: `ls C:/ziko-platform/apps/web/src/components/coach/vocal/*.tsx` → 5 files
4. VocalReview tests GREEN: `cd C:/ziko-platform && npx vitest run apps/web/src/components/coach/vocal/VocalReview.test.tsx 2>&1 | grep -c "PASS"` → 1
5. All frontend tests GREEN: `cd C:/ziko-platform && npm run test --workspace=apps/web`
6. All backend tests GREEN: `cd C:/ziko-platform && npm run test --workspace=backend/api`
7. TypeScript clean: `cd C:/ziko-platform && npm run type-check` → no errors
8. Proxy binary-safe: `grep -c "arrayBuffer" C:/ziko-platform/apps/web/src/app/api/coach/\[...path\]/route.ts` → 1
</verification>

<success_criteria>
- All 5 Wave 0 test stubs are GREEN (vocalReducer, useVocalRecorder, useVocalTimer, VocalReview frontend; voice.test.ts backend)
- ClientTabStrip has "Retour vocal" as 9th tab
- Route `/[locale]/coach/clients/[id]/vocal` renders VocalRetourPanel
- Full idle→recording→transcribing→review flow is functional end-to-end
- All GSAP animations wired per UI-SPEC.md Motion Design contract
- All French copy matches UI-SPEC.md Copywriting Contract exactly
- Full test suites (web + backend) are GREEN
- TypeScript type-check passes across the monorepo
</success_criteria>

<output>
Create `.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-05-SUMMARY.md` when done
</output>
