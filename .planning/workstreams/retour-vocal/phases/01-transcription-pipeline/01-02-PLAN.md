---
phase: 01-transcription-pipeline
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/api/src/coach/voice/service.ts
  - backend/api/test/voice.test.ts
  - backend/api/package.json
  - backend/api/.env.example
autonomous: true
requirements:
  - VOICE-02

user_setup:
  - service: openai
    why: "Whisper-1 transcription via openai package"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Platform → API Keys (https://platform.openai.com/api-keys)"
    dashboard_config: []

must_haves:
  truths:
    - "`openai` package installed in backend/api"
    - "OPENAI_API_KEY documented in .env.example"
    - "voice.test.ts exists with failing RED stubs for all 4 route behaviors"
    - "service.ts skeleton exists with voiceRouter and maxDuration = 60 exported"
  artifacts:
    - path: "backend/api/src/coach/voice/service.ts"
      provides: "voiceRouter skeleton (no Whisper call yet — Wave 1 completes it)"
      exports: ["voiceRouter", "maxDuration"]
    - path: "backend/api/test/voice.test.ts"
      provides: "RED test stubs for VOICE-02"
    - path: "backend/api/.env.example"
      provides: "OPENAI_API_KEY documentation"
  key_links:
    - from: "backend/api/test/voice.test.ts"
      to: "backend/api/src/coach/voice/service.ts"
      via: "imports voiceRouter, tests POST /transcribe"
      pattern: "voiceRouter.*transcribe"
---

<objective>
Install `openai` package, document `OPENAI_API_KEY` in `.env.example`, create the `voiceRouter` skeleton in `service.ts` (just enough to export the symbol and the route stub), and create the backend test stub `voice.test.ts` in RED state.

Wave 0 — no real Whisper call yet. The service.ts skeleton exports `voiceRouter` and `maxDuration = 60` and registers `POST /transcribe` that returns 501 for now. The test stubs import the router and assert the expected response shapes — they will fail until Wave 1 wires the real implementation.

Purpose: Unblocks Wave 1 (which wires the full Whisper logic into the existing service.ts skeleton) and satisfies Nyquist for VOICE-02 backend tests.

Output: `openai` in package.json, `OPENAI_API_KEY` in .env.example, `service.ts` skeleton, `voice.test.ts` RED stubs.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-CONTEXT.md
@.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-RESEARCH.md
@.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-VALIDATION.md

<interfaces>
<!-- From backend/api/src/app.ts — route mounting pattern -->
<!-- Pattern used: app.route('/coach/voice', voiceRouter) -->
<!-- All coach modules: import from './coach/{feature}/service.js' -->

From backend/api/src/coach/imports/service.ts (maxDuration pattern):
```typescript
export const maxDuration = 60; // Vercel: allow up to 60s for heavy operations
```

From backend/api/src/middleware/auth.ts (used by all coach routes):
```typescript
// authMiddleware: Hono middleware that validates Supabase Bearer JWT
// Usage: voiceRouter.use('*', authMiddleware)
// Sets c.var.userId (string) on success; returns 401 on failure
export const authMiddleware: MiddlewareHandler
```

From backend/api/src/coach/ai/service.ts (Hono router pattern):
```typescript
import { Hono } from 'hono';
import { authMiddleware } from '../../middleware/auth.js';
export const coachAiRouter = new Hono();
coachAiRouter.use('*', authMiddleware);
```

voiceRouter skeleton (from RESEARCH.md Code Examples):
```typescript
// exports: voiceRouter (Hono instance), maxDuration (number = 60)
// POST /transcribe: bodyLimit → parseBody → instanceof File check → toFile → Whisper
// Wave 0 skeleton: POST /transcribe returns 501 { error: 'not implemented' }
// Wave 1 will replace the 501 handler with real Whisper logic
```

Test structure expected (from VALIDATION.md):
```typescript
// voice.test.ts must cover:
// - POST /transcribe → 200 { transcript: string } (mocked OpenAI)
// - POST /transcribe → 401 without JWT
// - POST /transcribe → 400 without audio field
// - POST /transcribe → 413 oversized payload
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install openai package and document OPENAI_API_KEY</name>
  <files>
    backend/api/package.json
    backend/api/.env.example
  </files>
  <read_first>
    - backend/api/package.json — check dependencies block; confirm `openai` is not already present
    - backend/api/.env.example — read current contents; add OPENAI_API_KEY line without overwriting anything
  </read_first>
  <action>
    Install openai in backend/api workspace:
    `cd C:/ziko-platform && npm install openai@6.39.0 --workspace=backend/api`

    After install, verify it appears in backend/api/package.json dependencies.

    Update `backend/api/.env.example`: append a new line `OPENAI_API_KEY=sk-...` with a comment above it:
    ```
    # OpenAI Whisper transcription (Phase 01 — Retour Vocal)
    OPENAI_API_KEY=sk-...
    ```
    Do not modify any existing lines in .env.example.

    Also verify (do not modify) that the real `backend/api/.env` or `backend/api/.env.local` contains `OPENAI_API_KEY`. If it is missing, output a clear warning message: "ACTION REQUIRED: Add OPENAI_API_KEY=<your-key> to backend/api/.env.local before running Wave 1 tests". The plan executor must note this in the SUMMARY.
  </action>
  <verify>
    <automated>
      cd C:/ziko-platform/backend/api && node -e "require('openai')" 2>&1 | head -3
    </automated>
    Must output no error (module resolves). Also:
    `grep -c "OPENAI_API_KEY" C:/ziko-platform/backend/api/.env.example` → must return 1
  </verify>
  <done>
    - `node -e "require('openai')"` in backend/api succeeds (no module-not-found)
    - `OPENAI_API_KEY` line exists in `backend/api/.env.example`
    - Summary notes whether OPENAI_API_KEY is present in .env.local (user action required if not)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create voiceRouter skeleton + backend test stub</name>
  <files>
    backend/api/src/coach/voice/service.ts
    backend/api/test/voice.test.ts
  </files>
  <read_first>
    - backend/api/src/coach/ai/service.ts — read first 40 lines for Hono router pattern with authMiddleware
    - backend/api/src/middleware/auth.ts — confirm import path and export name
    - backend/api/test/setup.ts — read to understand test environment (env vars loaded, etc.)
    - backend/api/vitest.config.ts — confirm test include pattern covers test/voice.test.ts
  </read_first>
  <action>
    Create directory `backend/api/src/coach/voice/` if it does not exist.

    Create `backend/api/src/coach/voice/service.ts` as a skeleton:
    - `export const maxDuration = 60` at top (Vercel timeout — per RESEARCH.md Pitfall 7 and importsRouter precedent)
    - Import `Hono` from 'hono', `bodyLimit` from 'hono/body-limit', `OpenAI` and `toFile` from 'openai', `authMiddleware` from '../../middleware/auth.js'
    - Verify bodyLimit import path: run `node -e "require('hono/body-limit')"` in backend/api — if it fails, import from 'hono' directly and note the fallback in a comment
    - `export const voiceRouter = new Hono()`
    - `voiceRouter.use('*', authMiddleware)` — auth on all voice routes (per D-07, security requirement)
    - `voiceRouter.post('/transcribe', bodyLimit({ maxSize: 20 * 1024 * 1024, onError: (c) => c.json({ error: 'Audio file too large (max 20 MB)' }, 413) }), async (c) => { return c.json({ error: 'not implemented' }, 501) })` — stub handler; Wave 1 replaces this with Whisper logic
    - Add a comment: `// Wave 1 (plan 01-03) will replace the 501 stub with real Whisper transcription`

    Create `backend/api/test/voice.test.ts`:
    - Import `describe`, `it`, `expect`, `vi`, `beforeAll` from 'vitest'
    - Import `voiceRouter` from '../src/coach/voice/service.js'
    - Mock the OpenAI client: `vi.mock('openai', () => ({ default: vi.fn(), toFile: vi.fn() }))` — so tests don't hit the real API
    - Use Hono's `app.fetch()` or `app.request()` for unit testing (Hono provides a test client via `import { testClient } from 'hono/testing'` or use `new Request()` + `voiceRouter.fetch()`)
    - Test 1: `it('returns 401 without Authorization header', async () => { ... expect(res.status).toBe(401) })`
    - Test 2: `it('returns 400 when no audio field in multipart', async () => { ... })`  — send a request with a valid JWT mock but no audio field
    - Test 3: `it('returns 413 for oversized payload', async () => { ... })` — send a request with Content-Length exceeding bodyLimit
    - Test 4: `it('returns 200 with transcript when OpenAI mock resolves', async () => { ... expect(json.transcript).toBeDefined() })`

    Note: Tests 2–4 will initially fail because the handler returns 501. That is the correct RED state. They will go GREEN in Wave 1 (plan 01-03) when the real handler is wired.

    For auth mocking in tests: stub `authMiddleware` with `vi.mock('../../src/middleware/auth.js', ...)` returning a middleware that sets `c.set('auth', { userId: 'test-user' })`.
  </action>
  <verify>
    <automated>
      cd C:/ziko-platform && npx vitest run backend/api/test/voice.test.ts --passWithNoTests 2>&1 | tail -20
    </automated>
    Expected: tests are found and run. Test 1 (401) may pass if authMiddleware stub is correct. Tests 2–4 should fail because handler returns 501. The file must compile without TypeScript errors.
  </verify>
  <done>
    - `backend/api/src/coach/voice/service.ts` exists and exports `voiceRouter` and `maxDuration = 60`
    - `backend/api/test/voice.test.ts` exists with 4 test cases
    - Vitest runs the file without compilation errors
    - At least one test fails (RED for stubs 2–4 since handler is 501)
    - TypeScript does not report errors on service.ts: `cd backend/api && npm run type-check 2>&1 | grep voice` returns empty
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → OpenAI API | OPENAI_API_KEY must never be exposed in response bodies or logs |
| test environment | OpenAI client must be mocked; real API key must not be required for tests to run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | OPENAI_API_KEY in backend | mitigate | Key only in `backend/api/.env.local` (gitignored); `.env.example` contains placeholder `sk-...` only; never logged or returned in responses |
| T-01-02 | Elevation of Privilege | POST /coach/voice/transcribe — unauthenticated access | mitigate | `voiceRouter.use('*', authMiddleware)` — all routes require valid Supabase JWT before reaching any handler |
| T-01-SC | Tampering | npm install openai | mitigate | Package legitimacy audit in RESEARCH.md confirms openai@6.39.0 is [OK] — official OpenAI SDK, 3M+ weekly downloads |
</threat_model>

<verification>
After both tasks:

1. Package installed: `cd C:/ziko-platform/backend/api && node -e "const { default: OpenAI, toFile } = require('openai'); console.log(typeof OpenAI, typeof toFile)"` → `function function`
2. Env documented: `grep "OPENAI_API_KEY" C:/ziko-platform/backend/api/.env.example` → returns the line
3. Service exports: `grep -c "export const voiceRouter" C:/ziko-platform/backend/api/src/coach/voice/service.ts` → 1
4. maxDuration exported: `grep -c "export const maxDuration = 60" C:/ziko-platform/backend/api/src/coach/voice/service.ts` → 1
5. Auth on all routes: `grep -c "authMiddleware" C:/ziko-platform/backend/api/src/coach/voice/service.ts` → 1
6. Tests RED: `cd C:/ziko-platform && npx vitest run backend/api/test/voice.test.ts 2>&1 | grep -c "FAIL"` → >= 1
</verification>

<success_criteria>
- `openai@6.39.0` installed in backend/api (appears in package.json dependencies)
- `OPENAI_API_KEY=sk-...` documented in backend/api/.env.example
- `backend/api/src/coach/voice/service.ts` exports `voiceRouter` (Hono) and `maxDuration = 60`
- `voiceRouter.use('*', authMiddleware)` present — all routes require JWT
- `backend/api/test/voice.test.ts` has 4 test cases, compiles, runs via vitest
- At least one test is RED (Wave 1 will turn them green)
</success_criteria>

<output>
Create `.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-02-SUMMARY.md` when done
</output>
