---
phase: 01-transcription-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/vitest.config.ts
  - apps/web/src/components/coach/vocal/useVocalRecorder.test.ts
  - apps/web/src/components/coach/vocal/useVocalTimer.test.ts
  - apps/web/src/components/coach/vocal/vocalReducer.test.ts
  - apps/web/src/components/coach/vocal/VocalReview.test.tsx
  - apps/web/src/app/api/coach/[...path]/route.ts
autonomous: true
requirements:
  - VOICE-01
  - VOICE-02
  - VOICE-03

must_haves:
  truths:
    - "Five Wave 0 test stub files exist and compile"
    - "Frontend test stubs fail (RED) with 'not implemented' — no false greens"
    - "Proxy route forwards multipart/form-data as arrayBuffer without corrupting binary"
    - "Proxy forwards original Content-Type header verbatim (including boundary) for multipart"
  artifacts:
    - path: "apps/web/src/components/coach/vocal/useVocalRecorder.test.ts"
      provides: "RED stub for VOICE-01 blob capture"
    - path: "apps/web/src/components/coach/vocal/useVocalTimer.test.ts"
      provides: "RED stub for VOICE-01 auto-stop at 300s"
    - path: "apps/web/src/components/coach/vocal/vocalReducer.test.ts"
      provides: "RED stub for VOICE-03 state transitions"
    - path: "apps/web/src/components/coach/vocal/VocalReview.test.tsx"
      provides: "RED stub for VOICE-03 component render"
    - path: "apps/web/src/app/api/coach/[...path]/route.ts"
      provides: "Binary-safe multipart proxy"
  key_links:
    - from: "apps/web/src/app/api/coach/[...path]/route.ts"
      to: "Hono POST /coach/voice/transcribe"
      via: "arrayBuffer passthrough when Content-Type includes multipart/form-data"
      pattern: "isMultipart.*arrayBuffer"
---

<objective>
This plan does two things that must happen BEFORE any feature code is written:
1. Fix the binary proxy bug that would silently corrupt all audio uploads (blocking).
2. Create failing test stubs (RED state) for all five Wave 0 test files.

Purpose: Proxy fix unblocks Wave 1 backend work. Red stubs satisfy Nyquist — tests must fail before production code exists. The web vitest config must also be updated to support .tsx test files (VocalReview.test.tsx) and jsdom environment.

Output: Patched proxy route + 5 test stub files + updated vitest.config.ts.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/retour-vocal/ROADMAP.md
@.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-CONTEXT.md
@.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-RESEARCH.md
@.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-VALIDATION.md

<interfaces>
<!-- From apps/web/src/app/api/coach/[...path]/route.ts (current — read before editing) -->
<!-- Key issue: line 20 uses req.text() for ALL POST bodies — corrupts binary multipart -->
<!-- Line 13-16: headers block always sets Content-Type from req.headers — must NOT override for multipart -->

From apps/web/vitest.config.ts (current):
```typescript
// environment: 'node' — needs 'happy-dom' for .tsx component tests
// include: ['{src,test}/**/*.{spec,test}.ts'] — .tsx not included; must add .tsx
```

VocalState discriminated union (from RESEARCH.md Pattern 5 — used in vocalReducer.test.ts):
```typescript
type VocalState =
  | { status: 'idle' }
  | { status: 'recording'; startedAt: number }
  | { status: 'transcribing'; blob: Blob; mimeType: string }
  | { status: 'review'; transcript: string }
  | { status: 'error'; blob: Blob; mimeType: string; message: string };

type VocalAction =
  | { type: 'START_RECORDING' }
  | { type: 'STOP_RECORDING'; blob: Blob; mimeType: string }
  | { type: 'TRANSCRIPTION_SUCCESS'; transcript: string }
  | { type: 'TRANSCRIPTION_ERROR'; message: string }
  | { type: 'RETRY' }
  | { type: 'RELAUNCH' }
  | { type: 'VALIDATE' };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix binary proxy bug + update web vitest config for .tsx + jsdom</name>
  <files>
    apps/web/src/app/api/coach/[...path]/route.ts
    apps/web/vitest.config.ts
    apps/web/package.json
  </files>
  <read_first>
    - apps/web/src/app/api/coach/[...path]/route.ts — read current file; the bug is on the `const body` line and in the `headers` block at lines 12-18
    - apps/web/vitest.config.ts — read current config; need to add tsx glob and happy-dom environment
    - apps/web/package.json — check if @testing-library/react, @testing-library/user-event, @vitejs/plugin-react, happy-dom are present in devDependencies
  </read_first>
  <action>
    PROXY FIX — patch `apps/web/src/app/api/coach/[...path]/route.ts`:

    Replace the existing `headers` block and `const body` line with the multipart-aware version. The key changes are:

    1. Detect multipart before building headers: `const isMultipart = req.headers.get('Content-Type')?.includes('multipart/form-data') ?? false`

    2. Headers block: when isMultipart is true, do NOT set Content-Type at all — let the original header pass through verbatim (the fetch() call will inherit it from `req.headers`). When not multipart, keep existing behavior: `headers['Content-Type'] = req.headers.get('Content-Type') ?? 'application/json'`. Authorization header logic is unchanged.

    3. Body: `const body = ['GET', 'HEAD'].includes(req.method) ? undefined : isMultipart ? await req.arrayBuffer() : await req.text()`

    4. Update the `fetch(upstreamUrl, ...)` call: when isMultipart, pass `headers` WITHOUT Content-Type key (so browser/fetch propagates the original multipart Content-Type with boundary). Use spread: pass original Content-Type for multipart via `req.headers.get('Content-Type')` added only to the fetch headers object, not the `headers` Record used for auth injection. Simplest correct approach: build the fetch headers inline — `{ ...(!isMultipart ? { 'Content-Type': req.headers.get('Content-Type') ?? 'application/json' } : { 'Content-Type': req.headers.get('Content-Type')! }), ...(session?.access_token ? { 'Authorization': \`Bearer \${session.access_token}\` } : {}) }`.

    Critical: the Content-Type header forwarded for multipart MUST include the boundary parameter verbatim from `req.headers.get('Content-Type')`. Never construct a new `multipart/form-data` string without the boundary.

    VITEST CONFIG — update `apps/web/vitest.config.ts`:
    - Change `include` to `['{src,test}/**/*.{spec,test}.{ts,tsx}']` to pick up .tsx test files
    - Add `environmentMatchGlobs: [['**/*.test.tsx', 'happy-dom']]` so .tsx tests get DOM environment while .ts tests remain in node environment
    - Keep `environment: 'node'` as the default

    PACKAGE DEPS — in `apps/web/package.json` devDependencies, add if missing:
    - `@testing-library/react` at `^16.0.0`
    - `@testing-library/user-event` at `^14.5.2`
    - `happy-dom` at `^15.0.0`
    - `@vitejs/plugin-react` at `^4.3.1` (required by vitest for JSX transform)

    After editing package.json, run `npm install --workspace=apps/web` to install the new deps.
  </action>
  <verify>
    <automated>
      cd C:/ziko-platform/apps/web && node -e "const c = require('./vitest.config.ts'); console.log('ok')" 2>/dev/null || npx tsc --noEmit --project tsconfig.json 2>&1 | head -5
    </automated>
    Check proxy file contains `isMultipart` and `arrayBuffer` — run:
    `grep -c "isMultipart" C:/ziko-platform/apps/web/src/app/api/coach/[...path]/route.ts`
    Must return 2 or more (variable declaration + usage in body + usage in headers = 3).
  </verify>
  <done>
    - `apps/web/src/app/api/coach/[...path]/route.ts` contains `isMultipart` variable and `arrayBuffer()` call
    - `apps/web/vitest.config.ts` include pattern ends with `.{ts,tsx}`
    - `apps/web/vitest.config.ts` contains `environmentMatchGlobs` with `happy-dom` for .tsx
    - `npm install` ran without errors in apps/web workspace
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 4 frontend test stubs (RED state)</name>
  <files>
    apps/web/src/components/coach/vocal/useVocalRecorder.test.ts
    apps/web/src/components/coach/vocal/useVocalTimer.test.ts
    apps/web/src/components/coach/vocal/vocalReducer.test.ts
    apps/web/src/components/coach/vocal/VocalReview.test.tsx
  </files>
  <read_first>
    - apps/web/vitest.config.ts — confirm include pattern updated (from Task 1)
    - .planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-VALIDATION.md — Wave 0 test file list and what each stub must cover
    - .planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-RESEARCH.md — VocalState discriminated union type, getSupportedMimeType function signature, useVocalRecorder return shape
  </read_first>
  <action>
    Create the `apps/web/src/components/coach/vocal/` directory if it does not exist.

    Each stub must: (a) compile without errors, (b) contain at least one `it` or `test` block, (c) fail (RED) because the implementation files do not exist yet. Use `vi.fn()` and `throw new Error('not implemented')` or missing-import errors as the failure mechanism. Do NOT import the real implementation files (they don't exist yet) — instead write stubs that will fail when the actual implementations are imported.

    CORRECT APPROACH: write stub tests that import from the not-yet-created source files. The test runner will fail with a module-not-found error, which counts as RED. This is preferable to `throw new Error('not implemented')` because it validates the import contract.

    useVocalRecorder.test.ts:
    - Import `useVocalRecorder` from `./useVocalRecorder`
    - Test: `it('produces a non-empty blob on stop', async () => { ... })` — body calls `useVocalRecorder()` and asserts blob.size > 0
    - Test: `it('negotiates mimeType via isTypeSupported', () => { ... })` — stubs MediaRecorder.isTypeSupported

    useVocalTimer.test.ts:
    - Import `useVocalTimer` from `./useVocalTimer`
    - Test: `it('auto-stops at 300 seconds', () => { ... })` — uses `vi.useFakeTimers()`, advances 300s, asserts onAutoStop called
    - Test: `it('formats elapsed time as mm:ss', () => { ... })` — asserts format of 65 seconds is "01:05"

    vocalReducer.test.ts:
    - Import `vocalReducer` from `./vocalReducer` (this file will be created in Wave 2 as part of VocalRetourPanel.tsx — stub the import path now)
    - Test all transitions: idle→recording (START_RECORDING), recording→transcribing (STOP_RECORDING), transcribing→review (TRANSCRIPTION_SUCCESS), transcribing→error (TRANSCRIPTION_ERROR), error→transcribing (RETRY), error→idle (RELAUNCH), review→idle (VALIDATE)
    - Each test: `expect(vocalReducer(prevState, action).status).toBe('expectedStatus')`

    VocalReview.test.tsx:
    - Import React from 'react' and `{ render, screen }` from '@testing-library/react'
    - Import `VocalReview` from `./VocalReview`
    - Test: `it('renders transcript text', () => { render(<VocalReview transcript="test text" onValidate={vi.fn()} onRelaunch={vi.fn()} />); expect(screen.getByText('test text')).toBeDefined() })`
    - Test: `it('renders Valider and Relancer buttons', () => { ... screen.getByText('Valider') ... screen.getByText('Relancer') })`

    Note: vocalReducer is exported separately from VocalRetourPanel to make it independently testable. Plan 01-04 will create `vocalReducer.ts` as a standalone file in `apps/web/src/components/coach/vocal/`.
  </action>
  <verify>
    <automated>
      cd C:/ziko-platform && npx vitest run apps/web/src/components/coach/vocal/ --passWithNoTests 2>&1 | tail -20
    </automated>
    Expected: tests fail with "Cannot find module" or similar import error — NOT with syntax errors. All 4 files should be picked up by the runner.
  </verify>
  <done>
    - 4 files exist in `apps/web/src/components/coach/vocal/`
    - `vitest run` picks up all 4 files (they appear in output)
    - Tests fail with module-not-found errors (RED) — not syntax errors
    - No test passes (no false greens)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Next.js proxy | Next.js API route receives multipart binary from browser; must forward verbatim |
| Next.js proxy→Hono | Binary body must not be re-encoded; Content-Type boundary must be preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-P1 | Tampering | apps/web proxy route — Content-Type stripping | mitigate | Forward `req.headers.get('Content-Type')` verbatim for multipart; never construct new Content-Type without boundary |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Package legitimacy audit in RESEARCH.md confirms all new deps (@testing-library, happy-dom) are standard; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
After both tasks complete:

1. Proxy binary safety: `grep -c "arrayBuffer" C:/ziko-platform/apps/web/src/app/api/coach/[...path]/route.ts` → must return 1
2. Proxy Content-Type preserved: `grep -c "isMultipart" C:/ziko-platform/apps/web/src/app/api/coach/[...path]/route.ts` → must return >= 2
3. Test stubs exist: `ls C:/ziko-platform/apps/web/src/components/coach/vocal/*.test.*` → shows 4 files
4. Vitest picks up .tsx: `grep "tsx" C:/ziko-platform/apps/web/vitest.config.ts` → returns match
5. Tests are RED: `cd C:/ziko-platform && npx vitest run apps/web/src/components/coach/vocal/` → exits non-zero (failures expected)
</verification>

<success_criteria>
- Proxy route branches on `Content-Type: multipart/form-data` and uses `req.arrayBuffer()` for binary bodies
- Proxy forwards the original Content-Type header (with boundary) verbatim for multipart requests
- Web vitest config picks up `.tsx` test files with `happy-dom` environment
- All 4 frontend test stubs exist, compile, and fail (RED) due to missing source files
- `npm install` in apps/web workspace succeeded with new testing deps
</success_criteria>

<output>
Create `.planning/workstreams/retour-vocal/phases/01-transcription-pipeline/01-01-SUMMARY.md` when done
</output>
