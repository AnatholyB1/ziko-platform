---
phase: 43-coach-exercises-backend-ui
plan: 4
type: execute
wave: 4
depends_on: [43-03]
files_modified:
  - apps/web/src/components/coach/ExerciseSlideOver.tsx
  - apps/web/src/app/[locale]/(coach)/coach/exercises/ExercisesClient.tsx
  - apps/web/src/app/[locale]/(coach)/coach/exercises/page.tsx
  - apps/web/src/app/[locale]/(coach)/coach/exercises/loading.tsx
  - apps/web/src/components/coach/CoachSidebar.tsx
autonomous: true
requirements:
  - EXLIB-01
  - EXLIB-02
  - EXLIB-03
  - EXLIB-04

must_haves:
  truths:
    - "ExerciseSlideOver renders as fixed right panel w-[480px] with translate-x-0/translate-x-full Tailwind animation"
    - "ExerciseSlideOver uses role='dialog' aria-modal='true' aria-labelledby pointing to title"
    - "ExerciseSlideOver traps focus on open (first focusable = name input), restores focus on close"
    - "ExerciseSlideOver closes on Escape key via window.addEventListener('keydown')"
    - "ExerciseSlideOver validates name (required) and category (required) on submit — shows French error messages, GSAP shake animation"
    - "ExercisesClient renders empty state / loading skeleton / populated list per UI-SPEC Screen 1"
    - "ExercisesClient filter bar: Tous/Force/Cardio/Mobilité/HIIT/Hyrox/Autre — active pill bg-primary text-white"
    - "GSAP stagger animation on exercise list: gsap.from('.exercise-row', { y: 8, opacity: 0, duration: 0.2, stagger: 0.04 })"
    - "page.tsx fetches exercises server-side via fetch(`${apiUrl}/coach/exercises`) with auth header"
    - "CoachSidebar has 'Exercices' nav item with IoBarbellOutline icon between Programmes and Imports"
  artifacts:
    - path: "apps/web/src/components/coach/ExerciseSlideOver.tsx"
      provides: "Create/edit exercise drawer"
      contains: "ExerciseSlideOver"
    - path: "apps/web/src/app/[locale]/(coach)/coach/exercises/page.tsx"
      provides: "Server component for /coach/exercises route"
      contains: "exercises"
    - path: "apps/web/src/components/coach/CoachSidebar.tsx"
      provides: "Updated sidebar with Exercices nav item"
      contains: "Exercices"
---

<objective>
Wire up the complete exercises page: SlideOver drawer (create/edit), ExercisesClient (list + filter + GSAP animations), page.tsx server component, loading.tsx skeleton, and CoachSidebar nav item.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-UI-SPEC.md

<interfaces>
From apps/web/src/components/coach/SessionSlideOver.tsx — SlideOver structure to mirror:
- fixed right-0 top-0 h-full panel; Escape key via window.addEventListener; useEffect for cleanup
- SessionSlideOver width note: ExerciseSlideOver uses w-[480px] (UI-SPEC §Screen 2)

From apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx — server component pattern:
- import { createServerSupabase } from '@/lib/supabase/server'
- const { data: { session } } = await supabase.auth.getSession()
- fetch(`${process.env.NEXT_PUBLIC_API_URL}/coach/programs`, { headers: { Authorization: `Bearer ${session.access_token}` } })
- Pass accessToken, apiUrl, locale to client component

From apps/web/src/components/coach/CoachSidebar.tsx:
- Add after Programmes entry: { label: 'Exercices', href: `/${locale}/coach/exercises`, icon: IoBarbellOutline, disabled: false }
- Add IoBarbellOutline to imports from react-icons/io5

GSAP: gsap is already in apps/web/package.json. Import: import gsap from 'gsap'. Run animations in useEffect after data loads.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExerciseSlideOver.tsx</name>
  <files>apps/web/src/components/coach/ExerciseSlideOver.tsx</files>
  <read_first>
    - apps/web/src/components/coach/SessionSlideOver.tsx — Escape key pattern, fixed panel structure, useEffect lifecycle
    - apps/web/src/components/coach/MuscleChipSelector.tsx — import and usage pattern
    - apps/web/src/components/coach/ExerciseMediaUpload.tsx — import and usage pattern
    - .planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-UI-SPEC.md — Screen 2 all states (2A–2G): form fields, footer, validation errors, saving state
  </read_first>
  <action>
    'use client' component.

    Props: { exercise: CoachExercise | null; isOpen: boolean; onClose: () => void;
             onSave: (data: CreateExerciseBody | UpdateExerciseBody) => Promise<void>;
             userId: string; jwt: string }

    Panel: fixed inset-y-0 right-0 w-[480px] bg-white shadow-xl flex flex-col z-50
    Animation via className: translate-x-0 when isOpen, translate-x-full when !isOpen; transition-transform duration-200 ease-out (for open) / duration-150 ease-in (for close)

    Overlay backdrop: fixed inset-0 z-40 bg-transparent (click closes, aria-hidden="true")

    Escape key: useEffect adds/removes window keydown listener; calls onClose on 'Escape'.

    Focus trap: useEffect — when isOpen becomes true, focus the name input (useRef + .focus()). On close, restore focus to trigger element (pass triggerRef as optional prop, or use document.activeElement snapshot).

    ARIA: role="dialog" aria-modal="true" aria-labelledby="exercise-slideover-title" on panel. Title element has id="exercise-slideover-title".

    Header: flex items-start justify-between px-5 pt-5 pb-4. Title: isOpen&&exercise ? "Modifier l'exercice" : "Nouvel exercice". Subtitle: isOpen&&exercise ? exercise.name (text-sm text-muted) : "Renseignez les informations de base." Close button: aria-label="Fermer".

    Form body (flex-1 overflow-y-auto px-5 py-4 flex flex-col gap-5):
    - Name input (required): text, max 100, placeholder "Ex. : Squat bulgare, Hip thrust, RDL…", border-danger + error text on validation fail
    - Description textarea (optional): 3 rows, max 500, placeholder "Consignes d'exécution, points de vigilance…"
    - Category select (required): options Force/Cardio/Mobilité/HIIT/Hyrox/Autre, border-danger on validation fail
    - MuscleChipSelector for muscle_groups
    - Divider border-t border-border my-2
    - ExerciseMediaUpload section label "MÉDIAS DE DÉMO"
    - ExerciseMediaUpload component

    Validation on submit: name empty → setNameError("Le nom de l'exercice est obligatoire."); category empty → setCategoryError("Veuillez choisir une catégorie."); GSAP shake: gsap.to(invalidFieldRef.current, { x: [-6, 6, -4, 4, 0], duration: 0.3, ease: 'none' }); abort submit if errors.

    Saving state: isSaving — submit button shows "Enregistrement…" + disabled opacity-60 cursor-not-allowed.

    Footer (px-5 py-4 border-t border-border flex items-center justify-end gap-3 shrink-0):
    - Cancel button (text-sm text-muted): "Annuler" → onClose
    - Submit button (bg-primary text-white rounded-lg px-5 py-2 text-sm font-bold): label "Créer l'exercice" (create) or "Enregistrer les modifications" (edit); disabled during isSaving
  </action>
  <acceptance_criteria>
    - role="dialog" aria-modal="true" present on panel element
    - Escape key closes the panel (window keydown listener in useEffect)
    - Name input focused on open via useRef + .focus()
    - Validation shows French error messages and prevents submit
    - GSAP shake called on invalid fields (gsap.to with x array)
    - MuscleChipSelector and ExerciseMediaUpload rendered in form body
    - Submit button disabled with "Enregistrement…" text during isSaving
    - No window.confirm anywhere in file
  </acceptance_criteria>
  <done>grep finds role="dialog" and "Enregistrement" in ExerciseSlideOver.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: ExercisesClient.tsx + loading.tsx</name>
  <files>
    apps/web/src/app/[locale]/(coach)/coach/exercises/ExercisesClient.tsx
    apps/web/src/app/[locale]/(coach)/coach/exercises/loading.tsx
  </files>
  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/programs/ProgramsClient.tsx — page header pattern (title + subtitle + CTA), fetch pattern (fetch apiUrl with Bearer), router.refresh() after mutation
    - apps/web/src/components/coach/skeletons.tsx — skeleton pattern for loading states
    - .planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-UI-SPEC.md — Screen 1: empty state, loading skeleton, populated state, filter bar, search input threshold (>8)
  </read_first>
  <action>
    ExercisesClient.tsx ('use client'):

    Props: { initialExercises: CoachExercise[]; accessToken: string; apiUrl: string; locale: string; userId: string }

    State: exercises (initialized from initialExercises), activeCategory ('Tous'|category string), searchQuery, slideOverOpen, editingExercise (CoachExercise|null)

    CATEGORIES = ['Tous','Force','Cardio','Mobilité','HIIT','Hyrox','Autre']

    Filtered list: activeCategory==='Tous' ? exercises : exercises.filter(e => e.category === activeCategory), then filter by searchQuery if non-empty (case-insensitive name match).

    Page header (flex items-start justify-between mb-8):
    - Left: "Exercices" text-2xl font-bold text-text + count badge "{N} exercice(s)" (pluralize at N>1) + "Votre bibliothèque personnelle" text-sm text-muted mt-1
    - Right: "Nouvel exercice" CTA button (bg-primary text-white rounded-xl px-4 py-2 text-sm font-bold) → setSlideOverOpen(true), setEditingExercise(null)

    Filter bar (flex gap-2 mb-4 flex-wrap role="group" aria-label="Filtrer par catégorie"):
    - Active pill: bg-primary text-white rounded-full px-3 py-1 text-xs font-bold border border-primary
    - Inactive pill: bg-white text-muted rounded-full px-3 py-1 text-xs font-normal border border-border hover:border-primary hover:text-primary

    Search input: renders only when exercises.length > 8; border border-border rounded-lg px-3 py-2 text-sm w-full max-w-xs placeholder "Rechercher un exercice…"

    Empty state (filtered.length === 0 && exercises.length === 0): flex flex-col items-center py-16 text-center — IoBarbell text-5xl text-muted, heading "Aucun exercice pour l'instant", body copy, CTA "Créer mon premier exercice"

    Exercise list (flex flex-col gap-3): map filtered → ExerciseRow with onEdit and onDelete handlers.

    GSAP stagger: useEffect watching exercises — when exercises.length > 0: gsap.from('.exercise-row', { y: 8, opacity: 0, duration: 0.2, stagger: 0.04, ease: 'power2.out' })

    handleSave(data): POST (create) or PATCH /:id (edit) to `${apiUrl}/coach/exercises[/:id]`, then router.refresh() + update local state optimistically.
    handleDelete(id): DELETE `${apiUrl}/coach/exercises/${id}`, gsap.to(rowRef, { height: 0, opacity: 0, duration: 0.15 }) before removing from state.

    Renders ExerciseSlideOver with isOpen, exercise (editingExercise), onClose, onSave, userId, jwt (accessToken).

    loading.tsx: Export default function Loading() — returns page header immediately (not skeletonized) + skeleton filter bar (4 rounded-full bg-border animate-pulse) + 4 skeleton rows (bg-white border border-border rounded-2xl p-4 animate-pulse with h-4 bg-border rounded placeholders). All skeleton elements have aria-hidden="true". Parent div has aria-busy="true".
  </action>
  <acceptance_criteria>
    - Filter bar renders 7 pills (Tous + 6 categories) with role="group"
    - Active filter pill has bg-primary text-white classes
    - Search input only visible when exercises.length > 8
    - Empty state renders IoBarbell icon + "Aucun exercice pour l'instant"
    - GSAP gsap.from('.exercise-row') called after data loads
    - loading.tsx renders 4 animate-pulse skeleton rows with aria-hidden="true"
    - ExerciseSlideOver rendered and controlled by ExercisesClient state
  </acceptance_criteria>
  <done>grep finds 'Aucun exercice' and 'exercise-row' in ExercisesClient; loading.tsx has animate-pulse.</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx server component</name>
  <files>apps/web/src/app/[locale]/(coach)/coach/exercises/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx — canonical server component pattern: createServerSupabase, auth.getSession, fetch with Bearer, pass to client component
    - apps/web/src/app/[locale]/(coach)/coach/exercises/ExercisesClient.tsx — props interface
  </read_first>
  <action>
    Server component (no 'use client').

    1. const supabase = await createServerSupabase()
    2. const { data: { session } } = await supabase.auth.getSession() — redirect to login if !session
    3. const apiUrl = process.env.NEXT_PUBLIC_API_URL ?? ''
    4. fetch exercises: GET `${apiUrl}/coach/exercises` with { headers: { Authorization: `Bearer ${session.access_token}` }, cache: 'no-store' }
    5. On error or non-ok response: pass initialExercises=[] (graceful degradation)
    6. Return <ExercisesClient initialExercises={data.exercises ?? []} accessToken={session.access_token} apiUrl={apiUrl} locale={params.locale} userId={session.user.id} />

    Export generateMetadata returning title "Exercices | Ziko Coach".
  </action>
  <acceptance_criteria>
    - page.tsx is a server component (no 'use client' directive)
    - Fetches /coach/exercises with Authorization Bearer header
    - Passes userId={session.user.id} to ExercisesClient
    - Exports generateMetadata with title "Exercices | Ziko Coach"
    - Graceful degradation: initialExercises=[] on fetch error
  </acceptance_criteria>
  <done>File has no 'use client'; grep finds generateMetadata and Authorization Bearer.</done>
</task>

<task type="auto">
  <name>Task 4: Add Exercices to CoachSidebar</name>
  <files>apps/web/src/components/coach/CoachSidebar.tsx</files>
  <read_first>
    - apps/web/src/components/coach/CoachSidebar.tsx — current 7 nav items; add between Programmes and Imports
  </read_first>
  <action>
    1. Add IoBarbellOutline to the import from 'react-icons/io5' (alongside IoGridOutline etc.)
    2. In getNavItems(), insert after the Programmes entry and before Imports:
       { label: 'Exercices', href: `/${locale}/coach/exercises`, icon: IoBarbellOutline, disabled: false }
    No other changes.
  </action>
  <acceptance_criteria>
    - IoBarbellOutline imported from react-icons/io5
    - 'Exercices' nav item present in getNavItems() between 'Programmes' and 'Imports'
    - All existing 7 nav items still present (total: 8 items)
  </acceptance_criteria>
  <done>grep finds 'Exercices' in CoachSidebar.tsx nav items.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-43-04-01 | Spoofing | page.tsx — session check | mitigate | Redirect to login if !session before any data fetch |
| T-43-04-02 | XSS | ExercisesClient — exercise.name in DOM | mitigate | React escapes all JSX text interpolations by default; no dangerouslySetInnerHTML |
| T-43-04-03 | Tampering | SlideOver form submission | mitigate | Client-side validation; server re-validates via EXLIB-01 POST validation (Plan 43-02 Task 3) |
| T-43-04-SC | Supply Chain | gsap | accept | gsap already in apps/web/package.json; no new install |
</threat_model>

<verification>
1. Navigate to /coach/exercises → page loads with empty state or exercise list (no 500)
2. Click "Nouvel exercice" → SlideOver slides in from right
3. Submit with empty name → French error "Le nom de l'exercice est obligatoire." + field shake
4. Create exercise → appears in list with GSAP stagger animation
5. Delete exercise → inline confirm shown, no browser dialog; row animates out on confirm
6. CoachSidebar shows "Exercices" link between Programmes and Imports
7. `rtk tsc --noEmit` in apps/web exits 0
</verification>

<success_criteria>
/coach/exercises route accessible with full CRUD via SlideOver. CoachSidebar updated. All 6 requirements (EXLIB-01 through EXLIB-04) covered by plans 43-01 through 43-04.
</success_criteria>

<output>
Create `.planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-04-SUMMARY.md` when done.
</output>
