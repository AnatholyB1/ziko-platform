---
phase: 43-coach-exercises-backend-ui
plan: 2
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - backend/api/src/coach/exercises/types.ts
  - backend/api/src/coach/exercises/db.ts
  - backend/api/src/coach/exercises/service.ts
  - backend/api/src/app.ts
autonomous: true
requirements:
  - EXLIB-01
  - EXLIB-02
  - EXLIB-03
  - EXLIB-04

must_haves:
  truths:
    - "backend/api/src/coach/exercises/types.ts exports CoachExercise, CreateExerciseBody, UpdateExerciseBody interfaces"
    - "backend/api/src/coach/exercises/db.ts exports listExercises, createExercise, updateExercise, deleteExercise — all use createUserClient(jwt) pattern"
    - "deleteExercise deletes storage files (video_path, photo_path) before deleting the DB row using supabase.storage.from('coach-exercises').remove([path])"
    - "backend/api/src/coach/exercises/service.ts exports exercisesRouter with GET /, POST /, PATCH /:id, DELETE /:id — all protected by authMiddleware"
    - "backend/api/src/app.ts registers app.route('/coach/exercises', exercisesRouter)"
    - "GET /coach/exercises returns { exercises: CoachExercise[] } filtered by coach_id via RLS"
    - "POST /coach/exercises validates name (required, max 100) and category (required, enum) — returns 400 on invalid"
    - "PATCH /coach/exercises/:id returns 404 if exercise not found or not owned"
    - "DELETE /coach/exercises/:id removes storage files then DB row, returns { deleted: true }"
  artifacts:
    - path: "backend/api/src/coach/exercises/service.ts"
      provides: "Hono router for /coach/exercises CRUD"
      contains: "exercisesRouter"
    - path: "backend/api/src/app.ts"
      provides: "Route registration"
      contains: "/coach/exercises"
  key_links:
    - from: "exercisesRouter"
      to: "app.ts"
      via: "app.route('/coach/exercises', exercisesRouter)"
      pattern: "coach/exercises"
---

<objective>
Build the Hono backend module for coach custom exercises: types, DB functions (CRUD + storage cleanup), and the HTTP router. Register it in app.ts.

Purpose: The frontend (Plans 43-03/04) cannot call any exercises API until this exists.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/custom-coach/ROADMAP.md
@.planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-UI-SPEC.md

<interfaces>
From backend/api/src/coach/programs/db.ts — createUserClient pattern:
export function createUserClient(jwt: string) — uses SUPABASE_PUBLISHABLE_KEY + Bearer auth header. All coach DB reads use this (ARCH-03).

From backend/api/src/coach/programs/service.ts — Hono router pattern:
const programsRouter = new Hono(); programsRouter.use('*', authMiddleware); handlers extract const { userId, jwt } = c.get('auth');

From backend/api/src/routes/storage.ts — storage delete pattern:
supabase.storage.from(bucket).remove([path]) — paths array.
Storage delete in deleteExercise must use the user's JWT client (not service key) since the storage RLS allows authenticated DELETE for own prefix.

From backend/api/src/app.ts — registration:
import { exercisesRouter } from './coach/exercises/service.js'; app.route('/coach/exercises', exercisesRouter);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create types.ts</name>
  <files>backend/api/src/coach/exercises/types.ts</files>
  <read_first>
    - backend/api/src/coach/programs/types.ts — interface naming, export style, nullable vs optional field conventions
    - .planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-UI-SPEC.md — category values, muscle_groups, video_path/photo_path fields
  </read_first>
  <action>
    Create backend/api/src/coach/exercises/types.ts with:

    export interface CoachExercise {
      id: string;
      coach_id: string;
      name: string;
      description: string | null;
      category: string;
      muscle_groups: string[];
      video_path: string | null;
      photo_path: string | null;
      created_at: string;
      updated_at: string;
    }

    export interface CreateExerciseBody {
      name: string;               // required, max 100
      description?: string | null; // optional, max 500
      category: string;           // required, enum: Force|Cardio|Mobilité|HIIT|Hyrox|Autre
      muscle_groups?: string[];   // optional, defaults to []
      video_path?: string | null;
      photo_path?: string | null;
    }

    export interface UpdateExerciseBody {
      name?: string;
      description?: string | null;
      category?: string;
      muscle_groups?: string[];
      video_path?: string | null;
      photo_path?: string | null;
    }

    export const EXERCISE_CATEGORIES = ['Force','Cardio','Mobilité','HIIT','Hyrox','Autre'] as const;
  </action>
  <acceptance_criteria>
    - File exists and exports CoachExercise, CreateExerciseBody, UpdateExerciseBody, EXERCISE_CATEGORIES
    - CoachExercise has video_path and photo_path as string | null
    - EXERCISE_CATEGORIES tuple contains all 6 values
  </acceptance_criteria>
  <done>File exists with all 4 exports.</done>
</task>

<task type="auto">
  <name>Task 2: Create db.ts — CRUD + storage cleanup</name>
  <files>backend/api/src/coach/exercises/db.ts</files>
  <read_first>
    - backend/api/src/coach/programs/db.ts — createUserClient import path, function signatures, error handling pattern (if error) throw new Error(error.message)
    - backend/api/src/coach/exercises/types.ts — interfaces just created
  </read_first>
  <action>
    Create backend/api/src/coach/exercises/db.ts with these 4 exported functions:

    listExercises(jwt, coachId): queries coach_exercises WHERE coach_id=coachId (RLS handles this but be explicit for clarity), ORDER BY created_at DESC, returns { exercises: CoachExercise[] }

    createExercise(jwt, coachId, body: CreateExerciseBody): inserts row with coach_id=coachId, muscle_groups=body.muscle_groups??[], returns created row

    updateExercise(jwt, coachId, id, body: UpdateExerciseBody): updates only provided fields + updated_at=new Date().toISOString(), WHERE id=id AND coach_id=coachId, returns updated row or null if not found

    deleteExercise(jwt, coachId, id): 
      1. Fetch the row first (SELECT video_path, photo_path WHERE id=id AND coach_id=coachId)
      2. If not found, return { deleted: false }
      3. Delete storage files if paths exist: userClient.storage.from('coach-exercises').remove([path]) for each non-null path
      4. Delete DB row WHERE id=id AND coach_id=coachId
      5. Return { deleted: true }

    All functions use createUserClient(jwt) — same pattern as programs/db.ts (ARCH-03).
  </action>
  <acceptance_criteria>
    - File exports listExercises, createExercise, updateExercise, deleteExercise
    - deleteExercise calls supabase.storage.from('coach-exercises').remove(...) before deleting DB row
    - All functions throw Error on Supabase error (no silent failures)
    - createExercise sets muscle_groups to body.muscle_groups ?? [] (never undefined)
    - updateExercise sets updated_at explicitly
  </acceptance_criteria>
  <done>File exports 4 functions; grep finds storage.from('coach-exercises').remove in deleteExercise.</done>
</task>

<task type="auto">
  <name>Task 3: Create service.ts — Hono router</name>
  <files>backend/api/src/coach/exercises/service.ts</files>
  <read_first>
    - backend/api/src/coach/programs/service.ts — router setup, authMiddleware use, auth extraction pattern (c.get('auth')), error handling (try/catch → 500), 400/404 patterns
    - backend/api/src/coach/exercises/db.ts — function signatures
    - backend/api/src/coach/exercises/types.ts — EXERCISE_CATEGORIES for validation
  </read_first>
  <action>
    Create backend/api/src/coach/exercises/service.ts:

    const exercisesRouter = new Hono();
    exercisesRouter.use('*', authMiddleware);

    GET / → listExercises(jwt, userId) → c.json(result)

    POST / → validate body.name (required, trim, max 100) and body.category (must be in EXERCISE_CATEGORIES) → return 400 with { error, field } if invalid → createExercise(jwt, userId, body) → c.json(data, 201)

    PATCH /:id → updateExercise(jwt, userId, id, body) → if null return 404 { error: 'Exercise not found' } → c.json(data)

    DELETE /:id → deleteExercise(jwt, userId, id) → if !deleted return 404 → c.json({ deleted: true })

    All handlers wrapped in try/catch → c.json({ error: 'Internal server error' }, 500).
    Extract const { userId, jwt } = c.get('auth') from authMiddleware payload.

    export { exercisesRouter };
  </action>
  <acceptance_criteria>
    - File exports exercisesRouter
    - All 4 routes present: GET /, POST /, PATCH /:id, DELETE /:id
    - POST validates name + category, returns 400 on missing/invalid
    - PATCH/:id and DELETE/:id return 404 when not found
    - authMiddleware applied via exercisesRouter.use('*', authMiddleware)
  </acceptance_criteria>
  <done>File exports exercisesRouter with 4 routes; grep finds authMiddleware use.</done>
</task>

<task type="auto">
  <name>Task 4: Register exercisesRouter in app.ts</name>
  <files>backend/api/src/app.ts</files>
  <read_first>
    - backend/api/src/app.ts — current imports and app.route registrations; add after coachAiRouter registration
  </read_first>
  <action>
    In backend/api/src/app.ts:
    1. Add import: import { exercisesRouter } from './coach/exercises/service.js';
    2. Add route after line `app.route('/coach/ai', coachAiRouter);`:
       app.route('/coach/exercises', exercisesRouter);
    No other changes.
  </action>
  <acceptance_criteria>
    - backend/api/src/app.ts imports exercisesRouter from './coach/exercises/service.js'
    - app.route('/coach/exercises', exercisesRouter) present in file
    - All existing routes (ai, programs, clients, etc.) still present — no regressions
  </acceptance_criteria>
  <done>grep confirms both import and route registration in app.ts.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02-01 | Spoofing / IDOR | PATCH/DELETE /:id | mitigate | DB queries include AND coach_id=coachId — RLS + explicit coach filter |
| T-43-02-02 | Tampering | POST body injection | mitigate | category validated against EXERCISE_CATEGORIES enum; name/description length checked |
| T-43-02-03 | Info Disclosure | Storage path leakage | mitigate | video_path/photo_path are internal storage paths; signed read URLs generated on client, not returned by API |
| T-43-02-SC | Supply Chain | No new npm installs | accept | Reuses existing @supabase/supabase-js and hono already in package.json |
</threat_model>

<verification>
1. `grep "exercisesRouter" backend/api/src/app.ts` returns import + route lines
2. `grep "coach-exercises" backend/api/src/coach/exercises/db.ts` confirms storage bucket name
3. TypeScript: `rtk tsc --noEmit` in backend/api/ exits 0
</verification>

<success_criteria>
Four files created/updated. Backend accepts GET/POST/PATCH/DELETE on /coach/exercises with auth, validation, and storage cleanup on delete.
</success_criteria>

<output>
Create `.planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-02-SUMMARY.md` when done.
</output>
