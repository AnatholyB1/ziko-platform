---
phase: 43-coach-exercises-backend-ui
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/055_coach_exercises_schema.sql
  - apps/web/src/app/api/storage/upload-url/route.ts
  - backend/api/src/routes/storage.ts
autonomous: true
requirements:
  - EXLIB-01
  - EXLIB-02
  - EXLIB-03
  - EXLIB-04

must_haves:
  truths:
    - "supabase/migrations/055_coach_exercises_schema.sql exists and starts with SET LOCAL lock_timeout = '5s';"
    - "Table coach_exercises exists with columns: id UUID PK, coach_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, name TEXT NOT NULL, description TEXT, category TEXT NOT NULL, muscle_groups TEXT[] NOT NULL DEFAULT '{}', video_path TEXT, photo_path TEXT, created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ"
    - "coach_exercises has RLS enabled with policy FOR ALL USING (auth.uid() = coach_id) WITH CHECK (auth.uid() = coach_id)"
    - "coach_exercises has CHECK (category IN ('Force','Cardio','Mobilité','HIIT','Hyrox','Autre'))"
    - "Storage bucket 'coach-exercises' created via INSERT INTO storage.buckets with public: false"
    - "Storage objects policies: coach_exercises_insert (FOR INSERT), coach_exercises_select (FOR SELECT), coach_exercises_delete (FOR DELETE) — all scoped to auth.uid()::text prefix"
    - "'coach-exercises' added to ALLOWED_BUCKETS in apps/web/src/app/api/storage/upload-url/route.ts"
    - "'coach-exercises' added to ALLOWED_BUCKETS in backend/api/src/routes/storage.ts"
  artifacts:
    - path: "supabase/migrations/055_coach_exercises_schema.sql"
      provides: "coach_exercises table definition + RLS + coach-exercises storage bucket"
      contains: "coach_exercises"
    - path: "apps/web/src/app/api/storage/upload-url/route.ts"
      provides: "Next.js API route upload-url allowlist updated"
      contains: "coach-exercises"
    - path: "backend/api/src/routes/storage.ts"
      provides: "Hono storage router allowlist updated"
      contains: "coach-exercises"
  key_links:
    - from: "coach_exercises.coach_id"
      to: "auth.users(id)"
      via: "REFERENCES with ON DELETE CASCADE"
      pattern: "REFERENCES auth\\.users\\(id\\) ON DELETE CASCADE"
    - from: "coach_exercises RLS"
      to: "auth.uid()"
      via: "USING (auth.uid() = coach_id)"
      pattern: "auth\\.uid\\(\\) = coach_id"
---

<objective>
Create the Supabase migration that provisions the `coach_exercises` table + `coach-exercises` Storage bucket, and add `coach-exercises` to the ALLOWED_BUCKETS allowlists in both the Next.js API route and the Hono backend.

Purpose: Without this, no exercise can be persisted and no file can be uploaded. All subsequent plans depend on this schema foundation.
Output: Migration ready for `supabase db push`; upload-url endpoints unblocked for the `coach-exercises` bucket.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/custom-coach/ROADMAP.md
@.planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-UI-SPEC.md

<interfaces>
<!-- Canonical migration pattern — replicate exactly -->
From supabase/migrations/037_coach_kyc_bucket.sql:
- Header: SET LOCAL lock_timeout = '5s';
- Bucket: INSERT INTO storage.buckets (id, name, public) VALUES ('bucket-name', 'bucket-name', false) ON CONFLICT (id) DO NOTHING;
- Policies: CREATE POLICY "name" ON storage.objects FOR INSERT/SELECT/DELETE TO authenticated WITH CHECK/USING (bucket_id = 'x' AND (storage.foldername(name))[1] = auth.uid()::text)

From supabase/migrations/050_coach_ai_schema.sql:
- Table: CREATE TABLE IF NOT EXISTS public.{table} (...)
- RLS: ALTER TABLE public.{table} ENABLE ROW LEVEL SECURITY;
- Policy: CREATE POLICY "{table}_own" ON public.{table} FOR ALL USING (auth.uid() = coach_id) WITH CHECK (auth.uid() = coach_id)
- Index naming: idx_{table}_{purpose} with IF NOT EXISTS

ALLOWED_BUCKETS pattern (both files):
- apps/web/src/app/api/storage/upload-url/route.ts — const ALLOWED_BUCKETS = ['profile-photos', 'scan-photos', 'exports', 'coach-kyc'] as const;
- backend/api/src/routes/storage.ts — const ALLOWED_BUCKETS = ['profile-photos', 'scan-photos', 'exports', 'coach-kyc', 'ai-imports'] as const;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 055 — coach_exercises table + coach-exercises bucket</name>
  <files>supabase/migrations/055_coach_exercises_schema.sql</files>
  <read_first>
    - supabase/migrations/053_referral_schema.sql — confirms 055 is safe (054 reserved by custom-widget workstream per .planning/workstreams/custom-widget/phases/01-db-api-foundation/01-PLAN-1.md); verify no 054 or 055 file exists before writing
    - supabase/migrations/037_coach_kyc_bucket.sql — canonical pattern for Storage bucket creation + object RLS policies; replicate exactly for coach-exercises
    - supabase/migrations/050_coach_ai_schema.sql — canonical migration header, table definition, RLS enable + policy, index naming; replicate exactly for coach_exercises
    - .planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-UI-SPEC.md — category enum values (Force/Cardio/Mobilité/HIIT/Hyrox/Autre) and muscle_groups array requirement
  </read_first>
  <action>
    Create supabase/migrations/055_coach_exercises_schema.sql.

    IMPORTANT: Before writing, run `ls supabase/migrations/054_* supabase/migrations/055_*` to confirm the target number is free. If 054 is unclaimed, use 054 instead and update the filename accordingly.

    Start with: SET LOCAL lock_timeout = '5s';

    Section 1 — coach_exercises table:
    - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
    - coach_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE
    - name TEXT NOT NULL CHECK (char_length(name) <= 100)
    - description TEXT CHECK (description IS NULL OR char_length(description) <= 500)
    - category TEXT NOT NULL CHECK (category IN ('Force','Cardio','Mobilité','HIIT','Hyrox','Autre'))
    - muscle_groups TEXT[] NOT NULL DEFAULT '{}'
    - video_path TEXT — storage path, NOT a signed URL (per UI-SPEC design rationale)
    - photo_path TEXT — storage path, NOT a signed URL
    - created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    - updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
    Enable RLS. Policy "coach_exercises_own" FOR ALL USING (auth.uid() = coach_id) WITH CHECK (auth.uid() = coach_id).
    Index idx_coach_exercises_coach ON public.coach_exercises(coach_id) IF NOT EXISTS.

    Section 2 — coach-exercises Storage bucket + RLS:
    INSERT INTO storage.buckets (id, name, public) VALUES ('coach-exercises', 'coach-exercises', false) ON CONFLICT (id) DO NOTHING;
    Three policies on storage.objects scoped to bucket_id = 'coach-exercises' AND (storage.foldername(name))[1] = auth.uid()::text:
    - "coach_exercises_insert" FOR INSERT TO authenticated WITH CHECK
    - "coach_exercises_select" FOR SELECT TO authenticated USING
    - "coach_exercises_delete" FOR DELETE TO authenticated USING

    Do NOT add triggers for updated_at (API sets it explicitly on update, same as programs module).
  </action>
  <verify>
    <automated>grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/055_coach_exercises_schema.sql && grep "coach_exercises_own" supabase/migrations/055_coach_exercises_schema.sql && grep "coach-exercises" supabase/migrations/055_coach_exercises_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/055_coach_exercises_schema.sql (or 054 if free) exists
    - First line: SET LOCAL lock_timeout = '5s';
    - CREATE TABLE IF NOT EXISTS public.coach_exercises with all 10 columns
    - category CHECK constraint includes all 6 values: Force, Cardio, Mobilité, HIIT, Hyrox, Autre
    - muscle_groups is TEXT[] NOT NULL DEFAULT '{}'
    - video_path and photo_path are nullable TEXT (no NOT NULL)
    - ALTER TABLE public.coach_exercises ENABLE ROW LEVEL SECURITY;
    - Policy "coach_exercises_own" FOR ALL USING (auth.uid() = coach_id) WITH CHECK (auth.uid() = coach_id)
    - Index idx_coach_exercises_coach with IF NOT EXISTS
    - INSERT INTO storage.buckets ('coach-exercises', 'coach-exercises', false) ON CONFLICT (id) DO NOTHING
    - Three storage policies: insert, select, delete — all scoped to coach-exercises bucket + uid prefix
    - No updated_at trigger
  </acceptance_criteria>
  <done>File exists, grep finds "coach_exercises_own" and "coach-exercises" bucket insert.</done>
</task>

<task type="auto">
  <name>Task 2: Add coach-exercises to Next.js storage upload-url allowlist</name>
  <files>apps/web/src/app/api/storage/upload-url/route.ts</files>
  <read_first>
    - apps/web/src/app/api/storage/upload-url/route.ts — current ALLOWED_BUCKETS array; add 'coach-exercises' as the 5th entry
  </read_first>
  <action>
    In apps/web/src/app/api/storage/upload-url/route.ts, update the ALLOWED_BUCKETS const:
    From: const ALLOWED_BUCKETS = ['profile-photos', 'scan-photos', 'exports', 'coach-kyc'] as const;
    To:   const ALLOWED_BUCKETS = ['profile-photos', 'scan-photos', 'exports', 'coach-kyc', 'coach-exercises'] as const;
    Update the AllowedBucket type alias accordingly (it is derived from typeof ALLOWED_BUCKETS[number] — no manual change needed if TypeScript infers it).
  </action>
  <verify>
    <automated>grep "coach-exercises" apps/web/src/app/api/storage/upload-url/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/api/storage/upload-url/route.ts contains 'coach-exercises' in ALLOWED_BUCKETS
    - No other changes to the file (auth logic, path validation, response shape unchanged)
  </acceptance_criteria>
  <done>grep confirms 'coach-exercises' present in ALLOWED_BUCKETS.</done>
</task>

<task type="auto">
  <name>Task 3: Add coach-exercises to Hono backend storage allowlist</name>
  <files>backend/api/src/routes/storage.ts</files>
  <read_first>
    - backend/api/src/routes/storage.ts — current ALLOWED_BUCKETS array; add 'coach-exercises' as the 6th entry
  </read_first>
  <action>
    In backend/api/src/routes/storage.ts, update the ALLOWED_BUCKETS const:
    From: const ALLOWED_BUCKETS = ['profile-photos', 'scan-photos', 'exports', 'coach-kyc', 'ai-imports'] as const;
    To:   const ALLOWED_BUCKETS = ['profile-photos', 'scan-photos', 'exports', 'coach-kyc', 'ai-imports', 'coach-exercises'] as const;
    The AllowedBucket type is derived from typeof ALLOWED_BUCKETS[number] — no manual change needed.
  </action>
  <verify>
    <automated>grep "coach-exercises" backend/api/src/routes/storage.ts</automated>
  </verify>
  <acceptance_criteria>
    - backend/api/src/routes/storage.ts contains 'coach-exercises' in ALLOWED_BUCKETS
    - No other changes to the file (upload-url handler, cleanup logic unchanged)
  </acceptance_criteria>
  <done>grep confirms 'coach-exercises' present in ALLOWED_BUCKETS in backend storage router.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.users → coach_exercises | coach_id must equal auth.uid(); RLS enforces at DB level for all CRUD |
| auth.uid() → storage objects | Storage policies require path prefix = auth.uid()::text — prevents cross-coach file access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01-01 | Spoofing | coach_exercises.coach_id | mitigate | RLS FOR ALL USING (auth.uid() = coach_id) — coach reads/writes only own rows |
| T-43-01-02 | Information Disclosure | coach-exercises bucket cross-access | mitigate | storage policy: (storage.foldername(name))[1] = auth.uid()::text — own prefix only |
| T-43-01-03 | Elevation of Privilege | ALLOWED_BUCKETS bypass | mitigate | Both upload-url routes validate bucket against ALLOWED_BUCKETS before generating signed URL |
| T-43-01-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | SQL migration only + 2-line const changes |
</threat_model>

<verification>
After all 3 tasks complete:
1. `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/055_coach_exercises_schema.sql` returns 1
2. `grep "coach_exercises_own" supabase/migrations/055_coach_exercises_schema.sql` returns 1 match
3. `grep "coach-exercises" supabase/migrations/055_coach_exercises_schema.sql | wc -l` returns 4+ (table comment + bucket name + 3 policies)
4. `grep "coach-exercises" apps/web/src/app/api/storage/upload-url/route.ts` returns 1 match
5. `grep "coach-exercises" backend/api/src/routes/storage.ts` returns 1 match
</verification>

<success_criteria>
Migration 055 exists with coach_exercises table (all columns, RLS, category CHECK) + coach-exercises bucket (3 storage policies). Both upload-url allowlists updated.
</success_criteria>

<output>
Create `.planning/workstreams/custom-coach/phases/43-coach-exercises-backend-ui/43-01-SUMMARY.md` when done.
</output>
