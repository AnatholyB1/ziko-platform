---
plan: 05.1-04
phase: 05.1-close-audit-gaps-5-integration-blockers
status: complete
commit: 76b75aa
---

## Summary

Fixed BLOCKER-05: created SQL migration `20260529_fix_trigger_n_sessions.sql` replacing the trigger function from migration 058, changing the JSONB key read from `trigger_config->>'n'` to `trigger_config->>'n_sessions'`.

## What Was Built

New migration file with `CREATE OR REPLACE FUNCTION` for `public.create_form_instances_for_trigger`. The only change from migration 058 is in the `after_n_sessions` CASE branch:

```sql
-- Before (migration 058):
(trigger_config->>'n')::integer = p_n_sessions

-- After (this migration):
(trigger_config->>'n_sessions')::integer = p_n_sessions
```

`TriggerConfig.tsx` writes `{ n_sessions: N }` to `coach_forms.trigger_config`. The key `'n'` never existed; `NULL::integer` never matched `p_n_sessions`; no after_n_sessions trigger ever fired.

## Key Files

### Created
- `supabase/migrations/20260529_fix_trigger_n_sessions.sql` — full CREATE OR REPLACE FUNCTION with n_sessions fix, identical SECURITY DEFINER/EXCEPTION/GRANT structure

## Verification

```
grep -c "trigger_config->>'n_sessions'" 20260529_fix_trigger_n_sessions.sql → 2 (PASS)
grep -c "CREATE OR REPLACE FUNCTION" ...                                    → 1 (PASS)
grep -c "SECURITY DEFINER" ...                                              → 1 (PASS)
grep -c "GRANT EXECUTE" ...                                                 → 2 (PASS)
```

The 2 occurrences of `trigger_config->>'n'` in the file are both in comment lines, not executable SQL.

## Self-Check: PASSED

- Migration is idempotent (CREATE OR REPLACE)
- Existing DB rows already store `n_sessions` — retroactively fixed with no data migration
- TRIGGER-02 and FORM-04 unblocked; after_n_sessions triggers will now create pending instances
- Must be applied to Supabase: `supabase db push` or apply via dashboard
