---
phase: 05.1-close-audit-gaps-5-integration-blockers
plan: 03
type: execute
wave: 2
depends_on:
  - "05.1-01"
files_modified:
  - backend/api/src/coach/clients/db.ts
  - backend/api/src/routes/ai.ts
autonomous: true
requirements:
  - RESPONSES-02
  - CLAUDE-02

must_haves:
  truths:
    - "getFormsForClient in db.ts reads a.value (not a.answer_value) — answer text is visible in coach response viewer"
    - "ai.ts buildSystemPrompt checks q?.type === 'yes_no' which now matches DB data after Plan 05.1-01 fix"
    - "Yes/no answers appear as 'Oui'/'Non' in Claude's system prompt instead of raw 'yes'/'no'"
  artifacts:
    - path: "backend/api/src/coach/clients/db.ts"
      provides: "Corrected answer field reader: a.value instead of a.answer_value"
      contains: "answer_value: a.value"
    - path: "backend/api/src/routes/ai.ts"
      provides: "type check 'yes_no' which matches DB after BLOCKER-04 fix"
      contains: "q?.type === 'yes_no'"
  key_links:
    - from: "apps/mobile/src/components/PendingFormsOverlay.tsx (submit payload: {question_id, value})"
      to: "backend/api/src/coach/clients/db.ts (a.value reader)"
      via: "form_responses JSONB answers column"
      pattern: "answer_value: a\\.value"
    - from: "apps/web/src/components/coach/QuestionCard.tsx (type='yes_no' after Plan 01 fix)"
      to: "backend/api/src/routes/ai.ts (q?.type === 'yes_no')"
      via: "coach_forms.questions JSONB type field"
      pattern: "q\\?\\.type === 'yes_no'"
---

<objective>
Fix BLOCKER-02 and BLOCKER-03:

BLOCKER-02: getFormsForClient in db.ts (line 917) reads `a.answer_value` — but the submit route stores answers as `{question_id, value}`. The field 'answer_value' does not exist; all answers are undefined in the coach response viewer accordion. Fix: change `a.answer_value` to `a.value` in the answer mapping.

BLOCKER-03: ai.ts buildSystemPrompt checks `q?.type === 'yes_no'` — this now DOES match after Plan 05.1-01 fixes QuestionCard.tsx to write 'yes_no'. The ai.ts check itself is already correct; it only needed the upstream data to be fixed. No change needed in ai.ts for the type string itself.

Wait — re-reading BLOCKER-03: the audit says ai.ts checks 'yes_no' but data has 'yesno'. After Plan 05.1-01, data will have 'yes_no'. So ai.ts already has the correct check. The only actual code change in ai.ts would have been needed if Plan 05.1-01 chose 'yesno' as canonical. Since Plan 05.1-01 chose 'yes_no', ai.ts line 101 (`if (q?.type === 'yes_no')`) is already correct and requires NO edit.

Therefore this plan contains one real fix: db.ts `a.answer_value` → `a.value`.

This plan depends on Plan 05.1-01 to confirm 'yes_no' is the canonical type before touching ai.ts.

Purpose: RESPONSES-02 (blank accordion answers) and CLAUDE-02 (raw yes/no strings in prompt) are both resolved.
Output: db.ts with corrected field reader; ai.ts verified correct (no edit needed if already 'yes_no').
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/formulaire-condi/ROADMAP.md
@.planning/workstreams/formulaire-condi/STATE.md
@.planning/workstreams/formulaire-condi/v1.14-MILESTONE-AUDIT.md
@.planning/workstreams/formulaire-condi/phases/05.1-close-audit-gaps-5-integration-blockers/05.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix db.ts — change a.answer_value to a.value in getFormsForClient answer mapping</name>
  <files>backend/api/src/coach/clients/db.ts</files>

  <read_first>
    - backend/api/src/coach/clients/db.ts — focus on lines 905–920. The answer mapping block reads:
        ```
        answers = rawAnswers.map((a: any) => {
          const q = questionMap.get(a.question_id);
          return {
            question_id: a.question_id,
            question_label: q?.label ?? a.question_id,
            question_type: (q?.type ?? 'text') as FormAnswer['question_type'],
            answer_value: a.answer_value,   ← THIS LINE IS WRONG
          };
        });
        ```
      The `form_responses` table stores answers as JSONB array `[{question_id, value}]` — the field is named `value`, not `answer_value`. Read this block fully before editing.
    - .planning/workstreams/formulaire-condi/v1.14-MILESTONE-AUDIT.md — BLOCKER-02 section confirms exact line and fix.
  </read_first>

  <action>
    Make exactly one change in the `getFormsForClient` answer mapping block (around line 917):

    - Find: `answer_value: a.answer_value,`
    - Replace with: `answer_value: a.value,`

    This preserves the output property name `answer_value` (used by the response viewer UI and FormAnswer type) while correctly reading the stored `value` field from the JSONB answers array.

    Do not rename the output property (answer_value) — only the source field (a.answer_value → a.value). The coach response viewer UI reads `answer.answer_value` to display text; changing the output key would break the viewer.
  </action>

  <verify>
    <automated>grep -n "a\.answer_value" backend/api/src/coach/clients/db.ts | grep -v "^#"</automated>
  </verify>

  <done>
    The grep command returns zero lines — no remaining `a.answer_value` reads in db.ts. The answer mapping now reads `answer_value: a.value`. Answer text will be present in the coach response viewer accordion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify ai.ts — confirm 'yes_no' type check is correct after BLOCKER-04 fix</name>
  <files>backend/api/src/routes/ai.ts</files>

  <read_first>
    - backend/api/src/routes/ai.ts — read lines 90–115 (the buildSystemPrompt form block). The relevant check is at approximately line 101: `if (q?.type === 'yes_no') value = a.value === 'yes' ? 'Oui' : 'Non';`
    - .planning/workstreams/formulaire-condi/phases/05.1-close-audit-gaps-5-integration-blockers/05.1-01-SUMMARY.md — confirm that Plan 05.1-01 changed QuestionCard.tsx to write 'yes_no'. If confirmed, ai.ts already has the correct check and NO edit is needed here.
    - .planning/workstreams/formulaire-condi/v1.14-MILESTONE-AUDIT.md — BLOCKER-03 confirms the check in ai.ts is `q?.type === 'yes_no'`.
  </read_first>

  <action>
    Read ai.ts lines 90–115. Verify the type check at line ~101:
    - If the check reads `q?.type === 'yes_no'` → NO EDIT REQUIRED. The check is already correct. Document this as verified-no-change in the SUMMARY.
    - If the check reads `q?.type === 'yesno'` (unexpected — would mean Plan 05.1-01 took a different path) → change it to `q?.type === 'yes_no'`.

    The expected outcome is NO EDIT. ai.ts already uses 'yes_no' as the check string. BLOCKER-03 was a data-side bug (wrong value stored by QuestionCard.tsx), not an ai.ts code bug.
  </action>

  <verify>
    <automated>grep -n "q?.type" backend/api/src/routes/ai.ts</automated>
  </verify>

  <done>
    grep shows `q?.type === 'yes_no'` at the relevant line. The check is correct and will now match DB data after Plan 05.1-01 fixed the stored type string. Yes/no answers will render as 'Oui'/'Non' in Claude's system prompt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend db.ts → form_responses JSONB | Server-side read of athlete-submitted answer data |
| Backend ai.ts → Claude system prompt | Form Q&A injected into LLM context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-03 | Information Disclosure | db.ts answer reader | accept | Reading own-user data under RLS; no privilege escalation |
| T-05.1-04 | Tampering | ai.ts yes_no check | accept | Read-only string comparison; no user-controlled branching |
| T-05.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:

1. `grep -n "a\.answer_value" backend/api/src/coach/clients/db.ts` returns zero lines
2. `grep -n "answer_value: a\.value" backend/api/src/coach/clients/db.ts` returns exactly 1 line
3. `grep -n "q?.type === 'yes_no'" backend/api/src/routes/ai.ts` returns exactly 1 line
4. `grep -n "q?.type === 'yesno'" backend/api/src/routes/ai.ts` returns zero lines
</verification>

<success_criteria>
- db.ts answer mapping reads `a.value` (not `a.answer_value`) — answer text is no longer undefined in coach viewer
- ai.ts type check uses 'yes_no' — confirmed correct and matches DB data after Plan 05.1-01
- Coach response viewer accordion will display athlete's answer text when expanded
- Claude's system prompt will render yes/no answers as 'Oui'/'Non' instead of raw 'yes'/'no'
</success_criteria>

<output>
Create `.planning/workstreams/formulaire-condi/phases/05.1-close-audit-gaps-5-integration-blockers/05.1-03-SUMMARY.md` when done.
</output>
