---
plan: 05.1-01
phase: 05.1-close-audit-gaps-5-integration-blockers
status: complete
commit: 0c2ccee
---

## Summary

Fixed BLOCKER-04: standardized yes/no question type string to `yes_no` (with underscore) in the web form builder.

## What Was Built

Two minimal edits in two files:

1. **QuestionCard.tsx** — changed `TYPE_LABELS` key and `<option value>` from `'yesno'` to `'yes_no'`
2. **FormStatusBadge.tsx** — updated `QuestionType` union from `'yesno'` to `'yes_no'`

All mobile consumers (`FormQuestion.tsx`) and `ai.ts` already used `'yes_no'` — they required no change.

## Key Files

### Modified
- `apps/web/src/components/coach/QuestionCard.tsx` — TYPE_LABELS key + select option value corrected
- `apps/web/src/components/coach/FormStatusBadge.tsx` — QuestionType union updated

## Verification

```
grep -n "yesno" apps/web/src/components/coach/QuestionCard.tsx   → 0 lines (PASS)
grep -n "yesno" apps/web/src/components/coach/FormStatusBadge.tsx → 0 lines (PASS)
grep -n "yes_no" apps/web/src/components/coach/QuestionCard.tsx   → 2 lines (PASS)
grep -n "yes_no" apps/web/src/components/coach/FormStatusBadge.tsx → 1 line (PASS)
```

## Self-Check: PASSED

- Zero occurrences of 'yesno' remain in either file
- QuestionCard.tsx: `yes_no: 'Oui / Non'` in TYPE_LABELS + `value="yes_no"` in select
- FormStatusBadge.tsx: `'text' | 'scale' | 'yes_no' | 'choice'`
- Plan 05.1-03 (Wave 2) can now confirm `yes_no` as canonical and no ai.ts edit is needed
