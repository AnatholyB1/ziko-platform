---
plan: 05.1-02
phase: 05.1-close-audit-gaps-5-integration-blockers
status: complete
commit: eea861f
---

## Summary

Fixed BLOCKER-01: prepended `/forms` router prefix to both API URLs in PendingFormsOverlay.tsx.

## What Was Built

Two URL string fixes in one file:

1. **Fetch URL** (line 54): `/athlete/forms/pending` → `/forms/athlete/forms/pending`
2. **Submit URL** (line 146): `/athlete/forms/${currentForm.instance_id}/submit` → `/forms/athlete/forms/${currentForm.instance_id}/submit`

The `formsRouter` is mounted at `/forms` in `backend/api/src/app.ts`. Both calls were missing this prefix, causing HTTP 404 on every request.

## Key Files

### Modified
- `apps/mobile/src/components/PendingFormsOverlay.tsx` — corrected fetch and submit URLs

## Verification

```
grep -n "EXPO_PUBLIC_API_URL}/athlete/forms" PendingFormsOverlay.tsx → 0 lines (PASS)
grep -n "/forms/athlete/forms/pending" PendingFormsOverlay.tsx        → 1 line (PASS)
grep -n "/forms/athlete/forms/.*submit" PendingFormsOverlay.tsx       → 1 line (PASS)
```

## Self-Check: PASSED

- Zero occurrences of old prefix-less URL pattern
- Both URLs now include the `/forms` router prefix
- Pending-forms fetch will return 200; submit will return 200 and transition instance to submitted
- MOBILE-01 through MOBILE-05 unblocked
