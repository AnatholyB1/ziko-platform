---
phase: 05.1-close-audit-gaps-5-integration-blockers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/coach/QuestionCard.tsx
  - apps/web/src/components/coach/FormStatusBadge.tsx
autonomous: true
requirements:
  - FORM-02
  - MOBILE-03

must_haves:
  truths:
    - "QuestionCard.tsx writes type='yes_no' (with underscore) to DB when coach selects 'Oui / Non'"
    - "FormStatusBadge.tsx QuestionType union includes 'yes_no' (not 'yesno')"
    - "FormQuestion.tsx case 'yes_no' matches because the stored DB value is now 'yes_no'"
  artifacts:
    - path: "apps/web/src/components/coach/QuestionCard.tsx"
      provides: "select option value corrected from 'yesno' to 'yes_no'"
      contains: "value=\"yes_no\""
    - path: "apps/web/src/components/coach/FormStatusBadge.tsx"
      provides: "QuestionType type export with 'yes_no' canonical value"
      contains: "yes_no"
  key_links:
    - from: "apps/web/src/components/coach/QuestionCard.tsx (select option)"
      to: "apps/mobile/src/components/forms/FormQuestion.tsx (case 'yes_no')"
      via: "DB coach_forms.questions[].type stored value"
      pattern: "value=\"yes_no\""
---

<objective>
Fix BLOCKER-04: standardize the yes/no question type string to 'yes_no' (with underscore) across the web form builder. QuestionCard.tsx currently writes 'yesno' (no underscore) to the DB; FormQuestion.tsx on mobile switches on 'yes_no' — the case never matches and renders "Type de question inconnu".

The fix is minimal: change the one web-side select option value from 'yesno' to 'yes_no', and update the QuestionType union in FormStatusBadge.tsx to match. All mobile consumers and ai.ts already use 'yes_no' — they require no change.

This plan MUST complete before Plan 05.1-03 (which patches ai.ts using the canonical value confirmed here).

Purpose: Yes/no questions must render correctly on mobile. MOBILE-03 and FORM-02 are blocked entirely until this is fixed.
Output: QuestionCard.tsx with corrected option value; FormStatusBadge.tsx with updated QuestionType union.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/formulaire-condi/ROADMAP.md
@.planning/workstreams/formulaire-condi/STATE.md
@.planning/workstreams/formulaire-condi/v1.14-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix QuestionCard.tsx — change select option value from 'yesno' to 'yes_no'</name>
  <files>apps/web/src/components/coach/QuestionCard.tsx</files>

  <read_first>
    - apps/web/src/components/coach/QuestionCard.tsx — read fully before editing; the select option at line ~135 contains `value="yesno"` which must change to `value="yes_no"`. Also note TYPE_LABELS at top of file maps QuestionType keys — 'yesno' key must change to 'yes_no'.
    - apps/web/src/components/coach/FormStatusBadge.tsx — QuestionType is imported from this file; verify the type change in Task 2 is reflected consistently.
    - .planning/workstreams/formulaire-condi/v1.14-MILESTONE-AUDIT.md — BLOCKER-04 section confirms exact line and fix.
  </read_first>

  <action>
    Make exactly two changes in QuestionCard.tsx:

    1. In the TYPE_LABELS Record at the top of the file, change the key 'yesno' to 'yes_no':
       - Find: `yesno: 'Oui / Non',`
       - Replace with: `yes_no: 'Oui / Non',`

    2. In the JSX select element (the type dropdown), change the option value:
       - Find: `<option value="yesno">Oui / Non</option>`
       - Replace with: `<option value="yes_no">Oui / Non</option>`

    No other changes. Do not touch QuestionCard logic, handlers, or any other option values.
  </action>

  <verify>
    <automated>grep -n "yesno" apps/web/src/components/coach/QuestionCard.tsx | grep -v "^#"</automated>
  </verify>

  <done>
    The grep command returns zero lines (no remaining 'yesno' occurrences in QuestionCard.tsx). The file contains 'yes_no' in both TYPE_LABELS key and the select option value.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix FormStatusBadge.tsx — update QuestionType union to use 'yes_no'</name>
  <files>apps/web/src/components/coach/FormStatusBadge.tsx</files>

  <read_first>
    - apps/web/src/components/coach/FormStatusBadge.tsx — read fully. Line 5: `export type QuestionType = 'text' | 'scale' | 'yesno' | 'choice';` must change. This type is imported by QuestionCard.tsx and any other web components that reference QuestionType.
    - apps/web/src/components/coach/QuestionCard.tsx — after Task 1, this file imports QuestionType; confirm that changing the union here keeps the compiler happy with the 'yes_no' option value set in Task 1.
  </read_first>

  <action>
    Make one change in FormStatusBadge.tsx:

    In the exported QuestionType union on line 5:
    - Find: `export type QuestionType = 'text' | 'scale' | 'yesno' | 'choice';`
    - Replace with: `export type QuestionType = 'text' | 'scale' | 'yes_no' | 'choice';`

    No other changes. The FormStatusBadge component itself, STATUS_CONFIG, and the CoachForm/TriggerConfig interfaces are untouched.
  </action>

  <verify>
    <automated>grep -n "yesno" apps/web/src/components/coach/FormStatusBadge.tsx | grep -v "^#"</automated>
  </verify>

  <done>
    The grep command returns zero lines. FormStatusBadge.tsx QuestionType union reads `'text' | 'scale' | 'yes_no' | 'choice'`. Both QuestionCard.tsx and FormStatusBadge.tsx are now consistent with the 'yes_no' canonical value used by all mobile consumers and ai.ts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web form builder → DB | Coach-entered question type written to coach_forms JSONB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Tampering | QuestionCard.tsx type option value | accept | Fix normalizes a stored constant; no user-controlled input at this layer |
| T-05.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:

1. `grep -n "yesno" apps/web/src/components/coach/QuestionCard.tsx` returns zero lines
2. `grep -n "yesno" apps/web/src/components/coach/FormStatusBadge.tsx` returns zero lines
3. `grep -n "yes_no" apps/web/src/components/coach/QuestionCard.tsx` returns at least 2 lines (TYPE_LABELS key + option value)
4. `grep -n "yes_no" apps/web/src/components/coach/FormStatusBadge.tsx` returns at least 1 line (QuestionType union)
</verification>

<success_criteria>
- QuestionCard.tsx select option for yes/no type has `value="yes_no"` (not `value="yesno"`)
- FormStatusBadge.tsx QuestionType union contains `'yes_no'` (not `'yesno'`)
- Zero occurrences of 'yesno' remain in either file
- Mobile FormQuestion.tsx case 'yes_no' will now match questions built by the web coach builder
</success_criteria>

<output>
Create `.planning/workstreams/formulaire-condi/phases/05.1-close-audit-gaps-5-integration-blockers/05.1-01-SUMMARY.md` when done.
</output>
