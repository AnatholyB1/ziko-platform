---
phase: 05.1-close-audit-gaps-5-integration-blockers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/PendingFormsOverlay.tsx
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-04
  - MOBILE-05

must_haves:
  truths:
    - "PendingFormsOverlay.tsx fetches /forms/athlete/forms/pending (not /athlete/forms/pending)"
    - "PendingFormsOverlay.tsx submits to /forms/athlete/forms/:instanceId/submit (not /athlete/forms/:id/submit)"
    - "Pending forms fetch returns 200 instead of 404; overlay renders on app launch"
    - "Submit call returns 200; form instance status changes to submitted; overlay dismisses"
  artifacts:
    - path: "apps/mobile/src/components/PendingFormsOverlay.tsx"
      provides: "Corrected fetch and submit URLs with /forms prefix"
      contains: "/forms/athlete/forms/pending"
  key_links:
    - from: "apps/mobile/src/components/PendingFormsOverlay.tsx (fetch)"
      to: "backend/api/src/routes/forms.ts (formsRouter at /forms)"
      via: "HTTP GET /forms/athlete/forms/pending"
      pattern: "/forms/athlete/forms/pending"
    - from: "apps/mobile/src/components/PendingFormsOverlay.tsx (submit)"
      to: "backend/api/src/routes/forms.ts (POST /athlete/forms/:instanceId/submit)"
      via: "HTTP POST /forms/athlete/forms/:instanceId/submit"
      pattern: "/forms/athlete/forms/\\${currentForm.instance_id}/submit"
---

<objective>
Fix BLOCKER-01: PendingFormsOverlay.tsx constructs both its API URLs without the '/forms' router prefix that Hono's formsRouter is mounted under. Both calls receive HTTP 404 and the overlay never renders or submits.

Correct paths (confirmed from backend/api/src/app.ts where formsRouter is mounted at '/forms'):
- Fetch pending forms: `${EXPO_PUBLIC_API_URL}/forms/athlete/forms/pending`
- Submit form:         `${EXPO_PUBLIC_API_URL}/forms/athlete/forms/${currentForm.instance_id}/submit`

This is a two-line fix in one file. No other logic changes.

Purpose: MOBILE-01 through MOBILE-05 are all blocked by this 404. Fixing the URL prefixes unblocks the entire mobile athlete flow.
Output: PendingFormsOverlay.tsx with corrected URL strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/formulaire-condi/ROADMAP.md
@.planning/workstreams/formulaire-condi/STATE.md
@.planning/workstreams/formulaire-condi/v1.14-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix both fetch URLs in PendingFormsOverlay.tsx — prepend /forms prefix</name>
  <files>apps/mobile/src/components/PendingFormsOverlay.tsx</files>

  <read_first>
    - apps/mobile/src/components/PendingFormsOverlay.tsx — read fully before editing. The two URLs to fix are:
        Line ~54 (inside useQuery queryFn): `${process.env.EXPO_PUBLIC_API_URL}/athlete/forms/pending`
        Line ~146 (inside handleSubmit): `${process.env.EXPO_PUBLIC_API_URL}/athlete/forms/${currentForm.instance_id}/submit`
    - .planning/workstreams/formulaire-condi/v1.14-MILESTONE-AUDIT.md — BLOCKER-01 section confirms exact lines and fix.
  </read_first>

  <action>
    Make exactly two string changes in PendingFormsOverlay.tsx:

    1. In the useQuery queryFn (around line 54), fix the pending-fetch URL:
       - Find: `` `${process.env.EXPO_PUBLIC_API_URL}/athlete/forms/pending` ``
       - Replace with: `` `${process.env.EXPO_PUBLIC_API_URL}/forms/athlete/forms/pending` ``

    2. In handleSubmit (around line 146), fix the submit URL:
       - Find: `` `${process.env.EXPO_PUBLIC_API_URL}/athlete/forms/${currentForm.instance_id}/submit` ``
       - Replace with: `` `${process.env.EXPO_PUBLIC_API_URL}/forms/athlete/forms/${currentForm.instance_id}/submit` ``

    No other changes. Do not modify error handling, response parsing, auth headers, or any other logic.
  </action>

  <verify>
    <automated>grep -n "EXPO_PUBLIC_API_URL}/athlete/forms" apps/mobile/src/components/PendingFormsOverlay.tsx | grep -v "^#"</automated>
  </verify>

  <done>
    The grep command returns zero lines (no URLs remain with the incorrect '/athlete/forms' pattern missing the '/forms' prefix). The file contains exactly:
    - One occurrence of `/forms/athlete/forms/pending` (fetch URL)
    - One occurrence of `/forms/athlete/forms/${currentForm.instance_id}/submit` (submit URL)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile app → Hono API | Athlete JWT sent in Authorization header for both fetch and submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-02 | Information Disclosure | PendingFormsOverlay.tsx URL correction | accept | URL fix only; auth header already present; no new attack surface |
| T-05.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After task completes:

1. `grep -n "EXPO_PUBLIC_API_URL}/athlete/forms" apps/mobile/src/components/PendingFormsOverlay.tsx` returns zero lines
2. `grep -n "/forms/athlete/forms/pending" apps/mobile/src/components/PendingFormsOverlay.tsx` returns exactly 1 line
3. `grep -n "/forms/athlete/forms/\${currentForm.instance_id}/submit" apps/mobile/src/components/PendingFormsOverlay.tsx` returns exactly 1 line
</verification>

<success_criteria>
- PendingFormsOverlay.tsx has zero occurrences of `EXPO_PUBLIC_API_URL}/athlete/forms` (the incorrect prefix-less pattern)
- Both corrected URLs include the `/forms` router prefix
- Pending-forms fetch will return 200 (not 404) when the app is running against the backend
- Submit call will return 200 (not 404); form instance transitions to submitted; overlay dismisses
</success_criteria>

<output>
Create `.planning/workstreams/formulaire-condi/phases/05.1-close-audit-gaps-5-integration-blockers/05.1-02-SUMMARY.md` when done.
</output>
