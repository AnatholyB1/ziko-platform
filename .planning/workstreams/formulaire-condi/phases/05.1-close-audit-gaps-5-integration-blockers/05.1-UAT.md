---
status: testing
phase: 05.1-close-audit-gaps-5-integration-blockers
source:
  - 05.1-01-SUMMARY.md
  - 05.1-02-SUMMARY.md
  - 05.1-03-SUMMARY.md
  - 05.1-04-SUMMARY.md
started: 2026-05-30T00:00:00Z
updated: 2026-05-30T00:00:00Z
---

## Current Test

number: 1
name: Cold Start Smoke Test
expected: |
  Kill any running backend server. Start the backend API from scratch (cd backend/api && npm run dev).
  Server boots without errors. A health check call (GET /health) returns 200 with live data.
  No startup crashes or missing-env errors appear in the terminal.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running backend server. Start the backend API from scratch (cd backend/api && npm run dev). Server boots without errors. A health check call (GET /health) returns 200 with live data. No startup crashes or missing-env errors appear in the terminal.
result: [pending]

### 2. Yes/No question type in form builder
expected: On the coach web app, open the form builder and create or edit a form. Add a question and select "Oui / Non" from the question type dropdown. The option appears and saves correctly. On mobile (FormQuestion), the question renders as a Yes/No toggle (not broken/missing). Previously, the type was saved as 'yesno' causing a mismatch with mobile consumers; it should now save as 'yes_no'.
result: [pending]

### 3. Mobile overlay — pending forms load (no 404)
expected: An athlete account that has ≥1 pending form assignment logs into the mobile app. The PendingFormsOverlay appears with the form title, question count, and a CTA button. No HTTP 404 error in logs. Previously the overlay was fetching /athlete/forms/pending (missing /forms prefix); it now correctly calls /forms/athlete/forms/pending.
result: [pending]

### 4. Mobile overlay — form submission succeeds
expected: The athlete fills in all questions in the overlay and taps Submit. The request completes successfully (no 404). The overlay dismisses and normal app access is restored. If multiple forms were pending, the next form appears; once all are done, the app opens normally.
result: [pending]

### 5. Coach response viewer shows submitted answers
expected: On the coach web app, open a client's detail sheet → Formulaires tab. Expand a submitted form. The individual question+answer pairs are displayed with the actual answer text (not empty or undefined). Previously db.ts was reading a.answer_value (non-existent JSONB key) instead of a.value; answers were always null. They should now display correctly.
result: [pending]

### 6. After-N-sessions trigger fires after migration applied
expected: The SQL migration supabase/migrations/20260529_fix_trigger_n_sessions.sql has been applied to Supabase (supabase db push or dashboard). After an athlete completes the N-th session in a program that has an "after N sessions" form trigger, a pending form instance is created for that athlete. Previously the JSONB key was 'n' (never matched); it is now 'n_sessions' (matches what TriggerConfig.tsx writes).
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0

## Gaps

[none yet]
