---
phase: 05-response-viewer
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/api/src/context/user.ts
  - backend/api/src/routes/ai.ts
autonomous: true
requirements:
  - CLAUDE-01
  - CLAUDE-02

must_haves:
  truths:
    - "fetchUserContext() returns a recentFormResponses array in UserContext for every athlete call to the AI chat endpoint"
    - "recentFormResponses contains at most 5 entries, ordered by submitted_at DESC"
    - "The AI system prompt contains a '## Formulaires récents' block when the athlete has submitted at least one form response"
    - "Each entry in the block shows: ### {form title} ({YYYY-MM-DD}) followed by Q: {label} / R: {answer} pairs — scale answers formatted as '{N} / 10', yes_no as 'Oui'/'Non'"
    - "When the athlete has no submitted form responses, no '## Formulaires récents' section appears in the system prompt (empty string, not placeholder text)"
  artifacts:
    - path: "backend/api/src/context/user.ts"
      provides: "Extended fetchUserContext with 7th Promise.all query and recentFormResponses in return shape"
      contains: "recentFormResponses"
    - path: "backend/api/src/routes/ai.ts"
      provides: "formsSection appended to system prompt string"
      contains: "Formulaires récents"
  key_links:
    - from: "backend/api/src/routes/ai.ts"
      to: "backend/api/src/context/user.ts"
      via: "fetchUserContext() call and ctx.recentFormResponses access"
      pattern: "ctx\\.recentFormResponses"
    - from: "backend/api/src/context/user.ts"
      to: "form_responses + form_instances + coach_forms"
      via: "Supabase nested join using athlete JWT"
      pattern: "form_responses.*form_instances.*coach_forms"
---

<objective>
Extend fetchUserContext() in user.ts to query the last 5 submitted form responses (joined to coach_forms for title and questions) and expose them as recentFormResponses in the UserContext return value. Then extend the system prompt builder in ai.ts to append a formatted '## Formulaires récents' block when recentFormResponses is non-empty.

This satisfies CLAUDE-01 (last 5 responses injected automatically on every AI call) and CLAUDE-02 (formatted block: form title, date, Q&A pairs with localized answer values).

No new API routes. No new packages. The RLS check for form_responses uses the athlete's own JWT via clientForUser() — passes automatically (RF-05 is informational, not a risk).

Output: user.ts extended with 7th Promise.all query + return field; ai.ts extended with formsSection appended to system prompt.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/formulaire-condi/STATE.md
@C:/ziko-platform/.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-RESEARCH.md
@C:/ziko-platform/.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UserContext and fetchUserContext() in user.ts</name>
  <files>backend/api/src/context/user.ts</files>

  <read_first>
    - backend/api/src/context/user.ts — read the full file. Extract: (1) the UserContext interface (lines ~5–33) to see the exact field list and where to append recentFormResponses; (2) the Promise.all block (lines ~39–66) — current 6-tuple destructure and all 6 query patterns; (3) the return statement (lines ~96–118) — the exact return object shape; (4) how db (the Supabase client) is obtained — confirm it uses clientForUser(userToken) so RLS with athlete JWT applies automatically.
  </read_first>

  <action>
  Three changes to user.ts:

  Change 1 — UserContext interface: Add recentFormResponses field at the end of the interface (after personaInstruction or the last existing field):
    recentFormResponses: Array<{
      form_title: string;
      submitted_at: string;
      questions: Array<{ id: string; type: string; label: string }>;
      answers: Array<{ question_id: string; value: string | number }>;
    }>;

  Change 2 — Promise.all extension: Extend the destructure from 6-tuple to 7-tuple by adding formResponsesRes as the 7th element. Append this query as the 7th item in the Promise.all array (after the habit_logs query, before the closing bracket):
    db.from('form_responses')
      .select(`
        answers,
        submitted_at,
        form_instances!inner(
          form_id,
          coach_forms!inner(
            title,
            questions
          )
        )
      `)
      .eq('athlete_id', userId)
      .order('submitted_at', { ascending: false })
      .limit(5)

  The field name in the .eq() is 'athlete_id' (not 'user_id') — this matches the form_responses table schema from migration 055.

  Change 3 — Return object: Add recentFormResponses at the end of the return statement:
    recentFormResponses: (formResponsesRes.data ?? []).map((r: any) => ({
      form_title: r.form_instances?.coach_forms?.title ?? '',
      submitted_at: r.submitted_at,
      questions: r.form_instances?.coach_forms?.questions ?? [],
      answers: r.answers ?? [],
    })),

  Use `as any` casts on r to handle Supabase nested join typing (consistent with the existing pattern on pluginsRes.data, workoutsRes.data, etc. which all use `(data ?? []).map((x: any) => ...)`).

  Do NOT change the clientForUser(userToken) call or anything else in the function — only these three additions.
  </action>

  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep -i "user.ts" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - backend/api/src/context/user.ts UserContext interface contains recentFormResponses field with the exact shape described (Array of objects with form_title, submitted_at, questions, answers)
    - The Promise.all destructure has 7 elements (formResponsesRes is the 7th)
    - The 7th query targets 'form_responses' table with .eq('athlete_id', userId)
    - The return object includes recentFormResponses mapped from formResponsesRes.data
    - TypeScript compilation of user.ts produces no errors
  </acceptance_criteria>

  <done>UserContext.recentFormResponses populated from the last 5 submitted form responses for the athlete. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Inject ## Formulaires récents block into system prompt in ai.ts</name>
  <files>backend/api/src/routes/ai.ts</files>

  <read_first>
    - backend/api/src/routes/ai.ts — read the full file. Locate: (1) the call to fetchUserContext() — note the variable name used for the result (likely `ctx` or `userContext`); (2) where the system prompt string is assembled — it may be a template literal or a string concatenation; (3) where the system prompt is passed to the AI SDK (streamText or generateText call) — the `system` parameter; (4) any existing context sections appended to the prompt (e.g., workout summaries, plugin instructions) to understand the append pattern and where formsSection should be placed (at the end, after all other sections).
  </read_first>

  <action>
  One addition to ai.ts: after the system prompt string is built but before it is passed to the AI SDK call, insert the formsSection computation and append it to the system prompt.

  Compute formsSection from ctx.recentFormResponses (use whatever variable name the existing code uses for the fetchUserContext result):

    const formBlock = (ctx.recentFormResponses ?? []).map((r) => {
      const questionMap = new Map(
        r.questions.map((q: any) => [q.id, q]),
      );
      const qaLines = r.answers.map((a: any) => {
        const q = questionMap.get(a.question_id);
        const label = q?.label ?? a.question_id;
        let value = String(a.value);
        if (q?.type === 'scale') value = `${a.value} / 10`;
        if (q?.type === 'yes_no') value = a.value === 'yes' ? 'Oui' : 'Non';
        return `  Q: ${label}\n  R: ${value}`;
      }).join('\n');
      return `### ${r.form_title} (${(r.submitted_at ?? '').slice(0, 10)})\n${qaLines}`;
    }).join('\n\n');

    const formsSection = formBlock
      ? `\n\n## Formulaires récents\n${formBlock}`
      : '';

  Then append formsSection to the system prompt string. If the system prompt is already a variable (e.g., `let systemPrompt = ...`), do: `systemPrompt += formsSection;`. If it is assembled inline in the AI SDK call, extract it to a variable first, append formsSection, then pass the variable.

  Both streaming (POST /ai/chat/stream) and non-streaming (POST /ai/chat) routes call fetchUserContext — apply formsSection to both if they use separate system prompt assembly blocks. If they share a common helper, apply it once in that helper.

  CLAUDE-02 format contract:
  - Block header: '## Formulaires récents'
  - Each form entry: '### {form_title} ({YYYY-MM-DD})'
  - Each Q&A pair: '  Q: {question label}\n  R: {answer value}'
  - scale → '{N} / 10', yes_no → 'Oui' or 'Non', text/choice → raw string
  - Empty (no submitted forms) → formsSection = '' (nothing appended)
  </action>

  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep -i "ai.ts" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - backend/api/src/routes/ai.ts contains `Formulaires récents` string literal (the section header)
    - ai.ts contains ctx.recentFormResponses (or the equivalent variable name from the existing code) accessed to build formBlock
    - ai.ts contains formsSection variable computed from formBlock with the ternary (empty string when formBlock is falsy)
    - The formsSection is appended to the system prompt before the AI SDK streamText or generateText call
    - TypeScript compilation of ai.ts produces no errors
    - Manual smoke test: AI chat response for an athlete with submitted form responses — the system prompt visible in Claude API call logs contains "## Formulaires récents" with the form title and Q&A pairs
  </acceptance_criteria>

  <done>System prompt for AI coach calls includes '## Formulaires récents' block with last 5 submitted form responses, formatted per CLAUDE-02. Absent when athlete has no submissions. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| athlete JWT → fetchUserContext | athlete's own JWT used as Supabase client — RLS form_responses_athlete passes automatically |
| system prompt → Claude API | form response data is coach-created content; athletes see their own answers reflected back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | form_responses query in fetchUserContext | accept | Uses athlete JWT via clientForUser() — RLS form_responses_athlete (auth.uid() = athlete_id) ensures athlete only reads own responses. No cross-athlete data possible. |
| T-05-08 | Tampering | formsSection injection into system prompt | accept | Content is read from DB (athlete's own responses); no user-controlled string inserted without DB mediation. Coach-authored question labels flow through the same trust model as existing workout/habit context. |
| T-05-09 | Elevation of Privilege | 7th query added to Promise.all | accept | Query is scoped to userId (athlete); no coach data or other athlete data is accessible via this query. Limit 5 prevents prompt bloat abuse. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. TypeScript clean: `rtk tsc --noEmit -p backend/api/tsconfig.json` — zero errors in user.ts and ai.ts
2. UserContext shape: Confirm user.ts return type includes recentFormResponses with Array shape
3. Query scope: The 7th Supabase query uses .eq('athlete_id', userId) — not user_id, confirmed against migration 055 schema
4. Prompt injection: When testing the AI chat endpoint for an athlete with 1+ submitted form responses, the system prompt sent to Claude contains "## Formulaires récents" followed by the form title and Q&A pairs
5. Empty case: Athlete with no submitted form responses — system prompt does NOT contain "## Formulaires récents"
6. Answer formatting: A scale answer of 7 appears as "7 / 10"; a yes_no of 'yes' appears as "Oui"; a text answer appears verbatim
</verification>

<success_criteria>
- fetchUserContext() returns recentFormResponses: Array with up to 5 entries per athlete call
- System prompt for athletes with submitted forms includes "## Formulaires récents" block
- Each block entry: "### {title} ({date})" + "  Q: {label}\n  R: {value}" pairs
- Formatting correct: scale "{N} / 10", yes_no "Oui"/"Non", text/choice raw
- System prompt unchanged (no forms section) for athletes with zero submitted forms
- TypeScript clean in both user.ts and ai.ts
</success_criteria>

<output>
Create `.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-03-SUMMARY.md` when done.
</output>
