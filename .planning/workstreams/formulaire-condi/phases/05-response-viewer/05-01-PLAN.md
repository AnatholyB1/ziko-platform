---
phase: 05-response-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/src/coach/clients/db.ts
  - backend/api/src/coach/clients/service.ts
autonomous: true
requirements:
  - RESPONSES-01
  - RESPONSES-02
  - RESPONSES-03

must_haves:
  truths:
    - "GET /coach/clients/:clientId/forms returns 200 with a forms array sorted: submitted rows first (submitted_at DESC), pending rows at bottom"
    - "Each submitted FormInstance in the response contains question_label and question_type already joined from coach_forms.questions — no client-side cross-reference needed"
    - "Each pending FormInstance has answers: null and submitted_at: null"
    - "Only form instances for forms owned by the requesting coach are returned (security filter applied)"
    - "A client with no form instances returns { forms: [] }"
  artifacts:
    - path: "backend/api/src/coach/clients/db.ts"
      provides: "getFormsForClient() DB function"
      contains: "getFormsForClient"
    - path: "backend/api/src/coach/clients/service.ts"
      provides: "GET /:clientId/forms route registered on clientsRouter"
      contains: "/:clientId/forms"
  key_links:
    - from: "backend/api/src/coach/clients/service.ts"
      to: "backend/api/src/coach/clients/db.ts"
      via: "getFormsForClient import"
      pattern: "getFormsForClient"
    - from: "GET /coach/clients/:clientId/forms"
      to: "form_instances + coach_forms + form_responses"
      via: "Supabase join in getFormsForClient"
      pattern: "form_instances.*coach_forms.*inner"
---

<objective>
Add GET /coach/clients/:clientId/forms to the existing clientsRouter (per RF-01: mount in service.ts, NOT in formsRouter). The route returns all form instances for the given athlete — submitted rows with pre-joined Q&A answers (question_label + question_type resolved from coach_forms.questions server-side per RF-04), pending rows with null answers. Only forms owned by the requesting coach are returned (RF-02: manual ownership filter — no RLS).

Purpose: Provide the data contract that the Phase 05-02 web UI will consume. Also satisfies RESPONSES-01 (list), RESPONSES-02 (answers available for expand), and RESPONSES-03 (pending rows returned with status 'pending').

Output: One new exported function in db.ts + one new route handler in service.ts.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/formulaire-condi/STATE.md
@C:/ziko-platform/.planning/workstreams/formulaire-condi/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-RESEARCH.md
@C:/ziko-platform/.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getFormsForClient() to clients/db.ts</name>
  <files>backend/api/src/coach/clients/db.ts</files>

  <read_first>
    - backend/api/src/coach/clients/db.ts — read the full file; locate the getProgramsForClient function (around line 731) to copy the exact signature pattern: parameter list (jwt, coachId, clientId), createUserClient(jwt) call, multi-step Supabase queries, shaped return type. Also note where the file's type imports live (top of file).
    - backend/api/src/coach/clients/service.ts — read lines 1–50 to see which imports come from db.ts; confirm getFormsForClient is not already present.
  </read_first>

  <action>
  Add a new exported function `getFormsForClient(jwt: string, coachId: string, clientId: string): Promise<{ forms: FormInstanceResponse[] }>` at the end of db.ts, following the getProgramsForClient pattern exactly.

  Define local interfaces at the top of the new function block (or in the file's type section if one exists):

    interface FormAnswer {
      question_id: string;
      question_label: string;
      question_type: 'text' | 'scale' | 'yes_no' | 'choice';
      answer_value: string | number;
    }

    interface FormInstanceResponse {
      instance_id: string;
      form_id: string;
      form_title: string;
      status: 'pending' | 'submitted';
      submitted_at: string | null;
      question_count: number;
      answers: FormAnswer[] | null;
    }

  Implementation steps inside getFormsForClient:

  Step 1 — Create user client with the coach JWT (same pattern as getProgramsForClient): `const db = createUserClient(jwt);`

  Step 2 — Fetch all form_instances for this athlete, joining coach_forms:
    db.from('form_instances')
      .select('id, form_id, status, submitted_at, coach_forms!inner(title, questions, coach_id)')
      .eq('athlete_id', clientId)

  If instErr, throw new Error(instErr.message). If instances is null or empty, return { forms: [] }.

  Step 3 — Security filter (RF-02: no RLS on publishable client — manual ownership check):
    const ownedInstances = (instances as any[]).filter(
      (i) => i.coach_forms?.coach_id === coachId
    );

  Step 4 — Fetch form_responses for submitted instances only:
    const submittedIds = ownedInstances.filter(i => i.status === 'submitted').map(i => i.id);
    If submittedIds.length > 0, query: db.from('form_responses').select('instance_id, answers').in('instance_id', submittedIds)
    Build a Map<string, any[]>: responsesMap keyed by instance_id → answers array.

  Step 5 — Build FormInstanceResponse[] for each ownedInstance:
    - Extract questions: any[] from i.coach_forms?.questions ?? []
    - Build questionMap: Map<string, any> keyed by question.id
    - For submitted: map rawAnswers to FormAnswer[] resolving question_label (q?.label ?? a.question_id) and question_type (q?.type ?? 'text')
    - For pending: answers = null
    - Return { instance_id: i.id, form_id: i.form_id, form_title: i.coach_forms?.title ?? '', status: i.status, submitted_at: i.submitted_at ?? null, question_count: questions.length, answers }

  Step 6 — Sort: submitted rows first sorted by submitted_at DESC, then pending rows at the end:
    const sorted = [
      ...forms.filter(f => f.status === 'submitted').sort((a, b) => new Date(b.submitted_at!).getTime() - new Date(a.submitted_at!).getTime()),
      ...forms.filter(f => f.status === 'pending'),
    ];
    return { forms: sorted };

  Use `as any` casts for Supabase join results (the same pattern used in getProgramsForClient).
  </action>

  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep -i "db.ts" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - backend/api/src/coach/clients/db.ts contains exported function `getFormsForClient`
    - Function signature matches: `export async function getFormsForClient(jwt: string, coachId: string, clientId: string): Promise<{ forms: FormInstanceResponse[] }>`
    - File contains `ownedInstances` with filter on `coach_forms?.coach_id === coachId` (RF-02 manual security)
    - File contains `question_label` and `question_type` populated in the FormAnswer map step (RF-04 server-side join)
    - TypeScript compilation of db.ts produces no errors
  </acceptance_criteria>

  <done>getFormsForClient is exported from db.ts, TypeScript-clean, with manual coach ownership filter and server-side Q&A join.</done>
</task>

<task type="auto">
  <name>Task 2: Register GET /:clientId/forms route in clientsRouter (service.ts)</name>
  <files>backend/api/src/coach/clients/service.ts</files>

  <read_first>
    - backend/api/src/coach/clients/service.ts — read the full file; locate: (a) the import block from './db.js' (around lines 18–36) to see the exact import syntax and where to add getFormsForClient; (b) the GET /:id/programs handler block (around lines 449–462) as the exact pattern to copy for the new route; (c) the UUID_REGEX constant to confirm it is already declared and available.
  </read_first>

  <action>
  Two edits to service.ts:

  Edit 1 — Add getFormsForClient to the existing named import from './db.js'. Locate the import that includes getProgramsForClient and append getFormsForClient to that same import block. Do not create a new import statement.

  Edit 2 — Add the new route handler immediately after the GET /:id/programs block (before the PUT /:clientId/shared-note block if it exists, or at the end of the route registrations). Use this exact structure:

    // GET /:clientId/forms — form instances for this client (RESPONSES-01, RESPONSES-02, RESPONSES-03)
    clientsRouter.get('/:clientId/forms', async (c) => {
      const { userId: coachId } = c.get('auth');
      const jwt = c.req.header('Authorization')!.slice(7);
      const clientId = c.req.param('clientId');
      if (!UUID_REGEX.test(clientId)) return c.json({ error: 'Invalid client id' }, 400);
      try {
        const result = await getFormsForClient(jwt, coachId, clientId);
        return c.json(result);
      } catch (err: any) {
        console.error('[coach/clients] GET /:clientId/forms error:', err.message);
        return c.json({ error: err.message }, 500);
      }
    });

  RF-01 compliance: this route is in clientsRouter (mounted at /coach/clients in app.ts), so the final URL is /coach/clients/:clientId/forms — exactly matching the UI-SPEC fetch URL.
  RF-02 note: authMiddleware is already applied via clientsRouter.use('*', authMiddleware) — no per-route auth needed. c.get('auth') is always populated.
  </action>

  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep -i "service.ts" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - backend/api/src/coach/clients/service.ts contains `clientsRouter.get('/:clientId/forms'`
    - service.ts import from './db.js' includes getFormsForClient
    - UUID_REGEX validation is present in the new handler before the DB call
    - TypeScript compilation of service.ts produces no errors
    - Running `rtk curl http://localhost:3000/coach/clients/00000000-0000-0000-0000-000000000000/forms` with a valid Bearer token returns either `{ "forms": [] }` or a 400/500 (never a 404 "Cannot GET" which would indicate the route is not registered)
  </acceptance_criteria>

  <done>GET /coach/clients/:clientId/forms returns 200 JSON with { forms: [] } for a valid UUID clientId, or 400 for invalid UUID. Route is registered on clientsRouter, not formsRouter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coach browser → Hono API | Bearer JWT validated by authMiddleware before handler runs |
| Hono handler → Supabase | publishable key client — no RLS; manual coach_id ownership filter required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | GET /:clientId/forms | mitigate | authMiddleware validates Bearer JWT on every clientsRouter request; c.get('auth').userId is trust anchor |
| T-05-02 | Information Disclosure | getFormsForClient — cross-coach data leak | mitigate | Manual ownership filter: ownedInstances.filter(i => i.coach_forms?.coach_id === coachId) applied after fetch (RF-02) |
| T-05-03 | Tampering | clientId path param | mitigate | UUID_REGEX validation before any DB call; invalid UUID returns 400 immediately |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. TypeScript compilation clean: `rtk tsc --noEmit -p backend/api/tsconfig.json` — zero errors in db.ts and service.ts
2. Route registered: `curl -X GET http://localhost:3000/coach/clients/invalid-id/forms -H "Authorization: Bearer validtoken"` returns 400 (UUID guard fires)
3. Route accessible: `curl -X GET http://localhost:3000/coach/clients/{valid-uuid}/forms -H "Authorization: Bearer {coach-jwt}"` returns 200 with `{ "forms": [...] }`
4. Security gate: request with a valid JWT for Coach A cannot return form instances belonging to Coach B's forms (ownedInstances filter enforces this)
</verification>

<success_criteria>
- GET /coach/clients/:clientId/forms responds 200 with { forms: FormInstance[] }
- Submitted instances include answers array with question_label and question_type resolved
- Pending instances have answers: null, submitted_at: null
- Results sorted: submitted DESC first, pending at bottom
- Only forms owned by the requesting coach are returned
- TypeScript clean (no errors in db.ts or service.ts)
</success_criteria>

<output>
Create `.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-01-SUMMARY.md` when done.
</output>
