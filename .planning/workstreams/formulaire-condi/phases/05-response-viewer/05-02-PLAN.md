---
phase: 05-response-viewer
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/web/src/components/coach/ClientTabStrip.tsx
  - apps/web/src/components/coach/FormStatusBadge.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/page.tsx
  - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/ClientFormsContent.tsx
autonomous: true
requirements:
  - RESPONSES-01
  - RESPONSES-02
  - RESPONSES-03

must_haves:
  truths:
    - "A 'Formulaires' tab appears in ClientTabStrip and navigates to /coach/clients/[id]/forms"
    - "The forms page shows an empty state (icon + heading + body) when the client has no form instances"
    - "Submitted form rows show the submission date and a 'Soumis' green badge; clicking a row expands to reveal the Q&A transcript"
    - "The Q&A transcript renders each question label (uppercase, muted) paired with the answer: scale as '{N} / 10', yes_no localized to 'Oui'/'Non', text/choice as-is"
    - "Pending form rows show an 'En attente' amber badge, no chevron, no click handler; a note 'L athlète n a pas encore soumis de réponses.' appears when ALL rows are pending"
    - "Only one row is expanded at a time (accordion: expandedInstanceId is string | null)"
    - "Expand panel animates with CSS transition: max-height 0→600px + opacity 0→1 over 200ms ease-out; collapse is 150ms ease-in"
  artifacts:
    - path: "apps/web/src/components/coach/ClientTabStrip.tsx"
      provides: "Formulaires tab entry in TABS array"
      contains: "{ key: 'forms', label: 'Formulaires' }"
    - path: "apps/web/src/components/coach/FormStatusBadge.tsx"
      provides: "submitted and pending badge variants"
      contains: "submitted"
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/page.tsx"
      provides: "Server component — fetches GET /coach/clients/:clientId/forms and passes data to ClientFormsContent"
      contains: "ClientFormsContent"
    - path: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/ClientFormsContent.tsx"
      provides: "Client component — table with expand/collapse accordion for form responses"
      contains: "expandedInstanceId"
  key_links:
    - from: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/page.tsx"
      to: "GET /coach/clients/:clientId/forms"
      via: "fetch with Authorization Bearer JWT"
      pattern: "fetch.*coach/clients.*forms"
    - from: "ClientFormsContent"
      to: "FormStatusBadge"
      via: "import and status prop"
      pattern: "FormStatusBadge.*status"
---

<objective>
Build the "Formulaires" tab for the coach client detail sheet: extend ClientTabStrip with the new tab, extend FormStatusBadge with 'submitted'/'pending' variants, create the server page (forms/page.tsx) following the programs/page.tsx pattern, and create ClientFormsContent.tsx — a client component with accordion expand/collapse, Q&A transcript rendering, empty state, and pending-only note.

This plan satisfies RESPONSES-01 (tab + list), RESPONSES-02 (expand to read Q&A), and RESPONSES-03 ('En attente' badge for pending). It depends on 05-01 for the API contract.

Output: 4 files modified/created in the web app.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/formulaire-condi/STATE.md
@C:/ziko-platform/.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-UI-SPEC.md
@C:/ziko-platform/.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-PATTERNS.md
@C:/ziko-platform/.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-RESEARCH.md
@C:/ziko-platform/apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/page.tsx
@C:/ziko-platform/apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/ClientProgramsContent.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ClientTabStrip and FormStatusBadge</name>
  <files>
    apps/web/src/components/coach/ClientTabStrip.tsx,
    apps/web/src/components/coach/FormStatusBadge.tsx
  </files>

  <read_first>
    - apps/web/src/components/coach/ClientTabStrip.tsx — read full file (~53 lines); confirm current TABS array ends with { key: 'videos', label: 'Vidéos' }; confirm the isActive detection pattern (videos uses includes, all others use endsWith); confirm the href template structure.
    - apps/web/src/components/coach/FormStatusBadge.tsx — read full file (~67 lines); note lines 1–30 export QuestionType, FormQuestion, TriggerType, TriggerConfig, CoachForm — DO NOT touch these; locate the FormStatusBadgeProps interface (line ~34) and STATUS_CONFIG record (lines ~37–56); confirm the badge anatomy (rounded-full px-2 py-0.5 text-xs font-bold inline-flex items-center) for the new entries.
  </read_first>

  <action>
  ClientTabStrip.tsx — ONE change only:
  In the TABS array, add a new entry after { key: 'videos', label: 'Vidéos' }:
    { key: 'forms', label: 'Formulaires' }
  The 'forms' tab uses the default `pathname.endsWith('/forms')` branch in the isActive ternary — no special case needed. The href is auto-generated by the existing template. Do not change any other line.

  FormStatusBadge.tsx — TWO changes only (do not touch lines 1–30):

  Change 1: Extend FormStatusBadgeProps status union from 'draft' | 'active' | 'archived' to:
    status: 'draft' | 'active' | 'archived' | 'submitted' | 'pending'

  Change 2: Add two entries at the end of the STATUS_CONFIG Record (before the closing brace):
    submitted: { bg: 'bg-[#DCFCE7]', text: 'text-[#16A34A]', label: 'Soumis' },
    pending:   { bg: 'bg-[#FEF3C7]', text: 'text-[#D97706]', label: 'En attente' },

  The component function body (lines ~58–67) remains unchanged. The Record type annotation must now match the extended union — the TypeScript Record<FormStatusBadgeProps['status'], ...> will automatically enforce all 5 keys.
  </action>

  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -E "(ClientTabStrip|FormStatusBadge)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - ClientTabStrip.tsx TABS array contains { key: 'forms', label: 'Formulaires' } as the 12th entry (after videos)
    - FormStatusBadge.tsx FormStatusBadgeProps status union includes 'submitted' and 'pending'
    - FormStatusBadge.tsx STATUS_CONFIG contains submitted: { bg: 'bg-[#DCFCE7]', text: 'text-[#16A34A]', label: 'Soumis' }
    - FormStatusBadge.tsx STATUS_CONFIG contains pending: { bg: 'bg-[#FEF3C7]', text: 'text-[#D97706]', label: 'En attente' }
    - Exported types on lines 1–30 of FormStatusBadge.tsx are unchanged (QuestionType still uses 'yesno' not 'yes_no' — this is intentional; do not modify)
    - TypeScript compilation of both files produces no errors
  </acceptance_criteria>

  <done>12-tab ClientTabStrip with Formulaires last; FormStatusBadge accepting submitted and pending with correct amber/green tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Create forms/page.tsx server component and ClientFormsContent.tsx client component</name>
  <files>
    apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/page.tsx,
    apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/ClientFormsContent.tsx
  </files>

  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/page.tsx — read full file (94 lines); this is the exact analog for page.tsx. Copy the import block, getCachedCoachUser pattern, getLocale Promise.all pattern, supabase.auth.getSession() JWT extraction, and the Bearer fetch pattern with fallback.
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/ClientProgramsContent.tsx — read full file (323 lines); note: 'use client' directive, useState usage, empty state pattern (icon + heading + body in flex col centered), formatDate helper, the bg-white rounded-2xl border border-border overflow-hidden container, and table/thead/tbody structure.
    - apps/web/src/components/coach/FormStatusBadge.tsx — read lines 58–67 (component function) to confirm the import path used by ClientFormsContent for <FormStatusBadge status={...} />.
  </read_first>

  <action>
  CREATE apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/page.tsx:

  Server component. Imports: getLocale from 'next-intl/server', createServerSupabase from '@/lib/supabase/server', getCachedCoachUser from '@/lib/coach/auth', ClientFormsContent from './ClientFormsContent'.

  Define local types at the top of the file (outside the component, needed for the forms variable):
    interface FormAnswer { question_id: string; question_label: string; question_type: string; answer_value: string | number; }
    interface FormInstance { instance_id: string; form_id: string; form_title: string; status: 'pending' | 'submitted'; submitted_at: string | null; question_count: number; answers: FormAnswer[] | null; }
    interface FormInstancesResponse { forms: FormInstance[]; }

  Component signature: export default async function ClientFormsPage({ params }: { params: Promise<{ locale: string; id: string }> })

  Body:
  1. Destructure: const { id: clientId } = await params;
  2. Auth + locale: const [locale] = await Promise.all([getLocale(), getCachedCoachUser()]);
  3. JWT: const supabase = await createServerSupabase(); const { data: { session } } = await supabase.auth.getSession(); const jwt = session?.access_token ?? ''; const apiUrl = process.env.NEXT_PUBLIC_API_URL ?? '';
  4. Fetch with fallback: let formsData: FormInstancesResponse = { forms: [] }; if (jwt) { try { const res = await fetch(`${apiUrl}/coach/clients/${clientId}/forms`, { headers: { Authorization: `Bearer ${jwt}` }, cache: 'no-store' }); if (res.ok) { const json = await res.json(); formsData = json ?? formsData; } } catch (err) { console.error('[clients/[id]/forms/page] fetch error:', err); } }
  5. Return: <ClientFormsContent forms={formsData.forms} locale={locale} />

  ---

  CREATE apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/ClientFormsContent.tsx:

  'use client' directive. Imports: useState from 'react'; IoChevronDownOutline, IoChevronUpOutline, IoDocumentTextOutline from 'react-icons/io5'; FormStatusBadge from '@/components/coach/FormStatusBadge'.

  Local interfaces (RF-03: do NOT import QuestionType from FormStatusBadge — it uses 'yesno' not 'yes_no'):
    type QuestionType = 'text' | 'scale' | 'yes_no' | 'choice';
    interface FormAnswer { question_id: string; question_label: string; question_type: QuestionType; answer_value: string | number; }
    interface FormInstance { instance_id: string; form_id: string; form_title: string; status: 'pending' | 'submitted'; submitted_at: string | null; question_count: number; answers: FormAnswer[] | null; }
    interface ClientFormsContentProps { forms: FormInstance[]; locale: string; }

  Helper functions:
    function formatDate(iso: string | null): string — uses new Date(iso).toLocaleDateString('fr-FR', { day: 'numeric', month: 'short', year: 'numeric' }) — returns '—' for null.
    function formatAnswerValue(type: QuestionType, value: string | number): string — 'scale' → `${value} / 10`; 'yes_no' → value === 'yes' ? 'Oui' : 'Non'; default → String(value).
    const TYPE_LABELS: Record<QuestionType, string> = { text: 'Texte libre', scale: 'Échelle 1–10', yes_no: 'Oui / Non', choice: 'Choix unique' };

  Component export: export function ClientFormsContent({ forms, locale }: ClientFormsContentProps)
  State: const [expandedInstanceId, setExpandedInstanceId] = useState<string | null>(null);

  Sorting (before render):
    const sorted = [ ...forms.filter(f => f.status === 'submitted').sort((a, b) => new Date(b.submitted_at!).getTime() - new Date(a.submitted_at!).getTime()), ...forms.filter(f => f.status === 'pending') ];

  Empty state (when forms.length === 0):
    Render a div with className="flex flex-col items-center text-center py-16":
      IoDocumentTextOutline size={48} color="#E2E0DA" aria-hidden="true"
      h2: "Aucun formulaire pour ce client" — className="text-[22px] font-bold text-text mt-4"
      p: "Les formulaires assignés et les réponses apparaîtront ici." — className="text-sm text-muted mt-2"

  Main render (when forms.length > 0):
    Outer div className="mt-4". Inner div className="bg-white rounded-2xl border border-border overflow-hidden".

    Table: className="w-full". thead with one tr className="bg-background" containing 4 th elements className="py-3 px-4 text-left text-xs font-bold tracking-wide uppercase text-muted": FORMULAIRE, STATUT, DATE, RÉPONSES.

    tbody: map over sorted. For each form, render a React.Fragment with key={form.instance_id} containing:

    Row tr (submitted): className="border-t border-border hover:bg-background/60 cursor-pointer transition-colors duration-150 min-h-[56px]" onClick={() => setExpandedInstanceId(expandedInstanceId === form.instance_id ? null : form.instance_id).
      - td FORMULAIRE: className="py-3 px-4 text-sm font-bold text-text max-w-[280px] truncate" — form.form_title
      - td STATUT: className="py-3 px-4" — <FormStatusBadge status={form.status} />
      - td DATE: className="py-3 px-4 text-xs text-muted" — formatDate(form.submitted_at)
      - td RÉPONSES: className="py-3 px-4 text-xs text-muted text-right" — span "{form.question_count} réponses" + IoChevronDownOutline or IoChevronUpOutline 16px className="text-muted ml-2 inline" based on isExpanded (use expandedInstanceId === form.instance_id)

    Row tr (pending): className="border-t border-border cursor-default min-h-[56px]" — no onClick.
      - td FORMULAIRE: same as submitted
      - td STATUT: <FormStatusBadge status="pending" />
      - td DATE: className="py-3 px-4 text-xs text-muted italic" — "—"
      - td RÉPONSES: className="py-3 px-4 text-xs text-muted italic" — "—"

    Expand panel (submitted rows only): render as a tr with a single td colSpan={4} immediately after the submitted row tr. The td contains a div with inline style for CSS transition:
      style={{ maxHeight: isExpanded ? '600px' : '0', opacity: isExpanded ? 1 : 0, overflow: 'hidden', transition: isExpanded ? 'max-height 200ms ease-out, opacity 200ms ease-out' : 'max-height 150ms ease-in, opacity 150ms ease-in' }}

    Inside the transition div: a div className="bg-[#F7F6F3] px-4 py-4 border-t border-border".

    Q&A pairs: form.answers?.map((answer, idx) => (
      <div key={answer.question_id} className={`${idx < (form.answers?.length ?? 0) - 1 ? 'border-b border-[#E2E0DA] pb-4 mb-4' : ''}`}>
        <div className="flex items-center justify-between mb-1">
          <span className="text-xs font-bold text-muted uppercase tracking-wide">{answer.question_label}</span>
          <span className="bg-[#F0EFE9] text-[#6B6963] rounded px-1.5 py-0.5 text-xs">{TYPE_LABELS[answer.question_type]}</span>
        </div>
        <p className="text-sm text-text">{formatAnswerValue(answer.question_type, answer.answer_value)}</p>
      </div>
    ))

    Bottom of expand panel: p className="text-xs text-muted italic mt-2" — "Soumis le {formatDate(form.submitted_at)}" (use the full submitted_at with time if available; formatDate covers the date portion).

    Pending-only note (outside the table, inside the inner container div, after the table): render conditionally when sorted.every(f => f.status === 'pending'):
      p className="text-sm text-muted italic text-center py-4" — "L&apos;athlète n&apos;a pas encore soumis de réponses."

  Chevron rotation: use inline className for the chevron icon — className="transition-transform duration-200" with no additional rotate class (the swap between IoChevronDownOutline and IoChevronUpOutline already signals state; rotation is optional but the spec says rotate-180 on expanded — apply if preferred via a style or class).
  </action>

  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -E "(forms/page|ClientFormsContent)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/page.tsx exists and contains export default async function ClientFormsPage
    - page.tsx contains getCachedCoachUser() call (auth guard)
    - page.tsx contains fetch(`${apiUrl}/coach/clients/${clientId}/forms`, ...) with Authorization Bearer header
    - page.tsx renders <ClientFormsContent forms={formsData.forms} locale={locale} />
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/forms/ClientFormsContent.tsx exists with 'use client' directive
    - ClientFormsContent.tsx contains type QuestionType = 'text' | 'scale' | 'yes_no' | 'choice' (with underscore — RF-03 compliant)
    - ClientFormsContent.tsx contains useState<string | null>(null) for expandedInstanceId
    - ClientFormsContent.tsx contains formatAnswerValue function with scale, yes_no, default branches
    - ClientFormsContent.tsx contains TYPE_LABELS record with all 4 keys
    - ClientFormsContent.tsx contains empty state with IoDocumentTextOutline and "Aucun formulaire pour ce client"
    - ClientFormsContent.tsx contains pending-only note "L'athlète n'a pas encore soumis de réponses." conditional on sorted.every(f => f.status === 'pending')
    - TypeScript compilation of both new files produces no errors
  </acceptance_criteria>

  <done>forms/page.tsx fetches the API and passes data to ClientFormsContent. ClientFormsContent renders all 4 states (empty, populated, expanded, pending-only) per UI-SPEC. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js server component → Hono API | Coach JWT from session cookie sent as Bearer — server-side fetch, not client-side |
| ClientFormsContent → DOM | Read-only render; no mutations, no form submission, no user input fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | forms/page.tsx server fetch | mitigate | fetch gated behind `if (jwt)` — no unauthenticated requests to the API; getCachedCoachUser() redirects unauthenticated users before any data is fetched |
| T-05-05 | Tampering | ClientFormsContent rendering | accept | Read-only component; no state mutations, no API calls, no form submissions — attack surface is nil |
| T-05-06 | Information Disclosure | forms array passed as props | accept | Data is scoped to coach-owned forms only by the Hono route (05-01 plan); component only renders what it receives |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. TypeScript clean: `rtk tsc --noEmit -p apps/web/tsconfig.json` — zero errors in all 4 modified/created files
2. Tab visible: Navigate to /[locale]/coach/clients/[id]/forms — "Formulaires" tab appears active in the tab strip
3. Empty state: Client with no form instances shows IoDocumentTextOutline icon + "Aucun formulaire pour ce client" heading
4. Submitted row: Client with submitted form instances shows rows with green "Soumis" badge + date + "{N} réponses" + down chevron
5. Expand: Clicking a submitted row reveals Q&A panel; scale answer shows "{N} / 10"; yes_no shows "Oui"/"Non"; text shows raw string
6. Accordion: Clicking a second row collapses the first (expandedInstanceId is a single value)
7. Pending row: Pending form shows amber "En attente" badge, "—" in date and responses, no chevron, no click behavior
8. Pending-only note: Client with only pending forms shows the note below the table rows

Checkpoint: checkpoint:human-verify follows — executor flags for coach visual review
</verification>

<success_criteria>
- "Formulaires" tab in ClientTabStrip navigates to the correct URL and activates correctly
- forms/page.tsx fetches from /coach/clients/:clientId/forms with coach Bearer token
- All 4 UI states render correctly per UI-SPEC: empty, populated (collapsed), expanded Q&A, pending-only
- Answer formatting: scale = "{N} / 10", yes_no = "Oui"/"Non", text/choice = raw value
- Expand/collapse animation uses CSS transition (max-height + opacity), 200ms ease-out expand, 150ms ease-in collapse
- TypeScript clean across all 4 files
</success_criteria>

<output>
Create `.planning/workstreams/formulaire-condi/phases/05-response-viewer/05-02-SUMMARY.md` when done.
</output>
