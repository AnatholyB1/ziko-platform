---
plan: 38-02
phase: 38
wave: 1
depends_on: []
autonomous: true
files_modified:
  - plugins/stretching/src/screens/StretchingPlugin.tsx
  - plugins/stretching/src/index.ts
  - apps/mobile/app/(app)/(plugins)/stretching/dashboard.tsx
  - plugins/sleep/src/screens/SleepPlugin.tsx
  - plugins/sleep/src/index.ts
  - apps/mobile/app/(app)/(plugins)/sleep/dashboard.tsx
requirements:
  - PLUG-STR-01
  - PLUG-STR-02
  - PLUG-STR-03
  - PLUG-STR-04
  - PLUG-SLP-01
  - PLUG-SLP-02
  - PLUG-SLP-03
  - PLUG-SLP-04
must_haves:
  goal: Stretching et Sleep correspondent au mockup plugins-2.jsx avec SubTabs, données réelles et suppression des anciens dashboards.
  truths:
    - StretchingPlugin.tsx existe avec 3 SubTabs (Routines / Bibliothèque / Suivi)
    - Onglet Routines : liste depuis stretching_routines avec icône colorée 44×44, AISuggestion en premier
    - Onglet Bibliothèque : RowList statique de 6 exercices de stretching
    - Onglet Suivi : card "14 séances ce mois" + MiniBars 7 jours depuis stretching_logs, AISuggestion
    - SleepPlugin.tsx existe avec 3 SubTabs (Cette nuit / Historique / Réglages)
    - Onglet Cette nuit : durée 36px violet depuis sleep_logs, barre de phases heuristique (4 segments proportionnels à duration_hours), AISuggestion violet
    - Onglet Historique : MiniBars 7 jours duration_hours depuis sleep_logs, AISuggestion
    - Onglet Réglages : RowList statique de 5 paramètres sommeil
    - StretchingDashboard.tsx et SleepDashboard.tsx supprimés avec zéro import restant
  artifacts:
    - path: plugins/stretching/src/screens/StretchingPlugin.tsx
      provides: Stretching plugin entrypoint avec 3 SubTabs et données réelles
      exports: [StretchingPlugin]
    - path: plugins/sleep/src/screens/SleepPlugin.tsx
      provides: Sleep plugin entrypoint avec 3 SubTabs et données réelles
      exports: [SleepPlugin]
  key_links:
    - from: apps/mobile/app/(app)/(plugins)/stretching/dashboard.tsx
      to: plugins/stretching/src/screens/StretchingPlugin.tsx
      via: "@ziko/plugin-stretching/screens/StretchingPlugin import"
    - from: SleepPlugin.tsx (onglet Cette nuit)
      to: sleep_logs
      via: "TanStack Query useQuery ORDER BY date DESC LIMIT 1"
    - from: SleepPlugin.tsx barre de phases
      to: duration_hours
      via: "estimateSleepStages(duration_hours) — ratios heuristiques fixes (D-04)"
---

<objective>
Livrer le redesign complet des plugins Stretching et Sleep selon le mockup canonique `plugins-2.jsx`. Stretching affiche les routines DB + bibliothèque statique + historique avec MiniBars. Sleep affiche la nuit dernière avec barre de phases heuristique + historique 7 jours + réglages statiques. Les anciens dashboards sont supprimés après vérification.

Purpose: Stretching et Sleep s'exécutent en wave 1, en parallèle avec 38-01 et 38-03. Stretching lit `stretching_routines` (013) et `stretching_logs` (012). Sleep lit `sleep_logs` (012) et implémente la barre de phases via ratios heuristiques (D-04 — la table ne contient pas de colonnes de phases réelles).

Output: StretchingPlugin.tsx, SleepPlugin.tsx, route wrappers mis à jour, index.ts mis à jour, anciens dashboards supprimés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/RESEARCH.md

<interfaces>
<!-- Interfaces vérifiées depuis RESEARCH.md -->

SubTabs / AISuggestion / PluginHeader : voir 38-01-PLAN.md interfaces (même API — pill style Phase 37).

MiniBars local (même pattern que 38-01, hauteur absolue) :
```typescript
function MiniBars({ data, color, max }: { data: Array<{ l: string; v: number; today?: boolean }>; color: string; max?: number }) {
  const m = max ?? Math.max(...data.map((d) => d.v), 1);
  return (
    <View style={{ flexDirection: 'row', alignItems: 'flex-end', gap: 6, height: 80 }}>
      {data.map((d, i) => (
        <View key={i} style={{ flex: 1, alignItems: 'center', justifyContent: 'flex-end', height: 80, gap: 5 }}>
          <View style={{ width: '100%', height: Math.max((d.v / m) * 72, 4), backgroundColor: d.today ? color : color + '47', borderRadius: 5 }} />
          <Text style={{ fontSize: 9.5, fontWeight: '700', color: d.today ? color : '#6B6963' }}>{d.l}</Text>
        </View>
      ))}
    </View>
  );
}
```

Table stretching_routines (migration 013) :
- id TEXT PK, user_id UUID, name TEXT, type TEXT DEFAULT 'custom'
- muscle_groups TEXT[] DEFAULT '{}', duration_minutes INTEGER DEFAULT 0
- exercises JSONB DEFAULT '[]', created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ

Table stretching_logs (migration 012) :
- id UUID PK, user_id UUID, routine_name TEXT, duration_sec INTEGER
- exercises JSONB DEFAULT '[]', date DATE DEFAULT CURRENT_DATE, created_at TIMESTAMPTZ

Table sleep_logs (migration 012) :
- id UUID PK, user_id UUID, bedtime TEXT (ex: "23:12"), wake_time TEXT (ex: "07:00")
- duration_hours NUMERIC(4,2), quality INTEGER CHECK(1-5), notes TEXT
- date DATE DEFAULT CURRENT_DATE — UNIQUE (user_id, date)
- PAS DE colonnes de phases de sommeil (D-04)

Heuristique phases sommeil (D-04, RESEARCH.md Q9) :
```typescript
function estimateSleepStages(durationHours: number) {
  return [
    { l: 'Profond', h: +(durationHours * 0.18).toFixed(1), c: '#7C3AED' },
    { l: 'Léger',   h: +(durationHours * 0.53).toFixed(1), c: '#A78BFA' },
    { l: 'REM',     h: +(durationHours * 0.24).toFixed(1), c: '#2196F3' },
    { l: 'Éveillé', h: +(durationHours * 0.05).toFixed(1), c: '#9CA3AF' },
  ];
}
// Barre : flex proportionnel à h dans conteneur flexDirection 'row' height 24 borderRadius 6 overflow 'hidden'
```

Barrel exports :
- plugins/stretching/src/index.ts : ajouter `export { default as StretchingPlugin } from './screens/StretchingPlugin';`
- plugins/sleep/src/index.ts : ajouter `export { default as SleepPlugin } from './screens/SleepPlugin';`

Tokens design :
- Stretching : couleur accent = primary #FF5C1A
- Sleep : couleur accent = violet #7C3AED (var(--violet) du mockup)
- Card shadow : shadowColor '#1C1A17', shadowOffset {0,4}, shadowOpacity 0.08, shadowRadius 12, elevation 3
- paddingBottom: 100 sur tout ScrollView
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construire StretchingPlugin.tsx — 3 SubTabs avec routines DB et historique</name>
  <files>plugins/stretching/src/screens/StretchingPlugin.tsx</files>
  <read_first>
    - plugins/stretching/src/screens/StretchingDashboard.tsx — lire l'implémentation existante (patterns navigation, queries Supabase, types)
    - plugins/stretching/src/store.ts — lire StretchRoutine, StretchLog types disponibles
    - C:/Users/Anatholy/Downloads/ziko/plugins-2.jsx lignes 210–275 (StretchingPlugin : Routines/Bibliothèque/Suivi)
  </read_first>
  <behavior>
    - useQuery(['stretching_routines', userId]) : fetch stretching_routines WHERE user_id = userId ORDER BY created_at DESC
    - useQuery(['stretching_logs_7days', userId]) : fetch stretching_logs WHERE user_id = userId AND date >= (today - 7 jours) ORDER BY date ASC
    - useQuery(['stretching_count_month', userId]) : COUNT stretching_logs WHERE user_id AND date >= début du mois
    - AISuggestion onglet Routines : règle — si dernière séance de stretching > 2 jours → "Tu as fait [session] hier — la routine Post-séance réduira tes courbatures." sinon "Continue sur ta lancée — mobilité quotidienne !"
    - AISuggestion onglet Suivi : si jour sans routine dans les 7 derniers → "Tu sautes les routines le [jour]. Ajoute un rappel à 21h ?"
    - Bibliothèque : 6 exercices statiques (pas de table exercices stretching séparée) — Pigeon, Cobra, Couch stretch, World's greatest, Foam roll IT band, Cat-cow
    - État vide onglet Routines : "Aucune routine créée — commence par en créer une !"
  </behavior>
  <action>
    Créer plugins/stretching/src/screens/StretchingPlugin.tsx comme export default function StretchingPlugin({ supabase }: { supabase: any }).

    Imports : SafeAreaView, ScrollView, View, Text, TouchableOpacity from react-native + react-native-safe-area-context; useState from 'react'; useRouter from 'expo-router'; SubTabs, AISuggestion, PluginHeader from '@ziko/ui'; useThemeStore from '@ziko/plugin-sdk'; useQuery from '@tanstack/react-query'; Ionicons from '@expo/vector-icons'.

    Déclarer MiniBars localement (voir pattern interfaces — hauteur absolue Math.max(v/m*72, 4)px).

    Tabs : const TABS = ['Routines', 'Bibliothèque', 'Suivi'].

    ONGLET ROUTINES (activeTab === 'Routines') :
    - AISuggestion en premier avec règle basée sur la date du dernier log
    - Liste des routines depuis stretching_routines query
    - Chaque carte routine : View card flexDirection 'row' alignItems 'center' gap 12 padding 14
      * Icône 44×44 borderRadius 12 : backgroundColor fond coloré 14% (couleur cyclique per index: primary/info/violet/success), Ionicons 'body-outline' size 20
      * Couleurs cycliques : ['#FF5C1A','#2E7BF6','#7C3AED','#2E9E5B'][index % 4]
      * Text nom : fontSize 14 fontWeight '700' (h-display)
      * Text sub : fontSize 11 color muted — "{duration_minutes} min · {exercises.length || 0} exos · {type}"
      * Ionicons 'play-outline' à droite : size 16 color cyclique
    - État vide : Text centré "Aucune routine créée" avec CTA "Créer une routine"
    - Loading : ActivityIndicator color primary

    ONGLET BIBLIOTHÈQUE (activeTab === 'Bibliothèque') :
    - RowList statique de 6 exercices :
      * "Pigeon" — sub "Hanches · 60s/côté" — icon 'body-outline'
      * "Cobra" — sub "Bas du dos · 45s" — icon 'body-outline'
      * "Couch stretch" — sub "Quadriceps · 90s/côté" — icon 'body-outline'
      * "World's greatest" — sub "Full body · 8 reps" — icon 'body-outline'
      * "Foam roll IT band" — sub "Bandelette · 60s/jambe" — icon 'body-outline'
      * "Cat-cow" — sub "Colonne vertébrale · 10 cycles" — icon 'body-outline'
    - RowList local : View gap 6; chaque item View card padding 12 flexDirection 'row' alignItems 'center' gap 12; View icône 34×34 borderRadius 10 backgroundColor 'rgba(255,92,26,0.14)' color primary; Text title 12.5px bold; Text sub 10.5px muted

    ONGLET SUIVI (activeTab === 'Suivi') :
    - Card récap mois : Text "{countMonth} séances ce mois" fontSize 28 fontWeight '700'; suffixe " séances ce mois" en 12px color muted inline
    - MiniBars 7 jours depuis stretching_logs : data = 7 derniers jours, v = duration_sec / 60 (minutes), l = 'L'/'M'/'M'/'J'/'V'/'S'/'D', today = dernier jour; color = primary
    - Text tendance en dessous (vert si progression vs semaine précédente)
    - AISuggestion avec règle jours manqués

    Card shadow : shadowColor '#1C1A17', shadowOffset {0,4}, shadowOpacity 0.08, shadowRadius 12, elevation 3.
    paddingBottom: 100 sur contentContainerStyle.
    showAlert de '@ziko/plugin-sdk' si besoin.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "stretching/src/screens/StretchingPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - StretchingPlugin.tsx exporte une fonction default nommée StretchingPlugin
    - Fichier contient useState avec 'Routines' comme valeur initiale
    - Fichier contient useQuery pour stretching_routines et stretching_logs
    - Fichier contient MiniBars déclaré localement (pas importé)
    - Fichier contient les 6 exercices statiques (Pigeon, Cobra, Couch stretch...)
    - Fichier NE contient PAS de constante STRETCHING_DATA fixture
    - TypeScript : zéro erreur sur ce fichier
  </acceptance_criteria>
  <done>StretchingPlugin.tsx créé avec 3 SubTabs (Routines/Bibliothèque/Suivi), routines DB, bibliothèque statique, MiniBars 7 jours depuis stretching_logs; TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Construire SleepPlugin.tsx — 3 SubTabs avec barre de phases heuristique</name>
  <files>plugins/sleep/src/screens/SleepPlugin.tsx</files>
  <read_first>
    - plugins/sleep/src/screens/SleepDashboard.tsx — lire l'implémentation existante (patterns, queries, types)
    - plugins/sleep/src/store.ts — lire useSleepStore, SleepLog type
    - C:/Users/Anatholy/Downloads/ziko/plugins-2.jsx lignes 277–340 (SleepPlugin : Cette nuit/Historique/Réglages)
  </read_first>
  <behavior>
    - useQuery(['sleep_last', userId]) : fetch sleep_logs WHERE user_id = userId ORDER BY date DESC LIMIT 1 → nuit dernière
    - useQuery(['sleep_7days', userId]) : fetch sleep_logs WHERE user_id = userId AND date >= (today - 7 jours) ORDER BY date ASC → 7 nuits
    - estimateSleepStages(duration_hours) : Profond 18%, Léger 53%, REM 24%, Éveillé 5% × duration_hours (D-04)
    - Durée affichée : convertir duration_hours en format "7h48" → Math.floor(h) + 'h' + Math.round((h % 1) * 60).toString().padStart(2,'0')
    - AISuggestion Cette nuit : si quality < 3 → "Ton sommeil profond est faible. Évite la caféine après 14h ?" tintColor violet; si quality >= 4 → "Excellente nuit ! Continue cette routine sommeil."
    - AISuggestion Historique : identifier le jour avec durée la plus faible sur 7 jours → "Tu dors mal le [jour]. Décale ta séance avant 19h ?"
    - Réglages : 5 paramètres statiques (pas de backing DB pour les réglages sommeil)
  </behavior>
  <action>
    Créer plugins/sleep/src/screens/SleepPlugin.tsx comme export default function SleepPlugin({ supabase }: { supabase: any }).

    Imports : SafeAreaView, ScrollView, View, Text from react-native + react-native-safe-area-context; useState from 'react'; useRouter from 'expo-router'; SubTabs, AISuggestion, PluginHeader from '@ziko/ui'; useThemeStore from '@ziko/plugin-sdk'; useQuery from '@tanstack/react-query'; Ionicons from '@expo/vector-icons'.

    Couleur accent Sleep : VIOLET = '#7C3AED'.

    Déclarer estimateSleepStages localement :
    ```
    function estimateSleepStages(dh: number) {
      return [
        { l: 'Profond', h: +(dh * 0.18).toFixed(1), c: '#7C3AED' },
        { l: 'Léger',   h: +(dh * 0.53).toFixed(1), c: '#A78BFA' },
        { l: 'REM',     h: +(dh * 0.24).toFixed(1), c: '#2196F3' },
        { l: 'Éveillé', h: +(dh * 0.05).toFixed(1), c: '#9CA3AF' },
      ];
    }
    ```

    Déclarer formatDuration localement : `(h: number) => Math.floor(h) + 'h' + Math.round((h % 1) * 60).toString().padStart(2, '0')`.

    Déclarer MiniBars localement (hauteur absolue — même pattern 38-01).

    Tabs : const TABS = ['Cette nuit', 'Historique', 'Réglages'].

    ONGLET CETTE NUIT (activeTab === 'Cette nuit') :
    - Card principale :
      * Text durée : formatDuration(lastLog.duration_hours), fontSize 36, fontWeight '700', color VIOLET
      * Text horaires + score : "{lastLog.bedtime} → {lastLog.wake_time} · score {lastLog.quality * 20}/100", fontSize 12, color muted, marginTop 4
      * Barre de phases : View flexDirection 'row' height 24 borderRadius 6 overflow 'hidden' marginTop 14; children = estimateSleepStages(duration_hours).map(s => View key={s.l} style={{ flex: s.h, backgroundColor: s.c }} )
      * Légende : View flexDirection 'row' justifyContent 'space-between' marginTop 10; 4 colonnes; chaque colonne : View alignItems 'center'; View 8×8 borderRadius 2 bg=s.c; Text label 10px bold; Text "{s.h}h" 10px color muted
    - AISuggestion (tintColor=VIOLET) avec règle basée sur quality
    - Grille 2 StatTiles : "Fréq. card. min" valeur "— bpm" (pas de donnée DB) + "Variabilité" valeur "— ms" — afficher "—" si pas de wearable connecté; sub "non disponible"
    - État vide (pas de log) : Text "Aucune nuit enregistrée — commence à tracker ton sommeil !"

    ONGLET HISTORIQUE (activeTab === 'Historique') :
    - Card "7 derniers jours" :
      * Text title 13px bold marginBottom 12
      * MiniBars data = 7 nuits mappées : { l: jour court 'L'/'M'..., v: duration_hours, today: dernière entrée } color=VIOLET
      * Text "Moyenne : {avg}h · cible 8h00" fontSize 11 color muted marginTop 10
    - AISuggestion (tintColor=VIOLET) avec règle jour le plus faible

    ONGLET RÉGLAGES (activeTab === 'Réglages') :
    - RowList statique de 5 paramètres :
      * "Cible de sommeil" — sub "8h00 / nuit" — icon 'moon-outline'
      * "Rappel coucher" — sub "Tous les jours à 22h30" — icon 'notifications-outline'
      * "Source données" — sub "Non connectée" — icon 'watch-outline'
      * "Réveil intelligent" — sub "Désactivé" — icon 'timer-outline'
      * "Routine pré-sommeil" — sub "Désactivée" — icon 'body-outline'
    - Note : Réglages non-interactifs dans cette phase (D-02 de PLUG-SLP)

    StatTile local : View card padding 12, Text label uppercase 10px muted, Text value 22px fontWeight '700' color paramétrable, Text sub optionnel.
    RowList local : même pattern que 38-01.
    paddingBottom: 100 sur contentContainerStyle.
    Card shadow pattern standard.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "sleep/src/screens/SleepPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - SleepPlugin.tsx exporte une fonction default nommée SleepPlugin
    - Fichier contient useState avec 'Cette nuit' comme valeur initiale
    - Fichier contient estimateSleepStages déclaré localement avec les 4 ratios (0.18, 0.53, 0.24, 0.05)
    - Fichier contient useQuery pour sleep_logs (dernière nuit + 7 jours)
    - Fichier contient la barre de phases avec flex proportionnel (flex: s.h)
    - Fichier contient la couleur '#7C3AED' (violet Sleep)
    - Fichier NE contient PAS de constante SLEEP_DATA fixture
    - TypeScript : zéro erreur sur ce fichier
  </acceptance_criteria>
  <done>SleepPlugin.tsx créé avec 3 SubTabs (Cette nuit/Historique/Réglages), barre de phases heuristique flex proportionnel, MiniBars 7 nuits, AISuggestion avec règle quality; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Câbler les route wrappers, mettre à jour les barrels, supprimer les anciens dashboards</name>
  <files>
    apps/mobile/app/(app)/(plugins)/stretching/dashboard.tsx,
    apps/mobile/app/(app)/(plugins)/sleep/dashboard.tsx,
    plugins/stretching/src/index.ts,
    plugins/sleep/src/index.ts
  </files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/stretching/dashboard.tsx — lire l'import actuel
    - apps/mobile/app/(app)/(plugins)/sleep/dashboard.tsx — lire l'import actuel
    - plugins/stretching/src/index.ts — lire tous les exports (attention : pas d'export screen existant pour StretchingDashboard)
    - plugins/sleep/src/index.ts — lire tous les exports
  </read_first>
  <action>
    STRETCHING :
    Étape 1 — Route wrapper stretching : changer l'import de 'StretchingDashboard' à 'StretchingPlugin' via '@ziko/plugin-stretching/screens/StretchingPlugin'. Garder le corps du composant wrapper inchangé.

    Étape 2 — Barrel stretching : dans plugins/stretching/src/index.ts, ajouter `export { default as StretchingPlugin } from './screens/StretchingPlugin';`. Conserver tous les autres exports (useStretchingStore, types, manifeste).

    Étape 3 — Suppression stretching après vérification : grep -r 'StretchingDashboard' dans /c/ziko-platform/apps et /c/ziko-platform/plugins/stretching/src. Si count = 0, supprimer plugins/stretching/src/screens/StretchingDashboard.tsx.

    SLEEP :
    Étape 4 — Route wrapper sleep : changer l'import de 'SleepDashboard' à 'SleepPlugin' via '@ziko/plugin-sleep/screens/SleepPlugin'. Garder le corps du composant inchangé.

    Étape 5 — Barrel sleep : dans plugins/sleep/src/index.ts, ajouter `export { default as SleepPlugin } from './screens/SleepPlugin';`. Conserver useSleepStore, SleepLog type, manifeste.

    Étape 6 — Suppression sleep après vérification : grep -r 'SleepDashboard' dans /c/ziko-platform/apps et /c/ziko-platform/plugins/sleep/src. Si count = 0, supprimer plugins/sleep/src/screens/SleepDashboard.tsx.

    Attention RESEARCH.md Q6 : plugins/stretching/src/index.ts n'exporte pas de screen existant pour StretchingDashboard — ajouter seulement StretchingPlugin sans supprimer d'export existant inexistant. Vérifier l'index.ts réel avant modification.

    Commande grep de vérification :
    grep -r 'StretchingDashboard\|SleepDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/stretching/src /c/ziko-platform/plugins/sleep/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees
  </action>
  <verify>
    <automated>grep -r 'StretchingDashboard\|SleepDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/stretching/src /c/ziko-platform/plugins/sleep/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(plugins)/stretching/dashboard.tsx importe 'StretchingPlugin' (pas 'StretchingDashboard')
    - apps/mobile/app/(app)/(plugins)/sleep/dashboard.tsx importe 'SleepPlugin' (pas 'SleepDashboard')
    - plugins/stretching/src/index.ts exporte StretchingPlugin
    - plugins/sleep/src/index.ts exporte SleepPlugin
    - grep 'StretchingDashboard|SleepDashboard' dans apps/ et plugins/ retourne 0 ligne
    - plugins/stretching/src/screens/StretchingDashboard.tsx N'EXISTE PAS (supprimé)
    - plugins/sleep/src/screens/SleepDashboard.tsx N'EXISTE PAS (supprimé)
    - TypeScript : cd /c/ziko-platform && rtk tsc --noEmit retourne 0 erreur
  </acceptance_criteria>
  <done>Route wrappers mis à jour, barrels mis à jour, anciens dashboards supprimés avec zéro import restant confirmé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StretchingPlugin → Supabase | stretching_routines + stretching_logs lus avec user_id scopé; RLS enforce ownership |
| SleepPlugin → Supabase | sleep_logs lus avec user_id scopé; UNIQUE (user_id, date) côté DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Information Disclosure | stretching_routines query | mitigate | .eq('user_id', userId) sur toutes les queries; RLS enforce auth.uid() = user_id |
| T-38-02-02 | Information Disclosure | sleep_logs query | mitigate | .eq('user_id', userId) sur toutes les queries; données sommeil privées scopées correctement |
| T-38-02-03 | Spoofing | estimateSleepStages — données heuristiques | accept | Données heuristiques clairement dérivées depuis duration_hours; pas de fausses données médicales présentées comme réelles; risque médical nul dans contexte fitness |
| T-38-02-04 | Tampering | Bibliothèque exercices statique | accept | Liste hardcodée — pas d'écriture DB; pas de surface d'injection |
| T-38-02-SC | Tampering | npm installs | mitigate | Aucun nouveau paquet — toutes les dépendances déjà installées |
</threat_model>

<verification>
1. TypeScript clean : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'` retourne 0
2. StretchingPlugin créé : `grep 'export default function StretchingPlugin' /c/ziko-platform/plugins/stretching/src/screens/StretchingPlugin.tsx` retourne un match
3. SleepPlugin créé : `grep 'export default function SleepPlugin' /c/ziko-platform/plugins/sleep/src/screens/SleepPlugin.tsx` retourne un match
4. Barre de phases Sleep : `grep 'flex: s\.h\|flex:s\.h' /c/ziko-platform/plugins/sleep/src/screens/SleepPlugin.tsx` retourne un match
5. Heuristique présente : `grep '0\.18\|0\.53\|0\.24\|0\.05' /c/ziko-platform/plugins/sleep/src/screens/SleepPlugin.tsx` retourne des matchs (4 ratios)
6. Pas de fixture Stretching : `grep 'STRETCHING_DATA\|SLEEP_DATA' /c/ziko-platform/plugins/stretching/src/screens/StretchingPlugin.tsx /c/ziko-platform/plugins/sleep/src/screens/SleepPlugin.tsx` retourne vide
7. Anciens dashboards supprimés : `test -f /c/ziko-platform/plugins/stretching/src/screens/StretchingDashboard.tsx` et `test -f /c/ziko-platform/plugins/sleep/src/screens/SleepDashboard.tsx` échouent
8. Zéro référence résiduelles : `grep -r 'StretchingDashboard\|SleepDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l` retourne 0
</verification>

<success_criteria>
- StretchingPlugin affiche les routines depuis stretching_routines avec icônes colorées, bibliothèque statique de 6 exercices, et historique avec MiniBars 7 jours depuis stretching_logs
- SleepPlugin affiche la durée de la nuit dernière en violet 36px, la barre de phases heuristique (4 segments flex proportionnel à duration_hours), le graphique 7 jours MiniBars, et les réglages statiques
- AISuggestion Sleep utilise la règle quality (< 3 → conseil caféine, >= 4 → encouragement)
- Zéro donnée fixture dans les deux plugins
- StretchingDashboard.tsx et SleepDashboard.tsx supprimés avec zéro import résiduel
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-02-SUMMARY.md` une fois terminé.
</output>
