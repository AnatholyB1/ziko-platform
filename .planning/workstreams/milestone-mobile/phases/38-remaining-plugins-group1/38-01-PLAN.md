---
plan: 38-01
phase: 38
wave: 1
depends_on: []
autonomous: true
files_modified:
  - plugins/stats/src/screens/StatsPlugin.tsx
  - plugins/stats/src/index.ts
  - apps/mobile/app/(app)/(plugins)/stats/dashboard.tsx
  - plugins/gamification/src/screens/GamificationPlugin.tsx
  - plugins/gamification/src/index.ts
  - apps/mobile/app/(app)/(plugins)/gamification/dashboard.tsx
requirements:
  - PLUG-STA-01
  - PLUG-STA-02
  - PLUG-STA-03
  - PLUG-GAM-01
  - PLUG-GAM-02
  - PLUG-GAM-03
  - PLUG-GAM-04
must_haves:
  goal: Stats et Gamification correspondent au mockup plugins-2.jsx avec SubTabs, AISuggestion, données réelles et suppression des anciens dashboards.
  truths:
    - StatsPlugin.tsx existe avec 3 SubTabs (Progrès / Volume / Records)
    - Onglet Progrès : MiniBars inline 4 semaines (volume), AISuggestion tendance, grille 2×2 StatTiles (Séances/Volume/Temps/PR battus)
    - Onglet Volume : barres horizontales % par groupe musculaire depuis session_sets, AISuggestion
    - Onglet Records : RowList depuis session_sets GROUP BY exercise_id avec max weight_kg, icône trophy
    - GamificationPlugin.tsx existe avec 3 SubTabs (Niveau / Badges / Quêtes)
    - Onglet Niveau : carte dark #1C1A17, niveau 56px primary, barre XP (xp / level×500), AISuggestion, RowList niveaux suivants
    - Onglet Badges : compteur débloqués/total, grille 2 colonnes, gradient si got=true, opacity 0.45 si locked
    - Onglet Quêtes : AISuggestion, 3 cartes quêtes statiques avec progress bar + chip reward
    - StatsDashboard.tsx et GamificationDashboard.tsx supprimés avec zéro import restant
    - Route wrappers importent StatsPlugin et GamificationPlugin
  artifacts:
    - path: plugins/stats/src/screens/StatsPlugin.tsx
      provides: Stats plugin entrypoint avec 3 SubTabs et données réelles
      exports: [StatsPlugin]
    - path: plugins/gamification/src/screens/GamificationPlugin.tsx
      provides: Gamification plugin entrypoint avec 3 SubTabs
      exports: [GamificationPlugin]
  key_links:
    - from: apps/mobile/app/(app)/(plugins)/stats/dashboard.tsx
      to: plugins/stats/src/screens/StatsPlugin.tsx
      via: "@ziko/plugin-stats/screens/StatsPlugin import"
    - from: StatsPlugin.tsx (onglet Records)
      to: session_sets + exercises
      via: "TanStack Query useQuery JOIN sur exercise_id GROUP BY pour max weight_kg"
    - from: GamificationPlugin.tsx (onglet Niveau)
      to: user_gamification
      via: "TanStack Query useQuery sur user_gamification WHERE user_id"
---

<objective>
Livrer le redesign complet des plugins Stats et Gamification selon le mockup canonique `plugins-2.jsx`. Chaque plugin devient un fichier `<PluginName>Plugin.tsx` unique avec SubTabs interne, AISuggestion, et données réelles via TanStack Query. Les anciens dashboards sont supprimés après vérification zéro import.

Purpose: Stats et Gamification sont les deux premiers plugins du groupe 1 (phase 38). Ils s'exécutent en wave 1, sans dépendances amont. Stats utilise `workout_sessions` + `session_sets` pour les graphiques de volume et les records personnels. Gamification utilise `user_gamification` pour la carte de niveau et les badges depuis `shop_items`.

Output: StatsPlugin.tsx, GamificationPlugin.tsx, route wrappers mis à jour, index.ts mis à jour, anciens dashboards supprimés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/RESEARCH.md

<interfaces>
<!-- Interfaces vérifiées depuis RESEARCH.md + CONTEXT.md -->

SubTabs (packages/ui/src/components/SubTabs.tsx — pill style depuis Phase 37) :
```typescript
interface SubTabsProps {
  tabs: string[];        // tableau de strings — PAS { id, label }
  active: string;        // string correspondant à un élément de tabs
  onChange: (tab: string) => void;
}
// Usage : const TABS = ['Progrès', 'Volume', 'Records']; activeTab === 'Progrès'
```

AISuggestion (packages/ui/src/components/AISuggestion.tsx — label "COACH IA · SUGGESTION" depuis Phase 37) :
```typescript
interface AISuggestionProps {
  text: string;
  actionLabel?: string;
  onAction?: () => void;
  tintColor?: string;   // défaut: theme.primary (#FF5C1A)
}
```

PluginHeader (packages/ui/src/components/PluginHeader.tsx) :
```typescript
interface PluginHeaderProps {
  title: string;
  onBack: () => void;
  right?: React.ReactNode;
  dark?: boolean;
}
```

Pattern entrypoint Phase 37 :
```typescript
export default function XPlugin({ supabase }: { supabase: any }) {
  const theme = useThemeStore((s) => s.theme);
  const router = useRouter();
  const [activeTab, setActiveTab] = useState(TABS[0]);
  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: theme.background }}>
      <PluginHeader title="Nom" onBack={() => router.back()} />
      <ScrollView
        style={{ flex: 1 }}
        contentContainerStyle={{ padding: 18, paddingBottom: 100 }}
        showsVerticalScrollIndicator={false}
      >
        <SubTabs tabs={TABS} active={activeTab} onChange={setActiveTab} />
        {activeTab === TABS[0] && <Tab1 />}
        {activeTab === TABS[1] && <Tab2 />}
      </ScrollView>
    </SafeAreaView>
  );
}
```

MiniBars local (D-02, D-12 — pas de composant partagé) :
```typescript
// height: 80 fixe sur le conteneur parent — pas de % sur View enfant sans hauteur parent définie
function MiniBars({ data, color, max }: { data: Array<{ l: string; v: number; today?: boolean }>; color: string; max?: number }) {
  const m = max ?? Math.max(...data.map((d) => d.v), 1);
  return (
    <View style={{ flexDirection: 'row', alignItems: 'flex-end', gap: 6, height: 80 }}>
      {data.map((d, i) => (
        <View key={i} style={{ flex: 1, alignItems: 'center', justifyContent: 'flex-end', height: '100%', gap: 5 }}>
          <View style={{
            width: '100%',
            height: Math.max((d.v / m) * 80, 4),  // valeurs absolues, pas de %
            backgroundColor: d.today ? color : color + '47',
            borderRadius: 5,
          }} />
          <Text style={{ fontSize: 9.5, fontWeight: '700', color: d.today ? color : '#6B6963' }}>{d.l}</Text>
        </View>
      ))}
    </View>
  );
}
```

Tables DB — Stats :
- workout_sessions : id, user_id, name, started_at, ended_at, total_volume_kg, notes
- session_sets : id, session_id, exercise_id, reps, weight_kg
- exercises : id, name, category, muscle_groups[]

Tables DB — Gamification (migration 007) :
- user_gamification : user_id UUID PK, xp INT, level INT, coins INT, current_streak INT, longest_streak INT, equipped_title TEXT, equipped_badge TEXT
- shop_items : id UUID, name TEXT, category TEXT ('title'|'badge'|'theme'), price INT, icon TEXT, level_required INT
- user_inventory : user_id UUID, item_id UUID, is_equipped BOOL — pour déterminer badges unlockés

Tokens design :
- background: #F7F6F3, surface: #FFFFFF, border: #E2E0DA
- primary: #FF5C1A (= var(--warn) du mockup pour Stats), text: #1C1A17, muted: #6B6963
- Card shadow : shadowColor '#1C1A17', shadowOffset {0,4}, shadowOpacity 0.08, shadowRadius 12, elevation 3
- paddingBottom: 100 sur tout ScrollView (CLAUDE.md)

Barrel exports à mettre à jour :
- plugins/stats/src/index.ts : ajouter `export { default as StatsPlugin } from './screens/StatsPlugin';`
- plugins/gamification/src/index.ts : ajouter `export { default as GamificationPlugin } from './screens/GamificationPlugin';`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construire StatsPlugin.tsx — 3 SubTabs avec données réelles</name>
  <files>plugins/stats/src/screens/StatsPlugin.tsx</files>
  <read_first>
    - plugins/stats/src/screens/StatsDashboard.tsx — lire l'implémentation existante (patterns Supabase, queries, types utilisés)
    - plugins/stats/src/store.ts — lire les fonctions fetchAllStats, types disponibles
    - C:/Users/Anatholy/Downloads/ziko/plugins-2.jsx lignes 57–119 (StatsPlugin : Progrès/Volume/Records)
  </read_first>
  <behavior>
    - useQuery(['workout_sessions_month', userId]) : fetch workout_sessions WHERE user_id ET started_at >= 30 jours; agrège séances count, volume total (SUM total_volume_kg), durée totale estimée
    - useQuery(['volume_4weeks', userId]) : dernières 4 semaines, volume par semaine pour MiniBars
    - useQuery(['muscle_volume_month', userId]) : session_sets JOIN exercises WHERE session_id IN (sessions du mois), GROUP BY exercise.category/muscle_groups, SUM weight_kg*reps → répartition %
    - useQuery(['personal_records', userId]) : session_sets JOIN exercises WHERE session_id IN sessions user, GROUP BY exercise_id, exercise_name, reps, MAX(weight_kg); ORDER BY max_weight DESC; limit 10
    - AISuggestion onglet Progrès : comparer volume semaine courante vs semaine précédente; si +5% → "Belle progression ! Volume +X% cette semaine." sinon → "Ton volume est stable — essaie d'augmenter les charges sur 1 exercice."
    - AISuggestion onglet Volume : identifier le groupe musculaire le moins entraîné (pct min); tip ciblé
    - StatTile grid 2×2 : Séances (count ce mois), Volume total (SUM en kg arrondi à la tonne si >1000), Temps (durée estimée = séances × 50min), PR battus (count max_weight records this month > all-time avant ce mois)
  </behavior>
  <action>
    Créer plugins/stats/src/screens/StatsPlugin.tsx comme export default function StatsPlugin({ supabase }: { supabase: any }).

    Imports : SafeAreaView from 'react-native-safe-area-context'; View, Text, ScrollView, TouchableOpacity from 'react-native'; useRouter from 'expo-router'; useState from 'react'; SubTabs, AISuggestion, PluginHeader from '@ziko/ui'; useThemeStore from '@ziko/plugin-sdk'; useQuery from '@tanstack/react-query'; Ionicons from '@expo/vector-icons'.

    Déclarer MiniBars localement (voir pattern dans interfaces ci-dessus — hauteur absolue Math.max(v/m*80, 4), pas de %).

    Déclarer StatTile localement : View card (surface, borderRadius 16, borderWidth 1, borderColor border, shadow, padding 12), Text label (10px uppercase muted fontWeight 700 letterSpacing 0.06), Text value (22px fontWeight 700 couleur paramétrable), Text sub optionnel (10.5px muted).

    Déclarer RowList localement : map items → View card (padding 12, flexDirection row, gap 12), icône optionnelle 34×34 borderRadius 10 fond coloré 14% opacité, Text title (12.5px bold), Text sub (10.5px muted), Text right optionnel.

    Tabs : const TABS = ['Progrès', 'Volume', 'Records'].

    ONGLET PROGRÈS (activeTab === 'Progrès') :
    - Card "Volume soulevé · 4 semaines" : MiniBars data=[{l:'S1',v:vol_s1},{l:'S2',v:vol_s2},{l:'S3',v:vol_s3},{l:'S4',v:vol_s4,today:true}] color='#FF5C1A'; label progression en dessous (texte vert si +, muted sinon)
    - AISuggestion avec règle volume semaine courante vs précédente
    - Grille 2×2 StatTiles : Séances (couleur primary), Volume total (couleur text), Temps estimé (couleur text), PR battus (couleur success '#2E9E5B')

    ONGLET VOLUME (activeTab === 'Volume') :
    - Card "Répartition par groupe (mois)" : liste de groupes musculaires avec barres horizontales; chaque item : label 11.5px bold + pct muted à droite, puis View hauteur 6 borderRadius 999 fond rgba(28,26,23,0.06), View enfant width= (pct/100*screenWidth calculé) height 100% background primary borderRadius 999
    - AISuggestion : identifier groupe sous-stimulé (<10%)
    - Si pas de données : empty state "Commence tes séances pour voir ta répartition ici"

    ONGLET RECORDS (activeTab === 'Records') :
    - RowList items depuis personal_records query : icon='trophy', title=exercise_name, sub=`${reps} rep · il y a X j`, right=`${max_weight}kg`, rightColor=primary
    - Icône trophy : Ionicons name="trophy-outline" size=15 dans View fond 14% primary
    - Si vide : Text "Aucun record encore — commence à t'entraîner !"

    États loading : ActivityIndicator centré (color primary). États error : Text "Impossible de charger les données" + bouton "Réessayer".
    showAlert de '@ziko/plugin-sdk' si besoin d'alertes.
    paddingBottom: 100 sur contentContainerStyle.
    Card shadow pattern : shadowColor '#1C1A17', shadowOffset {width:0, height:4}, shadowOpacity 0.08, shadowRadius 12, elevation 3.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "stats/src/screens/StatsPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - StatsPlugin.tsx exporte une fonction default nommée StatsPlugin
    - Fichier contient useState avec 'Progrès' comme valeur initiale
    - Fichier contient 'SubTabs' et 'AISuggestion' importés de '@ziko/ui'
    - Fichier contient useQuery pour workout_sessions et session_sets
    - Fichier contient MiniBars déclaré localement (pas importé)
    - Fichier ne contient PAS de constante STATS_DATA ou VOLUME_DATA fixture
    - Onglet Records contient 'trophy' (icône Ionicons pour les records)
    - TypeScript : zéro erreur sur ce fichier
  </acceptance_criteria>
  <done>StatsPlugin.tsx créé avec 3 SubTabs (Progrès/Volume/Records), MiniBars local, StatTile local, RowList local, données réelles TanStack Query, AISuggestion avec règle de tendance; TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Construire GamificationPlugin.tsx — 3 SubTabs avec XP réel et badges</name>
  <files>plugins/gamification/src/screens/GamificationPlugin.tsx</files>
  <read_first>
    - plugins/gamification/src/screens/GamificationDashboard.tsx — lire l'implémentation existante pour patterns (NE PAS reprendre creditStore, MotiView, gift modal — absents du mockup)
    - plugins/gamification/src/store.ts — lire useGamificationStore, types disponibles
    - C:/Users/Anatholy/Downloads/ziko/plugins-2.jsx lignes 121–208 (GamificationPlugin : Niveau/Badges/Quêtes)
  </read_first>
  <behavior>
    - useQuery(['user_gamification', userId]) : fetch user_gamification WHERE user_id = userId → {xp, level, coins, current_streak, longest_streak, equipped_title}
    - Seuil niveau suivant : level × 500 XP (D-07; ex. level 12 → prochain à 6000 XP)
    - XP progress : xp / (level × 500) × 100% → width de la barre progress
    - useQuery(['badges', userId]) : fetch shop_items WHERE category = 'badge'; fetch user_inventory WHERE user_id = userId; croiser pour déterminer got = item_id IN inventory
    - Badges : 8 max (ou tous si >8); got → gradient primary; locked → opacity 0.45 fond rgba(text,0.08)
    - Quêtes (D-06) : 3 cartes statiques hardcodées avec progress dérivé de current_streak et xp; pas de table quests
    - AISuggestion onglet Niveau : "X quêtes en cours — termine-les pour gagner +Y XP et passer plus vite niv. {level+1}."
    - AISuggestion onglet Quêtes : "Choisis 3 quêtes max simultanément. Vise les +50 XP en priorité."
  </behavior>
  <action>
    Créer plugins/gamification/src/screens/GamificationPlugin.tsx comme export default function GamificationPlugin({ supabase }: { supabase: any }).

    Imports : SafeAreaView, ScrollView, View, Text, TouchableOpacity from react-native + react-native-safe-area-context; useState from 'react'; useRouter from 'expo-router'; SubTabs, AISuggestion, PluginHeader from '@ziko/ui'; useThemeStore from '@ziko/plugin-sdk'; useQuery from '@tanstack/react-query'; Ionicons from '@expo/vector-icons'.

    NE PAS importer : moti, creditStore, MotiView — ces éléments ne sont pas dans le mockup (D-05, A3).

    Tabs : const TABS = ['Niveau', 'Badges', 'Quêtes'].

    ONGLET NIVEAU (activeTab === 'Niveau') :
    - Carte hero dark : backgroundColor '#1C1A17', borderRadius 20, padding 24, overflow 'hidden'
    - Gradient radial simulé : View absolute inset 0 avec backgroundColor rgba(255,92,26,0.18) + borderRadius (pas de react-native-linear-gradient — utiliser View semi-transparent positionnée en absolu)
    - Numéro niveau : Text 56px fontWeight '900' color '#FF5C1A' textAlign 'center' (fontFamily h-display = fontWeight '700')
    - Titre : Text 12px fontWeight '700' color 'rgba(255,250,246,0.8)' textTransform 'uppercase' letterSpacing 0.08 marginTop 4 textAlign 'center' — "{equipped_title || 'ATHLÈTE'}" 
    - XP : Text 11px color 'rgba(255,250,246,0.55)' marginTop 16 textAlign 'center' — "{xp} / {level * 500} XP"
    - Barre XP : View height 8 backgroundColor 'rgba(255,250,246,0.12)' borderRadius 999 marginTop 8 overflow 'hidden'; View enfant width={(xp/(level*500)*100)+'%'} height '100%' backgroundColor '#FF5C1A' borderRadius 999
    - XP restant : Text 10px color 'rgba(255,250,246,0.55)' marginTop 6 textAlign 'center'
    - AISuggestion sous la carte (tintColor par défaut)
    - RowList niveaux suivants : 3 items [{icon:'trophy-outline', title:`Niveau ${level+1} — Pro`, sub:`${(level+1)*500 - xp} XP restants`}, {icon:'trophy-outline', title:`Niveau ${level+2} — Élite`, sub:'Déblocage : avatar doré'}, {icon:'trophy-outline', title:`Niveau ${level+3} — Légende`, sub:'Déblocage : titre exclusif'}]

    ONGLET BADGES (activeTab === 'Badges') :
    - Text compteur : "{unlockedCount} / {totalCount} débloqués" fontSize 12 color muted marginBottom 8
    - Grille 2 colonnes : View flexWrap 'wrap' flexDirection 'row' gap 8; chaque carte : View width='48%' backgroundColor surface borderRadius 16 padding 12 alignItems 'center' opacity={b.got ? 1 : 0.45} + shadow
    - Cercle badge 52×52 : borderRadius 26, fond si got → backgroundColor '#FF5C1A' (gradient simulé), si locked → backgroundColor 'rgba(28,26,23,0.08)'
    - Ionicons dans le cercle : size 22, color si got '#fff' sinon muted
    - Text badge name : fontSize 12 fontWeight '700' marginTop 8
    - Text badge desc : fontSize 10 color muted marginTop 1

    Map icônes badge → Ionicons : play → 'play-outline', check → 'checkmark-outline', flame → 'flame-outline', trophy → 'trophy-outline', shoe → 'footsteps-outline', barbell → 'barbell-outline', leaf → 'leaf-outline', drop → 'water-outline'.

    ONGLET QUÊTES (activeTab === 'Quêtes') :
    - AISuggestion en premier
    - 3 quêtes statiques (D-06 — pas de table quests) avec progress dérivé :
      * Quête 1 : "Battre un PR cette semaine", reward "+50 XP", progress 60% (hardcodé ou dérivé de xp récent)
      * Quête 2 : "5 séances en 7 jours", reward "+30 XP", progress 80%
      * Quête 3 : "3 jours hydratation parfaite", reward "+15 XP", progress 67%
    - Carte quête : View card padding 12; row haut : Text title 13px bold flex:1 + Text reward chip (backgroundColor 'rgba(255,92,26,0.12)' borderRadius 999 paddingHorizontal 8 paddingVertical 2 fontSize 11 color primary fontWeight '700')
    - Text sub : 11px color muted marginTop 1
    - Barre progress : View height 5 backgroundColor 'rgba(28,26,23,0.06)' borderRadius 999 marginTop 9; View enfant width={pct+'%'} height '100%' backgroundColor primary borderRadius 999

    Card shadow sur toutes les cartes : shadowColor '#1C1A17', shadowOffset {width:0,height:4}, shadowOpacity 0.08, shadowRadius 12, elevation 3.
    paddingBottom: 100 sur contentContainerStyle.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "gamification/src/screens/GamificationPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - GamificationPlugin.tsx exporte une fonction default nommée GamificationPlugin
    - Fichier contient useState avec 'Niveau' comme valeur initiale
    - Fichier contient useQuery pour user_gamification
    - Fichier contient backgroundColor '#1C1A17' (carte hero dark)
    - Fichier contient la logique de seuil niveau (level * 500)
    - Fichier NE contient PAS d'import 'moti' ou 'creditStore'
    - Fichier NE contient PAS de constante GAMIFICATION_DATA fixture
    - Onglet Quêtes est présent avec quêtes statiques (D-06)
    - TypeScript : zéro erreur sur ce fichier
  </acceptance_criteria>
  <done>GamificationPlugin.tsx créé avec 3 SubTabs (Niveau/Badges/Quêtes), carte dark avec barre XP réelle, badges depuis shop_items + user_inventory, quêtes statiques avec progress; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Câbler les route wrappers, mettre à jour les barrels, supprimer les anciens dashboards</name>
  <files>
    apps/mobile/app/(app)/(plugins)/stats/dashboard.tsx,
    apps/mobile/app/(app)/(plugins)/gamification/dashboard.tsx,
    plugins/stats/src/index.ts,
    plugins/gamification/src/index.ts
  </files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/stats/dashboard.tsx — lire l'import actuel (StatsDashboard)
    - apps/mobile/app/(app)/(plugins)/gamification/dashboard.tsx — lire l'import actuel (GamificationDashboard)
    - plugins/stats/src/index.ts — lire tous les exports
    - plugins/gamification/src/index.ts — lire tous les exports
  </read_first>
  <action>
    STATS :
    Étape 1 — Route wrapper stats : changer l'import de 'StatsDashboard' à 'StatsPlugin' via '@ziko/plugin-stats/screens/StatsPlugin'. Garder le corps du composant wrapper inchangé (il rend juste le composant importé avec la prop supabase).

    Étape 2 — Barrel stats : dans plugins/stats/src/index.ts, remplacer l'export de StatsDashboard par `export { default as StatsPlugin } from './screens/StatsPlugin';`. Conserver tous les autres exports (ExerciseStats, SessionDetail, useStatsStore, fetchAllStats, etc.).

    Étape 3 — Suppression stats après vérification : lancer grep -r 'StatsDashboard' dans /c/ziko-platform/apps et /c/ziko-platform/plugins/stats/src (hors node_modules et .worktrees). Si count = 0, supprimer plugins/stats/src/screens/StatsDashboard.tsx. Si des références restent, les corriger d'abord.

    GAMIFICATION :
    Étape 4 — Route wrapper gamification : changer l'import de 'GamificationDashboard' à 'GamificationPlugin' via '@ziko/plugin-gamification/screens/GamificationPlugin'. Garder le corps du composant inchangé.

    Étape 5 — Barrel gamification : dans plugins/gamification/src/index.ts, remplacer l'export de GamificationDashboard par `export { default as GamificationPlugin } from './screens/GamificationPlugin';`. Conserver ShopScreen, useGamificationStore, awardWorkoutXP, awardHabitXP.

    Étape 6 — Suppression gamification après vérification : grep -r 'GamificationDashboard' dans /c/ziko-platform/apps et /c/ziko-platform/plugins/gamification/src. Si count = 0, supprimer plugins/gamification/src/screens/GamificationDashboard.tsx.

    Commande grep de vérification :
    grep -r 'StatsDashboard\|GamificationDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/stats/src /c/ziko-platform/plugins/gamification/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees
  </action>
  <verify>
    <automated>grep -r 'StatsDashboard\|GamificationDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/stats/src /c/ziko-platform/plugins/gamification/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(plugins)/stats/dashboard.tsx importe 'StatsPlugin' (pas 'StatsDashboard')
    - apps/mobile/app/(app)/(plugins)/gamification/dashboard.tsx importe 'GamificationPlugin' (pas 'GamificationDashboard')
    - plugins/stats/src/index.ts exporte StatsPlugin (pas StatsDashboard)
    - plugins/gamification/src/index.ts exporte GamificationPlugin (pas GamificationDashboard)
    - grep 'StatsDashboard|GamificationDashboard' dans apps/ et plugins/*/src/ retourne 0 ligne
    - plugins/stats/src/screens/StatsDashboard.tsx N'EXISTE PAS (supprimé)
    - plugins/gamification/src/screens/GamificationDashboard.tsx N'EXISTE PAS (supprimé)
    - TypeScript : cd /c/ziko-platform && rtk tsc --noEmit retourne 0 erreur
  </acceptance_criteria>
  <done>Route wrappers mis à jour, barrels mis à jour, anciens dashboards supprimés avec zéro import restant confirmé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Supabase | workout_sessions + session_sets lus avec user_id scopé; user_gamification scopé user_id; RLS double-enforces |
| GamificationPlugin → user_inventory | Lecture seule pour déterminer badges débloqués; pas d'écriture dans ce plugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Information Disclosure | workout_sessions query | mitigate | Toutes les queries incluent .eq('user_id', userId) depuis session auth; RLS enforce auth.uid() = user_id |
| T-38-01-02 | Information Disclosure | user_gamification query | mitigate | Query scopée .eq('user_id', userId); RLS sur user_gamification enforce ownership |
| T-38-01-03 | Information Disclosure | session_sets + exercises JOIN | mitigate | session_sets filtrées via session_id IN (sessions du user); exercises est une table publique read-only |
| T-38-01-04 | Tampering | Quêtes statiques hardcodées | accept | Pas d'écriture DB — données de démonstration uniquement; aucun risque d'injection |
| T-38-01-SC | Tampering | npm installs | mitigate | Aucun nouveau paquet — toutes les dépendances déjà installées (@ziko/ui, @tanstack/react-query, expo-router) |
</threat_model>

<verification>
1. TypeScript clean : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'` retourne 0
2. StatsPlugin créé : `grep 'export default function StatsPlugin' /c/ziko-platform/plugins/stats/src/screens/StatsPlugin.tsx` retourne un match
3. GamificationPlugin créé : `grep 'export default function GamificationPlugin' /c/ziko-platform/plugins/gamification/src/screens/GamificationPlugin.tsx` retourne un match
4. Pas de fixture Stats : `grep 'STATS_DATA\|VOLUME_DATA\|GAMIFICATION_DATA' /c/ziko-platform/plugins/stats/src/screens/StatsPlugin.tsx /c/ziko-platform/plugins/gamification/src/screens/GamificationPlugin.tsx` retourne vide
5. Ancien StatsDashboard supprimé : `test -f /c/ziko-platform/plugins/stats/src/screens/StatsDashboard.tsx` échoue (exit 1)
6. Ancien GamificationDashboard supprimé : `test -f /c/ziko-platform/plugins/gamification/src/screens/GamificationDashboard.tsx` échoue (exit 1)
7. Zéro référence aux anciens dashboards : `grep -r 'StatsDashboard\|GamificationDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l` retourne 0
8. Carte dark Gamification : `grep '#1C1A17' /c/ziko-platform/plugins/gamification/src/screens/GamificationPlugin.tsx` retourne un match (backgroundColor carte hero)
9. Seuil XP présent : `grep 'level \* 500\|level\*500' /c/ziko-platform/plugins/gamification/src/screens/GamificationPlugin.tsx` retourne un match
</verification>

<success_criteria>
- StatsPlugin affiche MiniBars 4 semaines (volume réel de workout_sessions), grille 2×2 StatTiles, barres % groupes musculaires, RowList records personnels — toutes les données viennent de TanStack Query
- GamificationPlugin affiche la carte hero dark avec barre XP réelle (xp / level×500), grille badges (unlocked = gradient, locked = opacity 0.45), quêtes statiques avec progress bars
- Zéro donnée fixture dans les deux plugins
- Route wrappers pointent vers les nouveaux plugins
- StatsDashboard.tsx et GamificationDashboard.tsx supprimés avec zéro import résiduel
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-01-SUMMARY.md` une fois terminé.
</output>
