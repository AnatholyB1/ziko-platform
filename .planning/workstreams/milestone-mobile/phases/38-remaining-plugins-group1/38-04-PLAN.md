---
plan: 38-04
phase: 38
wave: 2
depends_on: [38-01, 38-02, 38-03]
autonomous: false
files_modified: []
requirements:
  - PLUG-STA-01
  - PLUG-STA-02
  - PLUG-STA-03
  - PLUG-GAM-01
  - PLUG-GAM-02
  - PLUG-GAM-03
  - PLUG-GAM-04
  - PLUG-STR-01
  - PLUG-STR-02
  - PLUG-STR-03
  - PLUG-STR-04
  - PLUG-SLP-01
  - PLUG-SLP-02
  - PLUG-SLP-03
  - PLUG-SLP-04
  - PLUG-MSR-01
  - PLUG-MSR-02
  - PLUG-MSR-03
  - PLUG-TMR-01
  - PLUG-TMR-02
  - PLUG-TMR-03
  - PLUG-TMR-04
must_haves:
  goal: Les 6 plugins redessinés en phase 38 passent toutes les vérifications automatiques et le smoke test visuel humain.
  truths:
    - TypeScript compile sans erreur (0 erreur TS) sur tout le monorepo
    - Aucun ancien nom de dashboard (StatsDashboard, GamificationDashboard, StretchingDashboard, SleepDashboard, MeasurementsDashboard, TimerDashboard) n'est référencé dans apps/ ou plugins/
    - Les 6 nouveaux fichiers Plugin existent et exportent chacun une fonction default
    - Les 6 anciens fichiers Dashboard n'existent plus
    - Chaque nouveau plugin contient SubTabs (aucun n'a d'onglet hardcodé)
    - Gamification utilise react-native-svg pour le radial ring — CORRECTION : d'après CONTEXT.md D-07, le XP ring est une barre de progression linéaire (pas de SVG radial) — vérifier que GamificationPlugin ne tente pas d'importer react-native-svg inutilement
    - Le Timer ne contient pas duration_minutes ni calories_burned dans l'insert workout_sessions
    - npm run mobile se lance sans crash sur les 6 écrans plugins
  artifacts:
    - path: plugins/stats/src/screens/StatsPlugin.tsx
      provides: Stats plugin entrypoint vérifié
    - path: plugins/gamification/src/screens/GamificationPlugin.tsx
      provides: Gamification plugin entrypoint vérifié
    - path: plugins/stretching/src/screens/StretchingPlugin.tsx
      provides: Stretching plugin entrypoint vérifié
    - path: plugins/sleep/src/screens/SleepPlugin.tsx
      provides: Sleep plugin entrypoint vérifié
    - path: plugins/measurements/src/screens/MeasurementsPlugin.tsx
      provides: Measurements plugin entrypoint vérifié
    - path: plugins/timer/src/screens/TimerPlugin.tsx
      provides: Timer plugin entrypoint vérifié
  key_links:
    - from: apps/mobile/app/(app)/(plugins)/*/dashboard.tsx (6 fichiers)
      to: plugins/*/src/screens/*Plugin.tsx (6 nouveaux)
      via: "Imports mis à jour — vérifiés par grep et TypeScript"
---

<objective>
Vérifier que les 6 plugins redessinés en phase 38 (Stats, Gamification, Stretching, Sleep, Measurements, Timer) sont corrects, complets et sans régressions. Cette phase de vérification exécute des checks automatisés puis demande un smoke test humain sur l'application mobile.

Purpose: Garantir que toutes les contraintes décisionnelles (D-04 heuristique sommeil, D-05 table user_gamification, D-06 quêtes statiques, D-07 XP bar linéaire, D-08 countdown, D-09 colonnes insert) sont bien respectées, qu'aucun ancien dashboard ne subsiste, et que l'app ne crashe pas sur les 6 écrans.

Output: Aucun fichier modifié — plan de vérification uniquement. Rapport de smoke test humain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-01-SUMMARY.md
@.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-02-SUMMARY.md
@.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vérifications automatisées — TypeScript + structure + contraintes décisionnelles</name>
  <files></files>
  <read_first>
    Aucun fichier à lire — toutes les vérifications sont des commandes shell.
  </read_first>
  <action>
    Exécuter les vérifications suivantes dans l'ordre. Pour chaque vérification en échec, signaler l'erreur précise et la corriger avant de continuer.

    CHECK 1 — TypeScript zéro erreur :
    ```
    cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'
    ```
    Résultat attendu : 0. Si non-zéro, lire les erreurs et les corriger.

    CHECK 2 — Les 6 nouveaux Plugin existent :
    ```
    for f in \
      /c/ziko-platform/plugins/stats/src/screens/StatsPlugin.tsx \
      /c/ziko-platform/plugins/gamification/src/screens/GamificationPlugin.tsx \
      /c/ziko-platform/plugins/stretching/src/screens/StretchingPlugin.tsx \
      /c/ziko-platform/plugins/sleep/src/screens/SleepPlugin.tsx \
      /c/ziko-platform/plugins/measurements/src/screens/MeasurementsPlugin.tsx \
      /c/ziko-platform/plugins/timer/src/screens/TimerPlugin.tsx; do
      test -f "$f" && echo "OK: $f" || echo "MANQUANT: $f"
    done
    ```
    Résultat attendu : 6 lignes "OK".

    CHECK 3 — Les 6 anciens Dashboard n'existent plus :
    ```
    for f in \
      /c/ziko-platform/plugins/stats/src/screens/StatsDashboard.tsx \
      /c/ziko-platform/plugins/gamification/src/screens/GamificationDashboard.tsx \
      /c/ziko-platform/plugins/stretching/src/screens/StretchingDashboard.tsx \
      /c/ziko-platform/plugins/sleep/src/screens/SleepDashboard.tsx \
      /c/ziko-platform/plugins/measurements/src/screens/MeasurementsDashboard.tsx \
      /c/ziko-platform/plugins/timer/src/screens/TimerDashboard.tsx; do
      test -f "$f" && echo "EXISTE ENCORE (erreur): $f" || echo "SUPPRIMÉ OK: $f"
    done
    ```
    Résultat attendu : 6 lignes "SUPPRIMÉ OK".

    CHECK 4 — Zéro référence aux anciens noms :
    ```
    grep -r 'StatsDashboard\|GamificationDashboard\|StretchingDashboard\|SleepDashboard\|MeasurementsDashboard\|TimerDashboard' \
      /c/ziko-platform/apps \
      /c/ziko-platform/plugins \
      --include='*.ts' --include='*.tsx' \
      | grep -v node_modules | grep -v .worktrees | wc -l
    ```
    Résultat attendu : 0. Si non-zéro, afficher les fichiers concernés et corriger.

    CHECK 5 — SubTabs présent dans les 5 plugins avec tabs (Stats, Stretching, Sleep, Measurements, Timer) :
    ```
    for f in \
      /c/ziko-platform/plugins/stats/src/screens/StatsPlugin.tsx \
      /c/ziko-platform/plugins/gamification/src/screens/GamificationPlugin.tsx \
      /c/ziko-platform/plugins/stretching/src/screens/StretchingPlugin.tsx \
      /c/ziko-platform/plugins/sleep/src/screens/SleepPlugin.tsx \
      /c/ziko-platform/plugins/measurements/src/screens/MeasurementsPlugin.tsx \
      /c/ziko-platform/plugins/timer/src/screens/TimerPlugin.tsx; do
      grep -l 'SubTabs' "$f" && echo "SubTabs OK" || echo "SubTabs MANQUANT: $f"
    done
    ```
    Résultat attendu : SubTabs présent dans les 6 fichiers.

    CHECK 6 — Pas de données fixture dans les 6 plugins :
    ```
    grep -r 'STATS_DATA\|GAMIFICATION_DATA\|STRETCHING_DATA\|SLEEP_DATA\|MEASUREMENTS_DATA\|TIMER_DATA' \
      /c/ziko-platform/plugins/stats/src/screens/StatsPlugin.tsx \
      /c/ziko-platform/plugins/gamification/src/screens/GamificationPlugin.tsx \
      /c/ziko-platform/plugins/stretching/src/screens/StretchingPlugin.tsx \
      /c/ziko-platform/plugins/sleep/src/screens/SleepPlugin.tsx \
      /c/ziko-platform/plugins/measurements/src/screens/MeasurementsPlugin.tsx \
      /c/ziko-platform/plugins/timer/src/screens/TimerPlugin.tsx
    ```
    Résultat attendu : aucune sortie (0 match).

    CHECK 7 — Contrainte D-09 Timer (pas de colonnes interdites dans insert) :
    ```
    grep -n 'duration_minutes\|calories_burned' /c/ziko-platform/plugins/timer/src/screens/TimerPlugin.tsx
    ```
    Résultat attendu : aucune sortie. Si trouvé, supprimer ces colonnes de l'insert workout_sessions.

    CHECK 8 — Contrainte D-04 Sleep (heuristique phases) :
    ```
    grep -c '0\.18\|0\.53\|0\.24\|0\.05' /c/ziko-platform/plugins/sleep/src/screens/SleepPlugin.tsx
    ```
    Résultat attendu : >= 1 (les 4 ratios sont présents).

    CHECK 9 — Contrainte D-08 Timer countdown (useRef + AppState) :
    ```
    grep -c 'intervalRef\|AppState\|setInterval' /c/ziko-platform/plugins/timer/src/screens/TimerPlugin.tsx
    ```
    Résultat attendu : >= 3 (les 3 patterns sont présents).

    CHECK 10 — Contrainte D-05 Gamification (table correcte) :
    ```
    grep 'user_gamification' /c/ziko-platform/plugins/gamification/src/screens/GamificationPlugin.tsx | head -5
    ```
    Résultat attendu : au moins une ligne contenant 'user_gamification' dans la query Supabase. Si 'user_xp' est présent à la place, corriger.

    Si TOUS les checks passent : reporter "TOUS LES CHECKS AUTOMATISÉS PASSENT — Prêt pour le smoke test humain."
    Si un check échoue : corriger le problème et relancer ce check avant de continuer.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript retourne 0 erreur TS
    - Les 6 nouveaux Plugin existent (test -f réussit pour chacun)
    - Les 6 anciens Dashboard n'existent plus (test -f échoue pour chacun)
    - grep des anciens noms retourne 0 résultat dans apps/ et plugins/
    - SubTabs présent dans les 6 Plugin
    - Zéro constante fixture dans les 6 Plugin
    - TimerPlugin ne contient pas duration_minutes ni calories_burned dans l'insert
    - SleepPlugin contient les 4 ratios heuristiques (0.18, 0.53, 0.24, 0.05)
    - TimerPlugin contient intervalRef + AppState + setInterval
    - GamificationPlugin query 'user_gamification' (pas 'user_xp')
  </acceptance_criteria>
  <done>Tous les checks automatisés passent — 0 erreur TS, 6 plugins créés, 6 dashboards supprimés, toutes contraintes décisionnelles respectées.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test humain — vérifier les 6 plugins visuellement</name>
  <what-built>
    Plans 38-01, 38-02, 38-03 ont livré 6 plugins redessinés selon le mockup plugins-2.jsx :
    - StatsPlugin : 3 tabs Progrès/Volume/Records, MiniBars volume, grille StatTiles, records PR
    - GamificationPlugin : 3 tabs Niveau/Badges/Quêtes, carte dark XP, grille badges
    - StretchingPlugin : 3 tabs Routines/Bibliothèque/Suivi, routines DB, MiniBars 7j
    - SleepPlugin : 3 tabs Cette nuit/Historique/Réglages, barre phases heuristique, durée violet
    - MeasurementsPlugin : 3 tabs Aujourd'hui/Évolution/Objectifs, carte poids delta, MiniBars
    - TimerPlugin : 3 tabs Séance/Tabata/Intervalle, countdown 72px dark, presets DB
  </what-built>
  <how-to-verify>
    1. Démarrer le serveur de développement :
       ```
       cd /c/ziko-platform && npm run mobile
       ```
       Ou depuis apps/mobile/ : `npx expo start`

    2. Ouvrir l'app sur iOS/Android simulator ou device physique.

    3. Naviguer vers chaque plugin et vérifier :

       STATS (plugin Stats dans le drawer ou tab plugins) :
       - [ ] Le plugin s'ouvre sans crash
       - [ ] 3 tabs "Progrès / Volume / Records" visibles en style pill
       - [ ] Tab Progrès : MiniBars visible (7 barres ou 4 semaines), StatTiles 2×2 en dessous, AISuggestion visible
       - [ ] Tab Volume : barres horizontales de répartition musculaire visibles
       - [ ] Tab Records : liste d'exercices avec poids max (ou état vide propre si pas de données)

       GAMIFICATION (plugin Récompenses) :
       - [ ] 3 tabs "Niveau / Badges / Quêtes" visibles
       - [ ] Tab Niveau : carte dark #1C1A17 avec numéro de niveau en orange 56px, barre XP, AISuggestion
       - [ ] Tab Badges : compteur "X / Y débloqués", grille 2 colonnes, badges locké en opacité réduite
       - [ ] Tab Quêtes : AISuggestion + 3 cartes quête avec barres de progression

       STRETCHING (plugin Mobilité) :
       - [ ] 3 tabs "Routines / Bibliothèque / Suivi" visibles
       - [ ] Tab Routines : AISuggestion en premier, liste des routines avec icônes colorées (ou état vide propre)
       - [ ] Tab Bibliothèque : 6 exercices statiques (Pigeon, Cobra, Couch stretch, etc.)
       - [ ] Tab Suivi : MiniBars 7 jours, compteur séances ce mois, AISuggestion

       SLEEP (plugin Sommeil) :
       - [ ] 3 tabs "Cette nuit / Historique / Réglages" visibles
       - [ ] Tab Cette nuit : durée en violet 36px (ex: "7h48"), barre de phases horizontale avec 4 segments colorés, légende 4 colonnes, AISuggestion violet
       - [ ] Si pas de données sleep_logs : état vide propre (pas de crash)
       - [ ] Tab Historique : MiniBars 7 nuits violet, moyenne affichée
       - [ ] Tab Réglages : 5 paramètres listés (non-interactifs)

       MEASUREMENTS (plugin Mesures) :
       - [ ] 3 tabs "Aujourd'hui / Évolution / Objectifs" visibles
       - [ ] Tab Aujourd'hui : carte poids avec chip delta, section Mensurations 5 items, CTA "Logger des mesures"
       - [ ] Taper "Logger des mesures" → Modal ou formulaire s'ouvre, saisie possible, bouton Enregistrer fonctionne
       - [ ] Tab Évolution : MiniBars 6 semaines vert, AISuggestion
       - [ ] Tab Objectifs : 3 objectifs listés avec valeurs

       TIMER (plugin Timer) :
       - [ ] 3 tabs "Séance / Tabata / Intervalle" visibles
       - [ ] Tab Séance : carte dark #1C1A17, countdown "01:30" en orange 72px, 3 boutons (-15s, play, +15s)
       - [ ] Taper play → countdown commence et décrémente chaque seconde
       - [ ] Taper -15s/-15s → ajuste le temps
       - [ ] Presets DB affichés en grille (ou presets builtin si aucun preset DB)
       - [ ] Tab Tabata : description 20s/10s/8 rounds, bouton Démarrer
       - [ ] Tab Intervalle : 4 lignes configurables, bouton Lancer
       - [ ] Après la fin d'un timer → "Sauvegarder comme séance" CTA visible

    4. Vérifier également qu'aucun des AUTRES plugins (Nutrition, Hydration, Habits, etc.) n'a régressé.

    5. Si un bug est trouvé : décrire précisément (plugin, tab, comportement observé vs attendu).
  </how-to-verify>
  <resume-signal>
    Taper "approved" si les 6 plugins s'affichent correctement sans crash.
    Taper "issues: [description]" pour signaler les problèmes trouvés.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vérification → codebase | Read-only — aucune écriture durant ce plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Tampering | Vérification grep (commentaires pourraient fausser les checks) | mitigate | Les checks grep ciblent des noms de constantes spécifiques (STATS_DATA, etc.) et des patterns de code réels; les commentaires avec ces noms exactement sont peu probables |
| T-38-04-SC | Tampering | npm installs | mitigate | Aucun nouveau paquet installé dans ce plan de vérification |
</threat_model>

<verification>
Ce plan est lui-même la vérification de phase — ses deux tâches constituent le protocole de vérification complet.

Récapitulatif des checks exécutés en Task 1 :
1. TypeScript : 0 erreur TS
2. Existence : 6 fichiers Plugin existent
3. Suppression : 6 fichiers Dashboard supprimés
4. Imports : 0 référence aux anciens noms
5. SubTabs : présent dans les 6 Plugin
6. Fixtures : 0 constante XXXX_DATA dans les 6 Plugin
7. D-09 Timer : duration_minutes et calories_burned absents de l'insert
8. D-04 Sleep : 4 ratios heuristiques présents (0.18, 0.53, 0.24, 0.05)
9. D-08 Timer : intervalRef + AppState + setInterval présents
10. D-05 Gamification : query sur 'user_gamification' (pas 'user_xp')

Smoke test humain (Task 2) : 6 plugins visuellement vérifiés sur device/simulator.
</verification>

<success_criteria>
- Tous les 10 checks automatisés passent sans exception
- Le smoke test humain confirme que les 6 plugins s'affichent sans crash, avec leurs tabs, leurs données réelles (ou états vides propres), et leurs AISuggestion
- Le countdown Timer fonctionne (décrémente chaque seconde, les boutons +15s/-15s répondent)
- La barre de phases Sleep est visible avec 4 segments colorés proportionnels
- La carte dark Gamification affiche le niveau et la barre XP
- Aucune régression sur les autres plugins (Nutrition, Habits, etc.)
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/38-remaining-plugins-group1/38-04-SUMMARY.md` une fois terminé.
</output>
