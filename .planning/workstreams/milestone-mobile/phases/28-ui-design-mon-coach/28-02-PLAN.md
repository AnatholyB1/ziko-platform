---
phase: 28-ui-design-mon-coach
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01-PLAN.md
files_modified:
  - .planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/28-CONTEXT.md
autonomous: false
requirements:
  - COACH-10

must_haves:
  truths:
    - "A Figma file named 'Ziko — Mon coach Plugin (Phase 28)' exists"
    - "The file contains a frame for State A (code entry)"
    - "The file contains a frame for State B (coach preview card)"
    - "The file contains a frame for State C (linked coach card)"
    - "The file contains a frame for ConfirmRevocationModal"
    - "The file contains a frame for the Settings section row (if linked)"
    - "All frames use #F7F6F3 background and #FF5C1A accent per UI-SPEC"
    - "The Figma file URL is recorded in 28-CONTEXT.md under ## Figma"
  artifacts:
    - path: ".planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/28-CONTEXT.md"
      provides: "Figma file URL recorded"
      contains: "figma.com"
  key_links:
    - from: "028-UI-SPEC.md"
      to: "Figma frames"
      via: "Design contract → Figma MCP generate_figma_design"
      pattern: "All color/shadow tokens match UI-SPEC"
---

<objective>
Generate Figma design frames for all 3 Mon coach screen states (A / B / C), the
ConfirmRevocationModal, and the Settings section row — precisely matching the approved
028-UI-SPEC.md design contract.

Purpose: Phase 28 SC1 — a Figma file must exist covering all states before Phase 29 begins.
Output: A Figma file URL recorded in 28-CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/ROADMAP.md
@.planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/028-UI-SPEC.md
@.planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/28-CONTEXT.md

<interfaces>
<!-- Design tokens (from approved 028-UI-SPEC.md) -->
Colors:
  background:    #F7F6F3
  surface:       #FFFFFF
  border:        #E2E0DA
  primary:       #FF5C1A   (accent, enabled CTA, "Retour à la saisie" text link)
  text:          #1C1A17
  muted:         #6B6963
  destructive:   #DC2626   (Retirer ce coach, modal confirm enabled state)
  error-bg:      #FEF2F2
  error-border:  #FECACA
  chip-bg:       #FFF4EF
  chip-text:     #FF5C1A
  disabled-bg:   #E2E0DA
  disabled-text: #6B6963

Shadows (applied to card surfaces):
  shadowColor:   #000000
  shadowOpacity: 0.08
  shadowRadius:  12
  elevation:     3

Typography (system font — iOS SF Pro / Android Roboto):
  screen-title:  20px / bold
  card-name:     18px / semibold
  body:          15px / regular
  label:         13px / regular
  caption:       12px / regular

Spacing scale (React Native points):
  xs=4  sm=8  md=16  lg=24  xl=32  tab-clearance=100

Touch targets: minimum 44 height for all tappable elements
Code input field height: 56
Coach avatar: 72×72 circular

Iconicons names:
  person-circle-outline   → avatar fallback
  shield-checkmark        → KYC verified badge
  calendar-outline        → "Lié depuis" date row
  trash-outline           → Retirer ce coach
  alert-circle-outline    → inline error icon
  chevron-forward         → Settings row trailing icon
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Figma file and generate State A — Code Entry</name>
  <files>.planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/28-CONTEXT.md</files>
  <action>
    Use the Figma MCP server to create a new file named "Ziko — Mon coach Plugin (Phase 28)".

    Generate the "State A — Code Entry" frame (375×812, mobile viewport):
    - Background: #F7F6F3
    - Screen title: "Mon coach" (20px bold, #1C1A17), centered or left-aligned per spec
    - Subtitle copy: "Entrez le code à 6 caractères fourni par votre coach." (15px, #6B6963)
    - Code input field:
      - Height 56, border 1.5px #E2E0DA (idle) / #FF5C1A (focused) / #DC2626 (error)
      - Placeholder: "Ex: AB3X7K" (muted, #6B6963, letter-spacing 4)
      - Font: 24px bold monospace, #1C1A17
    - Error state sub-frame: alert-circle-outline icon + "Code invalide ou expiré." on #FEF2F2 bg, #FECACA border
    - CTA button "Valider le code":
      - Enabled: #FF5C1A bg, #FFFFFF text, 16px semibold, borderRadius 12, height 52
      - Disabled: #E2E0DA bg, #6B6963 text
    - paddingBottom 100 noted as annotation

    Both CTA states (enabled + disabled) must appear — use variants or separate sub-frames.
  </action>
  <verify>
    <automated>MISSING — verify manually: open Figma file, confirm State A frame exists with input field (56h), CTA in both states, error container visible</automated>
  </verify>
  <done>State A frame exists in the Figma file with all sub-components per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Generate State B (coach preview), State C (linked coach), ConfirmRevocationModal, Settings row</name>
  <files>.planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/28-CONTEXT.md</files>
  <action>
    In the same Figma file, generate four additional frames.

    STATE B — Coach Preview Card (375×812):
    - Background: #F7F6F3
    - Card surface: #FFFFFF, borderRadius 16, shadow (opacity 0.08, radius 12, elev 3)
    - Coach avatar row: 72×72 circular image (person-circle-outline fallback), right of avatar:
      display name (18px semibold, #1C1A17), KYC badge (shield-checkmark, #FF5C1A, 14px "Coach certifié")
    - Specialty chips: row of pills, bg #FFF4EF, text #FF5C1A, 13px, 4px vertical / 8px horizontal padding, 6px radius, 8px gap
    - Bio: 15px regular #6B6963, max 3 lines
    - Divider: 1px #E2E0DA, full width
    - CTA row (side by side):
      - "Lier mon compte": #FF5C1A bg, #FFFFFF text, flex 1, height 52, borderRadius 12
      - "Retour à la saisie": text link only, #FF5C1A, 15px semibold, 44px min touch height

    STATE C — Linked Coach Card (375×812):
    - Same card surface as State B (avatar, name, KYC, chips, bio)
    - Additional metadata row: calendar-outline icon (#6B6963) + "Lié depuis [date]" (13px #6B6963)
    - "Retirer ce coach" button: trash-outline icon + label, #DC2626 text, transparent bg,
      border 1.5px #DC2626, borderRadius 12, height 52, full width

    CONFIRM REVOCATION MODAL (375×812 with overlay):
    - Semi-transparent overlay: rgba(0,0,0,0.5)
    - Modal card: #FFFFFF, borderRadius 20, shadow, padding 24
    - Title: "Retirer ce coach ?" (18px semibold, #1C1A17)
    - Body: "Cette action est irréversible. Tapez COACH pour confirmer." (15px #6B6963)
    - Text input: border 1.5px, placeholder "COACH", height 48, borderRadius 8
    - Button row:
      - "Annuler": #F7F6F3 bg, #1C1A17 text, flex 1, borderRadius 12
      - "Confirmer": disabled → #E2E0DA/#6B6963; enabled (after typing "COACH") → #DC2626/#FFFFFF

    SETTINGS ROW — Coach section entry (375×56):
    - Row height 56, bg #FFFFFF
    - Left: 72×72 avatar thumbnail (32×32 in settings context), display name (15px #1C1A17)
    - Right: chevron-forward icon, #6B6963
    - Bottom border: 1px #E2E0DA
  </action>
  <verify>
    <automated>MISSING — verify manually: all 4 frames (State B, State C, Modal, Settings row) present in Figma file</automated>
  </verify>
  <done>All 5 frames (A, B, C, Modal, Settings row) exist in the Figma file matching UI-SPEC tokens.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Figma file and record URL in 28-CONTEXT.md</name>
  <what-built>
    Figma file "Ziko — Mon coach Plugin (Phase 28)" with 5 frames generated from 028-UI-SPEC.md.
    If Figma MCP generation succeeded, the URL is available from the tool response.
    If Figma MCP was unavailable or failed, document that outcome and proceed with UI-SPEC as
    the sole design contract (SC1 fallback: note in CONTEXT.md that Figma generation was skipped
    with reason, and confirm SC1 is fulfilled via UI-SPEC per team agreement).
  </what-built>
  <how-to-verify>
    1. Open the Figma file URL returned by the MCP tools (or confirm fallback path)
    2. Confirm 5 frames are present: State A, State B, State C, ConfirmRevocationModal, Settings row
    3. Spot-check State A: input field height 56, CTA in both enabled/disabled states, error container
    4. Spot-check State C: calendar-outline icon, "Retirer ce coach" in #DC2626
    5. Confirm modal: disabled → enabled CTA state change when "COACH" typed
    6. If all good: update 28-CONTEXT.md — replace "TBD" under ## Figma with the actual file URL
  </how-to-verify>
  <resume-signal>
    Type "approved: [figma-url]" if Figma file is correct.
    Type "fallback: [reason]" if Figma generation failed and UI-SPEC is the sole contract.
    Type "issues: [description]" if frames need correction.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-SPEC → Figma MCP | Design contract translated into Figma frames via MCP tools |
| Figma file → Phase 29 executor | Mockup consumed as visual reference during implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Tampering | Figma file post-generation | accept | Figma file is read-only reference; source of truth remains 028-UI-SPEC.md in git |
| T-28-03 | Information Disclosure | Figma share link | accept | Design-only data, no PII or credentials in mockup frames |
</threat_model>

<verification>
- 28-CONTEXT.md `## Figma` section contains a figma.com URL (or documented fallback)
- All 5 frames exist in the file: State A, State B, State C, ConfirmRevocationModal, Settings row
- Token spot-check: `#FF5C1A` primary, `#DC2626` destructive, shadow opacity 0.08 visible on cards
</verification>

<success_criteria>
Phase 28 SC1 met: Figma file exists covering all 3 screen states (A/B/C), modal, and settings
row — all matching the approved UI-SPEC. File URL is recorded in 28-CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/28-02-SUMMARY.md`
</output>
