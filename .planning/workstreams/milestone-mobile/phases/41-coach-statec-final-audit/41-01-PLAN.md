---
plan: 41-01
phase: 41
status: complete
---

# Plan 41-01: Coach StateC Label Fix

## Goal
Update the i18n key `coach.state_c.progress_label` in both locale sections of `packages/plugin-sdk/src/i18n.ts` so the label accurately reflects the habits-adherence metric already rendered by CoachScreen.tsx.

## Tasks

### Task 1 — Update i18n labels in i18n.ts

File: `packages/plugin-sdk/src/i18n.ts`

The file has a single flat translation map with two language sections. Both entries must be updated.

**French section (around line 821):**
```
'coach.state_c.progress_label': 'Progression',
```
Change to:
```
'coach.state_c.progress_label': 'Habitudes aujourd\'hui',
```

**English section (around line 1645):**
```
'coach.state_c.progress_label': 'Progress',
```
Change to:
```
'coach.state_c.progress_label': 'Today\'s habits',
```

Do NOT modify any other key. Do NOT modify `CoachScreen.tsx` — the key reference `t('coach.state_c.progress_label')` at line 597 already uses this key correctly and requires no change.

### Task 2 — Verify the change

Run the verification checks listed in the Verification section below before committing.

## Commit
`feat(41-01): update coach state_c progress_label to habits today`

## Verification
1. `grep "progress_label" packages/plugin-sdk/src/i18n.ts` → must show exactly two lines: one with `'Habitudes aujourd'hui'` and one with `'Today's habits'`
2. `grep "progress_label" plugins/coach/src/screens/CoachScreen.tsx` → must still show `t('coach.state_c.progress_label')` (key reference unchanged)
3. `rtk tsc --noEmit` from repo root → 0 errors
