---
phase: 31-ai-tools-coach
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/src/tools/coach.ts
autonomous: true
requirements:
  - COACH-15

must_haves:
  truths:
    - "coach_get_link returns structured coach info (linked: true + coach_name + link_id + linked_at + kyc_verified + bio + specialties) when a link exists"
    - "coach_get_link returns { linked: false, message: ... } when no coach is linked"
    - "coach_revoke_link returns confirmation_required error when confirmed is false or missing"
    - "coach_revoke_link returns no_active_link error when confirmed: true but no active link exists"
    - "coach_revoke_link revokes the link by UUID and returns ok: true when confirmed: true and active link exists"
  artifacts:
    - path: "backend/api/src/tools/coach.ts"
      provides: "Two exported async tool functions: coach_get_link and coach_revoke_link"
      exports: ["coach_get_link", "coach_revoke_link"]
  key_links:
    - from: "backend/api/src/tools/coach.ts"
      to: "backend/api/src/coach/clients/db.ts"
      via: "named imports getActiveLink, revokeLink, createUserClient"
      pattern: "import.*getActiveLink.*revokeLink.*createUserClient.*from.*coach/clients/db"
---

<objective>
Create the coach tool implementation file with two exported async functions: `coach_get_link` and `coach_revoke_link`.

Purpose: Provide the Claude orchestrator with the ability to check an athlete's current coach link and revoke it on their behalf. The tool functions call existing Phase 25 DB helpers directly (same-process, no HTTP).

Output: `backend/api/src/tools/coach.ts` following the pattern of `habits.ts` — exported named functions, `(params, userId, userToken?) => Promise<unknown>` signatures, plain object returns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/ROADMAP.md
@.planning/workstreams/milestone-mobile/phases/31-ai-tools-coach/31-CONTEXT.md
</context>

<interfaces>
From backend/api/src/coach/clients/db.ts:

```typescript
export function createUserClient(jwt: string): SupabaseClient
// Returns Supabase client scoped to the athlete's JWT

export async function getActiveLink(
  jwt: string,
  clientId: string,
): Promise<{
  link: LinkRow | null;    // { id, coach_id, client_id, created_at }
  preview: CoachPreviewPayload | null;  // { coach_id, display_name, bio, specialties, photo_signed_url, kyc_status }
}>

export async function revokeLink(
  jwt: string,
  clientId: string,   // NOTE: parameter name is clientId (userId in the tool context)
  id: string,         // UUID of the link row
): Promise<{ id: string; revoked_at: string | null }>
```

From backend/api/src/tools/habits.ts (pattern reference):
```typescript
export async function habits_get_today(
  _params: Record<string, unknown>,
  userId: string,
  userToken?: string,
): Promise<unknown>
// Pattern: clientForUser(userToken) for habits — coach tools use createUserClient(userToken!) from db.ts
```

Note: `revokeLink` second param is `clientId` (same as `userId` in tool context). Always pass `userToken!` (non-null assertion safe because coach tools require JWT — they fail gracefully if missing).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement coach_get_link tool function</name>
  <files>backend/api/src/tools/coach.ts</files>
  <read_first>
    - backend/api/src/tools/habits.ts — gold standard pattern for tool file structure, error handling, and function signature
    - backend/api/src/coach/clients/db.ts — confirm getActiveLink signature and return shape (link.id, link.created_at, preview.display_name, preview.bio, preview.specialties, preview.kyc_status)
  </read_first>
  <behavior>
    - When userToken is falsy: return { linked: false, message: 'Authentication required.' }
    - When getActiveLink returns { link: null }: return { linked: false, message: 'No coach is currently linked. Enter a 6-character invitation code in the Mon coach plugin to connect.' }
    - When getActiveLink returns a link + preview: return { linked: true, link_id: link.id, coach_name: preview.display_name, linked_at: link.created_at, kyc_verified: preview.kyc_status === 'approved', bio: preview.bio ?? null, specialties: preview.specialties ?? [] }
    - getActiveLink throws: propagate the error (let the registry handle it)
  </behavior>
  <action>
    Create backend/api/src/tools/coach.ts. Import { getActiveLink, revokeLink, createUserClient } from '../coach/clients/db.js' (note .js extension for ESM).

    Implement coach_get_link as an exported async function with signature (params: Record<string, unknown>, userId: string, userToken?: string): Promise<unknown>.

    The function body: if !userToken return the auth-required object. Call getActiveLink(userToken, userId). If result.link is null, return the linked:false object per D-06. Otherwise return the linked:true object per D-05: map preview.kyc_status === 'approved' to the kyc_verified boolean (per specifics note), use preview.bio ?? null and preview.specialties ?? []. Do NOT include photo_url (per D-05 — signed URLs expire and are not useful for text AI responses).
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npx tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep -i "coach.ts" || echo "No errors in coach.ts"</automated>
  </verify>
  <done>
    backend/api/src/tools/coach.ts exists, exports coach_get_link, TypeScript compiles cleanly for coach.ts, function returns linked:false shape when no link and linked:true shape with correct field mapping when linked.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement coach_revoke_link tool function</name>
  <files>backend/api/src/tools/coach.ts</files>
  <read_first>
    - backend/api/src/tools/coach.ts — read the file as it exists after Task 1 before adding to it
    - backend/api/src/coach/clients/db.ts — confirm revokeLink(jwt, clientId, id) parameter order and return type
    - backend/api/src/tools/registry.ts lines 120-129 — nutrition_delete_entry pattern for "ask confirmation first" description
  </read_first>
  <behavior>
    - When params.confirmed is not strictly true (false, undefined, missing): return { ok: false, error: 'confirmation_required', message: 'Call again with confirmed: true after the user explicitly agrees to unlink their coach.' }
    - When params.confirmed === true and getActiveLink returns { link: null }: return { ok: false, error: 'no_active_link', message: 'No coach is currently linked.' }
    - When params.confirmed === true and link exists: call revokeLink(userToken!, userId, link.id), return { ok: true, link_id: link.id, revoked_at: result.revoked_at }
    - getActiveLink or revokeLink throws: propagate the error
  </behavior>
  <action>
    Append coach_revoke_link to backend/api/src/tools/coach.ts. Same function signature: (params: Record<string, unknown>, userId: string, userToken?: string): Promise<unknown>.

    Step 1: Cast params as { confirmed?: boolean }. If confirmed is not strictly true, return the confirmation_required error object per D-01.

    Step 2: Call getActiveLink(userToken!, userId). If result.link is null, return the no_active_link error object per specifics note.

    Step 3: Call revokeLink(userToken!, userId, result.link.id). Return { ok: true, link_id: result.link.id, revoked_at: revokeResult.revoked_at }.

    The function requires userToken — if it is falsy, return { ok: false, error: 'confirmation_required', message: 'Authentication required.' } (guards the !-assertion safely).
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npx tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep -i "coach.ts" || echo "No errors in coach.ts"</automated>
  </verify>
  <done>
    backend/api/src/tools/coach.ts exports both coach_get_link and coach_revoke_link. Full TypeScript compile passes with no errors in coach.ts. coach_revoke_link returns confirmation_required when confirmed !== true, returns no_active_link when confirmed:true but no link found, calls revokeLink with the correct (jwt, clientId, id) arg order.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator→tool function | Tool receives params from Claude orchestrator; params may be incomplete or malformed |
| tool→Supabase | JWT-authenticated Supabase calls; RLS enforces row ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | coach_revoke_link params | mitigate | confirmed must be strictly === true (not truthy); any other value returns confirmation_required without side effects |
| T-31-02 | Spoofing | getActiveLink(jwt, clientId) | mitigate | Supabase RLS + eq('client_id', clientId) + is('revoked_at', null) filter ensures only the calling user's link is visible |
| T-31-03 | Elevation of Privilege | revokeLink without userToken | mitigate | Guard at top of function: if !userToken return error object before any DB call |
</threat_model>

<verification>
1. `npx tsc --noEmit -p backend/api/tsconfig.json` passes with no errors
2. `backend/api/src/tools/coach.ts` exports `coach_get_link` and `coach_revoke_link`
3. Both functions follow the `(params, userId, userToken?) => Promise<unknown>` signature
4. Import path uses `'../coach/clients/db.js'` (ESM .js extension)
</verification>

<success_criteria>
- backend/api/src/tools/coach.ts exists with two exported named async functions
- coach_get_link returns the exact linked:true / linked:false shapes from D-05 and D-06
- coach_revoke_link enforces the confirmed:true gate per D-01 through D-04
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/31-ai-tools-coach/31-01-SUMMARY.md` when done
</output>
