---
plan: 39-02
phase: 39
wave: 1
depends_on: []
autonomous: true
files_modified:
  - plugins/supplements/src/screens/SupplementsPlugin.tsx
  - plugins/supplements/src/index.ts
  - apps/mobile/app/(app)/(plugins)/supplements/list.tsx
  - plugins/wearables/src/screens/WearablesPlugin.tsx
  - plugins/wearables/src/index.ts
  - apps/mobile/app/(app)/(plugins)/wearables/dashboard.tsx
requirements:
  - PLUG-SUP-01
  - PLUG-SUP-02
  - PLUG-WER-01
  - PLUG-WER-02
  - PLUG-WER-03
must_haves:
  goal: Supplements et Wearables correspondent au mockup plugins-2.jsx avec SubTabs, AISuggestion, données réelles et suppression des anciens dashboards.
  truths:
    - SupplementsPlugin.tsx existe avec 3 SubTabs (Aujourd'hui / Mon stack / Rappels)
    - Onglet Aujourd'hui — compteur X/Y pris, barre progression, checklist depuis supplement_prices + MMKV état local, AISuggestion vert
    - Onglet Mon stack — search input + RowList depuis supplements JOIN supplement_prices avec prix (PLUG-SUP-01)
    - Onglet Rappels — liste statique de rappels avec horaires
    - SupplementsListScreen.tsx supprimé avec zéro import restant; SupplementDetailScreen + SupplementCompareScreen INTACTS
    - WearablesPlugin.tsx existe avec 2 SubTabs (Aujourd'hui / Connexion)
    - Onglet Aujourd'hui — 4 metric cards (pas/fréquence cardiaque/calories/sommeil) depuis wearable_daily_summary, AISuggestion
    - Onglet Connexion — état connexion depuis health_sync_log, CTA Connect/Disconnect Apple Health / Health Connect
    - WearablesDashboard.tsx supprimé avec zéro import restant
  artifacts:
    - path: plugins/supplements/src/screens/SupplementsPlugin.tsx
      provides: Supplements plugin entrypoint avec 3 SubTabs
      exports: [SupplementsPlugin]
    - path: plugins/wearables/src/screens/WearablesPlugin.tsx
      provides: Wearables plugin entrypoint avec 2 SubTabs
      exports: [WearablesPlugin]
  key_links:
    - from: SupplementsPlugin.tsx (Mon stack)
      to: supplements JOIN supplement_prices
      via: "TanStack Query useQuery avec .select('*, supplement_prices(*)') ORDER BY name"
    - from: WearablesPlugin.tsx (Aujourd'hui)
      to: wearable_daily_summary
      via: "TanStack Query useQuery WHERE user_id AND date = today"
---

<objective>
Livrer le redesign complet des plugins Supplements et Wearables selon le mockup canonique `plugins-2.jsx` (lignes 633–779). Supplements : daily tracker checklist + search prix (PLUG-SUP-01) + rappels statiques. Wearables : health summary depuis wearable_daily_summary + état connexion Apple Health / Health Connect. Anciens dashboards supprimés.

Purpose: Wave 1, sans dépendances amont. Supplements lit supplements + supplement_prices pour la comparaison prix. Wearables lit wearable_daily_summary pour le résumé santé.

Output: SupplementsPlugin.tsx, WearablesPlugin.tsx, route wrappers mis à jour, index.ts mis à jour, anciens dashboards supprimés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-CONTEXT.md

<interfaces>
SubTabs, AISuggestion, PluginHeader — même interfaces que plans précédents (voir 39-CONTEXT.md).

Tables DB — Supplements (migration 018) :
- supplements : id UUID, name TEXT, brand_id UUID, category_id UUID
- supplement_brands : id UUID, name TEXT
- supplement_categories : id UUID, name TEXT
- supplement_prices : id UUID, supplement_id UUID, source TEXT, price_eur NUMERIC, per_serving_eur NUMERIC

Tables DB — Wearables (migration 014) :
- wearable_daily_summary : user_id UUID, date DATE, steps INT, calories_burned INT, active_minutes INT, heart_rate_avg INT, sleep_hours NUMERIC, last_synced_at TIMESTAMPTZ
- health_sync_log : user_id UUID, platform TEXT ('apple_health'|'health_connect'), last_sync_at TIMESTAMPTZ, status TEXT

Tokens design :
- supplements accent : #2E9E5B (success green — D-11)
- wearables accent : #2E7BF6 (info blue — per mockup)
- standard : background #F7F6F3, surface #FFFFFF, border #E2E0DA, text #1C1A17, muted #6B6963
- Card shadow : shadowColor '#1C1A17', shadowOffset {0,4}, shadowOpacity 0.08, shadowRadius 12, elevation 3
- paddingBottom: 100

MMKV pour la checklist daily supplements :
- Clé : `supplements_daily_${userId}_${dateStr}` (dateStr = 'YYYY-MM-DD')
- Format : JSON.stringify(Record<string, boolean>) — {supplementId: taken}
- Utiliser `import { storage } from '@ziko/plugin-sdk'` si disponible, sinon `import { MMKV } from 'react-native-mmkv'; const store = new MMKV();`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construire SupplementsPlugin.tsx — 3 SubTabs avec checklist MMKV et search prix</name>
  <files>plugins/supplements/src/screens/SupplementsPlugin.tsx</files>
  <read_first>
    - plugins/supplements/src/screens/SupplementsListScreen.tsx — lire l'implémentation existante (queries, types)
    - plugins/supplements/src/screens/SupplementCompareScreen.tsx — lire le pattern de query supplement_prices
    - C:/Users/Anatholy/Downloads/ziko/plugins-2.jsx lignes 633–710 (SupplementsPlugin)
  </read_first>
  <behavior>
    - Onglet Aujourd'hui : useQuery(['supplements_list', userId]) → supplements pour construire la checklist; état pris/non-pris dans MMKV keyed par date; affiche compteur X/Y + barre progression + liste checkboxes; AISuggestion tintColor='#2E9E5B'
    - Onglet Mon stack : TextInput search (query string local) + useQuery(['supplements_search', query]) → supplements JOIN supplement_prices WHERE name ILIKE '%{query}%'; RowList items avec title=name, sub=source + ' · ' + price_eur.toFixed(2) + '€', right=per_serving_eur + '€/dose', rightColor='#2E9E5B'
    - Onglet Rappels : liste statique de 4 rappels (Matin 8h / Avant séance / Midi / Soir 21h) avec toggle switch (état local)
  </behavior>
  <action>
    Créer plugins/supplements/src/screens/SupplementsPlugin.tsx comme export default function SupplementsPlugin({ supabase }: { supabase: any }).

    Tabs : const TABS = ['Aujourd\'hui', 'Mon stack', 'Rappels'].

    ONGLET AUJOURD'HUI :
    - Charger liste supplements via useQuery; charger état MMKV avec useMemo (key = supplements_daily_{userId}_{today})
    - Compteur "X/Y pris aujourd'hui" Text 22px fontWeight '700'; barre progression View height 6 bg 'rgba(28,26,23,.06)' borderRadius 999; View enfant width={`${(taken/total)*100}%`} bg '#2E9E5B'
    - AISuggestion tintColor='#2E9E5B' en dessous du compteur
    - Liste checkboxes : supplements.map → View card row; TouchableOpacity 32×32 borderRadius 16 (cercle) : si taken → bg '#2E9E5B' avec Ionicons checkmark-outline 14 blanc; sinon → bg transparent border 2px '#2E9E5B'; Text name 12.5px bold; Text dose+timing 10.5px muted
    - onPress checkbox → toggle MMKV key et setState local

    ONGLET MON STACK :
    - TextInput search : View card padding 12 row; Ionicons search-outline 16 muted; TextInput placeholder "Rechercher un complément…" flex:1 fontSize 13
    - Délai debounce 300ms (useRef setTimeout) sur query string
    - RowList depuis useQuery; si vide → "Aucun résultat — essaie 'créatine', 'whey', 'vitamine'"
    - Chaque item : title=supplement.name, sub=price_source + ' · ' + price_eur + '€', right=per_serving_eur + '€/dose', rightColor='#2E9E5B'
    - Icône Ionicons 'nutrition-outline' size 16 dans view 34×34 borderRadius 10 fond rgba(46,158,91,0.14) color '#2E9E5B'

    ONGLET RAPPELS :
    - 4 cartes statiques : [{time:'8h00', label:'Matin', items:['Vitamine D3','Oméga-3']}, {time:'Avant séance', label:'Pré-entraînement', items:['Caféine','Créatine']}, {time:'13h00', label:'Midi', items:['Magnésium']}, {time:'21h00', label:'Soir', items:['Magnésium bisglycinate']}]
    - Chaque carte : View card row; Ionicons alarm-outline 18 '#2E9E5B'; Text time 13px bold flex:1; Switch value={reminderEnabled[i]} onValueChange → setState; Text items.join(', ') 11px muted marginTop 2

    paddingBottom: 100. Card shadow standard.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "supplements/src/screens/SupplementsPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - SupplementsPlugin.tsx exporte une fonction default nommée SupplementsPlugin
    - Fichier contient 'Mon stack' comme tab label (PLUG-SUP-01 prix)
    - Fichier contient 'supplement_prices' dans la query Mon stack
    - Fichier contient '#2E9E5B' (accent vert Supplements)
    - Fichier contient AISuggestion importé de '@ziko/ui'
    - Fichier NE contient PAS de fixture SUPPLEMENT_STACK hardcodé sans query
    - TypeScript : zéro erreur sur ce fichier
  </acceptance_criteria>
  <done>SupplementsPlugin.tsx créé avec Aujourd'hui (checklist MMKV + barre progression), Mon stack (search + prix supplement_prices), Rappels statiques, AISuggestion vert; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Construire WearablesPlugin.tsx — 2 SubTabs health summary + connexion</name>
  <files>plugins/wearables/src/screens/WearablesPlugin.tsx</files>
  <read_first>
    - plugins/wearables/src/screens/WearablesDashboard.tsx — lire l'implémentation existante (patterns, types)
    - C:/Users/Anatholy/Downloads/ziko/plugins-2.jsx lignes 711–779 (WearablesPlugin)
  </read_first>
  <behavior>
    - Onglet Aujourd'hui : useQuery → wearable_daily_summary WHERE user_id AND date = today(); 4 metric cards (steps, heart_rate_avg, calories_burned, sleep_hours); si pas de données → empty state "Synchronise ton Apple Health ou Health Connect"; AISuggestion tintColor='#2E7BF6'
    - Onglet Connexion : useQuery → health_sync_log WHERE user_id; affiche platform + last_sync_at; CTA "Connecter Apple Health" / "Connecter Health Connect" — display-only (D-14, pas de SDK natif ici)
  </behavior>
  <action>
    Créer plugins/wearables/src/screens/WearablesPlugin.tsx comme export default function WearablesPlugin({ supabase }: { supabase: any }).

    Tabs : const TABS = ['Aujourd\'hui', 'Connexion'].

    ONGLET AUJOURD'HUI :
    - Si loading → ActivityIndicator centré color='#2E7BF6'
    - Si data.length === 0 ou data null → empty state : View centré, Ionicons 'watch-outline' 48 muted, Text "Aucune donnée pour aujourd'hui" 16px bold, Text "Synchronise ton appareil" 13px muted, AISuggestion tintColor='#2E7BF6' text="Connecte ton Apple Health ou Health Connect pour suivre tes données santé automatiquement."
    - Si data → 4 cards métriques en grille 2×2 :
      * Steps : Ionicons 'footsteps-outline', valeur steps.toLocaleString(), label 'Pas aujourd\'hui', color '#2E7BF6'
      * Fréquence cardiaque : Ionicons 'heart-outline', valeur heart_rate_avg + ' bpm', label 'FC moy.', color '#E94B3C'
      * Calories : Ionicons 'flame-outline', valeur calories_burned + ' kcal', label 'Calories brûlées', color '#F59E0B'
      * Sommeil : Ionicons 'moon-outline', valeur sleep_hours.toFixed(1) + 'h', label 'Sommeil', color '#7B5BD0'
    - Chaque card : View card borderRadius 16 padding 16 borderWidth 1 border shadow; View 36×36 borderRadius 10 fond 14% opacité icon; Text valeur 22px fontWeight '700'; Text label 11px muted marginTop 2
    - AISuggestion tintColor='#2E7BF6' en bas (règle : si steps < 5000 → "Seulement X pas aujourd'hui. Un tour de 20 min = +2000 pas." sinon → "Belle activité ! Tes X pas d'aujourd'hui contribuent à ta santé cardiaque.")

    ONGLET CONNEXION :
    - useQuery → health_sync_log WHERE user_id; 
    - Si connecté (health_sync_log.length > 0) : card par plateforme; Ionicons 'checkmark-circle' 20 '#2E9E5B'; Text platform_display; Text 'Dernière sync : ' + format(last_sync_at, 'dd/MM HH:mm'); Text 'Déconnecter' 12px color '#E94B3C' onPress → showAlert('Déconnecter', 'Confirmer ?', [{text:'Annuler'},{text:'Déconnecter', style:'destructive', onPress:()=>{}}])
    - Si non connecté : 2 CTA cards (Apple Health card : iOS only check, View card row Ionicons 'phone-portrait-outline' fond rgba(46,123,246,0.12) color '#2E7BF6'; Text 'Apple Health' bold; Text 'iOS uniquement' 11px muted; Ionicons 'chevron-forward' 16 muted) + (Health Connect card : même style Ionicons 'logo-android')
    - Note sous les CTAs : Text 12px muted "La connexion native nécessite l'app mobile avec permissions activées."

    paddingBottom: 100. Card shadow standard.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "wearables/src/screens/WearablesPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - WearablesPlugin.tsx exporte une fonction default nommée WearablesPlugin
    - Fichier contient 'wearable_daily_summary' dans la query Aujourd'hui
    - Fichier contient 'health_sync_log' dans la query Connexion
    - Fichier contient '#2E7BF6' (accent bleu Wearables)
    - Fichier contient AISuggestion avec tintColor='#2E7BF6'
    - Fichier NE contient PAS de fixture WEARABLES_DATA
    - TypeScript : zéro erreur sur ce fichier
  </acceptance_criteria>
  <done>WearablesPlugin.tsx créé avec Aujourd'hui (4 metric cards depuis wearable_daily_summary + empty state) et Connexion (health_sync_log status + CTAs display-only); TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Câbler route wrappers, barrels, supprimer anciens dashboards (Supplements + Wearables)</name>
  <files>
    apps/mobile/app/(app)/(plugins)/supplements/list.tsx,
    apps/mobile/app/(app)/(plugins)/wearables/dashboard.tsx,
    plugins/supplements/src/index.ts,
    plugins/wearables/src/index.ts
  </files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/supplements/list.tsx — lire l'import actuel
    - apps/mobile/app/(app)/(plugins)/wearables/dashboard.tsx — lire l'import actuel
    - plugins/supplements/src/index.ts — lire tous les exports
    - plugins/wearables/src/index.ts — lire tous les exports
  </read_first>
  <action>
    SUPPLEMENTS :
    Étape 1 — Route wrapper list.tsx : changer l'import de 'SupplementsListScreen' à 'SupplementsPlugin' via '@ziko/plugin-supplements/screens/SupplementsPlugin'. Corps inchangé.
    Étape 2 — Barrel : remplacer l'export SupplementsListScreen par `export { default as SupplementsPlugin } from './screens/SupplementsPlugin';`. Conserver SupplementDetailScreen, SupplementCompareScreen et autres exports.
    Étape 3 — Suppression : grep SupplementsListScreen dans apps/ et plugins/supplements/src/. Si count = 0 → supprimer plugins/supplements/src/screens/SupplementsListScreen.tsx. NE PAS toucher SupplementDetailScreen.tsx ni SupplementCompareScreen.tsx.

    WEARABLES :
    Étape 4 — Route wrapper : changer import 'WearablesDashboard' → 'WearablesPlugin' via '@ziko/plugin-wearables/screens/WearablesPlugin'.
    Étape 5 — Barrel : remplacer export WearablesDashboard par `export { default as WearablesPlugin } from './screens/WearablesPlugin';`.
    Étape 6 — Suppression : grep WearablesDashboard dans apps/ et plugins/wearables/src/. Si count = 0 → supprimer plugins/wearables/src/screens/WearablesDashboard.tsx.

    Vérification : grep -r 'SupplementsListScreen\|WearablesDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/supplements/src /c/ziko-platform/plugins/wearables/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l
  </action>
  <verify>
    <automated>grep -r 'SupplementsListScreen\|WearablesDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/supplements/src /c/ziko-platform/plugins/wearables/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(plugins)/supplements/list.tsx importe 'SupplementsPlugin'
    - apps/mobile/app/(app)/(plugins)/wearables/dashboard.tsx importe 'WearablesPlugin'
    - grep 'SupplementsListScreen|WearablesDashboard' retourne 0 ligne
    - SupplementsListScreen.tsx N'EXISTE PAS; SupplementDetailScreen.tsx et SupplementCompareScreen.tsx EXISTENT
    - WearablesDashboard.tsx N'EXISTE PAS
    - TypeScript : rtk tsc --noEmit retourne 0 erreur
  </acceptance_criteria>
  <done>Route wrappers mis à jour, barrels mis à jour, SupplementsListScreen et WearablesDashboard supprimés; SupplementDetail et SupplementCompare intacts; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-39-02-01 | Info Disclosure | supplement_prices query | mitigate | Données publiques — prices ne sont pas user-specific; pas de RLS nécessaire mais query scopée par supplement_id |
| T-39-02-02 | Info Disclosure | wearable_daily_summary | mitigate | Query .eq('user_id', userId) + RLS enforce ownership |
| T-39-02-03 | Tampering | MMKV supplement checklist | accept | Données locales uniquement, pas de sync DB — impossible de tamper autre utilisateur |
| T-39-02-04 | Info Disclosure | health_sync_log | mitigate | Lecture seule, scopée user_id, RLS enforce |
</threat_model>

<verification>
1. TypeScript clean : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'` retourne 0
2. SupplementsPlugin créé : `grep 'export default function SupplementsPlugin' /c/ziko-platform/plugins/supplements/src/screens/SupplementsPlugin.tsx` retourne match
3. WearablesPlugin créé : `grep 'export default function WearablesPlugin' /c/ziko-platform/plugins/wearables/src/screens/WearablesPlugin.tsx` retourne match
4. Prix présent : `grep 'supplement_prices' /c/ziko-platform/plugins/supplements/src/screens/SupplementsPlugin.tsx` retourne match (PLUG-SUP-01)
5. Wearable summary : `grep 'wearable_daily_summary' /c/ziko-platform/plugins/wearables/src/screens/WearablesPlugin.tsx` retourne match
6. SupplementsListScreen supprimé : `test -f /c/ziko-platform/plugins/supplements/src/screens/SupplementsListScreen.tsx` échoue
7. SupplementCompareScreen intact : `test -f /c/ziko-platform/plugins/supplements/src/screens/SupplementCompareScreen.tsx` réussit
8. WearablesDashboard supprimé : `test -f /c/ziko-platform/plugins/wearables/src/screens/WearablesDashboard.tsx` échoue
</verification>

<success_criteria>
- SupplementsPlugin affiche checklist MMKV daily tracker + search prix depuis supplement_prices + rappels statiques
- WearablesPlugin affiche 4 metric cards depuis wearable_daily_summary + statut connexion health_sync_log
- Zéro fixture dans les deux plugins
- Anciens dashboards supprimés; SupplementDetail, SupplementCompare intacts
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-02-SUMMARY.md` une fois terminé.
</output>
