---
plan: 39-04
phase: 39
wave: 2
depends_on: [39-01, 39-02, 39-03]
autonomous: true
files_modified: []
requirements:
  - PLUG-JNL-01
  - PLUG-JNL-02
  - PLUG-JNL-03
  - PLUG-CRD-01
  - PLUG-CRD-02
  - PLUG-CRD-03
  - PLUG-CRD-04
  - PLUG-SUP-01
  - PLUG-SUP-02
  - PLUG-WER-01
  - PLUG-WER-02
  - PLUG-WER-03
  - PLUG-RPE-01
  - PLUG-RPE-02
  - PLUG-RPE-03
  - PLUG-PAN-01
  - PLUG-PAN-02
  - PLUG-PAN-03
  - PLUG-PAN-04
must_haves:
  goal: Les 6 plugins Phase 39 sont conformes au mockup, TypeScript clean, zéro fixture, zéro ancien dashboard résiduel.
  truths:
    - TypeScript compile sans erreur (rtk tsc --noEmit retourne 0)
    - Les 6 nouveaux fichiers Plugin.tsx existent avec le bon export default
    - Zéro référence aux anciens dashboards dans apps/ et plugins/*/src/
    - Les fichiers satellites (CardioTracker, SupplementDetail, ShoppingList, etc.) existent toujours
    - JournalPlugin contient '#7B5BD0' (accent violet)
    - CardioPlugin contient '#E94B3C' (accent rouge) et les 6 activités dont 'Hyrox'
    - SupplementsPlugin contient 'supplement_prices' dans une query
    - WearablesPlugin contient 'wearable_daily_summary'
    - RPEPlugin contient calc1RM et TRAINING_ZONES (PAS de SubTabs)
    - PantryPlugin contient les 3 catégories frigo/congelateur/placard et AISuggestion
  artifacts: []
  key_links: []
---

<objective>
Vérification complète de la Phase 39. Exécuter tous les checks automatisés pour confirmer : TypeScript clean, nouveaux plugins créés, anciens dashboards supprimés, fichiers satellites intacts, accents couleurs corrects, données réelles (pas de fixture).

Purpose: Plan de vérification wave 2. Bloqué sur les plans 39-01/02/03. Confirme que la phase atteint son goal avant de marquer Complete.

Output: Rapport de vérification. Créer 39-04-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vérification TypeScript et existence fichiers</name>
  <files></files>
  <read_first>
    - .planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-01-SUMMARY.md
    - .planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-02-SUMMARY.md
    - .planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-03-SUMMARY.md
  </read_first>
  <action>
    Exécuter les vérifications suivantes dans l'ordre. Reporter PASS/FAIL pour chacune.

    1. TypeScript clean :
    `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'` → doit retourner 0

    2. Nouveaux plugins existent :
    ```
    test -f plugins/journal/src/screens/JournalPlugin.tsx && echo PASS || echo FAIL
    test -f plugins/cardio/src/screens/CardioPlugin.tsx && echo PASS || echo FAIL
    test -f plugins/supplements/src/screens/SupplementsPlugin.tsx && echo PASS || echo FAIL
    test -f plugins/wearables/src/screens/WearablesPlugin.tsx && echo PASS || echo FAIL
    test -f plugins/rpe/src/screens/RPEPlugin.tsx && echo PASS || echo FAIL
    test -f plugins/pantry/src/screens/PantryPlugin.tsx && echo PASS || echo FAIL
    ```

    3. Anciens dashboards supprimés :
    ```
    test ! -f plugins/journal/src/screens/JournalDashboard.tsx && echo PASS || echo FAIL
    test ! -f plugins/cardio/src/screens/CardioDashboard.tsx && echo PASS || echo FAIL
    test ! -f plugins/supplements/src/screens/SupplementsListScreen.tsx && echo PASS || echo FAIL
    test ! -f plugins/wearables/src/screens/WearablesDashboard.tsx && echo PASS || echo FAIL
    test ! -f plugins/rpe/src/screens/RPECalculatorScreen.tsx && echo PASS || echo FAIL
    test ! -f plugins/pantry/src/screens/PantryDashboard.tsx && echo PASS || echo FAIL
    ```

    4. Satellites intacts :
    ```
    test -f plugins/cardio/src/screens/CardioTracker.tsx && echo PASS || echo FAIL
    test -f plugins/supplements/src/screens/SupplementCompareScreen.tsx && echo PASS || echo FAIL
    test -f plugins/pantry/src/screens/ShoppingList.tsx && echo PASS || echo FAIL
    test -f plugins/pantry/src/screens/BarcodeScanner.tsx && echo PASS || echo FAIL
    ```

    5. Zéro référence aux anciens dashboards :
    `grep -r 'JournalDashboard\|CardioDashboard\|SupplementsListScreen\|WearablesDashboard\|RPECalculatorScreen\|PantryDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l` → doit retourner 0

    6. Accents couleurs :
    ```
    grep -l '#7B5BD0' plugins/journal/src/screens/JournalPlugin.tsx && echo PASS || echo FAIL
    grep -l '#E94B3C' plugins/cardio/src/screens/CardioPlugin.tsx && echo PASS || echo FAIL
    grep -l 'Hyrox' plugins/cardio/src/screens/CardioPlugin.tsx && echo PASS || echo FAIL
    grep -l 'supplement_prices' plugins/supplements/src/screens/SupplementsPlugin.tsx && echo PASS || echo FAIL
    grep -l 'wearable_daily_summary' plugins/wearables/src/screens/WearablesPlugin.tsx && echo PASS || echo FAIL
    grep -l 'calc1RM' plugins/rpe/src/screens/RPEPlugin.tsx && echo PASS || echo FAIL
    grep -l 'TRAINING_ZONES' plugins/rpe/src/screens/RPEPlugin.tsx && echo PASS || echo FAIL
    grep -l 'congelateur' plugins/pantry/src/screens/PantryPlugin.tsx && echo PASS || echo FAIL
    ```

    7. Zéro fixture :
    `grep -r 'JOURNAL_DATA\|CARDIO_DATA\|SUPPLEMENT_DATA\|WEARABLES_DATA\|RPE_DATA\|PANTRY_DATA' plugins/journal/src/screens/JournalPlugin.tsx plugins/cardio/src/screens/CardioPlugin.tsx plugins/supplements/src/screens/SupplementsPlugin.tsx plugins/wearables/src/screens/WearablesPlugin.tsx plugins/rpe/src/screens/RPEPlugin.tsx plugins/pantry/src/screens/PantryPlugin.tsx 2>/dev/null | wc -l` → doit retourner 0

    Si tous les checks passent → reporter "## VERIFICATION PASSED".
    Si un check échoue → reporter "## ISSUES FOUND" avec la liste des checks FAIL et les commandes de fix.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'</automated>
  </verify>
  <acceptance_criteria>
    - Tous les checks ci-dessus retournent PASS
    - TypeScript : 0 erreur TS
    - grep anciens dashboards retourne 0
    - grep fixtures retourne 0
  </acceptance_criteria>
  <done>Tous les checks Phase 39 passent — 6 plugins créés, 6 anciens dashboards supprimés, satellites intacts, TypeScript clean, zéro fixture.</done>
</task>

</tasks>

<threat_model>
## Notes
Phase de vérification uniquement — aucune modification de code. Pas de nouveau vecteur de menace introduit.
</threat_model>

<verification>
Voir Task 1 — tous les checks automatisés sont la vérification.
</verification>

<success_criteria>
- TypeScript compile sans erreur sur tout le monorepo
- Les 6 nouveaux Plugin.tsx existent et exportent le bon composant
- Les 6 anciens dashboards sont supprimés sans référence résiduelle
- Les fichiers satellites (CardioTracker, SupplementCompare, ShoppingList, BarcodeScanner) existent toujours
- Zéro fixture constant dans les nouveaux plugins
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-04-SUMMARY.md` une fois terminé.
</output>
