---
plan: 39-03
phase: 39
wave: 1
depends_on: []
autonomous: true
files_modified:
  - plugins/rpe/src/screens/RPEPlugin.tsx
  - plugins/rpe/src/store.ts
  - plugins/rpe/src/index.ts
  - apps/mobile/app/(app)/(plugins)/rpe/index.tsx
  - plugins/pantry/src/screens/PantryPlugin.tsx
  - plugins/pantry/src/index.ts
  - apps/mobile/app/(app)/(plugins)/pantry/dashboard.tsx
requirements:
  - PLUG-RPE-01
  - PLUG-RPE-02
  - PLUG-RPE-03
  - PLUG-PAN-01
  - PLUG-PAN-02
  - PLUG-PAN-03
  - PLUG-PAN-04
must_haves:
  goal: RPE et Pantry correspondent au mockup plugins-2.jsx avec données réelles/persistées et suppression des anciens dashboards.
  truths:
    - RPEPlugin.tsx existe — écran single (pas de SubTabs) avec carte dark 1RM grande, 3 rangées inputs (charge/reps/RPE), table zones % (TRAINING_ZONES)
    - calc1RM et rpeToPercent importés de plugins/rpe/src/index.ts (D-17)
    - Dernières valeurs (weight, reps, rpe) persistées dans Zustand store (D-16)
    - RPECalculatorScreen.tsx supprimé avec zéro import restant
    - PantryPlugin.tsx existe avec 3 SubTabs (Inventaire / Recettes / Shopping)
    - Onglet Inventaire — 3 tiles catégorie (Frigo/Congel/Placard) avec item counts depuis pantry_items, AISuggestion
    - Onglet Recettes — RowList rule-based match% (count pantry_items matching ingredients / total)
    - Onglet Shopping — checklist depuis shopping_list table ou MMKV persistée
    - PantryDashboard.tsx supprimé; BarcodeScanner/PantryItemForm/RecipeDetail/PantryRecipes/ShoppingList/RecipeConfirm INTACTS
  artifacts:
    - path: plugins/rpe/src/screens/RPEPlugin.tsx
      provides: RPE calculator redesigné avec calc1RM et persistence Zustand
      exports: [RPEPlugin]
    - path: plugins/pantry/src/screens/PantryPlugin.tsx
      provides: Pantry plugin entrypoint avec 3 SubTabs
      exports: [PantryPlugin]
  key_links:
    - from: RPEPlugin.tsx
      to: plugins/rpe/src/index.ts
      via: "import { calc1RM, rpeToPercent, rpeToRIR, RPE_VALUES, TRAINING_ZONES }"
    - from: RPEPlugin.tsx
      to: plugins/rpe/src/store.ts
      via: "useRpeStore((s) => s.lastWeight/lastReps/lastRpe) + setLast()"
    - from: PantryPlugin.tsx (Inventaire)
      to: pantry_items table
      via: "TanStack Query GROUP BY category, COUNT(*)"
---

<objective>
Livrer le redesign complet des plugins RPE et Pantry. RPE : redesign visuel dark big-number de RPECalculatorScreen.tsx → RPEPlugin.tsx (single screen, pas de SubTabs, persistence Zustand). Pantry : nouvelle entrée PantryPlugin.tsx avec 3 SubTabs (catégories/recettes rule-based/shopping). Anciens dashboards supprimés.

Purpose: Wave 1, sans dépendances. RPE n'a pas de DB — utilise uniquement calc1RM + Zustand store. Pantry lit pantry_items pour les counts catégorie et implémente un match% rule-based pour les recettes.

Output: RPEPlugin.tsx, store.ts RPE mis à jour, PantryPlugin.tsx, route wrappers mis à jour, barrels mis à jour, anciens dashboards supprimés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-CONTEXT.md

<interfaces>
RPE logic (plugins/rpe/src/index.ts — déjà existant) :
```typescript
export function calc1RM(weight: number, reps: number, rpe: number): number
export function rpeToPercent(reps: number, rpe: number): number  // returns 0–1
export function rpeToRIR(rpe: number): number
export const RPE_VALUES: number[]  // [5, 5.5, 6, 6.5, 7, 7.5, 8, 8.5, 9, 9.5, 10]
export const TRAINING_ZONES: Array<{ label: string; min: number; max: number; color: string }>
```

RPE store (plugins/rpe/src/store.ts — à créer ou mettre à jour) :
```typescript
interface RPEStoreState {
  lastWeight: number;
  lastReps: number;
  lastRpe: number;
  setLast: (w: number, r: number, rpe: number) => void;
}
// create avec persist MMKV : { name: 'rpe-store' }
```

Pantry tables (à vérifier au moment de l'implémentation) :
- Si pantry_items existe : id, user_id, name TEXT, category TEXT ('frigo'|'congelateur'|'placard'), quantity NUMERIC, unit TEXT
- Si absent : afficher tiles avec count = 0 + empty state gracieux (PAN-01 tolérant)

Shopping list :
- Tenter useQuery → shopping_list WHERE user_id; si table absente (erreur) → fallback MMKV array

Tokens design :
- RPE accent : primary #FF5C1A (calculateur) + dark card #1C1A17 (1RM display)
- Pantry accent : #FF5C1A (primary)
- Standard tokens et card shadow identiques aux plans précédents
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer RPEPlugin.tsx — single screen dark big-number avec persistence Zustand</name>
  <files>
    plugins/rpe/src/screens/RPEPlugin.tsx,
    plugins/rpe/src/store.ts
  </files>
  <read_first>
    - plugins/rpe/src/screens/RPECalculatorScreen.tsx — lire l'implémentation complète (inputs, calc logic, RPE_VALUES pattern)
    - plugins/rpe/src/index.ts — vérifier les exports existants (calc1RM, TRAINING_ZONES, etc.)
    - plugins/rpe/src/store.ts — lire si existe, sinon créer
    - C:/Users/Anatholy/Downloads/ziko/plugins-2.jsx lignes 780–859 (RPEPlugin)
  </read_first>
  <behavior>
    - Single screen sans SubTabs (D-16 — mockup ne montre pas de tabs pour RPE)
    - Valeurs initiales depuis useRpeStore (lastWeight, lastReps, lastRpe)
    - Résultat 1RM calculé en temps réel sur chaque changement d'input : useMemo(() => calc1RM(weight, reps, rpe), [weight, reps, rpe])
    - Persist : à chaque changement → store.setLast(weight, reps, rpe) via useEffect
    - Zones : TRAINING_ZONES map → RowList avec highlight si le poids courant est dans la zone (poids / oneRM between zone.min and zone.max)
  </behavior>
  <action>
    ÉTAPE 1 — Mettre à jour (ou créer) plugins/rpe/src/store.ts :
    ```typescript
    import { create } from 'zustand';
    import { persist, createJSONStorage } from 'zustand/middleware';
    import { MMKV } from 'react-native-mmkv';
    const mmkv = new MMKV({ id: 'rpe-store' });
    const storage = { getItem: (k:string) => mmkv.getString(k) ?? null, setItem: (k:string,v:string) => mmkv.set(k,v), removeItem: (k:string) => mmkv.delete(k) };
    interface RPEStoreState { lastWeight: number; lastReps: number; lastRpe: number; setLast: (w:number, r:number, rpe:number) => void }
    export const useRpeStore = create<RPEStoreState>()(persist(
      (set) => ({ lastWeight: 100, lastReps: 5, lastRpe: 8, setLast: (w,r,rpe) => set({lastWeight:w,lastReps:r,lastRpe:rpe}) }),
      { name: 'rpe-store', storage: createJSONStorage(() => storage) }
    ));
    ```

    ÉTAPE 2 — Créer plugins/rpe/src/screens/RPEPlugin.tsx :

    Imports : SafeAreaView, ScrollView, View, Text, TouchableOpacity from react-native + react-native-safe-area-context; useState, useMemo, useEffect from 'react'; useRouter from 'expo-router'; PluginHeader from '@ziko/ui'; useThemeStore from '@ziko/plugin-sdk'; Ionicons from '@expo/vector-icons'; calc1RM, rpeToPercent, rpeToRIR, TRAINING_ZONES from '../index'; useRpeStore from '../store'.

    PAS de SubTabs — single scrollview.

    CARTE 1RM (dark) :
    - View card backgroundColor '#1C1A17' borderRadius 20 padding 24 marginBottom 12
    - Text "1RM ESTIMÉ" 11px fontWeight '700' color 'rgba(255,250,246,0.55)' letterSpacing 0.08 textTransform 'uppercase'
    - Text oneRM.toFixed(1) + ' kg' 56px fontWeight '900' color '#FF5C1A' textAlign 'center' (si weight/reps/rpe valides)
    - Row sous le 1RM : Text rpeToRIR(rpe) + ' rep(s) en réserve' 12px color 'rgba(255,250,246,0.65)' | Text rpeToPercent(reps,rpe)*100 + '% 1RM' 12px same color

    3 RANGÉES INPUT (weight, reps, RPE) :
    - Chaque rangée : View card row (borderRadius 16 borderWidth 1 border padding 14 gap 12); Ionicons icon 20 primary; View flex:1; Text label 10.5px muted; View row gap 8 marginTop 4 — bouton '-' (TouchableOpacity 32×32 bg 'rgba(255,92,26,0.1)' borderRadius 8 Text '-' 18px primary) + Text value (fontSize 22 fontWeight '700' color text minWidth 60 textAlign 'center') + bouton '+' (même style, '+')
    - Weight steps : -/+ 2.5kg; min 0, max 500
    - Reps steps : -/+ 1; min 1, max 20; afficher const REPS_CHIPS = [1,2,3,4,5,6,8,10,12] comme row de chips scrollable (TouchableOpacity chip, actif → bg primary text white, inactif → bg rgba(primary,0.1) text primary)
    - RPE steps : -/+ 0.5; min 5, max 10; afficher const RPE_CHIPS = [5,6,7,7.5,8,8.5,9,9.5,10] comme row scrollable; couleur chip selon rpeColor() : ≤6 '#2E9E5B', ≤8 '#F59E0B', >8 '#E94B3C'

    TABLE ZONES :
    - Text "Zones d'entraînement" 13px bold marginTop 16 marginBottom 8
    - TRAINING_ZONES.map → View row card padding 10; View 10×10 borderRadius 5 backgroundColor zone.color; Text zone.label 12px bold flex:1; Text zone.min*100+'–'+zone.max*100+'%' 11px muted; Text (oneRM * zone.min).toFixed(1)+'–'+(oneRM * zone.max).toFixed(1)+' kg' 12px primary fontWeight '700'
    - Highlight zone courante : borderColor = primary si rpeToPercent(reps,rpe) between zone.min and zone.max; sinon borderColor = border

    useEffect([weight, reps, rpe], () => store.setLast(weight, reps, rpe)) pour persistence.
    paddingBottom: 100.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "rpe/src/screens/RPEPlugin\|rpe/src/store" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - RPEPlugin.tsx exporte une fonction default nommée RPEPlugin
    - Fichier contient import de calc1RM depuis '../index'
    - Fichier contient import de useRpeStore depuis '../store'
    - Fichier contient backgroundColor '#1C1A17' (carte dark 1RM)
    - Fichier contient TRAINING_ZONES map (table zones)
    - Fichier NE contient PAS de SubTabs (D-16 — single screen)
    - store.ts contient useRpeStore avec persist MMKV
    - TypeScript : zéro erreur sur ces fichiers
  </acceptance_criteria>
  <done>RPEPlugin.tsx créé avec carte dark 1RM, 3 inputs avec chips, table TRAINING_ZONES highlight zone courante; useRpeStore avec persist MMKV; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Construire PantryPlugin.tsx — 3 SubTabs avec inventory/recettes match%/shopping</name>
  <files>plugins/pantry/src/screens/PantryPlugin.tsx</files>
  <read_first>
    - plugins/pantry/src/screens/PantryDashboard.tsx — lire pour patterns Supabase, types pantry_items
    - plugins/pantry/src/screens/ShoppingList.tsx — lire le pattern de shopping list existant
    - C:/Users/Anatholy/Downloads/ziko/plugins-2.jsx lignes 860–1014 (PantryPlugin)
  </read_first>
  <behavior>
    - Onglet Inventaire : useQuery → pantry_items WHERE user_id; compter par category ('frigo'|'congelateur'|'placard'); 3 tiles; AISuggestion tintColor primary avec conseil "voici ce que tu peux cuisiner ce soir"
    - Onglet Recettes : useQuery → pantry_items; définir 4 recettes statiques avec liste d'ingrédients; match% = pantry_items.filter(i => recipe.ingredients.includes(i.name.toLowerCase())).length / recipe.ingredients.length * 100; RowList triée par match% DESC; si match >= 80 → chip "Prêt à cuisiner" vert; si >= 50 → chip "Presque" amber; sinon → "Il manque X ingrédients"
    - Onglet Shopping : tenter useQuery → shopping_list WHERE user_id; si erreur table absente → fallback useState + MMKV persist; checklist items avec toggle checked state
  </behavior>
  <action>
    Créer plugins/pantry/src/screens/PantryPlugin.tsx comme export default function PantryPlugin({ supabase }: { supabase: any }).

    Tabs : const TABS = ['Inventaire', 'Recettes', 'Shopping'].

    ONGLET INVENTAIRE :
    - Titre section "Mes stocks" 13px bold marginBottom 8
    - 3 tiles catégorie en colonne : const CATEGORIES = [{id:'frigo', label:'Frigo', icon:'snow-outline', color:'#2E7BF6'},{id:'congelateur', label:'Congél.', icon:'thermometer-outline', color:'#7B5BD0'},{id:'placard', label:'Placard', icon:'grid-outline', color:'#F59E0B'}]
    - Chaque tile : View card row padding 16 gap 12; View 48×48 borderRadius 14 fond 12% icon.color; Ionicons 22px icon.color; View flex:1; Text label 15px fontWeight '700'; Text count + ' articles' 12px muted; Ionicons chevron-forward 16 muted
    - onPress → router.push vers route pantry add/edit si disponible, sinon showAlert info
    - AISuggestion tintColor primary (D-21) en bas avec "Voici ce que tu peux cuisiner ce soir avec ce que tu as."

    ONGLET RECETTES :
    - const RECIPE_STUBS = [{name:'Omelette protéinée', ingredients:['œufs','fromage','épinards','ail'], time:'10 min', kcal:320},{name:'Bowl riz-poulet', ingredients:['riz','poulet','brocoli','sauce soja'], time:'25 min', kcal:520},{name:'Smoothie protéiné', ingredients:['banane','lait','whey','beurre de cacahuète'], time:'5 min', kcal:380},{name:'Salade Niçoise', ingredients:['thon','œufs','tomates','olives','haricots verts'], time:'15 min', kcal:410}]
    - Calculer match% pour chaque recette (pantryItems query)
    - RowList triée match% DESC : title=recipe.name, sub=recipe.time + ' · ' + recipe.kcal + ' kcal'; right = match.toFixed(0) + '%'; rightColor = match >= 80 ? '#2E9E5B' : match >= 50 ? '#F59E0B' : '#6B6963'
    - Chip sous le right : si match >= 80 → View chip bg 'rgba(46,158,91,0.12)' color '#2E9E5B' text 'Prêt ✓'; si >= 50 → amber; sinon → Text "−{missing} ingr." 10px muted

    ONGLET SHOPPING :
    - useQuery shopping_list avec .catch(() => []); si null ou vide → empty state "Liste vide — ajoute des articles" + bouton "Ajouter" (naviguer vers pantry add ou showAlert)
    - Checklist : items.map → View card row padding 12 gap 10; TouchableOpacity 24×24 borderRadius 12 (circle checkbox : checked → bg primary Ionicons check 12 white; unchecked → border 2 primary); Text item.name 13px bold (lineThrough si checked); Text item.quantity 11px muted
    - Bouton "Vider la liste" bas de page → showAlert confirmation

    paddingBottom: 100. Card shadow standard.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "pantry/src/screens/PantryPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - PantryPlugin.tsx exporte une fonction default nommée PantryPlugin
    - Fichier contient 3 SubTabs avec 'Inventaire' comme valeur initiale
    - Fichier contient les 3 catégories (frigo, congelateur, placard) — PLUG-PAN-01
    - Fichier contient RECIPE_STUBS avec calcul match% — PLUG-PAN-02
    - Fichier contient section Shopping avec checklist — PLUG-PAN-03
    - Fichier contient AISuggestion — PLUG-PAN-04
    - Fichier NE contient PAS de constante PANTRY_DATA fixture
    - TypeScript : zéro erreur sur ce fichier
  </acceptance_criteria>
  <done>PantryPlugin.tsx créé avec 3 SubTabs (Inventaire/Recettes/Shopping), 3 tiles catégorie avec counts, recettes rule-based match%, shopping list; AISuggestion; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Câbler route wrappers, barrels, supprimer anciens dashboards (RPE + Pantry)</name>
  <files>
    apps/mobile/app/(app)/(plugins)/rpe/index.tsx,
    apps/mobile/app/(app)/(plugins)/pantry/dashboard.tsx,
    plugins/rpe/src/index.ts,
    plugins/pantry/src/index.ts
  </files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/rpe/index.tsx — lire l'import actuel
    - apps/mobile/app/(app)/(plugins)/pantry/dashboard.tsx — lire l'import actuel
    - plugins/rpe/src/index.ts — lire tous les exports
    - plugins/pantry/src/index.ts — lire tous les exports
  </read_first>
  <action>
    RPE :
    Étape 1 — Route wrapper rpe/index.tsx : changer l'import de 'RPECalculatorScreen' → 'RPEPlugin' via '@ziko/plugin-rpe/screens/RPEPlugin'. Corps inchangé.
    Étape 2 — Barrel rpe : remplacer export RPECalculatorScreen par `export { default as RPEPlugin } from './screens/RPEPlugin';`. Conserver calc1RM, rpeToPercent, rpeToRIR, RPE_VALUES, TRAINING_ZONES et autres.
    Étape 3 — Suppression : grep RPECalculatorScreen dans apps/ et plugins/rpe/src/. Si count = 0 → supprimer plugins/rpe/src/screens/RPECalculatorScreen.tsx.

    PANTRY :
    Étape 4 — Route wrapper pantry/dashboard.tsx : changer import 'PantryDashboard' → 'PantryPlugin' via '@ziko/plugin-pantry/screens/PantryPlugin'.
    Étape 5 — Barrel pantry : remplacer export PantryDashboard par `export { default as PantryPlugin } from './screens/PantryPlugin';`. Conserver BarcodeScanner, PantryItemForm, RecipeDetail, PantryRecipes, ShoppingList, RecipeConfirm et autres.
    Étape 6 — Suppression : grep PantryDashboard dans apps/ et plugins/pantry/src/. Si count = 0 → supprimer plugins/pantry/src/screens/PantryDashboard.tsx.

    NE PAS toucher : BarcodeScanner, PantryItemForm, RecipeDetail, PantryRecipes, ShoppingList, RecipeConfirm.
  </action>
  <verify>
    <automated>grep -r 'RPECalculatorScreen\|PantryDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/rpe/src /c/ziko-platform/plugins/pantry/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - rpe/index.tsx importe 'RPEPlugin' (pas RPECalculatorScreen)
    - pantry/dashboard.tsx importe 'PantryPlugin' (pas PantryDashboard)
    - plugins/rpe/src/index.ts exporte RPEPlugin ET conserve calc1RM, TRAINING_ZONES
    - grep 'RPECalculatorScreen|PantryDashboard' retourne 0 ligne
    - RPECalculatorScreen.tsx N'EXISTE PAS; PantryDashboard.tsx N'EXISTE PAS
    - BarcodeScanner.tsx, ShoppingList.tsx, RecipeDetail.tsx EXISTENT toujours
    - TypeScript : rtk tsc --noEmit retourne 0 erreur
  </acceptance_criteria>
  <done>Route wrappers mis à jour, barrels mis à jour, RPECalculatorScreen et PantryDashboard supprimés; BarcodeScanner/ShoppingList/RecipeDetail intacts; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-39-03-01 | Info Disclosure | pantry_items query | mitigate | Query scopée user_id; RLS enforce ownership |
| T-39-03-02 | Info Disclosure | shopping_list query | mitigate | Query scopée user_id; RLS enforce ownership |
| T-39-03-03 | Tampering | RPE Zustand persist | accept | Données locales uniquement (poids/reps/RPE) — pas de données sensibles |
| T-39-03-04 | Tampering | Recipe match% calcul | accept | Calcul frontend rule-based; pas d'écriture DB; données statiques RECIPE_STUBS |
</threat_model>

<verification>
1. TypeScript clean : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'` retourne 0
2. RPEPlugin créé : `grep 'export default function RPEPlugin' /c/ziko-platform/plugins/rpe/src/screens/RPEPlugin.tsx` retourne match
3. PantryPlugin créé : `grep 'export default function PantryPlugin' /c/ziko-platform/plugins/pantry/src/screens/PantryPlugin.tsx` retourne match
4. calc1RM importé : `grep 'calc1RM' /c/ziko-platform/plugins/rpe/src/screens/RPEPlugin.tsx` retourne match (PLUG-RPE-02)
5. TRAINING_ZONES présent : `grep 'TRAINING_ZONES' /c/ziko-platform/plugins/rpe/src/screens/RPEPlugin.tsx` retourne match (PLUG-RPE-01 table %)
6. Zustand persist : `grep 'persist' /c/ziko-platform/plugins/rpe/src/store.ts` retourne match (PLUG-RPE-03)
7. 3 catégories pantry : `grep 'congelateur\|placard\|frigo' /c/ziko-platform/plugins/pantry/src/screens/PantryPlugin.tsx` retourne match (PLUG-PAN-01)
8. RPECalculatorScreen supprimé : `test -f /c/ziko-platform/plugins/rpe/src/screens/RPECalculatorScreen.tsx` échoue
9. PantryDashboard supprimé : `test -f /c/ziko-platform/plugins/pantry/src/screens/PantryDashboard.tsx` échoue
10. ShoppingList intact : `test -f /c/ziko-platform/plugins/pantry/src/screens/ShoppingList.tsx` réussit
</verification>

<success_criteria>
- RPEPlugin affiche 1RM en temps réel avec calc1RM, table TRAINING_ZONES avec highlight zone courante, persistence Zustand
- PantryPlugin affiche 3 tiles catégorie avec counts, recettes avec match%, shopping list
- Anciens dashboards supprimés; fichiers satellites intacts
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/39-remaining-plugins-group2/39-03-SUMMARY.md` une fois terminé.
</output>
