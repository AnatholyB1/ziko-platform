---
phase: 29-plugin-mon-coach-full-impl
plan: 04
type: execute
wave: 2
depends_on:
  - "29-01"
  - "29-02"
  - "29-03"
files_modified:
  - apps/mobile/app/(app)/profile/settings.tsx
  - apps/mobile/app/(app)/store/[id].tsx
autonomous: true
requirements:
  - COACH-04
  - COACH-11

must_haves:
  truths:
    - "settings.tsx has a MON COACH section visible only when role is 'client' or 'both' AND a coach is linked"
    - "MON COACH section navigates to /(plugins)/coach/dashboard on row press"
    - "settings.tsx MON COACH section is hidden entirely when no coach is linked or role is 'coach'"
    - "store/[id].tsx trash button for mandatory plugins is a TouchableOpacity (not View) with onLongPress showing tooltip"
    - "Tooltip message uses t('store.mandatory_tooltip') — available from Wave 0 i18n keys"
  artifacts:
    - path: "apps/mobile/app/(app)/profile/settings.tsx"
      provides: "MON COACH section injection with role+link gating"
      contains: "coach.settings_section"
    - path: "apps/mobile/app/(app)/store/[id].tsx"
      provides: "Long-press tooltip on mandatory plugin trash button"
      contains: "mandatory_tooltip"
  key_links:
    - from: "settings.tsx MON COACH STRow"
      to: "apps/mobile/app/(app)/(plugins)/coach/dashboard.tsx"
      via: "router.push('/(plugins)/coach/dashboard')"
      pattern: "plugins.*coach.*dashboard"
    - from: "store/[id].tsx TouchableOpacity"
      to: "packages/plugin-sdk/src/i18n.ts"
      via: "t('store.mandatory_tooltip')"
      pattern: "mandatory_tooltip"
---

<objective>
Wire two integration points: inject the role-gated MON COACH settings section into profile/settings.tsx, and upgrade the store/[id].tsx mandatory trash button from a non-interactive View to a TouchableOpacity with long-press tooltip.

Purpose: COACH-11 requires athletes with a linked coach to see a settings row navigating them to the coach screen. COACH-04 requires that the mandatory plugin trash button shows a tooltip on long-press — the Phase 27 gate already prevents uninstall; this plan adds the UX explanation.

Output: 2 files modified. No new files. TypeScript compiles clean.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-RESEARCH.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-PATTERNS.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-CONTEXT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/028-UI-SPEC.md
</context>

<interfaces>
From apps/mobile/app/(app)/profile/settings.tsx (confirmed in PATTERNS.md):

STGroup and STRow are inline-defined components (NOT exported). Injection point is between "Préférences" STGroup closing tag (~line 394) and "Aide & infos" STGroup opening tag (~line 396).

Current imports in settings.tsx include: useThemeStore, showAlert, usePluginRegistry from @ziko/plugin-sdk. Need to ADD: useTranslation, useQuery from @tanstack/react-query, supabase from '../../../src/lib/supabase' (relative from profile/ subdir).

role is read from profile?.role where profile comes from useAuthStore. Because UserProfile.role is typed after Wave 0, direct access is clean: const role = profile?.role ?? 'client'.

STRow props: { icon: string, tint?: string, label: string, sub?: string, right?: ReactNode, danger?: boolean, onPress?: () => void, toggleValue?: boolean, onToggle?: () => void }

Settings coach section design (from UI-SPEC §6):
- STGroup title: t('coach.settings_section') — renders "MON COACH" / "MY COACH"
- STRow: icon 'person-outline', tint '#FF5C1A', label = linkedCoachName (display_name from preview), onPress navigates to /(plugins)/coach/dashboard

From apps/mobile/app/(app)/store/[id].tsx (confirmed in PATTERNS.md lines 497–516):
Current mandatory block (lines 257–260):
{manifest.mandatory ? (
  <View style={{ backgroundColor: '#F4F3F0', borderRadius: 14, paddingVertical: 14, paddingHorizontal: 18, alignItems: 'center', opacity: 0.5 }}>
    <Ionicons name="trash-outline" size={18} color="#F44336" />
  </View>
) : (...)}

store/[id].tsx likely already imports showAlert from @ziko/plugin-sdk. If not, add it. Also confirm if useTranslation and t are already available or need to be added.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inject MON COACH section into settings.tsx</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/settings.tsx — read the full file to: (a) confirm import block at top (lines 1–15 approx), (b) confirm STGroup/STRow definitions (lines 31–93), (c) find the exact injection point between "Préférences" and "Aide & infos" STGroups (lines ~388–402), (d) confirm what hooks/values are already declared in component body, (e) confirm existing @ziko/plugin-sdk imports to add useTranslation if missing
  </read_first>
  <action>
    Make the following changes to apps/mobile/app/(app)/profile/settings.tsx:

    Change 1 — imports: Add these imports if not already present:
    - useQuery from '@tanstack/react-query' (add to existing import or new import line)
    - useTranslation from '@ziko/plugin-sdk' (add to existing @ziko/plugin-sdk import destructure)
    - supabase from '../../../src/lib/supabase' (adjust path: settings.tsx is at app/(app)/profile/ — three levels to apps/mobile, then src/lib/supabase)

    Change 2 — component body: After existing const declarations (after useAuthStore profile, user, etc.), add:
    - const { t } = useTranslation(); (if not already present)
    - const role = profile?.role ?? 'client';
    - A useQuery for coach link status:
      queryKey: ['coach-link-settings', profile?.id]
      queryFn: async function that: (a) calls supabase.auth.getSession() to get session, (b) fetches GET ${process.env.EXPO_PUBLIC_API_URL}/coach/clients/links/me with Authorization Bearer header, (c) if !res.ok returns null, (d) returns res.json()
      enabled: !!(role === 'client' || role === 'both') && !!profile?.id
      staleTime: 30_000
    - const linkedCoachName = coachData?.preview?.display_name ?? null; (where coachData is the { data } destructure from useQuery)

    Change 3 — JSX injection: Between the closing tag of the "Préférences" STGroup and the opening tag of the "Aide & infos" STGroup, add:
    A conditional block: {(role === 'client' || role === 'both') && linkedCoachName && (
      <STGroup title={t('coach.settings_section')}>
        <STRow
          icon="person-outline"
          tint="#FF5C1A"
          label={linkedCoachName}
          onPress={() => router.push('/(plugins)/coach/dashboard' as any)}
        />
      </STGroup>
    )}

    The router import (from 'expo-router') should already be in settings.tsx; if not, add it.

    Do NOT change any other part of settings.tsx. Do NOT modify existing STGroups. Do NOT add duplicate imports.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | grep -i error | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "coach.settings_section" apps/mobile/app/\(app\)/profile/settings.tsx` returns 1 match
    - `grep "linkedCoachName" apps/mobile/app/\(app\)/profile/settings.tsx` returns at least 2 matches (declaration + conditional)
    - `grep "role === 'client' || role === 'both'" apps/mobile/app/\(app\)/profile/settings.tsx` returns at least 1 match
    - `grep "plugins.*coach.*dashboard" apps/mobile/app/\(app\)/profile/settings.tsx` returns 1 match
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>settings.tsx renders MON COACH STGroup with coach display_name row when role is client/both and a coach is linked; section is absent otherwise. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade mandatory trash button to TouchableOpacity with long-press tooltip</name>
  <files>apps/mobile/app/(app)/store/[id].tsx</files>
  <read_first>
    - apps/mobile/app/(app)/store/[id].tsx — read lines 250–275 to see the current mandatory gate (View wrapper at lines 257–260); also read the imports section at top to confirm whether showAlert and useTranslation are already imported from @ziko/plugin-sdk, and whether t is already destructured in the component body
  </read_first>
  <action>
    Make the following changes to apps/mobile/app/(app)/store/[id].tsx:

    Change 1 — imports: If useTranslation is not already imported from @ziko/plugin-sdk, add it to the existing @ziko/plugin-sdk import. If showAlert is not already imported, add it too.

    Change 2 — component body: If t is not already destructured from useTranslation(), add: const { t } = useTranslation(); in the component body.

    Change 3 — JSX: At the mandatory gate (lines ~257–260), change the wrapping element from View to TouchableOpacity. The replacement must:
    - Use TouchableOpacity (not View)
    - Keep ALL existing style props verbatim: backgroundColor '#F4F3F0', borderRadius 14, paddingVertical 14, paddingHorizontal 18, alignItems 'center', opacity 0.5
    - Add: onLongPress={() => showAlert(t('store.mandatory_tooltip'), '')}
    - Keep the Ionicons child unchanged: name="trash-outline" size={18} color="#F44336"

    The TouchableOpacity must NOT have onPress (no action on single tap — only long-press triggers tooltip). Only onLongPress is added.

    Do NOT change any other logic in store/[id].tsx. The else-branch (actual uninstall TouchableOpacity) is unchanged.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | grep -i error | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TouchableOpacity" apps/mobile/app/\(app\)/store/\[id\].tsx | grep "onLongPress"` returns 1 match (the mandatory gate)
    - `grep "mandatory_tooltip" apps/mobile/app/\(app\)/store/\[id\].tsx` returns 1 match
    - `grep -n "^import.*View.*from 'react-native'" apps/mobile/app/\(app\)/store/\[id\].tsx` or grep confirms View is still imported (used elsewhere) — if TouchableOpacity was not in existing imports, add it
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Mandatory plugin trash button is a TouchableOpacity with onLongPress showing t('store.mandatory_tooltip') via showAlert. Single tap has no action. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.tsx → /coach/clients/links/me | Settings fetches link status with auth token; result displayed to the authenticated user only |
| store/[id].tsx tooltip | Long-press shows a read-only informational message — no state mutation, no data leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Information Disclosure | settings.tsx coach section visibility | mitigate | Section conditional on role AND linkedCoachName — invisible to coaches (role 'coach') and unlinked athletes |
| T-29-10 | Tampering | store/[id].tsx mandatory gate | mitigate | TouchableOpacity has no onPress — only onLongPress tooltip; actual uninstall path is unchanged in else branch; mandatory: true gate from Phase 27 is intact |
| T-29-SC | Tampering | npm installs | accept | No new packages; useQuery already hoisted |
</threat_model>

<verification>
After Plan 29-04 completes (all 4 plans done):
- npm run type-check exits 0 across full monorepo
- settings.tsx has coach.settings_section conditional block with role+link guard
- store/[id].tsx mandatory gate uses TouchableOpacity with onLongPress + mandatory_tooltip
- Full phase verification: `grep -r "Alert.alert" plugins/coach/` returns 0 matches
- Full phase verification: `grep -r "StyleSheet" plugins/coach/` returns 0 matches
</verification>

<success_criteria>
1. settings.tsx: MON COACH section appears for role 'client'/'both' + linked coach; invisible for coaches or unlinked athletes
2. settings.tsx: STRow shows coach display_name from TanStack Query, tint '#FF5C1A', navigates to /(plugins)/coach/dashboard
3. store/[id].tsx: mandatory trash gate is TouchableOpacity (not View) with onLongPress t('store.mandatory_tooltip') via showAlert
4. npm run type-check exits 0
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-04-SUMMARY.md` when done
</output>
