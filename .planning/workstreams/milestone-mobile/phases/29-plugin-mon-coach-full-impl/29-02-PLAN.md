---
phase: 29-plugin-mon-coach-full-impl
plan: 02
type: execute
wave: 1
depends_on:
  - "29-01"
files_modified:
  - apps/mobile/src/lib/PluginLoader.tsx
autonomous: true
requirements:
  - COACH-02
  - COACH-03

must_haves:
  truths:
    - "PluginLoader PLUGIN_LOADERS map contains a literal-string 'coach' entry importing @ziko/plugin-coach/manifest"
    - "autoInstallCoachPlugin function is called after the mandatory pre-load loop, before the user_plugins SELECT"
    - "Auto-install upsert uses onConflict: 'user_id,plugin_id' making it idempotent across sign-ins"
    - "Auto-install fires only when user role is 'client' or 'both' — coaches with role 'coach' are skipped"
  artifacts:
    - path: "apps/mobile/src/lib/PluginLoader.tsx"
      provides: "Coach plugin registration + auto-install"
      contains: "coach: () => import('@ziko/plugin-coach/manifest')"
  key_links:
    - from: "apps/mobile/src/lib/PluginLoader.tsx"
      to: "plugins/coach/src/manifest.ts"
      via: "import('@ziko/plugin-coach/manifest')"
      pattern: "@ziko/plugin-coach/manifest"
    - from: "autoInstallCoachPlugin"
      to: "supabase user_plugins table"
      via: "supabase.from('user_plugins').upsert"
      pattern: "user_plugins.*upsert"
---

<objective>
Register the coach plugin in PluginLoader's static PLUGIN_LOADERS map and implement idempotent auto-install for client-role users. After this plan, any athlete (role 'client' or 'both') who signs in will automatically have a user_plugins row for the coach plugin.

Purpose: COACH-02 requires the coach plugin to appear in the static PLUGIN_LOADERS map (Metro requires literal imports — dynamic template strings fail bundler). COACH-03 requires auto-install on sign-in — the upsert runs once per sign-in but is idempotent via onConflict.

Output: PluginLoader.tsx modified in two places — PLUGIN_LOADERS map entry added, autoInstallCoachPlugin call inserted after mandatory loop.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-CONTEXT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-RESEARCH.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-PATTERNS.md
</context>

<interfaces>
From apps/mobile/src/lib/PluginLoader.tsx (confirmed in PATTERNS.md lines 282–309):

PLUGIN_LOADERS map (lines 9–28):
```typescript
const PLUGIN_LOADERS: Record<string, () => Promise<{ default: PluginManifest }>> = {
  nutrition:     () => import('@ziko/plugin-nutrition/manifest') as any,
  // ... 15 more entries ...
  pantry:        () => import('@ziko/plugin-pantry/manifest') as any,
  // ← ADD HERE: coach: () => import('@ziko/plugin-coach/manifest') as any,
};
```

Mandatory pre-load loop ends at line ~87. Auto-install goes AFTER the loop, BEFORE the user_plugins SELECT at line ~89:
```typescript
// INSERT: await autoInstallCoachPlugin(user.id);
const { data: userPlugins, error } = await supabase
  .from('user_plugins')
  .select(...)
```

Supabase client already imported at line 4. user from useAuthStore already available (line 62).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register coach in PLUGIN_LOADERS and add auto-install logic</name>
  <files>apps/mobile/src/lib/PluginLoader.tsx</files>
  <read_first>
    - apps/mobile/src/lib/PluginLoader.tsx — read the full file to identify: (a) the exact closing brace of PLUGIN_LOADERS (line ~28), (b) the exact line where the mandatory pre-load loop ends (~line 87), (c) the exact line where user_plugins SELECT begins (~line 89), (d) confirm supabase and user are already imported/available in scope
  </read_first>
  <action>
    Make two changes to apps/mobile/src/lib/PluginLoader.tsx:

    Change 1 — PLUGIN_LOADERS map: add the following entry after the pantry entry (last item in the map, line ~27), before the closing `}` of the map:
      coach: () => import('@ziko/plugin-coach/manifest') as any,

    The key MUST be the literal string 'coach'. Do NOT use a template literal or computed key. This is a Metro bundler static import requirement.

    Change 2 — auto-install function: define a new async function named autoInstallCoachPlugin that accepts userId: string. Inside it:
    1. Query supabase.from('user_profiles').select('role').eq('id', userId).single()
    2. Read profileRow?.role, defaulting to 'client' if undefined/null
    3. If role === 'client' || role === 'both': call supabase.from('user_plugins').upsert({ user_id: userId, plugin_id: 'coach', is_enabled: true }, { onConflict: 'user_id,plugin_id' })
    4. Wrap the entire function body in a try/catch; on error log console.warn('[PluginLoader] autoInstallCoachPlugin failed:', err) and do NOT throw

    Change 3 — call site: after the mandatory pre-load loop closes (after its closing `}`), and BEFORE the existing user_plugins SELECT query, add: await autoInstallCoachPlugin(user.id);

    The autoInstallCoachPlugin function can be defined either (a) outside the component as a standalone async function (preferred — it receives userId and supabase from the outer scope via closure is fine) or (b) inside the component body. Either is acceptable. The function must NOT be defined inside the for-loop of the mandatory pre-load.

    Do NOT change any other logic in PluginLoader.tsx. Do NOT modify the mandatory pre-load loop itself.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | grep -i error | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "coach.*import('@ziko/plugin-coach/manifest')" apps/mobile/src/lib/PluginLoader.tsx` returns 1 match
    - `grep "autoInstallCoachPlugin" apps/mobile/src/lib/PluginLoader.tsx` returns at least 2 matches (definition + call site)
    - `grep "onConflict.*user_id,plugin_id" apps/mobile/src/lib/PluginLoader.tsx` returns 1 match
    - `grep "role === 'client' || role === 'both'" apps/mobile/src/lib/PluginLoader.tsx` returns 1 match
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Coach plugin is registered in PLUGIN_LOADERS. autoInstallCoachPlugin upserts a user_plugins row for client-role users on every sign-in, idempotently. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PluginLoader → Supabase | user_plugins upsert trusts user.id from useAuthStore session |
| role read → install gate | Auto-install decision based on user_profiles.role; default 'client' if row missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Elevation of Privilege | autoInstallCoachPlugin role gate | mitigate | role === 'client' \|\| role === 'both' check; coaches with role 'coach' only are skipped; default 'client' is conservative (installs for unknown) |
| T-29-04 | Tampering | PLUGIN_LOADERS static import | mitigate | Literal string import required (Metro bundler constraint); template string dynamism prevented by bundler |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; @ziko/plugin-coach resolved from Wave 0 scaffold |
</threat_model>

<verification>
After Plan 29-02 completes:
- `grep -c "coach" apps/mobile/src/lib/PluginLoader.tsx` ≥ 3 (map entry, function definition, call site)
- `grep "onConflict" apps/mobile/src/lib/PluginLoader.tsx` confirms idempotent upsert
- `npm run type-check` exits 0
</verification>

<success_criteria>
1. 'coach' entry appears in PLUGIN_LOADERS as a literal-string import
2. autoInstallCoachPlugin function is defined with role gate ('client' | 'both') and onConflict upsert
3. autoInstallCoachPlugin is called after the mandatory pre-load loop, before the user_plugins SELECT
4. npm run type-check exits 0
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-02-SUMMARY.md` when done
</output>
