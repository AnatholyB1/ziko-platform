---
phase: 29-plugin-mon-coach-full-impl
plan: 03
type: execute
wave: 1
depends_on:
  - "29-01"
files_modified:
  - plugins/coach/src/screens/CoachScreen.tsx
autonomous: true
requirements:
  - COACH-06
  - COACH-07
  - COACH-08
  - COACH-09
  - COACH-12
  - COACH-14

must_haves:
  truths:
    - "State A renders a 6-char TextInput with charset filter [A-Z2-9], submit CTA disabled when < 6 valid chars"
    - "State A error message uses a single i18n key coach.state_a.error regardless of error code (no branching per error type)"
    - "State B renders coach preview card with avatar, display_name, specialties chips, bio, KYC badge when kyc_status === 'verified'"
    - "State B 'Lier mon compte' CTA calls POST /coach/clients/links/redeem with body { code }, invalidates query on success"
    - "State C renders linked coach card with 'Lié depuis [date]' formatted via date-fns, sessions count, habits% stats row"
    - "ConfirmRevocationModal has controlled TextInput; confirm button enabled only when inputValue.trim() === 'COACH'"
    - "Revocation calls DELETE /coach/clients/links/:link.id, invalidates query on success, returns to State A"
    - "Full-screen ActivityIndicator with color '#FF5C1A' renders while initial useQuery is loading"
    - "Pull-to-refresh wired to refetch() with RefreshControl tintColor: '#FF5C1A'"
    - "ScrollView contentContainerStyle includes paddingBottom: 100"
    - "No Alert.react-native used — showAlert for non-modal alerts, custom Modal for revocation"
    - "No StyleSheet.create — all styles are inline style objects"
    - "All icons are Ionicons from @expo/vector-icons"
  artifacts:
    - path: "plugins/coach/src/screens/CoachScreen.tsx"
      provides: "Full 3-state coach screen with revocation modal"
      exports: ["CoachScreen"]
      min_lines: 200
  key_links:
    - from: "CoachScreen useQuery"
      to: "${EXPO_PUBLIC_API_URL}/coach/clients/links/me"
      via: "fetch with Bearer token"
      pattern: "/coach/clients/links/me"
    - from: "State A submit"
      to: "${EXPO_PUBLIC_API_URL}/coach/clients/links/preview"
      via: "POST fetch with body { code }"
      pattern: "/coach/clients/links/preview"
    - from: "State B confirm"
      to: "${EXPO_PUBLIC_API_URL}/coach/clients/links/redeem"
      via: "POST fetch with body { code }"
      pattern: "/coach/clients/links/redeem"
    - from: "ConfirmRevocationModal confirm"
      to: "${EXPO_PUBLIC_API_URL}/coach/clients/links/:id"
      via: "DELETE fetch with link.id from LinkRow"
      pattern: "/coach/clients/links"
---

<objective>
Replace the Wave 0 CoachScreen stub with the full production implementation: TanStack Query link-status fetch, 3-state conditional rendering (A/B/C), ConfirmRevocationModal with typed "COACH" confirmation, stats queries for State C, pull-to-refresh, and loading/error states — all pixel-for-pixel per 028-UI-SPEC.md.

Purpose: This is the core deliverable of Phase 29. The screen drives the full athlete-side UX for coach linking and revocation. All COACH-06/07/08/09/12/14 requirements land in this single file.

Output: plugins/coach/src/screens/CoachScreen.tsx — full implementation, TypeScript clean, all UI states per UI-SPEC.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/028-UI-SPEC.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-RESEARCH.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-PATTERNS.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-CONTEXT.md
</context>

<interfaces>
Backend endpoint contract (VERIFIED from service.ts — use ONLY these exact paths):

| Action | Method | Path | Body | Response |
|--------|--------|------|------|----------|
| Link status | GET | /coach/clients/links/me | — | { link: LinkRow\|null, preview: CoachPreviewPayload\|null } |
| Preview code | POST | /coach/clients/links/preview | { code: string } | { ok: boolean, error_code: string\|null, preview: CoachPreviewPayload\|null } |
| Redeem | POST | /coach/clients/links/redeem | { code: string } | { ok: boolean, error_code: string\|null, link: LinkRow\|null } |
| Revoke | DELETE | /coach/clients/links/:id | — | { ok: true } |

LinkRow shape (from db.ts):
{ id: string, coach_id: string, client_id: string, created_at: string }

CoachPreviewPayload shape (from types.ts):
{ coach_id: string, display_name: string, bio: string|null, specialties: string[]|null, photo_signed_url: string|null, kyc_status: 'pending'|'submitted'|'verified'|'rejected'|null }

KYC badge shows when kyc_status === 'verified' only.

Stats queries (direct Supabase — parallel useQuery alongside link-status query):
- Sessions count: supabase.from('workout_sessions').select('id', { count: 'exact', head: true }).eq('user_id', userId).gte('started_at', link.created_at)
- Habits %: query habits (is_active: true), query habit_logs (date: today YYYY-MM-DD), compute Math.round(logs.length / habits.length * 100)
- Both: if result is null/error show '--'; never throw on stats failure

i18n keys available from Wave 0 (plugin-sdk/src/i18n.ts) — use t() for all user-visible strings:
coach.screen_title, coach.state_a.subtitle, coach.state_a.placeholder, coach.state_a.submit, coach.state_a.error, coach.state_b.subtitle, coach.state_b.kyc_badge, coach.state_b.confirm, coach.state_b.cancel, coach.state_b.linking, coach.state_c.linked_since, coach.state_c.sessions_label, coach.state_c.progress_label, coach.state_c.revoke, coach.revoke_modal.title, coach.revoke_modal.body, coach.revoke_modal.placeholder, coach.revoke_modal.cancel, coach.revoke_modal.confirm, coach.error.link_failed, coach.error.revoke_failed, coach.error.try_again

UI-SPEC pixel values (source of truth — all values below are exact):
- Screen bg: '#F7F6F3', card bg: '#FFFFFF', border: '#E2E0DA', text: '#1C1A17', muted: '#6B6963', primary: '#FF5C1A', destructive: '#DC2626', success: '#16A34A', error-bg: '#FEF2F2', error-border: '#FCA5A5'
- Card shadow: shadowColor '#000', shadowOffset { width:0, height:2 }, shadowOpacity 0.08, shadowRadius 12, elevation 3
- Code input: height 56, borderRadius 12, fontSize 28, fontWeight '700', letterSpacing 6, textAlign 'center'
- CTA button: height 52, borderRadius 14; enabled bg '#FF5C1A'; disabled bg '#E2E0DA'
- Coach avatar: 72×72, borderRadius 36
- Chips: paddingHorizontal 8, paddingVertical 4, borderRadius 20, bg '#F7F6F3'
- ScrollView contentContainerStyle: { padding: 16, paddingBottom: 100 }
- Modal card: borderRadius 16, padding 24; backdrop rgba(0,0,0,0.5); justifyContent 'center'
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full CoachScreen implementation — 3 states, modal, stats</name>
  <files>plugins/coach/src/screens/CoachScreen.tsx</files>
  <read_first>
    - plugins/coach/src/screens/CoachScreen.tsx — current stub (Wave 0); will be fully replaced
    - .planning/workstreams/milestone-mobile/phases/28-ui-design-mon-coach/028-UI-SPEC.md — read §Component Inventory (sections 2–5) for exact pixel values; read §Interaction Contract for all event handlers; read §Copywriting Contract for t() key mapping
    - .planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-PATTERNS.md — read the CoachScreen.tsx section for confirmed import patterns, auth-token fetch pattern, Modal+TextInput pattern, date-fns usage, and showAlert usage
    - plugins/habits/src/screens/HabitsDashboardScreen.tsx — read for ScrollView+RefreshControl pattern and general screen structure
  </read_first>
  <action>
    Replace the CoachScreen stub entirely. The file implements the 3-state coach screen. Follow every pixel value from the interfaces block above. Structure:

    IMPORTS:
    - React, useState, useCallback from 'react'
    - View, Text, ScrollView, TouchableOpacity, RefreshControl, Modal, TextInput, ActivityIndicator, Image from 'react-native'
    - SafeAreaView from 'react-native-safe-area-context'
    - Ionicons from '@expo/vector-icons'
    - useQuery, useQueryClient from '@tanstack/react-query'
    - format from 'date-fns'
    - fr (locale), enUS (locale) from 'date-fns/locale'
    - useThemeStore, useTranslation, showAlert from '@ziko/plugin-sdk'
    - useAuthStore from the correct relative path to apps/mobile/src/stores/authStore

    STATE:
    - code: string (6-char input)
    - preview: CoachPreviewPayload | null (set after successful POST /preview)
    - isSubmitting: boolean (POST /preview in flight)
    - isLinking: boolean (POST /redeem in flight)
    - showError: boolean (preview returned ok:false)
    - isRevoking: boolean (DELETE in flight)
    - showRevokeModal: boolean
    - revokeInput: string (typed confirmation)
    - refreshing: boolean

    QUERY — link status:
    queryKey: ['coach-link', user?.id]
    queryFn: fetch GET ${EXPO_PUBLIC_API_URL}/coach/clients/links/me with Authorization: Bearer token from supabase.auth.getSession()
    staleTime: 30_000 (MUST NOT exceed 240_000 — signed URL TTL is 300s)
    enabled: !!user?.id

    PARALLEL QUERIES — stats (only enabled when link !== null):
    Query 1 (sessions): queryKey ['coach-sessions', user?.id, link?.created_at], direct supabase .from('workout_sessions').select('id', { count: 'exact', head: true }).eq('user_id', user.id).gte('started_at', link.created_at). On error return null.
    Query 2 (habits%): queryKey ['coach-habits-pct', user?.id], fetch habits (is_active:true) + habit_logs (date: today). Compute Math.round(completed/total*100). Return null if total === 0 or error.
    Both stats queries: if result is null show '--'. No error UI for stats.

    HANDLERS:
    handleCodeChange(v): filter v.toUpperCase().replace(/[^A-Z2-9]/g, '').slice(0, 6), setCode, reset showError to false
    handleSubmit(): set isSubmitting true, POST /coach/clients/links/preview with body { code }, if ok:true set preview(data.preview) clear showError, if ok:false set showError(true) — SAME error copy regardless of error_code (never branch), finally isSubmitting false
    handleConfirmLink(): set isLinking true, POST /coach/clients/links/redeem with body { code }, if ok:true queryClient.invalidateQueries(['coach-link', user?.id]) clear preview clear code, if ok:false showAlert(t('coach.error.link_failed'), t('coach.error.try_again')), finally isLinking false
    handleRevoke(): set isRevoking true, DELETE /coach/clients/links/${link.id}, if ok close modal queryClient.invalidateQueries(['coach-link', user?.id]) clear states return to State A, if error showAlert(t('coach.error.revoke_failed'), t('coach.error.try_again')), finally isRevoking false

    LOADING GUARD: if isLoading (initial fetch only, not refetch) return SafeAreaView with ActivityIndicator size="large" color="#FF5C1A" centered.

    RENDER LOGIC: determine state from data:
    - preview !== null (local state) → render State B
    - data?.link !== null → render State C
    - else → render State A
    Wrap all three in SafeAreaView (bg '#F7F6F3') + ScrollView (contentContainerStyle padding 16 paddingBottom 100) + RefreshControl tintColor '#FF5C1A'

    STATE A (CodeEntryView) — implement exactly per UI-SPEC §2:
    - Screen title Text 28/700 '#1C1A17': t('coach.screen_title')
    - Subtitle Text 14/400 '#6B6963': t('coach.state_a.subtitle')
    - TextInput: height 56, borderRadius 12, bg '#FFFFFF', border 1 '#E2E0DA' (focus '#FF5C1A'), padding 16, fontSize 28, fontWeight '700', letterSpacing 6, textAlign 'center', autoCapitalize 'characters', maxLength 6, placeholder t('coach.state_a.placeholder'), placeholderTextColor '#6B6963', accessibilityLabel t('coach.state_a.placeholder'), accessibilityHint t('coach.state_a.subtitle'), onChangeText handleCodeChange, value code
    - Error container (visible when showError): bg '#FEF2F2', border 1 '#FCA5A5', borderRadius 10, padding 12, marginTop 8; row with Ionicons 'alert-circle-outline' size 16 color '#DC2626' + Text 14/400 '#DC2626': t('coach.state_a.error')
    - Submit CTA TouchableOpacity: height 52, borderRadius 14, marginTop 24, enabled when code.length === 6 && !isSubmitting; enabled bg '#FF5C1A' disabled bg '#E2E0DA'; label Text 16/700 enabled '#FFFFFF' disabled '#6B6963': t('coach.state_a.submit'); accessibilityRole "button", accessibilityState {{ disabled: code.length < 6 }}; loading: ActivityIndicator color '#FFFFFF'

    STATE B (CoachPreviewCard) — implement exactly per UI-SPEC §3:
    Card container: bg '#FFFFFF', borderRadius 16, border 1 '#E2E0DA', shadow contract (shadowColor '#000', shadowOffset {width:0,height:2}, shadowOpacity 0.08, shadowRadius 12, elevation 3), padding 16, marginTop 24
    - Screen title above card (same as State A)
    - Subtitle Text 14/400 '#6B6963': t('coach.state_b.subtitle')
    - Avatar row (flexDirection 'row', alignItems 'center'): Image 72×72 borderRadius 36 source={{ uri: preview.photo_signed_url }} with fallback Ionicons 'person-circle-outline' size 72 color '#6B6963' when no URL; right column marginLeft 12: coach name Text 20/700 '#1C1A17'; KYC badge row (when kyc_status === 'verified'): Ionicons 'shield-checkmark' size 16 color '#16A34A' + Text 14/400 '#16A34A': t('coach.state_b.kyc_badge'), accessibilityLabel t('coach.state_b.kyc_badge') on wrapping View
    - Chips (flexDirection 'row', flexWrap 'wrap', gap 8, marginTop 12): each chip paddingHorizontal 8 paddingVertical 4 borderRadius 20 bg '#F7F6F3' border 1 '#E2E0DA'; Text 14/400 '#6B6963'
    - Bio Text 14/400 '#6B6963' lineHeight 20 marginTop 12 (skip if bio is null)
    - Divider View height 1 bg '#E2E0DA' marginTop 16
    - CTA row marginTop 16: 'Lier mon compte' full-width height 52 borderRadius 14 bg '#FF5C1A'; Text 16/700 '#FFFFFF': t('coach.state_b.confirm') or ActivityIndicator when isLinking
    - Cancel link TouchableOpacity marginTop 12 alignItems 'center': Text 14/400 '#FF5C1A': t('coach.state_b.cancel'); onPress: clear preview and code

    STATE C (LinkedCoachCard) — implement exactly per UI-SPEC §4:
    Card container: same shadow + border as State B, padding 16
    - Screen title above card
    - Avatar row: identical to State B (uses data.preview.photo_signed_url + data.preview.display_name)
    - Chips: identical to State B (read-only)
    - Stats row (flexDirection 'row', marginTop 12, gap 16): two stat blocks each with value Text 20/700 '#1C1A17' (sessionsCount ?? '--', habitsPct !== null ? habitsPct + '%' : '--') and label Text 12/400 '#6B6963' (t('coach.state_c.sessions_label'), t('coach.state_c.progress_label'))
    - "Lié depuis" row (flexDirection 'row', alignItems 'center', marginTop 12): Ionicons 'calendar-outline' size 16 color '#6B6963' marginRight 8; Text 14/400 '#6B6963': t('coach.state_c.linked_since').replace('{{date}}', formatted date using date-fns format(new Date(link.created_at), locale === 'fr' ? 'dd/MM/yyyy' : 'MM/dd/yyyy', { locale: locale === 'fr' ? fr : enUS })). Get current locale from useTranslation() if it exposes locale, otherwise default to 'fr'.
    - Divider View height 1 bg '#E2E0DA' marginTop 16
    - "Retirer ce coach" TouchableOpacity marginTop 16 flexDirection 'row' alignItems 'center' justifyContent 'center': Ionicons 'trash-outline' size 18 color '#DC2626' marginRight 8; Text 14/400 '#DC2626': t('coach.state_c.revoke'); accessibilityRole "button", accessibilityLabel t('coach.state_c.revoke'); onPress: setShowRevokeModal(true)

    CONFIRM REVOCATION MODAL — implement exactly per UI-SPEC §5:
    Modal transparent animationType 'fade', visible showRevokeModal
    Backdrop View: flex 1, bg 'rgba(0,0,0,0.5)', justifyContent 'center', padding 24
    Modal card View: bg '#FFFFFF', borderRadius 16, padding 24
    - Title Text 20/700 '#1C1A17': t('coach.revoke_modal.title')
    - Body Text 14/400 '#6B6963' marginTop 8: t('coach.revoke_modal.body')
    - TextInput: height 48, borderRadius 10, border 1 '#E2E0DA' (focus '#FF5C1A'), padding 12, fontSize 16, autoCapitalize 'characters', autoCorrect false, placeholder t('coach.revoke_modal.placeholder'), value revokeInput, onChangeText setRevokeInput
    - Button row flexDirection 'row' gap 12 marginTop 16:
      "Garder mon coach" TouchableOpacity flex 1 height 48 borderRadius 12 border 1 '#E2E0DA' alignItems 'center' justifyContent 'center': Text 16/400 '#1C1A17': t('coach.revoke_modal.cancel'); onPress: setShowRevokeModal(false), setRevokeInput('')
      "Confirmer" TouchableOpacity flex 1 height 48 borderRadius 12: enabled (revokeInput.trim() === 'COACH'): bg '#DC2626'; disabled: bg '#E2E0DA'; Text 16/700 enabled '#FFFFFF' disabled '#6B6963': t('coach.revoke_modal.confirm'); disabled prop when revokeInput.trim() !== 'COACH' || isRevoking; onPress: handleRevoke

    CRITICAL RULES:
    - Use showAlert (not Alert.react-native) for all non-modal error alerts
    - The ConfirmRevocationModal is the ONE exception — it uses custom Modal + TextInput
    - No StyleSheet.create — inline styles only
    - All icons are Ionicons (no emoji, no other icon libs)
    - paddingBottom: 100 on ScrollView contentContainerStyle
    - staleTime: 30_000 on the link-status query (signed URL TTL safety)
    - coach.state_a.error is used for ALL error codes from /preview — never branch on error_code
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | grep -i error | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0
    - `grep "Alert.alert" plugins/coach/src/screens/CoachScreen.tsx` returns 0 matches (showAlert used instead)
    - `grep "StyleSheet" plugins/coach/src/screens/CoachScreen.tsx` returns 0 matches
    - `grep "paddingBottom: 100" plugins/coach/src/screens/CoachScreen.tsx` returns at least 1 match
    - `grep "staleTime: 30_000" plugins/coach/src/screens/CoachScreen.tsx` returns 1 match
    - `grep "coach.state_a.error" plugins/coach/src/screens/CoachScreen.tsx` returns at least 1 match and no companion branching on error_code or status codes other than the single error flag
    - `grep "revokeInput.trim() === 'COACH'" plugins/coach/src/screens/CoachScreen.tsx` returns 1 match
    - `grep "/coach/clients/links/me" plugins/coach/src/screens/CoachScreen.tsx` returns 1 match
    - `grep "/coach/clients/links/preview" plugins/coach/src/screens/CoachScreen.tsx` returns 1 match
    - `grep "/coach/clients/links/redeem" plugins/coach/src/screens/CoachScreen.tsx` returns 1 match
    - `grep "DELETE" plugins/coach/src/screens/CoachScreen.tsx` returns 1 match (the revoke fetch)
  </acceptance_criteria>
  <done>CoachScreen.tsx fully implements all 3 states (A/B/C), ConfirmRevocationModal, stats queries, loading state, pull-to-refresh. All pixel values match 028-UI-SPEC.md. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| State A input → POST /preview | User-supplied code is sent as body { code }; filtered to [A-Z2-9] max 6 chars before submission |
| Modal TextInput → DELETE /links/:id | User must type exact string "COACH" before confirm button activates; link.id comes from authenticated GET response |
| signed URL (photo_signed_url) | 5-min TTL; staleTime 30s prevents stale broken image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Information Disclosure | State A error copy | mitigate | Single t('coach.state_a.error') key for all error codes (404/410/400/5xx/network) — no branching on error_code; backend also uses constant-time response |
| T-29-06 | Elevation of Privilege | ConfirmRevocationModal | mitigate | Confirm button disabled until revokeInput.trim() === 'COACH' (exact uppercase); disabled prop on TouchableOpacity; accessibilityState disabled |
| T-29-07 | Elevation of Privilege | DELETE /coach/clients/links/:id | mitigate | Auth middleware validates Bearer token; RLS enforces client_id = auth.uid(); link.id sourced from authenticated GET, not user input |
| T-29-08 | Denial of Service | photo_signed_url 5-min TTL | mitigate | staleTime: 30_000 ensures query refetches before URL expires on focus |
| T-29-SC | Tampering | npm installs | accept | No new packages; date-fns and @tanstack/react-query already hoisted |
</threat_model>

<verification>
After Plan 29-03 completes:
- npm run type-check exits 0
- No Alert.react-native usage in CoachScreen.tsx
- No StyleSheet usage in CoachScreen.tsx
- 4 correct endpoint paths in CoachScreen.tsx (links/me, links/preview, links/redeem, links/:id DELETE)
- Single error key coach.state_a.error for all preview errors (no error_code branching)
- Revoke confirm gated on exact 'COACH' match
- Stats show '--' on null/error
</verification>

<success_criteria>
CoachScreen.tsx is a full production implementation (not a stub) that:
1. Shows loading spinner while initial query resolves
2. State A: 6-char filtered input, disabled CTA below 6 chars, constant-time error copy
3. State B: coach preview card matching UI-SPEC §3, redeem CTA, cancel link
4. State C: linked card with date-fns formatted link date, sessions + habits% stats row (both '--' on failure), revoke trigger
5. ConfirmRevocationModal: centered modal, TextInput, confirm enabled only on exact 'COACH', DELETE call on confirm
6. Pull-to-refresh wired to refetch()
7. No Alert.react-native, no StyleSheet, all Ionicons, paddingBottom 100
8. TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/29-plugin-mon-coach-full-impl/29-03-SUMMARY.md` when done
</output>
