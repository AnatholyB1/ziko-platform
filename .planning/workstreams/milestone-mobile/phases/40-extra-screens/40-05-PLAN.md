---
plan: 40-05
phase: 40
wave: 2
depends_on:
  - 40-01
  - 40-02
  - 40-03
  - 40-04
autonomous: true
files_modified:
  - packages/ui/src/components/EmptyState.tsx
  - packages/ui/src/components/ErrorScreen.tsx
  - packages/ui/src/index.ts
  - apps/mobile/app/(app)/notifications.tsx
  - apps/mobile/app/(app)/calendar.tsx
  - apps/mobile/src/components/SearchOverlay.tsx
  - apps/mobile/app/(app)/ai/chat.tsx
requirements:
  - EXTRA-07
  - EXTRA-08
must_haves:
  goal: EmptyState et ErrorScreen sont créés dans packages/ui avec 4 variants chacun, exportés depuis l'index, et intégrés dans au moins 2 écrans de la Phase 40.
  truths:
    - packages/ui/src/components/EmptyState.tsx existe avec les variants no-data / error / offline / no-results
    - packages/ui/src/components/ErrorScreen.tsx existe avec les variants generic / network / auth / not-found
    - Les deux composants sont exportés depuis packages/ui/src/index.ts
    - EmptyState est importé et utilisé dans notifications.tsx (état vide)
    - EmptyState est importé et utilisé dans calendar.tsx (aucune séance ce mois)
    - EmptyState est importé et utilisé dans SearchOverlay.tsx (aucun résultat)
    - ErrorScreen est importé et utilisé dans au moins 2 écrans data-driven de la Phase 40
  artifacts:
    - path: packages/ui/src/components/EmptyState.tsx
      provides: Composant EmptyState avec 4 variants et props title/message/ctaLabel/onCta
      exports: [EmptyState]
    - path: packages/ui/src/components/ErrorScreen.tsx
      provides: Composant ErrorScreen plein écran avec 4 variants et props onRetry/onGoBack
      exports: [ErrorScreen]
    - path: packages/ui/src/index.ts
      provides: Exports EmptyState + ErrorScreen ajoutés
      exports: [EmptyState, ErrorScreen]
  key_links:
    - from: packages/ui/src/index.ts
      to: EmptyState.tsx + ErrorScreen.tsx
      via: "export { EmptyState } from './components/EmptyState'; export { ErrorScreen } from './components/ErrorScreen';"
    - from: notifications.tsx
      to: EmptyState
      via: "import { EmptyState } from '@ziko/ui'; <EmptyState variant='no-data' title='Pas encore de notifications' />"
    - from: calendar.tsx
      to: EmptyState
      via: "import { EmptyState } from '@ziko/ui'; <EmptyState variant='no-data' title='Aucune séance ce mois' />"
---

<objective>
Créer les deux composants partagés EmptyState et ErrorScreen dans packages/ui, les exporter depuis l'index, puis les intégrer dans les écrans Phase 40 qui affichent actuellement des états vides ou d'erreur inline non-standardisés.

Purpose: EXTRA-07 et EXTRA-08 requièrent des composants standardisés pour les états vides et d'erreur. Wave 2 car les écrans Phase 40 (plans 40-01 à 40-04) doivent exister avant d'y injecter les imports.

Output: EmptyState.tsx, ErrorScreen.tsx créés ; index.ts mis à jour ; 3 intégrations EmptyState + 2 intégrations ErrorScreen dans les écrans Phase 40.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-CONTEXT.md

<interfaces>
EmptyState props :
```typescript
interface EmptyStateProps {
  variant: 'no-data' | 'error' | 'offline' | 'no-results';
  title: string;
  message?: string;
  ctaLabel?: string;
  onCta?: () => void;
}
```

Variant → icône Ionicons + couleur :
- 'no-data'    → 'file-tray-outline'   color '#E2E0DA'
- 'error'      → 'warning-outline'     color '#F59E0B'
- 'offline'    → 'wifi-outline'        color '#6B6963' (barré visuellement)
- 'no-results' → 'search-outline'      color '#E2E0DA'

ErrorScreen props :
```typescript
interface ErrorScreenProps {
  variant: 'generic' | 'network' | 'auth' | 'not-found';
  onRetry?: () => void;
  onGoBack?: () => void;
}
```

Variant → icône + titre + message par défaut :
- 'generic'  → 'alert-circle-outline'  'Une erreur est survenue'          'Quelque chose s\'est mal passé. Réessaie.'
- 'network'  → 'cloud-offline-outline' 'Pas de connexion'                 'Vérifie ta connexion internet et réessaie.'
- 'auth'     → 'lock-closed-outline'   'Session expirée'                  'Reconnecte-toi pour continuer.'
- 'not-found'→ 'compass-outline'       'Page introuvable'                 'Ce contenu n\'existe plus ou a été déplacé.'

Tokens design :
- background: #F7F6F3, surface: #FFFFFF, border: #E2E0DA, text: #1C1A17, muted: #6B6963, primary: #FF5C1A
- EmptyState centré dans son conteneur (flex:1, alignItems center, justifyContent center, paddingVertical 40)
- ErrorScreen plein écran (flex:1, backgroundColor '#F7F6F3', alignItems center, justifyContent center, padding 32)
- Icône EmptyState : size 56, margin auto
- Icône ErrorScreen : size 72, marginBottom 16
- CTA button : backgroundColor '#FF5C1A' borderRadius 12 paddingH 24 paddingV 12 marginTop 16
- Bouton Retour (ErrorScreen) : Text "Retour" color '#6B6963' fontWeight '600' marginTop 8

Packages existants dans @ziko/ui (ne pas les casser) :
- SubTabs, AISuggestion, PluginHeader, WeekStrip, BugFab — déjà exportés
- Vérifier packages/ui/src/index.ts avant modification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Créer EmptyState.tsx + ErrorScreen.tsx + mettre à jour packages/ui/src/index.ts</name>
  <files>
    packages/ui/src/components/EmptyState.tsx,
    packages/ui/src/components/ErrorScreen.tsx,
    packages/ui/src/index.ts
  </files>
  <read_first>
    - packages/ui/src/index.ts — lire le fichier entier pour voir les exports existants et ne pas les casser
    - packages/ui/src/components/ — lister les composants existants (ls) pour voir le pattern de fichier
  </read_first>
  <action>
    CRÉER packages/ui/src/components/EmptyState.tsx :

    ```
    import React from 'react';
    import { View, Text, TouchableOpacity } from 'react-native';
    import { Ionicons } from '@expo/vector-icons';

    type EmptyStateVariant = 'no-data' | 'error' | 'offline' | 'no-results';

    interface EmptyStateProps {
      variant: EmptyStateVariant;
      title: string;
      message?: string;
      ctaLabel?: string;
      onCta?: () => void;
    }
    ```

    MAPPING VARIANT → config :
    - Objet VARIANT_CONFIG avec les 4 entrées (icon, color) selon interfaces
    - Pour 'offline' : icône 'wifi-outline' avec une View superposée pour simuler le barré (View height 2 backgroundColor '#6B6963' position absolute rotation '45deg' — approximation simple)

    LAYOUT EMPTYSTATE :
    - View flex:1 alignItems 'center' justifyContent 'center' paddingVertical 40 paddingHorizontal 24
    - View 80×80 borderRadius 999 backgroundColor '#F7F6F3' alignItems 'center' justifyContent 'center' marginBottom 16 ; Ionicons name=config.icon size 36 color=config.color
    - Text title fontWeight '700' fontSize 17 color '#1C1A17' textAlign 'center' marginBottom 4
    - Si message → Text message fontSize 14 color '#6B6963' textAlign 'center' lineHeight 20
    - Si ctaLabel + onCta → TouchableOpacity backgroundColor '#FF5C1A' borderRadius 12 paddingHorizontal 24 paddingVertical 12 marginTop 16 ; Text ctaLabel fontWeight '700' color '#FFFFFF' fontSize 14

    Export : export function EmptyState(props: EmptyStateProps) { ... }
    Et export default EmptyState

    -------

    CRÉER packages/ui/src/components/ErrorScreen.tsx :

    ```
    import React from 'react';
    import { View, Text, TouchableOpacity, SafeAreaView } from 'react-native';
    import { Ionicons } from '@expo/vector-icons';

    type ErrorScreenVariant = 'generic' | 'network' | 'auth' | 'not-found';

    interface ErrorScreenProps {
      variant: ErrorScreenVariant;
      onRetry?: () => void;
      onGoBack?: () => void;
    }
    ```

    MAPPING VARIANT → config :
    - Objet VARIANT_CONFIG avec les 4 entrées (icon, title, message) selon interfaces

    LAYOUT ERRORSCREEN :
    - SafeAreaView flex:1 backgroundColor '#F7F6F3'
    - View flex:1 alignItems 'center' justifyContent 'center' padding 32
    - View 96×96 borderRadius 999 backgroundColor surface alignItems center justifyContent center shadow standard ; Ionicons name=config.icon size 40 color='#6B6963'
    - Text config.title fontWeight '800' fontSize 20 color '#1C1A17' textAlign 'center' marginTop 20 marginBottom 8
    - Text config.message fontSize 15 color '#6B6963' textAlign 'center' lineHeight 22
    - Si onRetry → TouchableOpacity backgroundColor '#FF5C1A' borderRadius 14 paddingH 32 paddingV 14 marginTop 24 ; Text "Réessayer" fontWeight '700' color '#FFFFFF' fontSize 15
    - Si onGoBack → TouchableOpacity marginTop 8 paddingVertical 8 ; Text "Retour" fontWeight '600' color '#6B6963' fontSize 14

    Export : export function ErrorScreen(props: ErrorScreenProps) { ... }
    Et export default ErrorScreen

    -------

    METTRE À JOUR packages/ui/src/index.ts :
    - Lire le fichier actuel (déjà fait en read_first)
    - Ajouter après les derniers exports :
      export { EmptyState } from './components/EmptyState';
      export type { } from './components/EmptyState'; (si types exportés)
      export { ErrorScreen } from './components/ErrorScreen';
    - Ne pas supprimer les exports existants (SubTabs, AISuggestion, PluginHeader, WeekStrip, BugFab, etc.)
  </action>
  <verify>
    <automated>grep -c 'EmptyState\|ErrorScreen' /c/ziko-platform/packages/ui/src/index.ts</automated>
  </verify>
  <done>EmptyState.tsx et ErrorScreen.tsx créés avec 4 variants chacun ; packages/ui/src/index.ts exporte les deux ; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2 : Intégrer EmptyState dans 3 écrans et ErrorScreen dans 2 écrans Phase 40</name>
  <files>
    apps/mobile/app/(app)/notifications.tsx,
    apps/mobile/app/(app)/calendar.tsx,
    apps/mobile/src/components/SearchOverlay.tsx,
    apps/mobile/app/(app)/ai/chat.tsx,
    apps/mobile/app/(app)/store/index.tsx
  </files>
  <read_first>
    - apps/mobile/app/(app)/notifications.tsx — localiser l'état vide actuel (texte inline ou View centré) à remplacer
    - apps/mobile/app/(app)/calendar.tsx — localiser l'état vide (aucune séance) à remplacer
    - apps/mobile/src/components/SearchOverlay.tsx — localiser les messages "Aucun résultat" à remplacer
    - apps/mobile/app/(app)/ai/chat.tsx — localiser l'état d'erreur ou vide de conversation
    - apps/mobile/app/(app)/store/index.tsx — localiser l'état d'erreur du chargement plugins
  </read_first>
  <action>
    Pour chaque fichier, ajouter l'import depuis '@ziko/ui' et remplacer les états vides/erreurs inline par les composants standardisés.

    NOTIFICATIONS.TSX :
    - Ajouter : import { EmptyState } from '@ziko/ui';
    - Remplacer le bloc View centré "Pas encore de notifications" par :
      <EmptyState variant="no-data" title="Pas encore de notifications" message="Elles apparaîtront ici dès qu'il y aura de l'activité" />
    - Si le fichier a déjà un bloc erreur (isError) → remplacer par :
      <EmptyState variant="error" title="Impossible de charger" message="Vérifie ta connexion" ctaLabel="Réessayer" onCta={() => refetch()} />

    CALENDAR.TSX :
    - Ajouter : import { EmptyState } from '@ziko/ui';
    - Remplacer le message "Aucune séance" par :
      <EmptyState variant="no-data" title="Aucune séance ce mois" message="Commence un entraînement pour le voir apparaître ici" ctaLabel="Démarrer une séance" onCta={() => router.push('/(app)/workout')} />

    SEARCHOVERLAY.TSX :
    - Ajouter : import { EmptyState } from '@ziko/ui';
    - Remplacer les textes "Aucun exercice/programme/utilisateur trouvé" dans chaque section par un <EmptyState variant="no-results" title="Aucun résultat" /> compact (sans message ni CTA, dans un View height 80)
    - L'état initial (query < 2 chars) reste un texte simple (pas EmptyState — trop verbeux)

    AI/CHAT.TSX :
    - Ajouter : import { ErrorScreen } from '@ziko/ui';
    - Si le fichier a un état d'erreur (isError sur une query) → remplacer par :
      <ErrorScreen variant="network" onRetry={() => refetch()} />
    - Sinon (si aucune gestion d'erreur) → ajouter autour de la query principale :
      if (isError) return <ErrorScreen variant="generic" onRetry={() => refetch()} onGoBack={() => router.back()} />

    STORE/INDEX.TSX :
    - Ajouter : import { ErrorScreen } from '@ziko/ui';
    - Localiser la gestion d'erreur du chargement plugins_registry ; remplacer par :
      if (isError) return <ErrorScreen variant="network" onRetry={() => refetch()} />

    Pour chaque fichier : ne pas casser les imports existants. L'ajout de import { EmptyState } ou import { ErrorScreen } depuis '@ziko/ui' doit s'intégrer à la ligne imports '@ziko/ui' existante (ajouter à la déstructuration) si elle existe déjà, sinon ajouter une nouvelle ligne d'import.
  </action>
  <verify>
    <automated>grep -rl 'EmptyState\|ErrorScreen' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx /c/ziko-platform/apps/mobile/src/components/SearchOverlay.tsx /c/ziko-platform/apps/mobile/app/\(app\)/ai/chat.tsx /c/ziko-platform/apps/mobile/app/\(app\)/store/index.tsx | wc -l</automated>
  </verify>
  <done>EmptyState intégré dans notifications.tsx, calendar.tsx, SearchOverlay.tsx ; ErrorScreen intégré dans ai/chat.tsx et store/index.tsx ; tous importent depuis '@ziko/ui' ; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EmptyState/ErrorScreen → rendu local | Composants purement visuels, aucune donnée réseau, aucune surface d'attaque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-40-05-01 | Tampering | packages/ui exports | accept | Composants sont des exports purs sans logique métier ; aucune surface d'attaque |
| T-40-05-SC | Tampering | npm/pip/cargo installs | mitigate | Aucun nouveau package installé — utilise @expo/vector-icons et react-native déjà présents |
</threat_model>

<verification>
1. EmptyState créé : `test -f /c/ziko-platform/packages/ui/src/components/EmptyState.tsx && echo OK`
2. ErrorScreen créé : `test -f /c/ziko-platform/packages/ui/src/components/ErrorScreen.tsx && echo OK`
3. Index exports : `grep 'EmptyState\|ErrorScreen' /c/ziko-platform/packages/ui/src/index.ts | wc -l` retourne >= 2
4. 4 variants EmptyState : `grep 'no-data\|offline\|no-results\|error' /c/ziko-platform/packages/ui/src/components/EmptyState.tsx | wc -l` retourne >= 4
5. 4 variants ErrorScreen : `grep 'generic\|network\|auth\|not-found' /c/ziko-platform/packages/ui/src/components/ErrorScreen.tsx | wc -l` retourne >= 4
6. Intégrations 5 fichiers : `grep -rl 'EmptyState\|ErrorScreen' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx /c/ziko-platform/apps/mobile/src/components/SearchOverlay.tsx /c/ziko-platform/apps/mobile/app/\(app\)/ai/chat.tsx /c/ziko-platform/apps/mobile/app/\(app\)/store/index.tsx | wc -l` retourne 5
7. TypeScript : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep 'EmptyState\|ErrorScreen\|packages/ui' | head -10`
</verification>

<success_criteria>
- EmptyState.tsx et ErrorScreen.tsx créés dans packages/ui/src/components/ avec 4 variants chacun
- Les deux exportés depuis packages/ui/src/index.ts sans casser les exports existants
- EmptyState utilisé dans notifications.tsx, calendar.tsx, SearchOverlay.tsx
- ErrorScreen utilisé dans ai/chat.tsx et store/index.tsx
- TypeScript : zéro nouvelle erreur
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-05-SUMMARY.md` une fois terminé.
</output>
