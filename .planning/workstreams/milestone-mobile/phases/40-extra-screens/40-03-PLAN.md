---
plan: 40-03
phase: 40
wave: 1
depends_on: []
autonomous: true
files_modified:
  - apps/mobile/app/(app)/calendar.tsx
  - apps/mobile/src/components/SearchOverlay.tsx
  - apps/mobile/app/(app)/help.tsx
requirements:
  - EXTRA-04
  - EXTRA-05
  - EXTRA-06
must_haves:
  goal: CalendarScreen supprime buildMockSessions et utilise TanStack Query sur workout_sessions pour le mois courant ; SearchOverlay garantit 3 sections de résultats avec debounce 300ms ; HelpScreen est redessinée avec le système STGroup/STRow.
  truths:
    - La fonction buildMockSessions est supprimée de calendar.tsx
    - CalendarScreen charge workout_sessions du mois courant via TanStack Query groupées par date
    - La navigation prev/next entre les mois modifie la requête (mois dynamique)
    - Un tap sur un jour avec session navigue vers l'historique workout
    - SearchOverlay affiche 3 sections distinctes — Exercices / Programmes / Utilisateurs
    - La recherche est déclenchée après 300ms de debounce et minimum 2 caractères
    - Si aucun résultat dans une section → composant EmptyState ou message "Aucun résultat"
    - help.tsx utilise le système STGroup/STRow pour les catégories d'aide (contenu statique conservé)
  artifacts:
    - path: apps/mobile/app/(app)/calendar.tsx
      provides: CalendarScreen avec TanStack Query workout_sessions, navigation mois, tap vers historique
      exports: [default CalendarScreen]
    - path: apps/mobile/src/components/SearchOverlay.tsx
      provides: SearchOverlay avec 3 sections, debounce 300ms, min 2 chars
      exports: [SearchOverlay]
    - path: apps/mobile/app/(app)/help.tsx
      provides: HelpScreen redessinée avec STGroup/STRow
      exports: [default HelpScreen]
  key_links:
    - from: calendar.tsx
      to: workout_sessions
      via: "useQuery ['calendar', userId, year, month] → SELECT id, created_at, program_data FROM workout_sessions WHERE user_id = ? AND DATE_TRUNC('month', created_at) = ?"
    - from: SearchOverlay.tsx (section Exercices)
      to: exercises table
      via: "useQuery ['search_exercises', query] → SELECT id, name, category, muscle_groups FROM exercises WHERE name ILIKE '%?%' LIMIT 10"
    - from: SearchOverlay.tsx (section Programmes)
      to: ai_generated_programs
      via: "useQuery ['search_programs', userId, query] → SELECT id, goal, created_at FROM ai_generated_programs WHERE user_id = ? AND goal ILIKE '%?%' LIMIT 5"
    - from: SearchOverlay.tsx (section Utilisateurs)
      to: user_profiles
      via: "useQuery ['search_users', query] → SELECT id, name, avatar_url FROM user_profiles WHERE name ILIKE '%?%' LIMIT 5"
---

<objective>
Livrer trois écrans redessinés : CalendarScreen sans fixture avec données réelles de workout_sessions ; SearchOverlay garantissant 3 sections de résultats (exercices/programmes/utilisateurs) avec debounce 300ms ; HelpScreen redessinée avec le pattern STGroup/STRow.

Purpose: CalendarScreen utilise buildMockSessions (fixture), SearchOverlay peut manquer des sections ou du debounce, HelpScreen a besoin du redesign STGroup/STRow. Ce plan aligne EXTRA-04, EXTRA-05, EXTRA-06 en un seul passage.

Output: calendar.tsx sans fixture, SearchOverlay.tsx avec 3 sections debounced, help.tsx avec STGroup/STRow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-CONTEXT.md

<interfaces>
Tables DB :
- workout_sessions : id UUID, user_id UUID, created_at TIMESTAMPTZ, notes TEXT, program_data JSONB (ou champs séparés type, name)
- exercises : id UUID, name TEXT, category TEXT, muscle_groups TEXT[]
- ai_generated_programs : id UUID, user_id UUID, goal TEXT, created_at TIMESTAMPTZ, program_data JSONB
- user_profiles : id UUID, name TEXT, avatar_url TEXT

Tokens design :
- background: #F7F6F3, surface: #FFFFFF, border: #E2E0DA, text: #1C1A17, muted: #6B6963, primary: #FF5C1A
- Jour avec session : backgroundColor '#FF5C1A' color '#FFFFFF' borderRadius 8 fontWeight '700'
- Jour aujourd'hui sans session : borderWidth 1.5 borderColor '#FF5C1A' color '#FF5C1A' borderRadius 8
- STGroup : View backgroundColor surface borderRadius 16 borderWidth 1 borderColor '#E2E0DA' overflow 'hidden' marginBottom 12 shadow standard
- STRow : View flexDirection 'row' alignItems 'center' padding 14 borderBottomWidth 1 borderBottomColor '#E2E0DA' ; Text label flex:1 fontSize 15 color '#1C1A17' ; right element (Text/Ionicons/Switch)
- Dernière STRow dans un groupe : borderBottomWidth 0

Debounce pattern (React Native) :
  const [query, setQuery] = useState('')
  const [debouncedQuery, setDebouncedQuery] = useState('')
  useEffect(() => {
    const t = setTimeout(() => setDebouncedQuery(query), 300)
    return () => clearTimeout(t)
  }, [query])
  // Utiliser debouncedQuery dans les useQuery (enabled: debouncedQuery.length >= 2)

Calendar — groupe sessions par date :
  const sessionsByDate = useMemo(() => {
    const map: Record<string, boolean> = {}
    data?.forEach(s => {
      const d = new Date(s.created_at).toISOString().split('T')[0]
      map[d] = true
    })
    return map
  }, [data])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Redesign CalendarScreen — suppression buildMockSessions, TanStack Query workout_sessions, navigation mois</name>
  <files>apps/mobile/app/(app)/calendar.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/calendar.tsx — lire les 208 lignes complètes pour identifier : la fonction buildMockSessions, la structure du calendrier (rendu des jours), la navigation mois, les imports
  </read_first>
  <action>
    Réécrire calendar.tsx en conservant la structure visuelle du calendrier mais en remplaçant entièrement la couche de données.

    SUPPRESSION : retirer buildMockSessions et toute constante de sessions fictives.

    STATE NAVIGATION MOIS :
    - const [currentDate, setCurrentDate] = useState(new Date())
    - const year = currentDate.getFullYear() ; const month = currentDate.getMonth() (0-indexed)
    - Boutons prev/next : setCurrentDate(new Date(year, month - 1, 1)) / new Date(year, month + 1, 1)
    - Header : Text mois en format 'MMMM yyyy' (fr-FR via toLocaleDateString ou date-fns si disponible)

    TANSTACK QUERY :
    - const startOfMonth = new Date(year, month, 1).toISOString()
    - const endOfMonth = new Date(year, month + 1, 0, 23, 59, 59).toISOString()
    - useQuery clé ['calendar', userId, year, month] :
      SELECT id, created_at FROM workout_sessions WHERE user_id = ? AND created_at >= ? AND created_at <= ? ORDER BY created_at ASC
    - refetchOnWindowFocus: false

    GROUPE PAR DATE : useMemo → sessionsByDate (voir interfaces)

    RENDU DU CALENDRIER :
    - Calculer les jours du mois : daysInMonth = new Date(year, month + 1, 0).getDate()
    - Calculer le premier jour de la semaine : firstDayIndex = new Date(year, month, 1).getDay() (0=dim, adapter si semaine commence lundi)
    - Grille 7 colonnes (FlatList ou View grid) avec numColumns={7}
    - En-tête colonnes : L M M J V S D
    - Chaque cellule : View flex:1 aspectRatio:1 alignItems 'center' justifyContent 'center' margin 2
    - Jour avec session (sessionsByDate[dateStr]) → View 34×34 borderRadius 8 backgroundColor '#FF5C1A' ; Text date fontWeight '700' color '#FFFFFF'
    - Aujourd'hui sans session → borderWidth 1.5 borderColor '#FF5C1A' color '#FF5C1A'
    - Jour ordinaire → Text date color '#1C1A17'
    - Jour vide (padding avant premier) → View vide

    NAVIGATION SUR TAP :
    - Si sessionsByDate[dateStr] → router.push('/(app)/workout/history') avec params date=dateStr
    - Sinon → feedback visuel (légère opacité réduite, pas d'action)

    SECTION RÉSUMÉ MOIS :
    - Text `${Object.keys(sessionsByDate).length} séances en ${moisLabel}` fontSize 15 color '#6B6963' marginTop 16 textAlign 'center'

    ÉTATS : isLoading → ActivityIndicator color='#FF5C1A' ; error → Text "Impossible de charger le calendrier"
    paddingBottom: 100 sur contentContainerStyle du ScrollView racine.
  </action>
  <verify>
    <automated>grep -c 'buildMockSessions' /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx</automated>
  </verify>
  <done>calendar.tsx sans buildMockSessions, TanStack Query sur workout_sessions, navigation mois prev/next, tap jour avec session → historique, résumé mois ; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2 : Compléter SearchOverlay — 3 sections debounced + redesign HelpScreen STGroup/STRow</name>
  <files>
    apps/mobile/src/components/SearchOverlay.tsx,
    apps/mobile/app/(app)/help.tsx
  </files>
  <read_first>
    - apps/mobile/src/components/SearchOverlay.tsx — lire les 240 lignes pour identifier : sections existantes, gestion du debounce, états vides, imports
    - apps/mobile/app/(app)/help.tsx — lire les 284 lignes pour identifier : le contenu statique existant, la structure actuelle, les imports
  </read_first>
  <action>
    SEARCHOVERLAY :

    Identifier les gaps vs la spec (3 sections, debounce 300ms, min 2 chars). Compléter/réécrire les parties manquantes.

    DEBOUNCE (si absent) :
    - Ajouter useState debouncedQuery avec useEffect setTimeout 300ms (voir interfaces)
    - Toutes les useQuery doivent utiliser debouncedQuery avec enabled: debouncedQuery.length >= 2

    3 SECTIONS GARANTIES :

    Section 1 — EXERCICES :
    - useQuery ['search_exercises', debouncedQuery] : SELECT id, name, category FROM exercises WHERE name ILIKE '%${debouncedQuery}%' LIMIT 10
    - Titre section "Exercices" fontSize 11 fontWeight '700' color '#6B6963' letterSpacing 0.06 textTransform 'uppercase' marginBottom 8 marginTop 16
    - Si exercises.length > 0 → RowList : Ionicons 'barbell-outline' 20px color '#FF5C1A', Text exercise.name fontWeight '600', Text exercise.category fontSize 12 color '#6B6963'
    - Si exercises vide et query.length >= 2 → Text "Aucun exercice trouvé" fontSize 13 color '#6B6963' marginVertical 8

    Section 2 — PROGRAMMES :
    - useQuery ['search_programs', userId, debouncedQuery] : SELECT id, goal, created_at FROM ai_generated_programs WHERE user_id = ? AND goal ILIKE '%${debouncedQuery}%' LIMIT 5
    - Titre section "Programmes"
    - Si programs.length > 0 → RowList : Ionicons 'list-outline', Text goal, Text date
    - Si vide → Text "Aucun programme trouvé"

    Section 3 — UTILISATEURS :
    - useQuery ['search_users', debouncedQuery] : SELECT id, name, avatar_url FROM user_profiles WHERE name ILIKE '%${debouncedQuery}%' LIMIT 5
    - Titre section "Utilisateurs"
    - Si users.length > 0 → RowList : Image avatar 36×36 borderRadius 18 (fallback initiale), Text name fontWeight '600'
    - Si vide → Text "Aucun utilisateur trouvé"

    ÉTAT INITIAL (query < 2 chars) : Text "Tape au moins 2 caractères pour rechercher" centré color '#6B6963' marginTop 40

    ÉTAT CHARGEMENT : ActivityIndicator color='#FF5C1A' centré si isLoading sur une des 3 queries

    ENTÊTE : View row avec TextInput flex:1 placeholder "Rechercher un exercice, programme…" et TouchableOpacity "Fermer" (rappel onClose prop)

    -------

    HELPSCREEN :

    Lire le contenu statique existant (sections, textes). Réorganiser en STGroup/STRow sans modifier le texte.

    DÉFINIR STGroup et STRow localement (ou importer si déjà dans @ziko/ui) :
    - STGroup : View backgroundColor '#FFFFFF' borderRadius 16 borderWidth 1 borderColor '#E2E0DA' overflow 'hidden' marginBottom 12 shadow standard
    - STRow : View flexDirection 'row' alignItems 'center' paddingHorizontal 16 paddingVertical 14 borderBottomWidth 1 borderBottomColor '#E2E0DA' ; props : label TEXT, icon? string, right? ReactNode, onPress? fn ; si dernière row → borderBottomWidth 0

    SECTIONS STANDARD (adapter au contenu existant) :
    - Groupe "Aide & Support" : rows — "FAQ", "Contacter le support", "Signaler un bug" avec icônes Ionicons + chevron droit
    - Groupe "Légal" : rows — "Conditions d'utilisation", "Politique de confidentialité", "Mentions légales" avec chevron
    - Groupe "App" : rows — version de l'app (Text version en right), numéro de build

    HEADER : SafeAreaView + View ; Text "Aide" fontSize 24 fontWeight '800' padding 16 ; bouton retour Ionicons 'chevron-back-outline'

    paddingBottom: 100.
    Conserver le texte statique exact des pages légales/FAQ si elles existent en détail — ne pas les supprimer, les réorganiser.
  </action>
  <verify>
    <automated>grep -c 'debouncedQuery\|debounced' /c/ziko-platform/apps/mobile/src/components/SearchOverlay.tsx</automated>
  </verify>
  <done>SearchOverlay.tsx avec 3 sections (Exercices/Programmes/Utilisateurs), debounce 300ms, enabled à 2 chars minimum, messages vides par section ; help.tsx avec STGroup/STRow, contenu statique conservé ; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar.tsx → workout_sessions | Lecture scopée user_id = auth.uid() ; RLS enforce |
| SearchOverlay → exercises | Lecture publique (exercices = catalogue partagé) ; RLS permissive SELECT |
| SearchOverlay → user_profiles | Lecture des noms uniquement — pas de données sensibles ; résultats limités à 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-40-03-01 | Information Disclosure | workout_sessions calendar query | mitigate | Query filtrée .eq('user_id', userId) ; RLS enforce |
| T-40-03-02 | Information Disclosure | user_profiles search | accept | Uniquement name + avatar_url ; pas de données PII sensibles ; recherche min 2 chars limite exposition |
| T-40-03-03 | Denial of Service | Search queries sans debounce | mitigate | Debounce 300ms + enabled min 2 chars — réduit les requêtes Supabase à la saisie |
| T-40-03-SC | Tampering | npm/pip/cargo installs | mitigate | Aucun nouveau package installé dans ce plan |
</threat_model>

<verification>
1. Fixture supprimé : `grep -c 'buildMockSessions' /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx` retourne 0
2. TanStack Query calendar : `grep 'useQuery\|workout_sessions' /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx` retourne un match
3. Navigation mois : `grep 'currentDate\|month' /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx` retourne un match
4. Debounce search : `grep 'debouncedQuery\|setTimeout.*300' /c/ziko-platform/apps/mobile/src/components/SearchOverlay.tsx` retourne un match
5. 3 sections search : `grep 'Exercices\|Programmes\|Utilisateurs' /c/ziko-platform/apps/mobile/src/components/SearchOverlay.tsx` retourne 3 matches
6. STGroup/STRow help : `grep 'STGroup\|STRow' /c/ziko-platform/apps/mobile/app/\(app\)/help.tsx` retourne un match
7. TypeScript : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep 'calendar\|SearchOverlay\|help' | head -10`
</verification>

<success_criteria>
- calendar.tsx : buildMockSessions absent, TanStack Query workout_sessions avec navigation mois dynamique, tap jour → historique
- SearchOverlay.tsx : debounce 300ms, enabled min 2 chars, 3 sections distinctes avec états vides
- help.tsx : STGroup/STRow appliqué, contenu statique conservé
- TypeScript : zéro nouvelle erreur dans ces fichiers
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-03-SUMMARY.md` une fois terminé.
</output>
