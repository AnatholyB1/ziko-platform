---
plan: 40-06
phase: 40
wave: 3
depends_on:
  - 40-05
autonomous: true
files_modified: []
requirements:
  - EXTRA-01
  - EXTRA-02
  - EXTRA-03
  - EXTRA-04
  - EXTRA-05
  - EXTRA-06
  - EXTRA-07
  - EXTRA-08
  - EXTRA-09
  - EXTRA-10
  - EXTRA-11
  - EXTRA-12
must_haves:
  goal: Tous les critères de la Phase 40 sont vérifiés automatiquement via grep, wc -l et TypeScript compile — zéro fixture résiduelle, composants créés, intégrations présentes, stubs remplacés.
  truths:
    - EmptyState.tsx et ErrorScreen.tsx existent dans packages/ui/src/components/
    - packages/ui/src/index.ts exporte EmptyState et ErrorScreen
    - notifications.tsx ne contient pas INITIAL_ITEMS
    - calendar.tsx ne contient pas buildMockSessions
    - community/post.tsx a plus de 50 lignes
    - community/challenge-detail.tsx a plus de 50 lignes
    - store/index.tsx contient "featured" (section cards)
    - ai/chat.tsx contient "credits" ou "ai_credits_balance"
    - EmptyState est importé dans au moins 2 fichiers Phase 40
    - TypeScript compile sans nouvelles erreurs sur les fichiers Phase 40
  artifacts:
    - path: packages/ui/src/components/EmptyState.tsx
      provides: Composant vérifié présent
      exports: [EmptyState]
    - path: packages/ui/src/components/ErrorScreen.tsx
      provides: Composant vérifié présent
      exports: [ErrorScreen]
  key_links:
    - from: 40-06 vérification
      to: tous les fichiers Phase 40
      via: "grep + wc -l + rtk tsc --noEmit"
---

<objective>
Exécuter les 10 vérifications automatisées de la Phase 40 pour confirmer que tous les livrables sont en place : composants créés, fixtures supprimées, stubs remplacés, intégrations présentes, TypeScript clean.

Purpose: Plan de vérification finale Wave 3. Lit uniquement — aucune modification de fichier. Si une vérification échoue, documenter l'échec dans le SUMMARY avec les commandes correctives à exécuter avant de clore la phase.

Output: Rapport de vérification dans 40-06-SUMMARY.md avec statut PASS/FAIL par check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Exécuter les 10 vérifications automatisées Phase 40</name>
  <files></files>
  <action>
    Exécuter chaque vérification dans l'ordre. Pour chaque check : noter PASS ou FAIL et la valeur obtenue. Si FAIL : noter la commande corrective.

    CHECK 1 — EmptyState existe :
    test -f /c/ziko-platform/packages/ui/src/components/EmptyState.tsx && echo PASS || echo FAIL

    CHECK 2 — ErrorScreen existe :
    test -f /c/ziko-platform/packages/ui/src/components/ErrorScreen.tsx && echo PASS || echo FAIL

    CHECK 3 — packages/ui/src/index.ts exporte les deux composants :
    grep -c 'EmptyState\|ErrorScreen' /c/ziko-platform/packages/ui/src/index.ts
    → PASS si count >= 2

    CHECK 4 — INITIAL_ITEMS absent de notifications.tsx :
    grep -c 'INITIAL_ITEMS' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx
    → PASS si count == 0

    CHECK 5 — buildMockSessions absent de calendar.tsx :
    grep -c 'buildMockSessions' /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx
    → PASS si count == 0

    CHECK 6 — community/post.tsx non-stub (> 50 lignes) :
    wc -l /c/ziko-platform/apps/mobile/app/\(app\)/\(plugins\)/community/post.tsx | awk '{print $1}'
    → PASS si count >= 50

    CHECK 7 — community/challenge-detail.tsx non-stub (> 50 lignes) :
    wc -l /c/ziko-platform/apps/mobile/app/\(app\)/\(plugins\)/community/challenge-detail.tsx | awk '{print $1}'
    → PASS si count >= 50

    CHECK 8 — store/index.tsx contient "featured" :
    grep -c 'featured' /c/ziko-platform/apps/mobile/app/\(app\)/store/index.tsx
    → PASS si count >= 1

    CHECK 9 — ai/chat.tsx contient référence aux crédits :
    grep -c 'credits\|ai_credits_balance' /c/ziko-platform/apps/mobile/app/\(app\)/ai/chat.tsx
    → PASS si count >= 1

    CHECK 10 — EmptyState importé dans au moins 2 fichiers Phase 40 :
    grep -rl 'EmptyState' \
      /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx \
      /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx \
      /c/ziko-platform/apps/mobile/src/components/SearchOverlay.tsx \
      /c/ziko-platform/apps/mobile/app/\(app\)/ai/chat.tsx \
      /c/ziko-platform/apps/mobile/app/\(app\)/store/index.tsx \
      /c/ziko-platform/apps/mobile/app/\(app\)/workout/program-builder.tsx \
      /c/ziko-platform/apps/mobile/app/\(app\)/profile/referral.tsx \
      2>/dev/null | wc -l
    → PASS si count >= 2

    CHECK 11 — TypeScript compile sans nouvelles erreurs sur fichiers Phase 40 :
    cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -E 'notifications|calendar|SearchOverlay|ai/chat|store/index|store/\[id\]|program-builder|community/post|challenge-detail|lift-detail|goal-edit|referral|EmptyState|ErrorScreen' | grep 'error TS' | wc -l
    → PASS si count == 0

    Présenter les résultats sous la forme :
    | Check | Description | Résultat | Valeur |
    |-------|-------------|----------|--------|
    | 1 | EmptyState.tsx existe | PASS/FAIL | ... |
    ...

    Si tous PASS → écrire "Phase 40 : VALIDATION COMPLÈTE — tous les livrables sont en place."
    Si FAIL(s) → lister les échecs avec leurs commandes correctives recommandées.
  </action>
  <verify>
    <automated>grep -c 'EmptyState\|ErrorScreen' /c/ziko-platform/packages/ui/src/index.ts</automated>
  </verify>
  <done>Les 11 checks sont exécutés et documentés dans le SUMMARY avec statut PASS/FAIL. Si tous PASS : phase validée. Si FAIL(s) : commandes correctives documentées.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan de vérification | Lecture seule — aucune écriture, aucune surface d'attaque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-40-06-SC | Tampering | npm/pip/cargo installs | accept | Aucune installation dans ce plan de vérification |
</threat_model>

<verification>
Toutes les vérifications sont contenues dans la Task 1. Ce plan est auto-vérifiant.

Critère de succès global Phase 40 :
1. `test -f /c/ziko-platform/packages/ui/src/components/EmptyState.tsx` → exit 0
2. `test -f /c/ziko-platform/packages/ui/src/components/ErrorScreen.tsx` → exit 0
3. `grep -c 'EmptyState\|ErrorScreen' /c/ziko-platform/packages/ui/src/index.ts` → >= 2
4. `grep -c 'INITIAL_ITEMS' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx` → 0
5. `grep -c 'buildMockSessions' /c/ziko-platform/apps/mobile/app/\(app\)/calendar.tsx` → 0
6. `wc -l /c/ziko-platform/apps/mobile/app/\(app\)/\(plugins\)/community/post.tsx | awk '{print $1}'` → >= 50
7. `wc -l /c/ziko-platform/apps/mobile/app/\(app\)/\(plugins\)/community/challenge-detail.tsx | awk '{print $1}'` → >= 50
8. `grep -c 'featured' /c/ziko-platform/apps/mobile/app/\(app\)/store/index.tsx` → >= 1
9. `grep -c 'credits\|ai_credits_balance' /c/ziko-platform/apps/mobile/app/\(app\)/ai/chat.tsx` → >= 1
10. `grep -rl 'EmptyState' [...] | wc -l` → >= 2
11. `rtk tsc --noEmit 2>&1 | grep 'error TS' Phase40 files | wc -l` → 0
</verification>

<success_criteria>
- Les 11 checks retournent tous PASS
- 40-06-SUMMARY.md documente le tableau de résultats complet
- Si tout PASS : écrire "Phase 40 : VALIDATION COMPLÈTE"
- Si FAIL(s) : commandes correctives documentées pour résolution avant clôture de phase
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-06-SUMMARY.md` une fois terminé.
</output>
