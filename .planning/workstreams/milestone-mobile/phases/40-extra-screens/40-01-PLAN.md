---
plan: 40-01
phase: 40
wave: 1
depends_on: []
autonomous: true
files_modified:
  - apps/mobile/app/(app)/notifications.tsx
  - apps/mobile/app/(app)/store/index.tsx
  - apps/mobile/app/(app)/store/[id].tsx
requirements:
  - EXTRA-01
  - EXTRA-02
must_haves:
  goal: NotificationsScreen utilise TanStack Query sur la table notifications avec chips de filtre et suppression du fixture INITIAL_ITEMS ; StoreScreen est redessinée avec featured dark cards, category chips et CTAs install/uninstall corrects.
  truths:
    - NotificationsScreen affiche 5 chips de filtre (Tout / Coach IA / Communauté / Records / Système)
    - NotificationsScreen lit la table notifications via TanStack Query (si absente → EmptyState "Pas encore de notifications")
    - La constante INITIAL_ITEMS est supprimée de notifications.tsx
    - NFItem affiche un dot orange pour les notifications non lues, titre + sous-titre, CTA "Voir" optionnel
    - Un tap sur une notification déclenche une mutation mark-read optimiste (read = true)
    - StoreScreen affiche une section "featured" en haut avec des cards sombres (featured_plugins)
    - StoreScreen affiche des category chips permettant de filtrer les plugins par catégorie
    - Les plugin cards affichent un bouton "Installer" ou "Désinstaller" selon l'état user_plugins
    - store/[id].tsx est redessiné pour correspondre au mockup extras-3.jsx
  artifacts:
    - path: apps/mobile/app/(app)/notifications.tsx
      provides: NotificationsScreen avec TanStack Query, filter chips, NFItem rows, mark-read mutation
      exports: [default NotificationsScreen]
    - path: apps/mobile/app/(app)/store/index.tsx
      provides: StoreScreen redessinée avec featured section, category chips, plugin cards
      exports: [default StoreScreen]
    - path: apps/mobile/app/(app)/store/[id].tsx
      provides: Plugin detail page redessinée selon extras-3.jsx
      exports: [default PluginDetailScreen]
  key_links:
    - from: notifications.tsx
      to: notifications (table Supabase)
      via: "TanStack Query useQuery(['notifications', userId]) + useMutation mark-read"
    - from: store/index.tsx
      to: plugins_registry + user_plugins
      via: "Requêtes Supabase existantes conservées — refactoring visuel uniquement"
---

<objective>
Livrer le redesign complet de NotificationsScreen et StoreScreen selon les mockups canoniques `extras.jsx` et `extras-3.jsx`. Notifications : remplacer le fixture INITIAL_ITEMS par TanStack Query sur la table `notifications`, ajouter les chips de filtre et la mutation mark-read. Store : refactoring visuel uniquement (les requêtes Supabase existantes sont conservées) — ajout de la section featured dark cards, des category chips et des CTAs install/uninstall redessinés.

Purpose: Ces deux écrans sont actuellement non conformes au mockup v1.7 — l'un utilise des données fictives (notifications), l'autre a besoin d'un redesign visuel (store). Ce plan les aligne sur EXTRA-01 et EXTRA-02.

Output: notifications.tsx sans fixture, store/index.tsx et store/[id].tsx redessinés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-CONTEXT.md

<interfaces>
Tables DB — Notifications :
- notifications : id UUID PK, user_id UUID, type TEXT ('coach_ai'|'community'|'records'|'system'), title TEXT, body TEXT, read BOOL DEFAULT false, created_at TIMESTAMPTZ, action_url TEXT?

Tables DB — Store :
- plugins_registry : id UUID, name TEXT, description TEXT, category TEXT, icon TEXT, bundle_url TEXT, is_featured BOOL, screenshots JSONB
- user_plugins : id UUID, user_id UUID, plugin_id UUID (ref plugins_registry), is_enabled BOOL, settings JSONB, installed_at TIMESTAMPTZ

Tokens design :
- background: #F7F6F3, surface: #FFFFFF, border: #E2E0DA, text: #1C1A17, muted: #6B6963, primary: #FF5C1A
- Card shadow : shadowColor '#1C1A17', shadowOffset {width:0,height:4}, shadowOpacity 0.08, shadowRadius 12, elevation 3
- Featured card dark : backgroundColor '#1C1A17', borderRadius 20, padding 20
- paddingBottom: 100 sur tout ScrollView/contentContainerStyle

Notification type → icône Ionicons + tint :
- 'coach_ai'   → 'sparkles-outline'    tint '#FF5C1A'
- 'community'  → 'people-outline'      tint '#7B5BD0'
- 'records'    → 'trophy-outline'      tint '#F59E0B'
- 'system'     → 'settings-outline'    tint '#6B6963'
- default      → 'notifications-outline' tint '#2E7BF6'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Redesign NotificationsScreen — suppression fixture, TanStack Query, filter chips, mark-read</name>
  <files>apps/mobile/app/(app)/notifications.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/notifications.tsx — lire les 254 lignes complètes pour identifier le fixture INITIAL_ITEMS, la structure NFItem existante et les imports
  </read_first>
  <action>
    Réécrire notifications.tsx en conservant la structure générale mais en remplaçant entièrement la couche de données et en redessinant l'UI selon extras.jsx.

    SUPPRESSION : retirer la constante INITIAL_ITEMS et toute donnée fictive. Le fichier ne doit contenir aucun tableau de données hardcodées.

    IMPORTS à ajouter : useQuery, useMutation, useQueryClient depuis '@tanstack/react-query'; useSession ou équivalent pour userId.

    FILTER CHIPS :
    - Constante FILTERS = [{id:'all', label:'Tout'}, {id:'coach_ai', label:'Coach IA'}, {id:'community', label:'Communauté'}, {id:'records', label:'Records'}, {id:'system', label:'Système'}]
    - useState activeFilter = 'all'
    - ScrollView horizontal showsHorizontalScrollIndicator={false} ; chip actif : backgroundColor '#FF5C1A' color '#FFFFFF' ; inactif : backgroundColor surface border borderColor '#E2E0DA' color '#6B6963' ; borderRadius 999 paddingH 14 paddingV 7 fontSize 13 fontWeight '600'

    TANSTACK QUERY :
    - useQuery clé ['notifications', userId, activeFilter] : SELECT * FROM notifications WHERE user_id = ? [AND type = activeFilter si activeFilter !== 'all'] ORDER BY created_at DESC LIMIT 50
    - Si isLoading → ActivityIndicator centré color='#FF5C1A'
    - Si data vide ou table absente (error) → View centré avec Ionicons 'notifications-off-outline' size 48 color='#E2E0DA', Text "Pas encore de notifications" color='#6B6963' fontSize 15, Text "Elles apparaîtront ici" fontSize 13 color='#6B6963'

    NFITEM composant local :
    - Props : id, type, title, body, read, created_at, action_url
    - View card : backgroundColor surface, borderRadius 16, borderWidth 1, borderColor '#E2E0DA', marginBottom 8, padding 14, flexDirection 'row', gap 12, shadow standard
    - Icône : View 40×40 borderRadius 12, backgroundColor avec 14% opacité du tint ; Ionicons name selon type (voir tableau interfaces), size 20, color tint
    - Contenu : View flex:1; Text title fontWeight '700' fontSize 14 color '#1C1A17'; Text body fontSize 12 color '#6B6963' marginTop 2 numberOfLines 2; Text date relative fontSize 11 color '#6B6963' marginTop 4
    - Dot non-lu : si !read → View 8×8 borderRadius 4 backgroundColor '#FF5C1A' alignSelf 'flex-start' marginTop 3
    - CTA optionnel : si action_url → Text "Voir" fontSize 12 fontWeight '700' color '#FF5C1A' marginTop 6

    MARK-READ MUTATION :
    - useMutation : UPDATE notifications SET read = true WHERE id = ?
    - Optimistic update via useQueryClient : dans onMutate, mettre à jour le cache query en settant read=true sur l'item
    - onPress NFItem → si !item.read → mutation.mutate(item.id)

    HEADER : SafeAreaView + View header (backgroundColor surface, borderBottom 1 '#E2E0DA', padding 16) ; Text "Notifications" fontSize 20 fontWeight '700' color '#1C1A17' ; lien "Tout lire" (TouchableOpacity) → mutation batch mark-all-read

    FlatList avec keyExtractor={item => item.id} data={data ?? []} renderItem={({item}) => <NFItem ...item />} contentContainerStyle={{padding:16, paddingBottom:100}}
  </action>
  <verify>
    <automated>grep -c 'INITIAL_ITEMS' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx</automated>
  </verify>
  <done>notifications.tsx sans INITIAL_ITEMS, TanStack Query sur table notifications, 5 chips de filtre, NFItem avec dot non-lu, mutation mark-read optimiste ; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2 : Redesign StoreScreen — featured dark cards, category chips, plugin cards, store/[id].tsx</name>
  <files>
    apps/mobile/app/(app)/store/index.tsx,
    apps/mobile/app/(app)/store/[id].tsx
  </files>
  <read_first>
    - apps/mobile/app/(app)/store/index.tsx — lire les 439 lignes complètes pour identifier les requêtes Supabase existantes, la structure des données plugins_registry + user_plugins, et les imports actuels
    - apps/mobile/app/(app)/store/[id].tsx — lire le fichier entier pour identifier l'état actuel (fixtures ou données réelles)
  </read_first>
  <action>
    STORE INDEX — Conserver les requêtes Supabase existantes (plugins_registry + user_plugins). Refactoring visuel uniquement.

    SECTION FEATURED (nouveau) :
    - Filtrer les plugins où is_featured = true (ou les 2–3 premiers par catégorie 'featured') depuis les données déjà chargées
    - ScrollView horizontal gap 12 marginBottom 20
    - Featured card : View width 260 backgroundColor '#1C1A17' borderRadius 20 padding 20 overflow 'hidden' ; chip catégorie en haut (backgroundColor 'rgba(255,92,26,0.2)' color '#FF5C1A') ; Text plugin name 18px fontWeight '700' color '#FFFAF6' marginTop 8 ; Text description 12px color 'rgba(255,250,246,0.65)' marginTop 4 numberOfLines 2 ; bouton "Installer" ou "Installé ✓" en bas (backgroundColor '#FF5C1A' ou 'rgba(255,250,246,0.12)')

    CATEGORY CHIPS :
    - Dériver les catégories uniques depuis plugins_registry data
    - Constante FIXED_CATS = ['Tous', 'Santé', 'Entraînement', 'Nutrition', 'Coaching', 'Social']
    - useState activeCategory = 'Tous' ; filtrer les plugin cards affichées
    - ScrollView horizontal, même style que NotificationsScreen chips

    PLUGIN CARDS redessinées :
    - View card : surface borderRadius 16 borderWidth 1 '#E2E0DA' padding 14 shadow standard
    - Row : icône 48×48 borderRadius 14 backgroundColor '#F7F6F3', Text plugin.name fontWeight '700' fontSize 15, Text plugin.description fontSize 12 color '#6B6963' numberOfLines 2
    - CTA install/uninstall : TouchableOpacity borderRadius 10 paddingH 16 paddingV 8 ; installé → backgroundColor '#E2E0DA' Text "Installé" color '#6B6963' ; non installé → backgroundColor '#FF5C1A' Text "Installer" color '#FFFFFF' fontWeight '700'
    - onPress install → mutation existante pour insérer dans user_plugins ; onPress uninstall → mutation delete user_plugins

    HEADER STORE : Text "Modules" fontSize 24 fontWeight '800', Text "Étends ton expérience" fontSize 13 color '#6B6963' marginTop 2

    STORE [id].tsx — PLUGIN DETAIL :
    - Lire le fichier existant pour identifier comment le plugin est chargé (params.id)
    - Conserver la requête Supabase existante pour charger le plugin + état installed
    - Layout redessiné :
      * Header sombre : View backgroundColor '#1C1A17' padding 24 borderBottomLeftRadius 24 borderBottomRightRadius 24 ; icône 64×64 borderRadius 18 backgroundColor 'rgba(255,250,246,0.1)' ; Text name 22px fontWeight '800' color '#FFFAF6' ; Text category chip orange ; Text description 13px color 'rgba(255,250,246,0.65)' marginTop 8
      * Bouton CTA pleine largeur : installé → "Désinstaller" borderWidth 1 borderColor 'rgba(255,250,246,0.2)' bg transparent ; non installé → backgroundColor '#FF5C1A' "Installer gratuitement" ; borderRadius 14 height 48 marginTop 16
      * Section "À propos" : card surface borderRadius 16 padding 16 marginTop 16 ; Text description complète ; si plugin.screenshots → ScrollView horizontal d'images (aspectRatio 16/9 borderRadius 12)
      * Section "Permissions" : liste des requiredPermissions en RowList avec icône 'checkmark-circle-outline' color '#2E9E5B'
    - paddingBottom: 100

    Pour les deux fichiers, conserver tous les imports existants et ajouter uniquement ce qui manque.
  </action>
  <verify>
    <automated>grep -c 'featured' /c/ziko-platform/apps/mobile/app/\(app\)/store/index.tsx</automated>
  </verify>
  <done>store/index.tsx avec section featured dark cards, category chips actifs, plugin cards redessinées avec CTAs install/uninstall ; store/[id].tsx redessiné avec header sombre, CTA et section À propos ; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifications.tsx → notifications table | Lecture + écriture scopées user_id = auth.uid() ; RLS enforce |
| store → user_plugins | Insert/delete scopés user_id ; RLS enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-40-01-01 | Tampering | mark-read mutation | mitigate | UPDATE WHERE user_id = auth.uid() — seul l'owner peut marquer lu ; RLS enforce |
| T-40-01-02 | Information Disclosure | notifications query | mitigate | Query filtrée .eq('user_id', userId) ; RLS enforce ownership |
| T-40-01-03 | Tampering | user_plugins insert | mitigate | user_id injecté depuis session auth uniquement ; validation plugin_id via plugins_registry |
| T-40-01-SC | Tampering | npm/pip/cargo installs | mitigate | Aucun nouveau package installé dans ce plan |
</threat_model>

<verification>
1. Fixture supprimé : `grep -c 'INITIAL_ITEMS' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx` retourne 0
2. Featured présent : `grep -c 'featured' /c/ziko-platform/apps/mobile/app/\(app\)/store/index.tsx` retourne >= 1
3. TanStack Query notifications : `grep 'useQuery' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx` retourne un match
4. Filter chips : `grep 'Coach IA' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx` retourne un match
5. Mark-read : `grep 'useMutation' /c/ziko-platform/apps/mobile/app/\(app\)/notifications.tsx` retourne un match
6. Category chips store : `grep 'activeCategory\|Tous' /c/ziko-platform/apps/mobile/app/\(app\)/store/index.tsx` retourne un match
7. Plugin detail header sombre : `grep '#1C1A17' /c/ziko-platform/apps/mobile/app/\(app\)/store/\[id\].tsx` retourne un match
8. TypeScript clean : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep 'app/\(app\)/notifications\|app/\(app\)/store' | head -10`
</verification>

<success_criteria>
- notifications.tsx : INITIAL_ITEMS absent, TanStack Query sur notifications, 5 chips filtre, NFItem avec dot non-lu et mutation mark-read
- store/index.tsx : section featured dark cards en haut, category chips fonctionnels, plugin cards avec CTAs install/uninstall
- store/[id].tsx : header sombre, bouton CTA install/uninstall, section À propos
- TypeScript : zéro nouvelle erreur dans ces fichiers
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-01-SUMMARY.md` une fois terminé.
</output>
