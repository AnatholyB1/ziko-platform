---
plan: 40-04
phase: 40
wave: 1
depends_on: []
autonomous: true
files_modified:
  - apps/mobile/app/(app)/workout/program-builder.tsx
  - apps/mobile/app/(app)/(plugins)/community/post.tsx
  - apps/mobile/app/(app)/(plugins)/community/challenge-detail.tsx
  - apps/mobile/app/(app)/profile/lift-detail.tsx
  - apps/mobile/app/(app)/profile/goal-edit.tsx
  - apps/mobile/app/(app)/profile/referral.tsx
requirements:
  - EXTRA-10
  - EXTRA-11
  - EXTRA-12
must_haves:
  goal: ProgramBuilder est un wizard 4 étapes avec step indicator et insert dans ai_generated_programs ; PostDetail et ChallengeDetail sont implémentés depuis leurs stubs 8L/7L ; LiftDetail, GoalEdit et ReferralScreen sont redessinés selon extras-2.jsx.
  truths:
    - program-builder.tsx affiche un wizard 4 étapes (Objectif / Durée / Jours / Exercices) avec un indicateur d'étape en header
    - La dernière étape du wizard insère dans ai_generated_programs
    - community/post.tsx a plus de 50 lignes et affiche l'auteur, le corps du post, les compteurs et la liste de commentaires
    - community/challenge-detail.tsx a plus de 50 lignes et affiche le leaderboard top 3, le nombre de participants et le CTA join/leave
    - lift-detail.tsx affiche le PR header et l'historique des 10 dernières sessions pour l'exercice
    - goal-edit.tsx affiche 5 chips objectif et sauvegarde dans user_profiles.goal
    - referral.tsx affiche le code de parrainage depuis user_profiles.referral_code et un CTA Share API
  artifacts:
    - path: apps/mobile/app/(app)/workout/program-builder.tsx
      provides: Wizard 4 étapes avec step indicator, insert ai_generated_programs
      exports: [default ProgramBuilderScreen]
    - path: apps/mobile/app/(app)/(plugins)/community/post.tsx
      provides: PostDetail complet avec auteur, corps, commentaires
      exports: [default PostDetailScreen]
    - path: apps/mobile/app/(app)/(plugins)/community/challenge-detail.tsx
      provides: ChallengeDetail avec leaderboard et CTA join/leave
      exports: [default ChallengeDetailScreen]
    - path: apps/mobile/app/(app)/profile/lift-detail.tsx
      provides: LiftDetail redessiné avec 1RM history depuis session_sets
      exports: [default LiftDetailScreen]
    - path: apps/mobile/app/(app)/profile/goal-edit.tsx
      provides: GoalEdit avec 5 chips objectif et save user_profiles.goal
      exports: [default GoalEditScreen]
    - path: apps/mobile/app/(app)/profile/referral.tsx
      provides: ReferralScreen avec code, Share API, referred count
      exports: [default ReferralScreen]
  key_links:
    - from: program-builder.tsx (step 4)
      to: ai_generated_programs
      via: "useMutation → INSERT INTO ai_generated_programs (user_id, goal, program_data) VALUES (?, ?, ?)"
    - from: community/post.tsx
      to: community_posts + post_comments
      via: "useQuery ['post', postId] + useQuery ['comments', postId]"
    - from: community/challenge-detail.tsx
      to: challenges + challenge_participants
      via: "useQuery ['challenge', challengeId] + useQuery ['leaderboard', challengeId]"
    - from: lift-detail.tsx
      to: session_sets
      via: "useQuery ['liftHistory', userId, exerciseId] → SELECT * FROM session_sets WHERE user_id = ? AND exercise_id = ? ORDER BY created_at DESC LIMIT 10"
    - from: goal-edit.tsx
      to: user_profiles.goal
      via: "useMutation → UPDATE user_profiles SET goal = ? WHERE id = userId"
    - from: referral.tsx
      to: user_profiles.referral_code + referrals table
      via: "useQuery ['referral', userId] → SELECT referral_code FROM user_profiles + SELECT COUNT(*) FROM referrals WHERE referrer_id = ?"
---

<objective>
Livrer six écrans en un plan : redesign du wizard ProgramBuilder en 4 étapes, implémentation complète des stubs community (PostDetail + ChallengeDetail), redesign de LiftDetail et GoalEdit avec données réelles, et redesign de ReferralScreen selon extras-2.jsx avec gestion conditionnelle de la table referrals.

Purpose: Six écrans non conformes ou stubbed selon EXTRA-10, EXTRA-11, EXTRA-12. Ce plan est volumineux car les 6 screens partagent aucune dépendance entre eux et peuvent être traités séquentiellement dans un seul contexte d'exécution.

Output: 6 fichiers redessinés/implémentés avec données réelles, TypeScript clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-CONTEXT.md

<interfaces>
Tables DB :
- ai_generated_programs : id UUID, user_id UUID, goal TEXT, program_data JSONB, created_at TIMESTAMPTZ
- community_posts : id UUID, user_id UUID, content TEXT, image_url TEXT?, likes_count INT, comments_count INT, created_at TIMESTAMPTZ
- post_comments : id UUID, post_id UUID, user_id UUID, content TEXT, created_at TIMESTAMPTZ
- challenges : id UUID, name TEXT, description TEXT, icon TEXT, duration_days INT, xp_reward INT, created_at TIMESTAMPTZ
- challenge_participants : id UUID, challenge_id UUID, user_id UUID, score NUMERIC, joined_at TIMESTAMPTZ
- session_sets : id UUID, session_id UUID, user_id UUID, exercise_id UUID, exercise_name TEXT, weight_kg NUMERIC, reps INT, rpe NUMERIC?, created_at TIMESTAMPTZ
- user_profiles : id UUID, name TEXT, avatar_url TEXT, goal TEXT, referral_code TEXT, ai_credits_balance INT
- referrals : id UUID, referrer_id UUID, referred_user_id UUID, created_at TIMESTAMPTZ (peut ne pas exister)

Tokens design :
- background: #F7F6F3, surface: #FFFFFF, border: #E2E0DA, text: #1C1A17, muted: #6B6963, primary: #FF5C1A
- Card shadow : shadowColor '#1C1A17', shadowOffset {width:0,height:4}, shadowOpacity 0.08, shadowRadius 12, elevation 3
- Step indicator actif : backgroundColor '#FF5C1A' ; inactif : backgroundColor '#E2E0DA'
- Goal chips : 5 objectifs : [{id:'weight_loss', label:'Perte de poids', icon:'flame-outline'}, {id:'strength', label:'Force', icon:'barbell-outline'}, {id:'endurance', label:'Endurance', icon:'heart-outline'}, {id:'body_comp', label:'Composition', icon:'body-outline'}, {id:'wellness', label:'Bien-être', icon:'leaf-outline'}]
- Chip sélectionné : backgroundColor '#FF5C1A' color '#FFFFFF' ; non sélectionné : backgroundColor surface borderWidth 1 borderColor '#E2E0DA' color '#1C1A17'
- paddingBottom: 100 sur tout ScrollView
- Leaderboard top 3 couleurs : 1er '#F59E0B', 2ème '#6B7280', 3ème '#CD7C2F'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Wizard ProgramBuilder 4 étapes + PostDetail + ChallengeDetail</name>
  <files>
    apps/mobile/app/(app)/workout/program-builder.tsx,
    apps/mobile/app/(app)/(plugins)/community/post.tsx,
    apps/mobile/app/(app)/(plugins)/community/challenge-detail.tsx
  </files>
  <read_first>
    - apps/mobile/app/(app)/workout/program-builder.tsx — lire les 488 lignes pour identifier la structure wizard existante, les étapes, les imports, comment le résultat est sauvegardé
    - apps/mobile/app/(app)/(plugins)/community/post.tsx — lire le stub (8L) pour voir les imports et params
    - apps/mobile/app/(app)/(plugins)/community/challenge-detail.tsx — lire le stub (7L) pour voir les imports et params
  </read_first>
  <action>
    --- PROGRAM BUILDER ---

    Réécrire program-builder.tsx en conservant la logique existante des étapes mais en redessinant selon extras-2.jsx.

    STATE : const [step, setStep] = useState(1) (1–4) ; const [goal, setGoal] = useState('') ; const [weeks, setWeeks] = useState(8) ; const [days, setDays] = useState<number[]>([]) ; const [exercises, setExercises] = useState<string[]>([])

    STEP INDICATOR HEADER :
    - View flexDirection 'row' gap 8 paddingHorizontal 16 paddingVertical 12
    - 4 View flex:1 height 4 borderRadius 999 ; actif ou passé → backgroundColor '#FF5C1A' ; futur → backgroundColor '#E2E0DA'
    - Text "Étape {step}/4" fontSize 12 color '#6B6963' marginTop 4 textAlign 'center'

    ÉTAPE 1 — OBJECTIF :
    - Text "Quel est ton objectif ?" fontSize 22 fontWeight '800' marginBottom 20
    - 5 goal chips (voir interfaces) disposés en grille 2 colonnes (wrap)
    - Chaque chip : View borderRadius 14 padding 14 flexDirection 'row' alignItems 'center' gap 10 ; Ionicons icon 20px ; Text label fontWeight '600' ; sélectionné → bg '#FF5C1A' text blanc ; non → bg surface border '#E2E0DA'

    ÉTAPE 2 — DURÉE :
    - Text "Durée du programme" fontSize 22 fontWeight '800'
    - Options durée : [4, 6, 8, 12] semaines sous forme de chips horizontaux
    - Chip sélectionné → bg '#FF5C1A' text blanc

    ÉTAPE 3 — JOURS :
    - Text "Jours d'entraînement" fontSize 22 fontWeight '800'
    - 7 boutons jour : ['L','M','M','J','V','S','D'] (multi-select) → toggle dans days array
    - Chip sélectionné → bg '#FF5C1A' text blanc

    ÉTAPE 4 — EXERCICES :
    - Text "Exercices clés" fontSize 22 fontWeight '800' ; Text "Facultatif — on les inclura dans ton programme" color '#6B6963'
    - TextInput "Ajouter un exercice…" + bouton "+" → push dans exercises array
    - FlatList exercices ajoutés avec icône 'close-circle-outline' pour supprimer
    - Bouton final "Créer mon programme" backgroundColor '#FF5C1A' height 52 borderRadius 16

    MUTATION FINALE (step 4 → "Créer") :
    - useMutation : INSERT INTO ai_generated_programs { user_id, goal, program_data: { weeks, days, exercises, generated: true } }
    - onSuccess → showAlert('Programme créé !', 'Ton programme est prêt.') + router.back()
    - onError → showAlert('Erreur', error.message)

    NAVIGATION ENTRE ÉTAPES :
    - Bouton "Suivant" (step 1–3) ou "Créer" (step 4) en bas fixe (position absolute bottom 0)
    - Bouton "Retour" texte en haut gauche si step > 1

    --- POST DETAIL ---

    Implémenter community/post.tsx depuis le stub.

    PARAMS : const { id: postId } = useLocalSearchParams()

    DATA :
    - useQuery ['post', postId] : SELECT community_posts.*, user_profiles.name, user_profiles.avatar_url FROM community_posts JOIN user_profiles ON community_posts.user_id = user_profiles.id WHERE community_posts.id = ?
    - useQuery ['comments', postId] : SELECT post_comments.*, user_profiles.name, user_profiles.avatar_url FROM post_comments JOIN user_profiles ON post_comments.user_id = user_profiles.id WHERE post_comments.post_id = ? ORDER BY created_at ASC LIMIT 30

    LAYOUT :
    - SafeAreaView + ScrollView contentContainerStyle paddingBottom 100
    - HEADER POST : View flexDirection 'row' alignItems 'center' padding 16 ; Image avatar 40×40 borderRadius 20 (fallback initiale) ; View flex:1 ; Text post.name fontWeight '700' fontSize 14 ; Text date relative fontSize 12 color '#6B6963'
    - CORPS : Text post.content fontSize 15 color '#1C1A17' lineHeight 22 paddingHorizontal 16
    - IMAGE optionnelle : si post.image_url → Image borderRadius 12 marginH 16 aspectRatio 16/9
    - COMPTEURS : View flexDirection 'row' gap 16 paddingHorizontal 16 paddingVertical 12 borderTopWidth 1 borderBottomWidth 1 borderColor '#E2E0DA' marginTop 12 ; Ionicons 'heart-outline' + Text likes ; Ionicons 'chatbubble-outline' + Text comments
    - SECTION COMMENTAIRES : Text "Commentaires ({count})" fontWeight '700' fontSize 15 paddingHorizontal 16 paddingVertical 12 ; si vide → Text "Pas encore de commentaires" color '#6B6963' paddingH 16 ; sinon RowList : avatar 32×32, Text name fontWeight '600', Text content, Text date

    --- CHALLENGE DETAIL ---

    Implémenter community/challenge-detail.tsx depuis le stub.

    PARAMS : const { id: challengeId } = useLocalSearchParams()

    DATA :
    - useQuery ['challenge', challengeId] : SELECT * FROM challenges WHERE id = ?
    - useQuery ['leaderboard', challengeId] : SELECT challenge_participants.*, user_profiles.name, user_profiles.avatar_url FROM challenge_participants JOIN user_profiles ON challenge_participants.user_id = user_profiles.id WHERE challenge_id = ? ORDER BY score DESC LIMIT 20
    - useQuery ['myParticipation', challengeId, userId] : SELECT id FROM challenge_participants WHERE challenge_id = ? AND user_id = ?
    - useMutation join : INSERT INTO challenge_participants (challenge_id, user_id, score) VALUES (?, ?, 0)
    - useMutation leave : DELETE FROM challenge_participants WHERE challenge_id = ? AND user_id = ?

    LAYOUT :
    - HEADER CHALLENGE dark : View backgroundColor '#1C1A17' padding 24 ; Ionicons challenge.icon 36 color '#FF5C1A' ; Text challenge.name 22 fontWeight '800' color '#FFFAF6' ; Text description 13 color 'rgba(255,250,246,0.65)' ; Row chips : durée + XP reward (backgroundColor 'rgba(255,92,26,0.2)' color '#FF5C1A')
    - CTA JOIN/LEAVE : si myParticipation existe → bouton "Quitter" (border transparent text '#EF4444') ; sinon → "Rejoindre le défi" backgroundColor '#FF5C1A' ; height 48 borderRadius 14 marginHorizontal 16 marginTop -12 (overlap header)
    - LEADERBOARD "Top participants" : Text section fontWeight '700' paddingHorizontal 16 paddingVertical 12 ; top 3 → View row avec rank badge (couleur selon interfaces), avatar, nom, score ; rang 4+ → rows simples
    - Ligne "Ma position" : surlignée backgroundColor 'rgba(255,92,26,0.06)' borderLeftWidth 3 borderLeftColor '#FF5C1A'
    - STAT participants : Text "{count} participants" fontSize 13 color '#6B6963' textAlign 'center' marginTop 12
  </action>
  <verify>
    <automated>wc -l /c/ziko-platform/apps/mobile/app/\(app\)/\(plugins\)/community/post.tsx | awk '{print $1}'</automated>
  </verify>
  <done>program-builder.tsx : wizard 4 étapes avec step indicator, insert ai_generated_programs ; post.tsx > 50 lignes avec auteur/corps/commentaires ; challenge-detail.tsx > 50 lignes avec leaderboard et join/leave ; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2 : Redesign LiftDetail + GoalEdit + ReferralScreen</name>
  <files>
    apps/mobile/app/(app)/profile/lift-detail.tsx,
    apps/mobile/app/(app)/profile/goal-edit.tsx,
    apps/mobile/app/(app)/profile/referral.tsx
  </files>
  <read_first>
    - apps/mobile/app/(app)/profile/lift-detail.tsx — lire les 299 lignes pour identifier les données existantes, comment exerciseId est passé, les imports
    - apps/mobile/app/(app)/profile/goal-edit.tsx — lire les 312 lignes pour identifier la structure actuelle, les chips existants, comment la sauvegarde fonctionne
    - apps/mobile/app/(app)/profile/referral.tsx — lire les 563 lignes pour identifier : le code de parrainage actuel, si la table referrals est déjà utilisée, les imports
  </read_first>
  <action>
    --- LIFT DETAIL ---

    PARAMS : const { exerciseId, exerciseName } = useLocalSearchParams()

    DATA :
    - useQuery ['liftHistory', userId, exerciseId] : SELECT weight_kg, reps, rpe, created_at FROM session_sets WHERE user_id = ? AND exercise_id = ? ORDER BY created_at DESC LIMIT 10
    - Calculer 1RM estimé (Epley) : 1RM = weight_kg * (1 + reps / 30) par set ; max des 10 derniers

    LAYOUT :
    - HEADER : Text exerciseName fontSize 22 fontWeight '800' ; View row gap 12 marginTop 8 ; Card PR : View backgroundColor '#FF5C1A' borderRadius 14 paddingH 16 paddingV 10 ; Text "Record" fontSize 11 fontWeight '700' color 'rgba(255,255,255,0.7)' ; Text `${best1RM.toFixed(1)} kg` fontSize 22 fontWeight '800' color '#FFFFFF'
    - HISTORIQUE 1RM (MiniBars local) : data = 10 derniers sets mappés vers {l: format(date, 'd/M'), v: estimated1RM(set)} ; color='#FF5C1A' ; max = best1RM * 1.1
    - TABLEAU SETS RÉCENTS : Text "Séries récentes" fontWeight '700' marginTop 20 ; FlatList data={history} ; chaque row : View flexDirection 'row' paddingVertical 10 borderBottomWidth 1 borderBottomColor '#E2E0DA' ; Text date flex:0.3 color '#6B6963' fontSize 13 ; Text `${set.weight_kg}kg × ${set.reps}` flex:0.4 fontWeight '600' ; Text `${estimated1RM(set).toFixed(1)}kg 1RM` flex:0.3 color '#FF5C1A' fontSize 12 fontWeight '700'
    - Si aucun set → Text "Aucune donnée pour cet exercice" centré color '#6B6963'
    - MiniBars déclaré localement (même pattern Phase 38/39)
    - paddingBottom: 100

    --- GOAL EDIT ---

    PARAMS : const { currentGoal } = useLocalSearchParams() OU useQuery user_profiles

    STATE : const [selectedGoal, setSelectedGoal] = useState(currentGoal || '')

    DATA :
    - useQuery ['userGoal', userId] si currentGoal absent : SELECT goal FROM user_profiles WHERE id = ?
    - useMutation : UPDATE user_profiles SET goal = selectedGoal WHERE id = userId ; onSuccess → router.back() ; showAlert('Objectif mis à jour', ...)

    LAYOUT :
    - Text "Ton objectif" fontSize 22 fontWeight '800' paddingTop 16 paddingHorizontal 16
    - Text "Choisis ce qui correspond le mieux à tes ambitions actuelles" fontSize 13 color '#6B6963' paddingHorizontal 16 marginBottom 24
    - 5 goal cards (pas chips — cards larges) : voir interfaces pour les 5 objectifs
      * View borderRadius 16 borderWidth 2 borderColor (sélectionné '#FF5C1A' sinon '#E2E0DA') padding 16 marginHorizontal 16 marginBottom 10 backgroundColor surface
      * Row : View 48×48 borderRadius 14 backgroundColor (sélectionné 'rgba(255,92,26,0.1)' sinon '#F7F6F3') alignItems center justifyContent center ; Ionicons icon 22px color (sélectionné '#FF5C1A' sinon '#6B6963')
      * View flex:1 marginLeft 12 ; Text label fontWeight '700' fontSize 16 color (sélectionné '#FF5C1A' sinon '#1C1A17')
      * Si sélectionné → Ionicons 'checkmark-circle' 20 color '#FF5C1A' à droite
    - Bouton "Enregistrer" fixe en bas : View backgroundColor '#FF5C1A' height 52 borderRadius 16 marginH 16 marginBottom 32 ; disabled si selectedGoal === currentGoal

    --- REFERRAL ---

    Lire le fichier existant. Identifier si la table referrals est déjà utilisée. Appliquer D-22 : si referrals existe → vraies données ; sinon → referral_code depuis user_profiles + referred_count = 0.

    DATA :
    - useQuery ['referralData', userId] :
      1. SELECT referral_code FROM user_profiles WHERE id = ?
      2. Tenter SELECT COUNT(*) FROM referrals WHERE referrer_id = ? (wrapper try/catch ou .maybeSingle()) ; si error (table absente) → count = 0

    LAYOUT REDESSINÉ :
    - HEADER : Text "Parrainage" fontSize 24 fontWeight '800' ; Text "Invite tes amis et gagne des crédits IA" fontSize 14 color '#6B6963'

    - CARD CODE : View backgroundColor '#1C1A17' borderRadius 20 padding 20 marginTop 16
      * Text "Ton code de parrainage" fontSize 12 fontWeight '700' color 'rgba(255,250,246,0.55)' textTransform 'uppercase'
      * Text referralCode fontSize 28 fontWeight '900' color '#FFFFFF' letterSpacing 4 marginVertical 8
      * TouchableOpacity row "Copier" : Ionicons 'copy-outline' 16 color '#FF5C1A' ; Text "Copier" fontWeight '700' color '#FF5C1A' fontSize 13 ; onPress → Clipboard.setStringAsync(referralCode)

    - BOUTON PARTAGER : TouchableOpacity View backgroundColor '#FF5C1A' height 52 borderRadius 16 flexDirection 'row' alignItems 'center' justifyContent 'center' gap 10 marginTop 12
      * Ionicons 'share-social-outline' 20 color '#FFFFFF'
      * Text "Partager mon code" fontWeight '700' color '#FFFFFF' fontSize 16
      * onPress → Share.share({ message: `Rejoins Ziko avec mon code ${referralCode} et booste ton entraînement ! 💪`, title: 'Invitation Ziko' })

    - STAT FILLEULS : View card surface borderRadius 16 borderWidth 1 borderColor '#E2E0DA' padding 16 marginTop 12 flexDirection 'row' alignItems 'center' gap 12
      * Ionicons 'people-outline' 24 color '#FF5C1A' ; View ; Text "Amis invités" fontSize 13 color '#6B6963' ; Text `${referredCount}` fontSize 22 fontWeight '800' color '#1C1A17'

    - CHIP RÉCOMPENSE : si referredCount > 0 → View chip backgroundColor 'rgba(46,158,91,0.1)' borderRadius 999 paddingH 12 paddingV 6 ; Ionicons 'checkmark-circle' 14 color '#2E9E5B' ; Text "Récompenses reçues" fontSize 12 fontWeight '600' color '#2E9E5B'

    Import Share depuis 'react-native' ; Clipboard depuis 'expo-clipboard' ou '@react-native-clipboard/clipboard'.
    paddingBottom: 100.
  </action>
  <verify>
    <automated>grep -c 'referral_code\|Share\|referredCount' /c/ziko-platform/apps/mobile/app/\(app\)/profile/referral.tsx</automated>
  </verify>
  <done>lift-detail.tsx avec MiniBars 1RM et tableau sets depuis session_sets ; goal-edit.tsx avec 5 cards objectif et save user_profiles.goal ; referral.tsx avec code display card, Share API, referred count conditionnel ; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| program-builder → ai_generated_programs | Insert scopé user_id = auth.uid() ; RLS enforce |
| community/post → community_posts + post_comments | Lecture ; join avec user_profiles pour auteur |
| challenge-detail → challenge_participants | Insert/delete scopés user_id ; RLS enforce |
| goal-edit → user_profiles | UPDATE scopé id = userId ; RLS enforce |
| referral.tsx → referrals table | Lecture count pour le referrer ; scopé referrer_id = userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-40-04-01 | Tampering | program-builder insert | mitigate | user_id injecté depuis session auth ; program_data JSONB validé côté client (weeks 4–12, days subset 0–6) |
| T-40-04-02 | Tampering | challenge join/leave | mitigate | INSERT/DELETE scopés user_id ; ne peut pas rejoindre/quitter pour un autre utilisateur |
| T-40-04-03 | Information Disclosure | community posts/comments | accept | Contenu communautaire public par nature ; lecture seule dans ce plan |
| T-40-04-04 | Tampering | goal-edit UPDATE | mitigate | UPDATE WHERE id = userId depuis session auth ; goal validé parmi 5 valeurs permises |
| T-40-04-05 | Information Disclosure | referral code display | accept | Code de parrainage est destiné à être partagé (pas de données sensibles) |
| T-40-04-SC | Tampering | npm/pip/cargo installs | mitigate | expo-clipboard vérifié dans package.json avant utilisation ; Share est natif react-native |
</threat_model>

<verification>
1. Step indicator : `grep 'step.*indicator\|Étape.*4\|step === 4' /c/ziko-platform/apps/mobile/app/\(app\)/workout/program-builder.tsx` retourne un match
2. Insert ai_generated_programs : `grep 'ai_generated_programs' /c/ziko-platform/apps/mobile/app/\(app\)/workout/program-builder.tsx` retourne un match
3. PostDetail non-stub : `wc -l /c/ziko-platform/apps/mobile/app/\(app\)/\(plugins\)/community/post.tsx | awk '{print $1}'` retourne >= 50
4. ChallengeDetail non-stub : `wc -l /c/ziko-platform/apps/mobile/app/\(app\)/\(plugins\)/community/challenge-detail.tsx | awk '{print $1}'` retourne >= 50
5. Leaderboard : `grep 'leaderboard\|challenge_participants' /c/ziko-platform/apps/mobile/app/\(app\)/\(plugins\)/community/challenge-detail.tsx` retourne un match
6. 5 goal chips : `grep 'weight_loss\|strength\|endurance\|body_comp\|wellness' /c/ziko-platform/apps/mobile/app/\(app\)/profile/goal-edit.tsx | wc -l` retourne >= 5
7. Save goal : `grep 'user_profiles.*goal\|goal.*user_profiles' /c/ziko-platform/apps/mobile/app/\(app\)/profile/goal-edit.tsx` retourne un match
8. Share API referral : `grep "Share\|share-social" /c/ziko-platform/apps/mobile/app/\(app\)/profile/referral.tsx` retourne un match
9. TypeScript : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep 'program-builder\|community/post\|challenge-detail\|lift-detail\|goal-edit\|referral' | head -15`
</verification>

<success_criteria>
- program-builder.tsx : wizard 4 étapes avec step indicator visible, insert dans ai_generated_programs sur l'étape finale
- post.tsx + challenge-detail.tsx : plus de stubs — implementations complètes > 50 lignes chacun
- lift-detail.tsx : MiniBars 1RM history depuis session_sets, tableau sets récents
- goal-edit.tsx : 5 cards objectif, sauvegarde dans user_profiles.goal
- referral.tsx : code display card, Share API, referred count conditionnel (0 si table absente)
- TypeScript : zéro nouvelle erreur dans ces 6 fichiers
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-04-SUMMARY.md` une fois terminé.
</output>
