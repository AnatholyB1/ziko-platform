---
plan: 40-02
phase: 40
wave: 1
depends_on: []
autonomous: true
files_modified:
  - apps/mobile/app/(app)/ai/chat.tsx
  - apps/mobile/app/(app)/profile/avatar.tsx
requirements:
  - EXTRA-03
  - EXTRA-09
must_haves:
  goal: AIChatScreen est redessinée avec un chip compteur de crédits lisant ai_credits_balance, une animation de streaming dots et un header conversation list redessiné ; AvatarUploadScreen gère le flux complet image-picker → upload Supabase Storage → update avatar_url.
  truths:
    - ai/chat.tsx affiche un chip compteur de crédits lisant user_profiles.ai_credits_balance
    - L'animation streaming dots (3 points animés) apparaît pendant les réponses en cours
    - Le header/sidebar de liste de conversations est redessiné selon extras.jsx
    - La logique de streaming IA existante est conservée intacte
    - profile/avatar.tsx propose expo-image-picker pour choisir une image depuis la galerie
    - L'upload se fait vers le bucket Supabase Storage avatars/{userId}.jpg
    - Une barre de progression indique l'avancement de l'upload
    - Après upload réussi, user_profiles.avatar_url est mis à jour
  artifacts:
    - path: apps/mobile/app/(app)/ai/chat.tsx
      provides: AIChatScreen redessinée avec credit chip, streaming dots, conversation list redessinée
      exports: [default AIChatScreen]
    - path: apps/mobile/app/(app)/profile/avatar.tsx
      provides: AvatarUploadScreen avec image picker, upload Storage, update avatar_url
      exports: [default AvatarUploadScreen]
  key_links:
    - from: ai/chat.tsx (credit chip)
      to: user_profiles.ai_credits_balance
      via: "useQuery ['userCredits', userId] → SELECT ai_credits_balance FROM user_profiles WHERE id = ?"
    - from: profile/avatar.tsx
      to: Supabase Storage bucket avatars
      via: "supabase.storage.from('avatars').upload(userId+'.jpg', file, {upsert:true})"
    - from: profile/avatar.tsx
      to: user_profiles.avatar_url
      via: "useMutation → UPDATE user_profiles SET avatar_url = publicUrl WHERE id = userId"
---

<objective>
Livrer le redesign visuel de AIChatScreen (extras.jsx) avec le chip compteur de crédits et les streaming dots, sans toucher à la logique AI existante. Compléter AvatarUploadScreen pour un flux upload Supabase Storage complet avec progression.

Purpose: AIChatScreen (501L) est fonctionnelle mais non conforme au mockup v1.7 — manque le credit chip et les dots d'animation. AvatarUploadScreen (416L) peut avoir un flux upload incomplet. Ce plan aligne EXTRA-03 et EXTRA-09.

Output: chat.tsx redessiné avec credit chip + streaming dots ; avatar.tsx avec flux upload complet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-CONTEXT.md

<interfaces>
Tables DB :
- user_profiles : id UUID, avatar_url TEXT, ai_credits_balance INT (ou colonne équivalente du système crédits Phase 35)
- ai_conversations : id UUID, user_id UUID, title TEXT, plugin_context JSONB, created_at TIMESTAMPTZ
- ai_messages : id UUID, conversation_id UUID, role TEXT ('user'|'assistant'|'system'), content TEXT, created_at TIMESTAMPTZ

Supabase Storage :
- Bucket : 'avatars' (doit exister ; si absent → créer via supabase.storage.createBucket)
- Chemin fichier : '{userId}.jpg' (upload avec option upsert: true)
- URL publique : supabase.storage.from('avatars').getPublicUrl('{userId}.jpg').data.publicUrl

expo-image-picker :
- ImagePicker.launchImageLibraryAsync({ mediaTypes: ImagePicker.MediaTypeOptions.Images, allowsEditing: true, aspect: [1, 1], quality: 0.8 })
- Résultat : { canceled, assets: [{ uri, width, height, mimeType }] }
- Permissions : ImagePicker.requestMediaLibraryPermissionsAsync() avant lancement

Tokens design :
- background: #F7F6F3, surface: #FFFFFF, border: #E2E0DA, text: #1C1A17, muted: #6B6963, primary: #FF5C1A
- Credit chip : backgroundColor 'rgba(255,92,26,0.1)' borderRadius 999 paddingH 10 paddingV 4 ; Text '{n} crédits' fontSize 12 fontWeight '700' color '#FF5C1A' ; Ionicons 'flash-outline' size 12 color '#FF5C1A' marginRight 4
- Streaming dots : 3 View 6×6 borderRadius 3 backgroundColor '#6B6963' avec Animated.loop + stagger 200ms opacity 0.3→1
- Card shadow : shadowColor '#1C1A17', shadowOffset {width:0,height:4}, shadowOpacity 0.08, shadowRadius 12, elevation 3
- paddingBottom: 100
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Redesign AIChatScreen — credit chip, streaming dots, conversation list header</name>
  <files>apps/mobile/app/(app)/ai/chat.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/ai/chat.tsx — lire les 501 lignes complètes pour identifier : la structure existante, comment l'état isStreaming/isPending est géré, où le header est rendu, les imports actuels
  </read_first>
  <action>
    Modifier chat.tsx en préservant intégralement la logique de streaming IA existante (SSE, appendMessage, etc.). Trois ajouts visuels uniquement.

    CREDIT CHIP :
    - useQuery clé ['userCredits', userId] : SELECT ai_credits_balance FROM user_profiles WHERE id = ? LIMIT 1
    - Déclarer creditsBalance = data?.ai_credits_balance ?? 0
    - Chip rendu dans le header de l'écran (à droite du titre ou en dessous) : View flexDirection 'row' alignItems 'center' backgroundColor 'rgba(255,92,26,0.1)' borderRadius 999 paddingHorizontal 10 paddingVertical 4 ; Ionicons 'flash-outline' size 12 color '#FF5C1A' marginRight 4 ; Text `${creditsBalance} crédits` fontSize 12 fontWeight '700' color '#FF5C1A'
    - Si creditsBalance <= 5 → backgroundColor 'rgba(239,68,68,0.1)' color '#EF4444' pour alerter visuellement

    STREAMING DOTS :
    - Identifier dans le code existant le state qui indique qu'une réponse est en cours (isLoading, isStreaming, isPending ou équivalent)
    - Déclarer 3 Animated.Value : const dot1 = useRef(new Animated.Value(0.3)).current ; dot2, dot3 idem
    - useEffect sur [isStreaming] : si true → Animated.loop(Animated.stagger(200, [fadeAnim(dot1), fadeAnim(dot2), fadeAnim(dot3)])).start() ; si false → stop + reset
    - Fonction fadeAnim(dot) = Animated.sequence([Animated.timing(dot, {toValue:1, duration:400, useNativeDriver:true}), Animated.timing(dot, {toValue:0.3, duration:400, useNativeDriver:true})])
    - Rendu : si isStreaming → View flexDirection 'row' gap 4 padding 12 alignSelf 'flex-start' backgroundColor surface borderRadius 16 shadow ; 3 Animated.View 6×6 borderRadius 3 backgroundColor '#6B6963' style={{opacity: dot1/dot2/dot3}}
    - Insérer ce composant dots à la fin de la FlatList/ScrollView (après le dernier message assistant)

    CONVERSATION LIST HEADER redessiné :
    - Identifier comment la liste des conversations est affichée (header, drawer, ou section)
    - Redessin selon extras.jsx : chaque conversation item → View backgroundColor surface borderRadius 14 borderWidth 1 borderColor '#E2E0DA' padding 12 marginBottom 6 ; Text conversation.title fontWeight '600' fontSize 14 color '#1C1A17' ; Text date relative fontSize 11 color '#6B6963' marginTop 2 ; si conversation active → borderColor '#FF5C1A' backgroundColor 'rgba(255,92,26,0.04)'
    - Bouton "Nouvelle conversation" : View backgroundColor '#FF5C1A' borderRadius 12 padding 12 flexDirection 'row' alignItems 'center' gap 8 marginBottom 12 ; Ionicons 'add-outline' size 18 color '#FFFFFF' ; Text "Nouvelle conversation" fontWeight '700' color '#FFFFFF'

    Ne pas modifier : la logique SSE, appendMessage, sendMessage, les routes API, la gestion des messages, useAIStore ou équivalent.
  </action>
  <verify>
    <automated>grep -c 'ai_credits_balance\|creditsBalance\|credits' /c/ziko-platform/apps/mobile/app/\(app\)/ai/chat.tsx</automated>
  </verify>
  <done>chat.tsx avec credit chip lisant ai_credits_balance, streaming dots animés pendant les réponses, conversation list redessinée ; logique AI streaming intacte ; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2 : Compléter AvatarUploadScreen — image picker, upload Storage, progression, update avatar_url</name>
  <files>apps/mobile/app/(app)/profile/avatar.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/avatar.tsx — lire les 416 lignes complètes pour identifier : ce qui est déjà implémenté (picker? upload?), les fixtures éventuelles, les imports existants, comment supabase est passé
  </read_first>
  <action>
    Lire attentivement le fichier existant. Identifier les gaps :
    - Si expo-image-picker n'est pas importé → ajouter l'import
    - Si le flux upload vers Supabase Storage est absent ou incomplet → le compléter
    - Si la mutation update avatar_url est absente → l'ajouter
    - Supprimer tout fixture d'URL d'avatar hardcodée

    FLUX COMPLET À GARANTIR :

    ÉTAPE 1 — SÉLECTION :
    - Fonction handlePickImage :
      1. const perm = await ImagePicker.requestMediaLibraryPermissionsAsync() ; si !perm.granted → showAlert('Permission requise', 'Autorise l\'accès à ta galerie dans les réglages.') ; return
      2. const result = await ImagePicker.launchImageLibraryAsync({ mediaTypes: ImagePicker.MediaTypeOptions.Images, allowsEditing: true, aspect: [1,1], quality: 0.8 })
      3. Si !result.canceled → setSelectedUri(result.assets[0].uri) ; setUploadProgress(0)

    ÉTAPE 2 — PRÉVISUALISATION :
    - Si selectedUri → Image source={{uri: selectedUri}} 120×120 borderRadius 60 dans un View centré
    - Sinon → Image source={{uri: currentAvatarUrl}} ou View initiales fallback
    - Bouton "Changer la photo" (TouchableOpacity sous l'avatar) onPress={handlePickImage}

    ÉTAPE 3 — UPLOAD :
    - useState uploadProgress = 0 (0–1)
    - useState isUploading = false
    - Fonction handleUpload :
      1. setIsUploading(true)
      2. Lire le fichier : const response = await fetch(selectedUri) ; const blob = await response.blob()
      3. Upload : await supabase.storage.from('avatars').upload(`${userId}.jpg`, blob, { upsert: true, contentType: 'image/jpeg' })
      4. Si error → showAlert('Erreur', error.message) ; setIsUploading(false) ; return
      5. Obtenir URL publique : const { data: { publicUrl } } = supabase.storage.from('avatars').getPublicUrl(`${userId}.jpg`)
      6. Mutation update : await supabase.from('user_profiles').update({ avatar_url: publicUrl }).eq('id', userId)
      7. setIsUploading(false) ; setSelectedUri(null) ; showAlert('Succès', 'Photo de profil mise à jour !')
      8. Invalider le cache TanStack Query ['userProfile', userId] si queryClient disponible

    BARRE DE PROGRESSION :
    - Simuler la progression pendant l'upload (setUploadProgress dans un interval 0→0.9, puis 1 après succès)
    - View full-width height 4 backgroundColor '#E2E0DA' borderRadius 999 marginTop 16 ; View enfant width `${uploadProgress*100}%` height '100%' backgroundColor '#FF5C1A' borderRadius 999

    BOUTON SAUVEGARDER :
    - Si selectedUri → afficher bouton "Sauvegarder" height 48 backgroundColor '#FF5C1A' borderRadius 14 full-width marginTop 16
    - Si isUploading → ActivityIndicator blanc à l'intérieur ; sinon Text "Sauvegarder" fontWeight '700' color '#FFFFFF'

    paddingBottom: 100 ; showAlert pour les erreurs (depuis @ziko/plugin-sdk).
  </action>
  <verify>
    <automated>grep -c 'avatars\|upload\|ImagePicker' /c/ziko-platform/apps/mobile/app/\(app\)/profile/avatar.tsx</automated>
  </verify>
  <done>avatar.tsx avec flux complet : image picker expo, upload vers bucket avatars/{userId}.jpg, barre de progression, update user_profiles.avatar_url, showAlert ; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| avatar.tsx → Supabase Storage | Upload de fichier image ; chemin scopé userId |
| avatar.tsx → user_profiles | UPDATE avatar_url scopé auth.uid() ; RLS enforce |
| chat.tsx → user_profiles | Lecture seule ai_credits_balance scopé userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-40-02-01 | Tampering | Storage upload path | mitigate | Chemin = userId depuis session auth uniquement — l'utilisateur ne peut uploader que sur son propre chemin |
| T-40-02-02 | Information Disclosure | ai_credits_balance read | mitigate | Query filtrée .eq('id', userId) ; RLS enforce |
| T-40-02-03 | Denial of Service | Image upload size | accept | Quality 0.8 + aspect 1:1 limite la taille ; quota Supabase Storage suffisant pour avatars |
| T-40-02-SC | Tampering | npm/pip/cargo installs | mitigate | expo-image-picker déjà dans le projet (vérifié avant install) |
</threat_model>

<verification>
1. Credit chip : `grep 'ai_credits_balance\|creditsBalance' /c/ziko-platform/apps/mobile/app/\(app\)/ai/chat.tsx` retourne un match
2. Streaming dots : `grep 'Animated\|dot1\|streaming' /c/ziko-platform/apps/mobile/app/\(app\)/ai/chat.tsx` retourne un match
3. ImagePicker : `grep 'ImagePicker\|launchImageLibraryAsync' /c/ziko-platform/apps/mobile/app/\(app\)/profile/avatar.tsx` retourne un match
4. Upload Storage : `grep "from('avatars')\|storage.*avatars" /c/ziko-platform/apps/mobile/app/\(app\)/profile/avatar.tsx` retourne un match
5. Update avatar_url : `grep 'avatar_url' /c/ziko-platform/apps/mobile/app/\(app\)/profile/avatar.tsx` retourne un match
6. Progression : `grep 'uploadProgress\|Progress' /c/ziko-platform/apps/mobile/app/\(app\)/profile/avatar.tsx` retourne un match
7. TypeScript : `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep 'ai/chat\|profile/avatar' | head -10`
</verification>

<success_criteria>
- chat.tsx : credit chip visible lisant ai_credits_balance, streaming dots animés, conversation list redessinée ; logique AI streaming non modifiée
- avatar.tsx : flux complet pick → upload → progression → update avatar_url ; showAlert pour les erreurs et le succès
- TypeScript : zéro nouvelle erreur dans ces deux fichiers
</success_criteria>

<output>
Créer `.planning/workstreams/milestone-mobile/phases/40-extra-screens/40-02-SUMMARY.md` une fois terminé.
</output>
