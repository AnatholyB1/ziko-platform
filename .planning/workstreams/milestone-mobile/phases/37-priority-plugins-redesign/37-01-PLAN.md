---
plan: 37-01
phase: 37
wave: 1
depends_on: []
autonomous: true
files_modified:
  - packages/ui/src/components/SubTabs.tsx
  - packages/ui/src/components/AISuggestion.tsx
  - plugins/nutrition/src/screens/NutritionPlugin.tsx
  - apps/mobile/app/(app)/(plugins)/nutrition/dashboard.tsx
  - plugins/nutrition/src/index.ts
requirements:
  - PLUG-N-01
  - PLUG-N-02
  - PLUG-N-03
  - PLUG-N-04
  - PLUG-N-05
  - PLUG-N-06
  - PLUG-N-07
must_haves:
  truths:
    - SubTabs.tsx renders pill container style (not underline), same props API
    - AISuggestion.tsx renders "Coach IA · suggestion" label above tip text
    - NutritionPlugin.tsx exists with 4 SubTabs and real TanStack Query data
    - Route wrapper imports NutritionPlugin (not NutritionDashboard)
    - NutritionDashboard.tsx deleted with zero remaining imports verified
  artifacts:
    - path: plugins/nutrition/src/screens/NutritionPlugin.tsx
      provides: Nutrition plugin entrypoint with 4 SubTabs
      exports: [NutritionPlugin]
    - path: packages/ui/src/components/SubTabs.tsx
      provides: Pill-style segmented bar (visual update only, same props)
    - path: packages/ui/src/components/AISuggestion.tsx
      provides: AI tip card with "Coach IA · suggestion" label
  key_links:
    - from: apps/mobile/app/(app)/(plugins)/nutrition/dashboard.tsx
      to: plugins/nutrition/src/screens/NutritionPlugin.tsx
      via: "@ziko/plugin-nutrition/screens/NutritionPlugin import"
    - from: NutritionPlugin.tsx (Aujourd'hui tab)
      to: nutrition_logs
      via: "TanStack Query useQuery filtering by today's date"
  goal: Nutrition plugin matches 037-UI-SPEC §1 with SubTabs pill style, AISuggestion label, real data
---

<objective>
Wave 0: Patch two shared design system components (SubTabs pill style + AISuggestion label) used by all 6 plugins in Phase 37. Then deliver the full Nutrition plugin redesign as a single entrypoint file (NutritionPlugin.tsx) with 4 internal SubTabs, SVG calorie ring, macro bars, TanStack Query data wiring, and old dashboard deletion.

Purpose: All subsequent plans (37-02 through 37-06) depend on the updated SubTabs and AISuggestion components. Nutrition is the most complex plugin (4 tabs, SVG ring, macro bars) and establishes the implementation pattern for all others.

Output: Updated @ziko/ui components, new NutritionPlugin.tsx, updated route wrapper, deleted NutritionDashboard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-RESEARCH.md

<interfaces>
<!-- Verified component interfaces from Phase 37 research (Q9) -->

SubTabs (packages/ui/src/components/SubTabs.tsx) — CURRENT:
```typescript
interface SubTabsProps {
  tabs: string[];
  active: string;       // string — NOT index number
  onChange: (tab: string) => void;
}
```
Visual update target (037-UI-SPEC §0 SubTabs): pill container style.
Container: flexDirection row, gap 4, padding 4, backgroundColor rgba(28,26,23,0.05), borderRadius 12, marginBottom 16.
Each tab: flex 1, paddingVertical 8, paddingHorizontal 8, borderRadius 8, fontSize 12, fontWeight 700.
Active: backgroundColor '#FFFFFF', shadowOpacity 0.08, shadowRadius 4, elevation 1, color text.
Inactive: backgroundColor transparent, color muted.

AISuggestion (packages/ui/src/components/AISuggestion.tsx) — CURRENT:
```typescript
interface AISuggestionProps {
  text: string;
  actionLabel?: string;
  onAction?: () => void;
  tintColor?: string;   // defaults to theme.primary
}
```
Missing: "Coach IA · suggestion" label above tip text.
Add: label tier (11px), fontWeight 700, uppercase, letterSpacing 0.06em, color tintColor (or primary fallback).
Render above the tip text inside the card.

PluginHeader — correct as-is, no changes:
```typescript
interface PluginHeaderProps {
  title: string;
  onBack: () => void;
  right?: React.ReactNode;
  dark?: boolean;
}
```

Established patterns (Phase 36 + 35):
```typescript
const [activeTab, setActiveTab] = useState<string>('Aujourd\'hui');
// TanStack Query:
const { data, isLoading, isError } = useQuery({
  queryKey: ['nutrition_today', userId, today],
  queryFn: async () => { /* supabase query */ },
  enabled: !!userId,
});
// useMutation:
const mutation = useMutation({
  mutationFn: async (entry) => { const { error } = await supabase.from('...').insert(entry); if (error) throw error; },
  onSuccess: () => queryClient.invalidateQueries({ queryKey: ['nutrition_today'] }),
});
```

Design tokens:
- background: #F7F6F3, surface: #FFFFFF, border: #E2E0DA
- primary: #FF5C1A, text: #1C1A17, muted: #6B6963
- Card shadow: shadowColor '#1C1A17', shadowOffset {0,4}, shadowOpacity 0.08, shadowRadius 12, elevation 3
- ScrollView paddingBottom: 100 (tab bar clearance)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Patch SubTabs (pill style) and AISuggestion (label)</name>
  <files>packages/ui/src/components/SubTabs.tsx, packages/ui/src/components/AISuggestion.tsx</files>
  <read_first>
    - packages/ui/src/components/SubTabs.tsx — read current implementation to understand what changes
    - packages/ui/src/components/AISuggestion.tsx — read current implementation to add label
    - .planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md §0 (SubTabs Actual Rendering + AISuggestion Rendering sections)
  </read_first>
  <action>
    SubTabs.tsx — keep props interface (tabs/active/onChange) unchanged. Replace the render body:
    Outer container: View with flexDirection 'row', gap 4, padding 4, backgroundColor 'rgba(28,26,23,0.05)', borderRadius 12, marginBottom 16.
    Each tab: TouchableOpacity with flex 1, paddingVertical 8, paddingHorizontal 8, borderRadius 8, alignItems 'center'.
    Active tab inner style: backgroundColor '#FFFFFF', shadowColor '#1C1A17', shadowOffset {width:0,height:2}, shadowOpacity 0.08, shadowRadius 4, elevation 1.
    Active tab text: fontSize 12, fontWeight '700', color theme.text.
    Inactive tab text: fontSize 12, fontWeight '700', color theme.muted, backgroundColor transparent.
    Use useThemeStore to get theme tokens.

    AISuggestion.tsx — keep all existing props/layout. Before the tip text (the `text` prop rendering), add a Text element:
    "COACH IA · SUGGESTION" — fontSize 11, fontWeight '700', letterSpacing 0.06, textTransform 'uppercase', color tintColor (or theme.primary if tintColor not provided), marginBottom 4.
    No other layout changes.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit -p packages/ui/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SubTabs.tsx contains 'rgba(28,26,23,0.05)' as container backgroundColor
    - SubTabs.tsx active tab uses backgroundColor '#FFFFFF' with shadowOpacity
    - SubTabs.tsx does NOT contain 'borderBottom' or 'underline' (removed)
    - AISuggestion.tsx contains 'COACH IA · SUGGESTION' text string (or 'Coach IA · suggestion' in uppercase via textTransform)
    - TypeScript check passes for packages/ui
  </acceptance_criteria>
  <done>Both components updated; pill style and label verified; TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build NutritionPlugin.tsx — 4-tab entrypoint with real data</name>
  <files>plugins/nutrition/src/screens/NutritionPlugin.tsx</files>
  <read_first>
    - plugins/nutrition/src/screens/NutritionDashboard.tsx — read existing implementation (understand patterns to migrate, types, supabase usage)
    - plugins/nutrition/src/screens/LogMealScreen.tsx — read to confirm router path for navigation (D-02)
    - plugins/nutrition/src/screens/TDEECalculatorScreen.tsx — check export name and router path
    - plugins/nutrition/src/components/ScoreBadge.tsx — read to reuse in redesign
    - .planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md §1 (full Nutrition spec — §1.1 through §1.7)
  </read_first>
  <behavior>
    - Aujourd'hui tab: useQuery(['nutrition_today', userId, today]) fetches nutrition_logs WHERE user_id AND date = today; aggregates calories, protein_g, carbs_g, fat_g grouped by meal_type
    - Calorie ring: SVG arc at (consumed/target)*2π*38 strokeDasharray using react-native-svg Svg/Circle primitives; transform rotate(-90deg)
    - Macro bars: 3 View bars with width={(consumed/target)*100}+'%', Protéines=#FF5C1A, Glucides=#2E7BF6, Lipides=#F59E0B
    - AISuggestion rule: if protein_g < 0.30 * protein_target_g → tip with gap value; else generic encouragement
    - Meal cards: 4 rows (Petit-déj/Déjeuner/Collation/Dîner), logged ones show icon+name+time+kcal, missing ones show placeholder at opacity 0.65
    - Ajouter tab: search input + 3 shortcut cards (Scanner/Photo IA/Repas vite) + AISuggestion + Récents list from last 5 deduplicated nutrition_logs
    - Historique tab: useQuery(['nutrition_history', userId]) last 7 days grouped by date; 7-bar chart + Macros moyens card
    - Réglages tab: useQuery for user_profiles calorie/macro goals; Slider from @react-native-community/slider; settings list rows (non-functional chevrons acceptable for rows without backing screens)
    - useMutation for quick-add Récents (same insert as LogMealScreen)
    - All styling via NativeWind className or inline style objects for dynamic values only
    - Loading: ActivityIndicator or shimmer rectangles; Error: "Impossible de charger tes données" + Réessayer
    - paddingBottom: 100 on ScrollView contentContainerStyle
  </behavior>
  <action>
    Create plugins/nutrition/src/screens/NutritionPlugin.tsx as a single default export function NutritionPlugin({ supabase }: { supabase: SupabaseClient }).
    Internal state: const [activeTab, setActiveTab] = useState('Aujourd\'hui').
    Import: SubTabs, AISuggestion, PluginHeader from '@ziko/ui'; useThemeStore from '@ziko/plugin-sdk'; useQuery, useMutation, useQueryClient from '@tanstack/react-query'; Svg, Circle from 'react-native-svg'; Slider from '@react-native-community/slider'; router from 'expo-router'; showAlert from '@ziko/plugin-sdk'.
    Implement 4 tab content sections as inline components or conditional blocks:
    - TodayTab: calorie ring SVG (92×92, r=38, strokeWidth=8, strokeDasharray computed), macro bars 3-col grid, AISuggestion (protein rule per PLUG-N-06), Repas du jour section with 4 meal type rows
    - AddTab: search input (paddingLeft 40 with absolute search icon), 3-col shortcuts grid, AISuggestion (Photo IA tip tintColor #2E7BF6), Récents list with quick-add mutation
    - HistoriqueTab: 7-bar chart (height 112, today orange, past rgba(255,92,26,0.22)), Macros moyens 7j card (4 rows P/G/L/F with colored progress bars)
    - ReglagesTab: Objectif calorique Slider (min=1200 max=4000 step=50 accentColor='#FF5C1A'), AISuggestion with recalcTDEE → router.push to TDEECalculatorScreen, Settings list card (5 rows with chevron icons, non-interactive except Recalculer)
    Header right element: pill CTA "+ Ajouter" (bg primary, padding 8 12, borderRadius 999, text white fontSize 12 fontWeight 700) shown only when activeTab === 'Aujourd\'hui'; onPress sets activeTab to 'Ajouter'.
    Card pattern: backgroundColor surface, borderRadius 16, borderWidth 1, borderColor border, shadow as per design tokens.
    Nutrition user profile goals: useQuery(['nutrition_goals', userId]) → supabase.from('user_profiles').select('weight_kg, goal').eq('id', userId).single(); derive protein/carbs/fat targets from goal field or hardcode defaults (protein: 140g, carbs: 200g, fat: 60g) with note.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "nutrition/src/screens/NutritionPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - NutritionPlugin.tsx exports a default function named NutritionPlugin
    - File contains 'useState' with string tab values ('Aujourd\'hui', 'Ajouter', 'Historique', 'Réglages')
    - File contains 'SubTabs' import from '@ziko/ui'
    - File contains 'AISuggestion' import from '@ziko/ui'
    - File contains 'Svg' and 'Circle' from 'react-native-svg' (calorie ring)
    - File contains 'useQuery' for nutrition_logs data
    - File contains 'protein_g' < check for AISuggestion rule (PLUG-N-06)
    - File contains 'LogMealScreen' navigation reference or router.push path to LogMeal route
    - File does NOT contain 'NUTRITION_TODAY' fixture object
    - TypeScript: zero errors for this file
  </acceptance_criteria>
  <done>NutritionPlugin.tsx created with 4 SubTabs, SVG ring, macro bars, real TanStack Query hooks, AISuggestion rule, no fixture data; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire route wrapper + barrel, delete old dashboard</name>
  <files>apps/mobile/app/(app)/(plugins)/nutrition/dashboard.tsx, plugins/nutrition/src/index.ts</files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/nutrition/dashboard.tsx — read current import to confirm old name
    - plugins/nutrition/src/index.ts — read all exports to update barrel
    - plugins/nutrition/src/screens/NutritionDashboard.tsx — do NOT modify, only check import count before delete
  </read_first>
  <action>
    Step 1 — Update route wrapper: change the import in apps/mobile/app/(app)/(plugins)/nutrition/dashboard.tsx from 'NutritionDashboard' to 'NutritionPlugin' using import path '@ziko/plugin-nutrition/screens/NutritionPlugin'. Keep the wrapper component body unchanged (it just renders the imported component with supabase prop).

    Step 2 — Update barrel: in plugins/nutrition/src/index.ts, change the export for NutritionDashboard to export NutritionPlugin from './screens/NutritionPlugin'. Keep all other exports (LogMealScreen, TDEECalculatorScreen, ScoreBadge) unchanged.

    Step 3 — Delete-after-verify: run grep to count remaining references to 'NutritionDashboard' in apps/ and plugins/nutrition/src/ (excluding the old file itself and any worktree paths). If count = 0, delete plugins/nutrition/src/screens/NutritionDashboard.tsx. If any references remain, list them and fix before deleting.

    Grep command to verify: grep -r 'NutritionDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/nutrition/src/index.ts --include='*.ts' --include='*.tsx' -l
  </action>
  <verify>
    <automated>grep -r 'NutritionDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/nutrition/src --include='*.ts' --include='*.tsx' | grep -v 'node_modules' | grep -v '.worktrees' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(plugins)/nutrition/dashboard.tsx imports 'NutritionPlugin' (not 'NutritionDashboard')
    - plugins/nutrition/src/index.ts exports NutritionPlugin (not NutritionDashboard)
    - grep for 'NutritionDashboard' in apps/ and plugins/nutrition/src/ returns 0 results (excluding worktrees)
    - plugins/nutrition/src/screens/NutritionDashboard.tsx does NOT exist (deleted)
    - TypeScript check: cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS' should return 0
  </acceptance_criteria>
  <done>Route wrapper updated, barrel updated, old dashboard deleted with zero remaining imports confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Supabase | nutrition_logs reads are scoped by user_id; RLS on nutrition_logs enforces auth.uid() = user_id |
| Plugin → user_profiles | calorie/macro goals read-only; Réglages tab writes calorie goal via update (user_id scoped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Information Disclosure | nutrition_logs query | mitigate | All queries include .eq('user_id', userId) from auth session; RLS double-enforces |
| T-37-01-02 | Tampering | user_profiles calorie goal write | mitigate | Update scoped to .eq('id', userId); Slider clamped to min=1200 max=4000 (no arbitrary injection) |
| T-37-01-03 | Tampering | Quick-add mutation from Récents | accept | Values come from previous user-owned logs, not free-form input; low risk |
| T-37-01-SC | Tampering | npm installs | mitigate | No new packages — all dependencies confirmed installed (react-native-svg, @react-native-community/slider) |
</threat_model>

<verification>
1. TypeScript clean: `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'` returns 0
2. SubTabs renders pill container: grep 'rgba(28,26,23,0.05)' packages/ui/src/components/SubTabs.tsx returns a match
3. AISuggestion has label: grep -i 'coach ia' packages/ui/src/components/AISuggestion.tsx returns a match
4. NutritionPlugin exported: grep 'export default function NutritionPlugin' plugins/nutrition/src/screens/NutritionPlugin.tsx
5. Old file gone: test -f plugins/nutrition/src/screens/NutritionDashboard.tsx should fail (exit 1)
6. No fixture data: grep 'NUTRITION_TODAY' plugins/nutrition/src/screens/NutritionPlugin.tsx returns empty
</verification>

<success_criteria>
- SubTabs.tsx shows pill/capsule style (not underline) for all 6 upcoming plugins
- AISuggestion.tsx shows "COACH IA · SUGGESTION" label above every tip in all 6 plugins
- NutritionPlugin.tsx renders 4 SubTabs, SVG calorie ring, macro bars, and real nutrition_logs data
- Zero fixture data (NUTRITION_TODAY) in production plugin files
- Route wrapper correctly loads NutritionPlugin
- NutritionDashboard.tsx deleted with zero import references remaining
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-01-SUMMARY.md` when done.
</output>
