---
plan: 37-02
phase: 37
wave: 2
depends_on: [37-01]
autonomous: true
files_modified:
  - plugins/hydration/src/screens/HydrationPlugin.tsx
  - apps/mobile/app/(app)/(plugins)/hydration/dashboard.tsx
  - plugins/hydration/src/index.ts
requirements:
  - PLUG-H-01
  - PLUG-H-02
  - PLUG-H-03
  - PLUG-H-04
  - PLUG-H-05
must_haves:
  truths:
    - HydrationPlugin.tsx exists with 3 SubTabs (Aujourd'hui/Historique/Réglages)
    - SVG bottle-fill visualization renders at today_ml / goal_ml ratio with blue fill
    - +250/+500/+750ml buttons call hydration_logs insert mutation immediately
    - Custom button opens BottomSheet with numeric input
    - 7-day bar chart renders on Historique tab from real hydration_logs data
    - HydrationDashboard.tsx deleted with zero remaining imports
  artifacts:
    - path: plugins/hydration/src/screens/HydrationPlugin.tsx
      provides: Hydration plugin entrypoint with 3 SubTabs and SVG bottle
      exports: [HydrationPlugin]
  key_links:
    - from: apps/mobile/app/(app)/(plugins)/hydration/dashboard.tsx
      to: plugins/hydration/src/screens/HydrationPlugin.tsx
      via: "@ziko/plugin-hydration/screens/HydrationPlugin import"
    - from: HydrationPlugin.tsx (Aujourd'hui tab)
      to: hydration_logs
      via: "TanStack Query useQuery sum(amount_ml) WHERE user_id AND date = today"
  goal: Hydration plugin matches 037-UI-SPEC §2 with SVG bottle, quick-log buttons, real data
---

<objective>
Build the full Hydration plugin redesign as a single entrypoint HydrationPlugin.tsx with 3 internal SubTabs. Key UI: SVG bottle-fill visualization (clip-path approach) and 4 quick-log buttons (+250/+500/+750ml/Custom). All data from TanStack Query against hydration_logs. Old HydrationDashboard.tsx deleted.

Purpose: Hydration is the second priority plugin. Depends on Plan 37-01 for the updated SubTabs (pill style) and AISuggestion (label) shared components.

Output: HydrationPlugin.tsx, updated route wrapper, updated barrel, deleted HydrationDashboard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-RESEARCH.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-01-SUMMARY.md

<interfaces>
<!-- Verified interfaces from research Q9 + UI-SPEC §2 -->

SVG Bottle spec (UI-SPEC §2.3):
- viewBox "0 0 80 140", width=80, height=140
- Outline path: d="M28 8h24v14l8 14v100a6 6 0 0 1-6 6H26a6 6 0 0 1-6-6V36l8-14V8z", fill=none, stroke=#2E7BF6, strokeWidth=2
- ClipPath id="bottle-clip": same path
- Fill rect: x=0, y={140-(fillRatio*120)}, width=80, height={fillRatio*120}, fill=#2E7BF6, opacity=0.25, clipPath="url(#bottle-clip)"
- Surface line: x=0, y={140-(fillRatio*120)}, width=80, height=4, fill=#2E7BF6, opacity=0.85, clipPath="url(#bottle-clip)"
- fillRatio = Math.min(1, today_ml / goal_ml)
- Center overlay: "{(today_ml/1000).toFixed(1)}L" display tier 28px fontWeight 700 color #2E7BF6; "/ {(goal_ml/1000).toFixed(1)}L" label tier 11px color muted

Quick-log buttons (UI-SPEC §2.3):
- 4-column grid, gap 8; each card: Card style, padding 16 8, alignItems center, gap 4
- +250ml, +500ml, +750ml: immediate mutation → hydration_logs insert; confirmation flash (card bg rgba(46,123,246,0.15) for 300ms via useState flash)
- Custom: opens BottomSheet with numeric TextInput + "Logger" button

7-day chart (Historique, UI-SPEC §2.4):
- height 112, row, alignItems flex-end, gap 8, max scale 3000ml
- Today bar: bg #2E7BF6; past bars reached goal (>=goal_ml): rgba(46,123,246,0.35); below: rgba(46,123,246,0.18)
- minHeight 4 per bar

Hydration_logs table schema (migration 012):
- user_id UUID, amount_ml INTEGER, date DATE, created_at TIMESTAMPTZ

user_profiles fields for hydration goal: user may have hydration_goal_ml column — check during implementation; if absent, default to 2000ml saved to user_profiles.settings.hydration_goal_ml via JSONB.

Design tokens: primary #FF5C1A, info #2E7BF6, success #2E9E5B, surface #FFFFFF, border #E2E0DA, background #F7F6F3, text #1C1A17, muted #6B6963.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build HydrationPlugin.tsx — 3-tab entrypoint with SVG bottle and quick-log</name>
  <files>plugins/hydration/src/screens/HydrationPlugin.tsx</files>
  <read_first>
    - plugins/hydration/src/screens/HydrationDashboard.tsx — read existing implementation (patterns, Supabase usage, existing hydration store)
    - .planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md §2 (full Hydration spec §2.1–§2.6)
  </read_first>
  <behavior>
    - useQuery(['hydration_today', userId, today]): sum of hydration_logs.amount_ml WHERE user_id AND date = today
    - fillRatio = Math.min(1, today_ml / goal_ml); SVG bottle renders correctly at 0%, 50%, 100%
    - +250ml button triggers useMutation which inserts hydration_log {user_id, amount_ml: 250, date: today}; onSuccess invalidates ['hydration_today'] queryKey
    - Custom modal: TextInput accepts number, "Logger" triggers same insert mutation with custom amount
    - Streak: derived from last N days where sum(amount_ml) >= goal_ml (consecutive from today backward)
    - Historique tab: useQuery(['hydration_history', userId]) last 7 days; each day bar height = (sum/3000)*100 + '%' capped at 100%; day labels ['L','M','M','J','V','S','D']
    - Réglages tab: daily goal editable (TouchableOpacity opens numeric input inline or sheet); settings rows for reminders/glass size/coffee/workout bonus (display only for non-backing rows)
    - AISuggestion rule (Aujourd'hui): check if majority of logs created_at hour > 14; if yes show distribution tip; else generic
    - AISuggestion (Historique): workout day vs rest day comparison (if data available; else generic)
  </behavior>
  <action>
    Create plugins/hydration/src/screens/HydrationPlugin.tsx as default export HydrationPlugin({ supabase }).
    Internal state: const [activeTab, setActiveTab] = useState('Aujourd\'hui'); const [showCustomSheet, setShowCustomSheet] = useState(false); const [customAmount, setCustomAmount] = useState(''); const [flashingButton, setFlashingButton] = useState(null as null | number).
    Import: SubTabs, AISuggestion, PluginHeader from '@ziko/ui'; useThemeStore, showAlert from '@ziko/plugin-sdk'; useQuery, useMutation, useQueryClient from '@tanstack/react-query'; Svg, Path, Rect, Defs, ClipPath from 'react-native-svg'; router from 'expo-router'.
    Today tab layout (top to bottom): Bottle+Stats card (row, gap 16), AISuggestion (tintColor '#2E7BF6'), Logger rapide section (4 buttons), Aujourd'hui log list.
    Bottle: Svg width=80 height=80 (adjust to 140 per spec) viewBox "0 0 80 140". Use Defs/ClipPath for bottle clip. Rect for fill level. Path for outline. Absolute positioned Text overlay (separate View outside SVG, position absolute inset 0 center).
    Flash effect: on quick-log button press, set flashingButton to amount value, trigger timeout 300ms to reset to null; button backgroundColor uses flashingButton === amount ? 'rgba(46,123,246,0.20)' : 'rgba(46,123,246,0.12)'.
    Custom sheet: Modal or View at bottom (position absolute, bottom 0, left 0, right 0, bg surface, borderRadius 16, padding 16); TextInput keyboardType='numeric'; "Logger" calls logMutation.mutate({amount_ml: parseInt(customAmount)}); closes sheet after success.
    Goal value: useQuery(['hydration_goals', userId]) fetching user_profiles settings.hydration_goal_ml; default 2000 if absent.
    Streak computation: in useQuery for last 7 days data, compute consecutive days from today where daily_sum >= goal_ml.
    Card shadow pattern: shadowColor '#1C1A17', shadowOffset {width:0,height:4}, shadowOpacity 0.08, shadowRadius 12, elevation 3, borderRadius 16, borderWidth 1, borderColor theme.border.
    paddingBottom 100 on all ScrollView contentContainerStyle.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "hydration/src/screens/HydrationPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - HydrationPlugin.tsx exports default function HydrationPlugin
    - File contains 'ClipPath' and 'Rect' from 'react-native-svg' (SVG bottle fill)
    - File contains useState for 'Aujourd\'hui' as initial tab (string)
    - File contains 'useQuery' with 'hydration_today' or 'hydration_logs' queryKey
    - File contains 'useMutation' for hydration_log insert
    - File contains 'flashingButton' state or equivalent for 300ms flash confirmation
    - File does NOT contain 'WATER' fixture object or 'WATER_DATA' constant
    - TypeScript: zero errors for this file
  </acceptance_criteria>
  <done>HydrationPlugin.tsx created with 3 SubTabs, SVG bottle-fill, quick-log buttons with flash, 7-day chart, real TanStack Query data; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire route wrapper + barrel, delete old dashboard</name>
  <files>apps/mobile/app/(app)/(plugins)/hydration/dashboard.tsx, plugins/hydration/src/index.ts</files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/hydration/dashboard.tsx — read current import (confirm old name: HydrationDashboard)
    - plugins/hydration/src/index.ts — read all exports to update safely
  </read_first>
  <action>
    Step 1 — Update route wrapper: change import from 'HydrationDashboard' to 'HydrationPlugin' using path '@ziko/plugin-hydration/screens/HydrationPlugin'. Keep component body unchanged.

    Step 2 — Update barrel: in plugins/hydration/src/index.ts, replace HydrationDashboard export with export of HydrationPlugin from './screens/HydrationPlugin'. Preserve all other exports.

    Step 3 — Delete-after-verify: grep for 'HydrationDashboard' across /c/ziko-platform/apps and /c/ziko-platform/plugins/hydration/src excluding .worktrees and node_modules. If count = 0, delete plugins/hydration/src/screens/HydrationDashboard.tsx.
  </action>
  <verify>
    <automated>grep -r 'HydrationDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/hydration/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(plugins)/hydration/dashboard.tsx imports 'HydrationPlugin'
    - plugins/hydration/src/index.ts exports HydrationPlugin
    - grep 'HydrationDashboard' in apps/ and plugins/hydration/src/ returns 0 lines
    - plugins/hydration/src/screens/HydrationDashboard.tsx does NOT exist
    - TypeScript: zero new errors introduced
  </acceptance_criteria>
  <done>Route wrapper updated, barrel updated, HydrationDashboard.tsx deleted with zero remaining imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Supabase | hydration_logs writes scoped by user_id from auth session |
| Custom amount input → mutation | User-provided integer; must be validated before insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Tampering | Custom amount TextInput → hydration_logs insert | mitigate | Parse with parseInt and validate > 0 and <= 5000 before mutation; reject NaN/negative silently |
| T-37-02-02 | Information Disclosure | hydration_logs query | mitigate | .eq('user_id', userId) on all queries; RLS enforces same |
| T-37-02-SC | Tampering | npm installs | mitigate | No new packages — react-native-svg confirmed installed |
</threat_model>

<verification>
1. TypeScript clean: `cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'` returns 0
2. SVG bottle present: grep 'ClipPath' plugins/hydration/src/screens/HydrationPlugin.tsx returns a match
3. Quick-log mutations: grep 'useMutation' plugins/hydration/src/screens/HydrationPlugin.tsx returns a match
4. Old file gone: test -f plugins/hydration/src/screens/HydrationDashboard.tsx fails (exit 1)
5. No WATER fixture: grep 'WATER' plugins/hydration/src/screens/HydrationPlugin.tsx returns empty
</verification>

<success_criteria>
- HydrationPlugin renders SVG bottle fill at correct ratio from real hydration_logs data
- Quick-log buttons (+250/+500/+750ml) save to hydration_logs immediately with visual flash confirmation
- Custom amount BottomSheet opens, validates input, inserts to DB
- 7-day bar chart shows real daily totals on Historique tab
- HydrationDashboard.tsx deleted with zero import references remaining
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-02-SUMMARY.md` when done.
</output>
