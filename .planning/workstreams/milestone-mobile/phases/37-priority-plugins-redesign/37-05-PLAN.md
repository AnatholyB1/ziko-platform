---
plan: 37-05
phase: 37
wave: 2
depends_on: [37-01]
autonomous: true
files_modified:
  - supabase/migrations/20260526_add_user_profiles_settings.sql
  - plugins/persona/src/screens/CoachIAPlugin.tsx
  - apps/mobile/app/(app)/(plugins)/persona/customize.tsx
  - plugins/persona/src/index.ts
  - backend/api/src/context/user.ts
requirements:
  - PLUG-CIA-01
  - PLUG-CIA-02
  - PLUG-CIA-03
  - PLUG-CIA-04
  - PLUG-CIA-05
must_haves:
  truths:
    - Migration adds user_profiles.settings JSONB IF NOT EXISTS
    - CoachIAPlugin.tsx exists with 3 SubTabs (Discussion/Personas/Réglages)
    - Discussion tab shows ai_conversations list + credit chip in PluginHeader
    - Personas tab shows 4 persona cards (max/zoe/leo/rio) with selected state ring; selection saves to user_profiles.settings.ai_persona
    - Réglages tab saves ai_language/ai_coaching_style/ai_response_length to user_profiles.settings JSONB
    - fetchUserContext() in user.ts reads settings.ai_persona and injects persona instruction into system prompt
    - PersonaCustomizeScreen.tsx deleted with zero remaining imports
  artifacts:
    - path: supabase/migrations/20260526_add_user_profiles_settings.sql
      provides: user_profiles.settings JSONB column migration
      contains: "ALTER TABLE public.user_profiles ADD COLUMN IF NOT EXISTS settings"
    - path: plugins/persona/src/screens/CoachIAPlugin.tsx
      provides: Coach IA plugin entrypoint with 3 SubTabs
      exports: [CoachIAPlugin]
    - path: backend/api/src/context/user.ts
      provides: fetchUserContext reads settings.ai_persona and returns ai_persona field
  key_links:
    - from: CoachIAPlugin.tsx Personas tab
      to: user_profiles.settings.ai_persona
      via: "useMutation: supabase.from('user_profiles').update({ settings: {...existing, ai_persona: id} })"
    - from: backend/api/src/context/user.ts
      to: ai.ts buildSystemPrompt
      via: "userCtx.profile.ai_persona injected into sections[] as persona instruction"
  goal: Coach IA plugin matches 037-UI-SPEC §5; persona selection persisted to user_profiles.settings; backend injects persona into AI system prompt
---

<objective>
Wave 0: Add the user_profiles.settings JSONB migration (blocking for D-08 and D-09). Then build CoachIAPlugin.tsx with 3 internal SubTabs (Discussion/Personas/Réglages), the 4-persona selector cards, coaching settings rows, and update fetchUserContext() in user.ts to inject the selected persona into the AI system prompt.

Purpose: This is the most backend-impactful plan in Phase 37. The JSONB migration and persona injection in user.ts are prerequisites for correct persona behavior in all future AI conversations. Depends on 37-01 for updated shared components; runs in parallel with 37-02, 37-03, 37-04.

Output: SQL migration, CoachIAPlugin.tsx, updated route wrapper, updated barrel, updated backend user.ts, deleted PersonaCustomizeScreen.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-RESEARCH.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-01-SUMMARY.md

<interfaces>
<!-- Verified from research Q6, Q7, Q10 + UI-SPEC §5 -->

user_profiles.settings JSONB read/write (from profile/settings.tsx pattern):
  Read: const { data } = await supabase.from('user_profiles').select('settings').eq('id', userId).single();
  Write (merge): const current = (data as any)?.settings ?? {};
                 await supabase.from('user_profiles').update({ settings: { ...current, ai_persona: 'max' } }).eq('id', userId);
  Keys: ai_persona ('max'|'zoe'|'leo'|'rio'), ai_language ('fr'|'en'), ai_coaching_style ('direct'|'encouraging'|'technical'), ai_response_length ('short'|'medium'|'detailed')

Persona definitions (UI-SPEC §5.4):
  const PERSONAS = [
    { id: 'max', name: 'Max', style: 'Sergent motivant', desc: 'Te pousse, sans pitié, dans l\'effort.', color: '#FF5C1A', emoji: '💪' },
    { id: 'zoe', name: 'Zoé', style: 'Bienveillante', desc: 'Encourage, rassure, valorise tes progrès.', color: '#7B5BD0', emoji: '🌿' },
    { id: 'leo', name: 'Léo', style: 'Tactique pragmatique', desc: 'Données, optimisation, performance.', color: '#2E7BF6', emoji: '📊' },
    { id: 'rio', name: 'Rio', style: 'Cool & relax', desc: 'Le pote qui dédramatise et rend ça fun.', color: '#2E9E5B', emoji: '🏖️' },
  ]

Credit balance (research Q10):
  import { useCreditStore } from '../../../../src/stores/creditStore' (relative from plugins/persona/src/screens/)
  const balance = useCreditStore((s) => s.balance);
  const fetchBalance = useCreditStore((s) => s.fetchBalance);
  useEffect(() => { if (accessToken) fetchBalance(accessToken); }, [accessToken])

ai_conversations table (migration 001):
  user_id UUID, title TEXT, created_at, updated_at TIMESTAMPTZ
  RLS: auth.uid() = user_id (standard)
  Last message preview: join ai_messages WHERE conversation_id, order created_at DESC, limit 1

fetchUserContext (research Q6, user.ts):
  Current select: 'name, age, weight_kg, height_cm, goal, units'
  Change to: 'name, age, weight_kg, height_cm, goal, units, settings'
  Add to returned profile object: ai_persona: (profileRes.data?.settings as any)?.ai_persona ?? null
  Extend UserContext interface with ai_persona?: string | null on profile object

Backend persona prompt injection (UI-SPEC §5.6):
  const personaPrompts = {
    max: 'Tu t\'appelles Max, tu es un coach sport motivant et exigeant, style sergent d\'élite.',
    zoe: 'Tu t\'appelles Zoé, tu es une coach bienveillante qui encourage et valorise les progrès.',
    leo: 'Tu t\'appelles Léo, tu es un coach analytique qui optimise les performances avec les données.',
    rio: 'Tu t\'appelles Rio, tu es un coach détendu et fun qui rend le sport accessible et sympa.',
  }
  Inject into buildSystemPrompt() or fetchUserContext() return — add personaInstruction to profile
  Location: backend/api/src/context/user.ts returns personaInstruction alongside ai_persona
  Caller (routes/ai.ts buildSystemPrompt): if userCtx.profile?.ai_persona → push persona section to sections[]

Discussion tab - navigation to AIChatDetailScreen:
  Find existing AIChatDetailScreen route in apps/mobile — check apps/mobile/app/(app)/ai/ for chat detail screen
  Navigate: router.push('/ai/chat/' + conversationId) or equivalent existing route
  New conversation: create ai_conversation row via useMutation, then navigate with new id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Add user_profiles.settings JSONB migration</name>
  <files>supabase/migrations/20260526_add_user_profiles_settings.sql</files>
  <read_first>
    - supabase/migrations/ directory listing — check highest existing migration number to confirm 20260526 is next in sequence (ls supabase/migrations/ | sort)
    - supabase/migrations/001_initial_schema.sql — verify user_profiles table structure to confirm settings column is absent
  </read_first>
  <action>
    Create supabase/migrations/20260526_add_user_profiles_settings.sql with exactly this content:
    -- Phase 37: Add settings JSONB to user_profiles for persona and coaching preferences
    -- Safe: ADD COLUMN IF NOT EXISTS; idempotent on live DB where column may already exist
    ALTER TABLE public.user_profiles
      ADD COLUMN IF NOT EXISTS settings JSONB NOT NULL DEFAULT '{}';

    COMMENT ON COLUMN public.user_profiles.settings IS 'User AI and app preferences. Keys: ai_persona, ai_language, ai_coaching_style, ai_response_length, notif_prefs, hydration_goal_ml.';

    No other SQL. The IF NOT EXISTS clause makes this safe whether or not the column already exists in production.
  </action>
  <verify>
    <automated>test -f /c/ziko-platform/supabase/migrations/20260526_add_user_profiles_settings.sql && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260526_add_user_profiles_settings.sql exists
    - File contains 'ADD COLUMN IF NOT EXISTS settings JSONB'
    - File contains 'NOT NULL DEFAULT'
    - File does NOT drop any column or alter existing columns
  </acceptance_criteria>
  <done>Migration file created with IF NOT EXISTS JSONB settings column; idempotent and safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build CoachIAPlugin.tsx — 3-tab entrypoint + backend persona injection</name>
  <files>plugins/persona/src/screens/CoachIAPlugin.tsx, backend/api/src/context/user.ts</files>
  <read_first>
    - plugins/persona/src/screens/PersonaCustomizeScreen.tsx — read existing implementation (note: uses persona_settings table; new screen uses user_profiles.settings — do NOT use the old table)
    - backend/api/src/context/user.ts — read full fetchUserContext() implementation to understand select, return shape, and UserContext interface
    - backend/api/src/routes/ai.ts — read buildSystemPrompt() to understand how to inject persona section
    - apps/mobile/src/stores/creditStore.ts — read balance + fetchBalance signature
    - apps/mobile/app/(app)/ai/ — check for existing AIChatDetailScreen route path
    - .planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md §5 (§5.1–§5.7 full Coach IA spec)
  </read_first>
  <behavior>
    - Discussion tab: useQuery(['ai_conversations', userId]) fetches ai_conversations WHERE user_id ordered by updated_at DESC limit 20; each row shows conversation.title or "Nouvelle conversation", preview from ai_messages.content (last message), updated_at formatted as ago-time
    - Personas tab: useQuery(['user_settings', userId]) fetches user_profiles.settings; activePersonaId = settings?.ai_persona ?? 'max'; persona card selected state = borderWidth 2 borderColor persona.color; tap → useMutation merge-updates settings.ai_persona
    - Réglages tab: same settings query; rows show current ai_coaching_style/ai_response_length values; tapping a row → inline picker or showAlert options + mutation
    - Credit chip: useCreditStore balance shown in PluginHeader right slot (flash icon + "{balance}" text)
    - New conversation: useMutation inserts ai_conversations {user_id, title: 'Nouvelle conversation'}, then router.push to chat screen with new id
    - Quick prompts: each chip creates new conversation with pre-filled message param
    - Backend user.ts: add 'settings' to user_profiles select; add ai_persona to returned profile; add personaInstruction string to return value; in routes/ai.ts buildSystemPrompt add persona section
  </behavior>
  <action>
    PART A — CoachIAPlugin.tsx:
    Create plugins/persona/src/screens/CoachIAPlugin.tsx as default export CoachIAPlugin({ supabase }).
    Internal state: const [activeTab, setActiveTab] = useState('Discussion').
    Import: SubTabs, AISuggestion, PluginHeader from '@ziko/ui'; useThemeStore, showAlert from '@ziko/plugin-sdk'; useQuery, useMutation, useQueryClient from '@tanstack/react-query'; router from 'expo-router'; useCreditStore from '../../../../src/stores/creditStore'; useSession from supabase auth hook.

    Const PERSONAS array with 4 objects (max/zoe/leo/rio) as per UI-SPEC §5.4 table — id, name, style, desc, color, emoji. Define at file top level (not inside component).

    Discussion tab:
    - Active persona banner card: avatar circle 44×44 (bg persona.color), emoji, name + style muted, status "● En ligne · répond en moins de 2s" green (#2E9E5B); settings gear button → setActiveTab('Réglages')
    - Conversation list: FlatList of conversation cards (row: avatar circle 36×36, title, preview, time ago)
    - "Nouvelle conversation" CTA button: bg primary, borderRadius 16
    - Quick prompts: horizontal ScrollView with 4 ghost chips ("Plan ma séance", "Ai-je récupéré ?", "Mes macros du soir ?", "Motivation")
    - Tap conversation → router.push to existing AI chat screen (check apps/mobile/app/(app)/ai/ for correct path)

    Personas tab:
    - Intro text body tier 13px color muted
    - 4 persona cards: vertical list; selected → borderWidth 2 borderColor persona.color; avatar circle 56×56 bg persona.color; "Actif" chip on selected (success chip style); name title tier 17px fontWeight 700; style label tier 11px color persona.color; desc body tier 13px color muted
    - Tap persona card → mutation: read current settings → merge {ai_persona: persona.id} → update

    Réglages tab:
    - AISuggestion: "Active 'Notif quotidiennes du coach'" tip tintColor primary
    - Settings list card: 5 rows (Notifications quotidiennes, Ton du coach, Fréquence des suggestions, Données partagées, Effacer l'historique); row 5 uses showAlert with OS destructive button before deleting all ai_conversations WHERE user_id
    - Settings rows 2-4: tapping shows showAlert with options; selected value displayed as row right element
    - ai_coaching_style values: ['direct', 'encouraging', 'technical']; display labels: 'Direct', 'Encourageant', 'Technique'
    - ai_response_length values: ['short', 'medium', 'detailed']; display labels: 'Court', 'Moyen', 'Détaillé'

    PluginHeader right: View (row, gap 8, paddingHorizontal 8, paddingVertical 4, borderRadius 999, bg 'rgba(255,92,26,0.10)'): Ionicons 'flash-outline' 12px color '#FF5C1A'; Text balance fontWeight 700 color '#FF5C1A' fontSize 12.

    PART B — backend/api/src/context/user.ts:
    Edit fetchUserContext():
    1. Change user_profiles select from 'name, age, weight_kg, height_cm, goal, units' to 'name, age, weight_kg, height_cm, goal, units, settings'
    2. In the returned profile object add: ai_persona: (profileRes.data?.settings as any)?.ai_persona ?? null
    3. Add personaInstruction field to returned UserContext object:
       const personaPrompts: Record<string, string> = { max: 'Tu t\'appelles Max...', zoe: 'Tu t\'appelles Zoé...', leo: 'Tu t\'appelles Léo...', rio: 'Tu t\'appelles Rio...' } (same strings from UI-SPEC §5.6)
       personaInstruction: personaPrompts[profile.ai_persona ?? 'max'] ?? personaPrompts.max

    Edit backend/api/src/routes/ai.ts (if buildSystemPrompt is there):
    In buildSystemPrompt: if userCtx.profile?.ai_persona → sections.push('## Coaching Persona\n' + userCtx.personaInstruction)
    Or add as first section of system prompt before other context.

    Extend UserContext interface (or inline type) to include: profile.ai_persona?: string | null and personaInstruction: string.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "persona/src/screens/CoachIAPlugin\|context/user" | head -15</automated>
  </verify>
  <acceptance_criteria>
    - CoachIAPlugin.tsx exports default function CoachIAPlugin
    - File contains PERSONAS array with 4 entries including id 'max', 'zoe', 'leo', 'rio'
    - File contains useCreditStore import (credit chip in header)
    - File contains ai_persona key in settings merge mutation
    - File contains 'showAlert' for history deletion with destructive button
    - File does NOT import from 'persona_settings' table (legacy — forbidden)
    - File does NOT contain 'PERSONAS' fixture constant or 'COACH_MESSAGES' constant
    - backend/api/src/context/user.ts contains 'settings' in user_profiles select string
    - backend/api/src/context/user.ts contains 'ai_persona' in returned profile object
    - backend/api/src/context/user.ts contains 'personaPrompts' or 'personaInstruction' constant
    - TypeScript: zero errors for both files
  </acceptance_criteria>
  <done>CoachIAPlugin.tsx created with 3 SubTabs, 4-persona selector, coaching settings, credit chip; fetchUserContext() updated to inject persona into system prompt; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire route wrapper + barrel, delete old PersonaCustomizeScreen</name>
  <files>apps/mobile/app/(app)/(plugins)/persona/customize.tsx, plugins/persona/src/index.ts</files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/persona/customize.tsx — read current import (PersonaCustomizeScreen)
    - plugins/persona/src/index.ts — read all exports (PersonaCustomizeScreen, usePersonaStore, buildPersonaSystemPrompt)
  </read_first>
  <action>
    Step 1 — Update route wrapper: change import from 'PersonaCustomizeScreen' to 'CoachIAPlugin' using path '@ziko/plugin-persona/screens/CoachIAPlugin'. Keep component body unchanged.

    Step 2 — Update barrel: in plugins/persona/src/index.ts, add export of CoachIAPlugin from './screens/CoachIAPlugin'. Leave usePersonaStore and buildPersonaSystemPrompt exports in place (they may be used by legacy code still in src/index.ts — do not remove unless grep confirms zero usage). Remove PersonaCustomizeScreen export.

    Step 3 — Delete-after-verify: grep 'PersonaCustomizeScreen' in /c/ziko-platform/apps and /c/ziko-platform/plugins/persona/src excluding .worktrees and node_modules. If count = 0, delete plugins/persona/src/screens/PersonaCustomizeScreen.tsx.
  </action>
  <verify>
    <automated>grep -r 'PersonaCustomizeScreen' /c/ziko-platform/apps /c/ziko-platform/plugins/persona/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(plugins)/persona/customize.tsx imports 'CoachIAPlugin'
    - plugins/persona/src/index.ts exports CoachIAPlugin
    - grep 'PersonaCustomizeScreen' in apps/ and plugins/persona/src/ returns 0 lines
    - plugins/persona/src/screens/PersonaCustomizeScreen.tsx does NOT exist
    - TypeScript: zero new errors
  </acceptance_criteria>
  <done>Route wrapper updated, barrel updated, PersonaCustomizeScreen.tsx deleted with zero remaining imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Supabase | user_profiles.settings writes scoped by user_id; persona selection cannot affect another user |
| Backend user.ts → AI system prompt | Persona instruction injected from DB value; must sanitize to prevent prompt injection from malicious ai_persona values |
| Discussion tab → ai_conversations delete | "Effacer l'historique" deletes all conversations; must require OS destructive confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Tampering | user_profiles.settings write (persona) | mitigate | Update scoped to .eq('id', userId); persona value must be one of ['max','zoe','leo','rio'] — validate on write |
| T-37-05-02 | Spoofing | Persona injection in system prompt | mitigate | personaInstruction sourced from server-side hardcoded map (not from raw DB string); DB value is just a key lookup |
| T-37-05-03 | Tampering | ai_conversations delete all | mitigate | showAlert with OS destructive button required; delete scoped to .eq('user_id', userId) |
| T-37-05-04 | Information Disclosure | ai_conversations + ai_messages query | mitigate | .eq('user_id', userId) enforced; RLS on ai_conversations and ai_messages |
| T-37-05-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
1. Migration file: test -f supabase/migrations/20260526_add_user_profiles_settings.sql
2. Settings column SQL: grep 'ADD COLUMN IF NOT EXISTS settings JSONB' supabase/migrations/20260526_add_user_profiles_settings.sql
3. Persona injection backend: grep 'ai_persona' backend/api/src/context/user.ts returns a match
4. Persona prompts backend: grep 'personaPrompts\|personaInstruction' backend/api/src/context/user.ts returns a match
5. No fixture data: grep 'PERSONAS\|COACH_MESSAGES' plugins/persona/src/screens/CoachIAPlugin.tsx returns empty
6. No persona_settings table: grep 'persona_settings' plugins/persona/src/screens/CoachIAPlugin.tsx returns empty
7. Old file gone: test -f plugins/persona/src/screens/PersonaCustomizeScreen.tsx fails
8. TypeScript: rtk tsc --noEmit 2>&1 | grep -c 'error TS' returns 0
</verification>

<success_criteria>
- user_profiles.settings JSONB migration is safe and idempotent (IF NOT EXISTS)
- CoachIAPlugin renders ai_conversations list on Discussion tab with credit chip in header
- 4 persona cards (Max/Zoé/Léo/Rio) show selected state and save to user_profiles.settings.ai_persona
- Coaching settings (style/length) saved to user_profiles.settings JSONB keys
- fetchUserContext() in user.ts injects persona instruction into AI system prompt via hardcoded prompt map
- PersonaCustomizeScreen.tsx deleted with zero import references remaining
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-05-SUMMARY.md` when done.
</output>
