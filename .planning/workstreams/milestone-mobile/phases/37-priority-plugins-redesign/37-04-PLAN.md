---
plan: 37-04
phase: 37
wave: 2
depends_on: [37-01]
autonomous: true
files_modified:
  - plugins/ai-programs/src/screens/AIProgramsPlugin.tsx
  - apps/mobile/app/(app)/(plugins)/ai-programs/dashboard.tsx
  - plugins/ai-programs/src/index.ts
requirements:
  - PLUG-AI-01
  - PLUG-AI-02
  - PLUG-AI-03
  - PLUG-AI-04
  - PLUG-AI-05
  - PLUG-AI-06
must_haves:
  truths:
    - AIProgramsPlugin.tsx exists with 3 SubTabs (Programme/Générer/Bibliothèque)
    - Programme tab renders dark hero card with active ai_generated_programs row (is_active = true)
    - Programme tab shows empty state when no active program exists
    - Générer tab renders launch card with "Générer un programme" CTA navigating to workout/ai-generate.tsx
    - Bibliothèque tab renders past programs list with "Réactiver" button (two-step is_active update per D-11)
    - AIProgramsDashboard.tsx deleted with zero remaining imports
  artifacts:
    - path: plugins/ai-programs/src/screens/AIProgramsPlugin.tsx
      provides: AI Programs plugin entrypoint with 3 SubTabs, dark hero card, Réactiver mutation
      exports: [AIProgramsPlugin]
  key_links:
    - from: AIProgramsPlugin.tsx Programme tab
      to: ai_generated_programs
      via: "useQuery WHERE user_id AND is_active = true, single row"
    - from: Bibliothèque tab Réactiver button
      to: ai_generated_programs
      via: "useMutation: SET is_active=false WHERE user_id, then SET is_active=true WHERE id"
  goal: AI Programs plugin matches 037-UI-SPEC §4 with dark hero, is_active wiring, navigate-to AIGenerator
---

<objective>
Build the full AI Programs plugin redesign as AIProgramsPlugin.tsx with 3 internal SubTabs. Key features: dark hero card for the active program (bg #1C1A17), AISuggestion, two-step Réactiver mutation on past programs, and a launch card that navigates to the existing AIGenerator wizard.

Purpose: AI Programs is the fourth priority plugin. Runs in parallel with Plans 37-02 and 37-03, depends on 37-01 for updated shared components.

Output: AIProgramsPlugin.tsx, updated route wrapper, updated barrel, deleted AIProgramsDashboard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-RESEARCH.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-01-SUMMARY.md

<interfaces>
<!-- Verified from research Q2 + UI-SPEC §4 -->

ai_generated_programs table (migration 012):
- id UUID PK, user_id UUID, goal TEXT, program_data JSONB, is_active BOOLEAN DEFAULT false, created_at TIMESTAMPTZ
- Note: program_data JSONB shape inferred as { name?, weeks?, sessions_per_week?, sessions: [] }; use optional chaining for all program_data fields

Active program query:
  supabase.from('ai_generated_programs').select('*').eq('user_id', userId).eq('is_active', true).maybeSingle()
  → returns null if no active program (maybeSingle doesn't throw on 0 rows)

All programs query (Bibliothèque):
  supabase.from('ai_generated_programs').select('*').eq('user_id', userId).order('created_at', { ascending: false })

Réactiver mutation (D-11, per research Q2):
  Step 1: await supabase.from('ai_generated_programs').update({ is_active: false }).eq('user_id', userId)
  Step 2: await supabase.from('ai_generated_programs').update({ is_active: true }).eq('id', programId)
  Then: queryClient.invalidateQueries({ queryKey: ['ai_programs', userId] }); setActiveTab('Programme')

Dark hero card spec (UI-SPEC §4.3):
- backgroundColor '#1C1A17', borderRadius 16, padding 16, borderWidth 0, overflow 'hidden'
- Glow: absolute circle 140×140, borderRadius 70, backgroundColor 'rgba(46,123,246,0.30)', top -32, right -32
- Chip: "Programme actif" — bg rgba(46,123,246,0.25), color '#9DC4FF', Ionicons "sparkles" 12px
- Program name: fontSize 20, fontWeight 700, color '#FFFAF6'
- Progress sub: "Semaine {week}/{totalWeeks} · {sessions}/{total} séances" — fontSize 13, color 'rgba(255,250,246,0.6)'
- Progress bar: bg rgba(255,250,246,0.12), fill bg #FF5C1A (solid primary, not gradient), height 4
- CTAs: "Prochaine séance" → router.push('/workout/session'); "Détails" → ghost style bg rgba(255,250,246,0.10) color '#FFFAF6'

Navigate to AIGenerator: router.push('/workout/ai-generate') — confirmed file at apps/mobile/app/(app)/workout/ai-generate.tsx (D-10)
Navigate to session: router.push('/workout/session') (D-12)

AISuggestion rule (Programme tab): if programData available → positive reinforcement; else generic (since session tracking is opaque from program_data JSONB). Use generic text with program name.
AISuggestion rule (Générer tab): read user_profiles.goal; if 'hypertrophie' + sessions_per_week >= 4 → "Push/Pull/Legs 8 semaines"; if 'force' → "5×5 12 semaines"; else "Endurance progressive 6 semaines".
AISuggestion rule (Bibliothèque): count of past programs (length of library list); if > 0 → mention count + suggest new cycle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AIProgramsPlugin.tsx — 3-tab entrypoint with dark hero and Réactiver</name>
  <files>plugins/ai-programs/src/screens/AIProgramsPlugin.tsx</files>
  <read_first>
    - plugins/ai-programs/src/screens/AIProgramsDashboard.tsx — read existing implementation (patterns, store usage)
    - plugins/ai-programs/src/screens/GenerateProgram.tsx — check export name and navigation patterns (reference only)
    - .planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md §4 (§4.1–§4.6)
  </read_first>
  <behavior>
    - useQuery(['ai_program_active', userId]): fetch ai_generated_programs WHERE user_id AND is_active=true via .maybeSingle()
    - useQuery(['ai_programs', userId]): fetch all programs for Bibliothèque tab ordered by created_at DESC
    - useQuery(['user_profile', userId]): fetch user_profiles goal and weight_kg for AISuggestion rules + Générer tab
    - Réactiver useMutation: two-step clear-all + set-active; optimistic: update local library list active states immediately; on success setActiveTab('Programme') + invalidate
    - Programme tab: if activeProgram → dark hero card; if null → empty state ("Aucun programme actif") with CTA setActiveTab('Générer')
    - Générer tab: static launch card (info chip, title, description, CTA → router.push('/workout/ai-generate')); AISuggestion with recommendation based on user_profiles.goal
    - Bibliothèque tab: FlatList of program cards; each shows name, status chip (active/completed), progress bar width from program_data.sessions or default; "Réactiver" button on non-active programs
    - "Prochaine séance" CTA → router.push('/workout/session') (D-12)
  </behavior>
  <action>
    Create plugins/ai-programs/src/screens/AIProgramsPlugin.tsx as default export AIProgramsPlugin({ supabase }).
    Internal state: const [activeTab, setActiveTab] = useState('Programme').
    Import: SubTabs, AISuggestion, PluginHeader from '@ziko/ui'; useThemeStore, showAlert from '@ziko/plugin-sdk'; useQuery, useMutation, useQueryClient from '@tanstack/react-query'; useRouter from 'expo-router'; useSession from '@supabase/auth-helpers-react' or derive userId from supabase.auth.getUser().

    Programme tab:
    - isLoading: shimmer dark card (height 200, bg '#1C1A17', borderRadius 16, opacity 0.4) + shimmer AISuggestion
    - Active program card: absolute glow View (width/height 140, borderRadius 70, bg 'rgba(46,123,246,0.30)', position 'absolute', top -32, right -32, zIndex 0); all content at zIndex 1; chip View (bg 'rgba(46,123,246,0.25)', paddingHorizontal 8, paddingVertical 4, borderRadius 999, flexDirection 'row', gap 4, alignSelf 'flex-start', marginBottom 8); program name text; progress sub; progress bar View (bg 'rgba(255,250,246,0.12)'); fill View (bg '#FF5C1A'); CTA row with two buttons
    - Empty state: Ionicons 'barbell-outline' 48px color muted, centered; title; sub; primary CTA button
    - AISuggestion below card: tintColor '#2E7BF6'

    Générer tab:
    - Launch card: chip "Génération IA" (bg rgba(46,123,246,0.08) color #2E7BF6); title "Crée ton programme sur mesure"; sub description; CTA button (bg primary, borderRadius 12, "Générer un programme" + sparkles icon); onPress router.push('/workout/ai-generate')
    - AISuggestion: dynamic text based on user_profiles.goal

    Bibliothèque tab:
    - AISuggestion: count of programs; text mentions {N} programmes
    - Program list: ScrollView of cards; each card: name (title tier 17px fontWeight 700); sub row "En cours · {progress}% · {weeks} sem" or "Terminé · 100% · {weeks} sem"; progress bar (active: primary, completed: #2E9E5B); "Réactiver" ghost button (if program.id !== activeProgram?.id)
    - progress derived from program_data.sessions?.length or fallback to 0; weeks from program_data.weeks or 8

    paddingBottom 100 on ScrollViews.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "ai-programs/src/screens/AIProgramsPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - AIProgramsPlugin.tsx exports default function AIProgramsPlugin
    - File contains 'is_active' in at least one Supabase query (activeProgram fetch)
    - File contains two-step Réactiver mutation: .update({ is_active: false }).eq('user_id' + .update({ is_active: true }).eq('id'
    - File contains '#1C1A17' as dark card backgroundColor (Programme hero)
    - File contains router.push('/workout/ai-generate') (D-10)
    - File contains router.push('/workout/session') (D-12)
    - File does NOT contain 'AI_PROGRAMS' fixture constant
    - TypeScript: zero errors for this file
  </acceptance_criteria>
  <done>AIProgramsPlugin.tsx created with 3 SubTabs, dark hero card, is_active wiring, Réactiver mutation, AIGenerator navigation, real TanStack Query data; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire route wrapper + barrel, delete old dashboard</name>
  <files>apps/mobile/app/(app)/(plugins)/ai-programs/dashboard.tsx, plugins/ai-programs/src/index.ts</files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/ai-programs/dashboard.tsx — read current import (AIProgramsDashboard)
    - plugins/ai-programs/src/index.ts — read all exports
  </read_first>
  <action>
    Step 1 — Update route wrapper: change import from 'AIProgramsDashboard' to 'AIProgramsPlugin' using path '@ziko/plugin-ai-programs/screens/AIProgramsPlugin'. Keep component body unchanged.

    Step 2 — Update barrel: in plugins/ai-programs/src/index.ts, replace AIProgramsDashboard export with AIProgramsPlugin from './screens/AIProgramsPlugin'. Preserve GenerateProgram and ImportFileScreen exports.

    Step 3 — Delete-after-verify: grep 'AIProgramsDashboard' in /c/ziko-platform/apps and /c/ziko-platform/plugins/ai-programs/src excluding .worktrees and node_modules. If count = 0, delete plugins/ai-programs/src/screens/AIProgramsDashboard.tsx.
  </action>
  <verify>
    <automated>grep -r 'AIProgramsDashboard' /c/ziko-platform/apps /c/ziko-platform/plugins/ai-programs/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(plugins)/ai-programs/dashboard.tsx imports 'AIProgramsPlugin'
    - plugins/ai-programs/src/index.ts exports AIProgramsPlugin (not AIProgramsDashboard)
    - grep 'AIProgramsDashboard' in apps/ and plugins/ai-programs/src/ returns 0 lines
    - plugins/ai-programs/src/screens/AIProgramsDashboard.tsx does NOT exist
    - TypeScript: zero new errors
  </acceptance_criteria>
  <done>Route wrapper updated, barrel updated, AIProgramsDashboard.tsx deleted with zero remaining imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Supabase | ai_generated_programs reads and writes scoped by user_id |
| Réactiver mutation | Two-step update — clear all then activate one; both scoped by user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Tampering | Réactiver mutation (is_active writes) | mitigate | Both UPDATE calls use .eq('user_id', userId); user cannot activate another user's program |
| T-37-04-02 | Information Disclosure | ai_generated_programs query | mitigate | .eq('user_id', userId) on all queries; RLS enforces same |
| T-37-04-03 | Elevation of Privilege | router.push to workout/session | accept | Navigation only — no data written at nav time; session screen has its own auth guard |
| T-37-04-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
1. TypeScript clean: `rtk tsc --noEmit 2>&1 | grep -c 'error TS'` returns 0
2. Dark hero: grep "'#1C1A17'" plugins/ai-programs/src/screens/AIProgramsPlugin.tsx returns a match
3. Réactiver: grep 'is_active.*false\|is_active.*true' plugins/ai-programs/src/screens/AIProgramsPlugin.tsx returns 2+ matches
4. Navigate to generator: grep 'ai-generate' plugins/ai-programs/src/screens/AIProgramsPlugin.tsx returns a match
5. Old file gone: test -f plugins/ai-programs/src/screens/AIProgramsDashboard.tsx fails
6. No fixture data: grep 'AI_PROGRAMS' plugins/ai-programs/src/screens/AIProgramsPlugin.tsx returns empty
</verification>

<success_criteria>
- AIProgramsPlugin renders dark hero card for active program from real ai_generated_programs.is_active data
- "Générer un programme" CTA navigates to /workout/ai-generate (existing AIGenerator wizard)
- "Prochaine séance" CTA navigates to /workout/session
- Réactiver button updates is_active in two steps (clear all → set one)
- AIProgramsDashboard.tsx deleted with zero import references remaining
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-04-SUMMARY.md` when done.
</output>
