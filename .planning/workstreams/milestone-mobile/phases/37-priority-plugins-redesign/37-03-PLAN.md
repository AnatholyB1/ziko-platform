---
plan: 37-03
phase: 37
wave: 2
depends_on: [37-01]
autonomous: true
files_modified:
  - plugins/habits/src/screens/HabitsPlugin.tsx
  - apps/mobile/app/(app)/(plugins)/habits/dashboard.tsx
  - plugins/habits/src/index.ts
requirements:
  - PLUG-HAB-01
  - PLUG-HAB-02
  - PLUG-HAB-03
  - PLUG-HAB-04
  - PLUG-HAB-05
  - PLUG-HAB-06
must_haves:
  truths:
    - HabitsPlugin.tsx exists with 3 SubTabs (Aujourd'hui/Historique/Nouvelle)
    - Aujourd'hui tab renders habit rows with completion toggle + streak chip from real habits + habit_logs data
    - Historique tab shows 30-day calendar heatmap (10×3 grid orange intensity) + per-habit streak list
    - Nouvelle tab shows 6 template cards in 2-column grid + "Créer une habitude perso" CTA
    - AISuggestion applies rule: habit missed > 3 of last 7 days → suggest adjustment
    - HabitsDashboardScreen.tsx deleted with zero remaining imports
  artifacts:
    - path: plugins/habits/src/screens/HabitsPlugin.tsx
      provides: Habits plugin entrypoint with 3 SubTabs, heatmap, completion toggle
      exports: [HabitsPlugin]
  key_links:
    - from: apps/mobile/app/(app)/(plugins)/habits/dashboard.tsx
      to: plugins/habits/src/screens/HabitsPlugin.tsx
      via: "@ziko/plugin-habits/screens/HabitsPlugin import"
    - from: HabitsPlugin.tsx (Aujourd'hui tab)
      to: habits + habit_logs
      via: "TanStack Query useQuery joining habits and today's habit_logs"
  goal: Habits plugin matches 037-UI-SPEC §3 with completion toggle, heatmap, template grid, real data
---

<objective>
Build the full Habits plugin redesign as a single entrypoint HabitsPlugin.tsx with 3 internal SubTabs. Key features: completion toggle with optimistic update (habits + habit_logs), 30-day calendar heatmap inline component, 6-template grid for new habits. All data from TanStack Query.

Purpose: Habits is the third priority plugin. Depends on Plan 37-01 for updated shared components (SubTabs pill style + AISuggestion label). Runs in parallel with Plan 37-02.

Output: HabitsPlugin.tsx, updated route wrapper, updated barrel, deleted HabitsDashboardScreen.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-RESEARCH.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-01-SUMMARY.md

<interfaces>
<!-- Verified from research Q9 + UI-SPEC §3 -->

habits table (migration 002): id UUID, user_id UUID, name TEXT, type TEXT ('boolean'|'count'), target INTEGER, emoji TEXT, color TEXT ('success'|'violet'|'warn'|'primary'|'info'), created_at TIMESTAMPTZ.

habit_logs table (migration 002): id UUID, habit_id UUID, user_id UUID, date DATE, count INTEGER DEFAULT 1, created_at TIMESTAMPTZ. Unique constraint on (habit_id, date).

Habit color tokens:
- 'success' → #2E9E5B
- 'violet' → #7B5BD0
- 'warn' → #F59E0B
- 'primary' → #FF5C1A
- 'info' → #2E7BF6

Habit icon map (UI-SPEC §3.3):
- leaf → "leaf-outline", smile → "happy-outline", moon → "moon-outline"
- x → "close-outline", stretch → "body-outline", shoe → "footsteps-outline"
- barbell → "barbell-outline"
Map habit.emoji to Ionicons name using this mapping (emoji field may be text like 'leaf' or actual emoji).

30-day heatmap (UI-SPEC §3.4):
- flexDirection 'row', flexWrap 'wrap', gap 4 — 10 columns × 3 rows = 30 cells
- Each cell: aspectRatio 1, borderRadius 4; done day: backgroundColor primary opacity 0.5 to 1.0 proportional to completion rate; missed: rgba(28,26,23,0.05)
- Compute for each day: total habits done / total habits = rate; if done → primary at opacity 0.5 + rate*0.5

AISuggestion rule (PLUG-HAB-05): if any habit missed > 3 of last 7 days → show "Tu rates '{habitName}' {N} fois sur 7. On baisse la cible ?" (per UI-SPEC §3.3)

Completion toggle mutation:
- Toggle: if no habit_log for today + this habit → insert; if log exists → delete (toggle off)
- Optimistic update: flip completed state immediately; revert on error + showAlert "Erreur de sauvegarde"
- Invalidate queryKey ['habits_today', userId, today] on success

HabitLogScreen reference: keep file for completion logic patterns; do NOT modify it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build HabitsPlugin.tsx — 3-tab entrypoint with completion toggle and heatmap</name>
  <files>plugins/habits/src/screens/HabitsPlugin.tsx</files>
  <read_first>
    - plugins/habits/src/screens/HabitsDashboardScreen.tsx — read existing implementation (patterns, Supabase queries, habit completion logic)
    - plugins/habits/src/screens/HabitLogScreen.tsx — read completion toggle logic to replicate in new screen
    - .planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/037-UI-SPEC.md §3 (§3.1–§3.6 full Habits spec)
  </read_first>
  <behavior>
    - useQuery(['habits', userId]): fetch all habits for user from 'habits' table
    - useQuery(['habit_logs_today', userId, today]): fetch habit_logs WHERE user_id AND date = today; gives set of completed habit_ids
    - useMutation for toggle: if habit completed → DELETE habit_log WHERE habit_id AND date AND user_id; if not completed → INSERT habit_log {habit_id, user_id, date: today, count: 1}; optimistic update via queryClient.setQueryData
    - Historique tab: useQuery(['habit_logs_month', userId]) last 30 days of habit_logs; compute per-day completion rate (logs_count / total_habits); render 30 cells
    - Streak per habit: consecutive days from today backward where habit_log exists for that habit
    - AISuggestion rule: for last 7 days of habit_logs, count per habit how many days missed; if any habit missed > 3 → show specific tip; else show generic streak encouragement
    - Nouvelle tab template grid: 6 hardcoded templates (Méditer/Lire/Marcher 10k pas/Pas d'alcool/Vitamines/Étirements) with icons and colors from UI-SPEC §3.5; tap creates habit with useMutation on 'habits' table
    - "Créer une habitude perso" CTA: open inline form (Modal or bottom sheet) with name TextInput, frequency picker, confirm button
  </behavior>
  <action>
    Create plugins/habits/src/screens/HabitsPlugin.tsx as default export HabitsPlugin({ supabase }).
    Internal state: const [activeTab, setActiveTab] = useState('Aujourd\'hui').
    Import: SubTabs, AISuggestion, PluginHeader, WeekStrip from '@ziko/ui'; useThemeStore, showAlert from '@ziko/plugin-sdk'; useQuery, useMutation, useQueryClient from '@tanstack/react-query'; router from 'expo-router'; format from 'date-fns'.

    HABIT COLOR RESOLVER: const habitColor = (color: string) => ({ success: '#2E9E5B', violet: '#7B5BD0', warn: '#F59E0B', primary: '#FF5C1A', info: '#2E7BF6' }[color] ?? '#FF5C1A').
    HABIT ICON MAP: const habitIcon = (emoji: string) => ({ leaf: 'leaf-outline', smile: 'happy-outline', moon: 'moon-outline', x: 'close-outline', stretch: 'body-outline', shoe: 'footsteps-outline', barbell: 'barbell-outline' }[emoji] ?? 'checkmark-outline').

    Aujourd'hui tab:
    - Summary card: done/total count with dot grid (one 20×20 dot per habit, colored if done)
    - AISuggestion computed from habit miss counts over last 7 days
    - Habit list: FlatList or ScrollView of habit rows; each row has completion button (36×36, borderRadius 8); done: habit.color bg + checkmark; undone: transparent + 2px border + habit icon; streak chip if streak > 0 (rgba(habitColor, 0.14) bg)
    - Completion button onPress: optimistically update local queryData then trigger mutation; on error showAlert + revert

    Historique tab:
    - Month overview card: completion rate % (display tier 32px), "+{delta}% vs dernier mois" chip
    - 30-day heatmap: View with flexDirection 'row', flexWrap 'wrap', gap 4; 30 cells each with width computed as (screenWidth - 32 - 4*9) / 10, aspectRatio 1, borderRadius 4; color based on day completion rate
    - Top streaks section: habits sorted by streak DESC; each row: habit icon circle + name + "{streak}j"

    Nouvelle tab:
    - AISuggestion: rule based on user_profiles.goal + last sleep log (if sleep plugin data available; else generic)
    - 2-column template grid: 6 template cards with icon + name + "+ Ajouter" label; tap inserts new habit + showAlert success
    - Primary CTA "Créer une habitude perso": opens Modal with TextInput name + frequency TouchableOpacity options (Tous les jours / Lun-Ven) + "Créer" button that calls createHabitMutation

    All card shadows per design token pattern. paddingBottom 100 on ScrollViews.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep "habits/src/screens/HabitsPlugin" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - HabitsPlugin.tsx exports default function HabitsPlugin
    - File contains useState with 'Aujourd\'hui' initial value
    - File contains 'habitColor' or 'getHabitColor' resolver function mapping 'success'→'#2E9E5B' etc.
    - File contains useMutation for completion toggle (insert + delete pattern)
    - File contains 30-cell heatmap logic (flexWrap 'wrap', 30 cells or computed days array)
    - File contains 6 template definitions (Méditer, Lire, Marcher, etc.)
    - File does NOT contain 'HABITS' fixture constant or 'DEFAULT_HABITS' array
    - TypeScript: zero errors for this file
  </acceptance_criteria>
  <done>HabitsPlugin.tsx created with 3 SubTabs, completion toggle with optimistic update, 30-day heatmap, template grid, real habits + habit_logs data; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire route wrapper + barrel, delete old dashboard</name>
  <files>apps/mobile/app/(app)/(plugins)/habits/dashboard.tsx, plugins/habits/src/index.ts</files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/habits/dashboard.tsx — read current import (HabitsDashboardScreen)
    - plugins/habits/src/index.ts — read all exports
  </read_first>
  <action>
    Step 1 — Update route wrapper: change import from 'HabitsDashboardScreen' to 'HabitsPlugin' using path '@ziko/plugin-habits/screens/HabitsPlugin'. Keep component body unchanged.

    Step 2 — Update barrel: in plugins/habits/src/index.ts, replace HabitsDashboardScreen export with HabitsPlugin from './screens/HabitsPlugin'. Preserve HabitLogScreen and any other exports.

    Step 3 — Delete-after-verify: grep 'HabitsDashboardScreen' in /c/ziko-platform/apps and /c/ziko-platform/plugins/habits/src excluding .worktrees and node_modules. If count = 0, delete plugins/habits/src/screens/HabitsDashboardScreen.tsx.
  </action>
  <verify>
    <automated>grep -r 'HabitsDashboardScreen' /c/ziko-platform/apps /c/ziko-platform/plugins/habits/src --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v .worktrees | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/(plugins)/habits/dashboard.tsx imports 'HabitsPlugin'
    - plugins/habits/src/index.ts exports HabitsPlugin (not HabitsDashboardScreen)
    - grep 'HabitsDashboardScreen' in apps/ and plugins/habits/src/ returns 0 lines
    - plugins/habits/src/screens/HabitsDashboardScreen.tsx does NOT exist
    - TypeScript: zero new errors
  </acceptance_criteria>
  <done>Route wrapper updated, barrel updated, HabitsDashboardScreen.tsx deleted with zero remaining imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Supabase | habit_logs writes scoped by user_id + habit_id; RLS enforces user ownership |
| Habit creation form → habits table | User-provided name and frequency; must not be empty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Tampering | Habit completion toggle mutation | mitigate | All inserts include user_id from auth session; unique constraint (habit_id, date) prevents duplicates |
| T-37-03-02 | Tampering | New habit creation form | mitigate | Validate habit name non-empty before mutation; trim whitespace; max length 100 chars |
| T-37-03-03 | Information Disclosure | habit_logs + habits query | mitigate | .eq('user_id', userId) on all queries; RLS double-enforces |
| T-37-03-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
1. TypeScript clean: `rtk tsc --noEmit 2>&1 | grep -c 'error TS'` returns 0
2. Completion toggle: grep 'useMutation' plugins/habits/src/screens/HabitsPlugin.tsx returns a match
3. Heatmap: grep 'flexWrap' plugins/habits/src/screens/HabitsPlugin.tsx returns 'wrap'
4. Templates: grep 'Méditer\|Marcher\|Vitamines' plugins/habits/src/screens/HabitsPlugin.tsx returns matches
5. Old file gone: test -f plugins/habits/src/screens/HabitsDashboardScreen.tsx fails
6. No fixture data: grep 'HABITS\|DEFAULT_HABITS' plugins/habits/src/screens/HabitsPlugin.tsx returns empty
</verification>

<success_criteria>
- HabitsPlugin renders habit rows with working completion toggle (optimistic update) from real habits + habit_logs
- 30-day heatmap renders orange intensity cells proportional to daily completion rate
- Template grid allows adding a habit from 6 preset templates
- AISuggestion shows habit miss tip when any habit missed > 3 of last 7 days
- HabitsDashboardScreen.tsx deleted with zero import references remaining
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-03-SUMMARY.md` when done.
</output>
