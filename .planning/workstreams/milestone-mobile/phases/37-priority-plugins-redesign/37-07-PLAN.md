---
plan: 37-07
phase: 37
wave: 3
depends_on: [37-01, 37-02, 37-03, 37-04, 37-05, 37-06]
autonomous: false
files_modified: []
requirements:
  - PLUG-N-01
  - PLUG-N-02
  - PLUG-N-03
  - PLUG-N-04
  - PLUG-N-05
  - PLUG-N-06
  - PLUG-N-07
  - PLUG-H-01
  - PLUG-H-02
  - PLUG-H-03
  - PLUG-H-04
  - PLUG-H-05
  - PLUG-HAB-01
  - PLUG-HAB-02
  - PLUG-HAB-03
  - PLUG-HAB-04
  - PLUG-HAB-05
  - PLUG-HAB-06
  - PLUG-AI-01
  - PLUG-AI-02
  - PLUG-AI-03
  - PLUG-AI-04
  - PLUG-AI-05
  - PLUG-AI-06
  - PLUG-CIA-01
  - PLUG-CIA-02
  - PLUG-CIA-03
  - PLUG-CIA-04
  - PLUG-CIA-05
  - PLUG-COM-01
  - PLUG-COM-02
  - PLUG-COM-03
  - PLUG-COM-04
  - PLUG-COM-05
must_haves:
  truths:
    - TypeScript compiles with zero errors across all 6 plugin files and backend
    - Zero fixture constants (NUTRITION_TODAY, WATER, HABITS, AI_PROGRAMS, PERSONAS, COACH_MESSAGES, FEED) in production plugin files
    - All 5 old dashboard files are confirmed deleted
    - Both SQL migrations exist on disk
    - Human smoke test passes for all 6 plugins on device/simulator
  goal: All 6 priority plugins redesigned, real data wired, old files deleted, TypeScript clean
---

<objective>
Phase 37 gate: automated grep and TypeScript checks, followed by a human smoke test checklist for all 6 plugins.

Purpose: Final quality gate before marking Phase 37 complete. Verifies all fixture data is gone, TypeScript is clean, migrations exist, and plugins render correctly on a real device/simulator.

Output: No new files. Human confirmation that all 6 plugins are visually correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-01-SUMMARY.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-02-SUMMARY.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-03-SUMMARY.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-04-SUMMARY.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-05-SUMMARY.md
@.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification — TypeScript, grep checks, migrations, deleted files</name>
  <files><!-- read-only verification task — no files modified; runs grep + tsc commands only --></files>
  <read_first>
    No files to read — this task runs shell commands only.
  </read_first>
  <action>
    Run the following checks in sequence. Report pass/fail for each. If any check fails, identify which plugin/file is responsible and stop — do not proceed to human checkpoint until all pass.

    CHECK 1 — TypeScript clean:
      cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'
      Expected: 0

    CHECK 2 — No fixture constants in plugin screens (grep across all 6 new files):
      grep -r 'NUTRITION_TODAY\|WATER_DATA\|WATER_LOG\|DEFAULT_HABITS\|AI_PROGRAMS\|PERSONAS\|COACH_MESSAGES\|FEED_DATA' \
        /c/ziko-platform/plugins/nutrition/src/screens/NutritionPlugin.tsx \
        /c/ziko-platform/plugins/hydration/src/screens/HydrationPlugin.tsx \
        /c/ziko-platform/plugins/habits/src/screens/HabitsPlugin.tsx \
        /c/ziko-platform/plugins/ai-programs/src/screens/AIProgramsPlugin.tsx \
        /c/ziko-platform/plugins/persona/src/screens/CoachIAPlugin.tsx \
        /c/ziko-platform/plugins/community/src/screens/CommunityPlugin.tsx 2>/dev/null | wc -l
      Expected: 0

    CHECK 3 — Wider fixture grep across all plugin dirs (catch any other static data constants):
      grep -rn 'NUTRITION_TODAY\|WATER\|HABITS\|AI_PROGRAMS\|PERSONAS\|COACH_MESSAGES\|= \[.*kcal\|= \[.*ml\|= \[.*streak' \
        /c/ziko-platform/plugins/nutrition/src/screens/NutritionPlugin.tsx \
        /c/ziko-platform/plugins/hydration/src/screens/HydrationPlugin.tsx \
        /c/ziko-platform/plugins/habits/src/screens/HabitsPlugin.tsx \
        /c/ziko-platform/plugins/ai-programs/src/screens/AIProgramsPlugin.tsx \
        /c/ziko-platform/plugins/persona/src/screens/CoachIAPlugin.tsx \
        /c/ziko-platform/plugins/community/src/screens/CommunityPlugin.tsx 2>/dev/null
      Expected: no output (zero matches)

    CHECK 4 — Old dashboard files are deleted:
      for f in \
        /c/ziko-platform/plugins/nutrition/src/screens/NutritionDashboard.tsx \
        /c/ziko-platform/plugins/hydration/src/screens/HydrationDashboard.tsx \
        /c/ziko-platform/plugins/habits/src/screens/HabitsDashboardScreen.tsx \
        /c/ziko-platform/plugins/ai-programs/src/screens/AIProgramsDashboard.tsx \
        /c/ziko-platform/plugins/persona/src/screens/PersonaCustomizeScreen.tsx \
        /c/ziko-platform/plugins/community/src/screens/CommunityDashboard.tsx; do
        test -f "$f" && echo "STILL EXISTS: $f" || echo "DELETED OK: $f"
      done
      Expected: all 6 lines say "DELETED OK"

    CHECK 5 — Migration files exist:
      test -f /c/ziko-platform/supabase/migrations/20260526_add_user_profiles_settings.sql && echo "SETTINGS_MIGRATION: OK" || echo "SETTINGS_MIGRATION: MISSING"
      test -f /c/ziko-platform/supabase/migrations/20260526_workout_sessions_friends_rls.sql && echo "RLS_MIGRATION: OK" || echo "RLS_MIGRATION: MISSING"
      Expected: both say "OK"

    CHECK 6 — Route wrappers import new screen names:
      grep 'NutritionPlugin' /c/ziko-platform/apps/mobile/app/(app)/(plugins)/nutrition/dashboard.tsx | wc -l
      grep 'HydrationPlugin' /c/ziko-platform/apps/mobile/app/(app)/(plugins)/hydration/dashboard.tsx | wc -l
      grep 'HabitsPlugin' /c/ziko-platform/apps/mobile/app/(app)/(plugins)/habits/dashboard.tsx | wc -l
      grep 'AIProgramsPlugin' '/c/ziko-platform/apps/mobile/app/(app)/(plugins)/ai-programs/dashboard.tsx' | wc -l
      grep 'CoachIAPlugin' /c/ziko-platform/apps/mobile/app/(app)/(plugins)/persona/customize.tsx | wc -l
      grep 'CommunityPlugin' /c/ziko-platform/apps/mobile/app/(app)/(plugins)/community/dashboard.tsx | wc -l
      Expected: all 6 return 1 or more

    CHECK 7 — SubTabs pill style (not underline):
      grep 'rgba(28,26,23,0.05)' /c/ziko-platform/packages/ui/src/components/SubTabs.tsx | wc -l
      Expected: 1 or more
      grep 'borderBottom\|underline' /c/ziko-platform/packages/ui/src/components/SubTabs.tsx | wc -l
      Expected: 0

    CHECK 8 — AISuggestion label present:
      grep -i 'coach ia\|COACH IA' /c/ziko-platform/packages/ui/src/components/AISuggestion.tsx | wc -l
      Expected: 1 or more

    CHECK 9 — Backend persona injection:
      grep 'ai_persona\|personaInstruction\|personaPrompts' /c/ziko-platform/backend/api/src/context/user.ts | wc -l
      Expected: 3 or more

    CHECK 10 — No persona_settings table usage in CoachIAPlugin:
      grep 'persona_settings' /c/ziko-platform/plugins/persona/src/screens/CoachIAPlugin.tsx | wc -l
      Expected: 0

    CHECK 11 — CoachIAPlugin has only 2 tabs (no Réglages tab):
      grep 'Réglages' /c/ziko-platform/plugins/persona/src/screens/CoachIAPlugin.tsx | wc -l
      Expected: 0

    CHECK 12 — Habits template grid has 8 templates:
      grep 'Courir 30\|Dormir 8' /c/ziko-platform/plugins/habits/src/screens/HabitsPlugin.tsx | wc -l
      Expected: 2 or more (confirms the 2 new templates are present)

    CHECK 13 — No WeekStrip import in HabitsPlugin:
      grep 'WeekStrip' /c/ziko-platform/plugins/habits/src/screens/HabitsPlugin.tsx | wc -l
      Expected: 0

    If ALL checks pass: proceed to Task 2 (human smoke test).
    If ANY check fails: identify and fix the issue before proceeding. Do not mark the plan done until all pass.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit 2>&1 | grep -c 'error TS'</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript: zero error TS lines
    - Fixture grep: zero matches across all 6 plugin files
    - 6 old dashboard files: all deleted (DELETED OK for each)
    - 2 migration files: both exist on disk
    - 6 route wrappers: all import new screen names
    - SubTabs pill style: rgba(28,26,23,0.05) present, borderBottom absent
    - AISuggestion: "Coach IA" label present in component
    - Backend: ai_persona + persona logic in user.ts
    - CoachIAPlugin: zero persona_settings table references
    - CoachIAPlugin: zero 'Réglages' tab references (2-tab structure only)
    - HabitsPlugin: "Courir 30" and "Dormir 8" templates present (8 total)
    - HabitsPlugin: zero WeekStrip imports
  </acceptance_criteria>
  <done>All 13 automated checks pass; zero TypeScript errors; all fixtures replaced; migrations exist; old files deleted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 6 priority plugins redesigned with SubTabs, AISuggestion, real TanStack Query data, and SVG visualizations:
    - Nutrition: 4 SubTabs, SVG calorie ring, macro bars, meal list from nutrition_logs
    - Hydration: 3 SubTabs, SVG bottle-fill, +250/+500/+750ml quick-log buttons, 7-day chart
    - Habits: 3 SubTabs, completion toggle with optimistic update, 30-day inline heatmap, 8-template grid
    - AI Programs: 3 SubTabs, dark hero card (#1C1A17), Réactiver, navigate to AIGenerator
    - Coach IA: 2 SubTabs (Discussion/Personas), 4-persona selector, coaching settings in Personas tab, credit chip in header
    - Community: 3 SubTabs, friend activity feed, challenges progress bar, Groupes empty state

    Backend change: fetchUserContext() and buildSystemPrompt() in routes/ai.ts unconditionally inject selected persona (max/zoe/leo/rio) into AI system prompt.
    Shared components: SubTabs pill style + AISuggestion "COACH IA · SUGGESTION" label.
  </what-built>
  <how-to-verify>
    Start the Expo dev server: cd /c/ziko-platform && npm run mobile

    For each plugin, navigate to it from the app and verify:

    1. NUTRITION (open Nutrition plugin):
       - Header shows "Nutrition" with "+ Ajouter" orange pill button (visible only on Aujourd'hui tab)
       - SubTabs render as pill/capsule style (not underline) — 4 tabs: Aujourd'hui / Ajouter / Historique / Réglages
       - Aujourd'hui tab: SVG calorie ring visible (orange arc), 3 macro bars (P=orange, G=blue, L=amber)
       - Meal cards show at least 4 rows (Petit-déj/Déjeuner/Collation/Dîner)
       - AISuggestion card shows "COACH IA · SUGGESTION" label
       - Ajouter tab: 3 shortcut cards (Scanner/Photo IA/Repas vite) visible in 3-column grid
       - Historique tab: 7-bar chart visible
       - Réglages tab: calorie slider present

    2. HYDRATION (open Hydration plugin):
       - SubTabs pill style — 3 tabs: Aujourd'hui / Historique / Réglages
       - SVG bottle outline visible with fill level (blue)
       - 4 quick-log buttons: +250ml / +500ml / +750ml / Custom in 4-column grid
       - Tap +250ml: button briefly flashes blue, then today's total updates
       - Historique tab: 7-bar chart visible (blue bars)

    3. HABITS (open Habits plugin):
       - SubTabs pill style — 3 tabs: Aujourd'hui / Historique / Nouvelle
       - Aujourd'hui tab: habit rows with completion circles (colored when done)
       - Tap completion circle: toggles immediately (optimistic update)
       - Historique tab: 30-day heatmap grid visible (10 columns × 3 rows), built inline
       - Nouvelle tab: 8 template cards in 2-column grid (including "Courir 30 min" and "Dormir 8h")

    4. AI PROGRAMS (open Programmes IA plugin):
       - SubTabs pill style — 3 tabs: Programme / Générer / Bibliothèque
       - Programme tab: dark card (#1C1A17) if active program exists, OR centered empty state
       - "Prochaine séance" button (if active program) taps → navigates to session screen
       - Générer tab: launch card with "Générer un programme" CTA; tap → navigates to AI generator wizard
       - Bibliothèque tab: list of past programs with "Réactiver" ghost button on inactive ones

    5. COACH IA (open Coach IA plugin — previously Persona):
       - Header shows "Coach IA" with credit chip ⚡N (orange lightning + number)
       - SubTabs pill style — 2 tabs only: Discussion / Personas (NO Réglages tab)
       - Discussion tab: active persona banner + conversations list (or empty state)
       - "COACH IA · SUGGESTION" label visible on AISuggestion card
       - Personas tab: 4 persona cards (Max/Zoé/Léo/Rio) with colored avatar circles
       - Tap a persona card: border highlights in persona color; next AI conversation uses new persona
       - Personas tab (below persona cards): coaching settings rows visible — "Ton du coach", "Longueur des réponses", "Langue du coach"
       - Tap a settings row: showAlert options appear; selection saves and updates displayed value

    6. COMMUNITY (open Communauté plugin):
       - SubTabs pill style — 3 tabs: Fil / Défis / Groupes
       - Fil tab: friend activity cards (or empty state "Ton fil est vide")
       - Défis tab: challenge cards with progress bar (or empty state "Aucun défi actif")
       - Groupes tab: empty state "Groupes bientôt disponibles" with people icon

    Type "approved" if all 6 plugins look correct. List any visual issues with plugin name + tab name + description.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues for each plugin.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced in this verification plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07-01 | N/A | Verification plan | accept | No code changes; read-only checks only |
</threat_model>

<verification>
All verification is the task itself — see Task 1 automated checks (13 checks) and Task 2 human smoke test.
</verification>

<success_criteria>
- TypeScript: zero error TS across entire monorepo
- Zero fixture constants (NUTRITION_TODAY, WATER, HABITS, AI_PROGRAMS, PERSONAS, COACH_MESSAGES, FEED) in all 6 new plugin files
- All 5 old dashboard files confirmed deleted
- Both SQL migrations confirmed on disk
- All 6 route wrappers import the new screen names
- SubTabs pill style confirmed (not underline)
- AISuggestion "COACH IA · SUGGESTION" label confirmed
- Backend persona injection confirmed in user.ts and routes/ai.ts (unconditional)
- CoachIAPlugin: 2 tabs only; coaching settings rows (ai_coaching_style, ai_response_length, ai_language) in Personas tab
- HabitsPlugin: 8 templates including "Courir 30 min" and "Dormir 8h"; no WeekStrip import
- Human: all 6 plugins render correctly with real data on device/simulator
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/37-priority-plugins-redesign/37-07-SUMMARY.md` when done.
</output>
