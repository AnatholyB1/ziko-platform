---
phase: 30-ui-design-web-surfaces
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-UI-SPEC.md
  - .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-REWORK-PLAN.md
autonomous: false
requirements:
  - UIDESIGN-02
  - UIDESIGN-05

must_haves:
  truths:
    - "30-dashboard-UI-SPEC.md exists with a confirmed mockup_source field"
    - "UI-SPEC covers both the CoachSidebar layout shell AND the WelcomeCard dashboard content — both are in scope"
    - "30-dashboard-REWORK-PLAN.md lists S-01 (CoachSidebar locale hardcoding) as a structural deviation with the exact fix"
    - "30-dashboard-REWORK-PLAN.md lists 3 visual-only deviations (WelcomeCard padding, heading size, main area padding)"
  artifacts:
    - path: ".planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-UI-SPEC.md"
      provides: "Design contract for the /[locale]/(coach)/coach/dashboard surface including sidebar shell"
      contains: "mockup_source"
    - path: ".planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-REWORK-PLAN.md"
      provides: "Deviation list for the dashboard surface including CoachSidebar structural fix"
      contains: "S-01"
  key_links:
    - from: "30-dashboard-UI-SPEC.md"
      to: ".planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md"
      via: "@see reference"
      pattern: "@see.*24-UI-SPEC"
    - from: "30-dashboard-REWORK-PLAN.md"
      to: "apps/web/src/components/coach/CoachSidebar.tsx"
      via: "structural deviation S-01 File column"
      pattern: "CoachSidebar.tsx"
---

<objective>
Produce the retroactive design contract for the `/[locale]/(coach)/coach/dashboard` web surface, which includes the CoachLayout sidebar shell plus the dashboard page content.

Purpose: The dashboard surface spans TWO files: the shared `(coach)/coach/layout.tsx` (which renders `CoachSidebar`) and the `dashboard/page.tsx` (which renders `WelcomeCard`). The most critical finding is a structural deviation in `CoachSidebar.tsx` — all navigation hrefs are hardcoded to `/fr/coach/*`, which silently breaks the `/en/` locale route. This rework plan must clearly capture S-01 so it gets fixed when rework is implemented.

Output:
- `30-dashboard-UI-SPEC.md` — canonical design contract for the dashboard surface (sidebar + content)
- `30-dashboard-REWORK-PLAN.md` — 1 structural + 3 visual-only deviations with file paths and fix instructions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-01-SUMMARY.md
@.planning/workstreams/milestone-mobile/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Enumerate Claude Design project files and confirm dashboard surface mapping</name>
  <read_first>
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Claude Design Project — Mockup Mapping section; Dashboard Token Audit table)
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-CONTEXT.md (D-01 — mockup URL and enumeration requirement)
  </read_first>
  <what-built>
    The dashboard surface covers the full page layout including sidebar chrome (CoachSidebar from `layout.tsx`) and the page content (WelcomeCard + KycStatusChip). The mockup must show both sidebar and content area. This checkpoint confirms the exact file/section in the Claude Design project that represents the dashboard view.
  </what-built>
  <how-to-verify>
    1. Open `https://claude.ai/design/p/597c08f6-081b-4578-8c47-abd1ed09f772` in your browser (if not already open).
    2. Navigate to the Dashboard section or file. Note the exact file + section name.
    3. Inspect the sidebar in the mockup:
       - Does the mockup show a 240px left sidebar with: ZIKO logo header (h-14), nav items with active left-border stripe, disabled items with "Bientôt" badge?
       - Note the sidebar nav items visible (Dashboard, Clients, Invitations, Programmes, IA, Paramètres — or different list)
    4. Inspect the main content area in the mockup:
       - Does WelcomeCard use `p-6` (24px) or `p-8` (32px) padding? Research pre-audit found live code uses `p-8` but spec says `p-6` — confirm which the mockup shows.
       - Does WelcomeCard heading use `text-xl` (20px) or `text-2xl` (24px)? Research found live code uses `text-2xl`.
    5. Report back:
       - Confirmed file + section for dashboard surface
       - Sidebar nav items visible in mockup (list them)
       - WelcomeCard padding visible in mockup (`p-6` or `p-8`)
       - WelcomeCard heading size visible in mockup (`text-xl` or `text-2xl`)
  </how-to-verify>
  <resume-signal>
    Type the confirmed mockup source, sidebar nav item list, WelcomeCard padding (p-6 or p-8), and heading size (text-xl or text-2xl). Claude will use these to set correct spec values in Task 2 — mockup wins if it conflicts with the 24-UI-SPEC.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 30-dashboard-UI-SPEC.md from mockup + write 30-dashboard-REWORK-PLAN.md</name>
  <read_first>
    apps/web/src/app/[locale]/(coach)/coach/layout.tsx (full layout shell — CoachSidebar import, auth guard pattern)
    apps/web/src/components/coach/CoachSidebar.tsx (full component — hardcoded /fr/ hrefs are the S-01 deviation)
    apps/web/src/components/coach/NavItem.tsx (nav item rendering — active/inactive/disabled states)
    apps/web/src/app/[locale]/(coach)/coach/dashboard/page.tsx (dashboard page — WelcomeCard import)
    apps/web/src/components/coach/WelcomeCard.tsx (full component — padding and heading size deviations)
    apps/web/src/components/coach/KycStatusChip.tsx (KYC chip — 4 status states; confirmed compliant in research)
    .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md (shared tokens — @see reference source)
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Dashboard Token Audit table; CoachSidebar locale finding)
  </read_first>
  <action>
    Write TWO files using the Write tool. Use Task 1 checkpoint answers to set the correct mockup-derived values for WelcomeCard padding and heading size (mockup wins over 24-UI-SPEC if they conflict).

    FILE 1: `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-UI-SPEC.md`

    Frontmatter:
    ```
    ---
    phase: 30
    surface: dashboard
    route: /[locale]/(coach)/coach/dashboard
    status: approved
    mockup_source: "[INSERT confirmed file + section from Task 1 checkpoint]"
    created: 2026-05-20
    ---
    ```

    Required sections:

    **@see reference:** `@see .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md`

    **Section: Surface-Specific Component Structure**
    The dashboard surface = CoachLayout (wraps CoachSidebar + main) + DashboardPage. Document the full tree:

    CoachLayout (RSC) — auth guard: redirects to `/login?next=...` if no session
    - `div.flex.min-h-screen.bg-background`
      - CoachSidebar (client) — 240px fixed width, sticky top-0
        - `aside.bg-white.border-r.border-border.h-screen.sticky.top-0.w-60.flex.flex-col`
          - Logo header: `div.h-14.px-4.flex.items-center` → `<span>ZIKO</span>` using `text-3xl font-bold text-primary`
          - Nav: `nav.flex.flex-col.gap-1.px-2.py-4`
            - NavItem (active): `h-11`, `text-primary font-bold bg-primary/5 border-l-4 border-primary` + Ionicons icon + label
            - NavItem (inactive): `h-11`, `text-text font-normal hover:bg-background transition-colors`
            - NavItem (disabled): `h-11`, `text-muted font-normal cursor-default` + "Bientôt" badge (`text-xs bg-border text-muted px-1.5 py-0.5 rounded`)
      - `main.flex-1.overflow-auto`
        - `div.mx-auto.max-w-3xl.px-8.py-8` — SPEC value for main padding; live code uses `px-8 py-10` (V-03)
          - DashboardPage (RSC)
            - `div.flex.flex-col.gap-8`
              - WelcomeCard
                - `div.bg-white.rounded-2xl.{padding}.border.border-border.shadow-sm`
                  - `div.flex.items-center.gap-3.mb-3` → `<h1 className="{heading-size} font-bold text-text">` + KycStatusChip
                  - `<p className="text-sm font-normal text-muted mb-6">` — subtitle via `t('Dashboard.subtitle')`
                  - `<p className="text-sm font-normal text-muted">` — invite teaser via `t('Dashboard.inviteTeaser')`
    Note: For padding and heading-size, use the mockup-confirmed values from Task 1 checkpoint. If mockup shows `p-8` → spec is `p-8` and V-01 is not a deviation. If mockup shows `p-6` → spec is `p-6` and V-01 is a deviation.

    **Section: Surface-Specific Layout Rules**
    - Page layout: `flex min-h-screen bg-background`
    - Sidebar: `w-60` (240px fixed), `h-screen sticky top-0`, `bg-white border-r border-border`
    - Logo area: `h-14` (56px) — `px-4 flex items-center`
    - Nav section: `flex flex-col gap-1 px-2 py-4`
    - NavItem height: `h-11` (44px) — all states
    - Active NavItem: left border stripe `border-l-4 border-primary` + `rounded-r-lg` (right side only), `bg-primary/5`
    - Sidebar hrefs: MUST use `/${locale}/coach/*` prefix — built with `useLocale()` hook from next-intl (NOT hardcoded `/fr/`)
    - Main content: `flex-1 overflow-auto` → inner `mx-auto max-w-3xl px-8 py-{N}` — value from mockup
    - WelcomeCard: `bg-white rounded-2xl border border-border shadow-sm` — padding from mockup confirmation
    - KycStatusChip: inline after heading, all 4 states per 24-UI-SPEC.md

    **Section: Surface-Specific Copy Literals**
    - Sidebar nav labels: `t('Sidebar.dashboard')`, `t('Sidebar.clients')`, `t('Sidebar.invitations')`, `t('Sidebar.programmes')`, `t('Sidebar.ai')`, `t('Sidebar.settings')`
    - "Bientôt" badge: `t('Sidebar.comingSoon')`
    - WelcomeCard heading: `t('Dashboard.welcomeHeading', { name: coachName })`
    - WelcomeCard subtitle: `t('Dashboard.subtitle')`
    - WelcomeCard invite teaser: `t('Dashboard.inviteTeaser')`

    **Section: Surface-Specific Interaction States**
    - Auth guard: If no session → redirect to `/[locale]/login?next=/[locale]/coach/dashboard`
    - Active nav highlight: Applied based on `usePathname()` — current route matches href
    - Disabled nav items: `aria-disabled="true"`, no onClick, cursor-default, "Bientôt" badge
    - KycStatusChip states: `pending` (muted), `submitted` (blue), `verified` (primary/accent), `rejected` (red) — full spec in 24-UI-SPEC.md
    - WelcomeCard KYC chip: renders dynamically from user profile `kyc_status` field

    **Section: Deviation Findings**
    Embed the full deviation table from RESEARCH.md Dashboard Token Audit. Adjust V-01 and V-02 based on Task 1 mockup confirmation:
    - S-01: CoachSidebar locale hardcoding — always structural, always in this spec
    - V-01 (WelcomeCard padding): If mockup shows `p-8` → remove from deviations; if mockup shows `p-6` → deviation stands
    - V-02 (WelcomeCard heading text-2xl): If mockup shows `text-2xl` → remove; if `text-xl` → deviation stands
    - V-03 (main area py-10): If mockup shows `py-10` → remove; if `py-8` → deviation stands

    ---

    FILE 2: `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-REWORK-PLAN.md`

    Summary: 1 structural deviation (always S-01 CoachSidebar locale); visual-only count depends on mockup inspection (max 3 if all confirmed as deviations). Always 0 copy deviations.

    **### Structural Deviations**
    | S-01 | CoachSidebar | apps/web/src/components/coach/CoachSidebar.tsx | All nav `href` values hardcoded as `/fr/coach/*` — silently breaks `/en/` and any other locale routing | Add `const locale = useLocale()` from `next-intl/client`; replace all hardcoded `/fr/` prefixes with `/${locale}/`; affected hrefs: `/fr/coach/dashboard`, `/fr/coach/clients`, `/fr/coach/invitations`, `/fr/coach/settings` (and disabled items) |

    **### Visual-Only Deviations**
    Include only the deviations confirmed by Task 1 mockup inspection. At minimum write the full table from research and annotate each row with "(confirmed)" or "(cleared by mockup — not a deviation)":

    | # | Component | File | What's Wrong | Fix |
    |---|-----------|------|--------------|-----|
    | V-01 | WelcomeCard | apps/web/src/components/coach/WelcomeCard.tsx | `p-8` (32px) vs spec `p-6` (24px) [per 24-UI-SPEC] — confirm against mockup | Change outer div to `p-6` if mockup confirms spec; no action if mockup shows `p-8` |
    | V-02 | WelcomeCard heading | apps/web/src/components/coach/WelcomeCard.tsx | `text-2xl` (24px) vs spec `text-xl` (20px) — confirm against mockup | Change h1 to `text-xl` if mockup confirms spec |
    | V-03 | Main content area | apps/web/src/app/[locale]/(coach)/coach/layout.tsx | `py-10` (40px) vs spec `py-8` (32px) | Change inner wrapper to `py-8` |

    **### Copy Deviations** — "None."

    **## Fix Priority**
    1. Structural — S-01 CoachSidebar locale fix (one file, 4 href strings + `useLocale()` import)
    2. Visual-only — V-01, V-02 (WelcomeCard), V-03 (layout padding)

    **## Implementation Note for S-01**
    Add `import { useLocale } from 'next-intl'` at the top of `CoachSidebar.tsx`. Add `const locale = useLocale()` inside the component. Replace every `/fr/` prefix in href values with `` `/${locale}/` ``. This component is already a `'use client'` component so the hook is compatible.
  </action>
  <verify>
    <automated>ls C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-UI-SPEC.md C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-REWORK-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - `30-dashboard-UI-SPEC.md` exists in the phase directory
    - `30-dashboard-UI-SPEC.md` frontmatter contains `mockup_source:` with a non-empty value
    - `30-dashboard-UI-SPEC.md` contains `@see .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md`
    - `30-dashboard-UI-SPEC.md` documents BOTH the sidebar chrome (CoachSidebar) AND the dashboard page content (WelcomeCard) in the component structure section
    - `30-dashboard-UI-SPEC.md` Layout Rules section explicitly states sidebar hrefs must use `useLocale()` (not hardcoded `/fr/`)
    - `30-dashboard-REWORK-PLAN.md` exists
    - `30-dashboard-REWORK-PLAN.md` Structural Deviations table contains S-01 referencing `CoachSidebar.tsx` with the `useLocale()` fix
    - `30-dashboard-REWORK-PLAN.md` Implementation Note for S-01 includes the `import { useLocale }` instruction
    - `30-dashboard-REWORK-PLAN.md` Visual-Only table includes at minimum V-03 (main padding); V-01 and V-02 cleared or confirmed based on mockup
    - `30-dashboard-REWORK-PLAN.md` Copy Deviations section states "None"
  </acceptance_criteria>
  <done>
    Both files exist. UI-SPEC covers sidebar + content area with mockup-confirmed values. Rework plan has S-01 structural deviation (CoachSidebar locale fix) with implementation instructions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Routing | CoachSidebar hardcoded /fr/ hrefs are a routing trust boundary issue — wrong locale silently 404s or redirects incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Tampering | CoachSidebar locale routing | mitigate (in rework plan) | S-01 fix: `useLocale()` makes hrefs dynamic — routing correct for all locales |
| T-30-01 | Information Disclosure | Claude Design URL (authenticated) | accept | Developer-gated asset |
| T-30-SC | Tampering | No package installs | accept | Documentation-only phase |
</threat_model>

<verification>
After both tasks complete:
1. `ls .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-dashboard-*.md` returns 2 files
2. UI-SPEC mockup_source field is populated
3. Rework plan contains S-01 with `CoachSidebar.tsx` file reference
4. Rework plan S-01 fix column mentions `useLocale()`
</verification>

<success_criteria>
- `30-dashboard-UI-SPEC.md` covers the full page layout: sidebar chrome (NavItem states, logo, disabled items) + dashboard content (WelcomeCard, KycStatusChip). Uses mockup-confirmed padding and heading values.
- `30-dashboard-REWORK-PLAN.md` captures S-01 as a structural deviation with the exact `useLocale()` fix required in `CoachSidebar.tsx`, plus visual-only deviations confirmed against mockup.
- UIDESIGN-02 satisfied: design contract (Claude Design URL + UI-SPEC.md) exists for the dashboard surface.
- UIDESIGN-05 (dashboard portion) satisfied: rework plan exists with S-01 structural fix.
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-03-SUMMARY.md` when done.
</output>
