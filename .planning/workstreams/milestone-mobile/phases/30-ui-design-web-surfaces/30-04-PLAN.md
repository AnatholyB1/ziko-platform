---
phase: 30-ui-design-web-surfaces
plan: 04
type: execute
wave: 3
depends_on:
  - 30-02
  - 30-03
files_modified:
  - .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-UI-SPEC.md
  - .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-REWORK-PLAN.md
autonomous: false
requirements:
  - UIDESIGN-03
  - UIDESIGN-05

must_haves:
  truths:
    - "30-settings-UI-SPEC.md exists with a confirmed mockup_source field"
    - "UI-SPEC documents both the Profile section and the KYC section with their subsections and component nesting"
    - "UI-SPEC defines an explicit success state color for save confirmations (resolves the open question from research)"
    - "30-settings-REWORK-PLAN.md lists 0 structural + 6 visual-only + 0 copy deviations with exact file paths and class-level fix instructions"
    - "V-06 (success message color using text-primary) has an explicit recommended alternative color in the rework plan"
  artifacts:
    - path: ".planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-UI-SPEC.md"
      provides: "Design contract for the /[locale]/(coach)/coach/settings surface"
      contains: "mockup_source"
    - path: ".planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-REWORK-PLAN.md"
      provides: "Deviation list for the settings surface — 6 visual-only deviations"
      contains: "V-06"
  key_links:
    - from: "30-settings-UI-SPEC.md"
      to: ".planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md"
      via: "@see reference"
      pattern: "@see.*24-UI-SPEC"
    - from: "30-settings-REWORK-PLAN.md"
      to: "apps/web/src/app/[locale]/(coach)/coach/settings/SettingsClient.tsx"
      via: "deviation table File column"
      pattern: "SettingsClient.tsx"
---

<objective>
Produce the retroactive design contract for the `/[locale]/(coach)/coach/settings` web surface.

Purpose: The settings surface has 6 visual-only deviations and no structural issues — it is the most visually compliant surface in Phase 30. The key work here is (1) writing the UI-SPEC that covers both Profile and KYC sub-sections and their component nesting, (2) resolving the open question about success message color (text-primary used for save success is an accent usage outside the reserved list), and (3) producing the rework plan for the 6 visual deviations. This plan runs in Wave 3 so that shared patterns established by Plans 02 and 03 are available for consistency.

Output:
- `30-settings-UI-SPEC.md` — canonical design contract for the settings surface
- `30-settings-REWORK-PLAN.md` — 6 visual-only deviations with file paths and fix instructions, plus resolved success color definition
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-02-SUMMARY.md
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-03-SUMMARY.md
@.planning/workstreams/milestone-mobile/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Enumerate Claude Design project files, confirm settings surface mapping, resolve success color open question</name>
  <read_first>
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Open Questions section — question 4 is about settings success color; Settings Token Audit table)
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-CONTEXT.md (D-01 enumeration requirement; Specifics section — accent usage rule)
  </read_first>
  <what-built>
    The research session left one open question for the settings surface: the live code uses `text-primary` (orange) for save success messages, but accent `#FF5C1A` is only reserved for 5 specific uses (CTA bg, active sidebar stripe, wizard progress fill, verified KYC chip, logo wordmark). This checkpoint confirms what the mockup shows for the settings success state, allowing the UI-SPEC to specify the correct color.
  </what-built>
  <how-to-verify>
    1. Open `https://claude.ai/design/p/597c08f6-081b-4578-8c47-abd1ed09f772` in your browser (likely already open from Plans 01–03).
    2. Navigate to the Settings section or file. Note the exact file + section name.
    3. Check the settings mockup for the save success state:
       - Does the mockup show a success message after saving the profile form? What color is used?
       - Options: orange/primary (`text-primary`), green, neutral dark (`text-text`), or no success message shown
    4. Check section card padding in the mockup:
       - Do the Profile and KYC section cards appear to use `p-6` (24px) or `p-8` (32px)?
    5. Check page title in mockup:
       - Does the page title use `text-xl` (20px) or `text-2xl` (24px)?
    6. Report back:
       - Confirmed file + section for settings surface
       - Success message color (orange / green / neutral / not shown)
       - Section card padding (p-6 or p-8)
       - Page title size (text-xl or text-2xl)
  </how-to-verify>
  <resume-signal>
    Type the confirmed mockup source, success color (orange/green/neutral/not-shown), card padding (p-6 or p-8), and page title size (text-xl or text-2xl). Claude will use these to write correct spec values in Task 2.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 30-settings-UI-SPEC.md from mockup + write 30-settings-REWORK-PLAN.md</name>
  <read_first>
    apps/web/src/app/[locale]/(coach)/coach/settings/SettingsClient.tsx (full component — Profile section, KYC section, success/error states, button placement)
    apps/web/src/app/[locale]/(coach)/coach/settings/page.tsx (RSC route — auth guard, profile fetch, title rendering)
    apps/web/src/components/coach/ProfileForm.tsx (form fields: photo, display_name, bio, specialties, website — uses Onboarding namespace)
    apps/web/src/components/coach/KycStatusChip.tsx (chip states — confirmed compliant with spec in research)
    .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md (shared tokens — @see reference source)
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Settings Token Audit table; open question 4 on success color)
  </read_first>
  <action>
    Write TWO files using the Write tool. Incorporate the Task 1 checkpoint answers to: (a) set mockup_source, (b) determine whether card padding and page title are spec deviations or correct, (c) define the success state color as specified by mockup (or `text-green-600` if not shown, per research recommendation).

    FILE 1: `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-UI-SPEC.md`

    Frontmatter:
    ```
    ---
    phase: 30
    surface: settings
    route: /[locale]/(coach)/coach/settings
    status: approved
    mockup_source: "[INSERT confirmed file + section from Task 1 checkpoint]"
    created: 2026-05-20
    ---
    ```

    Required sections:

    **@see reference:** `@see .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md`

    **Section: Surface-Specific Component Structure**
    Document the full component tree as the mockup shows it. From research, the as-built structure is:

    SettingsPage (RSC) — auth guard: redirect to `/login?next=...` if no session
    - Fetches `user_profile` row for `userId`
    - `div.flex.flex-col.gap-8`
      - `<h1>` page title — `{size} font-bold text-text` via `t('Settings.title')` — size from mockup confirmation
      - SettingsClient (client) — `[userId, initialProfile]` props
        - Profile section: `section.bg-white.rounded-2xl.{padding}.border.border-border.shadow-sm`
          - `<h2>` section heading — `text-base font-bold text-text mb-{N}` — margin from mockup
          - `<form>` with flex-col `gap-{N}` — gap from mockup
            - ProfileForm — fields: photo upload, display_name input, bio textarea (`min-h-[120px]`), specialties (SpecialtyTagInput), website input
            - Status alert (conditional) — `text-sm font-normal {success-color}` on save success; `text-red-600` on error
            - `<div className="flex justify-end pt-2">` → Save button (`rounded-lg bg-primary text-white font-bold`)
        - KYC section: `section.bg-white.rounded-2xl.{padding}.border.border-border.shadow-sm`
          - `<div className="flex items-center gap-3 mb-{N}">` → `<h2>` + KycStatusChip
          - `<form>`
            - Hidden input (kyc_docs JSON serialized)
            - KycDocList — document upload rows
            - Status alert (conditional)
            - `<div className="flex justify-end pt-2">` → Save button

    Note: Use mockup-confirmed values for `{padding}`, `{size}`, `{N}` gaps from Task 1.

    **Section: Surface-Specific Layout Rules**
    - Page title: `{text-xl or text-2xl} font-bold text-text` — from mockup
    - Section card: `bg-white rounded-2xl border border-border shadow-sm` + padding from mockup (`p-6` spec value per 24-UI-SPEC, or `p-8` if mockup shows that)
    - Section heading margin: `mb-4` per 24-UI-SPEC — live code uses `mb-5` (V-02, V-07)
    - Profile form gap: `gap-4` (16px) — live code uses `gap-5` (V-03)
    - Save button: `w-full-or-auto rounded-lg bg-primary text-white font-bold px-4 py-2` — all primary CTA buttons use `rounded-lg`
    - Save button placement: `flex justify-end pt-2` in both sections
    - Status alert margin: `mt-2` or `-mt-2` (live code uses `-mt-2` for error; note explicitly)

    **Section: Surface-Specific Interaction States**
    - Save profile: submitting → button disabled/loading; success → success alert visible (color: from mockup confirmation — see success color rule below); error → red alert
    - Save KYC: same pattern
    - KYC upload: KycDocList handles upload state per document (pending, uploaded, error)
    - KycStatusChip: renders from `initialProfile.kyc_status`; updates after KYC form save

    **Success State Color Rule (explicit for this surface):**
    Per accent usage rule: `text-primary` (`#FF5C1A`) is reserved for CTA bg, active sidebar, progress fill, verified KYC chip, and logo — NOT for success messages. Based on Task 1 mockup inspection:
    - If mockup shows orange success → accept as intentional, document `text-primary` for success
    - If mockup shows green → spec is `text-green-600`
    - If mockup shows neutral → spec is `text-text`
    - If success state not shown in mockup → spec is `text-green-600` (mirrors `text-red-600` error pattern; keeps accent rule intact)
    Write the explicit success state color in this section.

    **Section: Surface-Specific Copy Literals**
    - Page title: `t('Settings.title')`
    - Profile section heading: `t('Settings.profileSection')` or `t('Settings.profile')`
    - KYC section heading: `t('Settings.kycSection')` or `t('Settings.kyc')`
    - Save button: `t('Settings.save')` or `t('Onboarding.save')` (ProfileForm reuses Onboarding namespace)
    - ProfileForm fields: `useTranslations('Onboarding')` namespace (confirmed in research — correct reuse)
    - SettingsClient: `useTranslations('Settings')` namespace

    **Section: Deviation Findings**
    Embed the full deviation table from RESEARCH.md Settings Token Audit. Adjust V-01 (page title size) and V-02 (card padding) based on Task 1 mockup inspection. V-06 (success color) is updated with the confirmed spec color:
    - V-01 (page title text-2xl vs text-xl): confirm or clear based on mockup
    - V-02/V-04 (card padding p-8 vs p-6): confirm or clear based on mockup
    - V-03 (section heading mb-5 vs mb-4): visual-only — likely stands unless mockup shows mb-5
    - V-05 (form gap gap-5 vs gap-4): visual-only
    - V-06 (success color text-primary): set final spec per mockup confirmation

    ---

    FILE 2: `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-REWORK-PLAN.md`

    Summary: 0 structural deviations. Visual-only count from Task 1 confirmation (max 6 if all confirmed; some may clear). 0 copy deviations.

    **### Structural Deviations** — "None. Settings surface is structurally compliant with the design spec."

    **### Visual-Only Deviations**
    Full table with the research pre-audit findings, annotated by mockup confirmation:

    | # | Component | File | What's Wrong | Fix |
    |---|-----------|------|--------------|-----|
    | V-01 | Page title | apps/web/src/app/[locale]/(coach)/coach/settings/page.tsx | `text-2xl` (24px) vs spec `text-xl` (20px) — confirm against mockup | Change h1 to `text-xl` if confirmed |
    | V-02 | Profile section card | apps/web/src/app/[locale]/(coach)/coach/settings/SettingsClient.tsx | `p-8` (32px) vs spec `p-6` (24px) — confirm against mockup | Change section padding to `p-6` if confirmed |
    | V-03 | Profile section heading | apps/web/src/app/[locale]/(coach)/coach/settings/SettingsClient.tsx | `mb-5` vs spec `mb-4` | Change to `mb-4` |
    | V-04 | KYC section card | apps/web/src/app/[locale]/(coach)/coach/settings/SettingsClient.tsx | `p-8` (32px) vs spec `p-6` (24px) — same pattern as V-02 | Change section padding to `p-6` if confirmed |
    | V-05 | ProfileForm gap | apps/web/src/components/coach/ProfileForm.tsx | `gap-5` (20px) vs spec `gap-4` (16px) | Change form flex gap to `gap-4` |
    | V-06 | Save success alert | apps/web/src/app/[locale]/(coach)/coach/settings/SettingsClient.tsx | `text-primary` used for success message — accent `#FF5C1A` is NOT reserved for success state | Change to `{confirmed spec color from Task 1}` — recommendation: `text-green-600` if mockup doesn't show a specific color |
    | V-07 | Save button radius (both) | apps/web/src/app/[locale]/(coach)/coach/settings/SettingsClient.tsx | `rounded-xl` vs spec `rounded-lg` on both save buttons | Change both save buttons to `rounded-lg` |
    | V-08 | KYC heading margin | apps/web/src/app/[locale]/(coach)/coach/settings/SettingsClient.tsx | `mb-5` vs spec `mb-4` | Change to `mb-4` |

    Note: Research pre-audit counted 6 deviations; the full audit above may yield up to 8 depending on distinction between card items. Annotate which are cleared by mockup confirmation.

    **### Copy Deviations** — "None."

    **## Fix Priority**
    1. Structural deviations — None
    2. Copy deviations — None
    3. Visual-only deviations:
       a. V-06 (success color) — accent rule violation; fix first
       b. V-07 (button radius ×2) — consistent with all-surfaces button fix
       c. V-02, V-04 (card padding) — if confirmed by mockup
       d. V-03, V-08 (heading margins)
       e. V-05 (form gap)
       f. V-01 (page title size) — if confirmed by mockup

    **## Implementation Note for V-06**
    `SettingsClient.tsx` renders `{status === 'success' && <p className="text-sm text-primary">...</p>}` in both forms. Change `text-primary` to `text-green-600` (or the spec color confirmed from mockup). This restores the accent usage rule: orange is only for CTA, sidebar, progress, verified chip, and logo.
  </action>
  <verify>
    <automated>ls C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-UI-SPEC.md C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-REWORK-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - `30-settings-UI-SPEC.md` exists in the phase directory
    - `30-settings-UI-SPEC.md` frontmatter contains `mockup_source:` with a non-empty value
    - `30-settings-UI-SPEC.md` contains `@see .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md`
    - `30-settings-UI-SPEC.md` documents both the Profile section and the KYC section in the component tree
    - `30-settings-UI-SPEC.md` Success State Color Rule section is present and states an explicit color (not "TBD" or left blank)
    - `30-settings-REWORK-PLAN.md` exists
    - `30-settings-REWORK-PLAN.md` Structural Deviations section states "None"
    - `30-settings-REWORK-PLAN.md` Visual-Only table contains V-06 (success color) with `text-primary` identified as the current violation and a specific replacement color
    - `30-settings-REWORK-PLAN.md` Visual-Only table contains V-07 (button radius — `rounded-xl` → `rounded-lg`)
    - `30-settings-REWORK-PLAN.md` Implementation Note for V-06 is present and names the `SettingsClient.tsx` line pattern
    - `30-settings-REWORK-PLAN.md` Copy Deviations section states "None"
  </acceptance_criteria>
  <done>
    Both files exist. UI-SPEC covers Profile and KYC sections with mockup-confirmed values and an explicit success state color. Rework plan has V-06 through V-08 plus any confirmed visual deviations, all referencing SettingsClient.tsx.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → Server | Settings surface has profile + KYC form submissions — not in scope for Phase 30 (documentation only), but rework plan notes must not introduce new server actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Information Disclosure | KYC document uploads | accept | Phase 30 is documentation-only; KYC upload handling is not modified by rework plans |
| T-30-01 | Information Disclosure | Claude Design URL (authenticated) | accept | Developer-gated design asset |
| T-30-SC | Tampering | No package installs | accept | Documentation-only phase |
</threat_model>

<verification>
After both tasks complete:
1. `ls .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-settings-*.md` returns 2 files
2. UI-SPEC mockup_source field is populated
3. UI-SPEC Success State Color Rule section names a specific color class (not empty)
4. Rework plan V-06 names `text-primary` as the current violation and a replacement class
</verification>

<success_criteria>
- `30-settings-UI-SPEC.md` is a complete design contract for the settings surface: both section structures (Profile + KYC), form component nesting, save button placement, interaction states for both forms, explicit success state color derived from mockup.
- `30-settings-REWORK-PLAN.md` contains the accent-rule violation (V-06 success color), button radius deviations (V-07), heading margin deviations (V-03, V-08), and form gap (V-05) — all with exact file and class-level fix instructions. No structural or copy deviations.
- UIDESIGN-03 satisfied: design contract (Claude Design URL + UI-SPEC.md) exists for the settings surface.
- UIDESIGN-05 (settings portion) satisfied: rework plan exists.
- Combined with Plans 01–03: all 4 rework plans exist — UIDESIGN-05 fully satisfied across the phase.
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-04-SUMMARY.md` when done.
</output>
