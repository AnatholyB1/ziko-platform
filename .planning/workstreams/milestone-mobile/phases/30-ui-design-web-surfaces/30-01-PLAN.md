---
phase: 30-ui-design-web-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-UI-SPEC.md
  - .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-REWORK-PLAN.md
autonomous: false
requirements:
  - UIDESIGN-04
  - UIDESIGN-05

must_haves:
  truths:
    - "30-login-UI-SPEC.md exists with a confirmed mockup_source field (Claude Design URL + section name)"
    - "UI-SPEC describes the Login surface component tree, layout rules, copy literals, and interaction states as shown in the mockup"
    - "UI-SPEC references 24-UI-SPEC.md for shared tokens and does not duplicate the color palette or spacing scale"
    - "30-login-REWORK-PLAN.md exists listing 6 pre-audited deviations (0 structural, 6 visual-only, 0 copy) with file paths and fix instructions"
  artifacts:
    - path: ".planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-UI-SPEC.md"
      provides: "Design contract for the /[locale]/login surface"
      contains: "mockup_source"
    - path: ".planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-REWORK-PLAN.md"
      provides: "Deviation list and fix instructions for the login surface"
      contains: "V-01"
  key_links:
    - from: "30-login-UI-SPEC.md"
      to: ".planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md"
      via: "@see reference"
      pattern: "@see.*24-UI-SPEC"
    - from: "30-login-REWORK-PLAN.md"
      to: "apps/web/src/app/[locale]/login/LoginForm.tsx"
      via: "deviation table File column"
      pattern: "LoginForm.tsx"
---

<objective>
Produce the retroactive design contract for the `/[locale]/login` web surface.

Purpose: Phase 24 shipped the login page without a `/gsd-ui-phase` session. This plan creates the missing design contract: a UI-SPEC.md derived from the Claude Design mockup, and a rework plan capturing the 6 visual deviations found during research pre-audit. Downstream plans (30-02, 30-03, 30-04) share the same design system; this plan establishes the mockup-to-file mapping pattern they follow.

Output:
- `30-login-UI-SPEC.md` — canonical design contract for `/login`
- `30-login-REWORK-PLAN.md` — 6 visual-only deviations with targeted fix instructions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md
@.planning/workstreams/milestone-mobile/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Enumerate Claude Design project files and confirm login surface mapping</name>
  <read_first>
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-CONTEXT.md (D-01 section — mockup URL and enumeration requirement)
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Claude Design Project — Mockup Mapping section — provisional table + enumeration gap)
  </read_first>
  <what-built>
    Per D-01, the canonical mockup lives at the Claude Design project URL. The research session could not enumerate the file list because the URL requires browser authentication. This checkpoint completes the mandatory enumeration step before any UI-SPEC is written.
  </what-built>
  <how-to-verify>
    1. Open `https://claude.ai/design/p/597c08f6-081b-4578-8c47-abd1ed09f772` in your browser.
    2. List ALL files visible in the project file panel (left sidebar or file picker). Note each filename exactly.
    3. Identify which file/section maps to the Login surface (`/[locale]/login`). Expected: a section within `Ziko+Onboarding.html` labelled "Login" — confirm or correct this.
    4. If additional files exist beyond `Ziko+Onboarding.html` (e.g. `Ziko+Login.html`, `Ziko+Dashboard.html`), note them — they may be more specific references for later plans.
    5. Report back:
       - Full file list in the project
       - Confirmed file + section name for the Login surface
       - Any per-surface dedicated files discovered
  </how-to-verify>
  <resume-signal>
    Type the file list and confirmed login mockup source (e.g. "Files: Ziko+Onboarding.html — login section maps to the first screen"). Claude will embed this into the UI-SPEC mockup_source field in Task 2.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 30-login-UI-SPEC.md from mockup + write 30-login-REWORK-PLAN.md</name>
  <read_first>
    apps/web/src/app/[locale]/login/LoginForm.tsx (full component — current implementation)
    apps/web/src/app/[locale]/login/page.tsx (route wrapper — current implementation)
    .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md (shared design token contract — do NOT duplicate, cross-reference with @see)
    apps/web/src/app/globals.css (authoritative CSS tokens — 5 vars only)
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Login Token Audit table + deviation summary — pre-audited findings)
  </read_first>
  <action>
    Write TWO files using the Write tool. Do not use bash heredoc.

    FILE 1: `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-UI-SPEC.md`

    Structure (per D-04, D-05 and RESEARCH.md Per-Surface UI-SPEC Template):

    ```
    ---
    phase: 30
    surface: login
    route: /[locale]/login
    status: approved
    mockup_source: "[INSERT confirmed file + section from Task 1 checkpoint]"
    created: 2026-05-20
    ---

    # Phase 30 — UI-SPEC: Login Page

    @see .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md
    (All shared design tokens — spacing scale, typography, color palette, shadow values,
    status chip colors, form component patterns — are defined there. Do not duplicate here.)
    ```

    Then write the following REQUIRED sections (surface-specific only):

    **Section: Surface-Specific Component Structure**
    Describe the component tree as it SHOULD look per the mockup (not as-built). Base on mockup confirmed in Task 1, referencing the RESEARCH.md component tree for structure. The tree is:
    - LoginPage (RSC) → Suspense (fallback: loading indicator) → LoginForm (client)
      - motion.div [fadeUp animation, per lib/motion.ts `fadeUp` variant]
        - Logo wordmark (ZIKO) — `text-3xl font-bold text-primary` (28px, per accent usage rule: logo wordmark is a reserved accent use)
        - `<h1>` page title — `text-xl font-bold text-text` (via `t('title')`)
        - `<p>` subtitle — `text-sm font-normal text-muted` (via `t('subtitle')`)
        - `<form>` with `gap-4` column flex
          - Hidden input (next search param)
          - Email field group: `<label>` + `<input type="email">` with `focus:border-text` ring
          - Password field group: `<label>` + `<input type="password">` with `focus:border-text` ring
          - Error alert div — conditional, `text-sm font-normal text-red-600` (shown only on error state)
          - Submit CTA button — `motion.button` with `ctaHover` + `ctaTap` variants, `rounded-lg bg-primary text-white font-bold`

    **Section: Surface-Specific Layout Rules**
    - Page outer: `min-h-screen bg-background flex items-center justify-center px-4 py-12`
    - Login card: `max-w-sm w-full bg-white rounded-2xl p-8 shadow-sm border border-border` — SPEC is `p-8` (32px); live code uses `px-10 py-10` (deviation V-01)
    - Logo margin below wordmark: `mb-8` — live code uses `mb-10` (deviation V-03)
    - Form fields: `gap-4` (16px column gap) — live code uses `gap-5` (deviation V-05)
    - CTA button: `w-full rounded-lg` — live code uses `rounded-xl` (deviation V-06)
    - Logo text: `text-3xl font-bold text-primary` (no tracking modifier) — live code uses `text-4xl tracking-widest` (deviations V-02, V-04)

    **Section: Surface-Specific Copy Literals**
    - All user-visible strings sourced from next-intl namespace. No hardcoded strings on login surface.
    - i18n namespace: `useTranslations()` (root namespace implied by LoginForm usage)
    - Keys used: `t('title')`, `t('subtitle')`, `t('email')`, `t('password')`, `t('submit')`
    - Suspense fallback: localized loading string (not hardcoded)

    **Section: Surface-Specific Interaction States**
    - Default state: form blank, no error, submit button enabled
    - Submitting state: button shows loading indicator or is disabled during async auth call
    - Error state: error alert div visible below password field, input border transitions to `border-red-400`
    - Success state: redirect to `next` param destination (no visible state — navigation)
    - Animation: `fadeUp` on card mount (from `apps/web/src/lib/motion.ts`), `ctaHover` scale-up + `ctaTap` scale-down on CTA button

    **Section: Deviation Findings**
    Embed the full deviation table from RESEARCH.md Login Token Audit. Flag each row with its severity. The 6 confirmed deviations are:
    - V-01: Card padding `px-10 py-10` vs spec `p-8`
    - V-02: Logo size `text-4xl` vs spec `text-3xl`
    - V-03: Logo margin `mb-10` vs spec `mb-8`
    - V-04: Logo tracking `tracking-widest` not in spec
    - V-05: Form gap `gap-5` vs spec `gap-4`
    - V-06: Button radius `rounded-xl` vs spec `rounded-lg`

    ---

    FILE 2: `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-REWORK-PLAN.md`

    Structure (per RESEARCH.md Per-Surface Rework Plan Template):

    ```
    # Rework Plan: Login Page

    **Phase:** 30
    **Surface:** login
    **Status:** Pending implementation (future phase)

    ## Summary
    0 structural, 6 visual-only, 0 copy deviations found.
    All deviations are in LoginForm.tsx. No structural changes required.
    Most common pattern: padding escalation and button radius drift.
    ```

    Then write:

    **### Structural Deviations** — "None."

    **### Visual-Only Deviations**
    Full table with 6 rows (V-01 through V-06):

    | # | Component | File | What's Wrong | Fix |
    |---|-----------|------|--------------|-----|
    | V-01 | LoginForm card | apps/web/src/app/[locale]/login/LoginForm.tsx | `px-10 py-10` (40px) vs spec `p-8` (32px) | Change outer card div to `p-8` |
    | V-02 | Logo wordmark | apps/web/src/app/[locale]/login/LoginForm.tsx | `text-4xl` (36px) vs spec `text-3xl` (28px) | Change to `text-3xl` |
    | V-03 | Logo margin | apps/web/src/app/[locale]/login/LoginForm.tsx | `mb-10` (40px) vs spec `mb-8` (32px) | Change to `mb-8` |
    | V-04 | Logo tracking | apps/web/src/app/[locale]/login/LoginForm.tsx | `tracking-widest` not in spec | Remove `tracking-widest` from logo element |
    | V-05 | Form gap | apps/web/src/app/[locale]/login/LoginForm.tsx | `gap-5` (20px) vs spec `gap-4` (16px) | Change form flex container to `gap-4` |
    | V-06 | Submit button | apps/web/src/app/[locale]/login/LoginForm.tsx | `rounded-xl` vs spec `rounded-lg` | Change to `rounded-lg` |

    **### Copy Deviations** — "None."

    **## Fix Priority**
    1. Structural deviations — N/A (none found)
    2. Copy deviations — N/A (none found)
    3. Visual-only deviations — V-01 through V-06 — single file, all in `LoginForm.tsx`

    **## Implementation Notes**
    All 6 fixes are single-file, single-property changes in `LoginForm.tsx`. Recommended fix order: padding (V-01), logo (V-02, V-03, V-04), form (V-05), button (V-06). Total estimated diff: ~6 line changes.
  </action>
  <verify>
    <automated>ls C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-UI-SPEC.md C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-REWORK-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - `30-login-UI-SPEC.md` exists in `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/`
    - `30-login-UI-SPEC.md` frontmatter contains `mockup_source:` field with a non-empty value (from Task 1 checkpoint)
    - `30-login-UI-SPEC.md` contains `@see .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md` — no inline color palette or spacing scale
    - `30-login-UI-SPEC.md` contains all 5 required sections: Surface-Specific Component Structure, Layout Rules, Copy Literals, Interaction States, Deviation Findings
    - `30-login-UI-SPEC.md` Deviation Findings section lists exactly 6 rows (V-01 through V-06)
    - `30-login-REWORK-PLAN.md` exists in the same directory
    - `30-login-REWORK-PLAN.md` contains a Visual-Only Deviations table with exactly 6 rows
    - `30-login-REWORK-PLAN.md` Structural Deviations section states "None"
    - `30-login-REWORK-PLAN.md` each row references `apps/web/src/app/[locale]/login/LoginForm.tsx` in the File column
  </acceptance_criteria>
  <done>
    Both files exist. UI-SPEC has confirmed mockup_source, @see reference, and 5 sections. Rework plan has 6 visual-only deviations with exact file paths and fix instructions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner → Executor | Plan describes the mockup-first rule; executor must open the authenticated URL before writing specs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | Claude Design URL (authenticated) | accept | URL is project-internal; no PII exposed; mockup access is developer-gated |
| T-30-SC | Tampering | No package installs in Phase 30 | accept | Documentation-only phase; no npm/pip/cargo installs |
</threat_model>

<verification>
After both tasks complete:
1. `ls .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-login-*.md` returns 2 files
2. UI-SPEC has `mockup_source:` in frontmatter (set from checkpoint response)
3. UI-SPEC does not contain a standalone "Color Palette" or "Typography Scale" section (these live in 24-UI-SPEC.md)
4. Rework plan V-01..V-06 all reference `LoginForm.tsx` — no other file
</verification>

<success_criteria>
- `30-login-UI-SPEC.md` is a complete design contract for the `/login` surface: mockup-derived component tree, layout measurements, copy i18n references, interaction states, deviation summary. References 24-UI-SPEC.md for shared tokens.
- `30-login-REWORK-PLAN.md` lists all 6 visual-only deviations found during research pre-audit, with exact component names, file paths, what is wrong, and what to change.
- UIDESIGN-04 satisfied: design contract (Claude Design URL + UI-SPEC.md) exists for the login surface.
- UIDESIGN-05 (login portion) satisfied: rework plan exists for the login surface.
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-01-SUMMARY.md` when done.
</output>
