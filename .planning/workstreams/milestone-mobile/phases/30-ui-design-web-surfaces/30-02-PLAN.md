---
phase: 30-ui-design-web-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-UI-SPEC.md
  - .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-REWORK-PLAN.md
autonomous: false
requirements:
  - UIDESIGN-01
  - UIDESIGN-05

must_haves:
  truths:
    - "30-onboarding-UI-SPEC.md exists with a confirmed mockup_source field (Claude Design URL + section name)"
    - "UI-SPEC describes the 3-step wizard component tree per the mockup including WizardProgress bar spec (bare 4px track, no labels — or updated if mockup shows labels)"
    - "UI-SPEC documents all 3 structural deviations and 4 visual deviations found in research pre-audit"
    - "30-onboarding-REWORK-PLAN.md lists 3 structural + 4 visual-only + 1 copy deviation with file paths and fix instructions"
    - "Structural deviation S-01 (WizardProgress label row) is conditionally confirmed or cleared based on mockup inspection"
  artifacts:
    - path: ".planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-UI-SPEC.md"
      provides: "Design contract for the /[locale]/coach/onboarding surface"
      contains: "mockup_source"
    - path: ".planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-REWORK-PLAN.md"
      provides: "Deviation list and fix instructions for the onboarding surface"
      contains: "S-01"
  key_links:
    - from: "30-onboarding-UI-SPEC.md"
      to: ".planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md"
      via: "@see reference"
      pattern: "@see.*24-UI-SPEC"
    - from: "30-onboarding-REWORK-PLAN.md"
      to: "apps/web/src/components/coach/WizardProgress.tsx"
      via: "structural deviation S-01 File column"
      pattern: "WizardProgress.tsx"
---

<objective>
Produce the retroactive design contract for the `/[locale]/coach/onboarding` 3-step wizard surface.

Purpose: This surface has the highest complexity in Phase 30 — 3 structural deviations plus 4 visual-only and 1 copy deviation, totalling 8 findings. Critically, two open questions from research must be resolved via mockup inspection: whether the WizardProgress label row is in the mockup (which changes S-01's status), and whether the ZIKO logo above the wizard is in the mockup (which changes S-03's status). The UI-SPEC must be written from the mockup as authoritative source, not from the live code.

Output:
- `30-onboarding-UI-SPEC.md` — canonical design contract for the onboarding wizard
- `30-onboarding-REWORK-PLAN.md` — deviations with severity, file paths, and fix instructions (structural deviations updated per mockup inspection)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md
@.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-01-SUMMARY.md
@.planning/workstreams/milestone-mobile/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Enumerate Claude Design project files and confirm onboarding surface mapping + resolve open questions</name>
  <read_first>
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Open Questions section — questions 2 and 3 concern onboarding)
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Onboarding Token Audit table — 3 structural deviations flagged as conditional)
  </read_first>
  <what-built>
    The research session identified 3 structural deviations for the onboarding wizard, but two are conditional on mockup confirmation that could not be done without browser access. This checkpoint resolves both open questions before the UI-SPEC is written — per the mockup-first rule, if the mockup shows an element, it stays in the spec; if not, it is confirmed as a deviation.
  </what-built>
  <how-to-verify>
    1. Open `https://claude.ai/design/p/597c08f6-081b-4578-8c47-abd1ed09f772` in your browser (if not already open from Plan 01).
    2. Navigate to the Onboarding section / file. Note the exact file + section name.
    3. Inspect the progress bar in the mockup:
       - Does the mockup show a text row above the progress bar (e.g. "Étape 1 sur 3" + "33%")?
       - Yes → WizardProgress label row is mockup-specified; remove S-01 structural deviation and add label spec to UI-SPEC
       - No → WizardProgress label row is a structural deviation (extra element); S-01 stands
    4. Inspect the area above the wizard card in the mockup:
       - Does the mockup show a ZIKO logo/wordmark above the wizard container?
       - Yes → Hardcoded logo is expected; remove S-03 structural deviation and add logo spec to UI-SPEC
       - No → Hardcoded ZIKO logo is a structural extra element; S-03 stands
    5. Confirm the step card layout: does the mockup show framer-motion slide transitions between steps (implied by the animation spec), or static step display?
    6. Report back:
       - Confirmed file + section for onboarding surface
       - Q2 answer: WizardProgress label row visible in mockup? (yes/no)
       - Q3 answer: ZIKO logo above wizard visible in mockup? (yes/no)
  </how-to-verify>
  <resume-signal>
    Type the confirmed mockup source, Q2 answer (label row yes/no), and Q3 answer (logo yes/no). Claude will use these to determine the final deviation list before writing the UI-SPEC.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 30-onboarding-UI-SPEC.md from mockup + write 30-onboarding-REWORK-PLAN.md</name>
  <read_first>
    apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx (full component — wizard container, step routing, WizardProgress usage)
    apps/web/src/app/[locale]/coach/onboarding/page.tsx (route wrapper)
    apps/web/src/components/coach/WizardProgress.tsx (progress bar — the label row question is here)
    apps/web/src/components/coach/WizardStep1Role.tsx (step 1 component — CTA button, card padding)
    apps/web/src/components/coach/WizardStep2Profile.tsx (step 2 — ProfileForm, CTA row mt)
    apps/web/src/components/coach/WizardStep3Kyc.tsx (step 3 — KYC upload, CTA button)
    .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md (shared tokens — @see reference source)
    .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-RESEARCH.md (Onboarding Token Audit table + structural deviation descriptions)
  </read_first>
  <action>
    Write TWO files using the Write tool. Incorporate the Task 1 checkpoint answers to determine whether S-01 (label row) and S-03 (ZIKO logo) are confirmed deviations or mockup-specified elements.

    FILE 1: `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-UI-SPEC.md`

    Frontmatter:
    ```
    ---
    phase: 30
    surface: onboarding
    route: /[locale]/coach/onboarding
    status: approved
    mockup_source: "[INSERT confirmed file + section from Task 1 checkpoint]"
    created: 2026-05-20
    ---
    ```

    Required sections:

    **@see reference:** `@see .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md` — no inline color/spacing duplication.

    **Section: Surface-Specific Component Structure**
    Document the component tree as the MOCKUP shows it (not as-built). From research pre-audit, the as-built tree is:
    - OnboardingPage (RSC) → Suspense → OnboardingWizard (client)
      - `div.max-w-lg.w-full.mx-auto.py-12.px-4`
        - [ZIKO logo — conditional per Task 1 Q3 answer]
        - [wizard title — via `t()` (not hardcoded)]
        - WizardProgress — bare `h-1` (4px) `bg-border` track with `bg-primary` fill
          - [Label row — conditional per Task 1 Q2 answer]
        - AnimatePresence wrapper (required by spec — per framer-motion step transition)
          - WizardStep1Role (step=1) — `bg-white rounded-2xl p-8 border border-border shadow-sm`
          - WizardStep2Profile (step=2) — `bg-white rounded-2xl p-8 border border-border shadow-sm`
          - WizardStep3Kyc (step=3) — `bg-white rounded-2xl p-8 border border-border shadow-sm`
    Write the tree to reflect the mockup's state — if Q2=yes, include the label row in the spec; if Q2=no, document it as absent.

    **Section: Surface-Specific Layout Rules**
    - Outer container: `min-h-screen bg-background flex flex-col items-center`
    - Wizard container: `max-w-lg w-full mx-auto py-12 px-4`
    - Progress bar track: `h-1` (4px), `bg-border`, `rounded-full` — SPEC height is 4px; live code uses `h-1.5` (6px) (deviation V-01)
    - Step card: `bg-white rounded-2xl p-8 border border-border shadow-sm` — all 3 steps share this shell
    - CTA button: `rounded-lg` on all primary buttons — live code uses `rounded-xl` (deviation V-03)
    - Step 2 CTA row: `flex gap-3 mt-8 justify-end` — live code uses `mt-6` (deviation V-04)
    - Step transitions: `AnimatePresence` with `motion.div` exit/enter, `x: ±40px`, `opacity: 0→1`, `duration: 0.25s easeOut`

    **Section: Surface-Specific Copy Literals**
    - Wizard title: `t('Onboarding.title')` — NOT hardcoded French ("Devenir coach Ziko" in live code is copy deviation C-01)
    - ZIKO logo (if in mockup): render as `<p>ZIKO</p>` or `<h1>ZIKO</h1>` using `text-3xl font-bold text-primary` — must use accent `#FF5C1A` (logo wordmark is a reserved accent use per accent rule)
    - All step content strings: `t('Onboarding.*')` namespace
    - Step 1 role selection labels: `t('Onboarding.roles.*')`
    - KYC step: `t('Onboarding.kyc.*')`

    **Section: Surface-Specific Interaction States**
    - URL state: `?step=1`, `?step=2`, `?step=3` via `useSearchParams` — back/forward browser nav updates step
    - Step 1→2 transition: AnimatePresence exit left (x: -40px, opacity: 0), enter right (x: 40px, opacity: 0 → 0)
    - Step 2→3 transition: same direction pattern
    - Step 2 back button: reversed direction (x: 40px exit, x: -40px enter)
    - WizardProgress fill: `width: {(step/3)*100}%` CSS width, `bg-primary`, `transition-width 0.3s`
    - KYC upload: doc upload state, pending state, complete state — captured in KycDocList component

    **Section: Deviation Findings**
    Embed the full deviation table from RESEARCH.md Onboarding Token Audit. Apply Task 1 checkpoint answers:
    - If Q2=yes (label row in mockup): move S-01 from "structural deviation" to "spec element" and remove from deviation list
    - If Q2=no: S-01 structural deviation stands
    - If Q3=yes (logo in mockup): move S-03 from "structural deviation" to "spec element" and remove from deviation list
    - If Q3=no: S-03 structural deviation stands
    - S-02 (missing AnimatePresence): always a structural deviation — framer-motion transitions are specified in 24-UI-SPEC.md
    - C-01 (hardcoded "Devenir coach Ziko"): always a copy deviation
    - V-01 through V-04: always visual-only deviations

    ---

    FILE 2: `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-REWORK-PLAN.md`

    Summary line: Write actual structural count based on Task 1 answers (min 1 structural = S-02 AnimatePresence; max 3 if Q2=no and Q3=no). Always 4 visual-only + 1 copy.

    **### Structural Deviations**

    Always include S-02:
    | S-02 | OnboardingWizard | apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx | No AnimatePresence wrapper around step cards — step transitions render instantly (no x-slide animation) | Wrap conditionally-rendered step cards with `<AnimatePresence mode="wait">` + `<motion.div>` with `x: ±40, opacity: 0→1, transition: 0.25s easeOut` variants |

    Include S-01 only if Task 1 Q2=no:
    | S-01 | WizardProgress | apps/web/src/components/coach/WizardProgress.tsx | Label row rendered above progress bar ("Étape X sur Y" + percentage%) — spec calls for bare 4px track only, no labels | Remove the label `<div>` wrapping the text row above the `role="progressbar"` div |

    Include S-03 only if Task 1 Q3=no:
    | S-03 | OnboardingWizard | apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx | Hardcoded `<h1>ZIKO</h1>` above wizard title — this element is not in the mockup spec | Remove the hardcoded `<h1>ZIKO</h1>` element from OnboardingWizard.tsx |

    **### Visual-Only Deviations**
    | # | Component | File | What's Wrong | Fix |
    |---|-----------|------|--------------|-----|
    | V-01 | WizardProgress | apps/web/src/components/coach/WizardProgress.tsx | Progress bar track `h-1.5` (6px) vs spec `h-1` (4px) | Change track div to `h-1` |
    | V-02 | WizardStep1Role | apps/web/src/components/coach/WizardStep1Role.tsx | Body margin `mb-8` vs spec `mb-6` | Change to `mb-6` |
    | V-03 | WizardStep1/2/3 CTAs | apps/web/src/components/coach/WizardStep1Role.tsx, WizardStep2Profile.tsx, WizardStep3Kyc.tsx | CTA button `rounded-xl` vs spec `rounded-lg` on all 3 steps | Change to `rounded-lg` on all primary CTA buttons |
    | V-04 | WizardStep2Profile CTA row | apps/web/src/components/coach/WizardStep2Profile.tsx | CTA row `mt-6` vs spec `mt-8` | Change to `mt-8` |

    **### Copy Deviations**
    | # | Component | File | Current Copy | Correct Copy |
    |---|-----------|------|--------------|--------------|
    | C-01 | OnboardingWizard | apps/web/src/app/[locale]/coach/onboarding/OnboardingWizard.tsx | `"Devenir coach Ziko"` hardcoded French string | `t('Onboarding.title')` via next-intl |

    **## Fix Priority**
    1. Structural deviations — S-02 (AnimatePresence) first; S-01 and S-03 if applicable
    2. Copy deviations — C-01 (one-line i18n replacement)
    3. Visual-only deviations — V-01, V-02, V-03, V-04
  </action>
  <verify>
    <automated>ls C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-UI-SPEC.md C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-REWORK-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - `30-onboarding-UI-SPEC.md` exists in the phase directory
    - `30-onboarding-UI-SPEC.md` frontmatter contains `mockup_source:` with a non-empty value
    - `30-onboarding-UI-SPEC.md` contains `@see .planning/phases/24-coach-identity-onboarding/24-UI-SPEC.md` — no inline color/spacing duplication
    - `30-onboarding-UI-SPEC.md` Deviation Findings section reflects Task 1 checkpoint answers: S-01 status is clearly stated (confirmed deviation OR removed because mockup shows label row); S-03 status is clearly stated
    - `30-onboarding-UI-SPEC.md` documents framer-motion step transitions (AnimatePresence, x: ±40px, 0.25s) under Interaction States
    - `30-onboarding-REWORK-PLAN.md` exists
    - `30-onboarding-REWORK-PLAN.md` always contains S-02 (missing AnimatePresence) as a structural deviation
    - `30-onboarding-REWORK-PLAN.md` contains Visual-Only table with V-01 through V-04 (4 rows)
    - `30-onboarding-REWORK-PLAN.md` contains Copy Deviations table with C-01 (hardcoded French string)
    - `30-onboarding-REWORK-PLAN.md` structural deviation rows reference the exact file paths (WizardProgress.tsx, OnboardingWizard.tsx)
  </acceptance_criteria>
  <done>
    Both files exist. UI-SPEC reflects mockup-confirmed component tree with conditional deviations resolved. Rework plan has S-02 at minimum, V-01..V-04, and C-01, each with exact file paths and fix instructions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner → Executor | Mockup inspection resolves two conditional structural deviations; executor must not assume deviation status without checking mockup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | Claude Design URL (authenticated) | accept | Developer-gated design asset; no PII |
| T-30-SC | Tampering | No package installs in Phase 30 | accept | Documentation-only phase |
</threat_model>

<verification>
After both tasks complete:
1. `ls .planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-onboarding-*.md` returns 2 files
2. UI-SPEC mockup_source field is populated (non-empty string)
3. Rework plan contains at minimum S-02 structural deviation
4. No inline color palette in the UI-SPEC (no `#FF5C1A` hex literals without @see deferral)
</verification>

<success_criteria>
- `30-onboarding-UI-SPEC.md` is a complete design contract for the 3-step wizard: mockup-derived component tree, progress bar spec (resolved per mockup), step transition animation spec, copy i18n keys, interaction states. References 24-UI-SPEC.md for shared tokens.
- `30-onboarding-REWORK-PLAN.md` lists structural deviations (S-02 always; S-01 and S-03 conditional on mockup), 4 visual-only deviations (V-01..V-04), and 1 copy deviation (C-01), each with exact file paths and targeted fix instructions.
- UIDESIGN-01 satisfied: design contract (Claude Design URL + UI-SPEC.md) exists for the onboarding wizard surface.
- UIDESIGN-05 (onboarding portion) satisfied: rework plan exists.
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/30-ui-design-web-surfaces/30-02-SUMMARY.md` when done.
</output>
