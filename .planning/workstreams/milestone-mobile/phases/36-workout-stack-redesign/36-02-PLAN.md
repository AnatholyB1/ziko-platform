---
phase: 36-workout-stack-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(app)/workout/ai-generate.tsx
autonomous: true
requirements:
  - WORK-03

must_haves:
  truths:
    - "AIGenerator renders a 4-step wizard: énergie (1–10 slider), durée (5 options), zone (4 options including cardio), matériel (4 options 2×2 grid)"
    - "Step 3 has exactly 4 zone options: haut/bas/full/cardio — 'cardio' was missing per D-05"
    - "Progress dots update as user advances through steps"
    - "Loading state shows MotiView sparkle orb (orange LinearGradient + sparkles icon) per D-06"
    - "Completing step 4 calls POST /ai/tools/execute with ai_programs_generate tool"
    - "Generated session (step 4) shows AI adaptation card and exercise list with Démarrer CTA"
  artifacts:
    - path: "apps/mobile/app/(app)/workout/ai-generate.tsx"
      provides: "4-step AIGenerator wizard full redesign"
      contains: "cardio"
  key_links:
    - from: "apps/mobile/app/(app)/workout/ai-generate.tsx"
      to: "/ai/tools/execute"
      via: "fetch POST on step 4 generation"
      pattern: "ai_programs_generate"
    - from: "apps/mobile/app/(app)/workout/ai-generate.tsx"
      to: "MotiView"
      via: "loading animation"
      pattern: "MotiView"
---

<objective>
Fully redesign ai-generate.tsx as a 4-step wizard matching the workout-program-ai.jsx mockup, with the missing "Cardio + core" zone option added (D-05) and a MotiView loading animation replacing the existing ActivityIndicator (D-06).

Purpose: The AIGenerator is how users create workout programs. The current code has 3 zone + 3 equipment options — both need expanding to match the v1.7 spec. The loading state must be visually engaging (sparkle orb) rather than a plain spinner.

Output: ai-generate.tsx fully redesigned with 4-step wizard, correct options, MotiView loading, and real POST /ai/tools/execute call.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/milestone-mobile/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-CONTEXT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-RESEARCH.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md

<interfaces>
<!-- Key contracts the executor needs. -->

MotiView animation (verified from session.tsx usage):
```typescript
import { MotiView } from 'moti';
// Loop animation pattern:
<MotiView
  from={{ scale: 1, opacity: 1 }}
  animate={{ scale: 1.08, opacity: 0.85 }}
  transition={{ type: 'timing', duration: 700, loop: true, repeatReverse: true }}
/>
```

Backend tool call pattern (from CLAUDE.md):
```typescript
POST /ai/tools/execute
Body: { tool: 'ai_programs_generate', input: { energy, duration, zone, equipment } }
Authorization: Bearer {session.access_token}
```

WSHeader import (created in plan 36-01):
```typescript
import WSHeader from '../../../src/components/WSHeader';
```

Zone options (per D-05 + UI-SPEC §3.5):
const ZONES = [
  { id: 'haut', label: 'Haut du corps', sub: 'Pec, dos, épaules, bras', icon: 'barbell-outline' },
  { id: 'bas', label: 'Bas du corps', sub: 'Quadri, ischios, mollets', icon: 'walk-outline' },
  { id: 'full', label: 'Full body', sub: 'Tout en équilibre', icon: 'body-outline' },
  { id: 'cardio', label: 'Cardio + core', sub: 'Léger, récup active', icon: 'heart-outline' },
]

Equipment options (per D-05 + UI-SPEC §3.6):
const EQUIPMENT = [
  { id: 'salle', label: 'Salle complète', icon: 'barbell-outline' },
  { id: 'maison', label: 'Maison · haltères', icon: 'home-outline' },
  { id: 'outdoor', label: 'Extérieur · poids du corps', icon: 'leaf-outline' },
  { id: 'hotel', label: 'Hôtel · minimal', icon: 'bed-outline' },
]

Duration options (per UI-SPEC §3.4):
const DURATIONS = [
  { value: 20, label: '20 min', tag: 'express' },
  { value: 30, label: '30 min', tag: 'express' },
  { value: 45, label: '45 min', tag: 'standard' },
  { value: 60, label: '60 min', tag: 'complète' },
  { value: 90, label: '90 min', tag: 'long' },
]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4-step wizard redesign (steps 0–3) with correct zone/equipment options</name>
  <files>
    apps/mobile/app/(app)/workout/ai-generate.tsx
  </files>
  <read_first>
    - apps/mobile/app/(app)/workout/ai-generate.tsx (current full file — read before rewriting)
    - C:/Users/Anatholy/Downloads/ziko/workout-program-ai.jsx (canonical mockup — wizard screens reference)
    - C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md (§3 AIGenerator spec — ALL subsections 3.1 through 3.9)
  </read_first>
  <action>
    Fully redesign apps/mobile/app/(app)/workout/ai-generate.tsx as a 4-step wizard (step 0=énergie, step 1=durée, step 2=zone, step 3=matériel).

    State to maintain:
    - step: 0|1|2|3|'generating'|4 (useState)
    - energy: number (1–10, default 7)
    - duration: number | null (selected minutes)
    - zone: string | null (selected zone id)
    - equipment: string | null (selected equipment id)
    - generatedSession: any | null (API response)

    Layout (per UI-SPEC §3.1):
    - View flex:1 backgroundColor theme.background
    - WSHeader: light variant, title="Coach IA", sub=`Étape ${Math.min(step+1, 4)}/4` (import from ../../../src/components/WSHeader)
    - Progress dots row (per UI-SPEC §3.2): 4 dots, flex 1, height 3, borderRadius 999, orange if index <= step else rgba(28,26,23,0.08)
    - ScrollView flex:1 paddingHorizontal 16 paddingTop 16 paddingBottom 100 with step content
    - Sticky footer (per UI-SPEC §3.9): position absolute, bottom 0

    Step 0 — Énergie (per UI-SPEC §3.3):
    - Heading "Comment tu te sens\naujourd'hui ?" fontSize 16 fontWeight '700'
    - Subheading "De 1 (épuisé) à 10 (forme olympique)" fontSize 12 color theme.muted
    - Energy value: fontSize 64, fontWeight '700', color '#FF5C1A', textAlign center, marginTop 32 — display `{energy}/10`
    - Slider: import Slider from @react-native-community/slider if available, else use TouchableOpacity row for 1–10 (check existing package.json). minimumValue=1, maximumValue=10, step=1, minimumTrackTintColor="#FF5C1A", thumbTintColor="#FF5C1A"
    - Scale labels: "Épuisé", "Modéré", "Au top" — flexDirection row, justifyContent space-between, fontSize 10 color theme.muted fontWeight '700'

    Step 1 — Durée (per UI-SPEC §3.4):
    - Heading "Tu as combien\nde temps ?" with DURATIONS vertical list (5 items: 20/30/45/60/90 min)
    - Each option card: borderRadius 14, padding 16, flexDirection row, gap 12
    - Selected: borderWidth 1.5, borderColor '#FF5C1A', backgroundColor rgba(255,92,26,0.06)
    - Unselected: borderWidth 1, borderColor theme.border, backgroundColor theme.surface
    - Timer icon (selected: color '#FF5C1A', else theme.muted) + label fontSize 12 fontWeight '700' + tag marginLeft auto fontSize 10 color theme.muted

    Step 2 — Zone (per UI-SPEC §3.5):
    - Heading "Quelle zone\naujourd'hui ?" with ZONES vertical list (4 items per D-05: haut/bas/full/cardio)
    - Icon badge 36×36, borderRadius 10: selected backgroundColor '#FF5C1A' icon color '#fff', else backgroundColor rgba(255,92,26,0.12) icon color '#FF5C1A'
    - Same card selected/unselected styling as step 1
    - Label fontSize 12 fontWeight '700' + sub fontSize 10 color theme.muted marginTop 1

    Step 3 — Matériel (per UI-SPEC §3.6):
    - Heading "Où tu t'entraînes ?" with EQUIPMENT 2×2 grid (flexDirection row, flexWrap wrap, gap 8)
    - Each cell width '48%', column direction, borderRadius 14, padding 16, alignItems center, gap 8
    - Icon Ionicons size 20, color selected '#FF5C1A' else theme.text
    - Label fontSize 12 fontWeight '700' textAlign center lineHeight 16
    - Same selected/unselected borders

    Footer (per UI-SPEC §3.9):
    - Step 0: single orange "Continuer" full-width button
    - Steps 1–2: Retour ghost + "Continuer" primary side by side
    - Step 3: Retour ghost + "Générer ma séance" primary (with MotiView scale pulse per UI-SPEC §11.3 on press)

    Use useThemeStore((s) => s.theme) for all theme tokens. Ionicons from @expo/vector-icons. No StyleSheet. No Alert.alert (use showAlert from @ziko/plugin-sdk).
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "cardio" apps/mobile/app/\(app\)/workout/ai-generate.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 4 zone options including id: 'cardio' (per D-05)
    - File contains exactly 5 duration options (20, 30, 45, 60, 90 min)
    - File contains exactly 4 equipment options (salle, maison, outdoor, hotel)
    - File imports WSHeader from ../../../src/components/WSHeader
    - File uses useThemeStore for theme tokens
    - Progress dots section renders 4 dots (check for 4-item array or hardcoded 4 dots)
    - Footer renders single button on step 0, two buttons on steps 1–3
    - No StyleSheet.create in the file
  </acceptance_criteria>
  <done>Steps 0–3 render correctly with correct option counts, selected state styling, and footer navigation. Zone step has 4 options including cardio.</done>
</task>

<task type="auto">
  <name>Task 2: Loading animation (MotiView) + generated session view (step 4) + API call</name>
  <files>
    apps/mobile/app/(app)/workout/ai-generate.tsx
  </files>
  <read_first>
    - apps/mobile/app/(app)/workout/ai-generate.tsx (after Task 1 — current state)
    - C:/Users/Anatholy/Downloads/ziko/workout-program-ai.jsx (generated session view reference)
    - C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md (§3.7 loading state, §3.8 generated session, §11.4 loading orb animation)
    - apps/mobile/src/stores/authStore.ts (for session.access_token pattern)
  </read_first>
  <action>
    Continue editing apps/mobile/app/(app)/workout/ai-generate.tsx to add:

    1. generateSession function (async) called when user taps "Générer ma séance" on step 3:
       - Sets step to 'generating'
       - Calls POST to EXPO_PUBLIC_API_URL + '/ai/tools/execute' with body { tool: 'ai_programs_generate', input: { energy, duration, zone, equipment } } and Authorization: Bearer from useAuthStore session.access_token
       - On success: parse response, set generatedSession, set step to 4
       - On error: showAlert("Erreur", "Impossible de générer la séance. Réessaie."), set step back to 3
       - Simulated minimum loading: await a Promise that resolves after 1800ms (Math.max) so animation always shows

    2. Loading state (step === 'generating', per UI-SPEC §3.7):
       - WSHeader: title="Génération…" with onBack disabled
       - Center container: paddingVertical 64, paddingHorizontal 24, alignItems center
       - MotiView sparkle orb (per UI-SPEC §11.4):
         from={{ scale: 1, opacity: 1 }}
         animate={{ scale: 1.08, opacity: 0.85 }}
         transition={{ type: 'timing', duration: 700, loop: true, repeatReverse: true }}
         style={{ width: 80, height: 80, borderRadius: 999, overflow: 'hidden', alignItems: 'center', justifyContent: 'center' }}
       - Inside MotiView: LinearGradient colors=['#FF5C1A', '#FFB07A'] start={x:0.13,y:0} end={x:1,y:1} width 80 height 80 with Ionicons name="sparkles" size=36 color="#fff"
       - Title: "Coach IA travaille…" fontSize 16 fontWeight '700' marginTop 24 textAlign center
       - Subtitle: dynamic — uses energy state value — fontSize 12 color theme.muted lineHeight 18 textAlign center

    3. Generated session view (step === 4, per UI-SPEC §3.8):
       - WSHeader: title="Ta séance générée", sub="~{duration} min · adapté à toi"
       - AI adaptation card: borderRadius 14, padding 16, backgroundColor rgba(255,92,26,0.08), borderWidth 1, borderColor rgba(255,92,26,0.22), flexDirection row, gap 10
         - Left icon badge 32×32, borderRadius 10, backgroundColor theme.text: Ionicons name="sparkles" size=15 color="#FFE6D9"
         - Right: "Adaptations IA" label (orange uppercase) + explanation text from generatedSession response
       - Exercise list section: map over generated exercises, each row with number badge (26×26, borderRadius 8, rgba(255,92,26,0.12)), exercise name fontSize 12 fontWeight '700', sets fontSize 10 color theme.muted
       - Regenerate button (ghost): flexDirection row, gap 8, marginTop 16 — "Régénérer" text + sparkles icon — calls generateSession()
       - Footer: "Démarrer cette séance" primary orange button with play icon — calls router.push('/(app)/workout/session') or triggers session start

    Import MotiView from 'moti'. Import LinearGradient from 'expo-linear-gradient'. MotiView from/animate values defined as constants outside render to avoid loop restart on re-render.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "MotiView\|ai_programs_generate\|generating" apps/mobile/app/\(app\)/workout/ai-generate.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains MotiView import from 'moti'
    - File contains 'ai_programs_generate' (tool name for API call)
    - File contains step === 'generating' conditional render block
    - File contains 'Génération…' (loading WSHeader title)
    - File contains 'Régénérer' (regenerate button in step 4)
    - File contains 1800 (simulated loading minimum time)
    - File contains LinearGradient with colors ['#FF5C1A', '#FFB07A'] (loading orb gradient)
    - TypeScript compiles without errors on this file: npx tsc --noEmit returns no errors for ai-generate.tsx
  </acceptance_criteria>
  <done>All 4 steps + loading animation + generated session view render correctly. API call to ai_programs_generate fires on step 3 completion. Loading shows MotiView sparkle orb. Step 4 shows exercises with Régénérer button and Démarrer CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → Hono API | ai_programs_generate call carries Bearer token; server validates via Supabase auth middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Spoofing | POST /ai/tools/execute | accept | Hono auth middleware validates JWT — no additional client-side controls needed |
| T-36-02-02 | Information Disclosure | generatedSession response stored in useState | accept | Local component state only; not persisted anywhere sensitive |
| T-36-02-03 | Denial of Service | Repeated "Régénérer" taps | mitigate | Disable Régénérer button while isGenerating=true; prevents duplicate API calls |
</threat_model>

<verification>
1. grep -c "cardio" apps/mobile/app/(app)/workout/ai-generate.tsx — returns >= 1
2. grep "hotel\|outdoor\|maison\|salle" apps/mobile/app/(app)/workout/ai-generate.tsx — returns 4 equipment options
3. grep "ai_programs_generate" apps/mobile/app/(app)/workout/ai-generate.tsx — returns match
4. grep "MotiView" apps/mobile/app/(app)/workout/ai-generate.tsx — returns match
5. npx tsc --noEmit -p apps/mobile/tsconfig.json — zero errors on ai-generate.tsx
</verification>

<success_criteria>
- ai-generate.tsx shows 4 zone options (haut/bas/full/cardio) per D-05
- Loading animation uses MotiView sparkle orb, not ActivityIndicator per D-06
- API call fires to /ai/tools/execute with ai_programs_generate tool
- Generated session step shows exercise list and Régénérer + Démarrer CTAs
- No new packages installed
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-02-SUMMARY.md` when done
</output>
