---
phase: 36-workout-stack-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/RestTimer.tsx
  - apps/mobile/app/(app)/workout/session.tsx
autonomous: true
requirements:
  - WORK-08

must_haves:
  truths:
    - "RestTimer.tsx exists at apps/mobile/src/components/RestTimer.tsx as a standalone component"
    - "RestTimer renders as a transparent Modal with dark overlay, 260×260 SVG ring r=110, M:SS countdown at 76pt"
    - "SVG ring animates strokeDashoffset using Animated.Value with useNativeDriver: false"
    - "Pulse animation fires when remaining <= 5 using Animated.loop on ring opacity"
    - "Controls render: −30s ghost, Pause/Reprendre center, +30s ghost"
    - "Reprendre maintenant CTA calls onSkip prop"
    - "session.tsx gets exactly 1 import line + 1 JSX conditional render — zero logic changes"
  artifacts:
    - path: "apps/mobile/src/components/RestTimer.tsx"
      provides: "Standalone RestTimer component with SVG ring countdown"
      exports: ["default RestTimer", "RestTimerProps"]
    - path: "apps/mobile/app/(app)/workout/session.tsx"
      provides: "session.tsx with RestTimer mounted (1 import + 1 JSX only)"
      contains: "import RestTimer"
  key_links:
    - from: "apps/mobile/app/(app)/workout/session.tsx"
      to: "apps/mobile/src/components/RestTimer.tsx"
      via: "import + conditional JSX mount"
      pattern: "import RestTimer"
    - from: "apps/mobile/src/components/RestTimer.tsx"
      to: "apps/mobile/src/lib/sounds"
      via: "playCountdownBeep at remaining <= 5"
      pattern: "playCountdownBeep"
---

<objective>
Create RestTimer.tsx as a standalone component and add exactly 1 import + 1 JSX mount point to session.tsx per D-01 and D-02.

Purpose: The rest timer is the most visually impactful component in the active workout flow. The current plain-circle rest overlay in session.tsx is being replaced by a proper SVG ring with gradient, countdown animation, and pulse at 5 seconds. The constraint (D-02) of zero logic changes to session.tsx is critical to avoid breaking the active workout flow.

Output: RestTimer.tsx (new file) + minimal 2-line change to session.tsx.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/milestone-mobile/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-CONTEXT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-RESEARCH.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md

<interfaces>
<!-- Key types and contracts. Extracted from codebase and research. -->

RestTimerProps interface (per UI-SPEC §8.1):
```typescript
interface RestTimerProps {
  visible: boolean;           // controls Modal visibility
  remaining: number;          // current countdown seconds — driven by session.tsx restTimer state (DISPLAY-ONLY)
  duration: number;           // total seconds, e.g. 90 — used for ring progress ratio and sub-label
  nextLabel?: string;         // e.g. "95 kg × 7 · Développé couché"
  onSkip: () => void;         // "Reprendre maintenant" tap
  onClose: () => void;        // X button tap
  onAdjust?: (delta: number) => void;  // optional ±30s
}
```

session.tsx state variables (verified from source, lines 286-299):
```typescript
const [restTimer, setRestTimer] = useState(0);       // remaining seconds
const [restTimerMax, setRestTimerMax] = useState(0); // initial duration
const [phase, setPhase] = useState<Phase>('review'); // 'review'|'exercise'|'rest'|'complete'
// phase==='rest' block currently at lines 1354–1450
```

session.tsx existing functions for RestTimer callbacks:
- skipRest: existing function (not shown but confirmed exists) — used for "skip rest" action
- setRestTimer: setState setter for remaining countdown

SVG ring animation (from research Q1 + UI-SPEC §11.5):
```typescript
import Svg, { Circle, Defs, LinearGradient as SvgLinearGradient, Stop } from 'react-native-svg';
import { Animated, Easing } from 'react-native';

const AnimatedCircle = Animated.createAnimatedComponent(Circle);
const r = 110;
const CIRC = 2 * Math.PI * r;  // 691.15
// useNativeDriver: false REQUIRED for strokeDashoffset
```

playCountdownBeep import (from CONTEXT.md):
```typescript
import { playCountdownBeep } from '../lib/sounds';
// (optional import — wrap in try/catch if export doesn't exist)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RestTimer.tsx with SVG ring, countdown, controls, pulse animation</name>
  <files>
    apps/mobile/src/components/RestTimer.tsx
  </files>
  <read_first>
    - C:/Users/Anatholy/Downloads/ziko/workout-rest-summary.jsx (canonical mockup — RestTimer overlay visual reference, SVG ring specs at lines 64–80)
    - C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md (§8 RestTimer spec — ALL subsections 8.1 through 8.9)
    - apps/mobile/src/lib/sounds.ts or apps/mobile/src/lib/sounds.tsx (check what playCountdownBeep exports)
  </read_first>
  <action>
    Create apps/mobile/src/components/RestTimer.tsx (NEW FILE).

    Implementation follows UI-SPEC §8 exactly and research Q1 animation pattern.

    CRITICAL CONSTRAINT: useNativeDriver: false on ALL strokeDashoffset animations. useNativeDriver: true ONLY on opacity-based pulse (per research pitfall 1).

    ARCHITECTURE: RestTimer is DISPLAY-ONLY. It receives remaining from session.tsx as a prop and renders it. It does NOT own the countdown logic. Per D-02, session.tsx keeps all countdown state (restTimer, restTimerMax, setInterval). The SVG ring uses remaining/duration to compute strokeDashoffset.

    Component signature (RestTimerProps):
    - visible, remaining, duration, nextLabel?, onSkip, onClose, onAdjust?

    Internal state:
    - paused: boolean (useState(false)) — for local Pause/Reprendre button toggle only

    Refs:
    - animOffset: useRef(new Animated.Value(0)).current — for strokeDashoffset
    - pulseOpacity: useRef(new Animated.Value(1)).current — for ring opacity pulse

    Constants (defined OUTSIDE component to prevent loop restart):
    ```
    const r = 110;
    const CIRC = 2 * Math.PI * r;  // ~691.15
    const AnimatedCircle = Animated.createAnimatedComponent(Circle);
    ```

    NO internal countdown: Do not create a setInterval inside RestTimer. The remaining prop updates every second from session.tsx existing countdown (setInterval already in session.tsx). RestTimer just renders the value it receives.

    If remaining reaches 0, call onClose() in a useEffect on [remaining]: if (remaining <= 0 && visible) { setTimeout(onClose, 600); }.

    SVG ring animation (per UI-SPEC §11.5):
    - useEffect on [remaining]: Animated.timing(animOffset, { toValue: CIRC * (1 - remaining/duration), duration: 1000, useNativeDriver: false, easing: Easing.linear }).start()

    Pulse animation (per UI-SPEC §11.6):
    - useEffect on [remaining <= 5]:
      - if (remaining <= 5 && remaining > 0): start Animated.loop(Animated.sequence([Animated.timing(pulseOpacity, {toValue: 0.55, duration: 400, useNativeDriver: true}), Animated.timing(pulseOpacity, {toValue: 1.0, duration: 400, useNativeDriver: true})]))
      - else: pulseOpacity.setValue(1); (stop loop by setting new value)

    Layout (per UI-SPEC §8.2, using Modal):
    Modal visible={visible} transparent={true} animationType="fade" onRequestClose={onClose}

    Root View (inside Modal):
    - StyleSheet.absoluteFillObject equivalent: position absolute with top/bottom/left/right 0
    - backgroundColor '#1C1A17'
    - zIndex 80, flexDirection column

    MotiView entrance wrapper (per UI-SPEC §8.3):
    - MotiView from={{ opacity:0 }} animate={{ opacity:1 }} transition={{ type:'timing', duration:250 }}

    Header row (per UI-SPEC §8.4): flexDirection row, alignItems center, padding 16
    - "Repos" label: fontSize 10, fontWeight '700', letterSpacing 1.2, textTransform uppercase, color rgba(255,250,246,0.55)
    - Close button (marginLeft auto): width 36, height 36, borderRadius 12, backgroundColor rgba(255,250,246,0.08), Ionicons name="close" size=16 color="#FFFAF6", accessibilityLabel="Fermer le chrono"

    Center content (per UI-SPEC §8.5): flex:1, alignItems center, justifyContent center, paddingHorizontal 24
    - SVG container (260×260): transform [{ rotate: '-90deg' }]
      - Defs: SvgLinearGradient id="restGrad" x1="0" x2="1" y1="0" y2="1"
        - Stop offset="0%" stopColor={theme.primary}
        - Stop offset="100%" stopColor="#FFB07A"
      - Background circle: cx=130, cy=130, r=110, stroke="rgba(255,250,246,0.08)", strokeWidth=10, fill="none"
      - AnimatedCircle: cx=130, cy=130, r=110, stroke="url(#restGrad)", strokeWidth=10, fill="none", strokeLinecap="round", strokeDasharray={CIRC}, strokeDashoffset={animOffset}, opacity={pulseOpacity}
    - Countdown text container (position absolute, alignItems center):
      - Main text: `${Math.floor(remaining/60)}:${String(remaining%60).padStart(2,'0')}` — fontSize 76, fontWeight '700', color '#FFFAF6', lineHeight 76, fontVariant ['tabular-nums']
      - Sub: `/ ${Math.floor(duration/60)}:${String(duration%60).padStart(2,'0')}` — fontSize 12, color rgba(255,250,246,0.5), fontWeight '700', marginTop 8

    Next series label (per UI-SPEC §8.6): below ring, marginTop 24, conditional on nextLabel
    - "Prochaine série" uppercase fontSize 10 muted
    - nextLabel value: fontSize 16 fontWeight '700' color '#FFFAF6' marginTop 4

    Controls row (per UI-SPEC §8.7): paddingHorizontal 24, paddingBottom 16, flexDirection row, gap 8
    - −30s ghost button: flex 1, paddingVertical 12, borderRadius 14, borderWidth 1, borderColor rgba(255,250,246,0.14), backgroundColor rgba(255,250,246,0.06) — text "−30s" fontSize 12 fontWeight '700' color '#FFFAF6'
      - onPress: if onAdjust provided call onAdjust(-30), else setRemaining(r => Math.max(0, r-30))
    - Pause/Reprendre center: flex 1, paddingVertical 12, borderRadius 14, backgroundColor rgba(255,250,246,0.92), flexDirection row, gap 4
      - Paused: Ionicons play size 12 color '#1C1A17' + "Reprendre"
      - Running: "Pause"
      - Text: fontSize 12 fontWeight '700' color '#1C1A17'
      - onPress: setPaused(p => !p)
    - +30s ghost: same style as −30s — text "+30s" — onPress: if onAdjust call onAdjust(30), else setRemaining(r => r+30)

    CTA button (per UI-SPEC §8.8): paddingHorizontal 24, paddingBottom 24
    - View width '100%', paddingVertical 16, borderRadius 16, backgroundColor '#FF5C1A', flexDirection row, alignItems center, justifyContent center, gap 8
    - Ionicons name="play" size=13 color="#fff"
    - "Reprendre maintenant" fontSize 12 fontWeight '700' color '#fff'
    - onPress: onSkip()

    Imports: Animated, Easing, View, Text, TouchableOpacity, Modal from react-native; Svg, Circle, Defs, LinearGradient as SvgLinearGradient, Stop from react-native-svg; MotiView from moti; Ionicons from @expo/vector-icons; useThemeStore from @ziko/plugin-sdk; { playCountdownBeep } from '../lib/sounds' (optional).

    No StyleSheet.create.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && node -e "const fs=require('fs'); const f=fs.readFileSync('apps/mobile/src/components/RestTimer.tsx','utf8'); console.log('SVG r=110:', f.includes('r={110}') || f.includes('r=110') || f.includes('r = 110')); console.log('useNativeDriver false:', f.includes('useNativeDriver: false')); console.log('useNativeDriver true (pulse):', f.includes('useNativeDriver: true')); console.log('Modal:', f.includes('Modal')); console.log('AnimatedCircle:', f.includes('AnimatedCircle')); console.log('pulseOpacity:', f.includes('pulseOpacity')); console.log('CIRC:', f.includes('CIRC') || f.includes('circ')); console.log('onSkip:', f.includes('onSkip'));"</automated>
  </verify>
  <acceptance_criteria>
    - File apps/mobile/src/components/RestTimer.tsx exists
    - RestTimerProps interface contains: visible, remaining, duration, nextLabel?, onSkip, onClose, onAdjust?
    - File contains r = 110 (SVG ring radius per UI-SPEC)
    - File contains CIRC (circumference constant) or equivalent Math.PI * 2 * 110
    - File contains AnimatedCircle (Animated.createAnimatedComponent(Circle))
    - File contains useNativeDriver: false (for strokeDashoffset animation)
    - File contains useNativeDriver: true (for opacity pulse animation)
    - File contains 'pulseOpacity' (pulse animation ref)
    - File contains 'paused' state (Pause/Reprendre toggle — ONLY internal state; no countdown useState)
    - File contains onSkip (CTA callback)
    - File contains 'Reprendre maintenant' (CTA label per UI-SPEC §8.8)
    - File contains Modal from 'react-native' (transparent overlay pattern per UI-SPEC §8.2)
    - No StyleSheet.create
    - TypeScript compiles without errors on this file
  </acceptance_criteria>
  <done>RestTimer renders: dark overlay, SVG ring with gradient animates smoothly as remaining decreases, countdown pulses at <=5s, controls adjust remaining, Reprendre maintenant calls onSkip, X button calls onClose. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RestTimer into session.tsx (1 import + 1 JSX conditional — zero logic changes)</name>
  <files>
    apps/mobile/app/(app)/workout/session.tsx
  </files>
  <read_first>
    - apps/mobile/app/(app)/workout/session.tsx (READ lines 1–30 for imports section, lines 280–310 for state variables, lines 1340–1460 for current rest phase render block)
    - C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-RESEARCH.md (Q10 section — exact session.tsx RestTimer hook-up analysis)
  </read_first>
  <action>
    Make EXACTLY TWO changes to apps/mobile/app/(app)/workout/session.tsx per D-02.

    CRITICAL CONSTRAINT: Zero changes to any logic, state, effects, or existing JSX. Only add the import and the JSX mount. Do NOT:
    - Modify restTimer state management (lines ~391–408)
    - Change the phase transitions
    - Remove or modify the existing rest phase return block
    - Touch any other part of the file

    CHANGE 1 — Add import at the top of the file (after existing imports):
    ```
    import RestTimer from '../../../src/components/RestTimer';
    ```
    Place after the last existing import line.

    CHANGE 2 — Find the existing rest phase render block (lines ~1354–1450 where phase === 'rest' returns a SafeAreaView with a plain circle). According to research Q10 Option A (overlay mount — recommended): instead of replacing the entire return block, add a conditional overlay mount.

    Locate the exercise phase SafeAreaView root (the main return for exercise phase). Add before its closing </SafeAreaView> tag:
    ```
    {phase === 'rest' && (
      <RestTimer
        visible={phase === 'rest'}
        remaining={restTimer}
        duration={restTimerMax}
        nextLabel={nextLabel}
        onSkip={skipRest}
        onClose={skipRest}
        onAdjust={(delta) => setRestTimer((t) => Math.max(0, t + delta))}
      />
    )}
    ```

    The variables passed as props (restTimerMax, nextLabel, skipRest, setRestTimer) all already exist in session.tsx — confirm their exact names by reading the file before editing.

    If the current rest phase block is structured as `if (phase === 'rest') { return <SafeAreaView>...</SafeAreaView>; }` (early return pattern), convert it to mounting RestTimer as a Modal overlay that sits on top of the exercise phase view instead. The RestTimer component uses Modal transparent={true} so it overlays without changing the underlying layout.

    VERIFICATION OF CONSTRAINT: After the edit, run a diff and confirm only 2 line additions (import + JSX block) were made. If the diff shows more than these 2 additions, the edit exceeded scope.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && node -e "const fs=require('fs'); const f=fs.readFileSync('apps/mobile/app/(app)/workout/session.tsx','utf8'); console.log('has RestTimer import:', f.includes('import RestTimer')); console.log('has RestTimer JSX:', f.includes('<RestTimer')); console.log('restTimerMax passed:', f.includes('restTimerMax')); console.log('onSkip prop:', f.includes('onSkip')); const importCount=(f.match(/import RestTimer/g)||[]).length; console.log('RestTimer import count (expect 1):', importCount); const jsxCount=(f.match(/<RestTimer/g)||[]).length; console.log('RestTimer JSX count (expect 1):', jsxCount);"</automated>
  </verify>
  <acceptance_criteria>
    - session.tsx contains exactly 1 occurrence of "import RestTimer" at the top
    - session.tsx contains exactly 1 occurrence of "<RestTimer" in JSX
    - session.tsx passes remaining={restTimer} to RestTimer (display-only countdown value from session.tsx state)
    - session.tsx passes duration={restTimerMax} to RestTimer (ring ratio denominator)
    - session.tsx passes onSkip prop that triggers rest skip behavior
    - Countdown logic (setInterval/clearInterval for restTimer) is NOT changed — still in session.tsx
    - TypeScript compiles without errors on session.tsx
    - Git diff of session.tsx shows no more than 10 added lines (import + JSX mount block only)
  </acceptance_criteria>
  <done>session.tsx has exactly 1 import line for RestTimer and 1 JSX conditional mount. All existing countdown logic, phase transitions, and other session behavior is completely unchanged. RestTimer displays as a transparent Modal overlay when phase === 'rest'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RestTimer ↔ session.tsx | Props flow one direction (session → RestTimer) — RestTimer only calls provided callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05-01 | Tampering | session.tsx logic | mitigate | D-02 constraint: only 1 import + 1 JSX added. Verified by line count diff check in verification. |
| T-36-05-02 | Denial of Service | setInterval in RestTimer | mitigate | useEffect cleanup returns clearInterval — prevents memory leak; interval stops when visible=false |
</threat_model>

<verification>
1. File exists: ls apps/mobile/src/components/RestTimer.tsx — returns file
2. Import count: grep -c "import RestTimer" apps/mobile/app/(app)/workout/session.tsx — returns 1
3. JSX count: grep -c "<RestTimer" apps/mobile/app/(app)/workout/session.tsx — returns 1
4. SVG ring: grep "r={110}\|r = 110\|r=110" apps/mobile/src/components/RestTimer.tsx — returns match
5. Native driver: grep "useNativeDriver: false" apps/mobile/src/components/RestTimer.tsx — returns match
6. npx tsc --noEmit -p apps/mobile/tsconfig.json — zero errors on both files
</verification>

<success_criteria>
- RestTimer.tsx: dark overlay Modal, 260×260 SVG ring r=110, animated strokeDashoffset, M:SS at 76pt, pulse at <=5s, controls (-30s/Pause/+30s), Reprendre maintenant CTA
- session.tsx: exactly 1 import + 1 JSX mount added, zero other changes
- SVG animation uses useNativeDriver: false (stroke) and useNativeDriver: true (opacity pulse)
- TypeScript clean on both files
- No new packages installed (react-native-svg already present)
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-05-SUMMARY.md` when done
</output>
