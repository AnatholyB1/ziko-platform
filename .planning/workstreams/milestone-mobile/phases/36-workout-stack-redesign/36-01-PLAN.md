---
phase: 36-workout-stack-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/WSHeader.tsx
  - apps/mobile/app/(app)/workout/index.tsx
  - packages/ui/src/components/PluginHeader.tsx
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-09

must_haves:
  truths:
    - "Séance tab renders dark hero card with real AI program data from ai_generated_programs table"
    - "Progress bar fills to (currentWeek / totalWeeks) * 100% using LinearGradient orange-to-light-orange"
    - "SubTabs toggle between 'Semaine type' (weekly schedule rows) and 'N semaines' (weeks plan rows)"
    - "Empty state shows barbell icon + 'Aucun programme actif' + orange CTA navigating to ai-generate"
    - "Recent sessions strip shows last 3 sessions with 'Tout voir' link"
    - "WSHeader component exists with dark/light variant and correct props interface"
  artifacts:
    - path: "apps/mobile/src/components/WSHeader.tsx"
      provides: "Shared header for all workout screens"
      exports: ["default WSHeader", "WSHeaderProps"]
    - path: "apps/mobile/app/(app)/workout/index.tsx"
      provides: "Séance tab full redesign"
      contains: "ai_generated_programs"
    - path: "packages/ui/src/components/PluginHeader.tsx"
      provides: "Extended PluginHeader with optional dark prop"
      contains: "dark?: boolean"
  key_links:
    - from: "apps/mobile/app/(app)/workout/index.tsx"
      to: "ai_generated_programs"
      via: "TanStack Query useQuery"
      pattern: "queryKey.*active-program"
    - from: "apps/mobile/app/(app)/workout/index.tsx"
      to: "/(app)/workout/ai-generate"
      via: "router.push"
      pattern: "ai-generate"
    - from: "apps/mobile/app/(app)/workout/index.tsx"
      to: "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"
      via: "exercise row tap — router.push"
      pattern: "workout/exercise"
---

<objective>
Build the WSHeader shared component and fully redesign the Séance tab (index.tsx) to match the workout-program-ai.jsx mockup.

Purpose: This is the entry point of the workout stack — the first thing users see. It must show real AI program data (ai_generated_programs table), not manual programs. The dark hero, gradient progress bar, dual-tab schedule, and empty state CTA are all core to the v1.7 experience.

Output: WSHeader component, updated PluginHeader with dark prop, fully redesigned index.tsx with TanStack Query fetching ai_generated_programs.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/milestone-mobile/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-CONTEXT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-RESEARCH.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase and research. -->

From apps/mobile/src/stores/workoutStore.ts:
```typescript
// recentSessions: WorkoutSession[]
// loadRecentSessions(days: number): void
// currentSession: ActiveSession | null
```

From packages/ui/src/components/PluginHeader.tsx (current — no dark prop):
```typescript
interface PluginHeaderProps {
  title: string;
  onBack: () => void;
  right?: React.ReactNode;
}
```

From supabase/migrations/012_new_plugins_schema.sql:
```sql
-- ai_generated_programs columns: id, user_id, goal, split, program_data (JSONB), created_at
-- Use: .select('*').eq('user_id', userId).order('created_at', { ascending: false }).limit(1)
-- No is_active column — use most-recent as active (confirmed by research open question)
```

New WSHeader props (to create):
```typescript
interface WSHeaderProps {
  title: string;
  sub?: string;
  onBack?: () => void;
  right?: React.ReactNode;
  dark?: boolean;  // true = dark workout context
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WSHeader component + extend PluginHeader with dark prop</name>
  <files>
    apps/mobile/src/components/WSHeader.tsx
    packages/ui/src/components/PluginHeader.tsx
  </files>
  <read_first>
    - packages/ui/src/components/PluginHeader.tsx (current implementation to understand what to extend)
    - C:/Users/Anatholy/Downloads/ziko/workout-program-ai.jsx (WSHeader HTML reference — lines 103–131)
    - C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md (§9 WSHeader spec)
  </read_first>
  <action>
    Create apps/mobile/src/components/WSHeader.tsx as a standalone component (NOT wrapping PluginHeader — it has its own layout matching the mockup spec).

    WSHeaderProps interface (per UI-SPEC §9.1):
    - title: string
    - sub?: string
    - onBack?: () => void
    - right?: React.ReactNode
    - dark?: boolean (default false)

    Light variant (dark=false, per UI-SPEC §9.2):
    - Container: flexDirection row, alignItems center, gap 10, paddingHorizontal 16, paddingVertical 12, backgroundColor theme.background + 'F0' (94% opacity), borderBottomWidth 1, borderBottomColor theme.border, zIndex 10
    - Back button (if onBack): width 34, height 34, borderRadius 11, backgroundColor rgba(28,26,23,0.06), Ionicons name="chevron-back" size=16 color=theme.text
    - Title: fontSize 16, fontWeight '700', lineHeight 16, color theme.text
    - Sub (if provided): fontSize 12, color theme.muted, marginTop 1
    - Right slot: marginLeft auto

    Dark variant (dark=true, per UI-SPEC §9.3):
    - Container: backgroundColor rgba(28,26,23,0.94), borderBottomColor rgba(255,250,246,0.06)
    - Title color: #FFFAF6
    - Sub color: rgba(255,250,246,0.7)
    - Back button: backgroundColor rgba(255,250,246,0.08), icon color #FFFAF6

    Also add dark?: boolean to PluginHeader in packages/ui/src/components/PluginHeader.tsx — optional prop defaulting to false. Only add the prop typing; WSHeader is the primary component for workout screens.

    Use useThemeStore((s) => s.theme) from @ziko/plugin-sdk for theme tokens. Import Ionicons from @expo/vector-icons.
    No StyleSheet — inline style objects only.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | grep -i "WSHeader\|PluginHeader" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/WSHeader.tsx exists and exports default function WSHeader(
    - WSHeaderProps interface contains: title, sub?, onBack?, right?, dark?
    - Light variant uses theme.background + 'F0' as backgroundColor
    - Dark variant uses rgba(28,26,23,0.94) as backgroundColor
    - packages/ui/src/components/PluginHeader.tsx contains "dark?: boolean"
    - No StyleSheet.create used in WSHeader.tsx
    - Ionicons name="chevron-back" used for back button
  </acceptance_criteria>
  <done>WSHeader renders in light and dark variants. PluginHeader accepts dark prop. TypeScript clean on both files.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign index.tsx — ProgramDetail + empty state + TanStack Query for ai_generated_programs</name>
  <files>
    apps/mobile/app/(app)/workout/index.tsx
  </files>
  <read_first>
    - apps/mobile/app/(app)/workout/index.tsx (current full file — read before modifying)
    - C:/Users/Anatholy/Downloads/ziko/workout-program-ai.jsx (canonical mockup — ProgramDetail visual reference)
    - C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md (§2 Séance tab spec — ALL subsections 2.1 through 2.10)
    - apps/mobile/src/stores/workoutStore.ts (for recentSessions, loadRecentSessions, currentSession patterns)
  </read_first>
  <action>
    Fully redesign apps/mobile/app/(app)/workout/index.tsx to match the ProgramDetail mockup per UI-SPEC §2.

    KEEP from current code:
    - Import of useAuthStore (for userId)
    - Import of supabase from lib
    - Import of useWorkoutStore (for recentSessions + loadRecentSessions + currentSession)
    - ResumeBar conditional render (if currentSession exists)
    - loadRecentSessions(90) call in useEffect on mount

    REPLACE entirely:
    - The program list UI (ProgramCard, StartModes management modals) — remove all of this
    - The existing `useState<Program[]>` + direct Supabase program query — replace with TanStack Query

    NEW TanStack Query for ai_generated_programs:
    - queryKey: ['active-program', userId]
    - queryFn: supabase.from('ai_generated_programs').select('*').eq('user_id', userId).order('created_at', { ascending: false }).limit(1).single() — returns most recent as active
    - enabled: !!userId
    - Store as const { data: activeProgram, isLoading } = useQuery(...)

    program_data JSONB shape (from research + ai-programs tool): treat as { weeks: Array<{w: number, sessions: Array<{name: string, focus: string, exercises: Array<{name: string, sets: number, reps: string}>}>}> }. Parse safely with optional chaining.

    Layout (per UI-SPEC §2.1):
    SafeAreaView (bg theme.background) → ScrollView (paddingBottom: 100) containing:
    1. [Optional] ResumeBar if currentSession (keep unchanged)
    2. [Conditional on activeProgram] WSHeader light variant: title=activeProgram.goal, sub="Coach Ziko · Intermédiaire", right=three-dot menu TouchableOpacity (shows showAlert with "Activer"/"Dupliquer"/"Supprimer" options)
    3. [Conditional on activeProgram] ProgramDetail hero card (per UI-SPEC §2.3):
       - LinearGradient colors=['#1C1A17', '#2A211B'], start={x:0.13, y:0}, end={x:1,y:1}, borderRadius 14, padding 16, overflow hidden
       - Orange glow orb: position absolute, top -50, right -50, width 200, height 200, borderRadius 100, backgroundColor rgba(255,92,26,0.35)
       - Status badges row: "ACTIF" badge (orange pill) + focus badge (cream pill)
       - Week progress row: large "1" semaine / total weeks with divider
       - Progress bar: height 6, borderRadius 999, LinearGradient fill animated per UI-SPEC §11.7
    4. Description text (per UI-SPEC §2.4): fontSize 12, lineHeight 18, color theme.muted
    5. SubTabs (per UI-SPEC §2.5): pill container, "Semaine type" / "N semaines" tabs — use local useState activeTab
    6. Tab content:
       - "Semaine type": schedule rows per UI-SPEC §2.6 — map over program_data.weeks[0].sessions or equivalent; day badge, session name, muscles, chevron
       - Each session in "Semaine type" that has exercises: each exercise row is wrapped in TouchableOpacity with onPress={() => router.push(`/(app)/workout/exercise/${exercise.exercise_id}` as any)} — navigates to ExerciseDetail per D-07
       - "N semaines": week rows per UI-SPEC §2.7 — done (checkmark+green)/current (orange badge)/future state chips, "EN COURS" tag
    7. [Conditional on NOT activeProgram] Empty state per UI-SPEC §2.9: barbell icon, "Aucun programme actif", subtitle, orange "Créer un programme" CTA → router.push('/(app)/workout/ai-generate') (per D-04)
    8. [Always] Recent sessions strip: section title "Séances récentes", last 3 from recentSessions, "Tout voir →" link navigating to /(app)/workout/history

    Sticky footer CTA (per UI-SPEC §2.8): position absolute, bottom 0, left 0, right 0, paddingHorizontal 16, paddingBottom 16 — orange button "Démarrer la séance d'aujourd'hui · {todaySessionName}" — only show when activeProgram exists.

    Do NOT use StyleSheet. No Alert.alert — use showAlert from @ziko/plugin-sdk. Import WSHeader from ../../../src/components/WSHeader. Import LinearGradient from expo-linear-gradient.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | grep "workout/index" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/workout/index.tsx imports WSHeader from ../../../src/components/WSHeader
    - File contains queryKey: ['active-program' (for TanStack Query)
    - File contains 'ai_generated_programs' (Supabase table reference)
    - File contains router.push.*ai-generate (empty state CTA per D-04)
    - File contains LinearGradient (for hero gradient and progress bar)
    - File contains rgba(255,92,26,0.35) (orange glow orb color)
    - File contains router.push and workout/exercise (exercise row tap navigates to ExerciseDetail per D-07)
    - File does NOT contain Alert.alert (must use showAlert)
    - File does NOT contain SESSION_DATA, PROGRAM_DETAIL, HISTORY_DETAIL, SUMMARY_DATA (no fixtures per WORK-10)
    - File contains paddingBottom: 100 on ScrollView
    - TypeScript compiles without errors on this file
  </acceptance_criteria>
  <done>Séance tab renders ProgramDetail dark hero with SubTabs when ai_generated_programs data exists, empty state with orange CTA when no program, recent sessions strip always visible. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase RLS → index.tsx | ai_generated_programs only returns rows where auth.uid() = user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Information Disclosure | ai_generated_programs query | accept | Supabase RLS enforces user_id = auth.uid() — existing policy from migration 012 |
| T-36-01-02 | Tampering | program_data JSONB parse | mitigate | Use optional chaining on all JSONB field accesses; never trust shape without null checks |
</threat_model>

<verification>
1. npx tsc --noEmit -p apps/mobile/tsconfig.json — zero errors related to index.tsx or WSHeader
2. grep -r "ai_generated_programs" apps/mobile/app/\(app\)/workout/index.tsx — returns match
3. grep -r "active-program" apps/mobile/app/\(app\)/workout/index.tsx — returns match (TanStack Query key)
4. grep -r "ai-generate" apps/mobile/app/\(app\)/workout/index.tsx — returns match (empty state CTA)
5. grep "SESSION_DATA\|PROGRAM_DETAIL\|HISTORY_DETAIL\|SUMMARY_DATA" apps/mobile/app/\(app\)/workout/index.tsx — returns no matches
</verification>

<success_criteria>
- WSHeader component with light/dark variants exists and is TypeScript-clean
- index.tsx fetches ai_generated_programs via TanStack Query (not workout_programs store)
- Dark hero card with orange glow orb, gradient progress bar, SubTabs rendered when program active
- Empty state with barbell icon and CTA to ai-generate when no program
- Recent sessions strip always present at bottom
- Zero new packages installed (LinearGradient already in expo-linear-gradient)
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-01-SUMMARY.md` when done
</output>
