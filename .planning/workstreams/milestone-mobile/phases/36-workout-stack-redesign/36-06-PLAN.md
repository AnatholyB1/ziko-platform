---
phase: 36-workout-stack-redesign
plan: 06
type: execute
wave: 2
depends_on:
  - 36-01
  - 36-02
  - 36-03
  - 36-04
  - 36-05
files_modified: []
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - WORK-07
  - WORK-08
  - WORK-09
  - WORK-10

must_haves:
  truths:
    - "All 5 new/redesigned screen files exist"
    - "TypeScript compiles clean across all workout files"
    - "No fixture constants remain in production workout screens"
    - "RestTimer SVG ring animation uses useNativeDriver: false for strokeDashoffset"
    - "AIGenerator has exactly 4 zone options including cardio"
    - "All smoke tests pass on device"
  artifacts:
    - path: "apps/mobile/src/components/WSHeader.tsx"
      provides: "Shared header component"
      contains: "dark?: boolean"
    - path: "apps/mobile/src/components/RestTimer.tsx"
      provides: "SVG countdown overlay"
      contains: "AnimatedCircle"
    - path: "apps/mobile/src/components/ExercisePicker.tsx"
      provides: "Exercise selection modal"
      contains: "exercises-picker"
    - path: "apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx"
      provides: "ExerciseDetail dynamic route"
      contains: "exercise-history"
    - path: "apps/mobile/app/(app)/workout/session/[sessionId].tsx"
      provides: "HistoryDetail dynamic route"
      contains: "session-sets"
  key_links:
    - from: "apps/mobile/app/(app)/workout/history.tsx"
      to: "apps/mobile/app/(app)/workout/session/[sessionId].tsx"
      via: "router.push"
      pattern: "workout/session"
    - from: "apps/mobile/app/(app)/workout/session.tsx"
      to: "apps/mobile/src/components/RestTimer.tsx"
      via: "import + JSX"
      pattern: "import RestTimer"
---

<objective>
Verify all Phase 36 deliverables are complete, structurally correct, and pass smoke tests. This plan runs automated grep checks, TypeScript compile, and a human smoke test checklist.

Purpose: Phase 36 ships 5 redesigned/new screens + 3 new components. This verification plan confirms all success criteria from the ROADMAP are met before the phase is marked complete.

Output: Verification report confirming all 10 WORK requirements are satisfied.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/milestone-mobile/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-CONTEXT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated grep + TypeScript verification of all Phase 36 deliverables</name>
  <files></files>
  <read_first>
    - C:/ziko-platform/.planning/workstreams/milestone-mobile/ROADMAP.md (Phase 36 success criteria)
    - C:/ziko-platform/.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/36-CONTEXT.md (decisions D-01 through D-13 to verify)
  </read_first>
  <action>
    Run each verification check below and report results. Fix any failures before declaring pass.

    1. FILE EXISTENCE CHECKS:
    - apps/mobile/src/components/WSHeader.tsx
    - apps/mobile/src/components/RestTimer.tsx
    - apps/mobile/src/components/ExercisePicker.tsx
    - apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx
    - apps/mobile/app/(app)/workout/session/[sessionId].tsx

    2. WORK-09 — WSHeader dark variant:
    grep "dark?: boolean" apps/mobile/src/components/WSHeader.tsx
    Expected: match found

    3. WORK-08 — RestTimer SVG ring correctness:
    grep "r={110}\|r = 110\|r=110" apps/mobile/src/components/RestTimer.tsx
    grep "useNativeDriver: false" apps/mobile/src/components/RestTimer.tsx
    grep "useNativeDriver: true" apps/mobile/src/components/RestTimer.tsx
    grep "AnimatedCircle" apps/mobile/src/components/RestTimer.tsx
    grep "Reprendre maintenant" apps/mobile/src/components/RestTimer.tsx
    grep "pulseOpacity\|pulse" apps/mobile/src/components/RestTimer.tsx
    Expected: all match

    4. WORK-08 D-02 — session.tsx exactly 1 import + 1 JSX:
    grep -c "import RestTimer" apps/mobile/app/(app)/workout/session.tsx
    grep -c "<RestTimer" apps/mobile/app/(app)/workout/session.tsx
    Expected: both return 1

    5. WORK-03 D-05 — AIGenerator 4 zone options including cardio:
    grep -c "cardio" apps/mobile/app/(app)/workout/ai-generate.tsx
    grep "haut\|bas\|full\|cardio" apps/mobile/app/(app)/workout/ai-generate.tsx
    Expected: all 4 zone ids present

    6. WORK-01/WORK-02 — TanStack Query for ai_generated_programs:
    grep "ai_generated_programs" apps/mobile/app/(app)/workout/index.tsx
    grep "active-program" apps/mobile/app/(app)/workout/index.tsx
    grep "ai-generate" apps/mobile/app/(app)/workout/index.tsx
    Expected: all match

    7. WORK-10 — No fixture constants in production workout files:
    Run each:
    grep "SESSION_DATA\|PROGRAM_DETAIL\|HISTORY_DETAIL\|SUMMARY_DATA" \
      apps/mobile/app/(app)/workout/index.tsx \
      apps/mobile/app/(app)/workout/ai-generate.tsx \
      apps/mobile/app/(app)/workout/summary.tsx \
      apps/mobile/app/(app)/workout/history.tsx \
      apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx \
      apps/mobile/app/(app)/workout/session/[sessionId].tsx
    Expected: NO matches in any file

    8. WORK-04 — History navigation wired:
    grep "workout/session" apps/mobile/app/(app)/workout/history.tsx
    Expected: match found

    9. WORK-07 — Summary key features:
    grep "Share.share\|Share\.share" apps/mobile/app/(app)/workout/summary.tsx
    grep "lastCompletedSession" apps/mobile/app/(app)/workout/summary.tsx
    grep "react-native-svg" apps/mobile/app/(app)/workout/summary.tsx
    grep "workout_sessions" apps/mobile/app/(app)/workout/summary.tsx
    Expected: all match

    10. WORK-05/WORK-06 — ExerciseDetail + ExercisePicker:
    grep "exercise-history" apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx
    grep "ExercisePicker" apps/mobile/app/(app)/workout/exercise/[exerciseId].tsx
    grep "exercises-picker" apps/mobile/src/components/ExercisePicker.tsx
    grep "selectedIds" apps/mobile/src/components/ExercisePicker.tsx
    Expected: all match

    11. Convention checks:
    grep "Alert\.alert" apps/mobile/app/(app)/workout/index.tsx apps/mobile/app/(app)/workout/summary.tsx apps/mobile/app/(app)/workout/ai-generate.tsx
    Expected: NO matches (must use showAlert)

    grep "StyleSheet\.create" apps/mobile/src/components/WSHeader.tsx apps/mobile/src/components/RestTimer.tsx apps/mobile/src/components/ExercisePicker.tsx
    Expected: NO matches

    12. TypeScript compile:
    cd apps/mobile && npx tsc --noEmit
    Expected: zero errors
    (note: warnings are acceptable, only errors fail this check)

    Report all results as a table: Check | Expected | Actual | Status (PASS/FAIL).
    Fix any FAIL before proceeding to the human smoke test.
  </action>
  <verify>
    <automated>cd C:/ziko-platform/apps/mobile && npx tsc --noEmit 2>&1 | grep -E "error TS|Error" | grep -v "node_modules" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new/modified files exist at their exact paths
    - RestTimer.tsx contains r=110, AnimatedCircle, useNativeDriver: false, useNativeDriver: true, Reprendre maintenant
    - session.tsx has exactly 1 import RestTimer and 1 JSX <RestTimer mount
    - ai-generate.tsx contains all 4 zone ids (haut, bas, full, cardio)
    - index.tsx queries ai_generated_programs via TanStack Query
    - history.tsx navigates to workout/session/[sessionId]
    - summary.tsx uses Share.share, react-native-svg, lastCompletedSession, updates workout_sessions
    - Zero SESSION_DATA/PROGRAM_DETAIL/HISTORY_DETAIL/SUMMARY_DATA in any production workout file
    - Zero Alert.alert in modified workout files
    - Zero StyleSheet.create in new component files
    - TypeScript compile returns zero errors (warnings acceptable)
  </acceptance_criteria>
  <done>All automated checks pass. Report produced with PASS status for all 12 check groups. TypeScript compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — visual + functional verification on device</name>
  <what-built>
    Phase 36 full workout stack redesign:
    - Séance tab (index.tsx): dark hero ProgramDetail, SubTabs, empty state CTA
    - AIGenerator (ai-generate.tsx): 4-step wizard with MotiView loading
    - ExerciseDetail (exercise/[exerciseId].tsx): new route with history
    - ExercisePicker (ExercisePicker.tsx): modal with search + multi-select
    - HistoryDetail (session/[sessionId].tsx): new route with set chips
    - history.tsx: redesigned list, tap navigates to HistoryDetail
    - WorkoutSummary (summary.tsx): dark hero, PRs, SVG sparkline, notes, Share
    - RestTimer (RestTimer.tsx): SVG ring overlay mounted in session.tsx
    - WSHeader component with dark/light variants
  </what-built>
  <how-to-verify>
    Run the Expo dev server:
    ```
    cd apps/mobile && npx expo start
    ```

    Open on device/simulator and verify each item:

    SMOKE TEST 1 — Séance tab (WORK-01, WORK-02):
    - Navigate to Séance tab
    - IF active AI program exists: dark hero card shows with orange glow orb, current week / total weeks progress bar fills, SubTabs "Semaine type" / "N semaines" both switch content
    - IF no active program: "Aucun programme actif" shows with barbell icon and orange "Créer un programme" button that navigates to AIGenerator
    - Recent sessions strip is visible at bottom with "Tout voir" link

    SMOKE TEST 2 — AIGenerator wizard (WORK-03):
    - From empty state OR main nav, open AIGenerator
    - Step 0: energy slider shows value 1–10, progress dots visible
    - Step 1: 5 duration options (20/30/45/60/90 min) selectable
    - Step 2: 4 zone options visible — confirm "Cardio + core" is present (D-05)
    - Step 3: 2×2 equipment grid (Salle/Maison/Extérieur/Hôtel)
    - Tap "Générer ma séance": MotiView sparkle orb loading animation shows (D-06), NOT a plain ActivityIndicator
    - After loading: generated session displays with exercise list and Régénérer/Démarrer buttons

    SMOKE TEST 3 — ExerciseDetail + ExercisePicker (WORK-05, WORK-06):
    - Tap any exercise row in ProgramDetail (if active program) → ExerciseDetail opens at workout/exercise/[id]
    - 16:9 dark placeholder visible with play button
    - 3 stat tiles show (Record/Séances/Tendance)
    - SubTabs switch between Consignes/Muscles/Historique
    - Consignes tab shows numbered cue rows + AISuggestion block (orange sparkles icon)
    - Tap "Ajouter à ma séance" → ExercisePicker modal slides up
    - Search input functional, filter chips selectable, exercise rows multi-selectable
    - Footer button activates with count when items selected

    SMOKE TEST 4 — history.tsx + HistoryDetail (WORK-04):
    - Navigate to history.tsx
    - Date group headers visible ("Aujourd'hui" / "Hier" / date)
    - Session rows show icon badge, name, meta, volume chip, chevron
    - Tap a session row → navigates to workout/session/[id]
    - HistoryDetail: 4-up stats card visible, exercise set chips visible

    SMOKE TEST 5 — WorkoutSummary (WORK-07):
    - Complete a short workout to reach summary.tsx OR navigate directly if a lastCompletedSession exists in store
    - Dark hero card with orange glow orb visible
    - IF PRs exist: orange trophy cards show with trophy icon + record values
    - HR sparkline SVG curve visible
    - Notes textarea present and editable
    - "Partager" button opens native share sheet with text summary
    - "Sauvegarder & fermer" button works

    SMOKE TEST 6 — RestTimer (WORK-08):
    - In an active workout session, complete a set to trigger rest phase
    - RestTimer dark overlay appears (Modal covers full screen)
    - SVG ring visible and animating (countdown decreasing)
    - M:SS countdown readable at 76pt
    - −30s / Pause / +30s controls functional
    - "Reprendre maintenant" button dismisses timer and resumes session

    REPORT any issues found with:
    - Screen name
    - What was expected (from smoke test above)
    - What actually appeared
    - Severity: crash / wrong data / wrong visual
  </how-to-verify>
  <verify>
    <human-check>Run smoke tests above on device or simulator. All 6 smoke tests must pass without crashes or wrong navigation.</human-check>
  </verify>
  <resume-signal>Type "approved" if all 6 smoke tests pass, or describe specific failures found so they can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification phase | No new code written — verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06-01 | Tampering | Verification grep commands | accept | Grep on source files — read-only; no risk |
</threat_model>

<verification>
Final checklist against ROADMAP Phase 36 Success Criteria:

1. "Séance tab shows active program dark hero with real ai_generated_programs data" → Verified by grep check #6 (ai_generated_programs query in index.tsx) + smoke test 1
2. "AIGenerator wizard completes and calls ai_programs_generate tool" → Verified by grep check #5 (cardio zone present) + smoke test 2
3. "ExerciseDetail shows real exercise data + session history from session_sets" → Verified by grep check #10 (exercise-history queryKey) + smoke test 3
4. "WorkoutSummary saves notes and correctly identifies PRs" → Verified by grep check #9 (workout_sessions update + isNewPR) + smoke test 5
5. "RestTimer SVG ring animates and triggers correctly at end of a set" → Verified by grep check #3 (r=110, AnimatedCircle, useNativeDriver:false) + smoke test 6
</verification>

<success_criteria>
- All 12 automated grep checks report PASS
- TypeScript compile returns zero errors
- Human smoke test returns "approved" for all 6 test scenarios
- ROADMAP Phase 36 marked complete (all 5 success criteria verified)
</success_criteria>

<output>
Create `.planning/workstreams/milestone-mobile/phases/36-workout-stack-redesign/036-06-SUMMARY.md` when done
</output>
