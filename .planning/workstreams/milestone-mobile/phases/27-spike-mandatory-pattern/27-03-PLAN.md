---
phase: 27-spike-mandatory-pattern
plan: 03
type: execute
wave: 3
depends_on:
  - 27-02
files_modified: []
autonomous: false
requirements:
  - COACH-05

must_haves:
  truths:
    - "TypeScript compiles cleanly across the full monorepo with no errors"
    - "All 18 existing plugin manifests compile without modification"
    - "Expo dev build confirms the coach plugin detail screen shows a grayed-out non-interactive trash icon"
  artifacts:
    - path: ".planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md"
      provides: "Decision record (already exists from Plan 01)"
      contains: "registry-driven"
    - path: "packages/plugin-sdk/src/types.ts"
      provides: "Updated type (already modified by Plan 02)"
      contains: "mandatory?: boolean"
  key_links:
    - from: "tsc --noEmit"
      to: "packages/plugin-sdk/src/types.ts + apps/mobile"
      via: "TypeScript project references"
      pattern: "exit code 0"
---

<objective>
Verify the spike end-to-end: TypeScript compiles clean across the monorepo, then a human confirms the grayed-out trash button in a live Expo dev build.

Purpose: Satisfy D-06 (end-to-end = TypeScript clean compile + dev build visual confirmation). This is the acceptance gate for the entire Phase 27 spike.

Output: No new files. The checkpoint collects human sign-off on the visual behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-CONTEXT.md
@.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript clean compile — full monorepo</name>
  <files></files>

  <read_first>
    - `apps/mobile/tsconfig.json` — confirm project references structure
    - `packages/plugin-sdk/tsconfig.json` — confirm it is included in the compile chain
  </read_first>

  <action>
    Run TypeScript type checking across the monorepo in two steps:

    Step 1: Check plugin-sdk in isolation to confirm the type addition is clean:
    `npx tsc --noEmit -p packages/plugin-sdk/tsconfig.json`

    Step 2: Check the mobile app (which imports plugin-sdk types):
    `npx tsc --noEmit -p apps/mobile/tsconfig.json`

    If either step produces errors:
    - Read the error output carefully
    - Identify which file/line causes the error
    - Fix the exact issue (do not make broad changes)
    - Re-run the failing step until it exits 0
    - Then re-run both steps to confirm both are clean

    Do not proceed to the checkpoint task until both steps exit 0.
  </action>

  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p packages/plugin-sdk/tsconfig.json && npx tsc --noEmit -p apps/mobile/tsconfig.json && echo "ALL_CLEAN"</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit -p packages/plugin-sdk/tsconfig.json` exits with code 0
    - `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits with code 0
    - Terminal output ends with "ALL_CLEAN"
    - Zero TypeScript errors in the output (warnings are acceptable)
    - The three files modified in Plan 02 are the only files that changed (verify with `rtk git diff --name-only`)
  </acceptance_criteria>

  <done>TypeScript compiles clean in both plugin-sdk and mobile app. Zero type errors across all modified and existing files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three targeted code changes implementing the registry-driven mandatory plugin pattern:
    1. `mandatory?: boolean` added to PluginManifest in packages/plugin-sdk/src/types.ts
    2. PluginLoader pre-loads plugins with mandatory: true before querying user_plugins
    3. store/[id].tsx trash button is grayed out (opacity 0.5, no onPress) when manifest.mandatory is true

    TypeScript compiles clean across the monorepo (confirmed in Task 1).
  </what-built>

  <how-to-verify>
    To visually confirm the grayed-out trash button behavior:

    1. Open a terminal and start the Expo dev server:
       `cd apps/mobile && npx expo start`

    2. Open the Ziko app in an iOS Simulator or Android Emulator (press `i` or `a` in the Expo CLI).

    3. Sign in with any test account.

    4. Navigate to the plugin store: tap the Store tab.

    5. To test with a non-mandatory plugin (confirm normal behavior is preserved):
       - Tap any installed plugin (e.g., Habits)
       - Confirm the red trash icon is present and tappable
       - Tap it — the uninstall confirmation dialog should appear

    6. To test mandatory behavior, add `mandatory: true` to any ONE plugin manifest temporarily:
       - Edit `plugins/habits/src/manifest.ts` and add `mandatory: true` to the manifest object
       - Save the file — Expo hot-reload triggers automatically
       - Navigate to the Habits plugin detail screen in the store
       - Expected: the trash icon area renders at 50% opacity with no response when tapped

    7. Remove `mandatory: true` from the habits manifest after confirming (it was only for testing).

    Note: The coach plugin manifest does not exist yet (Phase 29 scope). The habits manifest is used as a stand-in for this visual smoke test.
  </how-to-verify>

  <resume-signal>
    Type "approved" if the grayed-out trash icon behavior is confirmed.
    Type "issue: [description]" if the behavior is wrong or the app does not start.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tsc output → plan gate | TypeScript compiler output is authoritative; non-zero exit code blocks the checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Information Disclosure | tsc error output | accept | Errors are logged locally, not transmitted |
| T-27-05 | Tampering | Temporary mandatory: true in habits manifest | accept | Temporary edit is local only, removed after test, never committed |
</threat_model>

<verification>
Phase 27 spike is complete when:
- `npx tsc --noEmit -p packages/plugin-sdk/tsconfig.json` exits 0
- `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0
- Human confirms grayed-out trash icon behavior in dev build
- 27-SPIKE.md exists with all required sections
- No unintended files modified (rtk git diff --name-only shows only the 3 expected files + SPIKE.md)
</verification>

<success_criteria>
TypeScript compiles clean. Human confirms visual grayed-out trash button in Expo dev build. Phase 27 spike is complete and Phase 28 (UI Design — Mon coach Plugin) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-03-SUMMARY.md`
</output>
