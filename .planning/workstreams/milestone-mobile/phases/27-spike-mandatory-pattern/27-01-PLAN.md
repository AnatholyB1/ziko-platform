---
phase: 27-spike-mandatory-pattern
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md
autonomous: true
requirements:
  - COACH-05

must_haves:
  truths:
    - "27-SPIKE.md exists with a comparison table covering both enforcement approaches"
    - "Decision record names registry-driven as the chosen approach and data-driven as the documented rollback"
    - "All three code files to touch are named explicitly in the document"
  artifacts:
    - path: ".planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md"
      provides: "Architecture decision record for mandatory plugin enforcement"
      contains: "registry-driven"
    - path: ".planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md"
      provides: "Rollback plan"
      contains: "data-driven"
  key_links:
    - from: "27-SPIKE.md"
      to: "packages/plugin-sdk/src/types.ts"
      via: "files-to-touch table"
      pattern: "plugin-sdk"
    - from: "27-SPIKE.md"
      to: "apps/mobile/src/lib/PluginLoader.tsx"
      via: "files-to-touch table"
      pattern: "PluginLoader"
    - from: "27-SPIKE.md"
      to: "apps/mobile/app/(app)/store/[id].tsx"
      via: "files-to-touch table"
      pattern: "store/\\[id\\]"
---

<objective>
Write the 27-SPIKE.md decision record that documents the mandatory plugin enforcement spike.

Purpose: Capture the architecture decision (registry-driven chosen, data-driven deferred) before any code changes are made, so Phase 29 implementers have a clear, authoritative reference.

Output: `.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone-mobile/ROADMAP.md
@.planning/workstreams/milestone-mobile/STATE.md
@.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 27-SPIKE.md decision record</name>
  <files>.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md</files>

  <read_first>
    - `.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-CONTEXT.md` — All locked decisions (D-01 through D-08); the SPIKE.md must faithfully reflect each one
    - `.planning/workstreams/milestone-mobile/ROADMAP.md` — Phase 27 success criteria to ensure SPIKE.md satisfies them
    - `packages/plugin-sdk/src/types.ts` — Current `PluginManifest` interface; SPIKE.md must cite the exact field being added
    - `apps/mobile/src/lib/PluginLoader.tsx` — Current loading logic; SPIKE.md must describe the exact insertion point
    - `apps/mobile/app/(app)/store/[id].tsx` lines 257–262 — Current trash button code; SPIKE.md must describe the exact gate
  </read_first>

  <action>
    Create `.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md` with the following sections and concrete content — do NOT use fenced code blocks inside the document, use inline backticks for field names and file paths:

    1. **Header** — "Phase 27 Spike: Mandatory Plugin Enforcement — Architecture Decision Record" with date 2026-05-18 and status "DECIDED".

    2. **Problem Statement** — One paragraph: the coach plugin must be pre-installed and non-removable for all authenticated athletes; two patterns were considered before writing any Phase 29 code.

    3. **Comparison Table** — Per D-01/D-04, Claude's discretion for table structure (per CONTEXT.md). The table MUST have these columns: Approach | Enforcement Layer | DB Migration Required | Code Files Touched | Rollback Path | Pros | Cons. Rows: "Registry-driven (chosen)" and "Data-driven (rollback)".

    Registry-driven row values:
    - Enforcement Layer: manifest field (`manifest.mandatory: true`)
    - DB Migration Required: No
    - Code Files Touched: `packages/plugin-sdk/src/types.ts`, `apps/mobile/src/lib/PluginLoader.tsx`, `apps/mobile/app/(app)/store/[id].tsx`
    - Rollback Path: Switch to data-driven (see below)
    - Pros: No migration, single source of truth in manifest, backward compatible (optional field defaults to undefined/false)
    - Cons: Enforcement lives in client code, not enforced at DB layer

    Data-driven row values:
    - Enforcement Layer: `user_plugins.is_mandatory boolean` column
    - DB Migration Required: Yes — new column + RLS policy update
    - Code Files Touched: new Supabase migration, `apps/mobile/src/lib/PluginLoader.tsx`, `apps/mobile/app/(app)/store/[id].tsx`
    - Rollback Path: Remove column (requires another migration)
    - Pros: DB enforces constraint, survives client bugs
    - Cons: Requires migration, backend dependency, harder to change

    4. **Decision** — "Registry-driven is chosen (per D-01)." State that `mandatory?: boolean` will be added to `PluginManifest` in `packages/plugin-sdk/src/types.ts`. Omitting the field is equivalent to `false`; all 18 existing manifests compile cleanly without modification.

    5. **Files to Touch** — Numbered list of exactly three entries with the exact file path and one-sentence description of the change for each:
       1. `packages/plugin-sdk/src/types.ts` — add `mandatory?: boolean` field to `PluginManifest` interface, after `aiSystemPromptAddition`
       2. `apps/mobile/src/lib/PluginLoader.tsx` — before the `user_plugins` Supabase query, iterate PLUGIN_LOADERS entries, load each whose resolved manifest has `mandatory === true`, call `registerPlugin` + `aiBridge.registerPlugin`, add to `loadedRef.current` — no `user_plugins` row required (per D-08)
       3. `apps/mobile/app/(app)/store/[id].tsx` lines 257–262 — replace the unconditional `<TouchableOpacity onPress={uninstall}>` trash icon with a conditional: if `manifest.mandatory === true` render a `<View>` with opacity 0.5 and no `onPress`; otherwise render the existing `TouchableOpacity` (per D-03)

    6. **Auto-Install Strategy** — One paragraph per D-07/D-08: mandatory plugins are loaded unconditionally for all authenticated users inside `PluginLoader`. No role check. No `user_plugins` row. The `loadedRef` Set still guards against double-registration.

    7. **Rollback Plan** — One paragraph per D-04: if registry-driven fails during Phase 29, the fallback is data-driven — add `is_mandatory boolean DEFAULT false` to `user_plugins`, backfill the coach plugin row, and update `PluginLoader` to read the column instead of the manifest field. This requires a Supabase migration but no manifest changes.

    8. **End-to-End Verification Criteria** — Per D-06: (a) `npx tsc --noEmit` from monorepo root exits 0 across all 18 plugin manifests and three modified files; (b) running `expo start` and navigating to the coach plugin detail screen shows the trash icon grayed out (opacity 0.5, no `onPress` handler).

    9. **Phase 29 Scope Boundary** — Bullet list of what is NOT done in this spike: full screen scaffolding, 3-state UX, i18n strings, tooltip text ("Ce plugin est obligatoire"), role-gated loading.
  </action>

  <verify>
    - File exists at `.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md`
    - Document contains the string "registry-driven" and "data-driven"
    - Document contains all three file paths: `packages/plugin-sdk/src/types.ts`, `apps/mobile/src/lib/PluginLoader.tsx`, `apps/mobile/app/(app)/store/[id].tsx`
    - Document contains the string "mandatory?: boolean"
    - Document contains the string "Rollback" with the data-driven fallback described
    - `wc -l` on the file returns > 60 lines (a stub is not acceptable)
  </verify>

  <acceptance_criteria>
    - 27-SPIKE.md exists and is non-empty (wc -l > 60)
    - Comparison table has exactly two rows: "Registry-driven (chosen)" and "Data-driven (rollback)"
    - All three files-to-touch are listed by exact path
    - Field name `mandatory?: boolean` appears in the document
    - Rollback section explicitly names `user_plugins.is_mandatory` as the fallback column
    - No section uses vague language ("will be done later", "TBD") — all rollback and criteria are concrete
  </acceptance_criteria>

  <done>27-SPIKE.md written with all 9 sections, comparison table, files-to-touch list, rollback plan, and end-to-end verification criteria.</done>
</task>

</tasks>

<verification>
- `ls .planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-SPIKE.md` returns the file
- File size > 2KB
</verification>

<success_criteria>
SPIKE.md decision record exists with comparison table, chosen approach named (registry-driven), rollback documented (data-driven), all three code files named with exact paths, and end-to-end verification criteria stated.
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone-mobile/phases/27-spike-mandatory-pattern/27-01-SUMMARY.md`
</output>
