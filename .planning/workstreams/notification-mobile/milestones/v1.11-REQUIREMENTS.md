# Requirements Archive — v1.11 Notification System

**Workstream:** notification-mobile  
**Milestone:** v1.11 Notification System  
**Status:** SHIPPED  
**Archived:** 2026-05-30  
**Coverage:** 28/28 requirements satisfied

---

## Infrastructure & Configuration

- [x] **INFRA-01:** expo-notifications plugin + iOS `aps-environment` + Android `POST_NOTIFICATIONS` added to `app.json` — *validated*
- [x] **INFRA-02:** Migration 054 creates `notification_tokens`, `notification_log`, `notification_preferences` with RLS + ON DELETE CASCADE — *validated*
- [x] **INFRA-03:** `notificationService.ts` manages push send with prefs check, token fetch + chunking ≤100, idempotency via `UNIQUE(idempotency_key)`, `notification_log` write — *validated*
- [x] **INFRA-04:** EAS Development Build (profile `development`) available; test push delivered end-to-end on real device — *validated (human smoke test 2026-05-26)*

## Token Registration & Permission

- [x] **TOKEN-01:** Custom pre-permission screen shown before native OS prompt — native prompt never displayed cold — *validated (human smoke test 2026-05-26)*
- [x] **TOKEN-02:** Push token auto-registered via `useNotificationSetup` on startup; stored via UPSERT on `(user_id, device_id)` — *validated*
- [x] **TOKEN-03:** Token rotation handled via `addPushTokenListener` — re-registration transparent — *validated*
- [x] **TOKEN-04:** `canAskAgain = false` → no re-prompt; Settings screen shows OS Settings CTA — *validated*

## Action-triggered Push Notifications

- [x] **PUSH-01:** Push on program assignment — *validated (end-to-end, 2026-05-27)*
- [x] **PUSH-02:** Push on invitation accepted (bidirectional: athlete + coach) — *validated (end-to-end, 2026-05-27)*
- [x] **PUSH-03:** Push ~2 min after workout session end; suppressed if active session — *validated (webhook + Hono handler; pipeline proven by PUSH-04)*
- [x] **PUSH-04:** Push on level-up / badge unlock; multiple events collapsed — *validated (human smoke test on device, 2026-05-27)*

## In-app Notification Center

- [x] **CENTER-01:** Notification center displays real `notification_log` data via TanStack Query — *validated*
- [x] **CENTER-02:** Tap marks notification read; "Mark all" sets `read_at` on all unread entries — *validated*
- [x] **CENTER-03:** App icon badge + header counter reflect unread count via `setBadgeCountAsync` — *validated*
- [x] **CENTER-04:** Tapping a notification navigates to relevant screen via `data.url` deep link — *validated*
- [x] **CENTER-05:** Supabase Realtime subscription in `_layout.tsx` updates center in real time — *validated*

## Cron / Scheduled Notifications

- [x] **CRON-01:** Daily 21h UTC streak-at-risk cron — streak ≥ 3, not logged today → single push per user — *validated*
- [x] **CRON-02:** Every 15min receipt poll — `DeviceNotRegistered` tokens marked `is_active = false` — *validated*
- [x] **CRON-03:** Sunday 9h UTC weekly digest — opt-in only (disabled by default) — *validated*

## Notification Preferences

- [x] **PREF-01:** Master switch (`push_enabled`) disables all push; `notificationService.ts` enforces — *validated*
- [x] **PREF-02:** 5 independent category toggles (Coach, Workout, Gamification, Santé & Habitudes, App); server respects — *validated*
- [x] **PREF-03:** Quiet hours start/end `InlinePicker`; server skips push outside window — *validated*
- [x] **PREF-04:** 600ms debounced auto-save to `notification_preferences`; no Save button — *validated*

## Local Reminders

- [x] **LOCAL-01:** Per-habit daily reminder configurable in habit create modal; `scheduleNotificationAsync` fires at chosen hour — *validated*
- [x] **LOCAL-02:** Workout-day WEEKLY reminders (days + time) configurable in workout detail screen; stored in `notification_preferences` — *validated*
- [x] **LOCAL-03:** Reminders auto-cancelled and rescheduled when prefs change (habit deletion, day/time update) — *validated*

## App Update Notifications

- [x] **APP-01:** OTA update card appears in notification center under "App" category via `expo-updates`; no native push — *validated*

---

## Deferred to Future Milestones

| Item | Target | Notes |
|------|--------|-------|
| Push native pour mises à jour app | v1.12+ | OTA only in v1.11 |
| Snooze action on workout reminders (iOS/Android action button) | v1.12+ | |
| IANA timezone for quiet hours | v1.12+ | v1.11 uses UTC offset integer |
| `InlinePicker` export + deduplication | v1.12+ | Copied inline in 2 files |

## Out of Scope (confirmed not delivered)

- Email/SMS notifications — push mobile only ✓
- Firebase Admin SDK direct — Expo Push Service only ✓
- Marketing / A/B test push — no OneSignal, Braze ✓
- Dark mode for preferences screen ✓
