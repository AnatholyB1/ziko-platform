---
phase: 02-action-triggered-push
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/src/coach/programs/service.ts
autonomous: true
requirements:
  - PUSH-01

must_haves:
  truths:
    - "An athlete receives a push notification when their coach assigns them a program"
    - "The API route returns 200 immediately after the DB write — push delivery never blocks the response"
    - "Push is deduplicated: a second assignment of the same program to the same athlete produces only one push (idempotency gate)"
    - "Notification deep-links to /(app)/coach on the athlete's device"
  artifacts:
    - path: "backend/api/src/coach/programs/service.ts"
      provides: "PUSH-01 waitUntil integration in POST /:id/assign handler"
      contains: "waitUntil"
  key_links:
    - from: "backend/api/src/coach/programs/service.ts (POST /:id/assign)"
      to: "backend/api/src/services/notificationService.ts"
      via: "import notificationService — called inside waitUntil block"
---

<objective>
Wire PUSH-01 (program assignment push) into the existing POST /coach/programs/:id/assign Hono route.

Purpose: Athletes are alerted the moment their coach assigns them a new program, driving immediate engagement with the highest-value coach action. This proves the Hono → notificationService → Expo Push → device pipeline for coach-triggered events.
Output: Modified backend/api/src/coach/programs/service.ts — one waitUntil block added after assignProgram() succeeds that fires a push to every assigned athlete.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/notification-mobile/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/notification-mobile/phases/02-action-triggered-push/02-CONTEXT.md

<!-- Key decisions in effect:
  D-06: fire-and-forget via waitUntil — route returns 200 after DB write, push runs in background
  D-08: French copy, motivational sport tone
  D-09: PUSH-01 deep link = /(app)/coach
  D-10: title "Nouveau programme 💪", body "Ton coach t'a assigné un nouveau programme. Commence maintenant !"
  idempotencyKey: program_assign_{athleteId}_{programId}
-->
</context>

<interfaces>
<!-- Extracted from backend/api/src/services/notificationService.ts (built in Phase 1) -->
interface NotificationPayload {
  recipientUserId: string;
  category: 'coach' | 'workout' | 'gamification' | 'health' | 'system';
  type: string;
  title: string;
  body: string;
  data?: { url?: string; [key: string]: unknown };
  idempotencyKey: string;
}
export const notificationService: {
  send(payload: NotificationPayload): Promise<{ sent: boolean; reason?: string }>;
};

<!-- From backend/api/src/coach/programs/service.ts — POST /:id/assign handler (lines 232–251) -->
// The result of assignProgram() contains an array of assigned client IDs.
// Shape (from the existing response): result.assigned (string[] of athleteIds) and result.programId (the forked program UUID per client).
// NOTE: read assignProgram() implementation to confirm exact shape of result before writing the waitUntil block.

<!-- @vercel/functions waitUntil import -->
import { waitUntil } from '@vercel/functions';
// waitUntil(promise) — Vercel keeps the function alive until the promise resolves.
// The HTTP response is sent before waitUntil resolves. Max background time on Vercel Pro: 300s.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add PUSH-01 waitUntil block to POST /:id/assign</name>
  <files>backend/api/src/coach/programs/service.ts</files>
  <read_first>
    - backend/api/src/coach/programs/service.ts (full file — read to understand assignProgram() return shape, existing imports, and where to insert the waitUntil call after line 245)
    - backend/api/src/services/notificationService.ts (confirm NotificationPayload interface and send() signature)
    - backend/api/package.json (confirm @vercel/functions is already a dependency — if missing, add it)
  </read_first>
  <action>
    Step 1 — Read assignProgram() return type from the data layer (imported function). The POST /:id/assign handler at line 244–246 calls it as: const result = await assignProgram(jwt, coachId, id, body.client_ids). Identify the field on result that contains the list of assigned athlete IDs. It is either result.assigned (string[]) or result.clients (string[]) — confirm by reading the function. The program ID assigned to each athlete may be a per-client fork or the template ID — confirm which UUID to use for idempotencyKey.

    Step 2 — Add import { waitUntil } from '@vercel/functions'; at the top of the file alongside existing imports. Also add import { notificationService } from '../../services/notificationService.js';

    Step 3 — In the POST /:id/assign handler, after the line `const result = await assignProgram(jwt, coachId, id, body.client_ids);` and BEFORE `return c.json(result)`, insert a waitUntil block. The block must:
    - Iterate over every athlete ID in result (the array field confirmed in Step 1)
    - For each athleteId, call notificationService.send() with:
      - recipientUserId: athleteId
      - category: 'coach'
      - type: 'program_assigned'
      - title: 'Nouveau programme 💪'
      - body: "Ton coach t'a assigné un nouveau programme. Commence maintenant !"
      - data: { url: '/(app)/coach' }
      - idempotencyKey: program_assign_{athleteId}_{programId} — where programId is the UUID of the template (the :id param) — use the template ID, not a per-client fork ID, so the key is stable regardless of fork count
    - Wrap all sends in Promise.allSettled() so a single push failure does not abort the others
    - The full waitUntil call: waitUntil(Promise.allSettled(assignedAthleteIds.map((athleteId) => notificationService.send({...}))))

    Step 4 — The return c.json(result) line is unchanged and comes immediately after the waitUntil() call (not inside it). The route returns 200 before any push resolves.

    Step 5 — TypeScript: if @vercel/functions is not yet in backend/api/package.json, run: cd backend/api && npm install @vercel/functions. Otherwise skip.

    NEVER use fenced code blocks in this action. Do not modify any other route handler in this file.
  </action>
  <verify>
    <automated>cd /c/ziko-platform/backend/api && rtk tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - backend/api/src/coach/programs/service.ts contains the import line: import { waitUntil } from '@vercel/functions'
    - backend/api/src/coach/programs/service.ts contains the import line: import { notificationService } from '../../services/notificationService.js'
    - The POST /:id/assign handler contains a call to waitUntil() that wraps Promise.allSettled()
    - Each notificationService.send() call inside the block uses category: 'coach', type: 'program_assigned'
    - idempotencyKey follows the pattern program_assign_{athleteId}_{programId} (grep confirms the literal prefix "program_assign_")
    - data.url is '/(app)/coach'
    - title is 'Nouveau programme 💪' and body contains 'Ton coach t'a assigné un nouveau programme'
    - return c.json(result) is outside and after the waitUntil() call — not inside the async block
    - rtk tsc --noEmit exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>POST /coach/programs/:id/assign sends a push notification to every assigned athlete in the background via waitUntil. Route returns 200 immediately. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hono route → notificationService | Internal call — coach-authenticated route triggers send to a different user (athlete) |
| notificationService → Expo Push API | Backend send on behalf of user — push payload crosses to external service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Spoofing | POST /:id/assign | mitigate | Route is protected by authMiddleware; coachId from JWT, not from request body. Athletes cannot trigger their own program-assign push. |
| T-02-01-02 | Repudiation | Push deduplication | mitigate | idempotencyKey program_assign_{athleteId}_{programId} ensures notification_log UNIQUE constraint blocks duplicate sends — one push per assignment. |
| T-02-01-03 | Denial of Service | waitUntil batch size | accept | client_ids is validated as non-empty array in the route; no max cap applied here. Expo SDK chunks at 100. For very large batches, individual push failures are isolated by Promise.allSettled(). Acceptable for current scale. |
| T-02-01-SC | Tampering | @vercel/functions import | mitigate | Verify @vercel/functions on npmjs.com/package/@vercel/functions before install if not already in package.json. |
</threat_model>

<verification>
After task completes:
- grep "waitUntil" backend/api/src/coach/programs/service.ts — returns at least one match
- grep "program_assign_" backend/api/src/coach/programs/service.ts — returns match confirming idempotency key prefix
- grep "notificationService" backend/api/src/coach/programs/service.ts — returns import and usage lines
- grep "Nouveau programme" backend/api/src/coach/programs/service.ts — returns match confirming French copy
- cd backend/api && rtk tsc --noEmit — exits 0
</verification>

<success_criteria>
- POST /coach/programs/:id/assign fires a push to every athlete in client_ids after a successful assignProgram() call
- Push runs in waitUntil background — route returns 200 synchronously
- Push category='coach', type='program_assigned', deep link '/(app)/coach', French copy per D-10
- idempotencyKey format: program_assign_{athleteId}_{programId}
- TypeScript compiles without errors
</success_criteria>

<output>
Create .planning/workstreams/notification-mobile/phases/02-action-triggered-push/02-01-SUMMARY.md when done.
</output>
