---
phase: 03-in-app-notification-center
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/mobile/app/(app)/_layout.tsx
autonomous: true
requirements:
  - CENTER-03
  - CENTER-05

must_haves:
  truths:
    - "The AppState placeholder in _layout.tsx is replaced with a real syncUnreadCount call — badge updates when app comes to foreground"
    - "A Supabase Realtime subscription on notification_log (filtered by user_id) is created on mount and cleaned up on unmount"
    - "When a new notification_log row is inserted for the user, the notifications TanStack Query cache is invalidated and the notification center updates without a pull-to-refresh"
    - "Badge count reflects live unread count via notificationStore.syncUnreadCount"
  artifacts:
    - path: "apps/mobile/app/(app)/_layout.tsx"
      provides: "AppState badge sync + Supabase Realtime subscription"
      contains: "syncUnreadCount"
  key_links:
    - from: "apps/mobile/app/(app)/_layout.tsx AppState listener"
      to: "notificationStore.syncUnreadCount(userId)"
      via: "import { useNotificationStore } from '../../src/stores/notificationStore'"
    - from: "apps/mobile/app/(app)/_layout.tsx Realtime subscription"
      to: "queryClient.invalidateQueries(['notifications', userId])"
      via: "useQueryClient from @tanstack/react-query"
---

<objective>
Wire badge sync and Supabase Realtime into the root authenticated layout. After this plan, the app icon badge updates whenever the app foregrounds, and new notifications from the server appear in the center in real time without a manual refresh.

Purpose: CENTER-03 (badge sync) and CENTER-05 (Realtime) are both wired at the layout level — the only component that always mounts when a user is authenticated. The Phase 1 placeholder comments in _layout.tsx mark exactly where this code goes.

Output: Updated _layout.tsx with real AppState badge sync and Realtime notification subscription.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/notification-mobile/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/notification-mobile/phases/03-in-app-notification-center/03-01-SUMMARY.md

<!-- Key implementation notes:
  - _layout.tsx already has AppState.addEventListener at lines 79-85 with placeholder comment — replace it
  - _layout.tsx already imports AppState from 'react-native', supabase from '../../src/lib/supabase'
  - Realtime subscription: supabase.channel() API. Filter: event='INSERT', schema='public', table='notification_log', filter=`user_id=eq.${userId}`
  - On INSERT payload received: call queryClient.invalidateQueries({ queryKey: ['notifications', userId] }) AND notificationStore.syncUnreadCount(userId)
  - Cleanup: channel.unsubscribe() in the useEffect return function
  - useQueryClient is already used in index.tsx — same import pattern applies here
  - Guard: both AppState listener and Realtime subscription useEffects must check if (!userId) return early
  - Do NOT modify Tabs, TabsScreen, NotificationPermissionModal, or any unrelated layout logic
-->
</context>

<interfaces>
<!-- From apps/mobile/app/(app)/_layout.tsx (current state) -->
// Existing imports already present — DO NOT re-add:
import { AppState } from 'react-native';
import { supabase } from '../../src/lib/supabase';
import { useAuthStore } from '../../src/stores/authStore';
// userId already available: const userId = useAuthStore((s) => s.user?.id);

// AppState placeholder at lines 79-85 (REPLACE this effect):
useEffect(() => {
  const stateSub = AppState.addEventListener('change', (state) => {
    if (state === 'active') {
      // badge sync placeholder — Phase 3 will add notificationStore.getState().syncUnreadCount()
    }
  });
  return () => stateSub.remove();
}, []);

<!-- From apps/mobile/src/stores/notificationStore.ts (created in Plan 01) -->
import { useNotificationStore } from '../../src/stores/notificationStore';
// Use .getState() form inside callbacks (not hook form — callbacks are not React render):
// useNotificationStore.getState().syncUnreadCount(userId)

<!-- Supabase Realtime channel API (from supabase-js v2) -->
const channel = supabase
  .channel('notification_log_realtime')
  .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'notification_log', filter: `user_id=eq.${userId}` }, (payload) => { ... })
  .subscribe();
// Cleanup: supabase.removeChannel(channel)

<!-- From @tanstack/react-query — useQueryClient for cache invalidation -->
import { useQueryClient } from '@tanstack/react-query';
const queryClient = useQueryClient();
// In Realtime handler: queryClient.invalidateQueries({ queryKey: ['notifications', userId] })
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire AppState badge sync + Supabase Realtime subscription in _layout.tsx</name>
  <files>apps/mobile/app/(app)/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/_layout.tsx (FULL FILE — understand all existing imports, effects, state variables, and the placeholder comment at lines 79-85 before making any changes)
    - apps/mobile/src/stores/notificationStore.ts (syncUnreadCount signature: syncUnreadCount(userId: string) => Promise<void>; use .getState() form in callbacks)
    - apps/mobile/app/(app)/index.tsx (useQueryClient usage pattern — import and call queryClient.invalidateQueries)
  </read_first>
  <action>
    Make two targeted changes to apps/mobile/app/(app)/_layout.tsx. All other code in the file remains unchanged.

    Change 1 — Add two new imports at the top of the file (after existing imports):
    - Add: import { useQueryClient } from '@tanstack/react-query';
    - Add: import { useNotificationStore } from '../../src/stores/notificationStore';

    Change 2 — Add useQueryClient hook call inside AppLayout component (after existing hook calls):
    - Add: const queryClient = useQueryClient();

    Change 3 — Replace the AppState placeholder useEffect (lines 79-85):

    Replace the entire existing AppState useEffect block with:
    - Same event listener on AppState 'change'
    - When state === 'active' and userId is truthy: call useNotificationStore.getState().syncUnreadCount(userId) (fire-and-forget, no await needed at the call site)
    - Keep the same cleanup: return () => stateSub.remove()
    - Add userId to the dependency array: }, [userId]);

    Change 4 — Add a NEW useEffect for the Realtime subscription (insert it directly after the AppState useEffect):
    - Guard: if (!userId) return (no subscription without a user)
    - Create channel: const channel = supabase.channel(`notifications_${userId}`) using a unique channel name that includes userId (avoids stale subscriptions across logout/login)
    - Subscribe to postgres_changes: event: 'INSERT', schema: 'public', table: 'notification_log', filter: `user_id=eq.${userId}`
    - In the callback (payload is the Supabase RealtimePostgresChangesPayload): call queryClient.invalidateQueries({ queryKey: ['notifications', userId] }) AND call useNotificationStore.getState().syncUnreadCount(userId)
    - Call channel.subscribe() to activate the subscription
    - Return cleanup: return () => { supabase.removeChannel(channel); }
    - Dependency array: [userId, queryClient]

    Ensure the Realtime useEffect is placed AFTER the AppState useEffect and BEFORE the return statement. It must be after the session/userId guard effects.

    NOTE: Do NOT modify any of the following: Tabs configuration, TabsScreen definitions, NotificationPermissionModal, useNotificationSetup call, the notification response listener effect, the user profile fetch effect, or any import that already exists.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | grep -i "_layout\|layout" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/(app)/_layout.tsx imports useQueryClient from '@tanstack/react-query' (grep confirms)
    - apps/mobile/app/(app)/_layout.tsx imports useNotificationStore from '../../src/stores/notificationStore' (grep confirms)
    - The AppState useEffect no longer contains the "badge sync placeholder" comment (grep 'badge sync placeholder' returns 0)
    - The AppState useEffect calls useNotificationStore.getState().syncUnreadCount(userId) when state === 'active' (grep 'syncUnreadCount' in file returns at least 2 matches — one in AppState, one in Realtime handler)
    - A supabase.channel() call exists in the file targeting 'notification_log' with event: 'INSERT' and filter containing 'user_id=eq.' (grep confirms)
    - queryClient.invalidateQueries is called in the Realtime handler with queryKey ['notifications', userId] (grep 'invalidateQueries' confirms)
    - supabase.removeChannel(channel) exists in a useEffect cleanup return (grep 'removeChannel' confirms)
    - TypeScript: npx tsc --noEmit exits 0 for this file
    - All existing Tabs, TabsScreen, NotificationPermissionModal, and other layout elements remain unchanged
  </acceptance_criteria>
  <done>_layout.tsx syncs the app badge on foreground via syncUnreadCount. A Supabase Realtime subscription fires on new notification_log inserts for the current user, invalidating the TanStack Query cache and updating the badge count — no pull-to-refresh needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Realtime → mobile | Server-pushed INSERT events arrive via WebSocket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | Realtime postgres_changes filter | mitigate | Filter `user_id=eq.${userId}` ensures only the authenticated user's rows are pushed; Supabase Realtime also enforces RLS for postgres_changes subscriptions |
| T-03-07 | Denial of Service | Realtime channel leak on userId change | mitigate | Channel name includes userId (`notifications_${userId}`); cleanup via supabase.removeChannel in useEffect return; dep array [userId, queryClient] ensures re-subscription on userId change |
| T-03-08 | Tampering | queryClient.invalidateQueries on Realtime event | accept | Invalidation triggers a refetch of user's own data — RLS still enforced on the refetch; no untrusted data injected into cache directly |
</threat_model>

<verification>
After the task completes:
- grep -v '^//' apps/mobile/app/\(app\)/_layout.tsx | grep 'syncUnreadCount' returns at least 2 lines
- grep 'removeChannel' apps/mobile/app/\(app\)/_layout.tsx returns a match
- grep 'notification_log' apps/mobile/app/\(app\)/_layout.tsx returns a match (Realtime table reference)
- grep 'badge sync placeholder' apps/mobile/app/\(app\)/_layout.tsx returns 0 (placeholder gone)
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
- App foregrounded: syncUnreadCount(userId) fires, badge count updates to current unread count from notification_log
- New push delivered by server: Realtime INSERT event fires within 1-3 seconds, queryClient invalidation causes notification center to refetch and show new item
- Badge count on app icon matches unread count in the notification center header
- No Realtime channel leak: navigating away from authenticated layout calls supabase.removeChannel
</success_criteria>

<output>
Create .planning/workstreams/notification-mobile/phases/03-in-app-notification-center/03-03-SUMMARY.md when done.
</output>
