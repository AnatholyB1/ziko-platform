---
phase: 03-in-app-notification-center
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/mobile/app/(app)/notifications.tsx
autonomous: true
requirements:
  - CENTER-01
  - CENTER-02
  - CENTER-04

must_haves:
  truths:
    - "notifications.tsx renders real notification_log rows via useNotifications — INITIAL_ITEMS array is removed"
    - "Tapping a notification calls router.push(item.url) (deep link) and markRead(item.id)"
    - "Tapping 'Tout marquer lu' calls markAllRead() mutation and unreadCount drops to zero in the header"
    - "Category filter pills work correctly against real data (category mapping: coach/workout/gamification/health/system)"
    - "Today/earlier grouping is derived from real created_at timestamps, not hardcoded mock groups"
    - "Empty state renders when no items match the active filter"
  artifacts:
    - path: "apps/mobile/app/(app)/notifications.tsx"
      provides: "Live notification center screen — no mock data"
      contains: "useNotifications"
  key_links:
    - from: "apps/mobile/app/(app)/notifications.tsx"
      to: "apps/mobile/src/hooks/useNotifications.ts"
      via: "import { useNotifications } from '../../src/hooks/useNotifications'"
    - from: "NotifCard onTap handler"
      to: "router.push(item.url)"
      via: "Expo Router deep navigation — item.url from notification_log data.url"
---

<objective>
Replace the static INITIAL_ITEMS mock data in notifications.tsx with real data from the useNotifications hook. Wire mark-read on tap, markAllRead on button press, and deep-link navigation.

Purpose: This is the user-visible deliverable of Phase 3. After this plan, the notification center shows real server data and read/unread state is persisted to Supabase.

Output: Updated notifications.tsx — same visual structure, real data, live mutations.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/notification-mobile/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/notification-mobile/phases/03-in-app-notification-center/03-01-SUMMARY.md

<!-- Critical constraints:
  - DO NOT redesign the screen — preserve all visual elements (NotifCard, filter pills, header, Settings CTA, empty state)
  - ONLY replace mock data with real data — no new components, no layout changes
  - notifications.tsx is a Tabs.Screen hidden from tab bar (href: null in _layout.tsx) — no routing changes needed
  - item.url may be undefined (older notifications with no data.url) — router.push only when url is truthy
  - Loading state: show nothing (or a minimal skeleton) while isLoading is true
  - Filter pill 'pr' covers both 'pr' and 'achievement' cats — this logic is already in the file; preserve it
-->
</context>

<interfaces>
<!-- From apps/mobile/src/hooks/useNotifications.ts (created in Plan 01) -->
import { useNotifications, NotificationRow } from '../../src/hooks/useNotifications';
// Returns: { items: NotifItem[], isLoading: boolean, unreadCount: number, markRead: (id: string) => void, markAllRead: () => void }

<!-- NotifItem shape (as defined in notifications.tsx, extended with url field) -->
interface NotifItem {
  id: string;
  cat: 'pr' | 'achievement' | 'coach' | 'community' | 'reminder';
  group: 'today' | 'earlier';
  icon: keyof typeof Ionicons.glyphMap;
  tint: string;
  title: string;
  body: string;
  time: string;
  unread: boolean;
  url?: string;    // deep link path from notification_log data.url
  action?: string;
}

<!-- Existing filter logic in notifications.tsx (PRESERVE exactly) -->
const filtered = items.filter((n) => {
  if (filter === 'all') return true;
  if (filter === 'pr') return n.cat === 'pr' || n.cat === 'achievement';
  return n.cat === filter;
});

<!-- Existing time grouping (PRESERVE exactly) -->
const today = filtered.filter((n) => n.group === 'today');
const earlier = filtered.filter((n) => n.group === 'earlier');
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire useNotifications into notifications.tsx — replace mock, add deep link tap</name>
  <files>apps/mobile/app/(app)/notifications.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/notifications.tsx (FULL FILE — understand every component, the useState items pattern, tapNotif, markAllRead, NotifCard, and the filter/group logic before touching anything)
    - apps/mobile/src/hooks/useNotifications.ts (the hook's return signature: items, isLoading, unreadCount, markRead, markAllRead)
    - apps/mobile/src/stores/authStore.ts (to get userId: useAuthStore(s => s.user?.id))
  </read_first>
  <action>
    Make the following targeted changes to apps/mobile/app/(app)/notifications.tsx. Preserve ALL visual elements and layout exactly — only change state management and handlers.

    Step 1 — Add imports at the top of the file:
    - Add: import { useNotifications } from '../../src/hooks/useNotifications';
    - Add: import { useAuthStore } from '../../src/stores/authStore';
    - Keep all existing imports (View, Text, ScrollView, TouchableOpacity, SafeAreaView, router, Ionicons, useThemeStore)

    Step 2 — Update NotifItem interface:
    - Add optional url field: url?: string;
    - Keep all other existing fields unchanged

    Step 3 — Remove INITIAL_ITEMS constant (the entire const INITIAL_ITEMS: NotifItem[] = [...] array). Do not replace it with anything — it is replaced by the hook.

    Step 4 — Update NotificationsScreen component:
    - Add: const userId = useAuthStore((s) => s.user?.id);
    - Add: const { items, isLoading, unreadCount, markRead, markAllRead } = useNotifications(userId);
    - Remove: const [items, setItems] = useState<NotifItem[]>(INITIAL_ITEMS);
    - Remove: const unreadCount = items.filter((n) => n.unread).length; (unreadCount now comes from hook)
    - Keep: const [filter, setFilter] = useState('all');

    Step 5 — Update markAllRead handler:
    - Remove the inline lambda: const markAllRead = () => setItems((prev) => prev.map((n) => ({ ...n, unread: false })));
    - The hook already exports markAllRead — it is already destructured. The existing button's onPress={markAllRead} now calls the mutation. No change needed to the button JSX.

    Step 6 — Update tapNotif / NotifCard tap handler:
    - Remove: const tapNotif = (id: string) => setItems((prev) => prev.map((n) => n.id === id ? { ...n, unread: false } : n));
    - Replace with: const tapNotif = (id: string, url?: string) => { markRead(id); if (url) router.push(url as any); };
    - Update each NotifCard onTap call in the JSX from onTap={() => tapNotif(n.id)} to onTap={() => tapNotif(n.id, n.url)}

    Step 7 — Add loading state:
    - At the top of the ScrollView content (after the filter pills block), add: if (isLoading) return null; inside the component body, BEFORE the filtered/today/earlier derivations.
    - Wrap the return like: if (isLoading) { return ( <SafeAreaView style={{ flex: 1, backgroundColor: theme.background }} /> ); }

    Step 8 — filtered/today/earlier/unreadCount derivations: these already use `items` and `unreadCount` — no changes needed since items now comes from the hook and unreadCount also from hook.

    Step 9 — Keep ALL of the following unchanged:
    - FILTERS constant
    - cardStyle computation
    - sectionLabel function
    - Header JSX (back button, title with unread count, "Tout marquer lu" button)
    - Filter pills ScrollView
    - Empty state View
    - today and earlier section renders with NotifCard
    - Settings CTA
    - NotifCard component function (at bottom of file)
    - All style objects and theme usages

    NOTE: The NotifCard component receives item.unread — since items from the hook are already mapped NotifItem objects, unread: boolean is correctly set. No changes needed to NotifCard.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | grep -i "notifications" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - INITIAL_ITEMS constant no longer exists in the file (grep 'INITIAL_ITEMS' notifications.tsx returns empty)
    - useNotifications import present at top of file (grep 'useNotifications' confirms)
    - useAuthStore import present (grep 'useAuthStore' confirms)
    - useState for items is removed — no const [items, setItems] = useState in the file
    - tapNotif function calls markRead(id) and calls router.push(url) when url is truthy
    - markAllRead from hook is used directly (no inline lambda replacing it)
    - Loading guard present: component returns early when isLoading is true
    - NotifCard onTap calls tapNotif(n.id, n.url) — url forwarded
    - TypeScript: npx tsc --noEmit exits 0 for this file
    - Visual structure preserved: FILTERS array unchanged, NotifCard component body unchanged, all style objects present
  </acceptance_criteria>
  <done>notifications.tsx shows real data from notification_log. Tapping a notification calls markRead and navigates via deep link. "Tout marquer lu" calls the markAllRead mutation. Category filters and time grouping work against real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Expo Router push(url) | url comes from notification_log data.url — server-controlled value navigated client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | router.push(data.url) | accept | data.url is written server-side by notificationService.ts; mobile read is RLS-protected (auth.uid() = user_id); no user input involved. Expo Router push with an unknown path navigates to the 404 screen — no crash risk. |
| T-03-05 | Information Disclosure | notifications.tsx renders full notification body | accept | Items are user's own notifications only (RLS enforced); no cross-user data exposure. |
</threat_model>

<verification>
After the task completes:
- grep -c 'INITIAL_ITEMS' apps/mobile/app/\(app\)/notifications.tsx returns 0
- grep 'useNotifications' apps/mobile/app/\(app\)/notifications.tsx returns at least 2 matches (import + usage)
- grep 'markRead' apps/mobile/app/\(app\)/notifications.tsx returns at least 2 matches
- grep 'router.push' apps/mobile/app/\(app\)/notifications.tsx returns at least 1 match
- npx tsc --noEmit from repo root exits 0
</verification>

<success_criteria>
- notifications.tsx renders real notification_log data (no hardcoded INITIAL_ITEMS)
- Tap notification: markRead(id) called + router.push(url) navigates to deep-link screen
- "Tout marquer lu" button: markAllRead() called + unread badge drops to zero
- Filter pills: 'coach', 'reminder', 'achievement' categories work with real data
- Today / earlier grouping works on real created_at timestamps
- Loading state handled gracefully (blank screen while isLoading)
</success_criteria>

<output>
Create .planning/workstreams/notification-mobile/phases/03-in-app-notification-center/03-02-SUMMARY.md when done.
</output>
