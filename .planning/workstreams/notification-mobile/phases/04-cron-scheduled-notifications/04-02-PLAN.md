---
phase: 04-cron-scheduled-notifications
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/api/src/app.ts
  - backend/api/vercel.json
autonomous: true
requirements:
  - CRON-01
  - CRON-02
  - CRON-03
must_haves:
  truths:
    - "app.ts mounts notificationsCronRouter at /notifications alongside the existing notificationsRouter"
    - "vercel.json crons array contains entries for /notifications/cron/streak-at-risk, /notifications/cron/check-receipts, /notifications/cron/weekly-digest"
    - "The three cron endpoints respond 401 (not 404) when hit without auth, confirming they are registered"
  artifacts:
    - path: "backend/api/src/app.ts"
      provides: "notificationsCronRouter mounted at /notifications"
      contains: "notificationsCronRouter"
    - path: "backend/api/vercel.json"
      provides: "Three Vercel Cron schedule entries"
      contains: "/notifications/cron/streak-at-risk"
  key_links:
    - from: "app.ts"
      to: "notifications-cron.ts"
      via: "import { notificationsCronRouter } from './routes/notifications-cron.js'"
      pattern: "notificationsCronRouter"
    - from: "vercel.json crons"
      to: "app.ts route handlers"
      via: "Vercel Cron GET request to /notifications/cron/* routes"
      pattern: "/notifications/cron/"
---

<objective>
Wire `notificationsCronRouter` into the Hono app and register the three cron schedules in `vercel.json`.

Purpose: The router created in Plan 04-01 is not yet reachable — it needs an import and a mount in `app.ts`, plus three schedule entries in `vercel.json` to activate Vercel Cron triggering. After this plan all three endpoints are live.

Output: Updated `app.ts` with the new import and route mount; updated `vercel.json` with three new cron entries (7 total, up from 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-mobile/phases/04-cron-scheduled-notifications/04-01-SUMMARY.md

<interfaces>
<!-- Patterns extracted from the current codebase -->

From backend/api/src/app.ts — dual router mount pattern (existing lines):
```typescript
import { notificationsRouter } from './routes/notifications.js';
// ... (line 14)
import { storageRouter, storageCleanupRouter } from './routes/storage.js';
import { formsRouter, formsCronRouter } from './routes/forms.js';

app.route('/notifications', notificationsRouter);   // line 70
app.route('/storage', storageRouter);               // line 71
app.route('/storage', storageCleanupRouter);        // dual mount same prefix
app.route('/forms', formsRouter);
app.route('/forms', formsCronRouter);               // dual mount same prefix
```

From backend/api/vercel.json — existing crons array (4 entries):
```json
{
  "crons": [
    { "path": "/supplements/cron/scrape",       "schedule": "0 3 * * 1" },
    { "path": "/storage/cron/cleanup",           "schedule": "0 4 * * *" },
    { "path": "/coach/ai/monitor-cron",          "schedule": "0 7 * * *" },
    { "path": "/forms/cron/trigger-fixed-date",  "schedule": "0 6 * * *" }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount notificationsCronRouter in app.ts</name>
  <files>backend/api/src/app.ts</files>
  <read_first>
    - backend/api/src/app.ts — read full file to identify exact insertion points for import and app.route() call
    - backend/api/src/routes/notifications-cron.ts — confirm export name is notificationsCronRouter
  </read_first>
  <action>
Make two targeted edits to `backend/api/src/app.ts`.

Edit 1 — Add import after the existing notificationsRouter import line:
Find: `import { notificationsRouter } from './routes/notifications.js';`
Add after it: `import { notificationsCronRouter } from './routes/notifications-cron.js';`

Edit 2 — Mount the cron router after the existing notificationsRouter mount:
Find: `app.route('/notifications', notificationsRouter);`
Add after it: `app.route('/notifications', notificationsCronRouter);`

No other changes. The mount prefix `/notifications` is correct — the cron router's handler paths `/cron/streak-at-risk`, `/cron/check-receipts`, `/cron/weekly-digest` are sub-paths, so the full registered paths become `/notifications/cron/streak-at-risk` etc., matching the vercel.json entries in Task 2.
  </action>
  <verify>
    <automated>cd /c/ziko-platform/backend/api && npm run type-check 2>&1 | head -30</automated>
  </verify>
  <done>app.ts imports notificationsCronRouter and mounts it at /notifications; TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add three cron entries to vercel.json</name>
  <files>backend/api/vercel.json</files>
  <read_first>
    - backend/api/vercel.json — read full current content (4 existing entries) before editing
  </read_first>
  <action>
Append three new objects to the `crons` array in `backend/api/vercel.json`. The array currently has 4 entries; after this task it will have 7.

New entry 1 (CRON-01 — streak-at-risk, daily 21:00 UTC):
`{ "path": "/notifications/cron/streak-at-risk", "schedule": "0 21 * * *" }`

New entry 2 (CRON-02 — receipt polling, every 15 minutes):
`{ "path": "/notifications/cron/check-receipts", "schedule": "*/15 * * * *" }`
IMPORTANT: `*/15 * * * *` requires Vercel Pro plan (Hobby minimum is 1 hour). Default to `*/15` (Pro). If the project is on Hobby plan, change this entry's schedule to `"0 * * * *"` — hourly is still within Expo's 24-hour receipt expiry window. No code change needed, only this line.

New entry 3 (CRON-03 — weekly digest, Sunday 09:00 UTC):
`{ "path": "/notifications/cron/weekly-digest", "schedule": "0 9 * * 0" }`

All other top-level keys (`buildCommand`, `rewrites`, `ignoreCommand`) remain exactly as they are.
  </action>
  <verify>
    <automated>node -e "const v = JSON.parse(require('fs').readFileSync('./backend/api/vercel.json','utf8')); const paths = v.crons.map(c => c.path); console.log('cron count:', v.crons.length); console.log('streak-at-risk:', paths.includes('/notifications/cron/streak-at-risk')); console.log('check-receipts:', paths.includes('/notifications/cron/check-receipts')); console.log('weekly-digest:', paths.includes('/notifications/cron/weekly-digest'));" 2>&1</automated>
  </verify>
  <done>vercel.json crons array has 7 entries; the three /notifications/cron/* paths are present; node parse confirms all three booleans are true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron scheduler → Hono routes | Vercel sends GET with Authorization: Bearer $CRON_SECRET; no JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | /notifications/cron/* endpoints post-mount | mitigate | verifyCronSecret() in each handler (implemented in Plan 04-01); 401 returned before any DB query |
| T-04-06 | Denial of Service | vercel.json 15-min cron on Hobby plan | accept | Schedule rejects at deploy time on Hobby — visible immediately; fallback documented in task action |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
After both tasks complete:
1. `cd backend/api && npm run type-check` passes with zero errors
2. `grep "notificationsCronRouter" backend/api/src/app.ts` returns 2 lines (import + app.route)
3. `node -e "const v = JSON.parse(require('fs').readFileSync('./backend/api/vercel.json','utf8')); console.log(v.crons.length)"` prints 7
4. Manual smoke test after deploy: `curl -H "Authorization: Bearer $CRON_SECRET" https://ziko-api-lilac.vercel.app/notifications/cron/check-receipts` returns 200 with `{checked, deactivated_users}`
5. Without auth header: `curl https://ziko-api-lilac.vercel.app/notifications/cron/check-receipts` returns 401
</verification>

<success_criteria>
- app.ts imports and mounts notificationsCronRouter at /notifications (two lines added, no other changes)
- vercel.json crons array grows from 4 to 7 entries with the three /notifications/cron/* paths
- TypeScript compiles clean
- All three cron paths return 401 on unauthenticated request (not 404 — confirms routing is active)
</success_criteria>

<output>
Create `.planning/workstreams/notification-mobile/phases/04-cron-scheduled-notifications/04-02-SUMMARY.md` when done
</output>
