---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(app)/profile/settings.tsx
autonomous: true
requirements:
  - PREF-01
  - PREF-03
  - PREF-04

must_haves:
  truths:
    - "On mount, a row always exists in notification_preferences for the user (UPSERT defaults with ignoreDuplicates=true runs before the first toggle)"
    - "All preference changes are persisted to notification_preferences via 600ms debounced UPSERT — no Save button exists, no change is lost on navigation"
    - "The push_enabled master switch state reflects the real value from notification_preferences.push_enabled"
    - "Each UPSERT includes timezone_offset auto-detected via Math.round(-new Date().getTimezoneOffset() / 60)"
  artifacts:
    - path: "apps/mobile/app/(app)/profile/settings.tsx"
      provides: "Rewritten NotifSubScreen with correct state shape and data layer"
      contains: "notification_preferences"
  key_links:
    - from: "NotifSubScreen state (s.push_enabled)"
      to: "notification_preferences.push_enabled"
      via: "Supabase direct UPSERT"
      pattern: "supabase\\.from\\('notification_preferences'\\)"
---

<objective>
Replace the data layer of `NotifSubScreen` in `settings.tsx`. This means: (1) redefining the local state shape to match `notification_preferences` columns, (2) implementing mount logic (UPSERT defaults with `ignoreDuplicates: true`, then SELECT existing row), and (3) implementing the 600ms debounced auto-save UPSERT that always includes `timezone_offset`.

Purpose: The current `NotifSubScreen` reads/writes `user_profiles.settings.notif_prefs` with 9 unmapped keys. After this plan, the data layer correctly targets `notification_preferences` and all four PREF requirements have the persistence foundation they depend on.

Output: A `NotifSubScreen` function whose state, mount effect, and save logic are complete and correct. The JSX render is a stub (loading spinner + placeholder) — Plan 02 replaces it with the real UI.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-mobile/ROADMAP.md
@.planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-CONTEXT.md
@.planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NotifSubScreen state shape and mount logic</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>

  <read_first>
    - apps/mobile/app/(app)/profile/settings.tsx — Read entirely. The current NotifSubScreen (lines 40-146) is the only function body to replace. Note the InlinePicker component (lines 164-218) is unchanged. Note the saveRef pattern (line 49: `useRef<ReturnType<typeof setTimeout> | undefined>(undefined)`). Note the existing notifDenied banner (lines 97-127): keep this HTML block verbatim.
    - supabase/migrations/054_notification_schema.sql — Lines 79-105: exact column names for notification_preferences. Columns to use: user_id, push_enabled, coach_enabled, workout_enabled, gamification_enabled, health_enabled, system_enabled, quiet_hours_start, quiet_hours_end, timezone_offset. The type_prefs JSONB column is NOT part of this phase's UI.
  </read_first>

  <action>
  Replace the `NotifSubScreen` function body (lines 40-146 in settings.tsx) entirely. Do NOT touch any other function in the file (STHeader, InlinePicker, AppearanceSubScreen, IntegrationsSubScreen, SettingsScreen).

  The new function signature stays identical: `function NotifSubScreen({ onBack, userId }: { onBack: () => void; userId: string })`.

  New local state shape — replace the old `s` object (9 keys: sessionsReminder, hydration, streakAlert, coach, achievements, social, marketing, sound, haptics) with the following 7-key object matching notification_preferences columns exactly (per D-02, D-03):

    const [s, setS] = useState({
      push_enabled: true,
      coach_enabled: true,
      workout_enabled: true,
      gamification_enabled: true,
      health_enabled: true,
      system_enabled: true,
      quiet_hours_start: 22,
      quiet_hours_end: 7,
    });

  Keep these existing declarations unchanged:
    - `const theme = useThemeStore((s) => s.theme);`
    - `const [isLoading, setIsLoading] = useState(true);`
    - `const [notifDenied, setNotifDenied] = useState(false);`
    - `const saveRef = useRef<ReturnType<typeof setTimeout> | undefined>(undefined);`

  Add two new picker-visibility state vars after saveRef (needed by Plan 02's JSX but declared now):
    - `const [startPickerVisible, setStartPickerVisible] = useState(false);`
    - `const [endPickerVisible, setEndPickerVisible] = useState(false);`

  Replace the two existing useEffect hooks with three new ones:

  EFFECT 1 — Permission check (keep from existing, no change needed):
    useEffect(() => {
      Notifications.getPermissionsAsync().then(({ canAskAgain, status }) => {
        setNotifDenied(status === 'denied' && !canAskAgain);
      });
    }, []);

  EFFECT 2 — Mount: UPSERT defaults then SELECT (per D-08, D-11, D-12):
    useEffect(() => {
      if (!userId) return;
      const tzOffset = Math.round(-new Date().getTimezoneOffset() / 60);
      supabase
        .from('notification_preferences')
        .upsert(
          {
            user_id: userId,
            push_enabled: true,
            coach_enabled: true,
            workout_enabled: true,
            gamification_enabled: true,
            health_enabled: true,
            system_enabled: true,
            quiet_hours_start: 22,
            quiet_hours_end: 7,
            timezone_offset: tzOffset,
          },
          { onConflict: 'user_id', ignoreDuplicates: true }
        )
        .then(() => {
          return supabase
            .from('notification_preferences')
            .select('push_enabled, coach_enabled, workout_enabled, gamification_enabled, health_enabled, system_enabled, quiet_hours_start, quiet_hours_end')
            .eq('user_id', userId)
            .single();
        })
        .then(({ data }) => {
          if (data) {
            setS({
              push_enabled: data.push_enabled ?? true,
              coach_enabled: data.coach_enabled ?? true,
              workout_enabled: data.workout_enabled ?? true,
              gamification_enabled: data.gamification_enabled ?? true,
              health_enabled: data.health_enabled ?? true,
              system_enabled: data.system_enabled ?? true,
              quiet_hours_start: data.quiet_hours_start ?? 22,
              quiet_hours_end: data.quiet_hours_end ?? 7,
            });
          }
          setIsLoading(false);
        })
        .catch(() => setIsLoading(false));
    }, [userId]);

  Note: If UPSERT succeeds but row existed (ignoreDuplicates=true skips write), the chained SELECT still returns the existing row. If SELECT returns no data (edge case), defaults in state are already set.

  Replace the old `updateToggle` function with a new generic `handleChange` helper (per D-09, D-10):
    const handleChange = (patch: Partial<typeof s>) => {
      const next = { ...s, ...patch };
      setS(next);
      clearTimeout(saveRef.current);
      const tzOffset = Math.round(-new Date().getTimezoneOffset() / 60);
      saveRef.current = setTimeout(async () => {
        await supabase
          .from('notification_preferences')
          .upsert(
            {
              user_id: userId,
              push_enabled: next.push_enabled,
              coach_enabled: next.coach_enabled,
              workout_enabled: next.workout_enabled,
              gamification_enabled: next.gamification_enabled,
              health_enabled: next.health_enabled,
              system_enabled: next.system_enabled,
              quiet_hours_start: next.quiet_hours_start,
              quiet_hours_end: next.quiet_hours_end,
              timezone_offset: tzOffset,
            },
            { onConflict: 'user_id' }
          );
      }, 600);
    };

  Note: `handleChange({ push_enabled: false })` updates only that key but always sends the full next state. `ignoreDuplicates` is NOT set here (this is a real update, not a defaults init).

  For the JSX return, keep the existing loading state block verbatim (lines 82-91 in the current file — STHeader + ActivityIndicator). For the non-loading return, keep:
    - The SafeAreaView + STHeader wrapper verbatim
    - The ScrollView with `contentContainerStyle={{ padding: 16, paddingTop: 4, paddingBottom: 40 }}`
    - The `notifDenied` TouchableOpacity card verbatim (lines 97-127 of current file)
    - Add a placeholder `<View />` comment after the denied card — Plan 02 fills in the groups

  The JSX placeholder in the non-loading return (after the denied card) is:
    {/* TODO Plan 02: master switch, category toggles, quiet hours */}

  This ensures the file compiles after Plan 01 and Plan 02 replaces this comment with real groups.
  </action>

  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - `notification_preferences` appears in the file (grep confirms `supabase.from('notification_preferences')`)
    - `user_profiles` does NOT appear inside `NotifSubScreen` (the old read/write path is gone)
    - `push_enabled`, `coach_enabled`, `workout_enabled`, `gamification_enabled`, `health_enabled`, `system_enabled`, `quiet_hours_start`, `quiet_hours_end` all appear in the useState initial object
    - `ignoreDuplicates: true` appears exactly once (in the mount UPSERT)
    - `onConflict: 'user_id'` appears twice (once in mount UPSERT, once in save UPSERT)
    - `Math.round(-new Date().getTimezoneOffset() / 60)` appears twice (mount + save) — timezone_offset auto-detect per D-07
    - `setTimeout` with delay `600` appears in `handleChange` — confirming debounce per D-09
    - `clearTimeout(saveRef.current)` appears before the setTimeout call in `handleChange`
    - `startPickerVisible` and `endPickerVisible` state vars are declared
    - TypeScript compilation passes with no errors in settings.tsx
  </acceptance_criteria>

  <done>The NotifSubScreen data layer is complete: state shape matches notification_preferences columns, mount UPSERT initialises defaults then SELECT loads existing row, handleChange provides 600ms debounced full UPSERT with auto-detected timezone_offset. File compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → Supabase | User JWT used for all notification_preferences reads/writes; RLS enforces user_id = auth.uid() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | notification_preferences UPSERT | mitigate | RLS policy `notification_preferences_own` enforces `auth.uid() = user_id`; mobile sends user JWT via Supabase client |
| T-05-02 | Information Disclosure | SELECT notification_preferences | accept | User reads only their own row; RLS enforces this; no sensitive data exposed beyond the user's own preferences |
| T-05-SC | Tampering | npm installs | accept | No new packages in this phase; all imports (expo-notifications, @ziko/ui, supabase-js) already installed in Phase 1 |
</threat_model>

<verification>
After Plan 01 execution:
- `grep -c "notification_preferences" apps/mobile/app/(app)/profile/settings.tsx` returns ≥ 3 (mount upsert, mount select, save upsert)
- `grep -c "user_profiles" apps/mobile/app/(app)/profile/settings.tsx` returns the same count as before (the old profile queries in AppearanceSubScreen/IntegrationsSubScreen still exist; none inside NotifSubScreen)
- TypeScript compiles without errors
</verification>

<success_criteria>
- NotifSubScreen state has exactly 8 keys: push_enabled, coach_enabled, workout_enabled, gamification_enabled, health_enabled, system_enabled, quiet_hours_start, quiet_hours_end
- Mount sequence: UPSERT defaults (ignoreDuplicates=true) then SELECT — no spinner required if no row exists (defaults shown immediately)
- handleChange accepts Partial and sends full state in debounced UPSERT with timezone_offset
- No Save button, no toast, silent fire-and-forget persistence (per PREF-04)
- File compiles and existing tests pass (TypeScript clean)
</success_criteria>

<output>
Create `.planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-01-SUMMARY.md` when done.
</output>
