---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - apps/mobile/app/(app)/profile/settings.tsx
autonomous: true
requirements:
  - PREF-01
  - PREF-02
  - PREF-03
  - PREF-04

must_haves:
  truths:
    - "User can toggle push_enabled (master switch) and category rows are immediately disabled (opacity 0.4 + pointerEvents none) while quiet hours section is hidden"
    - "User can independently toggle 5 category rows: Coach (coach_enabled), Workout (workout_enabled), Gamification (gamification_enabled), Santé & Habitudes (health_enabled), App (system_enabled)"
    - "User can set quiet hours start and end via InlinePicker bottom sheets showing 24 items formatted as '0h00' through '23h00'"
    - "OS Settings CTA card appears at the top of the screen when push permission is denied and canAskAgain = false"
    - "No Save button exists; all changes persist via the 600ms debounce inherited from Plan 01's handleChange"
  artifacts:
    - path: "apps/mobile/app/(app)/profile/settings.tsx"
      provides: "Complete NotifSubScreen JSX: master switch, 5 category toggles, quiet hours pickers, OS Settings CTA"
      contains: "STGroup"
  key_links:
    - from: "STRow toggleValue prop (master switch)"
      to: "s.push_enabled"
      via: "handleChange({ push_enabled: v })"
      pattern: "push_enabled"
    - from: "InlinePicker onSelect (quiet hours start)"
      to: "s.quiet_hours_start"
      via: "handleChange({ quiet_hours_start: parseInt(id, 10) })"
      pattern: "quiet_hours_start"
    - from: "InlinePicker onSelect (quiet hours end)"
      to: "s.quiet_hours_end"
      via: "handleChange({ quiet_hours_end: parseInt(id, 10) })"
      pattern: "quiet_hours_end"
---

<objective>
Replace the JSX placeholder comment left by Plan 01 with the complete visual implementation of `NotifSubScreen`: master switch group, 5-category toggle group, conditional quiet hours group with two InlinePicker bottom sheets, and the OS Settings CTA card. All interaction logic calls `handleChange` from Plan 01 to trigger debounced auto-save.

Purpose: After this plan, all four PREF requirements are satisfied end-to-end: master switch (PREF-01), 5 category toggles (PREF-02), quiet hours (PREF-03), auto-save (PREF-04 already wired in Plan 01 — this plan ensures every toggle and picker calls handleChange correctly).

Output: A complete `NotifSubScreen` that matches the UI-SPEC.md screen layout contract exactly, including disabled states, conditional sections, and InlinePicker bottom sheets.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-mobile/ROADMAP.md
@.planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-CONTEXT.md
@.planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-UI-SPEC.md
@.planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement master switch group and 5 category toggle groups</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>

  <read_first>
    - apps/mobile/app/(app)/profile/settings.tsx — Read entirely after Plan 01 execution. Confirm: (a) NotifSubScreen contains the `{/* TODO Plan 02: master switch, category toggles, quiet hours */}` placeholder; (b) `handleChange`, `s.push_enabled`, `startPickerVisible`, `endPickerVisible` are all defined; (c) `InlinePicker` component is intact at lines ~164-218.
    - .planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-UI-SPEC.md — Screen Layout Contract section: exact top-to-bottom structure. Color section: category toggle tints. Interaction Contract: disabled state spec (opacity 0.4 + pointerEvents none on category row wrappers when push_enabled = false).
  </read_first>

  <action>
  Replace the `{/* TODO Plan 02: master switch, category toggles, quiet hours */}` placeholder in the non-loading return block of NotifSubScreen.

  Insert the following JSX after the notifDenied conditional card and before the closing `</ScrollView>`:

  GROUP 1 — Master switch (no title, per D-04 and UI-SPEC):
    STGroup with no title prop containing a single STRow:
    - icon: "notifications-outline"
    - tint: "#FF5C1A"
    - label: "Toutes les notifications"
    - toggleValue: {s.push_enabled}
    - onToggle: {(v) => handleChange({ push_enabled: v })}

  GROUP 2 — Category toggles (title="Catégories", per D-03 and PREF-02):
    Wrap all 5 STRow items in a single View with the disabled-state style wrapper.
    Disabled-state logic (per D-04): when `s.push_enabled === false`, apply `opacity: 0.4` and `pointerEvents: 'none'` on a wrapping View around the STGroup. This makes all 5 rows visually grayed and non-interactive while remaining visible. The structure is:

      <View style={{ opacity: s.push_enabled ? 1 : 0.4, pointerEvents: s.push_enabled ? 'auto' : 'none' }}>
        <STGroup title="Catégories">
          {5 STRow items}
        </STGroup>
      </View>

    The 5 STRow items in order (label, icon, tint, toggleValue, onToggle) — per D-03 column mapping:
    1. label="Coach", icon="person-outline", tint="#FF5C1A", toggleValue={s.coach_enabled}, onToggle={(v) => handleChange({ coach_enabled: v })}
    2. label="Workout", icon="barbell-outline", tint="#E94B3C", toggleValue={s.workout_enabled}, onToggle={(v) => handleChange({ workout_enabled: v })}
    3. label="Gamification", icon="trophy-outline", tint="#E8A33A", toggleValue={s.gamification_enabled}, onToggle={(v) => handleChange({ gamification_enabled: v })}
    4. label="Santé & Habitudes", icon="heart-outline", tint="#22C55E", toggleValue={s.health_enabled}, onToggle={(v) => handleChange({ health_enabled: v })}
    5. label="App", icon="phone-portrait-outline", tint="#6B6963", toggleValue={s.system_enabled}, onToggle={(v) => handleChange({ system_enabled: v })}

  Note: The pointerEvents value on the View must be typed as the React Native PointerEvents type. Use `pointerEvents={s.push_enabled ? 'auto' : 'none'}` as a JSX prop directly on the View (not inside a style object) to avoid TypeScript errors — React Native's View component accepts `pointerEvents` as a direct prop, not as a style property.
  </action>

  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - `STGroup` appears at least twice in NotifSubScreen JSX (master group + categories group)
    - `Catégories` appears as a title string in the file
    - All 5 category fields appear as toggleValue props: `s.coach_enabled`, `s.workout_enabled`, `s.gamification_enabled`, `s.health_enabled`, `s.system_enabled`
    - Each category row's onToggle calls `handleChange` with the correct key: `{ coach_enabled: v }`, `{ workout_enabled: v }`, `{ gamification_enabled: v }`, `{ health_enabled: v }`, `{ system_enabled: v }`
    - `opacity: s.push_enabled ? 1 : 0.4` appears on the category group wrapper View
    - `pointerEvents` prop appears on the category group wrapper View
    - `Toutes les notifications` appears as a label string in the master switch STRow
    - `s.push_enabled` is the toggleValue of the master switch STRow
    - TypeScript compiles without errors
  </acceptance_criteria>

  <done>Master switch and 5 category toggle groups are rendered with correct icons, tints, labels, column bindings, and disabled-state opacity/pointer logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement quiet hours pickers and remove the TODO placeholder</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>

  <read_first>
    - apps/mobile/app/(app)/profile/settings.tsx — Read after Task 1. Confirm the file compiles and the categories group is in place. Locate `startPickerVisible` and `endPickerVisible` state vars. Confirm InlinePicker component signature: props are `visible`, `items`, `selectedId`, `onSelect`, `onClose`, `theme`.
    - .planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-UI-SPEC.md — Quiet hours picker row specs: "De" label for start, "À" label for end; right text format: `"${hour}h00"` (e.g. "22h00", "7h00"); 24 items (id = string of integer 0-23, label = "0h00" through "23h00"); paddingVertical: 12 on InlinePicker rows (already in InlinePicker component — no override needed). Interaction Contract section for mount sequence.
  </read_first>

  <action>
  Perform two additions to the NotifSubScreen JSX:

  ADDITION 1 — Quiet hours group (conditionally rendered after the categories View, per D-06):

  The quiet hours STGroup is only rendered when `s.push_enabled === true`:

    {s.push_enabled && (
      <STGroup title="Heures silencieuses">
        <STRow
          icon="moon-outline"
          tint="#6B6963"
          label="De"
          right={`${s.quiet_hours_start}h00`}
          onPress={() => setStartPickerVisible(true)}
        />
        <STRow
          icon="sunny-outline"
          tint="#E8A33A"
          label="À"
          right={`${s.quiet_hours_end}h00`}
          onPress={() => setEndPickerVisible(true)}
        />
      </STGroup>
    )}

  ADDITION 2 — Two InlinePicker modals placed after the ScrollView closing tag but still inside the SafeAreaView (same pattern as AppearanceSubScreen):

  Build the HOUR_ITEMS array. It must be defined inside NotifSubScreen (not as a module-level const, to avoid any module-scope initialization issues with the 24-item array):

    const HOUR_ITEMS = Array.from({ length: 24 }, (_, i) => ({
      id: String(i),
      label: `${i}h00`,
    }));

  Place this `const HOUR_ITEMS` declaration inside the NotifSubScreen function, after the state declarations and before the useEffect hooks (or directly before the return statement — either location is fine as long as it is inside the function body).

  Then add the two InlinePicker instances after `</ScrollView>` and before `</SafeAreaView>`:

    <InlinePicker
      visible={startPickerVisible}
      items={HOUR_ITEMS}
      selectedId={String(s.quiet_hours_start)}
      onSelect={(id) => {
        handleChange({ quiet_hours_start: parseInt(id, 10) });
        setStartPickerVisible(false);
      }}
      onClose={() => setStartPickerVisible(false)}
      theme={theme}
    />
    <InlinePicker
      visible={endPickerVisible}
      items={HOUR_ITEMS}
      selectedId={String(s.quiet_hours_end)}
      onSelect={(id) => {
        handleChange({ quiet_hours_end: parseInt(id, 10) });
        setEndPickerVisible(false);
      }}
      onClose={() => setEndPickerVisible(false)}
      theme={theme}
    />

  After inserting both additions, confirm the `{/* TODO Plan 02: master switch, category toggles, quiet hours */}` placeholder comment no longer exists in the file.

  Final check: The complete NotifSubScreen non-loading return structure from top to bottom must be:
  1. SafeAreaView (flex: 1, backgroundColor: theme.background)
  2.   STHeader (onBack, title="Notifications")
  3.   ScrollView (padding: 16, paddingTop: 4, paddingBottom: 40)
  4.     {notifDenied && (TouchableOpacity OS Settings CTA card)}
  5.     STGroup (no title) → STRow master switch "Toutes les notifications"
  6.     View (opacity/pointerEvents) → STGroup title="Catégories" → 5 STRows
  7.     {s.push_enabled && STGroup title="Heures silencieuses" → 2 STRows}
  8.   </ScrollView>
  9.   InlinePicker (startPickerVisible, HOUR_ITEMS, s.quiet_hours_start)
  10.  InlinePicker (endPickerVisible, HOUR_ITEMS, s.quiet_hours_end)
  11. </SafeAreaView>
  </action>

  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - `Heures silencieuses` appears as a STGroup title string in the file
    - `s.push_enabled &&` conditional appears wrapping the quiet hours STGroup (confirming it is hidden when master is OFF, per D-06)
    - `De` and `À` appear as STRow label strings inside the quiet hours section
    - `${s.quiet_hours_start}h00` and `${s.quiet_hours_end}h00` appear as right prop values
    - `HOUR_ITEMS` is defined inside NotifSubScreen function body with `Array.from({ length: 24 }` producing 24 items
    - `parseInt(id, 10)` appears twice in InlinePicker onSelect handlers (once for start, once for end)
    - `startPickerVisible` and `endPickerVisible` are passed as `visible` prop to the two InlinePicker instances respectively
    - `setStartPickerVisible(false)` appears both in `onSelect` and `onClose` of the start picker
    - `setEndPickerVisible(false)` appears both in `onSelect` and `onClose` of the end picker
    - The `TODO Plan 02` placeholder comment does NOT appear in the final file
    - TypeScript compiles without errors in settings.tsx
    - No `user_profiles` query exists anywhere inside NotifSubScreen (grep confirms the old data path is fully gone)
  </acceptance_criteria>

  <done>Quiet hours section is conditionally rendered when push_enabled = true; two InlinePicker modals open on "De"/"À" row press; selection calls handleChange with parsed integer hour and closes the picker; HOUR_ITEMS provides 24 entries "0h00"–"23h00". Phase 5 UI is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → Supabase | User JWT for all notification_preferences writes; RLS enforces user_id = auth.uid() |
| mobile client → OS | Linking.openSettings() — no data passed, OS handles permission UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | InlinePicker onSelect — quiet hours | accept | parseInt input is from a controlled array of string integers "0"–"23"; no user-typed value; no injection surface |
| T-05-04 | Denial of Service | 600ms debounce on rapid toggles | accept | debounce is enforced via clearTimeout + setTimeout; rapid changes only produce one UPSERT per 600ms window |
| T-05-SC | Tampering | npm/pip installs | accept | No new packages in this plan; all dependencies already installed |
</threat_model>

<verification>
After Plan 02 execution:

1. TypeScript clean: `rtk tsc --noEmit -p apps/mobile/tsconfig.json` — zero errors
2. Structure assertion: grep the file for each of these strings (each must appear):
   - `notification_preferences` (≥ 3 occurrences: mount UPSERT, mount SELECT, save UPSERT)
   - `Toutes les notifications` (master switch label)
   - `Catégories` (category group title)
   - `Heures silencieuses` (quiet hours group title)
   - `push_enabled` (master switch toggleValue binding)
   - `coach_enabled` (category toggle binding)
   - `health_enabled` (category toggle binding — "Santé & Habitudes")
   - `system_enabled` (category toggle binding — "App")
   - `quiet_hours_start` (state key + InlinePicker selectedId)
   - `quiet_hours_end` (state key + InlinePicker selectedId)
   - `ignoreDuplicates: true` (mount UPSERT only)
   - `HOUR_ITEMS` (24-item array for pickers)
   - `Réactiver les notifications` (OS Settings CTA title — retained from old implementation)
3. Absent strings in NotifSubScreen: `sessionsReminder`, `hydration`, `streakAlert`, `achievements`, `social`, `marketing`, `sound`, `haptics` — none of these must appear inside NotifSubScreen
</verification>

<success_criteria>
1. Master switch OFF: category rows at opacity 0.4 + non-interactive via pointerEvents none; quiet hours section not rendered
2. Master switch ON: category rows at opacity 1.0 + interactive; quiet hours section rendered
3. 5 category toggles independently toggle coach_enabled, workout_enabled, gamification_enabled, health_enabled, system_enabled
4. Quiet hours "De" row shows current start hour as "Xh00"; tap opens start InlinePicker; selection saves immediately via handleChange
5. Quiet hours "À" row shows current end hour as "Xh00"; tap opens end InlinePicker; selection saves immediately via handleChange
6. OS Settings CTA card (Réactiver les notifications) appears at top when push permission is denied + canAskAgain = false
7. No Save button anywhere in the screen
8. TypeScript clean, no regressions in other sub-screens
</success_criteria>

<output>
Create `.planning/workstreams/notification-mobile/phases/05-notification-preferences-ui/05-02-SUMMARY.md` when done.
</output>
