---
phase: 06-local-reminders-app-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/062_workout_reminder_prefs.sql
autonomous: true
requirements:
  - LOCAL-02

must_haves:
  truths:
    - "Migration file 062_workout_reminder_prefs.sql exists in supabase/migrations/"
    - "notification_preferences table has workout_reminder_days JSONB column defaulting to '[]'"
    - "notification_preferences table has workout_reminder_time TEXT column defaulting to NULL"
    - "supabase db push executes without error"
  artifacts:
    - path: "supabase/migrations/062_workout_reminder_prefs.sql"
      provides: "Two new columns on notification_preferences"
      contains: "workout_reminder_days"
  key_links:
    - from: "supabase/migrations/062_workout_reminder_prefs.sql"
      to: "notification_preferences table"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "workout_reminder_days|workout_reminder_time"
---

<objective>
Create migration 062 that adds `workout_reminder_days` and `workout_reminder_time` columns to `notification_preferences`, then push it to Supabase so the schema is ready before any code writes to those columns.

Purpose: Plans 06-02 and 06-03 both read and write these columns. If the migration is not applied first, the UPSERT in workout/[id].tsx will fail at runtime with a Supabase column-not-found error.

Output: `supabase/migrations/062_workout_reminder_prefs.sql` and the schema live in Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-mobile/ROADMAP.md
@.planning/workstreams/notification-mobile/phases/06-local-reminders-app-updates/06-CONTEXT.md
@.planning/workstreams/notification-mobile/phases/06-local-reminders-app-updates/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 062_workout_reminder_prefs.sql</name>
  <files>supabase/migrations/062_workout_reminder_prefs.sql</files>
  <read_first>
    - supabase/migrations/054_notification_schema.sql — confirms current notification_preferences columns (push_enabled, coach_enabled, workout_enabled, gamification_enabled, health_enabled, system_enabled, type_prefs JSONB, quiet_hours_start INTEGER, quiet_hours_end INTEGER, timezone_offset INTEGER, updated_at). The new migration must NOT redefine any of these.
    - Run `ls supabase/migrations/` to confirm 062 is the next available number (highest currently is 061_coach_read_client_program_workouts.sql; 055 is taken twice — do NOT use 055).
  </read_first>
  <action>
    Create `supabase/migrations/062_workout_reminder_prefs.sql` with the following exact content:

    A comment block explaining the migration purpose (workout reminder day/time preferences), then a single ALTER TABLE statement using IF NOT EXISTS to add both columns atomically:

    - Column 1: `workout_reminder_days JSONB NOT NULL DEFAULT '[]'` — stores lowercase weekday strings as a JSON array, e.g. ["monday","wednesday","friday"]. NOT NULL with DEFAULT '[]' so existing rows get an empty array rather than NULL.
    - Column 2: `workout_reminder_time TEXT DEFAULT NULL` — stores time in "HH:MM" format, e.g. "07:30". NULL means no workout reminder is configured.

    No RLS changes are needed — the existing `notification_preferences_own` policy from migration 054 (auth.uid() = user_id) covers the new columns automatically.

    Do NOT add an updated_at trigger — the column already exists and the application sets it explicitly via UPSERT.
  </action>
  <verify>
    <automated>grep -c "workout_reminder_days" supabase/migrations/062_workout_reminder_prefs.sql</automated>
  </verify>
  <done>File exists at supabase/migrations/062_workout_reminder_prefs.sql and contains both "workout_reminder_days" and "workout_reminder_time" column definitions inside an ALTER TABLE statement.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push migration 062 to Supabase</name>
  <read_first>
    - supabase/migrations/062_workout_reminder_prefs.sql (just created in Task 1)
  </read_first>
  <what-built>Migration file 062_workout_reminder_prefs.sql is ready with the two new columns for workout_reminder_days and workout_reminder_time.</what-built>
  <how-to-verify>
    Run the following command from the project root and confirm it succeeds with no errors:

    ```
    supabase db push
    ```

    After push completes, optionally confirm in Supabase Dashboard (Table Editor → notification_preferences) that the two new columns appear:
    - workout_reminder_days (JSONB, default [])
    - workout_reminder_time (text, nullable)
  </how-to-verify>
  <resume-signal>Type "pushed" after supabase db push succeeds, or describe the error if it fails.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration script → Supabase | DDL runs with migration privileges; ADD COLUMN IF NOT EXISTS is idempotent and safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | migration file creation | mitigate | IF NOT EXISTS guard prevents duplicate column errors if migration is rerun |
| T-06-02 | Elevation of Privilege | notification_preferences columns | accept | Existing RLS policy notification_preferences_own (auth.uid() = user_id) covers new columns automatically — no additional policy needed |
</threat_model>

<verification>
After both tasks complete:
- `supabase/migrations/062_workout_reminder_prefs.sql` exists and grep for "workout_reminder_days" returns count > 0
- `supabase db push` exited 0 (no error)
- Plans 06-02 and 06-03 can proceed — the columns are live in Supabase
</verification>

<success_criteria>
Migration 062 is applied: notification_preferences table has workout_reminder_days (JSONB NOT NULL DEFAULT '[]') and workout_reminder_time (TEXT DEFAULT NULL) columns live in Supabase. Code in wave 2 plans can write to these columns without runtime errors.
</success_criteria>

<output>
Create `.planning/workstreams/notification-mobile/phases/06-local-reminders-app-updates/06-01-SUMMARY.md` when done
</output>
