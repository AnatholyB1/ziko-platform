---
phase: "04"
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/web/src/hooks/useCoachMemory.ts
  - apps/web/src/components/coach/dashboard/TemplateCard.tsx
  - apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx
autonomous: true
requirements: [MEM-01]

must_haves:
  truths:
    - "useCoachMemory hook returns { preferences, templates, recent_actions } via TanStack Query with staleTime 5min"
    - "TemplateNamingModal renders all 5 states (A1–A5) per UI-SPEC: empty, valid, loading, duplicate error, success"
    - "TemplateCard renders badge, name, widget summary, date, and 'Utiliser ce modèle' button per UI-SPEC"
    - "GSAP modal entrance (fromTo, scale 0.97→1, y 8→0, 200ms power2.out) and exit (to, scale 0.97, y 4, 150ms power2.in) fire correctly"
    - "Duplicate name check is client-side using cached memory data (no extra API round-trip)"
    - "Successful save calls PUT /api/coach/dashboards/memory with { templates: [...existing, newTemplate] } and shows 'Modèle enregistré' toast"
  artifacts:
    - path: apps/web/src/hooks/useCoachMemory.ts
      provides: "TanStack Query hook returning CoachMemoryData"
      exports: ["useCoachMemory"]
    - path: apps/web/src/components/coach/dashboard/TemplateCard.tsx
      provides: "Individual template card component"
      exports: ["TemplateCard"]
    - path: apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx
      provides: "Controlled modal for saving the current dashboard as a named template"
      exports: ["TemplateNamingModal"]
  key_links:
    - from: apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx
      to: /api/coach/dashboards/memory
      via: "PUT fetch on save button click"
      pattern: "fetch.*memory.*PUT"
    - from: apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx
      to: apps/web/src/hooks/useCoachMemory.ts
      via: "useCoachMemory() for duplicate check"
      pattern: "useCoachMemory"
---

<objective>
Create the useCoachMemory hook, TemplateCard component, and TemplateNamingModal component.

Purpose: These three files are the shared data layer and core UI primitives for MEM-01. useCoachMemory is consumed by TemplateNamingModal (duplicate check), TemplatePicker (list templates), and EditChatPanel (personalized opening message). TemplateNamingModal is the primary MEM-01 interaction surface where coaches save the current dashboard as a named template.

Output: Three new files — hook, card, modal — all TypeScript-clean, GSAP-animated per UI-SPEC contracts, accessible per UI-SPEC accessibility requirements.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/workstreams/custom-widget/ROADMAP.md
@C:/ziko-platform/.planning/workstreams/custom-widget/phases/04-polish-coach-memory/04-UI-SPEC.md
</context>

<interfaces>
<!-- useDashboardConfig.ts — pattern to replicate for useCoachMemory: -->
<!-- import { useQuery, UseQueryResult } from '@tanstack/react-query' -->
<!-- import { createBrowserClient } from '@supabase/ssr' -->
<!-- Uses session.access_token for Bearer auth to NEXT_PUBLIC_API_URL -->
<!-- Query key: ['dashboard-config', clientId] → for memory: ['coach-memory'] (no clientId param) -->
<!-- Fetches: ${process.env.NEXT_PUBLIC_API_URL}/coach/dashboards/${clientId} -->
<!-- For memory: ${process.env.NEXT_PUBLIC_API_URL}/coach/dashboards/memory -->

<!-- CoachMemoryData shape (from types.ts after Plan 01 update): -->
<!-- {
  preferences: {
    preferred_period?: '7d' | '30d' | '90d',
    preferred_widgets?: string[],
    preferred_chart_type?: 'line' | 'bar'
  },
  templates: Array<{
    id: string,
    name: string,
    widgets: Widget[],
    created_at: string
  }>,
  recent_actions: string[]
} -->

<!-- Widget type display names for TemplateCard summary (from UI-SPEC Copywriting section): -->
<!-- line_chart → not in the 7 types; actual widget types are:
  sessions_summary → 'Séances'     (NOTE: UI-SPEC uses 'sessions_summary' but Widget type is from dashboard.ts)
  sleep_chart → 'Sommeil'
  mood_trend → 'Humeur'
  weight_progression → 'Poids'
  nutrition_macros → 'Nutrition'
  cardio_stats → 'Cardio'
  habits_streak → 'Habitudes'
  HOWEVER — apps/web/src/types/dashboard.ts defines WidgetType as:
  'line_chart' | 'bar_chart' | 'kpi_tile' | 'table' | 'athlete_list' | 'threshold_indicator' | 'callout'
  The UI-SPEC display names use semantic names (weight_progression, etc.) but the codebase widget types
  are structural (line_chart, kpi_tile, etc.). For TemplateCard summary, use widget.title (the human-readable
  title string on each widget) rather than mapping type enums — this avoids the mismatch. -->

<!-- DashboardEditOverlay.tsx — SaveToast usage (for reuse in TemplateNamingModal success): -->
<!-- import { SaveToast } from './SaveToast' -->
<!-- <SaveToast onUndo={handleUndo} onDismiss={() => setToastVisible(false)} /> -->
<!-- SaveToast has: fixed bottom-right, green left border, GSAP slide-in from y:16 -->
<!-- For "Modèle enregistré" we need a SaveToast without Undo — use the same component but -->
<!-- pass onUndo={undefined} and customize the label via a prop, OR just show a simple toast. -->
<!-- Per UI-SPEC, the success toast is identical spec to Phase 03 SaveToast — same component. -->
<!-- Simplest: TemplateNamingModal calls its parent's onSuccess() which triggers the SaveToast -->
<!-- at DashboardEditOverlay level with different copy. -->

<!-- GSAP contracts verbatim from UI-SPEC Motion Design section 1 and 2: -->
<!-- Entrance: gsap.fromTo(modalRef.current, { opacity: 0, scale: 0.97, y: 8 }, { opacity: 1, scale: 1, y: 0, duration: 0.2, ease: 'power2.out' }) -->
<!-- Exit (cancel or save): gsap.to(modalRef.current, { opacity: 0, scale: 0.97, y: 4, duration: 0.15, ease: 'power2.in', onComplete: () => setIsModalOpen(false) }) -->
<!-- The exit callback calls setIsModalOpen(false) which lives in DashboardEditOverlay — -->
<!-- expose via onClose prop that TemplateNamingModal calls through the exit onComplete -->

<!-- "Enregistrer comme modèle" button press feedback (UI-SPEC Motion Design section 7): -->
<!-- gsap.to(saveAsTemplateBtnRef.current, { scale: 0.97, duration: 0.1, yoyo: true, repeat: 1, ease: 'power3.out' }) -->
<!-- This lives in DashboardEditOverlay — the button press feedback is on the trigger button, not in the modal -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create useCoachMemory hook + TemplateCard component</name>
  <files>
    apps/web/src/hooks/useCoachMemory.ts
    apps/web/src/components/coach/dashboard/TemplateCard.tsx
  </files>
  <read_first>
    - apps/web/src/hooks/useDashboardConfig.ts (full file — pattern to replicate for auth + fetch + TanStack Query)
    - apps/web/src/types/dashboard.ts (Widget type — TemplateCard needs to render widget.title for summary)
    - apps/web/src/components/coach/dashboard/DashboardEditOverlay.tsx (lines 1-10 — import patterns, existing component style)
    - apps/web/src/components/coach/dashboard/SaveToast.tsx (understand its props — TemplateNamingModal will need to show a similar toast)
  </read_first>
  <action>
    **Create apps/web/src/hooks/useCoachMemory.ts:**

    Define a `CoachMemoryTemplate` interface: `{ id: string; name: string; widgets: Widget[]; created_at: string }`.
    Define a `CoachMemoryPreferences` interface: `{ preferred_period?: string; preferred_widgets?: string[]; preferred_chart_type?: string }`.
    Define a `CoachMemoryData` interface: `{ preferences: CoachMemoryPreferences; templates: CoachMemoryTemplate[]; recent_actions: string[] }`.

    Export `useCoachMemory()` function using `useQuery` from `@tanstack/react-query`:
    - `queryKey: ['coach-memory']`
    - `queryFn`: create browser Supabase client (same pattern as useDashboardConfig.ts), get session, fetch `${process.env.NEXT_PUBLIC_API_URL}/coach/dashboards/memory` with Bearer token. On 404, return empty defaults `{ preferences: {}, templates: [], recent_actions: [] }` (do NOT throw). On other errors, throw.
    - `staleTime: 5 * 60 * 1000` (5 minutes per UI-SPEC Implementation Notes)
    - `retry: 1`
    - Return type: `UseQueryResult<CoachMemoryData>`

    Also export the `CoachMemoryTemplate`, `CoachMemoryPreferences`, and `CoachMemoryData` interfaces for use in other components.

    **Create apps/web/src/components/coach/dashboard/TemplateCard.tsx:**

    Props: `{ template: CoachMemoryTemplate; onApply: (template: CoachMemoryTemplate) => void; isApplying?: boolean; isOtherApplying?: boolean }`

    Render per UI-SPEC Layout Specs section 3 TemplateCard layout:
    - Container: `position: relative`, `background: #FFFFFF`, `border: 1px solid #E2E0DA`, `borderRadius: 8`, `padding: 20`, `shadow: 0 4px 8px rgba(28,26,23,0.08)`, `minHeight: 160`, `cursor: pointer`, `transition: border-color 150ms ease, box-shadow 150ms ease` (CSS via style prop or className)
    - Hover state: on mouseEnter set border to `#FF5C1A`, shadow to `0 8px 24px rgba(28,26,23,0.10)` — manage via `useState(false)` for hovered
    - Widget count badge: `bg-[#F0EFE9] text-[#1C1A17] rounded-sm px-2 py-0.5 text-[14px] font-semibold` — content: `${n} widget${n !== 1 ? 's' : ''}`
    - Template name: `fontSize: 18, fontWeight: 600, color: '#1C1A17'`, `mt-3`, `overflow: hidden, whiteSpace: nowrap, textOverflow: ellipsis` (single line)
    - Widget summary: comma-separated `template.widgets.map(w => w.title).join(', ')` — max 2 lines via `-webkit-line-clamp: 2`, `fontSize: 14, color: '#6B6963'`, `mt-2`
    - Divider: `borderTop: 1px solid #E2E0DA`, `paddingTop: 12, marginTop: 12`
    - Creation date: `"Créé le " + format(new Date(template.created_at), 'dd/MM/yyyy')` — format using vanilla JS `toLocaleDateString('fr-FR', { day: '2-digit', month: '2-digit', year: 'numeric' })` (no date-fns needed), `fontSize: 12, color: '#6B6963'`
    - "Utiliser ce modèle" button: `aria-label={"Utiliser le modèle " + template.name}` (per UI-SPEC accessibility), `w-full h-9 mt-3 bg-[#FF5C1A] text-white rounded-md text-[14px] font-semibold`; when `isApplying`: show spinner (16px CSS spin) + `disabled`; when `isOtherApplying`: `opacity: 0.5`
    - GSAP press feedback on button click (before onApply call, per UI-SPEC Motion section 6): `const useTemplateBtnRef = useRef<HTMLButtonElement>(null)` then `gsap.to(useTemplateBtnRef.current, { scale: 0.96, duration: 0.1, yoyo: true, repeat: 1, ease: 'power3.out' })` — then call `onApply(template)` after a 0ms timeout so GSAP has time to start
  </action>
  <verify>
    <automated>cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `apps/web/src/hooks/useCoachMemory.ts` exists and exports `useCoachMemory`, `CoachMemoryTemplate`, `CoachMemoryData`
    - `apps/web/src/components/coach/dashboard/TemplateCard.tsx` exists and exports `TemplateCard`
    - `tsc --noEmit` in apps/web exits 0
    - TemplateCard renders widget count badge, name, widget summary (from widget.title), creation date, "Utiliser ce modèle" button
  </done>
  <acceptance_criteria>
    - `grep "export function useCoachMemory" apps/web/src/hooks/useCoachMemory.ts` returns 1 match
    - `grep "export.*CoachMemoryTemplate\|export.*CoachMemoryData" apps/web/src/hooks/useCoachMemory.ts` returns at least 2 matches
    - `grep "staleTime.*5.*60.*1000\|staleTime.*300000" apps/web/src/hooks/useCoachMemory.ts` returns 1 match
    - `grep "coach-memory" apps/web/src/hooks/useCoachMemory.ts` returns 1 match (queryKey)
    - `grep "export function TemplateCard\|export default TemplateCard" apps/web/src/components/coach/dashboard/TemplateCard.tsx` returns 1 match
    - `grep "Utiliser ce modèle" apps/web/src/components/coach/dashboard/TemplateCard.tsx` returns 1 match
    - `grep "Créé le" apps/web/src/components/coach/dashboard/TemplateCard.tsx` returns 1 match
    - `grep "power3.out" apps/web/src/components/coach/dashboard/TemplateCard.tsx` returns 1 match (GSAP press feedback)
    - `cd C:/ziko-platform/apps/web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create TemplateNamingModal component</name>
  <files>
    apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx
  </files>
  <read_first>
    - apps/web/src/components/coach/dashboard/DashboardEditOverlay.tsx (full file — understand existing props, configRef, how SaveToast is used, existing button style conventions)
    - apps/web/src/components/coach/dashboard/SaveToast.tsx (full file — understand existing props so TemplateNamingModal can trigger the same toast via parent callback)
    - apps/web/src/hooks/useCoachMemory.ts (just created — check CoachMemoryTemplate interface export and useCoachMemory hook signature)
    - apps/web/src/types/dashboard.ts (Widget type — TemplateNamingModal receives currentWidgets to include in the new template)
  </read_first>
  <action>
    Create apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx as a 'use client' component.

    **Props interface `TemplateNamingModalProps`:**
    - `currentWidgets: Widget[]` — the pending dashboard widgets to save as a template
    - `onClose: () => void` — called via GSAP exit onComplete
    - `onSuccess: () => void` — called after successful save (triggers SaveToast in parent)

    **Internal state:**
    - `templateName: string` — controlled input value
    - `error: string | null` — validation error message
    - `isSaving: boolean` — loading state during PUT request

    **Refs:**
    - `modalRef = useRef<HTMLDivElement>(null)` — for GSAP animations
    - `inputRef = useRef<HTMLInputElement>(null)` — for focus management

    **useCoachMemory:** `const { data: memory } = useCoachMemory()` — provides existing templates for client-side duplicate check.

    **GSAP entrance** (useEffect on mount, per UI-SPEC Motion section 1):
    ```
    gsap.fromTo(modalRef.current,
      { opacity: 0, scale: 0.97, y: 8 },
      { opacity: 1, scale: 1, y: 0, duration: 0.2, ease: 'power2.out' }
    )
    ```

    **handleClose function** (triggers exit animation, per UI-SPEC Motion section 2):
    ```
    gsap.to(modalRef.current, {
      opacity: 0, scale: 0.97, y: 4,
      duration: 0.15, ease: 'power2.in',
      onComplete: () => onClose()
    })
    ```

    **handleSave async function:**
    1. Trim the name. If empty, return (button is disabled anyway).
    2. Client-side duplicate check: `const isDuplicate = memory?.templates.some(t => t.name === templateName.trim())` → if true, `setError('Ce nom est déjà utilisé. Choisissez un nom différent.')` then focus input and return.
    3. `setIsSaving(true)`, `setError(null)`
    4. Build `newTemplate: CoachMemoryTemplate = { id: crypto.randomUUID(), name: templateName.trim(), widgets: currentWidgets, created_at: new Date().toISOString() }`
    5. `PUT /api/coach/dashboards/memory` with body `{ templates: [...(memory?.templates ?? []), newTemplate] }`
    6. On success (res.ok): call GSAP exit → `onSuccess()` (triggers toast in parent), then `onClose()` via GSAP onComplete
    7. On 409: `setError('Ce nom est déjà utilisé. Choisissez un nom différent.')`, focus input
    8. On 5xx: `setError('Erreur lors de l\'enregistrement. Réessayer ?')`
    9. Always: `setIsSaving(false)` in finally

    **Keyboard handler:** `onKeyDown` on modal backdrop `div`: `if (e.key === 'Escape' && !isSaving) handleClose()`

    **Input Enter key:** `onKeyDown` on input: `if (e.key === 'Enter' && templateName.trim() && !isSaving) handleSave()`

    **autoFocus:** input receives `autoFocus` or use `useEffect(() => inputRef.current?.focus(), [])`.

    **Layout per UI-SPEC Layout Specs section 2:**
    - Backdrop: `fixed inset-0 bg-black/40 z-40` — `<div onClick={(e) => { if (e.target === e.currentTarget && !isSaving) handleClose() }}`
    - Modal container: `fixed top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 z-50 w-[480px] bg-white rounded-lg` with `boxShadow: '0 8px 24px rgba(28,26,23,0.10)'`, `role="dialog" aria-modal="true" aria-labelledby="template-modal-title"`
    - Header (px-6 pt-6 pb-5, border-bottom `1px solid #E2E0DA`):
      - Title: `<h2 id="template-modal-title">Enregistrer comme modèle</h2>` — `fontSize: 22, fontWeight: 600, color: '#1C1A17'`
      - Body text: `"Ce modèle sera disponible lorsque vous créerez un dashboard pour un autre athlète."` — `fontSize: 14, color: '#6B6963'`, mt-2
    - Content (px-6, pt-5, pb-4):
      - Label: `<label htmlFor="template-name-input">Nom du modèle</label>` — `fontSize: 14, fontWeight: 600, color: '#1C1A17'`, mb-2
      - Input: `id="template-name-input"`, `aria-describedby={error ? 'template-name-error' : undefined}`, `aria-invalid={!!error}`, `maxLength={60}`, height 40px, padding `10px 12px`, background `#F0EFE9` (idle) / `#FFFFFF` (focus), border `1px solid ${error ? '#EF4444' : focused ? '#FF5C1A' : '#E2E0DA'}`, borderRadius 8, `fontSize: 14, color: '#1C1A17'`, placeholder `"Ex. : Programme force 4j"`, `readOnly={isSaving}`, opacity on readOnly: 0.6
      - Character counter: `${templateName.length}/60` — right-aligned, `fontSize: 12, color: '#6B6963'`
      - Error text: `id="template-name-error"` — `fontSize: 14, color: '#EF4444'`, mt-1, rendered only when `error !== null`
    - Footer (px-6 pb-6, pt-5, border-top `1px solid #E2E0DA`, flex justify-end gap-2):
      - "Annuler" button: `height: 36, paddingLeft: 16, paddingRight: 16, border: '1px solid #E2E0DA', borderRadius: 8, background: 'transparent', fontSize: 14, fontWeight: 600, color: '#1C1A17'`, hover bg `#F0EFE9`, disabled when `isSaving` (opacity 0.4)
      - "Enregistrer le modèle" button: primary filled — `height: 36, paddingLeft: 16, paddingRight: 16, background: '#FF5C1A', borderRadius: 8, border: 'none', fontSize: 14, fontWeight: 600, color: '#FFFFFF'`, disabled when `!templateName.trim() || isSaving` (opacity 0.4, cursor not-allowed), `aria-busy={isSaving}`, `aria-disabled={!templateName.trim() || isSaving}` — when isSaving: render spinner (16px, white, `border: 2px solid rgba(255,255,255,0.4); borderTopColor: #FFFFFF; animation: spin 0.7s linear infinite`) instead of text

    Add `@keyframes spin { to { transform: rotate(360deg) } }` in a `<style>` tag (same pattern as DashboardEditOverlay.tsx).
  </action>
  <verify>
    <automated>cd C:/ziko-platform/apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` exists and exports `TemplateNamingModal`
    - All 5 UI states (A1-A5) are implemented: empty (button disabled), valid (button enabled), loading (spinner), duplicate error (red error text), success (closes + fires onSuccess)
    - GSAP entrance and exit match UI-SPEC Motion contracts exactly (fromTo entrance, to exit with onComplete)
    - Modal has correct ARIA: `role="dialog" aria-modal="true" aria-labelledby="template-modal-title"`, input has `htmlFor` + `aria-describedby` + `aria-invalid`
    - `tsc --noEmit` exits 0
  </done>
  <acceptance_criteria>
    - `grep "export function TemplateNamingModal\|export default TemplateNamingModal" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns 1 match
    - `grep "Enregistrer comme modèle" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns at least 1 match (modal title)
    - `grep "Enregistrer le modèle" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns at least 1 match (save button label)
    - `grep "Ce nom est déjà utilisé" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns 1 match
    - `grep "Ce modèle sera disponible" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns 1 match
    - `grep "fromTo.*modalRef\|gsap\.fromTo" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns 1 match (GSAP entrance)
    - `grep "power2\.in.*onComplete\|onComplete.*power2\.in" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns 1 match (GSAP exit)
    - `grep "template-modal-title" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns at least 2 matches (id on h2 + aria-labelledby on dialog)
    - `grep "maxLength.*60\|maxLength={60}" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns 1 match
    - `grep "crypto\.randomUUID" apps/web/src/components/coach/dashboard/TemplateNamingModal.tsx` returns 1 match (template ID generation)
    - `cd C:/ziko-platform/apps/web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (TemplateNamingModal PUT) | Coach sends new template array to memory endpoint; validated at API layer in Plan 01 |
| client-side duplicate check | Relies on TanStack Query cache being fresh (5min staleTime) — race condition is low risk since memory data only changes from this coach's browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | TemplateNamingModal — currentWidgets prop | accept | The modal receives currentWidgets from DashboardEditOverlay which already validated widgets via Zod in Plan 01's PUT handler; client-side crafting is coach's own data |
| T-04-07 | Spoofing | useCoachMemory — auth token | mitigate | Uses same Supabase session pattern as useDashboardConfig — session.access_token from browser Supabase client; no new auth surface |
| T-04-08 | Tampering | Template name input — XSS via name string | mitigate | React renders name via JSX text nodes (not dangerouslySetInnerHTML), so XSS is prevented by React's default escaping |
| T-04-09 | Denial of Service | TemplateNamingModal — save button double-click | mitigate | `isSaving` state gates the button (disabled + aria-disabled); second click ignored |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `cd C:/ziko-platform/apps/web && npx tsc --noEmit` — 0 errors
2. `grep -r "useCoachMemory" apps/web/src/` returns 1 definition + at least 1 usage (in TemplateNamingModal)
3. TemplateNamingModal: verify all 5 state transitions via code inspection: A1 (empty), A2 (has text), A3 (isSaving), A4 (error text visible), A5 (onSuccess call)
4. TemplateCard: verify GSAP press feedback ref and motion contract present
</verification>

<success_criteria>
- useCoachMemory.ts: exports hook + types, correct queryKey ['coach-memory'], staleTime 5min, 404 returns empty defaults
- TemplateCard.tsx: renders all card elements, GSAP press feedback on apply button, hover state via useState
- TemplateNamingModal.tsx: all 5 states implemented, GSAP entrance fromTo + exit to with onComplete, accessibility per UI-SPEC, French copy matches UI-SPEC exactly
- TypeScript: 0 errors in apps/web after all 3 files created
</success_criteria>

<output>
Create `.planning/workstreams/custom-widget/phases/04-polish-coach-memory/04-02-SUMMARY.md` when done.
</output>
