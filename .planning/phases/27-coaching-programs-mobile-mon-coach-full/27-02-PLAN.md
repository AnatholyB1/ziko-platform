---
phase: 27
plan: 02
type: execute
wave: 1
depends_on:
  - 27-01
files_modified:
  - supabase/migrations/045_coaching_programs_schema.sql
autonomous: false
requirements:
  - PROG-01
  - PROG-04
  - PROG-06
  - PROG-07
  - PROG-09

must_haves:
  truths:
    - "coach_program_folders table exists with RLS (folders_own policy)"
    - "workout_programs.folder_id (UUID NULL, FK coach_program_folders ON DELETE SET NULL) column exists"
    - "workout_programs.start_date (DATE NULL) column exists — distinct from existing cycle_start_date"
    - "exercises.is_user_defined (BOOLEAN NOT NULL DEFAULT FALSE) column exists alongside existing is_custom"
    - "coach_client_links.shared_note (TEXT NULL) column exists"
    - "workout_sessions.source_program_id (UUID NULL, FK workout_programs ON DELETE SET NULL) column exists"
    - "workout_sessions.source_session_id (TEXT NULL) column exists"
    - "workout_programs_coach_read RLS policy exists — coaches can SELECT assigned programs for their clients via is_coach_of()"
    - "Seed templates (is_template=TRUE, created_by_coach_id=NULL) are readable by any authenticated user via RLS"
    - "supabase db push succeeds with zero errors"
  artifacts:
    - path: supabase/migrations/045_coaching_programs_schema.sql
      provides: all 7 schema changes + RLS additions
      contains: coach_program_folders, folder_id, start_date, is_user_defined, shared_note, source_program_id, source_session_id, workout_programs_coach_read
  key_links:
    - from: workout_programs.folder_id
      to: coach_program_folders.id
      via: FK ON DELETE SET NULL
      pattern: "folder_id.*REFERENCES.*coach_program_folders"
    - from: workout_programs_coach_read policy
      to: is_coach_of function
      via: public.is_coach_of(auth.uid(), assigned_to_user_id)
      pattern: "is_coach_of.*assigned_to_user_id"
    - from: workout_sessions.source_program_id
      to: workout_programs.id
      via: FK ON DELETE SET NULL
      pattern: "source_program_id.*REFERENCES.*workout_programs"

user_setup:
  - service: supabase
    why: "Push migration 045 to production Supabase project"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens"
    dashboard_config:
      - task: "Confirm project ref is correct"
        location: "supabase/config.toml project_id field or run: supabase projects list"
---

<objective>
Write and push the core schema migration (045) that adds all 7 new columns/tables required by the coaching programs feature, plus two RLS policy additions on workout_programs.

Purpose: Backend routes and web pages cannot be built until these columns exist in the database. This is the blocking gate for all Wave 2+ work.
Output: supabase/migrations/045_coaching_programs_schema.sql committed + successfully pushed to the Supabase project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-CONTEXT.md
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-RESEARCH.md

<interfaces>
From supabase/migrations/036_workout_programs_ai_imports.sql — columns already on workout_programs (DO NOT re-add):
- created_by_coach_id UUID NULL
- assigned_to_user_id UUID NULL
- template_source_id UUID NULL
- is_template BOOLEAN DEFAULT FALSE
- weeks_data JSONB NULL

From supabase/migrations/016_program_cycles_schema.sql:
- cycle_start_date DATE already exists on workout_programs
- New column must be named start_date (different concept: assignment start date)

From supabase/migrations/035_coach_invitations_links_rls.sql:
- is_coach_of(coach_id UUID, client_id UUID) SECURITY DEFINER STABLE function is already defined
- coach_client_links table already exists with coach_id, client_id, status columns
- No shared_note column yet

From supabase/migrations/001_initial_schema.sql:
- exercises.is_custom BOOLEAN exists — do NOT rename it; add is_user_defined as a NEW column
- workout_programs.user_id NOT NULL constraint exists
- workout_sessions table exists with id, user_id, program_id, notes columns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 045 with all schema changes and RLS additions</name>
  <files>supabase/migrations/045_coaching_programs_schema.sql</files>
  <read_first>
    - supabase/migrations/036_workout_programs_ai_imports.sql — exact column names already added; must not duplicate
    - supabase/migrations/035_coach_invitations_links_rls.sql — is_coach_of() function signature; coach_client_links schema
    - supabase/migrations/001_initial_schema.sql — exercises table (is_custom column), workout_sessions schema, workout_programs user_id NOT NULL
    - supabase/migrations/016_program_cycles_schema.sql — cycle_start_date column to confirm naming conflict avoidance
    - supabase/migrations/041_coach_client_tags_notes.sql — pattern for adding columns to coach_ tables
  </read_first>
  <action>
    Create supabase/migrations/045_coaching_programs_schema.sql with the following SQL in this exact order:

    1. NEW TABLE coach_program_folders:
       - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
       - coach_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE
       - name TEXT NOT NULL CHECK (char_length(name) <= 100)
       - UNIQUE (coach_id, name)
       - created_at TIMESTAMPTZ DEFAULT now()
       Enable RLS: ALTER TABLE public.coach_program_folders ENABLE ROW LEVEL SECURITY;
       Policy "folders_own": FOR ALL USING (auth.uid() = coach_id) WITH CHECK (auth.uid() = coach_id);

    2. ALTER workout_programs — ADD COLUMN folder_id UUID REFERENCES public.coach_program_folders(id) ON DELETE SET NULL;
       Comment: '-- Phase 27: single-level folder organization (D-08)'

    3. ALTER workout_programs — ADD COLUMN start_date DATE NULL;
       Comment: '-- Phase 27: assignment start date (D-10). Distinct from cycle_start_date (migration 016).'

    4. ALTER exercises — ADD COLUMN is_user_defined BOOLEAN NOT NULL DEFAULT FALSE;
       Comment: '-- Phase 27: marks coach-created exercises (D-07). Coexists with is_custom; do not remove is_custom.'

    5. ALTER coach_client_links — ADD COLUMN shared_note TEXT NULL;
       Comment: '-- Phase 27: coach-written shared note visible to athlete (D-15). No CHECK — max 500 enforced at API layer.'

    6. ALTER workout_sessions — ADD COLUMN source_program_id UUID REFERENCES public.workout_programs(id) ON DELETE SET NULL;
       Comment: '-- Phase 27: compliance tracking — which program spawned this session (D-14)'

    7. ALTER workout_sessions — ADD COLUMN source_session_id TEXT NULL;
       Comment: '-- Phase 27: nanoid of the ProgramSession from weeks_data (D-14)'

    8. RLS policy — workout_programs_coach_read:
       The existing "own_programs" policy is FOR ALL. Add a new FOR SELECT policy to allow coaches to read assigned programs for their clients AND seed templates:
       CREATE POLICY "workout_programs_coach_read" ON public.workout_programs
         FOR SELECT
         USING (
           auth.uid() = user_id
           OR (
             assigned_to_user_id IS NOT NULL
             AND public.is_coach_of(auth.uid(), assigned_to_user_id)
           )
           OR (
             is_template = TRUE
             AND created_by_coach_id IS NULL
           )
         );
       Comment: The third OR clause makes seed templates (created_by_coach_id=NULL, is_template=TRUE) readable by any authenticated user.

    9. Add indexes for performance:
       CREATE INDEX IF NOT EXISTS idx_workout_programs_folder ON public.workout_programs(folder_id) WHERE folder_id IS NOT NULL;
       CREATE INDEX IF NOT EXISTS idx_workout_sessions_source_program ON public.workout_sessions(source_program_id) WHERE source_program_id IS NOT NULL;

    Do NOT modify: existing "own_programs" FOR ALL policy, is_custom column on exercises, any existing columns.
  </action>
  <verify>
    <automated>test -f /c/ziko-platform/supabase/migrations/045_coaching_programs_schema.sql && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>File exists with all 9 sections: coach_program_folders table + RLS, 6 ALTER TABLE statements, workout_programs_coach_read RLS policy, 2 indexes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push migration 045 to Supabase</name>
  <what-built>Migration 045 SQL file with all schema changes</what-built>
  <how-to-verify>
    Run the following commands in sequence:

    1. Verify the migration file looks correct:
       cat /c/ziko-platform/supabase/migrations/045_coaching_programs_schema.sql

    2. Push the migration:
       cd /c/ziko-platform && supabase db push

    3. Verify success — all 7 columns/table should exist:
       supabase db diff --use-migra 2>/dev/null | head -20
       (should show no diff if push succeeded)

    Expected outcomes:
    - supabase db push exits 0 with "Applying migration 045_coaching_programs_schema.sql"
    - No error messages about constraint violations or duplicate columns
    - coach_program_folders table visible in Supabase Dashboard → Table Editor
    - workout_programs shows folder_id and start_date columns
    - exercises shows is_user_defined column
    - coach_client_links shows shared_note column
    - workout_sessions shows source_program_id and source_session_id columns
  </how-to-verify>
  <resume-signal>Type "pushed" when supabase db push succeeded and all columns are visible in the dashboard. Describe any errors encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration script → Supabase | DDL executes with migration user privileges; no user input at this stage |
| coach_program_folders → coach_id | RLS ensures coach can only CRUD their own folders |
| workout_programs_coach_read | New SELECT policy must not allow coach to read programs not belonging to their clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Elevation of Privilege | workout_programs_coach_read RLS | mitigate | Policy uses is_coach_of() which verifies active coach_client_links row; the third OR clause for seed templates is limited to is_template=TRUE AND created_by_coach_id IS NULL — no client data exposed |
| T-27-02-02 | Tampering | shared_note column | mitigate | No direct client write access; column only writable via PUT /coach/clients/:id/shared-note which requires coach JWT and is_coach_of() verification |
| T-27-02-03 | Information Disclosure | source_program_id on workout_sessions | mitigate | workout_sessions already has its own RLS policy (user_id = auth.uid()); new FK column does not expand read access |
| T-27-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After push:

```bash
# Verify all columns via supabase CLI
supabase db diff --use-migra 2>/dev/null | grep -c "ALTER\|CREATE" || echo "No diff — schema is current"
```

Check migration is recorded:
```bash
grep -r "045_coaching_programs" /c/ziko-platform/supabase/ 2>/dev/null | grep -v ".sql:" | head -5
```
</verification>

<success_criteria>
- supabase/migrations/045_coaching_programs_schema.sql exists with all sections
- supabase db push exits 0 (no errors)
- coach_program_folders table exists in Supabase with RLS enabled
- workout_programs has folder_id and start_date columns
- exercises has is_user_defined column (is_custom untouched)
- coach_client_links has shared_note column
- workout_sessions has source_program_id and source_session_id columns
- workout_programs_coach_read policy exists allowing coach to read assigned programs via is_coach_of()
</success_criteria>

<output>
Create `.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-02-SUMMARY.md` when done
</output>
