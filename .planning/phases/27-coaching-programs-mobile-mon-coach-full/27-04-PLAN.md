---
phase: 27
plan: 04
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - backend/api/src/coach/programs/types.ts
  - backend/api/src/coach/programs/db.ts
  - backend/api/src/coach/programs/service.ts
  - backend/api/src/app.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - PROG-04
  - PROG-05
  - PROG-06
  - PROG-07
  - PROG-08

must_haves:
  truths:
    - "GET /coach/programs returns the coach's own templates + seed templates (created_by_coach_id=NULL is_template=TRUE)"
    - "POST /coach/programs creates a new template with is_template=TRUE and created_by_coach_id=coachId; weeks_data validated against ProgramWeekSchema[]"
    - "GET /coach/programs/folders and POST /coach/programs/folders work for folder CRUD"
    - "GET /coach/programs/:id returns a single program owned or assigned to coach's clients"
    - "PUT /coach/programs/:id updates a program; coach can only update programs they own (created_by_coach_id = coachId)"
    - "DELETE /coach/programs/:id deletes with RevokeConfirmModal-style guard (confirmed=true query param)"
    - "POST /coach/programs/:id/assign accepts { client_ids: string[] }, verifies is_coach_of() for each client, forks the template per client with start_date=CURRENT_DATE, template_source_id=templateId, is_template=FALSE"
    - "POST /coach/programs/:id/duplicate creates a copy of the program with new id and ' (copie)' suffix on name"
    - "GET /coach/programs/exercises?q= returns up to 10 exercises matching name ILIKE query"
    - "POST /coach/programs/exercises creates a new exercise with is_user_defined=TRUE"
    - "/folders routes are registered BEFORE /:id routes in Hono to prevent 'folders' matching as :id param"
    - "All routes use authMiddleware and per-request createUserClient(jwt) — never service-role"
  artifacts:
    - path: backend/api/src/coach/programs/types.ts
      provides: internal TypeScript types for programs module
    - path: backend/api/src/coach/programs/db.ts
      provides: all DB query functions
      exports: [listPrograms, createProgram, getProgram, updateProgram, deleteProgram, assignProgram, duplicateProgram, listFolders, createFolder, searchExercises, createExercise]
    - path: backend/api/src/coach/programs/service.ts
      provides: Hono programsRouter with all 9 routes
      exports: [programsRouter]
    - path: backend/api/src/app.ts
      provides: programsRouter mounted at /coach/programs
  key_links:
    - from: backend/api/src/app.ts
      to: backend/api/src/coach/programs/service.ts
      via: app.route('/coach/programs', programsRouter)
      pattern: "route.*coach/programs.*programsRouter"
    - from: backend/api/src/coach/programs/service.ts
      to: backend/api/src/coach/programs/db.ts
      via: import DB functions
      pattern: "import.*from.*./db"
    - from: assignProgram in db.ts
      to: is_coach_of via RLS
      via: createUserClient(jwt) — RLS enforces is_coach_of on INSERT for assigned_to_user_id
      pattern: "is_coach_of\|assigned_to_user_id"
---

<objective>
Build the full programsRouter bounded module: types.ts, db.ts, service.ts — and mount it in app.ts. This module handles all program template lifecycle operations including exercise search.

Purpose: Web pages for /coach/programs, /coach/programs/new, /coach/programs/[id] all depend on these endpoints.
Output: 3 new files (types.ts, db.ts, service.ts) + 1 edit (app.ts), providing 9 routes under /coach/programs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-CONTEXT.md
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-RESEARCH.md

<interfaces>
From backend/api/src/coach/clients/db.ts — createUserClient pattern to replicate exactly:
```typescript
export function createUserClient(jwt: string) {
  return createClient(
    process.env.SUPABASE_URL!,
    process.env.SUPABASE_PUBLISHABLE_KEY!,
    {
      auth: { autoRefreshToken: false, persistSession: false },
      global: { headers: { Authorization: `Bearer ${jwt}` } },
    },
  );
}
```

From backend/api/src/coach/clients/service.ts — authMiddleware usage and coachId extraction:
```typescript
import { authMiddleware } from '../../middleware/auth.js';
clientsRouter.use('*', authMiddleware);
clientsRouter.get('/', async (c) => {
  const { userId } = c.get('auth');
  const jwt = c.req.header('Authorization')!.slice(7);
  ...
});
```

From backend/api/src/app.ts — mount pattern:
```typescript
app.route('/coach/identity', identityRouter);
app.route('/coach/invitations', invitationsRouter);
app.route('/coach/clients', clientsRouter);
// New: app.route('/coach/programs', programsRouter);
```

From packages/coach-sdk/src/schemas/index.ts (after plan 27-01):
ProgramWeekSchema, ProgramSessionSchema, ProgramExerciseSchema — import from '@ziko/coach-sdk'

From RESEARCH.md — exercise search SQL:
```sql
SELECT id, name, category, muscle_groups
FROM public.exercises
WHERE (is_custom = FALSE OR user_id = auth.uid())
  AND name ILIKE '%' || $1 || '%'
ORDER BY
  CASE WHEN name ILIKE $1 || '%' THEN 0 ELSE 1 END,
  name ASC
LIMIT 10
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create programs/types.ts and programs/db.ts</name>
  <files>
    backend/api/src/coach/programs/types.ts
    backend/api/src/coach/programs/db.ts
  </files>
  <read_first>
    - backend/api/src/coach/clients/db.ts — createUserClient exact pattern to replicate
    - backend/api/src/coach/clients/types.ts — internal types pattern to follow
    - backend/api/src/coach/identity/db.ts — how DB functions return data (error handling pattern)
    - packages/coach-sdk/src/schemas/index.ts — import path for ProgramWeekSchema after plan 27-01 adds it
  </read_first>
  <action>
    Create backend/api/src/coach/programs/types.ts:
    - Import ProgramWeek from '@ziko/coach-sdk' (use .js extension if needed per ESM)
    - Export interface CreateProgramBody: { name: string; description?: string; goal?: string; weeks_count: number; folder_id?: string | null; weeks_data?: ProgramWeek[] }
    - Export interface UpdateProgramBody: Partial of CreateProgramBody
    - Export interface AssignProgramBody: { client_ids: string[] }
    - Export interface CreateFolderBody: { name: string }
    - Export interface CreateExerciseBody: { name: string; category?: string }

    Create backend/api/src/coach/programs/db.ts:
    - Copy createUserClient function exactly from clients/db.ts
    - Implement these exported async functions (each accepts jwt: string as first param):

    listPrograms(jwt, coachId): SELECT id, name, description, goal, weeks_count, folder_id, is_template, created_by_coach_id, created_at, updated_at FROM workout_programs WHERE (created_by_coach_id = coachId AND is_template = TRUE) OR (is_template = TRUE AND created_by_coach_id IS NULL) ORDER BY created_at DESC. Returns { programs: row[] }.

    createProgram(jwt, coachId, body: CreateProgramBody): INSERT INTO workout_programs (user_id, name, description, goal, weeks_count, folder_id, is_template, created_by_coach_id, weeks_data) VALUES (coachId, ..., TRUE, coachId, body.weeks_data ?? []). Returns created row.

    getProgram(jwt, coachId, id): SELECT * FROM workout_programs WHERE id = id — RLS handles visibility. Returns row or null.

    updateProgram(jwt, coachId, id, body: UpdateProgramBody): UPDATE workout_programs SET name=..., description=..., goal=..., weeks_count=..., folder_id=..., weeks_data=..., updated_at=now() WHERE id=id AND created_by_coach_id=coachId. Returns updated row.

    deleteProgram(jwt, coachId, id): DELETE FROM workout_programs WHERE id=id AND created_by_coach_id=coachId AND is_template=TRUE. Returns { deleted: boolean }.

    assignProgram(jwt, coachId, templateId, clientIds: string[]): For each clientId, INSERT INTO workout_programs (user_id, created_by_coach_id, assigned_to_user_id, template_source_id, is_template, name, description, goal, weeks_count, weeks_data, start_date) SELECT coachId, coachId, clientId, id, FALSE, name, description, goal, weeks_count, weeks_data, CURRENT_DATE FROM workout_programs WHERE id=templateId AND created_by_coach_id=coachId. Use Promise.all for batch. Returns { assigned: clientIds.length }.

    duplicateProgram(jwt, coachId, id): SELECT * from workout_programs WHERE id=id, then INSERT with name=original.name+' (copie)', new id via gen_random_uuid(), same coachId, is_template=TRUE. Returns new row.

    listFolders(jwt, coachId): SELECT * FROM coach_program_folders WHERE coach_id=coachId ORDER BY name. Returns { folders: row[] }.

    createFolder(jwt, coachId, body: CreateFolderBody): INSERT INTO coach_program_folders (coach_id, name). Returns created row.

    searchExercises(jwt, query: string): SELECT id, name, category, muscle_groups FROM exercises WHERE (is_custom=FALSE OR user_id=auth.uid()) AND name ILIKE '%'||query||'%' ORDER BY CASE WHEN name ILIKE query||'%' THEN 0 ELSE 1 END, name ASC LIMIT 10. Use supabase.rpc or raw text search via .ilike(). Returns { exercises: row[] }.

    createExercise(jwt, body: CreateExerciseBody): INSERT INTO exercises (name, category, is_user_defined) VALUES (body.name, body.category, TRUE). Returns created row.

    Error handling: follow clients/db.ts pattern — throw Error with message on Supabase error.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npx tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep "programs" | head -20</automated>
  </verify>
  <done>types.ts and db.ts exist; tsc reports no errors in the programs/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Create programs/service.ts and mount in app.ts</name>
  <files>
    backend/api/src/coach/programs/service.ts
    backend/api/src/app.ts
  </files>
  <read_first>
    - backend/api/src/coach/clients/service.ts — full file; exact pattern for route registration, auth middleware, error responses
    - backend/api/src/app.ts — current mount list; add programsRouter after clientsRouter
    - backend/api/src/coach/programs/db.ts — just created; function signatures to call
    - backend/api/src/coach/programs/types.ts — body interfaces for request parsing
    - packages/coach-sdk/src/schemas/index.ts — import ProgramWeekSchema for request body validation
  </read_first>
  <action>
    Create backend/api/src/coach/programs/service.ts:

    Import Hono, authMiddleware, all db functions from ./db.js, body types from ./types.js, ProgramWeekSchema from '@ziko/coach-sdk'.

    Create programsRouter = new Hono(). Apply programsRouter.use('*', authMiddleware).

    Register routes in THIS ORDER (critical — static paths before :id to prevent Hono route collision):

    1. GET /exercises?q= → searchExercises(jwt, q). Return 400 if q is empty.
    2. POST /exercises → createExercise(jwt, body). Validate body.name is present.
    3. GET /folders → listFolders(jwt, coachId)
    4. POST /folders → createFolder(jwt, coachId, body). Validate name <= 100 chars.
    5. GET / → listPrograms(jwt, coachId). Return { programs }.
    6. POST / → createProgram: Validate weeks_data with z.array(ProgramWeekSchema).safeParse() before DB call. Return 400 on schema failure with error details. Return 201 on success.
    7. GET /:id → getProgram(jwt, coachId, id). Return 404 if null.
    8. PUT /:id → updateProgram. If weeks_data present, validate with ProgramWeekSchema[]. Return 400 on schema failure.
    9. DELETE /:id → deleteProgram. Require ?confirmed=true query param; return 400 without it.
    10. POST /:id/assign → assignProgram. Parse { client_ids } from body. Return 400 if client_ids empty or not array. Return { assigned: N }.
    11. POST /:id/duplicate → duplicateProgram. Return 201 with new program.

    All routes extract: const { userId: coachId } = c.get('auth'); const jwt = c.req.header('Authorization')!.slice(7);

    Error responses: return c.json({ error: message }, statusCode). Use 400 for validation, 404 for not found, 403 for unauthorized operations, 500 for unexpected.

    Edit backend/api/src/app.ts:
    - Import programsRouter from './coach/programs/service.js'
    - Add app.route('/coach/programs', programsRouter) after the existing app.route('/coach/clients', ...) line
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npx tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep -v "^$" | head -30</automated>
  </verify>
  <done>service.ts exists with all 11 routes in correct order; app.ts mounts /coach/programs; tsc compiles clean across backend/api</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coach browser → POST /coach/programs | JWT validated by authMiddleware; weeks_data Zod-validated before DB write |
| POST /coach/programs/:id/assign | Coach may only assign to clients they are linked to via is_coach_of(); RLS on INSERT enforces this |
| PUT /coach/programs/:id | AND created_by_coach_id=coachId in WHERE clause prevents editing other coaches' programs |
| GET /coach/programs/exercises | RLS read_exercises policy on exercises table limits results to public + own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Elevation of Privilege | POST /:id/assign | mitigate | assignProgram WHERE clause requires created_by_coach_id=coachId on template; RLS on INSERT verifies is_coach_of() for assigned_to_user_id — coach cannot fork to non-linked clients |
| T-27-04-02 | Tampering | POST / weeks_data | mitigate | z.array(ProgramWeekSchema).safeParse() validates full JSONB shape at route entry before any DB write; .strict() rejects unknown keys |
| T-27-04-03 | Tampering | PUT /:id weeks_data | mitigate | Same Zod validation applied on PUT when weeks_data is present in body |
| T-27-04-04 | Tampering | Shared note IDOR | accept | shared_note is on clients router (plan 27-05), not programs router — handled there |
| T-27-04-05 | Elevation of Privilege | DELETE /:id | mitigate | ?confirmed=true required (prevents accidental deletion); WHERE created_by_coach_id=coachId prevents deleting other coaches' templates |
| T-27-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
TypeScript clean compile:
```bash
cd /c/ziko-platform && npx tsc --noEmit -p backend/api/tsconfig.json 2>&1 | grep -c "error TS" || echo "0 errors"
```

Route order verification (folders before :id):
```bash
grep -n "\/folders\|'\/:id'" /c/ziko-platform/backend/api/src/coach/programs/service.ts | head -15
```
Expected: /folders lines appear at lower line numbers than /:id lines.

Mount verification:
```bash
grep "coach/programs" /c/ziko-platform/backend/api/src/app.ts
```
Expected: app.route('/coach/programs', programsRouter)
</verification>

<success_criteria>
- backend/api/src/coach/programs/{types.ts, db.ts, service.ts} exist
- app.ts imports and mounts programsRouter at /coach/programs
- /folders routes registered before /:id routes in service.ts
- POST / validates weeks_data with ProgramWeekSchema[] before DB write
- POST /:id/assign requires ?confirmed=true; forks template per client_id with start_date=CURRENT_DATE
- POST /:id/duplicate creates copy with ' (copie)' suffix
- GET /exercises?q= returns exercises matching ILIKE query
- TypeScript compiles clean across backend/api
</success_criteria>

<output>
Create `.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-04-SUMMARY.md` when done
</output>
