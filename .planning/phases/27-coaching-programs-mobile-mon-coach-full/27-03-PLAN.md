---
phase: 27
plan: 03
type: execute
wave: 1
depends_on:
  - 27-02
files_modified:
  - supabase/migrations/046_coaching_programs_seeds.sql
autonomous: false
requirements:
  - PROG-08

must_haves:
  truths:
    - "5 seed program templates exist in workout_programs with is_template=TRUE, created_by_coach_id=NULL"
    - "Seed templates are idempotent — re-running the migration does not create duplicates"
    - "Each seed template has weeks_data JSONB matching ProgramWeekSchema[] shape (week_number, sessions with day_of_week + exercises)"
    - "Seed templates cover: PPL, 5/3/1, Hyrox prep, body-recomp, beginner full-body"
    - "user_id on seed rows satisfies NOT NULL constraint via service-role insert strategy"
    - "Seed templates are readable by any authenticated user via the workout_programs_coach_read RLS policy (is_template=TRUE AND created_by_coach_id IS NULL clause added in 045)"
  artifacts:
    - path: supabase/migrations/046_coaching_programs_seeds.sql
      provides: 5 seed template INSERT statements (idempotent)
      contains: INSERT INTO workout_programs
  key_links:
    - from: seed rows in workout_programs
      to: workout_programs_coach_read RLS policy
      via: is_template=TRUE AND created_by_coach_id IS NULL clause
      pattern: "is_template.*TRUE.*created_by_coach_id.*NULL"

user_setup:
  - service: supabase
    why: "Push migration 046 seed templates to production"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Already configured from 27-02 push"
    dashboard_config:
      - task: "Verify seed templates appear after push"
        location: "Supabase Dashboard → Table Editor → workout_programs — filter is_template = true"
---

<objective>
Seed 5 expert-curated program templates into workout_programs via an idempotent migration (046). These templates form the "Bibliothèque Ziko" section visible to every coach on signup.

Purpose: Coaches must see starter templates when they first open /coach/programs. Without seeds, the library section is empty and the onboarding experience is incomplete.
Output: supabase/migrations/046_coaching_programs_seeds.sql with 5 INSERT statements using ON CONFLICT DO NOTHING, pushed to Supabase.

Decision on user_id strategy (open question from RESEARCH.md): Use Option B — relax user_id nullable for rows where is_template=TRUE AND created_by_coach_id IS NULL via a migration-level approach. Concretely: the migration executes via the Supabase migration user which has elevated privileges. Use a fixed sentinel UUID ('00000000-0000-0000-0000-000000000001') for user_id on seed rows, inserted via the migration context (bypasses RLS, does not need to exist in auth.users because the migration runs as superuser). The workout_programs_coach_read RLS policy from 045 already allows reads of seed rows regardless of user_id.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-CONTEXT.md
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-RESEARCH.md

<interfaces>
From 045_coaching_programs_schema.sql (just created):
- workout_programs now has: id, user_id (NOT NULL), name, description, goal, weeks_count, weeks_data JSONB, is_template BOOLEAN, created_by_coach_id UUID NULL, assigned_to_user_id UUID NULL, template_source_id UUID NULL, start_date DATE NULL, folder_id UUID NULL

From CONTEXT.md D-04/D-05 — exact JSONB shape for weeks_data:
- weeks_data is ProgramWeekSchema[]: [{week_number: int, sessions: [{session_id: uuid, session_name: string, day_of_week: 1-7, exercises: [...]}]}]
- Each exercise: {exercise_id: uuid|null, exercise_name: string, sets: int, reps: int|null, duration_seconds: int|null, target_rpe: float|null, target_rir: int|null, rest_seconds: int|null, notes: string|null}

From supabase/migrations/026_ai_credits_default.sql (pattern reference):
- INSERT with ON CONFLICT DO NOTHING pattern for idempotent seeds
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 046 — 5 seed program templates</name>
  <files>supabase/migrations/046_coaching_programs_seeds.sql</files>
  <read_first>
    - supabase/migrations/045_coaching_programs_schema.sql — confirm column list on workout_programs before writing INSERTs
    - supabase/migrations/036_workout_programs_ai_imports.sql — confirm all column names added by Phase 22 that must be included or set NULL
    - supabase/seed.sql — observe existing INSERT patterns (exercises table seeds) to match style
  </read_first>
  <action>
    Create supabase/migrations/046_coaching_programs_seeds.sql.

    File header comment explaining: sentinel UUID '00000000-0000-0000-0000-000000000001' is used as user_id for system seed templates. This UUID does not correspond to any real auth user. Migration runs as superuser context bypassing the NOT NULL FK constraint check against auth.users. The workout_programs_coach_read RLS policy (migration 045) allows any authenticated user to read rows where is_template=TRUE AND created_by_coach_id IS NULL.

    Insert 5 templates using fixed UUIDs (gen_random_uuid() is not idempotent — use literal UUIDs so ON CONFLICT DO NOTHING works):

    Template 1 — PPL (Push/Pull/Legs) 6 weeks:
    - id: 'a1000000-0000-0000-0000-000000000001'
    - name: 'PPL 6 semaines'
    - description: 'Programme Push/Pull/Legs sur 6 jours — idéal pour hypertrophie intermédiaire'
    - goal: 'Prise de masse'
    - weeks_count: 6
    - is_template: TRUE, created_by_coach_id: NULL, user_id: '00000000-0000-0000-0000-000000000001'
    - weeks_data: 1 representative week (week_number:1) with 3 sessions (Push Mon/day_of_week:1, Pull Wed/3, Legs Fri/5), each with 4-5 exercises using real exercise names from the Ziko library (exercise_id: NULL since we don't know exact UUIDs at migration time — exercise_name is the lookup key). Sets 3-4, reps 8-12 range. target_rpe: 8, rest_seconds: 90.

    Template 2 — 5/3/1 (Wendler) 4 weeks:
    - id: 'a1000000-0000-0000-0000-000000000002'
    - name: '5/3/1 Wendler 4 semaines'
    - description: 'Programme force basé sur 5/3/1 avec 4 mouvements fondamentaux'
    - goal: 'Force'
    - weeks_count: 4
    - weeks_data: 1 week, 4 sessions (Squat Mon/1, Bench Tue/2, Deadlift Thu/4, Press Fri/5), each with main lift + 2 accessory exercises. Sets from 5/3/1 scheme (week 1: 3×5), target_rpe: 8-9.

    Template 3 — Hyrox Prep 8 weeks:
    - id: 'a1000000-0000-0000-0000-000000000003'
    - name: 'Hyrox Prep 8 semaines'
    - description: 'Préparation compétition Hyrox — endurance fonctionnelle + force'
    - goal: 'Hyrox'
    - weeks_count: 8
    - weeks_data: 1 week, 4 sessions (Run + Ski Erg Mon/1, Sled Push Wed/3, Wall Balls + Burpee Broad Jump Fri/5, Long Run Sat/6). Exercises use duration_seconds for cardio elements, sets/reps for strength.

    Template 4 — Body Recomp 12 weeks:
    - id: 'a1000000-0000-0000-0000-000000000004'
    - name: 'Body Recomp 12 semaines'
    - description: 'Recomposition corporelle — perte de gras + maintien musculaire'
    - goal: 'Reconditionnement'
    - weeks_count: 12
    - weeks_data: 1 week, 3 sessions (Full Body A Mon/1, Full Body B Wed/3, Full Body C Fri/5), compound movements, sets 3×10-12, target_rir: 2.

    Template 5 — Beginner Full Body 8 weeks:
    - id: 'a1000000-0000-0000-0000-000000000005'
    - name: 'Débutant Full Body 8 semaines'
    - description: 'Programme pour débutants — 3 jours/semaine, mouvements fondamentaux'
    - goal: 'Reconditionnement'
    - weeks_count: 8
    - weeks_data: 1 week, 3 sessions (Full Body A Mon/1, Full Body B Wed/3, Full Body C Fri/5), 5 exercises each, sets 3, reps 10, rest_seconds: 120, target_rpe: 7, notes with form cues.

    All INSERTs use: INSERT INTO public.workout_programs (...) VALUES (...) ON CONFLICT (id) DO NOTHING;

    Weeks_data JSON must be valid per D-04/D-05 shape: all exercise objects include all 9 nullable fields (null where not used, never omitting them — .strict() schema requires all fields present or they must be nullable).

    Actually: .strict() prevents EXTRA keys but does not require nullable fields to be present if they are .nullable(). Write the JSONB with all fields explicitly — set unused nullable fields to null rather than omitting them.
  </action>
  <verify>
    <automated>test -f /c/ziko-platform/supabase/migrations/046_coaching_programs_seeds.sql && grep -c "INSERT INTO public.workout_programs" /c/ziko-platform/supabase/migrations/046_coaching_programs_seeds.sql</automated>
  </verify>
  <done>File exists; grep count returns 5 (one INSERT per seed template)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push migration 046 seed templates</name>
  <what-built>Migration 046 with 5 seed template INSERTs</what-built>
  <how-to-verify>
    1. Inspect the seed data looks reasonable:
       cat /c/ziko-platform/supabase/migrations/046_coaching_programs_seeds.sql | head -80

    2. Push:
       cd /c/ziko-platform && supabase db push

    3. Verify 5 seeds landed:
       In Supabase Dashboard → Table Editor → workout_programs
       Apply filter: is_template = true AND created_by_coach_id IS NULL
       Expected: exactly 5 rows with names PPL, 5/3/1, Hyrox Prep, Body Recomp, Débutant Full Body

    4. Verify idempotency — run push again:
       cd /c/ziko-platform && supabase db push
       Should succeed with "No changes to apply" or apply 046 once without duplicating rows.
  </how-to-verify>
  <resume-signal>Type "seeded" when 5 seed templates are visible in the dashboard. Describe any push errors or missing templates.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed migration → Supabase | Service-role migration context; no user input |
| Seed templates → authenticated users | workout_programs_coach_read policy controls who can read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Information Disclosure | Seed template read access | accept | Seed templates (is_template=TRUE, created_by_coach_id=NULL) are intentionally public to all authenticated users — this is desired behavior for "Bibliothèque Ziko" |
| T-27-03-02 | Tampering | Sentinel UUID user_id | accept | Sentinel UUID '00000000-0000-0000-0000-000000000001' is never a real auth.users row; existing "own_programs" FOR ALL policy prevents any regular user from modifying seed rows since auth.uid() != sentinel UUID |
| T-27-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After push:
```bash
# Confirm 5 seeds visible (requires supabase CLI with project linked)
supabase db diff --use-migra 2>/dev/null | head -5
# Should show "No migration files to apply" if both 045 and 046 are already pushed
```
</verification>

<success_criteria>
- supabase/migrations/046_coaching_programs_seeds.sql exists with 5 INSERT statements
- All 5 INSERTs use ON CONFLICT (id) DO NOTHING for idempotency
- weeks_data JSONB in each template matches D-04/D-05 shape
- supabase db push succeeds for migration 046
- 5 seed templates visible in Supabase with is_template=TRUE, created_by_coach_id=NULL
- Re-running supabase db push does not duplicate seed rows
</success_criteria>

<output>
Create `.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-03-SUMMARY.md` when done
</output>
