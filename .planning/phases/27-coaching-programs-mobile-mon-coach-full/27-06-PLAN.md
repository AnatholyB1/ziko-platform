---
phase: 27
plan: 06
type: execute
wave: 3
depends_on:
  - 27-04
files_modified:
  - apps/web/src/components/coach/CoachSidebar.tsx
  - apps/web/src/components/coach/ProgramCard.tsx
  - apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx
  - apps/web/src/app/[locale]/(coach)/coach/programs/new/page.tsx
autonomous: true
requirements:
  - PROG-01
  - PROG-04
  - PROG-08

must_haves:
  truths:
    - "CoachSidebar Programmes nav entry is enabled (disabled: false) and links to /fr/coach/programs"
    - "GET /fr/coach/programs renders without error; shows MES TEMPLATES section and BIBLIOTHÈQUE ZIKO section"
    - "Programs list shows own templates with context menu (Modifier, Dupliquer, Assigner, Supprimer)"
    - "Bibliothèque Ziko section shows 5 seed templates with Officiel badge and Utiliser ce template hover action"
    - "Folder rail shows existing folders; clicking a folder filters the programs list"
    - "Empty state shows IoDocumentTextOutline + 'Aucun programme' heading + CTA when coach has no own templates"
    - "GET /fr/coach/programs/new renders a form with name, description, goal chips, weeks count, folder select fields"
    - "Submitting the form calls POST /coach/programs and redirects to /coach/programs/[id]"
    - "Name validation shows 'Nom requis' inline error when submitting empty name"
    - "All pages use force-dynamic + revalidate=0 + createServerSupabase() pattern"
  artifacts:
    - path: apps/web/src/components/coach/CoachSidebar.tsx
      provides: Programmes nav entry enabled
    - path: apps/web/src/components/coach/ProgramCard.tsx
      provides: reusable program card component for list page
    - path: apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx
      provides: programs list page (Server Component)
    - path: apps/web/src/app/[locale]/(coach)/coach/programs/new/page.tsx
      provides: new program form page (Client Component for interactivity)
  key_links:
    - from: programs/page.tsx
      to: EXPO_PUBLIC_API_URL /coach/programs
      via: fetch with Authorization header
      pattern: "fetch.*coach/programs"
    - from: ProgramCard.tsx
      to: programs/page.tsx
      via: rendered in grid
      pattern: "ProgramCard"
    - from: new/page.tsx
      to: POST /coach/programs
      via: fetch on form submit
      pattern: "POST.*coach/programs"
---

<objective>
Build the programs list page (A1), new program form page (A2), ProgramCard component, and flip the CoachSidebar "Programmes" entry from disabled to active.

Purpose: These are the entry point surfaces for the entire program builder. Coaches land here first and need to see their templates, seed templates, and be able to create a new one.
Output: 4 files modified/created. After this plan, a coach can navigate to /coach/programs, see their templates + the Bibliothèque Ziko, and create a new template via the form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-CONTEXT.md
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-UI-SPEC.md

<interfaces>
From apps/web/src/components/coach/CoachSidebar.tsx (verified content):
```typescript
const NAV_ITEMS = [
  { label: 'Dashboard', href: '/fr/coach/dashboard', icon: IoGridOutline, disabled: false },
  { label: 'Clients', href: '/fr/coach/clients', icon: IoPeopleOutline, disabled: false },
  { label: 'Invitations', href: '/fr/coach/invitations', icon: IoMailOutline, disabled: false },
  { label: 'Programmes', href: '/fr/coach/programs', icon: IoBarChartOutline, disabled: true }, // <- change to false
  { label: 'IA', href: '/fr/coach/ai', icon: IoSparklesOutline, disabled: true },
  { label: 'Paramètres', href: '/fr/coach/settings', icon: IoSettingsOutline, disabled: false },
];
```

From apps/web/src/app/[locale]/(coach)/coach/dashboard/page.tsx — Server Component pattern:
```typescript
export const dynamic = 'force-dynamic';
export const revalidate = 0;
// createServerSupabase() + getUser() + redirect to login if null
```

From apps/web/src/components/coach/InvitationsTable.tsx — table/card list pattern with FilterChipGroup; reuse for programs list layout structure.

From apps/web/src/components/coach/FilterChipGroup.tsx — filter chips for folder nav (reference pattern).

From apps/web/src/components/coach/RevokeConfirmModal.tsx — backdrop/dialog pattern for delete confirmation (use when implementing delete action on ProgramCard context menu).

Environment variable for API URL: process.env.NEXT_PUBLIC_API_URL (used in all existing web fetch calls).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip CoachSidebar + create ProgramCard component</name>
  <files>
    apps/web/src/components/coach/CoachSidebar.tsx
    apps/web/src/components/coach/ProgramCard.tsx
  </files>
  <read_first>
    - apps/web/src/components/coach/CoachSidebar.tsx — current file; one-line change only
    - apps/web/src/components/coach/InvitationsTable.tsx — card/row pattern with context menu to reference
    - apps/web/src/components/coach/RevokeConfirmModal.tsx — modal pattern to replicate for delete confirmation in ProgramCard
    - apps/web/src/app/[locale]/(coach)/coach/clients/[id]/sessions/page.tsx — how existing pages pass API_URL and auth token to client components
  </read_first>
  <action>
    Edit apps/web/src/components/coach/CoachSidebar.tsx:
    - Change exactly one character: `disabled: true` → `disabled: false` on the Programmes NAV_ITEMS entry (line containing 'Programmes'). Touch nothing else.

    Create apps/web/src/components/coach/ProgramCard.tsx as 'use client' component:

    Props interface ProgramCardProps:
    - id: string
    - name: string
    - goal: string | null
    - weeks_count: number
    - folder_id: string | null
    - folderName: string | null (resolved folder name)
    - is_template: boolean
    - created_by_coach_id: string | null (null = seed template)
    - onEdit: (id: string) => void
    - onDuplicate: (id: string) => void
    - onAssign: (id: string) => void
    - onDelete: (id: string) => void
    - isSeed: boolean (derived from created_by_coach_id === null)

    Card structure per UI-SPEC Screen A1:
    - Container: div role="button" + aria-label="Ouvrir le programme [name]" with onClick navigating to /fr/coach/programs/[id]
    - className: "bg-white border border-border rounded-2xl p-4 flex items-center justify-between hover:shadow-md transition-shadow cursor-pointer"
    - Left: program name (text-sm font-bold) + meta row "N semaines · [goal] · template" (text-xs text-muted) + folder chip if folderName set
    - Right for own templates: IoEllipsisHorizontal button (aria-label "Options pour [name]") + context menu dropdown (Modifier, Dupliquer, Assigner, Supprimer in red)
    - Right for seed templates: IoLockClosedOutline (hover shows "Utiliser ce template" overlay via CSS group-hover)
    - Context menu: state-managed dropdown, closes on outside click (useEffect + document click listener). Items call onEdit/onDuplicate/onAssign/onDelete. "Supprimer" triggers a delete confirmation modal (reuse RevokeConfirmModal pattern: type "SUPPRIMER" to enable).
    - Context menu div: "bg-white border border-border shadow-lg rounded-xl py-1 absolute right-0 mt-1 z-10 min-w-40"
    - Context menu items: "px-4 py-2 text-sm font-normal text-text hover:bg-background cursor-pointer"
    - "Supprimer" item: text-red-600 hover:bg-red-50

    Export ProgramCard as named export.
  </action>
  <verify>
    <automated>grep "disabled: false" /c/ziko-platform/apps/web/src/components/coach/CoachSidebar.tsx | grep -c "Programmes\|BarChart" || grep -c "disabled: false" /c/ziko-platform/apps/web/src/components/coach/CoachSidebar.tsx</automated>
  </verify>
  <done>CoachSidebar has disabled: false on Programmes entry; ProgramCard.tsx exists with all props, context menu, and delete confirmation modal</done>
</task>

<task type="auto">
  <name>Task 2: Create programs list page (A1) and new program form (A2)</name>
  <files>
    apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx
    apps/web/src/app/[locale]/(coach)/coach/programs/new/page.tsx
  </files>
  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/dashboard/page.tsx — force-dynamic + createServerSupabase() + redirect pattern; replicate exactly
    - apps/web/src/app/[locale]/(coach)/coach/clients/page.tsx — how programs list page should be structured (Server Component wrapper + client subcomponent or inline)
    - apps/web/src/app/[locale]/(coach)/coach/invitations/page.tsx — breadcrumb/header pattern
    - apps/web/src/lib/supabase/server.ts — createServerSupabase import path (verify exact path before using)
    - apps/web/src/components/coach/ProgramCard.tsx — just created in Task 1
  </read_first>
  <action>
    Create apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx as Server Component:

    - export const dynamic = 'force-dynamic'; export const revalidate = 0;
    - Props: { params: { locale: string } }
    - Auth check: createServerSupabase() → getUser() → if not logged in, redirect to /${locale}/login
    - Fetch programs: server-side fetch to ${process.env.NEXT_PUBLIC_API_URL}/coach/programs with { headers: { Authorization: Bearer ${session.access_token}, cache: 'no-store' } }
    - Fetch folders: same pattern to /coach/programs/folders
    - Render layout: flex with CoachSidebar (existing) + main content area

    Main content layout (from UI-SPEC Screen A1):
    - Page header: h1 "Programmes" (text-2xl font-bold) + sub "Vos templates d'entraînement" (text-sm text-muted) + right-aligned "Nouveau programme" button linking to /fr/coach/programs/new (bg-primary text-white rounded-xl px-4 py-2 text-sm font-bold)
    - Body: flex gap-8
      - Left folder rail (aside w-48 shrink-0 border-r border-border pr-4): "Tous les programmes" pill (active filter) + "Sans dossier" item + folders list from fetched data + "[+ Nouveau dossier]" link (text-sm text-muted). Folder filtering is client-side state — wrap folder rail + program grid in a 'use client' sub-component ProgramsListClient or handle via URL search params.
      - Right program grid:
        - "MES TEMPLATES" section header (text-sm font-bold text-muted uppercase tracking-wide)
        - grid grid-cols-1 gap-3 of ProgramCard for own templates (programs where created_by_coach_id != null)
        - Empty state when no own templates (per UI-SPEC: IoDocumentTextOutline + heading + body + CTA)
        - "BIBLIOTHÈQUE ZIKO" section header + orange "Officiel" badge (bg-primary/10 text-primary text-xs font-bold px-2 py-0.5 rounded-full)
        - ProgramCard list for seed templates (isSeed=true) — always shown

    Client-side state for folder filter and context menu actions: extract into a ProgramsClient component ('use client') that receives programs and folders as props from the Server Component.

    Loading skeleton for programs: 3 skeleton cards "bg-border animate-pulse rounded-2xl h-16" (shown via React Suspense boundary or loading.tsx if desired — at minimum handle the empty/loaded state).

    Create apps/web/src/app/[locale]/(coach)/coach/programs/new/page.tsx as 'use client' component (needs form state):

    - export const dynamic = 'force-dynamic'; export const revalidate = 0;
    - Client component with useState for: name, description, goal (string), weeks_count (number, default 4), folder_id (string|null), nameError (string)
    - Fetch folders on mount: GET /coach/programs/folders with auth header from useSupabase or cookie (use the same pattern as other client components in the coach section — check apps/web/src/components/coach/InvitationsTable.tsx for how it gets the auth session)
    - Layout per UI-SPEC Screen A2: breadcrumb nav + form card (bg-white border border-border rounded-2xl p-8 shadow-sm)
    - Fields:
      1. Nom: input with value/onChange, placeholder "ex : PPL 12 semaines Hyrox", nameError shown below as text-xs text-red-600 mt-1
      2. Description: textarea rows=3, optional
      3. Objectif: chip group with values ["Prise de masse", "Perte de poids", "Force", "Hyrox", "Endurance", "Mobilité", "Reconditionnement"] — selected chip: border-primary bg-primary/10 text-primary font-bold
      4. Nombre de semaines: input type=number min=1 max=52 w-24
      5. Dossier: select from fetched folders + "Aucun dossier" default
    - On submit: validate name.trim() not empty (set nameError if empty); POST ${API_URL}/coach/programs with { name, description, goal, weeks_count, weeks_data: [] }; on success redirect to /fr/coach/programs/[newId]
    - Cancel button ("Abandonner le programme") navigates to /fr/coach/programs using router.push
    - Submit button disabled while loading; text "Créer le programme"
    - Action row: flex justify-end gap-3 mt-8 border-t border-border pt-6
  </action>
  <verify>
    <automated>test -f "/c/ziko-platform/apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx" && test -f "/c/ziko-platform/apps/web/src/app/[locale]/(coach)/coach/programs/new/page.tsx" && echo "BOTH EXIST" || echo "MISSING"</automated>
  </verify>
  <done>Both page.tsx files exist; programs/page.tsx uses force-dynamic + Server Component pattern; new/page.tsx is a client component with form state and POST /coach/programs call; TypeScript compiles clean for apps/web</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /fr/coach/programs | Server Component auth check via createServerSupabase() + getUser(); redirects if unauthenticated |
| new/page.tsx POST → /coach/programs | JWT from Supabase session sent in Authorization header; backend validates via authMiddleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Elevation of Privilege | programs/page.tsx | mitigate | force-dynamic + createServerSupabase().auth.getUser() + redirect on null user — no unauthenticated access |
| T-27-06-02 | Tampering | ProgramCard delete action | mitigate | Delete confirmation requires typing "SUPPRIMER"; actual DELETE request sent to backend which validates created_by_coach_id=coachId |
| T-27-06-03 | Information Disclosure | Seed templates | accept | Seed templates (is_template=TRUE, created_by_coach_id=NULL) are intentionally visible to all authenticated coaches |
| T-27-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
TypeScript check on web:
```bash
cd /c/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -v "^$" | head -30
```

Sidebar flip:
```bash
grep -A2 "Programmes" /c/ziko-platform/apps/web/src/components/coach/CoachSidebar.tsx | grep "disabled"
```
Expected: disabled: false

Page files exist:
```bash
ls "/c/ziko-platform/apps/web/src/app/[locale]/(coach)/coach/programs/"
```
Expected: page.tsx, new/ directory
</verification>

<success_criteria>
- CoachSidebar.tsx has disabled: false on "Programmes" entry
- ProgramCard.tsx renders card with context menu (own) or lock icon (seed); delete confirmation reuses typed-confirmation pattern
- programs/page.tsx uses force-dynamic, fetches programs + folders server-side, renders MES TEMPLATES + BIBLIOTHÈQUE ZIKO sections
- programs/new/page.tsx has all 5 form fields with correct UI-SPEC styling; validates name; POSTs to /coach/programs; redirects on success
- TypeScript compiles clean for apps/web
</success_criteria>

<output>
Create `.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-06-SUMMARY.md` when done
</output>
