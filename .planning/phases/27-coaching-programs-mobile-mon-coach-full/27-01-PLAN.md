---
phase: 27
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/coach-sdk/src/schemas/program-exercise.ts
  - packages/coach-sdk/src/schemas/program-session.ts
  - packages/coach-sdk/src/schemas/program-week.ts
  - packages/coach-sdk/src/schemas/index.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02

must_haves:
  truths:
    - "ProgramExerciseSchema, ProgramSessionSchema, and ProgramWeekSchema are importable from @ziko/coach-sdk"
    - "D-04 exact field list is enforced by ProgramExerciseSchema including nullable exercise_id, exercise_name min/max, sets 1-20, reps nullable, duration_seconds nullable, target_rpe 1-10 nullable, target_rir 0-5 nullable, rest_seconds nullable, notes max 300 nullable"
    - "D-05 exact shape is enforced: session_id uuid string, session_name, day_of_week int 1-7, exercises array; week_number int min 1, sessions array"
    - "Schemas are .strict() so unknown keys are rejected"
    - "New schemas do not conflict with existing imported-program.ts ExerciseSchema"
  artifacts:
    - path: packages/coach-sdk/src/schemas/program-exercise.ts
      provides: ProgramExerciseSchema export + ProgramExercise type
      exports: [ProgramExerciseSchema, ProgramExercise]
    - path: packages/coach-sdk/src/schemas/program-session.ts
      provides: ProgramSessionSchema export + ProgramSession type
      exports: [ProgramSessionSchema, ProgramSession]
    - path: packages/coach-sdk/src/schemas/program-week.ts
      provides: ProgramWeekSchema export + ProgramWeek type
      exports: [ProgramWeekSchema, ProgramWeek]
    - path: packages/coach-sdk/src/schemas/index.ts
      provides: re-exports all three new schemas alongside existing exports
  key_links:
    - from: packages/coach-sdk/src/schemas/program-session.ts
      to: packages/coach-sdk/src/schemas/program-exercise.ts
      via: import ProgramExerciseSchema
      pattern: "import.*ProgramExerciseSchema.*program-exercise"
    - from: packages/coach-sdk/src/schemas/program-week.ts
      to: packages/coach-sdk/src/schemas/program-session.ts
      via: import ProgramSessionSchema
      pattern: "import.*ProgramSessionSchema.*program-session"
---

<objective>
Define the locked Zod schema hierarchy for coaching programs before any backend or web work begins. This plan implements D-01 through D-05 from CONTEXT.md.

Purpose: Downstream plans (programsRouter, web editor) import these types and schemas directly. Defining them first prevents inference drift and ensures strict JSONB shape validation at all API boundaries.
Output: Three new schema files in packages/coach-sdk/src/schemas/ plus updated index.ts re-exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-CONTEXT.md
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-RESEARCH.md

<interfaces>
From packages/coach-sdk/src/schemas/imported-program.ts (DO NOT modify or duplicate):
- Exports a lighter ExerciseSchema with fields: name (not exercise_name), reps max 1000, confidence field
- The new D-04 schema is a SEPARATE file with different field names and stricter constraints

From packages/coach-sdk/src/schemas/index.ts (current exports):
- ImportedProgramSchema
- CoachClientLinkSchema, CoachProfileSchema
- CoachInvitationSchema + variants
- CoachLinkPreviewSchema + variants
- ClientSummarySchema, CoachClientTagSchema, CoachClientNoteSchema
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProgramExerciseSchema (D-04)</name>
  <files>packages/coach-sdk/src/schemas/program-exercise.ts</files>
  <read_first>
    - packages/coach-sdk/src/schemas/imported-program.ts — verify field names differ (name vs exercise_name); do not modify
    - packages/coach-sdk/src/schemas/index.ts — understand export pattern to replicate exactly
    - packages/coach-sdk/tsconfig.json — check module resolution (uses .js extensions in imports)
  </read_first>
  <behavior>
    - Schema rejects unknown keys (.strict())
    - exercise_id: nullable uuid string — z.string().uuid().nullable()
    - exercise_name: required, min 1, max 100
    - sets: int, min 1, max 20
    - reps: int, min 1, max 100, nullable
    - duration_seconds: int, min 1, nullable
    - target_rpe: float, min 1, max 10, nullable (not int — per D-03)
    - target_rir: int, min 0, max 5, nullable
    - rest_seconds: int, min 0, max 600, nullable
    - notes: string, max 300, nullable
    - z.infer export as ProgramExercise type
  </behavior>
  <action>
    Create packages/coach-sdk/src/schemas/program-exercise.ts.

    Import z from 'zod'. Export const ProgramExerciseSchema using z.object({...}).strict() with the D-04 field list. Export type ProgramExercise = z.infer from the schema. Use .js extension on any relative imports (TypeScript ESM convention already used in this package). Do NOT export anything from imported-program.ts — these are separate schemas.

    Key distinction from imported-program.ts: field is exercise_name (not name), reps max is 100 (not 1000), no confidence field, .strict() applied.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npx tsc --noEmit -p packages/coach-sdk/tsconfig.json 2>&1 | grep -v "^$" | head -30</automated>
  </verify>
  <done>packages/coach-sdk/src/schemas/program-exercise.ts exists, exports ProgramExerciseSchema and ProgramExercise type, TypeScript compiles without errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ProgramSessionSchema and ProgramWeekSchema (D-05)</name>
  <files>
    packages/coach-sdk/src/schemas/program-session.ts
    packages/coach-sdk/src/schemas/program-week.ts
    packages/coach-sdk/src/schemas/index.ts
  </files>
  <read_first>
    - packages/coach-sdk/src/schemas/program-exercise.ts — just created in Task 1; import from ./program-exercise.js
    - packages/coach-sdk/src/schemas/index.ts — current export pattern; append new exports at end without removing any existing line
  </read_first>
  <behavior>
    ProgramSessionSchema (.strict()):
    - session_id: z.string().uuid() — client-generated, required
    - session_name: z.string().min(1).max(100)
    - day_of_week: z.number().int().min(1).max(7) — 1=Mon ... 7=Sun (D-02)
    - exercises: z.array(ProgramExerciseSchema)
    - type ProgramSession = z.infer

    ProgramWeekSchema (.strict()):
    - week_number: z.number().int().min(1)
    - sessions: z.array(ProgramSessionSchema)
    - type ProgramWeek = z.infer

    index.ts additions (appended, not replacing):
    - export { ProgramExerciseSchema } + type ProgramExercise from ./program-exercise.js
    - export { ProgramSessionSchema } + type ProgramSession from ./program-session.js
    - export { ProgramWeekSchema } + type ProgramWeek from ./program-week.js
  </behavior>
  <action>
    Create packages/coach-sdk/src/schemas/program-session.ts: import ProgramExerciseSchema from './program-exercise.js', define ProgramSessionSchema z.object({session_id, session_name, day_of_week, exercises}).strict(), export type ProgramSession.

    Create packages/coach-sdk/src/schemas/program-week.ts: import ProgramSessionSchema from './program-session.js', define ProgramWeekSchema z.object({week_number, sessions}).strict(), export type ProgramWeek.

    Edit packages/coach-sdk/src/schemas/index.ts: append exports for all three new schemas and their inferred types. Use named re-exports matching the existing pattern in the file (export { ... } from './file.js' and export type { ... } from './file.js' on separate lines).
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npx tsc --noEmit -p packages/coach-sdk/tsconfig.json 2>&1 | grep -v "^$" | head -30</automated>
  </verify>
  <done>All three schema files exist; index.ts exports ProgramExerciseSchema, ProgramSessionSchema, ProgramWeekSchema and their types; TypeScript compiles clean; existing exports in index.ts are untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema import | Downstream code imports these types — no external data crosses here at this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | ProgramExerciseSchema | mitigate | .strict() rejects unknown keys at all API boundaries where the schema is applied |
| T-27-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — zod already a verified dependency |
</threat_model>

<verification>
Run TypeScript type check across the monorepo for the coach-sdk package:

```bash
cd /c/ziko-platform && npx tsc --noEmit -p packages/coach-sdk/tsconfig.json
```

Verify exports are present:

```bash
grep -c "ProgramExerciseSchema\|ProgramSessionSchema\|ProgramWeekSchema" /c/ziko-platform/packages/coach-sdk/src/schemas/index.ts
```

Expected: count >= 3
</verification>

<success_criteria>
- packages/coach-sdk/src/schemas/program-exercise.ts exports ProgramExerciseSchema with all 9 D-04 fields and .strict()
- packages/coach-sdk/src/schemas/program-session.ts exports ProgramSessionSchema with session_id, session_name, day_of_week (1-7), exercises array
- packages/coach-sdk/src/schemas/program-week.ts exports ProgramWeekSchema with week_number, sessions array
- packages/coach-sdk/src/schemas/index.ts exports all three schemas and their inferred types
- npx tsc --noEmit on coach-sdk passes with zero errors
- imported-program.ts ExerciseSchema is unchanged
</success_criteria>

<output>
Create `.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-01-SUMMARY.md` when done
</output>
