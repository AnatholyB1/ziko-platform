---
phase: 33-home-screen-realignment
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - apps/mobile/app/(app)/index.tsx
autonomous: true
requirements:
  - HOME-02
  - HOME-06
  - HOME-07

must_haves:
  truths:
    - "MissionCard renders from useActiveAIProgram hook — shows active ai_generated_programs row or empty state"
    - "HomeWeekStrip renders session completion dots from useWeeklySessions query (done/today/rest states)"
    - "Recent section renders 3 latest workout_sessions from useRecentSessions hook"
    - "All three sections show Skeleton components during isLoading === true"
    - "Empty states render when data arrays are empty or null"
  artifacts:
    - path: "apps/mobile/app/(app)/index.tsx"
      provides: "MissionCard, HomeWeekStrip, Recent section with real data wiring"
      contains: "useActiveAIProgram|useWeeklySessions|useRecentSessions"
  key_links:
    - from: "apps/mobile/app/(app)/index.tsx"
      to: "useActiveAIProgram"
      via: "import from ../../src/hooks/useHomeData"
      pattern: "useActiveAIProgram"
    - from: "apps/mobile/app/(app)/index.tsx"
      to: "useRecentSessions"
      via: "import from ../../src/hooks/useHomeData"
      pattern: "useRecentSessions"
---

<objective>
Wire MissionCard, HomeWeekStrip, and Recent section in index.tsx with real TanStack Query data. This plan modifies index.tsx to replace the STREAK, RECENT, and TODAY fixtures with live hooks for these three sections only. Plan 33-01 must complete before this plan — it creates useHomeData.ts which this plan imports.

Purpose: These three sections require the most structural work: MissionCard parses program_data JSONB, HomeWeekStrip needs a local component (not @ziko/ui), and Recent requires session formatting. Isolating them here prevents context overload in the final assembly plan.

Output: index.tsx gains real data for MissionCard + WeekStrip + Recent. Other sections (FormRing, AICoachInline, QuickLog) are left unchanged for plan 33-04.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/PROJECT.md
@C:/ziko-platform/.planning/phases/33-home-screen-realignment/33-RESEARCH.md
@C:/ziko-platform/.planning/phases/33-home-screen-realignment/33-PATTERNS.md
@C:/ziko-platform/.planning/phases/33-home-screen-realignment/33-UI-SPEC.md
</context>

<interfaces>
<!-- @ziko/ui Skeleton — from packages/ui/src/components.tsx -->
```typescript
// Usage:
import { Skeleton } from '@ziko/ui';
<Skeleton width={90} height={14} borderRadius={6} />
<Skeleton width={160} height={160} borderRadius={80} />
```

<!-- HomeWeekStrip: keep local — @ziko/ui WeekStrip is a date-picker, incompatible with done/today/rest states -->
<!-- The existing local WeekStrip in index.tsx (lines 318-373) implements done/today/rest correctly -->
<!-- Rename it HomeWeekStrip and wire useWeeklySessions -->

<!-- useWeeklySessions return shape — from plan 33-01 design -->
```typescript
// Returns: Array<{ id: string; started_at: string; name: string; total_volume_kg: number | null }>
// The component needs to map over 7 days of the current week and check if started_at falls on each day
```

<!-- useActiveAIProgram return shape -->
```typescript
// Returns: ai_generated_programs row | null
// Row has: id, user_id, goal, program_data (JSONB), is_active, created_at
// program_data is opaque JSONB — inspect before parsing; defensive access required
// MissionCard needs: session name, exercise list (up to 3), estimated duration
```

<!-- UI-SPEC MissionCard sections -->
```
POPULATED: dark card #1C1A17, absolute SVG orange circle decoration
  - "Mission du jour" label: 11px 700 uppercase rgba(255,250,246,0.65)
  - Workout name: 22px 800 #FFFAF6
  - Sub: "{programName} · {N} exos · ~{duration} min" 12px rgba(255,250,246,0.60)
  - Exercise list (first 3): number badge + name + scheme 12px
  - "+{N-3} autres exercices" if more than 3
  - Actions: "Allez, c'est parti !" CTA (#FF5C1A) + chevron-down button

EMPTY: same dark card, centered content
  - barbell-outline 56x56 tile, "Aucun programme actif", description, "Créer un programme IA" CTA

LOADING: dark card with shimmer bars (rgba(255,250,246,0.12))
```

<!-- UI-SPEC Recent row spec -->
```
Row: bg #FFFFFF, border 1px #E2E0DA, borderRadius 12, padding 12, gap 12
  Icon: 36x36 borderRadius 10, bg rgba(255,92,26,0.12), barbell-outline 16px #FF5C1A
  Center: session name 13px 700, sub "{date} · {duration} min"
  Volume: 13px 800 #22C55E (e.g. "5 240 kg")
  Optional PR chip: bg rgba(245,158,11,0.12), "PR" 9.5px 800 #F59E0B
```

<!-- UI-SPEC WeekStrip states -->
```
done:      bg #22C55E, checkmark-outline icon 12px, color #FFFFFF
today:     bg #1C1A17, date number 13px 800, color #FFFFFF
scheduled: bg rgba(255,92,26,0.14), border 1px dashed rgba(255,92,26,0.40), color #FF5C1A
rest:      bg transparent, border 1px solid #E2E0DA, color #6B6963

Day label: 10px 700, opacity 0.75
Inner header: "Semaine · {done}/{goal}" with {done}/{goal} in #FF5C1A
Hint: "+{N} pour atteindre l'objectif" 11px #6B6963
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire MissionCard with useActiveAIProgram + pixel-exact layout</name>
  <files>apps/mobile/app/(app)/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/index.tsx (read full file — understand current MissionCard implementation, exercise display, action buttons, and what imports already exist)
    - apps/mobile/src/hooks/useHomeData.ts (created by plan 33-01 — read it to verify exports: useActiveAIProgram, useWeeklySessions, useRecentSessions)
    - .planning/phases/33-home-screen-realignment/33-UI-SPEC.md (Section 3 "MissionCard Populated State" and "3b MissionCard Empty State" for exact specs)
    - packages/ui/src/components.tsx (Skeleton export — verify import path and props)
  </read_first>
  <action>
    In `apps/mobile/app/(app)/index.tsx`:

    ADD import: `import { useActiveAIProgram } from '../../src/hooks/useHomeData';`
    ADD import: `import { Skeleton } from '@ziko/ui';` (if not already present)

    KEEP the existing `useWorkoutStore` import for now — only remove it in plan 33-04 after all sections are migrated. This plan ONLY touches MissionCard.

    REPLACE the current MissionCard JSX (find the dark card section that renders workout program data). The new implementation must:

    At component top level, call `const { data: activeProgram, isLoading: programLoading } = useActiveAIProgram();`

    Parse program_data defensively — `activeProgram?.program_data` may be null or have any shape. Safe access pattern:
    - `const sessions: any[] = activeProgram?.program_data?.sessions ?? activeProgram?.program_data?.workouts ?? [];`
    - `const todaySession = sessions[0] ?? null;` (show first session as today's mission)
    - `const exercises: any[] = todaySession?.exercises ?? todaySession?.sets ?? [];`
    - `const sessionName: string = todaySession?.name ?? todaySession?.title ?? activeProgram?.goal ?? 'Séance du jour';`
    - `const programName: string = activeProgram?.goal ?? 'Programme IA';`
    - `const duration: number = todaySession?.duration_minutes ?? todaySession?.duration ?? 45;`

    LOADING STATE (programLoading === true): render the dark card with `bg: '#1C1A17'`, borderRadius 12, padding 18, and 3 shimmer bars using inline style `backgroundColor: 'rgba(255,250,246,0.12)'` with height 14 and borderRadius 6. No Skeleton component for dark card — use inline View shimmer.

    EMPTY STATE (activeProgram === null): render the dark card per UI-SPEC section 3b:
    - barbell-outline icon tile 56×56, borderRadius 14, bg `rgba(255,92,26,0.15)`, icon `barbell-outline` 28px color `#FF8E5A`
    - "Aucun programme actif" text: fontSize 18, fontWeight '800', color '#FFFAF6', marginTop 14
    - Description text: fontSize 13, color `rgba(255,250,246,0.60)`, marginTop 6, textAlign 'center', lineHeight 18
    - CTA: "Créer un programme IA", paddingHorizontal 20, paddingVertical 12, borderRadius 9999, bg '#FF5C1A', color '#FFFFFF', fontSize 13.5, fontWeight '700'. onPress: `router.push('/(app)/(plugins)/ai-programs/dashboard' as any)`

    POPULATED STATE: render per UI-SPEC section 3:
    - Card: bg '#1C1A17', borderRadius 12, overflow 'hidden', position 'relative', no shadow
    - Decoration: absolute Svg with orange circle (cx=320, cy=-30, r=180, fill via Defs LinearGradient or a single orange circle with opacity 0.14)
    - Content at relative z-index: padding 18
    - "Mission du jour" label: fontSize 11, fontWeight '700', textTransform 'uppercase', letterSpacing 0.10 * 11, color `rgba(255,250,246,0.65)`
    - Session name: fontSize 22, fontWeight '800', lineHeight 22 * 1.1, color '#FFFAF6', marginTop 6, maxWidth: '85%'
    - Sub line: `${programName} · ${exercises.length} exos · ~${duration} min`, fontSize 12, color `rgba(255,250,246,0.60)`, marginTop 4
    - Exercise list (exercises.slice(0,3)): each row has number badge (18×18, borderRadius 5, bg `rgba(255,92,26,0.18)`, text color `#FFB287`, fontSize 10, fontWeight '800'), name text (flex 1, color `rgba(255,250,246,0.92)`, fontSize 12), and scheme text (color `rgba(255,250,246,0.55)`, fontSize 11)
    - If `exercises.length > 3`: render `+${exercises.length - 3} autres exercices`, fontSize 11, color `rgba(255,250,246,0.50)`, paddingLeft 26, marginTop 2
    - Action row (height 48, borderRadius 10): primary CTA bg '#FF5C1A', "Allez, c'est parti !" with play-outline icon 14px. onPress: `router.push('/(app)/workout' as any)`. Expand button: 48×48, bg `rgba(255,250,246,0.10)`, chevron-down-outline 16px.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -c 'error TS' -- PASS condition: output equals 0</automated>
  </verify>
  <done>
    - index.tsx imports useActiveAIProgram from ../../src/hooks/useHomeData
    - MissionCard renders Skeleton/shimmer when programLoading
    - MissionCard renders empty state when activeProgram === null
    - MissionCard renders populated state when program exists
    - No `workout_programs` or `workoutStore.programs` reference in the MissionCard section
    - TypeScript compiles without errors (grep -c 'error TS' output is 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire HomeWeekStrip and Recent section with real data</name>
  <files>apps/mobile/app/(app)/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/index.tsx (read lines 318–450 — find existing local WeekStrip component and Recent section; understand current structure after Task 1 has been applied)
    - .planning/phases/33-home-screen-realignment/33-UI-SPEC.md (Section 7 "WeekStrip" and Section 8 "Recent" for exact visual spec)
    - .planning/phases/33-home-screen-realignment/33-PATTERNS.md (HomeWeekStrip decision: keep local, not @ziko/ui; and card shadow token section)
  </read_first>
  <action>
    In `apps/mobile/app/(app)/index.tsx`:

    ADD imports: `import { useWeeklySessions, useRecentSessions, HOME_DEFAULTS, parseWorkoutFrequency } from '../../src/hooks/useHomeData';`

    WEEKSTRIP section:
    At component top level add:
    - `const { data: weeklySessions = [], isLoading: weekLoading } = useWeeklySessions();`
    - `const { data: profile } = useProfile();` (add useProfile to import if not already there from Task 1)

    RENAME the existing local `WeekStrip` function to `HomeWeekStrip` (it is defined inline in index.tsx — this is the correct component to keep, not replace).

    UPDATE the HomeWeekStrip usage: replace the current call that passes workoutStore data with:
    - Compute `doneDates`: set of ISO date strings (YYYY-MM-DD) extracted from `weeklySessions.map(s => s.started_at.split('T')[0])`
    - Compute `weeklyGoal = parseWorkoutFrequency(profile?.workout_frequency)`
    - Compute `doneCount = weeklySessions.length` (sessions this week)
    - Pass `doneDates`, `doneCount`, `weeklyGoal` to HomeWeekStrip (update its props if needed)

    UPDATE HomeWeekStrip internals to use `doneDates: Set<string>` (or convert from array) and determine state per day:
    - `done`: `doneDates.has(dayISOString)` AND day is before today
    - `today`: day equals today's ISO string
    - `rest`: all other days (omit `scheduled` state per research guidance — render as rest)

    INNER HEADER ROW inside the HomeWeekStrip card:
    - Left: "Semaine · {doneCount}/{weeklyGoal}", where `{doneCount}/{weeklyGoal}` span has color '#FF5C1A', fontSize 12, fontWeight '700'
    - Right: if weeklyGoal > doneCount, show "+{weeklyGoal - doneCount} pour atteindre l'objectif", fontSize 11, color '#6B6963'

    VISUAL SPEC per UI-SPEC Section 7:
    - done day: bg '#22C55E', checkmark-outline icon 12px, color '#FFFFFF'
    - today day: bg '#1C1A17', date number 13px fontWeight '800', color '#FFFFFF'
    - rest day: bg transparent, border 1px solid '#E2E0DA', color '#6B6963', date number 13px
    - Day abbreviation label above each cell: 10px fontWeight '700', color '#6B6963' opacity 0.75

    SECTION HEADER "Cette semaine": update to new section header style: fontSize 11, fontWeight '700', letterSpacing 0.66, textTransform 'uppercase', color '#6B6963', marginBottom 10.

    RECENT section:
    At component top level add: `const { data: recentSessions = [], isLoading: recentLoading } = useRecentSessions();`

    SECTION HEADER ROW (space-between):
    - Left: "Récent" section title (same uppercase 11px style)
    - Right: "Tout voir" link, fontSize 11, color '#FF5C1A', fontWeight '600', onPress: `router.push('/(app)/workout/history' as any)`

    LOADING STATE (recentLoading): render 3 skeleton rows — each row has a 36×36 circle Skeleton + two Skeleton bars.

    EMPTY STATE (recentSessions.length === 0): render centered view with barbell-outline icon 40px color '#6B6963', "Aucune séance récente" 14px 700, "Lance ta première séance via l'onglet Séance." 12px color '#6B6963', paddingVertical 24.

    POPULATED: for each session in recentSessions (max 3):
    - Card: bg '#FFFFFF', border 1px '#E2E0DA', borderRadius 12, shadow (opacity 0.06, radius 8, elevation 2), padding 12, flexDirection 'row', alignItems 'center', gap 12
    - Icon tile: 36×36, borderRadius 10, bg `rgba(255,92,26,0.12)`, barbell-outline 16px color '#FF5C1A'
    - Center (flex 1, minWidth 0): name row with session name 13px 700 color '#1C1A17' numberOfLines 1. Sub: `${format(new Date(session.started_at), 'd MMM')} · ${Math.round((session.duration_seconds ?? 0) / 60)} min` fontSize 11 color '#6B6963'.
    - Volume (right): `${session.total_volume_kg ? session.total_volume_kg.toLocaleString('fr-FR') + ' kg' : '—'}`, fontSize 13, fontWeight '800', color '#22C55E'

    Remove any reference to `workoutStore.recentSessions` or the `RECENT` fixture constant in these sections. LEAVE all other sections (FormRing, AICoachInline, QuickLog, Header) untouched — they are migrated in plan 33-04.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -c 'error TS' -- PASS condition: output equals 0</automated>
    <automated>grep -c "from 'date-fns'" C:/ziko-platform/apps/mobile/app/(app)/index.tsx -- PASS condition: output >= 1</automated>
  </verify>
  <done>
    - HomeWeekStrip uses weeklySessions from useWeeklySessions (not workoutStore)
    - WeekStrip inner header shows "Semaine · {done}/{goal}" with orange fraction
    - Recent section uses useRecentSessions (not workoutStore.recentSessions or RECENT fixture)
    - Loading skeletons present for both sections
    - Empty states present for both sections
    - Section headers use: fontSize 11, fontWeight '700', letterSpacing 0.66, textTransform 'uppercase', color '#6B6963'
    - TypeScript compiles without errors (grep -c 'error TS' output is 0)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → Supabase | RLS enforces user_id ownership on workout_sessions and ai_generated_programs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Information Disclosure | ai_generated_programs query | mitigate | RLS policy `eq('user_id', auth.uid())` enforced at DB level; `enabled: !!userId` prevents unauthenticated reads |
| T-33-03-02 | Tampering | program_data JSONB parsing | accept | Defensive access via `?.sessions ?? ?.workouts ?? []` — no eval or dynamic execution; display only |
| T-33-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
TypeScript compile check:

```bash
cd C:/ziko-platform && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -c 'error TS'
```

PASS condition: output equals 0 (zero TypeScript errors in the project).

Fixture removal checks:
- `grep -v '^[[:space:]]*//' apps/mobile/app/(app)/index.tsx | grep -c "const RECENT\|RECENT\["` returns 0
- `grep -c "useActiveAIProgram\|useWeeklySessions\|useRecentSessions" apps/mobile/app/(app)/index.tsx` returns at least 3
- `grep -c "\.maybeSingle\(\)" apps/mobile/src/hooks/useHomeData.ts` returns at least 2 (verified in plan 33-01)
</verification>

<success_criteria>
- MissionCard wired to useActiveAIProgram with populated/empty/loading states
- HomeWeekStrip renamed and wired to useWeeklySessions
- Recent section wired to useRecentSessions with loading/empty/populated states
- RECENT fixture and workout_programs references removed from these three sections
- Section headers updated to 11px uppercase style
- TypeScript compiles without errors (grep -c 'error TS' output is 0)
</success_criteria>

<output>
Create `.planning/phases/33-home-screen-realignment/33-03-SUMMARY.md` when done.
</output>
