---
phase: 17-db-foundation-model-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/src/config/models.ts
  - backend/api/src/routes/ai.ts
  - backend/api/src/tools/ai-programs.ts
  - backend/api/src/routes/pantry-recipes.ts
autonomous: true
requirements: [COST-01]
must_haves:
  truths:
    - "No inline anthropic() model ID strings exist in backend route or tool files"
    - "A centralized models.ts file exports both AGENT_MODEL and VISION_MODEL"
    - "All AI routes and tools import AGENT_MODEL from the shared config"
    - "Zero occurrences of claude-3-haiku-20240307 exist anywhere in the codebase source files"
  artifacts:
    - path: "backend/api/src/config/models.ts"
      provides: "Centralized model constants"
      exports: ["AGENT_MODEL", "VISION_MODEL"]
    - path: "backend/api/src/routes/ai.ts"
      provides: "AI chat routes importing from shared config"
      contains: "import { AGENT_MODEL } from '../config/models.js'"
    - path: "backend/api/src/tools/ai-programs.ts"
      provides: "AI programs tool importing from shared config"
      contains: "import { AGENT_MODEL } from '../config/models.js'"
    - path: "backend/api/src/routes/pantry-recipes.ts"
      provides: "Pantry recipes route importing from shared config"
      contains: "import { AGENT_MODEL } from '../config/models.js'"
  key_links:
    - from: "backend/api/src/routes/ai.ts"
      to: "backend/api/src/config/models.ts"
      via: "named import AGENT_MODEL"
      pattern: "import.*AGENT_MODEL.*from.*config/models"
    - from: "backend/api/src/tools/ai-programs.ts"
      to: "backend/api/src/config/models.ts"
      via: "named import AGENT_MODEL"
      pattern: "import.*AGENT_MODEL.*from.*config/models"
---

<objective>
Eliminate the deprecated Haiku model ID from the codebase (grep audit) and centralize all AI model constants into a shared config file, preparing the VISION_MODEL constant for Phase 19.

Purpose: The old `claude-3-haiku-20240307` model retires April 19, 2026. Centralizing model IDs prevents drift across 3+ files when IDs change. VISION_MODEL enables Phase 19 Haiku vision endpoint at 70% cost reduction (COST-01).
Output: `backend/api/src/config/models.ts` with AGENT_MODEL + VISION_MODEL; 3 files updated to import from it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-db-foundation-model-fix/17-CONTEXT.md

<interfaces>
<!-- Current inline model references to replace -->

From backend/api/src/routes/ai.ts (line 51):
```typescript
const AGENT_MODEL = anthropic('claude-sonnet-4-20250514');
```

From backend/api/src/tools/ai-programs.ts (lines 68, 169):
```typescript
model: anthropic('claude-sonnet-4-20250514'),
```

From backend/api/src/routes/pantry-recipes.ts (line 125):
```typescript
model: anthropic('claude-sonnet-4-20250514'),
```

All three files currently import `anthropic` from `@ai-sdk/anthropic` directly.
After this plan, only `models.ts` imports `anthropic` — the other files import the pre-built constants.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit + create shared model constants file</name>
  <files>backend/api/src/config/models.ts</files>
  <read_first>
    - backend/api/src/routes/ai.ts (current AGENT_MODEL definition at line 51)
    - backend/api/src/tools/ai-programs.ts (inline anthropic() calls at lines 68, 169)
    - backend/api/src/routes/pantry-recipes.ts (inline anthropic() call at line 125)
  </read_first>
  <action>
**Step 1 — Grep audit (per D-08, first commit of the phase):**
Run `grep -rn "claude-3-haiku-20240307" backend/ apps/ plugins/ packages/ supabase/` to confirm zero matches in source code. The old ID should only exist in `.planning/` docs. If any source file matches, replace with `claude-haiku-4-5-20251001` immediately.

**Step 2 — Create `backend/api/src/config/models.ts` (per D-09, D-10):**

Create directory `backend/api/src/config/` if it does not exist.

Write the file with exactly this content:

```typescript
import { anthropic } from '@ai-sdk/anthropic';

// ─── AI Model Constants ─────────────────────────────────────
// Centralized model management. All AI routes import from here.
// When a model ID changes, update ONE file.

// Orchestrator agent — all AI chat routes
export const AGENT_MODEL = anthropic('claude-sonnet-4-20250514');

// Vision model — food scan (Phase 19 COST-01)
// Replaces deprecated claude-3-haiku-20240307 (retires April 19, 2026)
export const VISION_MODEL = anthropic('claude-haiku-4-5-20251001');
```

This file is the ONLY place in the codebase that calls `anthropic()` with a model ID string.
  </action>
  <verify>
    <automated>grep -rn "claude-3-haiku-20240307" backend/ apps/ plugins/ packages/ supabase/ && echo "FAIL: old model ID found" || echo "PASS: no old model ID"</automated>
    <automated>grep -c "AGENT_MODEL\|VISION_MODEL" backend/api/src/config/models.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/api/src/config/models.ts` exists
    - File contains `export const AGENT_MODEL = anthropic('claude-sonnet-4-20250514')`
    - File contains `export const VISION_MODEL = anthropic('claude-haiku-4-5-20251001')`
    - `grep -rn "claude-3-haiku-20240307" backend/ apps/ plugins/ packages/ supabase/` returns zero matches
  </acceptance_criteria>
  <done>Shared model constants file exists with both AGENT_MODEL and VISION_MODEL exported. No deprecated Haiku model ID in codebase source files.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all inline model references to shared imports</name>
  <files>backend/api/src/routes/ai.ts, backend/api/src/tools/ai-programs.ts, backend/api/src/routes/pantry-recipes.ts</files>
  <read_first>
    - backend/api/src/config/models.ts (just created in Task 1)
    - backend/api/src/routes/ai.ts (full file — need to see all imports and the AGENT_MODEL line)
    - backend/api/src/tools/ai-programs.ts (full file — need to find both anthropic() calls)
    - backend/api/src/routes/pantry-recipes.ts (full file — need to find the anthropic() call)
  </read_first>
  <action>
**File 1: `backend/api/src/routes/ai.ts`**
- Remove `import { anthropic } from '@ai-sdk/anthropic';` (line 4) — anthropic is no longer called directly in this file
- Add `import { AGENT_MODEL } from '../config/models.js';` in the import block
- Delete line 51: `const AGENT_MODEL = anthropic('claude-sonnet-4-20250514');` — the constant now comes from the import
- The rest of the file uses `AGENT_MODEL` variable by name and needs no changes

**File 2: `backend/api/src/tools/ai-programs.ts`**
- Remove `import { anthropic } from '@ai-sdk/anthropic';` (line 2)
- Add `import { AGENT_MODEL } from '../config/models.js';` in the import block
- Replace line 68: `model: anthropic('claude-sonnet-4-20250514'),` with `model: AGENT_MODEL,`
- Replace line 169: `model: anthropic('claude-sonnet-4-20250514'),` with `model: AGENT_MODEL,`

**File 3: `backend/api/src/routes/pantry-recipes.ts`**
- Remove `import { anthropic } from '@ai-sdk/anthropic';` (line 3)
- Add `import { AGENT_MODEL } from '../config/models.js';` in the import block
- Replace line 125: `model: anthropic('claude-sonnet-4-20250514'),` with `model: AGENT_MODEL,`

**Verification:** Run `npm run type-check` from `backend/api/` to confirm all imports resolve correctly.
  </action>
  <verify>
    <automated>cd backend/api && npx tsc --noEmit 2>&1 | head -20</automated>
    <automated>grep -rn "anthropic('" backend/api/src/routes backend/api/src/tools | grep -v node_modules</automated>
  </verify>
  <acceptance_criteria>
    - `backend/api/src/routes/ai.ts` contains `import { AGENT_MODEL } from '../config/models.js'`
    - `backend/api/src/routes/ai.ts` does NOT contain `import { anthropic } from '@ai-sdk/anthropic'`
    - `backend/api/src/routes/ai.ts` does NOT contain `const AGENT_MODEL = anthropic(`
    - `backend/api/src/tools/ai-programs.ts` contains `import { AGENT_MODEL } from '../config/models.js'`
    - `backend/api/src/tools/ai-programs.ts` does NOT contain `anthropic('claude-sonnet`
    - `backend/api/src/routes/pantry-recipes.ts` contains `import { AGENT_MODEL } from '../config/models.js'`
    - `backend/api/src/routes/pantry-recipes.ts` does NOT contain `anthropic('claude-sonnet`
    - `grep -rn "anthropic('" backend/api/src/routes backend/api/src/tools` returns zero matches (all inline calls eliminated)
    - `npx tsc --noEmit` in `backend/api/` exits with code 0
  </acceptance_criteria>
  <done>All three files import AGENT_MODEL from shared config. Zero inline anthropic() calls remain in routes/ or tools/. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `grep -rn "claude-3-haiku-20240307" backend/ apps/ plugins/ packages/ supabase/` — zero matches
2. `grep -rn "anthropic('" backend/api/src/routes backend/api/src/tools` — zero matches (all centralized)
3. `grep -c "export const" backend/api/src/config/models.ts` — returns 2 (AGENT_MODEL + VISION_MODEL)
4. `cd backend/api && npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- Deprecated `claude-3-haiku-20240307` model ID appears nowhere in source code
- `backend/api/src/config/models.ts` exports AGENT_MODEL and VISION_MODEL
- All 3 consumer files (`ai.ts`, `ai-programs.ts`, `pantry-recipes.ts`) import from shared config
- Zero inline `anthropic()` calls remain in routes or tools directories
- TypeScript type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-db-foundation-model-fix/17-01-SUMMARY.md`
</output>
