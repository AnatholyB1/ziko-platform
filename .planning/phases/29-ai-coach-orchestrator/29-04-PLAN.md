---
phase: 29-ai-coach-orchestrator
plan: 04
type: execute
wave: 4
depends_on:
  - 29-03
files_modified:
  - apps/web/src/components/coach/AlertsPanel.tsx
  - apps/web/src/components/coach/AlertCard.tsx
  - apps/web/src/components/coach/AdaptWithAIButton.tsx
  - apps/web/src/components/coach/CoachSidebar.tsx
  - apps/web/src/app/[locale]/(coach)/coach/dashboard/page.tsx
  - apps/web/src/app/[locale]/(coach)/coach/programs/ProgramsClient.tsx
autonomous: true
requirements:
  - AIC-06
  - AIC-07
must_haves:
  truths:
    - "Dashboard shows AlertsPanel below WelcomeCard — no-alerts state renders green shield icon + message"
    - "With unread alerts, AlertsPanel lists alert cards with severity dot, client name, summary, 'Ouvrir le chat →' link"
    - "Marking an alert as read removes it from the list with a GSAP collapse animation"
    - "CoachSidebar 'IA' nav item is enabled (clickable) and shows an orange badge when unread > 0"
    - "ProgramCard has 'Adapter avec l'IA' button for is_template=TRUE programs"
    - "Clicking 'Adapter avec l'IA' navigates to /{locale}/coach/ai?template={programId}"
  artifacts:
    - path: "apps/web/src/components/coach/AlertsPanel.tsx"
      provides: "Alerts list with dismiss/expand — fetches from coach_alerts via backend"
      contains: "'use client'"
    - path: "apps/web/src/components/coach/AlertCard.tsx"
      provides: "Individual alert card with severity dot, summary, action buttons"
      contains: "Ouvrir le chat"
    - path: "apps/web/src/components/coach/AdaptWithAIButton.tsx"
      provides: "Outlined secondary button navigating to /coach/ai with template query param"
      contains: "IoSparklesOutline"
    - path: "apps/web/src/components/coach/CoachSidebar.tsx"
      provides: "Updated sidebar — 'IA' nav item enabled, alert badge prop"
      contains: "disabled: false"
  key_links:
    - from: "apps/web/src/app/[locale]/(coach)/coach/dashboard/page.tsx"
      to: "apps/web/src/components/coach/AlertsPanel.tsx"
      via: "<AlertsPanel initialAlerts={alerts ?? []} coachId={user.id} accessToken={accessToken} />"
      pattern: "AlertsPanel"
    - from: "apps/web/src/app/[locale]/(coach)/coach/programs/ProgramsClient.tsx"
      to: "apps/web/src/components/coach/AdaptWithAIButton.tsx"
      via: "rendered conditionally when program.is_template === true"
      pattern: "AdaptWithAIButton"
---

<objective>
Add the coach alerts panel to the dashboard, enable the "IA" sidebar nav item with alert badge, and add the "Adapter avec l'IA" button to the programs page.

Purpose: Surfaces background monitoring results (from the daily cron) in the coach's dashboard, and enables the AI deep-link entry point from program templates.

Output: AlertsPanel + AlertCard + AdaptWithAIButton components; CoachSidebar updated; dashboard and ProgramsClient updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-ai-coach-orchestrator/29-UI-SPEC.md
@.planning/phases/29-ai-coach-orchestrator/29-PATTERNS.md

<interfaces>
<!-- Existing files the executor must read first. -->

From apps/web/src/components/coach/CoachSidebar.tsx (current — full file):
```typescript
// Line 19 — change disabled: true → false
{ label: 'IA', href: '/fr/coach/ai', icon: IoSparklesOutline, disabled: true },
// NavItem component accepts a disabled prop — see NavItem.tsx
```

From apps/web/src/app/[locale]/(coach)/coach/dashboard/page.tsx (current pattern):
```typescript
// After WelcomeCard import, add AlertsPanel import
// Server-side fetch:
const { count: unreadAlertCount } = await supabase
  .from('coach_alerts').select('id', {count:'exact', head:true})
  .eq('coach_id', user.id).eq('is_read', false);
// Pass to JSX:
<AlertsPanel initialAlerts={alerts ?? []} coachId={user.id} accessToken={session?.access_token ?? ''} apiUrl={process.env.NEXT_PUBLIC_API_URL ?? ''} />
// Also pass unreadAlertCount to CoachSidebar via layout — but CoachSidebar is in the layout, not this page. Pass as data attribute or use a separate fetch in layout.
// Simplest: pass unreadAlertCount via page data only; sidebar badge will be updated separately.
```

From apps/web/src/components/coach/NavItem.tsx (badge support):
// NavItem renders a link or disabled span. Check if it accepts badge/count props.
// If not, update NavItem to accept optional `badgeCount?: number` and render accordingly.

From apps/web/src/app/[locale]/(coach)/coach/programs/ProgramsClient.tsx:
```typescript
// handleAdaptWithAI(programId: string) { router.push(`/${locale}/coach/ai?template=${programId}`); }
// In ProgramCard action row, conditionally render <AdaptWithAIButton> when is_template=true
```

Alert types and severity:
// alert_type: 'missed_sessions' | 'sleep_drop' | 'mood_decline' | 'rpe_inflation'
// severity: 'low' | 'medium' | 'high' — dot colors: #EF4444 high / #F59E0B medium / #EAB308 low

GSAP dismiss animation (from 29-UI-SPEC.md §Motion Design):
gsap.to(alertCard, { opacity: 0, height: 0, marginBottom: 0, duration: 0.2, ease: 'power2.in', onComplete: remove })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AlertsPanel + AlertCard + CoachSidebar + dashboard page update</name>
  <files>
    apps/web/src/components/coach/AlertsPanel.tsx
    apps/web/src/components/coach/AlertCard.tsx
    apps/web/src/components/coach/CoachSidebar.tsx
    apps/web/src/app/[locale]/(coach)/coach/dashboard/page.tsx
  </files>
  <read_first>
    apps/web/src/components/coach/CoachSidebar.tsx (full file — current disabled:true to flip)
    apps/web/src/components/coach/NavItem.tsx (check if badgeCount prop exists)
    apps/web/src/app/[locale]/(coach)/coach/dashboard/page.tsx (full file — add AlertsPanel)
    .planning/phases/29-ai-coach-orchestrator/29-UI-SPEC.md §Screen 2 (AlertsPanel spec, all states)
  </read_first>
  <action>
    **AlertCard.tsx** (`'use client'`):
    - Props: `alert: CoachAlert, onDismiss: (id: string) => void, apiUrl: string, accessToken: string`
    - Import `CoachAlert` type from `@/types/coach` (or define inline if the type package isn't set up — use `interface CoachAlert { id:string; client_id:string; alert_type:string; severity:'low'|'medium'|'high'; summary:string; is_read:boolean; created_at:string; clientName?:string }`)
    - Import: `useRef` from `react`; `gsap` from `gsap`; `IoOpenOutline` from `react-icons/io5`
    - Severity dot: 8px circle — colors: `#EF4444` for 'high', `#F59E0B` for 'medium', `#EAB308` for 'low'
    - Alert type label mapping: `missed_sessions`→"Séances manquées", `sleep_drop`→"Chute de sommeil", `mood_decline`→"Humeur en baisse", `rpe_inflation`→"RPE en hausse"
    - Card container ref `cardRef`; dismiss handler: `gsap.to(cardRef.current, { opacity: 0, height: 0, marginBottom: 0, duration: 0.2, ease: 'power2.in', onComplete: () => onDismiss(alert.id) })`; ALSO `PATCH ${apiUrl}/coach/ai/alerts/${alert.id}/read` with Bearer token (fire-and-forget)
    - Layout per UI-SPEC §AlertsPanel — With Alerts: `<div ref={cardRef} className="border border-border rounded-lg p-4 mb-3 last:mb-0 alert-card">`
    - Header row: severity dot + client name (14px 600 #1C1A17) + `·` + alert type label (14px 400 #6B6963) + timestamp (12px 400 #6B6963 right-aligned)
    - Summary text: 14px 400 #1C1A17, mt-1, `line-clamp-2`
    - Footer row: `<a href={\`/fr/coach/ai?client=${alert.client_id}\`} className="text-sm text-primary font-semibold">Ouvrir le chat →</a>` + `<button onClick={handleDismiss} className="text-sm text-[#6B6963] hover:text-[#1C1A17] ml-auto">Marquer lu</button>`

    **AlertsPanel.tsx** (`'use client'`):
    - Props: `initialAlerts: CoachAlert[], coachId: string, accessToken: string, apiUrl: string`
    - State: `alerts: CoachAlert[]` (initialized from `initialAlerts`), `showAll: boolean` (false = show first 3)
    - Import: `useState, useEffect, useRef` from `react`; `gsap` from `gsap`; `IoShieldCheckmarkOutline` from `react-icons/io5`; `AlertCard` from `./AlertCard`
    - Panel entrance GSAP on mount: `gsap.from('.alerts-panel', { opacity: 0, y: 16, duration: 0.2, ease: 'power2.out' })` + stagger cards: `gsap.from('.alert-card', { opacity: 0, x: -8, duration: 0.2, stagger: 0.07, ease: 'power2.out' })`
    - Dismiss handler: removes alert from local state (the card's own GSAP animation handles the visual collapse before calling this)
    - No-alerts state (per UI-SPEC): `<div className="bg-white rounded-2xl p-6 border border-border shadow-sm flex items-center gap-3"><IoShieldCheckmarkOutline size={20} color="#22C55E" /><span className="text-sm text-[#6B6963]">Tous vos clients sont dans de bonnes conditions.</span></div>`
    - With-alerts state: panel card with header ("Alertes coaching" title + unread badge), list of AlertCard components (show 3 max if !showAll), overflow indicator ("+ N autres alertes · Afficher tout ↓"), footer ("Tout marquer comme lu" + "Voir dans l'IA Coach →")
    - "Afficher tout" expand GSAP: `gsap.from('.overflow-alerts', { height: 0, opacity: 0, duration: 0.25, ease: 'power2.inOut' })`
    - "Tout marquer comme lu": calls `POST ${apiUrl}/coach/ai/alerts/read-all` with Bearer token (fire-and-forget), then clears alerts state
    - Unread badge GSAP on mount if count > 0: `gsap.fromTo('.alert-badge', { scale: 0.5, opacity: 0 }, { scale: 1, opacity: 1, duration: 0.3, ease: 'back.out(1.7)' })`

    **CoachSidebar.tsx** — update:
    - Read full current file first
    - Change `disabled: true` → `disabled: false` for the 'IA' nav item
    - Add `unreadAlertCount?: number` prop to `CoachSidebar` function signature
    - Pass `badgeCount={unreadAlertCount}` to the NavItem for 'IA' nav item (or add it to the NAV_ITEMS entry as a dynamic property)
    - Check NavItem.tsx — if it does not accept a `badgeCount` prop, update NavItem to conditionally render the orange badge: when `badgeCount && badgeCount > 0`, render `<span className="absolute -top-1 -right-1 w-5 h-5 bg-primary text-white text-xs font-semibold rounded-full flex items-center justify-center">{badgeCount}</span>` on the nav item link container (relative positioning)

    **dashboard/page.tsx** — update:
    - Add `import { AlertsPanel } from '@/components/coach/AlertsPanel';`
    - Server-side fetch unread alerts: `const { data: alerts } = await supabase.from('coach_alerts').select('id, client_id, alert_type, severity, summary, is_read, created_at').eq('coach_id', user.id).eq('is_read', false).order('created_at', {ascending: false}).limit(10);`
    - Add `<AlertsPanel initialAlerts={alerts ?? []} coachId={user.id} accessToken={session?.access_token ?? ''} apiUrl={process.env.NEXT_PUBLIC_API_URL ?? ''} />` below `<WelcomeCard>` in the flex col
    - Also get `session` for the access token: add `const { data: { session } } = await supabase.auth.getSession();`
    - Pass `unreadAlertCount={alerts?.length ?? 0}` to CoachSidebar — but CoachSidebar is in the layout, not the page. The simplest fix: pass `unreadAlertCount` as a searchParam or cookie is too complex. Instead, CoachSidebar should fetch its own unread count client-side. Update CoachSidebar to accept an `initialUnreadCount?: number` prop passed through the layout OR make it self-fetch. Given the sidebar is in the layout server component, the dashboard page can set a cookie via `cookies().set('unread_alerts', String(count))` and the layout reads it. Simplest approach for v1: CoachSidebar renders the badge only when explicitly passed; the dashboard layout gets unread count from its own server-side fetch. Document this as a known limitation (badge only visible on dashboard load, updates on next navigation).
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep "AlertsPanel\|AlertCard\|CoachSidebar" | head -20</automated>
  </verify>
  <done>TypeScript clean. CoachSidebar.tsx no longer contains `disabled: true` for the IA item. AlertsPanel.tsx contains "Tous vos clients sont dans de bonnes conditions". AlertCard.tsx contains "Ouvrir le chat". Dashboard page.tsx contains "AlertsPanel".</done>
</task>

<task type="auto">
  <name>Task 2: AdaptWithAIButton + ProgramsClient update</name>
  <files>
    apps/web/src/components/coach/AdaptWithAIButton.tsx
    apps/web/src/app/[locale]/(coach)/coach/programs/ProgramsClient.tsx
  </files>
  <read_first>
    apps/web/src/app/[locale]/(coach)/coach/programs/ProgramsClient.tsx (full file — current action handlers, ProgramCard usage)
    apps/web/src/components/coach/ProgramCard.tsx (full interface — props and action handlers)
    .planning/phases/29-ai-coach-orchestrator/29-UI-SPEC.md §Screen 3 (AdaptWithAIButton spec)
  </read_first>
  <action>
    **AdaptWithAIButton.tsx** (`'use client'`):
    - Props: `programId: string, programName: string, locale: string, disabled?: boolean`
    - Import: `useState, useRef` from `react`; `useRouter` from `next/navigation`; `gsap` from `gsap`; `IoSparklesOutline, IoRefreshOutline` from `react-icons/io5`
    - State: `isNavigating: boolean` (false)
    - Default state (per UI-SPEC §Screen 3): `<button className="flex items-center gap-2 border border-primary text-primary bg-transparent rounded-lg text-sm font-semibold h-10 px-4 py-2 hover:bg-[#FF5C1A]/5 transition-colors disabled:opacity-40 disabled:cursor-not-allowed adapt-btn" disabled={disabled || isNavigating} onClick={handleAdapt}>`
    - Content when !isNavigating: `<IoSparklesOutline size={16} />` + "Adapter avec l'IA"
    - Content when isNavigating: `<IoRefreshOutline size={16} className="animate-spin" />` + "Ouverture..."
    - `handleAdapt`: GSAP press feedback `gsap.to('.adapt-btn', {scale:0.97, duration:0.1, yoyo:true, repeat:1, ease:'power3.out'})`, set `isNavigating=true`, then `router.push(\`/${locale}/coach/ai?template=${programId}\`)`
    - Disabled tooltip: `title="Liez d'abord un client pour adapter ce programme"` on button when disabled

    **ProgramsClient.tsx** — update:
    - Add import: `import { AdaptWithAIButton } from '@/components/coach/AdaptWithAIButton';`
    - Add `import { IoSparklesOutline } from 'react-icons/io5';` to existing imports (if not already present)
    - Add `handleAdaptWithAI(id: string, name: string)` function: `router.push(\`/${locale}/coach/ai?template=${id}\`)`
    - Add `onAdaptWithAI` prop to `ProgramCardProps` usage in ProgramCard render: `onAdaptWithAI={handleAdaptWithAI}`
    - In ProgramCard — add `onAdaptWithAI?: (id:string, name:string) => void` to props interface
    - In ProgramCard's action row (the `···` menu or inline buttons), add AdaptWithAIButton conditionally: when `program.is_template === true` (or `isSeed === false && is_template`), render `<AdaptWithAIButton programId={id} programName={name} locale={locale prop} />` in the action row between Dupliquer and Assigner buttons
    - The `locale` prop must be threaded from ProgramsClient down to ProgramCard. Add `locale: string` to ProgramCardProps and pass from ProgramsClient (it already has locale in scope).
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep "AdaptWithAI\|ProgramsClient" | head -20</automated>
  </verify>
  <done>TypeScript clean. AdaptWithAIButton.tsx contains "border-primary" and "Adapter avec l'IA" and "IoSparklesOutline". ProgramsClient.tsx contains "AdaptWithAIButton" and "handleAdaptWithAI". ProgramCard receives locale prop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AlertsPanel PATCH /coach/ai/alerts/:id/read | Bearer token required; only the alert's coach can mark it read (RLS) |
| AdaptWithAIButton deep-link | template param is the programId from the coach's own data; no cross-user data in URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-13 | Elevation of Privilege | PATCH /coach/ai/alerts/:id/read | mitigate | Backend route will use authMiddleware + verify alert.coach_id = auth.uid(); any coach cannot dismiss another coach's alert |
| T-29-14 | Information Disclosure | alert summary text visible in panel | accept | Alerts are the coach's own monitoring data; only rendered to authenticated coach via RLS-fetched server-side data |
| T-29-15 | Tampering | AdaptWithAIButton deep-link params | accept | template/client are UUIDs from coach's own program data; backend chat does not trust these as access grants — it uses RLS for all data reads |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; gsap already installed |
</threat_model>

<verification>
- /coach/dashboard shows AlertsPanel below WelcomeCard
- No-alerts state: green shield + "Tous vos clients sont dans de bonnes conditions."
- CoachSidebar "IA" item is clickable (not disabled/greyed out)
- /coach/programs — "Adapter avec l'IA" button visible on template cards
- Clicking "Adapter avec l'IA" navigates to /coach/ai?template={id}
</verification>

<success_criteria>
- AlertsPanel renders both empty and populated states per UI-SPEC
- Alert dismiss works with GSAP collapse animation
- CoachSidebar IA item enabled — navigates to /coach/ai
- ProgramCard shows AdaptWithAIButton for is_template programs
- All 6 files TypeScript-clean
</success_criteria>

<output>
Create `.planning/phases/29-ai-coach-orchestrator/29-04-SUMMARY.md` when done.
</output>
