---
phase: 29-ai-coach-orchestrator
plan: 06
type: execute
wave: 6
depends_on:
  - 29-04
  - 29-05
files_modified: []
autonomous: false
requirements:
  - AIC-01
  - AIC-02
  - AIC-03
  - AIC-04
  - AIC-05
  - AIC-06
  - AIC-07
  - AIC-08
  - AIC-09
  - AIC-10
must_haves:
  truths:
    - "POST /coach/ai/chat/stream responds with SSE and includes a tool call when prompted with 'Analyze client {id}'"
    - "ai_tool_audit row exists in Supabase after tool invocation"
    - "POST /coach/ai/monitor-cron returns 200 JSON with coaches_processed and alerts_written fields"
    - "POST /coach/ai/monitor-cron returns 401 without CRON_SECRET header"
    - "/coach/ai page loads without 404 or runtime error"
    - "AlertsPanel renders on dashboard page"
    - "CoachSidebar IA nav item is clickable"
    - "ProgramCard shows Adapter avec l'IA button for template programs"
  artifacts: []
  key_links: []
---

<objective>
Verify Phase 29 end-to-end: smoke-test the chat + tool call flow, validate cron dry-run, confirm audit logging, and human-verify the UI.

Purpose: Final gate before marking Phase 29 complete. Ensures every AIC requirement has an observable pass condition.

Output: Verified phase — no code changes; human approval to close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-ai-coach-orchestrator/29-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend smoke tests — cron auth + TypeScript compile</name>
  <files></files>
  <read_first>
    backend/api/src/coach/ai/service.ts (verify CRON_SECRET check and monitor-cron route)
    backend/api/src/config/credits.ts (verify coach_chat key present)
    backend/api/vercel.json (verify 3 cron entries)
  </read_first>
  <action>
    Run the following automated checks and confirm each passes:

    1. TypeScript compile check (backend):
       `cd /c/ziko-platform/backend/api && npx tsc --noEmit --project tsconfig.json`
       Expected: exit 0, no errors.

    2. TypeScript compile check (web):
       `cd /c/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json`
       Expected: exit 0, no errors.

    3. Verify credits.ts has coach_chat:
       `grep -c 'coach_chat' /c/ziko-platform/backend/api/src/config/credits.ts`
       Expected: ≥ 1

    4. Verify coachAiRouter mounted in app.ts:
       `grep -c 'coachAiRouter' /c/ziko-platform/backend/api/src/app.ts`
       Expected: 2 (import + mount)

    5. Verify monitor-cron in vercel.json:
       `grep -c 'monitor-cron' /c/ziko-platform/backend/api/vercel.json`
       Expected: 1

    6. Verify ai_tool_audit table in migration 050:
       `grep -c 'ai_tool_audit' /c/ziko-platform/supabase/migrations/050_coach_ai_schema.sql`
       Expected: ≥ 3 (CREATE TABLE + ENABLE RLS + policy)

    7. Verify CoachSidebar IA item enabled:
       `grep -v 'disabled: true' /c/ziko-platform/apps/web/src/components/coach/CoachSidebar.tsx | grep -c 'IA'`
       Expected: ≥ 1

    8. Verify AdaptWithAIButton referenced in ProgramsClient:
       `grep -c 'AdaptWithAIButton' /c/ziko-platform/apps/web/src/app/[locale]/\(coach\)/coach/programs/ProgramsClient.tsx`
       Expected: ≥ 1

    9. Verify WeeklyDigest template exists:
       `test -f /c/ziko-platform/packages/email/src/templates/WeeklyDigest.tsx && echo "EXISTS"`
       Expected: "EXISTS"

    10. Verify force-dynamic on AI page:
        `grep -c 'force-dynamic' /c/ziko-platform/apps/web/src/app/[locale]/\(coach\)/coach/ai/page.tsx`
        Expected: 1

    If any check fails, fix the underlying issue before proceeding to the human verification step.
  </action>
  <verify>
    <automated>cd /c/ziko-platform/backend/api && npx tsc --noEmit --project tsconfig.json && echo "BACKEND_OK" && cd /c/ziko-platform && npx tsc --noEmit -p apps/web/tsconfig.json && echo "WEB_OK"</automated>
  </verify>
  <done>All 10 grep/test checks pass. BACKEND_OK and WEB_OK printed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — chat flow + dashboard + programs page</name>
  <what-built>
    Full Phase 29: /coach/ai chat page with SSE streaming + 3 tools, daily monitoring cron, alerts panel on dashboard, CoachSidebar IA enabled, "Adapter avec l'IA" button on programs, weekly digest email scaffold.
  </what-built>
  <how-to-verify>
    With the backend API running (`cd backend/api && npm run dev`) and web app running (`cd apps/web && npm run dev`), verify each area:

    **1. /coach/ai — Chat page loads**
    - Navigate to http://localhost:3001/fr/coach/ai (or deployed URL)
    - Expected: Page loads showing "IA Coach — Votre assistant coaching" header, empty state with "Commencez une conversation" heading and 3 suggestion chips

    **2. Credit widget visible**
    - Header right: flash icon + credit balance + "Analyser: 2cr · Générer: 3cr · Surveiller: 1cr" chips visible

    **3. Chat message sends**
    - Type "Bonjour" in the input and press Send (or Enter)
    - Expected: Message appears as user bubble, AI response streams in with orange cursor blink
    - Check browser Network tab: POST /coach/ai/chat/stream returns 200 with text/event-stream Content-Type

    **4. Tool invocation (requires a linked client)**
    - If you have a linked client, type: "Analyse les données de [client name] sur 30 jours"
    - Expected: ToolResultCard appears inline showing "analyze_client · En cours..." → "Terminé"
    - Verify in Supabase dashboard: ai_tool_audit table has a new row with tool_name='analyze_client'

    **5. Monitor-cron dry-run**
    - Run from terminal:
      ```
      curl -X POST http://localhost:3000/coach/ai/monitor-cron \
        -H "Authorization: Bearer ${CRON_SECRET}" \
        -H "Content-Type: application/json"
      ```
    - Expected: 200 JSON with `{"success": true, "coaches_processed": N, "alerts_written": M}`
    - Without Authorization header: 401

    **6. /coach/dashboard — Alerts panel**
    - Navigate to http://localhost:3001/fr/coach/dashboard
    - Expected: AlertsPanel renders below WelcomeCard (either empty state with green shield, or alert cards if any were written by the cron)

    **7. CoachSidebar — IA item clickable**
    - Sidebar: "IA" nav item should be clickable and navigate to /coach/ai (not disabled/greyed)

    **8. /coach/programs — Adapter avec l'IA button**
    - Navigate to http://localhost:3001/fr/coach/programs
    - Find a template program (marked as template)
    - Expected: "Adapter avec l'IA" button visible in the program card action row
    - Click it: navigates to /coach/ai?template={programId}

    **9. Deep-link auto-fill**
    - Navigate to /coach/ai?template={any-program-id}
    - Expected: Input is pre-filled with "Adapte le programme..." text, message auto-sends after 500ms
  </how-to-verify>
  <resume-signal>Type "verified" if all checks pass. Describe any failures with specific error messages or screenshots.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification phase | Read-only checks; no new data written to production except by the cron dry-run test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-19 | Information Disclosure | curl cron test sends CRON_SECRET | accept | Test is run locally against local dev server; CRON_SECRET is not production |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan |
</threat_model>

<verification>
All 10 automated checks pass (TypeScript clean + grep gates).
Human verifies: chat streams, tools invoke, cron returns 200, dashboard shows alerts panel, sidebar IA enabled, programs show Adapter button.
</verification>

<success_criteria>
- Backend TypeScript: exit 0
- Web TypeScript: exit 0
- All 10 grep/file checks pass
- Human approves all 9 UI + API verification steps
- Phase 29 marked complete in STATE.md
</success_criteria>

<output>
Create `.planning/phases/29-ai-coach-orchestrator/29-06-SUMMARY.md` when done.
Update `.planning/STATE.md` to mark Phase 29 complete.
</output>
