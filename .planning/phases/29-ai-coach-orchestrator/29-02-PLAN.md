---
phase: 29-ai-coach-orchestrator
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - backend/api/src/coach/ai/types.ts
  - backend/api/src/coach/ai/context.ts
  - backend/api/src/coach/ai/db.ts
  - backend/api/src/coach/ai/tools.ts
  - backend/api/src/coach/ai/service.ts
  - backend/api/src/config/credits.ts
  - backend/api/src/app.ts
autonomous: true
requirements:
  - AIC-01
  - AIC-02
  - AIC-03
  - AIC-04
  - AIC-05
  - AIC-06
  - AIC-09
  - AIC-10
must_haves:
  truths:
    - "POST /coach/ai/chat/stream returns SSE stream with meta + chunk + [DONE] events"
    - "All 3 tools (analyze_client, generate_coaching_program, monitor_client_alerts) are accessible to the AI in the chat route"
    - "analyze_client rejects requests for unlinked clients with an error"
    - "generate_coaching_program persists a workout_programs row with is_template=FALSE and assigned_to_user_id set"
    - "Every tool invocation writes a row to ai_tool_audit (fire-and-forget)"
    - "POST /coach/ai/monitor-cron returns 401 without correct CRON_SECRET header"
    - "/coach/ai/chat/stream returns 402 when coach credit balance is zero"
    - "coach_chat credit action exists in CREDIT_COSTS"
  artifacts:
    - path: "backend/api/src/coach/ai/types.ts"
      provides: "CoachAlert, AiToolAuditRow, CoachContext interfaces"
      exports: ["CoachAlert", "AiToolAuditRow", "CoachContext"]
    - path: "backend/api/src/coach/ai/context.ts"
      provides: "fetchCoachContext(coachId, jwt) returning linked clients"
      exports: ["fetchCoachContext"]
    - path: "backend/api/src/coach/ai/db.ts"
      provides: "getCoachAlerts, logToolAudit, listCoachesForCron, insertAlerts"
      exports: ["getCoachAlerts", "logToolAudit", "listCoachesForCron", "insertAlerts"]
    - path: "backend/api/src/coach/ai/tools.ts"
      provides: "coachToolSchemas array (3 tools) + getCoachToolExecutor factory"
      exports: ["coachToolSchemas", "getCoachToolExecutor"]
    - path: "backend/api/src/coach/ai/service.ts"
      provides: "coachAiRouter with /chat/stream + /monitor-cron routes"
      exports: ["coachAiRouter"]
  key_links:
    - from: "backend/api/src/coach/ai/service.ts"
      to: "backend/api/src/app.ts"
      via: "app.route('/coach/ai', coachAiRouter)"
      pattern: "coachAiRouter"
    - from: "backend/api/src/coach/ai/tools.ts"
      to: "backend/api/src/coach/clients/db.ts"
      via: "createUserClient(jwt) — MUST NOT use clientForUser()"
      pattern: "createUserClient"
    - from: "backend/api/src/coach/ai/service.ts"
      to: "backend/api/src/config/credits.ts"
      via: "creditCheck('coach_chat') + creditDeduct('coach_chat')"
      pattern: "coach_chat"
---

<objective>
Build the complete `backend/api/src/coach/ai/` bounded module — types, context, db, tools, service — then wire it to the credit gate, mount in app.ts, and add the Vercel cron entry.

Purpose: This is the AI brain of Phase 29. The bounded module exposes the streaming chat route, 3 coach-only tools with RLS-aware execution, fire-and-forget audit logging, and the daily monitoring cron.

Output: 5 new backend files + 2 updated config files. POST /coach/ai/chat/stream + POST /coach/ai/monitor-cron fully operational.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-ai-coach-orchestrator/29-CONTEXT.md
@.planning/phases/29-ai-coach-orchestrator/29-RESEARCH.md
@.planning/phases/29-ai-coach-orchestrator/29-PATTERNS.md

<interfaces>
<!-- Key interfaces executor needs — extracted from codebase. -->

From backend/api/src/routes/ai.ts (SSE pattern to clone):
```typescript
// Import pattern:
import { stream } from 'hono/streaming';
import { streamText, tool, jsonSchema, stepCountIs } from 'ai';
// Route pattern (HEADERS BEFORE stream() call):
c.header('Content-Type', 'text/event-stream');
c.header('Cache-Control', 'no-cache');
c.header('Connection', 'keep-alive');
return stream(c, async (s) => {
  await s.write(`data: ${JSON.stringify({ type: 'meta', conversation_id })}\n\n`);
  for await (const part of result.fullStream) {
    if (part.type === 'text-delta') { ... }
  }
  await s.write('data: [DONE]\n\n');
});
// Tool definition (AI SDK v6 — inputSchema NOT parameters):
tool({ description: '...', inputSchema: jsonSchema<T>({...}), execute: async (input) => { ... } })
// stepCountIs (NOT maxSteps):
stopWhen: stepCountIs(5)
// onFinish (totalUsage NOT usage):
onFinish: ({ totalUsage }) => logTokenUsage(coachId, 'claude-sonnet-4-20250514', totalUsage)
```

From backend/api/src/coach/clients/db.ts (createUserClient — MUST use this, never clientForUser):
```typescript
export function createUserClient(jwt: string) {
  return createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_PUBLISHABLE_KEY!, {
    auth: { autoRefreshToken: false, persistSession: false },
    global: { headers: { Authorization: `Bearer ${jwt}` } },
  });
}
```

From backend/api/src/routes/storage.ts (CRON_SECRET auth pattern):
```typescript
router.post('/cron/cleanup', async (c) => {
  const authHeader = c.req.header('authorization');
  const cronSecret = process.env.CRON_SECRET;
  if (cronSecret && authHeader !== `Bearer ${cronSecret}`) {
    return c.json({ error: 'Unauthorized' }, 401);
  }
  // ... handler
});
```

From backend/api/src/config/credits.ts (shape to extend):
```typescript
export const CREDIT_COSTS = { chat: 4, scan: 3, program: 4, import: 0 } as const;
export type CreditAction = keyof typeof CREDIT_COSTS;
```

From backend/api/src/middleware/creditGate.ts (usage):
```typescript
router.post('/chat/stream', creditCheck('coach_chat'), creditDeduct('coach_chat'), async (c) => { ... });
```

From backend/api/src/context/conversation.ts (reuse directly):
```typescript
getOrCreateConversation(userId: string, conversationId?: string, jwt?: string): Promise<{conversationId: string, history: Message[]}>
appendMessages(conversationId: string, messages: Message[], jwt?: string): void
```

From backend/api/src/coach/programs/service.ts (generate_coaching_program integration):
// createProgram creates workout_programs; assignProgram sets assigned_to_user_id
// The generate_coaching_program tool must create with is_template=FALSE, assigned_to_user_id set

From backend/api/src/app.ts (mount after importsRouter):
app.route('/coach/imports', importsRouter);  // ← add coachAiRouter after this line
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types.ts + context.ts + db.ts — contracts and data layer</name>
  <files>
    backend/api/src/coach/ai/types.ts
    backend/api/src/coach/ai/context.ts
    backend/api/src/coach/ai/db.ts
  </files>
  <read_first>
    backend/api/src/coach/clients/db.ts (createUserClient + createServiceClient + typed function patterns)
    backend/api/src/context/user.ts (fetchUserContext pattern to clone for fetchCoachContext)
    supabase/migrations/050_coach_ai_schema.sql (table column names for type definitions)
  </read_first>
  <behavior>
    - types.ts: CoachAlert has fields {id, coach_id, client_id, alert_type, severity, summary, is_read, created_at}
    - types.ts: AiToolAuditRow has fields {coach_id, tool_name, target_client_id, args_hash, result_status, conversation_id}
    - types.ts: CoachContext has {profile: {display_name: string | null} | null, clients: Array<{id: string, name: string}>}
    - context.ts: fetchCoachContext(coachId, jwt) fetches coach_profiles + coach_client_links in parallel; returns CoachContext; active links only (revoked_at IS NULL)
    - db.ts: getCoachAlerts(jwt, coachId) queries coach_alerts with is_read=false using createUserClient
    - db.ts: logToolAudit({coachId, toolName, targetClientId, argsHash, resultStatus, conversationId}) inserts to ai_tool_audit fire-and-forget using service client
    - db.ts: insertAlerts(alerts: Array<{coach_id, client_id, alert_type, severity, summary}>) batch inserts to coach_alerts using service client
    - db.ts: listCoachesForCron() returns [{coach_id, email}] from coach_profiles join auth.users via service client
  </behavior>
  <action>
    **types.ts** — interfaces only, no runtime code:
    - Export `CoachAlert`, `AiToolAuditRow`, `CoachContext` per the behavior block above.
    - `alert_type` is a union: `'missed_sessions' | 'sleep_drop' | 'mood_decline' | 'rpe_inflation'`
    - `severity` is a union: `'low' | 'medium' | 'high'`
    - `result_status` is a union: `'success' | 'error'`

    **context.ts** — mirrors `backend/api/src/context/user.ts` structure:
    - Import `createUserClient` from `../clients/db.js` (NOT `clientForUser` from tools/db — service-key bypass risk)
    - Add comment: `// MUST use JWT client — service key bypasses is_coach_of RLS`
    - `fetchCoachContext(coachId: string, jwt: string)`: `Promise.all([coach_profiles query, coach_client_links query])`
    - Linked clients query: `.eq('coach_id', coachId).is('revoked_at', null)` on `coach_client_links`, joining `user_profiles` for name
    - System prompt client list format: `clients: [{id: row.client_id, name: row.user_profiles?.name ?? 'Client inconnu'}]`

    **db.ts** — data access layer:
    - Re-export `createUserClient` from `../clients/db.js` for tool executors to use
    - `createServiceClient()` private function (service key) for cron + audit inserts
    - `getCoachAlerts`: uses `createUserClient(jwt)`, queries `coach_alerts` table, `.eq('coach_id', coachId).eq('is_read', false).order('created_at', {ascending: false}).limit(10)`
    - `logToolAudit`: fire-and-forget — `Promise.resolve(serviceClient.from('ai_tool_audit').insert({...})).catch(err => console.error('[ToolAudit]', err))`
    - `insertAlerts`: `await serviceClient.from('coach_alerts').insert(alerts)` — throws on error
    - `listCoachesForCron`: `serviceClient.from('coach_profiles').select('user_id, display_name').then(...)` combined with `auth.listUsers()` or a join to get emails — use `serviceClient.from('coach_profiles').select('user_id')` and accept that email is not available without auth admin; return `{coachId: string}[]` only — email will be fetched from `auth.users` via `supabase.auth.admin.getUserById` in the cron handler if needed for email delivery (defer email lookup to service.ts cron handler)
  </action>
  <verify>
    <automated>cd /c/ziko-platform/backend/api && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "coach/ai" | head -20</automated>
  </verify>
  <done>TypeScript compilation emits no errors in the three new files. All functions exported. createUserClient used (not clientForUser).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tools.ts + service.ts + credits.ts + app.ts + vercel.json</name>
  <files>
    backend/api/src/coach/ai/tools.ts
    backend/api/src/coach/ai/service.ts
    backend/api/src/config/credits.ts
    backend/api/src/app.ts
    backend/api/vercel.json
  </files>
  <read_first>
    backend/api/src/routes/ai.ts (full SSE streaming pattern — lines 1-50 + lines 90-260)
    backend/api/src/coach/ai/types.ts (just written — CoachContext, CoachAlert types)
    backend/api/src/coach/ai/context.ts (just written — fetchCoachContext)
    backend/api/src/coach/ai/db.ts (just written — logToolAudit, listCoachesForCron, insertAlerts)
    backend/api/src/coach/programs/service.ts (createProgram / duplicate patterns for generate tool)
    backend/api/src/config/credits.ts (current shape to extend)
    backend/api/src/app.ts (current mount list — add coachAiRouter after importsRouter)
    backend/api/vercel.json (current crons array — add monitor-cron entry)
  </read_first>
  <behavior>
    - tools.ts: coachToolSchemas array contains exactly 3 entries: analyze_client, generate_coaching_program, monitor_client_alerts
    - tools.ts: getCoachToolExecutor(name) returns the executor function or undefined
    - tools.ts: analyze_client executor calls createUserClient(jwt), performs defense-in-depth check (coach_client_links.eq(coach_id).eq(client_id).is(revoked_at, null)), rejects with Error('Client not linked to this coach') if no link found
    - tools.ts: generate_coaching_program executor creates workout_programs row with is_template=FALSE and assigned_to_user_id set; returns {program_id, name, weeks_count}
    - tools.ts: monitor_client_alerts executor runs SQL alert detection for all linked clients and returns structured alerts array
    - tools.ts: all 3 executors call logToolAudit with result_status 'success' or 'error' after execution
    - service.ts: buildCoachSDKTools(coachId, jwt) factory captures jwt in closure and wraps executors in tool({inputSchema: jsonSchema(...)}) — uses inputSchema NOT parameters
    - service.ts: /chat/stream uses creditCheck('coach_chat') + creditDeduct('coach_chat') middleware
    - service.ts: /monitor-cron checks CRON_SECRET, returns 401 if wrong — does NOT use authMiddleware
    - service.ts: /monitor-cron checks new Date().getUTCDay() === 1 to decide weekly digest path
    - credits.ts: CREDIT_COSTS now includes coach_chat: 4
  </behavior>
  <action>
    **tools.ts** — 3 coach tool schemas + executors:
    - Import `createUserClient` from `../clients/db.js` and `logToolAudit`, `insertAlerts` from `./db.js`
    - `coachToolSchemas`: array of 3 `AITool` objects with `name`, `description`, `parameters` (JSON Schema shape for the tools file — `buildCoachSDKTools` in service.ts translates these to `inputSchema: jsonSchema(...)`)
      - `analyze_client`: required `client_id` (string), optional `period_days` (integer, default 30)
      - `generate_coaching_program`: required `client_id`, `goal` (enum: muscle_gain|fat_loss|strength|endurance|general_fitness), `days_per_week` (integer); optional `split_type`, `experience_level`, `equipment`, `duration_weeks`
      - `monitor_client_alerts`: no required params (runs for all coach's linked clients)
    - `analyzeClientExecutor(input, coachId, jwt)`:
      - `const db = createUserClient(jwt)` — comment: `// MUST use JWT client — service key bypasses is_coach_of RLS`
      - Defense-in-depth: `.from('coach_client_links').select('id').eq('coach_id', coachId).eq('client_id', input.client_id).is('revoked_at', null).maybeSingle()` → throw if no link
      - Fetch workout_sessions (count last `period_days`), sleep_logs (avg duration_hours), journal_entries (avg mood), body_measurements (latest weight) for the client
      - Build structured result: `{client_id, period_days, sessions_count, avg_sleep_hours, avg_mood, latest_weight_kg, suggestions: []}`
      - Fire-and-forget: `logToolAudit({coachId, toolName:'analyze_client', targetClientId: input.client_id, argsHash: SHA256_hint, resultStatus:'success', conversationId:''})` — for argsHash use `JSON.stringify(input).length.toString()` as a lightweight placeholder (no crypto dep needed)
    - `generateCoachingProgramExecutor(input, coachId, jwt)`:
      - Defense-in-depth same coach_client_links check as above
      - Use `generateText` (not streamText) with `AGENT_MODEL` to produce `weeks_data` JSON for a `duration_weeks`-week program based on `goal` + `days_per_week` + `split_type` + `equipment`
      - Insert to `workout_programs`: `{name: 'Programme IA — [goal]', goal: input.goal, created_by_coach_id: coachId, assigned_to_user_id: input.client_id, is_template: false, weeks_data: parsedWeeksData, weeks_count: input.duration_weeks ?? 8}` via `createUserClient(jwt)`
      - Return `{program_id: data.id, name: data.name, weeks_count: data.weeks_count}`
      - `logToolAudit` with tool_name `'generate_coaching_program'`
    - `monitorClientAlertsExecutor(input, coachId, jwt)`:
      - Fetch all linked clients via `createUserClient(jwt)` — `coach_client_links.eq('coach_id', coachId).is('revoked_at', null)`
      - For each client, run the 3 SQL alert detection patterns from RESEARCH.md §Alert Detection SQL Patterns (missed_sessions, sleep_drop, mood_decline) — RPE inflation is optional; if `session_sets` lacks `rpe` column, skip silently
      - Collect `CoachAlert[]`; call `insertAlerts` for any new alerts
      - Return `{alerts_count: N, alerts: [...]}`
      - `logToolAudit` with tool_name `'monitor_client_alerts'`
    - Export `coachToolSchemas` array and `getCoachToolExecutor(name: string)` function

    **service.ts** — Hono router (close clone of routes/ai.ts):
    - `const router = new Hono(); router.use('*', authMiddleware);` — auth applies to all routes EXCEPT monitor-cron (cron route must be defined outside this middleware scope — use a second separate Hono instance `const cronRouter = new Hono()` for the cron route, then export both and mount separately in app.ts, OR define monitor-cron on the same router but skip authMiddleware by defining it before `router.use('*', authMiddleware)`)
    - Simplest approach: define `monitor-cron` route BEFORE the `router.use('*', authMiddleware)` line so it doesn't require a JWT
    - `buildCoachSystemPrompt(ctx: CoachContext)`: string containing `COACH_BASE_SYSTEM` + `## Linked Clients\n` + bullet list of `- ${c.name} (id: ${c.id})` per client
    - `buildCoachSDKTools(coachId, jwt)`: calls `getCoachToolExecutor` per tool in `coachToolSchemas`, wraps in `tool({description, inputSchema: jsonSchema(s.parameters), execute: async (input) => executor(input, coachId, jwt)})`
    - `/chat/stream` route: `creditCheck('coach_chat'), creditDeduct('coach_chat')` middleware, then parallel `fetchCoachContext` + `getOrCreateConversation` (pass `plugin_context: {context:'coach'}` — insert the ai_conversations row directly to set plugin_context if `getOrCreateConversation` doesn't support it; see Research §Open Questions #2), then `streamText({model: AGENT_MODEL, system, messages, tools, stopWhen: stepCountIs(5), onFinish: ({totalUsage}) => logTokenUsage(...)})`, then SSE headers + `stream()` call per Pattern 1 from RESEARCH.md
    - `/monitor-cron` route: CRON_SECRET check (copy exact pattern from storage.ts lines 133-137), then run `monitorClientAlertsExecutor` for each coach via `listCoachesForCron()`, then check `new Date().getUTCDay() === 1` for Monday weekly digest path (digest logic is a stub in this plan — Plan 05 completes the email send)
    - Export: `export { router as coachAiRouter }`

    **credits.ts** — add `coach_chat: 4` to `CREDIT_COSTS`:
    - Preserve existing keys `{chat: 4, scan: 3, program: 4, import: 0}` and add `coach_chat: 4`
    - Add `COACH_TOOL_COSTS` constant: `{analyze_client: 2, generate_coaching_program: 3, monitor_client_alerts: 1} as const` + `export type CoachToolName = keyof typeof COACH_TOOL_COSTS`
    - `CreditAction` type updates automatically from `keyof typeof CREDIT_COSTS`

    **app.ts** — add after `importsRouter` line:
    - `import { coachAiRouter } from './coach/ai/service.js';`
    - `app.route('/coach/ai', coachAiRouter);`

    **vercel.json** — add to `"crons"` array:
    - `{"path": "/coach/ai/monitor-cron", "schedule": "0 7 * * *"}`
    - Result: 3 cron entries total
  </action>
  <verify>
    <automated>cd /c/ziko-platform/backend/api && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles cleanly. grep -c '"coach_chat"' backend/api/src/config/credits.ts returns 1. grep -c 'coachAiRouter' backend/api/src/app.ts returns 2 (import + mount). grep -c 'monitor-cron' backend/api/vercel.json returns 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach browser → /coach/ai/chat/stream | JWT required; creditCheck gates usage; tool executors use RLS JWT |
| Vercel cron → /coach/ai/monitor-cron | CRON_SECRET gates; no user JWT; uses service key with explicit WHERE scope |
| tool executor → Supabase | createUserClient(jwt) for all coach data reads — is_coach_of RLS fires automatically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Elevation of Privilege | analyze_client tool executor | mitigate | Defense-in-depth check in executor: verify coach_client_links before any data read; createUserClient(jwt) so is_coach_of RLS also fires |
| T-29-05 | Elevation of Privilege | generate_coaching_program — assigns to client_id | mitigate | Same defense-in-depth coach_client_links check before workout_programs insert |
| T-29-06 | Elevation of Privilege | monitor-cron runs without user JWT | mitigate | CRON_SECRET check; queries explicitly scope by coach_id + client_id in WHERE; no broad SELECT * without filter |
| T-29-07 | DoS | Coach tool chaining | mitigate | `stopWhen: stepCountIs(5)` caps tool-call depth; `creditCheck('coach_chat')` gates conversation entry |
| T-29-08 | Tampering | AI SDK v6 parameters vs inputSchema | mitigate | Use `inputSchema: jsonSchema(...)` — confirmed by routes/ai.ts line 99; never `parameters:` |
| T-29-09 | Spoofing | SSE headers order | mitigate | Set Content-Type + Cache-Control headers BEFORE calling stream() — same order as routes/ai.ts lines 207-211 |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan; all imports from already-installed deps |
</threat_model>

<verification>
- `npx tsc --noEmit` in backend/api exits 0
- `POST /coach/ai/chat/stream` with valid JWT + balance: returns `text/event-stream` response
- `POST /coach/ai/monitor-cron` without Authorization: returns 401
- `POST /coach/ai/monitor-cron` with `Authorization: Bearer {CRON_SECRET}`: returns 200 JSON
- `grep -c 'coach_chat' backend/api/src/config/credits.ts` returns ≥ 1
- `grep -c 'coachAiRouter' backend/api/src/app.ts` returns 2
</verification>

<success_criteria>
- 5 new files in backend/api/src/coach/ai/ — all TypeScript-clean
- CREDIT_COSTS includes coach_chat: 4; COACH_TOOL_COSTS exports 3 tool costs
- /coach/ai router mounted at /coach/ai in app.ts
- vercel.json has 3 cron entries including monitor-cron at 0 7 * * *
- analyze_client tool rejects unlinked clients
- ai_tool_audit row inserted on every tool call (fire-and-forget)
</success_criteria>

<output>
Create `.planning/phases/29-ai-coach-orchestrator/29-02-SUMMARY.md` when done.
</output>
