---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(auth)/welcome.tsx
  - apps/mobile/app/(auth)/login.tsx
  - apps/mobile/app/(auth)/register.tsx
  - apps/mobile/app/(auth)/forgot.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "AuthWelcome shows Apple button above Google button, both distinct (Apple = white bg, Google = glass border)"
    - "AuthSignup 4-segment strength bar appears when password.length > 0 and updates in real-time"
    - "AuthForgot shows success card with email address and 30-minute expiry note after send"
    - "All auth form screens (login/register/forgot) use #F7F6F3 background, #FFFFFF fields"
    - "TypeScript compiles without errors across all 4 screens"
  artifacts:
    - path: "apps/mobile/app/(auth)/welcome.tsx"
      provides: "AuthWelcome screen with Apple/Google/Email buttons and social proof"
      contains: "logo-apple"
    - path: "apps/mobile/app/(auth)/login.tsx"
      provides: "AuthSignin screen with email/password and forgot link"
      contains: "Se connecter"
    - path: "apps/mobile/app/(auth)/register.tsx"
      provides: "AuthSignup screen with 4-segment strength bar"
      contains: "STRENGTH_COLORS"
    - path: "apps/mobile/app/(auth)/forgot.tsx"
      provides: "AuthForgot screen with form/sent state switch"
      contains: "Vérifie ta boîte"
  key_links:
    - from: "apps/mobile/app/(auth)/welcome.tsx"
      to: "/(auth)/register"
      via: "Continuer avec un email button"
      pattern: "register"
    - from: "apps/mobile/app/(auth)/register.tsx"
      to: "getStrength function"
      via: "STRENGTH_COLORS array"
      pattern: "getStrength"
---

<objective>
Rebuild all 4 auth screens to match the Phase 34 UI-SPEC pixel-for-pixel.

Purpose: The auth flow is the user's first impression. AuthWelcome needs Apple button added above Google (currently missing — Google is rendered where Apple should be). Login/register/forgot are structurally ~85-90% correct but need exact style values locked.

Output: 4 updated screen files matching the UI-SPEC spec for AuthWelcome, AuthSignin, AuthSignup, AuthForgot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-auth-onboarding-redesign/34-UI-SPEC.md

<interfaces>
<!-- Design tokens from packages/ui/src/design-system.ts -->
```typescript
export const colors = {
  bg: '#F7F6F3', surface: '#FFFFFF', border: '#E2E0DA', primary: '#FF5C1A',
  text: '#1C1A17', muted: '#6B6963', cardDark: '#1C1A17', cardDarkText: '#FFFAF6',
  success: '#2E9E5B', info: '#2E7BF6', violet: '#7B5BD0', warn: '#E8A33A',
} as const;
```

Supabase import: `import { supabase } from '../../src/lib/supabase'` (auth screens)
Router: `import { router } from 'expo-router'`
Icons: `import { Ionicons } from '@expo/vector-icons'`
Alert: `import { showAlert } from '@ziko/plugin-sdk'` — auth screens may also use React Native Alert directly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthWelcome — add Apple button + fix CTA order</name>
  <files>apps/mobile/app/(auth)/welcome.tsx</files>

  <read_first>
    - apps/mobile/app/(auth)/welcome.tsx (read current state before modifying)
    - .planning/phases/34-auth-onboarding-redesign/34-UI-SPEC.md — section "AuthWelcome"
  </read_first>

  <action>
The file is 85% correct. The single required change is in the CTA stack: add an Apple button as the FIRST button, then keep Google as the second button, then Email as third.

Current CTA order: [Google white bg] [Email transparent border] [sign-in link] [legal]
Target CTA order: [Apple white bg] [Google glass border] [Email transparent border] [sign-in link] [legal]

Apple button spec (per UI-SPEC AuthWelcome):
- `paddingVertical: 14, paddingHorizontal: 16, borderRadius: 14, backgroundColor: '#fff'`
- `flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 10`
- Ionicons `logo-apple` size 16 color `#1C1A17`
- Text: `"Continuer avec Apple"` fontWeight 700 fontSize 14.5 color `#1C1A17`
- onPress: `() => showAlert('Bientôt disponible', 'La connexion Apple arrive prochainement.')` (Apple OAuth not yet wired — placeholder)

Google button becomes the second button with glass style (per spec):
- `paddingVertical: 14, paddingHorizontal: 16, borderRadius: 14`
- `borderWidth: 1, borderColor: 'rgba(255,250,246,0.16)', backgroundColor: 'rgba(255,250,246,0.06)'`
- `flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 10`
- Ionicons `logo-google` size 16 color `#FFFAF6`
- Text: `"Continuer avec Google"` fontWeight 700 fontSize 14.5 color `#FFFAF6`

The existing Google button currently has `backgroundColor: '#fff'` and white icon — it was styled as Apple. Change it to the glass style above.

Email button (third) — keep as-is (already correct transparent border style).

Do NOT change glows, logo row, hero copy, social proof row, sign-in link, or legal footer — those are already correct.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('apps/mobile/app/(auth)/welcome.tsx','utf8');const checks=['logo-apple','Continuer avec Apple','rgba(255,250,246,0.06)','Continuer avec Google','Continuer avec un email'];const missing=checks.filter(s=>!c.includes(s));if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('OK');"</automated>
  </verify>

  <acceptance_criteria>
    - welcome.tsx contains `logo-apple` (Apple icon import)
    - welcome.tsx contains `"Continuer avec Apple"` text
    - welcome.tsx contains `rgba(255,250,246,0.06)` (Google glass background)
    - Apple button appears before Google button in JSX order (Apple JSX block precedes Google JSX block)
    - Apple button backgroundColor is `'#fff'`, Google button backgroundColor is `'rgba(255,250,246,0.06)'`
    - Email button remains with `backgroundColor: 'transparent'`
  </acceptance_criteria>

  <done>AuthWelcome renders Apple/Google/Email in correct order. Apple is white, Google is glass-border, Email is transparent outline. Social proof, glows, hero copy unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: AuthSignin + AuthSignup — lock exact style values</name>
  <files>apps/mobile/app/(auth)/login.tsx, apps/mobile/app/(auth)/register.tsx</files>

  <read_first>
    - apps/mobile/app/(auth)/login.tsx (read current state)
    - apps/mobile/app/(auth)/register.tsx (read current state)
    - .planning/phases/34-auth-onboarding-redesign/34-UI-SPEC.md — sections "AuthSignin", "AuthSignup", "AuthHeader"
  </read_first>

  <action>
Both screens are structurally correct. Lock exact values per UI-SPEC.

**login.tsx changes:**

Back button: currently uses `theme.text + '10'` as bg, which is `#1C1A17` + hex. Per spec: `rgba(28,26,23,0.06)` — replace `theme.text + '10'` with `'rgba(28,26,23,0.06)'` for the back button background. Icon size: change 18 → 16, color: `#1C1A17`.

Header values: eyebrow letterSpacing currently 2 → keep (matches spec). Title lineHeight 34 → correct. Sub lineHeight 19 → change to 19.5 (spec says 19.5).

Field label: currently `fontSize: 11, fontWeight: '700'` — spec confirms these. Remove `useThemeStore` usage: replace all `theme.background` with `'#F7F6F3'`, `theme.surface` with `'#FFFFFF'`, `theme.border` with `'#E2E0DA'`, `theme.text` with `'#1C1A17'`, `theme.muted` with `'#6B6963'`. Remove `useThemeStore` import and `const theme = ...` line.

CTA valid state: currently `backgroundColor: valid ? theme.text : theme.text + '30'` → change to `backgroundColor: valid ? '#1C1A17' : 'rgba(28,26,23,0.18)'` per spec (invalid state uses rgba not hex+30).

Add CTA shadow: spec shows no explicit shadow on login CTA (dark bg) — keep flat.

Remove `showAlert` import if it comes from `@ziko/plugin-sdk` — auth screens can use React Native Alert directly for error messages if needed, but showAlert is also fine since it's already imported.

**register.tsx changes:**

Same theme token → hardcoded values replacement: `theme.background` → `'#F7F6F3'`, `theme.surface` → `'#FFFFFF'`, `theme.border` → `'#E2E0DA'`, `theme.text` → `'#1C1A17'`, `theme.muted` → `'#6B6963'`. Remove `useThemeStore` import and `const theme = ...` line.

Back button: same fix as login — `'rgba(28,26,23,0.06)'` bg, icon size 16.

CTA (Continuer) invalid state: currently `'#FF5C1A' + '40'` → change to `'rgba(255,92,26,0.30)'` per spec.

CTA valid state shadow — spec says: `shadowColor: 'rgba(255,92,26,0.55)', shadowOffset: {width:0, height:8}, shadowRadius: 22` — add these to the valid CTA style (with `elevation: 8` for Android).

Strength bar: already correct (height 3, borderRadius 2, gap 4, colors matching spec). Confirm `STRENGTH_COLORS = ['transparent', '#E94B3C', '#E8A33A', '#2E9E5B', '#2E9E5B']` — already correct. Confirm `STRENGTH_LABELS = ['', 'Faible', 'Correct', 'Fort', 'Excellent']` — already correct. No changes needed to strength logic.

Password field in register: currently `secureTextEntry` without show/hide toggle. UI-SPEC for AuthSignup does NOT show a toggle (only AuthSignin has toggle). Keep as-is.

Legal footer in register: currently uses `theme.muted` color → change to `'rgba(28,26,23,0.40)'` per spec (matches dark faint pattern, but on light bg context use `#6B6963` which is the muted value — keep as `'#6B6963'` since it's a light screen).
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');['apps/mobile/app/(auth)/login.tsx','apps/mobile/app/(auth)/register.tsx'].forEach(f=>{const c=fs.readFileSync(f,'utf8');if(c.includes('useThemeStore')){console.error(f+': still has useThemeStore');process.exit(1);}if(!c.includes('rgba(28,26,23,0.06)')){console.error(f+': missing back button rgba');process.exit(1);}});console.log('OK');"</automated>
  </verify>

  <acceptance_criteria>
    - login.tsx does NOT contain `useThemeStore` (hardcoded tokens replace theme usage)
    - login.tsx contains `'rgba(28,26,23,0.06)'` for back button background
    - login.tsx contains `'rgba(28,26,23,0.18)'` for disabled CTA background
    - register.tsx does NOT contain `useThemeStore`
    - register.tsx contains `'rgba(255,92,26,0.30)'` for disabled CTA
    - register.tsx contains `shadowRadius: 22` on the valid CTA TouchableOpacity style
    - register.tsx contains `STRENGTH_COLORS` array with `'#E94B3C'` at index 1
    - register.tsx contains `STRENGTH_LABELS` array with `'Faible'` at index 1
  </acceptance_criteria>

  <done>AuthSignin and AuthSignup use hardcoded design token values. Strength bar on AuthSignup shows correct 4 segments with exact colors. CTA states match spec exactly.</done>
</task>

<task type="auto">
  <name>Task 3: AuthForgot — lock style values, verify sent-state card</name>
  <files>apps/mobile/app/(auth)/forgot.tsx</files>

  <read_first>
    - apps/mobile/app/(auth)/forgot.tsx (read current state)
    - .planning/phases/34-auth-onboarding-redesign/34-UI-SPEC.md — section "AuthForgot"
  </read_first>

  <action>
The screen is structurally complete (form state + sent state both exist). Apply the same hardcoded-token pattern.

Replace theme tokens: `theme.background` → `'#F7F6F3'`, `theme.surface` → `'#FFFFFF'`, `theme.border` → `'#E2E0DA'`, `theme.text` → `'#1C1A17'`, `theme.muted` → `'#6B6963'`. Remove `useThemeStore` import and `const theme = ...` line.

Back button: `'rgba(28,26,23,0.06)'` bg, icon size 16, color `#1C1A17`.

Eyebrow letterSpacing: spec says 2 — current is 2 — keep.
Title fontSize: spec says 32 — current is 32 — keep.

Success card spec:
- bg: `'rgba(46,158,91,0.08)'` (spec says "color-mix #2E9E5B 8%", RN approximation)
- border: `'rgba(46,158,91,0.25)'` (spec says "color-mix #2E9E5B 25%")  
- borderRadius: 12
- padding: 16

Current card: has `backgroundColor: '#2E9E5B' + '14'` and `borderColor: '#2E9E5B' + '40'` — update to exact rgba values: `backgroundColor: 'rgba(46,158,91,0.08)'`, `borderColor: 'rgba(46,158,91,0.25)'`, `borderRadius: 12`, padding 16.

Check icon in card: currently `width: 36, height: 36, borderRadius: 11, backgroundColor: '#2E9E5B'` — spec says same, keep.

"Email envoyé à {email}" text: fontSize 13.5, fontWeight 700 — already matches.

"Le lien expire dans 30 minutes. Pas reçu ? Renvoyer" sub text: fontSize 12, color `#6B6963` (current `theme.muted`) — keep value, just replace token.

"Retour à la connexion" CTA: `backgroundColor: '#1C1A17'` — current uses `theme.text` → replace with `'#1C1A17'`.

Form CTA "Envoyer le lien": currently `email.length >= 4 ? theme.text : theme.text + '30'` → change to `email.length >= 4 ? '#1C1A17' : 'rgba(28,26,23,0.18)'`.

FadeIn animation on success card: spec says 250ms easeOut via `FadeIn` from reanimated. Import `Animated` from `react-native-reanimated` and wrap the success card `View` in `Animated.View entering={FadeIn.duration(250)}`. Only add this if react-native-reanimated is already imported in the project (it is, per UI-SPEC Registry Safety section).
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('apps/mobile/app/(auth)/forgot.tsx','utf8');const checks=['rgba(46,158,91,0.08)','rgba(46,158,91,0.25)','Vérifie ta boîte','Envoyer le lien','Retour à la connexion'];const missing=checks.filter(s=>!c.includes(s));if(missing.length){console.error('MISSING:',missing);process.exit(1);}if(c.includes('useThemeStore')){console.error('still has useThemeStore');process.exit(1);}console.log('OK');"</automated>
  </verify>

  <acceptance_criteria>
    - forgot.tsx does NOT contain `useThemeStore`
    - forgot.tsx contains `'rgba(46,158,91,0.08)'` for success card background
    - forgot.tsx contains `'rgba(46,158,91,0.25)'` for success card border
    - forgot.tsx contains `"Vérifie ta boîte"` as title in sent state
    - forgot.tsx contains `"Retour à la connexion"` as back CTA copy
    - forgot.tsx contains `"Envoyer le lien"` as form CTA copy
    - forgot.tsx contains FadeIn animation import (from react-native-reanimated) on success card
    - TypeScript compile passes: `cd apps/mobile && npx tsc --noEmit 2>&1 | grep -E "(auth)" | head -20` returns no errors for auth files
  </acceptance_criteria>

  <done>AuthForgot uses hardcoded design tokens. Success card renders with correct green tints and FadeIn animation. Both CTA states (form/sent) match UI-SPEC exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→supabase.auth | Credentials cross this boundary; must not be logged or exposed |
| OAuth provider→app | OAuth redirect tokens handled by Supabase SDK; no custom handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | login.tsx — error message display | mitigate | Use generic "Connexion impossible" message; supabase error.message may leak account existence — already using showAlert with user-facing string, not raw error |
| T-34-02 | Spoofing | AuthWelcome Apple button | accept | Apple OAuth not yet wired; showAlert placeholder prevents credential phishing via fake OAuth flow |
| T-34-SC | Tampering | npm installs (none in this plan) | accept | No new packages installed in this plan; reanimated already in package.json |
</threat_model>

<verification>
After all 3 tasks complete, run:
```bash
cd C:/ziko-platform && rtk tsc
```
Expect: 0 TypeScript errors in auth screen files.

Manual smoke:
- welcome.tsx diff shows Apple button added before Google
- register.tsx diff shows `rgba(255,92,26,0.30)` and shadow on CTA
</verification>

<success_criteria>
- AuthWelcome: `logo-apple` present, Apple button first in CTA stack
- AuthSignin: hardcoded tokens, `rgba(28,26,23,0.18)` on disabled CTA
- AuthSignup: hardcoded tokens, strength bar with `#E94B3C`/`#E8A33A`/`#2E9E5B`, shadow on valid CTA
- AuthForgot: hardcoded tokens, sent-state card with `rgba(46,158,91,0.08)` bg, FadeIn animation
- TypeScript: 0 compile errors across all 4 files
</success_criteria>

<output>
Create `.planning/phases/34-auth-onboarding-redesign/34-01-SUMMARY.md` when done
</output>
