---
phase: 25-invitations-mobile-mon-coach-minimal
plan: 09
type: execute
wave: 2
depends_on: [25-08]
files_modified:
  - apps/web/src/components/coach/InvitationCodeCard.tsx
  - apps/web/src/components/coach/GeneratePanel.tsx
  - apps/web/src/components/coach/InvitationsTable.tsx
  - apps/web/src/components/coach/ExpirationChipGroup.tsx
  - apps/web/src/components/coach/FilterChipGroup.tsx
  - apps/web/src/components/coach/RevokeConfirmModal.tsx
  - apps/web/src/components/coach/CodeInput.tsx
  - apps/web/src/components/coach/CoachPreviewCard.tsx
  - apps/web/src/components/coach/RedeemStateMachine.tsx
  - apps/web/src/app/[locale]/(coach)/coach/invitations/page.tsx
  - apps/web/src/app/[locale]/(coach)/coach/invitations/InvitationsClient.tsx
  - apps/web/src/app/[locale]/redeem/page.tsx
autonomous: true
requirements: [INVITE-01, INVITE-02, INVITE-03, INVITE-05, INVITE-06]
gap_closure: true
gaps_addressed:
  - GAP-2: Visual inaccuracies on ~95% of pages vs canonical mockups (Ziko-Screens.html + Ziko-Onboarding.html)

must_haves:
  truths:
    - "/coach/invitations page header, GeneratePanel, InvitationCodeCard, filter chips, and table all match UI-SPEC.md exactly"
    - "/redeem State A input, submit button, and error text match UI-SPEC.md exactly"
    - "CoachPreviewCard photo row, specialties chips, and bio truncation match UI-SPEC.md exactly"
    - "RevokeConfirmModal overlay, panel, confirm input, and button row match UI-SPEC.md exactly"
    - "All Tailwind classes match the exact strings specified in UI-SPEC.md §Surface & Component Patterns"
  artifacts:
    - path: "apps/web/src/components/coach/InvitationCodeCard.tsx"
      provides: "Generated-code card with correct px-10 py-8 padding, text-4xl mono code, two copy buttons"
    - path: "apps/web/src/components/coach/GeneratePanel.tsx"
      provides: "Collapsible panel with correct animate-in classes and chip group"
    - path: "apps/web/src/components/coach/InvitationsTable.tsx"
      provides: "Table with correct thead bg-background text-xs tracking-wide and tbody hover:bg-background/60"
    - path: "apps/web/src/components/coach/RevokeConfirmModal.tsx"
      provides: "Modal with correct overlay bg-black/40, panel rounded-2xl p-8, focus trap"
  key_links:
    - from: "UI-SPEC.md §Surface & Component Patterns"
      to: "every component Tailwind class string"
      via: "exact class-by-class audit"
---

<objective>
Systematic audit and pixel-perfect fix of all Phase 25 web surfaces against UI-SPEC.md. The HUMAN-UAT confirmed the /coach/invitations page and /redeem State A are correct, but States B/C could not be verified. This plan audits every component class string against UI-SPEC.md and fixes all deviations.

Purpose: Phase 25 UI must match the canonical design contract so the product looks professional and internally consistent.
Output: All 9 coach components and the 3 page wrappers match UI-SPEC.md §Surface & Component Patterns exactly.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-invitations-mobile-mon-coach-minimal/25-UI-SPEC.md
@.planning/phases/25-invitations-mobile-mon-coach-minimal/25-HUMAN-UAT.md
</context>

<interfaces>
<!-- UI-SPEC.md canonical class strings — executor MUST use these verbatim -->

Card:
  bg-white rounded-2xl p-6 border border-border shadow-sm

Generated-Code Card (InvitationCodeCard):
  bg-white rounded-2xl px-10 py-8 border border-border shadow-sm
  Label: text-xs font-semibold text-muted uppercase tracking-wide
  Code: text-4xl font-bold font-mono tabular-nums text-text tracking-widest
  Copy button: border border-border bg-white text-text rounded-xl px-4 py-2 text-sm font-normal hover:bg-background transition-colors
  Divider: border-t border-border my-4
  Share URL: text-sm font-normal text-muted font-mono
  Expiration line: text-sm font-normal text-muted

Chip (default):
  border border-border bg-white text-text text-sm font-normal rounded-full px-4 py-1.5 cursor-pointer
Chip (selected):
  border border-primary bg-primary/10 text-primary text-sm font-semibold rounded-full px-4 py-1.5
Chip (small specialty, CoachPreviewCard):
  border border-border bg-background text-text text-xs font-normal rounded-full px-3 py-1

Table:
  w-full text-sm
  thead row: bg-background
  thead th: text-muted font-semibold uppercase text-xs tracking-wide py-3 px-4 text-left
  tbody row: border-t border-border hover:bg-background/60
  tbody td: py-3 px-4

Input (CodeInput):
  w-full max-w-xs h-14 rounded-xl border border-border bg-white text-center text-2xl font-bold font-mono tabular-nums tracking-widest uppercase text-text focus:outline-none focus:ring-2 focus:ring-primary placeholder:text-muted

Button Primary:
  bg-primary text-white rounded-xl px-6 py-3 text-sm font-bold hover:bg-primary/90 disabled:opacity-40 disabled:cursor-not-allowed transition-colors

Button Outlined:
  border border-border bg-white text-text rounded-xl px-6 py-3 text-sm font-normal hover:bg-background transition-colors

Button Destructive:
  bg-red-600 text-white rounded-xl px-6 py-3 text-sm font-bold hover:bg-red-700 disabled:opacity-40 disabled:cursor-not-allowed transition-colors

Modal:
  Overlay: fixed inset-0 bg-black/40 z-50 flex items-center justify-center
  Panel:   bg-white rounded-2xl p-8 max-w-md w-full border border-border shadow-lg

GeneratePanel (expanded):
  bg-white rounded-2xl border border-border p-6 mt-4 space-y-4 animate-in fade-in slide-in-from-top-2

InvitationsClient page wrapper:
  max-w-3xl mx-auto py-12 px-6

Page heading:
  text-2xl font-bold text-text

Muted action text (Révoquer in table):
  text-red-600 text-sm font-normal

Status chip colors:
  Actif:    bg-green-50 text-green-700 border border-green-200 rounded-full px-3 py-1 text-xs font-semibold
  Utilisé:  bg-blue-50 text-blue-700 border border-blue-200 rounded-full px-3 py-1 text-xs font-semibold
  Expiré:   bg-yellow-50 text-yellow-700 border border-yellow-200 rounded-full px-3 py-1 text-xs font-semibold
  Révoqué:  bg-neutral-100 text-neutral-500 border border-neutral-200 rounded-full px-3 py-1 text-xs font-semibold

RedeemStateMachine State A wrapper:
  max-w-sm mx-auto pt-16 text-center

RedeemStateMachine State B/C wrapper:
  max-w-md mx-auto pt-8

CoachPreviewCard:
  bg-white rounded-2xl border border-border p-6 shadow-sm text-left
  Photo: w-[72px] h-[72px] rounded-full object-cover border border-border
  Name: text-lg font-bold text-text
  KYC verified: inline-flex items-center gap-1 mt-2 bg-primary/10 text-primary border border-primary/20 rounded-full px-3 py-1 text-xs font-bold
  KYC pending: inline-flex items-center gap-1 mt-2 bg-yellow-50 text-yellow-700 border border-yellow-200 rounded-full px-3 py-1 text-xs font-bold
  Bio: text-sm font-normal text-muted mt-4 whitespace-pre-line
  Bio toggle: text-primary text-sm font-normal underline

RevokeConfirmModal confirm label:
  text-sm font-semibold text-text mt-6 block
RevokeConfirmModal confirm input:
  mt-2 w-full rounded-xl border border-border px-4 py-3 text-sm font-normal text-text focus:outline-none focus:ring-2 focus:ring-primary
RevokeConfirmModal action row:
  flex items-center gap-3 mt-6 justify-end
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix Phase 25 coach-side components (InvitationCodeCard, GeneratePanel, InvitationsTable, ExpirationChipGroup, FilterChipGroup)</name>
  <files>
    apps/web/src/components/coach/InvitationCodeCard.tsx,
    apps/web/src/components/coach/GeneratePanel.tsx,
    apps/web/src/components/coach/InvitationsTable.tsx,
    apps/web/src/components/coach/ExpirationChipGroup.tsx,
    apps/web/src/components/coach/FilterChipGroup.tsx,
    apps/web/src/app/[locale]/(coach)/coach/invitations/InvitationsClient.tsx
  </files>

  <read_first>
    - apps/web/src/components/coach/InvitationCodeCard.tsx
    - apps/web/src/components/coach/GeneratePanel.tsx
    - apps/web/src/components/coach/InvitationsTable.tsx
    - apps/web/src/components/coach/ExpirationChipGroup.tsx
    - apps/web/src/components/coach/FilterChipGroup.tsx
    - apps/web/src/app/[locale]/(coach)/coach/invitations/InvitationsClient.tsx
    - .planning/phases/25-invitations-mobile-mon-coach-minimal/25-UI-SPEC.md (§Surface & Component Patterns + §Page Layouts + §Color)
  </read_first>

  <action>
Read each component file fully. For each one, compare EVERY Tailwind class string against the canonical values in the `<interfaces>` block above. Fix any deviation. Key audit points:

**InvitationCodeCard.tsx:**
- Outer wrapper: must be `bg-white rounded-2xl px-10 py-8 border border-border shadow-sm` (not `p-6` — generated-code card has `px-10 py-8`)
- Label row: `text-xs font-semibold text-muted uppercase tracking-wide`
- Code text: `text-4xl font-bold font-mono tabular-nums text-text tracking-widest`
- Copy buttons: `border border-border bg-white text-text rounded-xl px-4 py-2 text-sm font-normal hover:bg-background transition-colors` with an `IoClipboardOutline` icon from `react-icons/io5`
- Divider: `border-t border-border my-4`
- Share URL text: `text-sm font-normal text-muted font-mono`
- Expiration line: `text-sm font-normal text-muted`
- The label must read "Code d'invitation" (the UAT noted it shows "CODE" — check if this is the rendered label text or the key; update the i18n key value in `apps/web/messages/fr.json` if the French copy is wrong)

**GeneratePanel.tsx:**
- Expanded panel wrapper: `bg-white rounded-2xl border border-border p-6 mt-4 space-y-4 animate-in fade-in slide-in-from-top-2`
- When `open === false`: render `null` or `hidden` (not visible)
- "Générer" submit button inside panel: primary button class (`bg-primary text-white rounded-xl px-6 py-3 text-sm font-bold hover:bg-primary/90 disabled:opacity-40 disabled:cursor-not-allowed transition-colors`)

**ExpirationChipGroup.tsx:**
- Default chip: `border border-border bg-white text-text text-sm font-normal rounded-full px-4 py-1.5 cursor-pointer`
- Selected chip: `border border-primary bg-primary/10 text-primary text-sm font-semibold rounded-full px-4 py-1.5`
- Chip labels: "7j" / "14j" / "30j" / "Sans expiration" — verify these match fr.json

**FilterChipGroup.tsx:**
- Same chip classes as ExpirationChipGroup
- Chip labels: "Actives" (default selected) / "Toutes"
- Wrapper: `flex gap-2 mt-6 mb-4`

**InvitationsTable.tsx:**
- Table: `w-full text-sm`
- thead row: `bg-background`
- thead th: `text-muted font-semibold uppercase text-xs tracking-wide py-3 px-4 text-left`
- tbody row: `border-t border-border hover:bg-background/60`
- tbody td: `py-3 px-4`
- Status chips: use exact color classes from `<interfaces>` above (Actif/Utilisé/Expiré/Révoqué)
- Revoke button (active codes only): `text-red-600 text-sm font-normal` (text button, no background)
- Empty state: centered, heading `text-base font-semibold text-text`, body `text-sm text-muted mt-1`
- Code column: `font-mono text-sm`

**InvitationsClient.tsx:**
- Page heading h1: `text-2xl font-bold text-text`
- "Générer un code" button: `inline-flex items-center gap-2 bg-primary text-white rounded-xl px-6 py-3 text-sm font-bold hover:bg-primary/90 transition-colors`
- Header row: `flex items-center justify-between`

After making all changes, run TypeScript check.
  </action>

  <verify>
    <automated>
      cd C:/ziko-platform/apps/web && rtk npx tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "px-10 py-8" apps/web/src/components/coach/InvitationCodeCard.tsx` returns a match (generated-code card uses wider padding)
    - `grep "text-4xl font-bold font-mono tabular-nums" apps/web/src/components/coach/InvitationCodeCard.tsx` returns a match
    - `grep "animate-in fade-in slide-in-from-top-2" apps/web/src/components/coach/GeneratePanel.tsx` returns a match
    - `grep "bg-background" apps/web/src/components/coach/InvitationsTable.tsx` returns a match (thead background)
    - `grep "hover:bg-background/60" apps/web/src/components/coach/InvitationsTable.tsx` returns a match (tbody row hover)
    - `grep "text-xs tracking-wide" apps/web/src/components/coach/InvitationsTable.tsx` returns a match (thead th classes)
    - `grep "bg-green-50 text-green-700" apps/web/src/components/coach/InvitationsTable.tsx` returns a match (Actif chip)
    - `grep "border border-primary bg-primary/10" apps/web/src/components/coach/ExpirationChipGroup.tsx` returns a match (selected chip)
    - TypeScript compiles with zero errors across all modified files
  </acceptance_criteria>

  <done>Coach-side components (InvitationCodeCard, GeneratePanel, InvitationsTable, ExpirationChipGroup, FilterChipGroup, InvitationsClient) match UI-SPEC.md class strings exactly. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix athlete-side components (CoachPreviewCard, CodeInput, RedeemStateMachine, RevokeConfirmModal)</name>
  <files>
    apps/web/src/components/coach/CoachPreviewCard.tsx,
    apps/web/src/components/coach/CodeInput.tsx,
    apps/web/src/components/coach/RedeemStateMachine.tsx,
    apps/web/src/components/coach/RevokeConfirmModal.tsx,
    apps/web/src/app/[locale]/redeem/page.tsx
  </files>

  <read_first>
    - apps/web/src/components/coach/CoachPreviewCard.tsx
    - apps/web/src/components/coach/CodeInput.tsx
    - apps/web/src/components/coach/RedeemStateMachine.tsx
    - apps/web/src/components/coach/RevokeConfirmModal.tsx
    - apps/web/src/app/[locale]/redeem/page.tsx
    - .planning/phases/25-invitations-mobile-mon-coach-minimal/25-UI-SPEC.md (§Coach Preview Card + §Typed-Confirmation Modal + §Input)
  </read_first>

  <action>
Read each component fully. Compare EVERY Tailwind class against the `<interfaces>` block above. Fix deviations.

**CodeInput.tsx:**
- Input element: `w-full max-w-xs h-14 rounded-xl border border-border bg-white text-center text-2xl font-bold font-mono tabular-nums tracking-widest uppercase text-text focus:outline-none focus:ring-2 focus:ring-primary placeholder:text-muted`
- aria-label must be `"Code d'invitation"`

**CoachPreviewCard.tsx (already read — matches mostly but verify):**
- Outer card: `bg-white rounded-2xl border border-border p-6 shadow-sm text-left`
- Photo: `w-[72px] h-[72px] rounded-full object-cover border border-border`
- Name: `text-lg font-bold text-text`
- KYC verified chip: `inline-flex items-center gap-1 mt-2 bg-primary/10 text-primary border border-primary/20 rounded-full px-3 py-1 text-xs font-bold`
- KYC pending chip: `inline-flex items-center gap-1 mt-2 bg-yellow-50 text-yellow-700 border border-yellow-200 rounded-full px-3 py-1 text-xs font-bold`
- Specialty chips: `border border-border bg-background text-text text-xs font-normal rounded-full px-3 py-1`
- Bio text: `text-sm font-normal text-muted mt-4 whitespace-pre-line`
- Bio toggle: `text-primary text-sm font-normal underline`
- Photo row layout: `flex items-start gap-4` (photo on left, name+KYC stacked on right)
- The `flex-1 min-w-0` wrapper around name+KYC is correct — keep it

**RedeemStateMachine.tsx — layout classes:**
- State A wrapper: `max-w-sm mx-auto pt-16 text-center`
- State A icon: `mx-auto text-muted` at `size={48}` — correct
- State A heading: `text-2xl font-bold text-text mt-4`
- State A body: `text-sm font-normal text-muted mt-2`
- State A code input wrapper: `mt-8 flex justify-center`
- State A submit button: primary button class + `mt-4`
- State A error: `text-sm text-red-600 mt-3 text-center` with `role="alert"`
- State B wrapper: `max-w-md mx-auto pt-8`
- State B back link: `text-sm text-muted hover:text-text` — the text must be `"← Entrer un autre code"` (check fr.json CoachRedeem.stateB.back key)
- State B CTA row: `flex items-center gap-4 mt-6 justify-center`
- State B "Lier mon compte": primary button class
- State B "Annuler": `text-muted text-sm hover:text-text` (text link, no border)
- State C wrapper: `max-w-md mx-auto pt-8`
- State C banner: `text-sm font-normal text-muted text-center mb-6`
- State C "Retirer ce coach": `text-red-600 text-sm font-normal underline`
- State C revoke wrapper: `mt-6 text-center`

**RevokeConfirmModal.tsx:**
- Overlay: `fixed inset-0 bg-black/40 z-50 flex items-center justify-center`
- Panel: `bg-white rounded-2xl p-8 max-w-md w-full border border-border shadow-lg`
- Title: `text-xl font-bold text-text`
- Body: `text-sm font-normal text-muted mt-2`
- Confirm label: `text-sm font-semibold text-text mt-6 block`
- Confirm input: `mt-2 w-full rounded-xl border border-border px-4 py-3 text-sm font-normal text-text focus:outline-none focus:ring-2 focus:ring-primary`
- Action row: `flex items-center gap-3 mt-6 justify-end`
- Destructive button: `bg-red-600 text-white rounded-xl px-6 py-3 text-sm font-bold hover:bg-red-700 disabled:opacity-40 disabled:cursor-not-allowed transition-colors`
- Cancel/Annuler button: `border border-border bg-white text-text rounded-xl px-6 py-3 text-sm font-normal hover:bg-background transition-colors`
- The focus trap (Tab key handler added in the UAT gap fix) must be preserved — do NOT remove the `onKeyDown` handler on the window or the `dialogRef` usage

**redeem/page.tsx:**
- Page wrapper: `min-h-screen bg-background px-6 py-8` — verify this is present (it is in the current file; confirm no regression from plan 08 changes)

After all fixes, check i18n keys:
- `apps/web/messages/fr.json` CoachRedeem.stateB.back should be `"← Entrer un autre code"`
- `apps/web/messages/fr.json` CoachRedeem.stateA.heading should be `"Rejoignez votre coach"`
- `apps/web/messages/fr.json` CoachRedeem.stateA.body should be `"Entrez le code de 6 caractères fourni par votre coach."`
- `apps/web/messages/fr.json` CoachRedeem.stateA.submit should be `"Valider"`
- If any key value is wrong, fix it in fr.json AND en.json

Run TypeScript check after all changes.
  </action>

  <verify>
    <automated>
      cd C:/ziko-platform/apps/web && rtk npx tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "h-14 rounded-xl border border-border" apps/web/src/components/coach/CodeInput.tsx` returns a match
    - `grep "text-2xl font-bold font-mono tabular-nums" apps/web/src/components/coach/CodeInput.tsx` returns a match
    - `grep "bg-black/40" apps/web/src/components/coach/RevokeConfirmModal.tsx` returns a match (overlay)
    - `grep "rounded-2xl p-8 max-w-md" apps/web/src/components/coach/RevokeConfirmModal.tsx` returns a match (panel)
    - `grep "bg-red-600" apps/web/src/components/coach/RevokeConfirmModal.tsx` returns a match (destructive button)
    - `grep "max-w-sm mx-auto pt-16" apps/web/src/components/coach/RedeemStateMachine.tsx` returns a match (State A)
    - `grep "max-w-md mx-auto pt-8" apps/web/src/components/coach/RedeemStateMachine.tsx` returns a match (State B/C)
    - `grep "bg-primary/10 text-primary border border-primary/20" apps/web/src/components/coach/CoachPreviewCard.tsx` returns a match (KYC verified chip)
    - RevokeConfirmModal focus trap (dialogRef + keydown handler) is still present after edits — `grep "dialogRef" apps/web/src/components/coach/RevokeConfirmModal.tsx` returns a match
    - TypeScript compiles with zero errors across all modified files
    - `grep "Entrer un autre code" apps/web/messages/fr.json` returns a match
  </acceptance_criteria>

  <done>Athlete-side components (CoachPreviewCard, CodeInput, RedeemStateMachine, RevokeConfirmModal) match UI-SPEC.md class strings exactly. i18n keys verified. Focus trap preserved. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI classes → rendered pixels | No trust boundary — pure visual audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09-01 | Tampering | RevokeConfirmModal focus trap removal | mitigate | Acceptance criterion explicitly requires dialogRef grep to pass; plan instructs "do NOT remove the onKeyDown handler" |
</threat_model>

<verification>
After both tasks:

1. `cd C:/ziko-platform/apps/web && rtk npx tsc --noEmit` — zero errors
2. Spot-check grep battery (run each):
   - `grep "px-10 py-8" apps/web/src/components/coach/InvitationCodeCard.tsx`
   - `grep "animate-in fade-in" apps/web/src/components/coach/GeneratePanel.tsx`
   - `grep "bg-background" apps/web/src/components/coach/InvitationsTable.tsx`
   - `grep "bg-black/40" apps/web/src/components/coach/RevokeConfirmModal.tsx`
   - `grep "h-14 rounded-xl" apps/web/src/components/coach/CodeInput.tsx`
   - `grep "max-w-sm mx-auto pt-16" apps/web/src/components/coach/RedeemStateMachine.tsx`
3. Visual browser spot-check: open `/fr/coach/invitations` and `/fr/redeem` side by side with `.planning/mockups/Ziko-Screens.html` and confirm major layout elements align.
</verification>

<success_criteria>
- All 9 coach components have Tailwind classes that exactly match the UI-SPEC.md canonical strings
- Zero TypeScript errors
- The focus trap in RevokeConfirmModal is preserved (not regressed)
- i18n keys for CoachRedeem State A/B/C copy match UI-SPEC.md §Copywriting Contract
- Visual comparison with Ziko-Screens.html shows no obvious layout divergences in the page header, table, generated-code card, State A input, or modal
</success_criteria>

<output>
After completion, create `.planning/phases/25-invitations-mobile-mon-coach-minimal/25-09-SUMMARY.md` using the standard summary template.
</output>
