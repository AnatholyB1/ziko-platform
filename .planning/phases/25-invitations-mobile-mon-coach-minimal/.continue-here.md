---
phase: 25
phase_name: invitations-mobile-mon-coach-minimal
paused_at: 2026-05-17T17:50:00Z
status: paused
tasks_done: 8/8
gaps: 1
---

## Resume From

Phase 25 UAT is complete. One minor gap remains: focus trap broken in revoke modal.

**Next action:** `/gsd-execute-phase 25 --gaps-only`

## What Was Done

All 8 phase tasks executed and UAT-verified this session:

1. **25-01** — DB migrations: `coach_invitations` + `coach_client_links` ✓
2. **25-02** — Backend API: preview/redeem/revoke routes ✓
3. **25-03** — Web: `InvitationCodeCard` + invitations page ✓
4. **25-04** — Web: `/redeem` `RedeemStateMachine` (States A/B/C) ✓
5. **25-05** — Web: `/r/[code]` deep-link handler ✓
6. **25-06** — Security: `safeNext` open-redirect prevention ✓
7. **25-07a** — Mobile: Mon Coach tab (State A empty) ✓
8. **25-07b** — Mobile: Mon Coach tab (State B/C preview+revoke) ✓

## UAT Results

| Test | Result |
|------|--------|
| Pixel-perfect visual (invitations + redeem) | pass |
| Pixel-perfect visual (Phase 24 surfaces) | pass |
| Clipboard copy buttons | blocked (needs real browser gesture) |
| Revoke modal focus trap | **issue** (minor) |
| Deep-link unauthenticated round-trip | pass |
| Coach photo signed-URL TTL | blocked (needs 5-6 min wait) |
| Full 10-step smoke /coach/invitations | pass |

## Gap: Focus Trap

**What's broken:** Tab escapes the revoke modal instead of wrapping back to input.

Sequence observed: `INPUT(COACH)` → `BUTTON(Annuler)` → `BUTTON(Révoquer)` → `NEXTJS-PORTAL` → `BODY`

Expected: wraps back to `INPUT(COACH)` after last focusable element.

Escape key dismissal ✓ and initial focus on input ✓ both work correctly.

**Severity:** minor

## Decision Made This Session

`apps/web/src/actions/login.ts` had `export const REDEEM_DEEPLINK_RE` and `export function safeNext` — both non-async exports in a `'use server'` file, causing a Next.js build error. Removed `export` from both; neither was imported externally. Only `loginAction` and `LoginState` type are exported from this file.

## Infrastructure State

- Next.js web app: `http://localhost:3000`
- Hono backend API: `http://localhost:8080`
- Supabase: remote project (linked)
- Mobile: not running in this session
