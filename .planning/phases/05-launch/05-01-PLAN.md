---
phase: 05-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/marketing/Hero.tsx
  - src/app/layout.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Hero image references the real app screenshot (screen.jpg), not a placeholder"
    - "Plausible Analytics script is present in the root layout with data-domain ziko-app.com"
    - "The site builds successfully with both changes applied"
  artifacts:
    - path: "src/components/marketing/Hero.tsx"
      provides: "Hero with real screenshot reference"
      contains: 'src="/screen.jpg"'
    - path: "src/app/layout.tsx"
      provides: "Root layout with Plausible script"
      contains: "plausible.io/js/script.js"
  key_links:
    - from: "src/components/marketing/Hero.tsx"
      to: "public/screen.jpg"
      via: "next/image src prop"
      pattern: 'src="/screen.jpg"'
    - from: "src/app/layout.tsx"
      to: "https://plausible.io/js/script.js"
      via: "next/script tag"
      pattern: 'data-domain="ziko-app.com"'
---

<objective>
Swap the Hero placeholder image to the real Expo app screenshot, and install the Plausible Analytics script in the root layout.

Purpose: These are the only two code changes needed before launch. The Hero currently points to a placeholder PNG; the real screenshot is already at public/screen.jpg. Plausible provides cookieless pageview analytics without requiring a cookie consent banner (per D-05, D-06).

Output: Two modified files ready for production deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launch/05-CONTEXT.md

<interfaces>
<!-- Current state of files being modified -->

From /c/ziko-web/src/components/marketing/Hero.tsx (line 58-59):
```tsx
<Image
  src="/app-screenshot-placeholder.png"
```
Change to: src="/screen.jpg"

From /c/ziko-web/src/app/layout.tsx:
```tsx
import type { Metadata } from 'next'

const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000'

export const metadata: Metadata = {
  metadataBase: new URL(siteUrl),
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Hero placeholder to real screenshot (D-04)</name>
  <files>/c/ziko-web/src/components/marketing/Hero.tsx</files>
  <read_first>
    - /c/ziko-web/src/components/marketing/Hero.tsx
    - /c/ziko-web/public/screen.jpg (verify it exists)
  </read_first>
  <action>
In `/c/ziko-web/src/components/marketing/Hero.tsx`, change the Image `src` prop on line 59 from:
```
src="/app-screenshot-placeholder.png"
```
to:
```
src="/screen.jpg"
```

Per D-04: the real Expo app screenshot is already at `public/screen.jpg`. No other changes to this file. Keep `alt`, `fill`, `priority`, `sizes`, and `style` props exactly as they are.
  </action>
  <verify>
    <automated>cd /c/ziko-web && grep -c 'src="/screen.jpg"' src/components/marketing/Hero.tsx && grep -c 'app-screenshot-placeholder' src/components/marketing/Hero.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'src="/screen.jpg"' src/components/marketing/Hero.tsx` returns exactly 1 match
    - `grep 'app-screenshot-placeholder' src/components/marketing/Hero.tsx` returns 0 matches
    - File still contains `priority` prop on the Image component
    - File still contains `sizes="(max-width: 768px) 100vw, 220px"` prop
  </acceptance_criteria>
  <done>Hero.tsx references /screen.jpg instead of the placeholder. All other Image props unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Install Plausible Analytics script in root layout (D-05, D-06)</name>
  <files>/c/ziko-web/src/app/layout.tsx</files>
  <read_first>
    - /c/ziko-web/src/app/layout.tsx
  </read_first>
  <action>
In `/c/ziko-web/src/app/layout.tsx`:

1. Add the import at the top of the file (after the existing `import type { Metadata } from 'next'`):
```tsx
import Script from 'next/script'
```

2. Inside the `RootLayout` function, add the Plausible `<Script>` tag inside `<body>`, after `{children}`:
```tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="fr">
      <body>
        {children}
        <Script
          defer
          data-domain="ziko-app.com"
          src="https://plausible.io/js/script.js"
          strategy="afterInteractive"
        />
      </body>
    </html>
  )
}
```

Per D-05/D-06: Plausible is cookieless, no consent banner needed. `strategy="afterInteractive"` ensures the script loads after hydration (Next.js best practice). `data-domain` must be exactly `ziko-app.com`.

Do NOT add a `<noscript>` fallback (per Claude's Discretion in CONTEXT.md — Plausible works without it).
  </action>
  <verify>
    <automated>cd /c/ziko-web && grep -c 'plausible.io/js/script.js' src/app/layout.tsx && grep -c 'data-domain="ziko-app.com"' src/app/layout.tsx && grep -c "import Script from 'next/script'" src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'import Script from .next/script.' src/app/layout.tsx` returns exactly 1 match
    - `grep 'data-domain="ziko-app.com"' src/app/layout.tsx` returns exactly 1 match
    - `grep 'plausible.io/js/script.js' src/app/layout.tsx` returns exactly 1 match
    - `grep 'strategy="afterInteractive"' src/app/layout.tsx` returns exactly 1 match
    - The Script tag is inside the body element (after {children})
  </acceptance_criteria>
  <done>Root layout includes Plausible Analytics script with data-domain="ziko-app.com" and strategy="afterInteractive".</done>
</task>

<task type="auto">
  <name>Task 3: Build verification gate</name>
  <files></files>
  <read_first>
    - /c/ziko-web/src/app/layout.tsx (verify Script import is present)
    - /c/ziko-web/src/components/marketing/Hero.tsx (verify screen.jpg reference)
  </read_first>
  <action>
Run the production build from `/c/ziko-web` to verify both changes compile without errors:

```bash
cd /c/ziko-web && npm run build
```

Verify:
1. Build succeeds with exit code 0
2. All `[locale]/*` routes remain static (marked with `○` in build output, not `ƒ`)
3. No TypeScript or import errors from the Script addition

If the build fails, fix the issue before proceeding. The most likely failure would be a missing import or incorrect JSX syntax in layout.tsx.
  </action>
  <verify>
    <automated>cd /c/ziko-web && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - Build output shows routes as static (○ symbol), not dynamic (ƒ symbol)
    - No TypeScript errors in build output
  </acceptance_criteria>
  <done>Production build passes with both code changes applied. All routes remain statically generated.</done>
</task>

</tasks>

<verification>
1. `grep 'src="/screen.jpg"' /c/ziko-web/src/components/marketing/Hero.tsx` — returns match
2. `grep 'app-screenshot-placeholder' /c/ziko-web/src/components/marketing/Hero.tsx` — returns no match
3. `grep 'plausible.io' /c/ziko-web/src/app/layout.tsx` — returns match
4. `grep 'data-domain="ziko-app.com"' /c/ziko-web/src/app/layout.tsx` — returns match
5. `cd /c/ziko-web && npm run build` — exits 0, all routes static
</verification>

<success_criteria>
- Hero.tsx uses /screen.jpg (real screenshot), no placeholder reference remains
- Root layout has Plausible script with correct data-domain and strategy
- Production build passes with zero errors
- All routes remain statically generated
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch/05-01-SUMMARY.md`
</output>
