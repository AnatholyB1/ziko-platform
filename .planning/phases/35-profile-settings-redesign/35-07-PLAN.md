---
phase: 35-profile-settings-redesign
plan: 07
type: execute
wave: 4
depends_on: []
files_modified:
  - apps/mobile/app/(app)/profile/security.tsx
  - apps/mobile/app/(app)/profile/settings.tsx
autonomous: true
requirements: [SET-01]

must_haves:
  truths:
    - "La row 'Mot de passe' dans le groupe Compte navigue vers l'écran Security"
    - "L'utilisateur peut changer son mot de passe via supabase.auth.updateUser"
    - "Validation : 8 chars min, mots de passe concordants, erreur inline en rouge"
    - "Les toggles de confidentialité (Profil public, Stats, Activités) se persistent en JSONB"
  artifacts:
    - path: apps/mobile/app/(app)/profile/security.tsx
      provides: "Écran changement mot de passe + toggles confidentialité"
      exports: [default SecurityScreen]
  key_links:
    - from: apps/mobile/app/(app)/profile/settings.tsx
      to: apps/mobile/app/(app)/profile/security.tsx
      via: router.push('/(app)/profile/security')
      pattern: "router.push.*security"
    - from: apps/mobile/app/(app)/profile/security.tsx
      to: supabase.auth.updateUser
      via: "{ password: newPassword }"
      pattern: "updateUser.*password"
---

<objective>
Créer l'écran `/(app)/profile/security` (changement de mot de passe + toggles de confidentialité JSONB) et câbler la row "Mot de passe" de settings.tsx vers ce nouvel écran.

Purpose: La row "Mot de passe" pointe actuellement vers `/(auth)/forgot` — un flux de réinitialisation par email. L'utilisateur connecté doit pouvoir changer son mot de passe directement via supabase.auth.updateUser.
Output: security.tsx (nouveau), settings.tsx (wiring de la row Mot de passe).
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-profile-settings-redesign/35-UI-SPEC.md
@.planning/phases/35-profile-settings-redesign/35-03-SUMMARY.md

<interfaces>
<!-- supabase.auth.updateUser -->
const { error } = await supabase.auth.updateUser({ password: newPassword });
// Ne nécessite pas le mot de passe actuel si l'utilisateur est déjà authentifié via session active
// error.message si échec

<!-- user_profiles.settings JSONB — pattern merge-safe (établi en plan 03) -->
// 1. Lire : const { data } = await supabase.from('user_profiles').select('settings').eq('id', userId).single();
// 2. Merger : const current = data?.settings ?? {};
// 3. Upsert : supabase.from('user_profiles').upsert({ id: userId, settings: { ...current, privacy: next } });

<!-- Clés JSONB utilisées pour la confidentialité -->
// user_profiles.settings.privacy.is_public (boolean, default true)
// user_profiles.settings.privacy.show_stats (boolean, default true)
// user_profiles.settings.privacy.show_activities (boolean, default true)
// Remarque : is_public n'est pas une colonne dédiée — utiliser settings JSONB

<!-- STRow depuis @ziko/ui (plan 01 established) -->
import { STGroup, STRow, STToggle } from '@ziko/ui';
// STRow props: icon, tint, label, sub?, right?, danger?, onPress?, toggleValue?, onToggle?

<!-- useAuthStore -->
const user = useAuthStore((s) => s.user); // { id, email }

<!-- Design tokens settings.tsx -->
theme.background, theme.surface, theme.border, theme.text, theme.muted
primary: '#FF5C1A', danger: '#E94B3C', success: '#22C55E'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Créer apps/mobile/app/(app)/profile/security.tsx</name>
  <files>apps/mobile/app/(app)/profile/security.tsx</files>
  <action>
Créer l'écran Security. Pattern identique à NotifSubScreen dans settings.tsx — SafeAreaView + STHeader + ScrollView.

Imports : React, useState, useEffect, useRef ; View, Text, TextInput, TouchableOpacity, ActivityIndicator depuis react-native ; SafeAreaView depuis react-native-safe-area-context ; router depuis expo-router ; Ionicons depuis @expo/vector-icons ; useThemeStore, showAlert depuis @ziko/plugin-sdk ; STGroup, STRow depuis @ziko/ui ; useAuthStore depuis stores/authStore ; supabase depuis lib/supabase.

Header : STHeader maison identique à settings.tsx (reproduire le pattern local — chevron-back + titre "Sécurité & Confidentialité"). onBack → router.back().

Section 1 — Changer le mot de passe :
Rendre un STGroup title="Mot de passe" contenant 2 TextInput rows custom (pas STRow — besoin d'input) :
- Row "Nouveau mot de passe" : TextInput secureTextEntry, placeholder "••••••••", style identique aux cards settings.
- Row "Confirmer" : TextInput secureTextEntry, placeholder "••••••••".
Chaque row dans la card (bg:surface, borderRadius:12, shadow md) avec séparateur 1px.

Validation inline (affichée sous les champs) :
- Si newPwd.length > 0 && newPwd.length < 8 → Text rouge "8 caractères minimum".
- Si confirmPwd.length > 0 && newPwd !== confirmPwd → Text rouge "Les mots de passe ne correspondent pas".

Bouton "Enregistrer le mot de passe" (bg:#FF5C1A, color:#fff, borderRadius:12, padding:16, marginTop:16) :
- Disabled si validation échoue ou champs vides.
- opacity:0.5 quand disabled.
- onPress : si newPwd !== confirmPwd → showAlert('Erreur', 'Les mots de passe ne correspondent pas') ; sinon appel async supabase.auth.updateUser({ password: newPwd }) ; si error → showAlert('Erreur', error.message) ; sinon → showAlert('Succès', 'Mot de passe modifié avec succès.') → reset champs.
- Afficher ActivityIndicator à la place du texte pendant l'appel.

Section 2 — Confidentialité (STGroup title="Confidentialité") :
3 STRow avec toggle :
- "Profil public" sub:"Visible des autres utilisateurs" — toggleValue:privacy.is_public — onToggle:updatePrivacy('is_public')
- "Afficher mes stats" sub:"Séances, records" — toggleValue:privacy.show_stats — onToggle:updatePrivacy('show_stats')
- "Afficher mes activités" sub:"Cardio, programmes" — toggleValue:privacy.show_activities — onToggle:updatePrivacy('show_activities')

useEffect au mount : charger settings JSONB depuis user_profiles → setPrivacy({ is_public: prefs?.is_public ?? true, show_stats: prefs?.show_stats ?? true, show_activities: prefs?.show_activities ?? true }).

updatePrivacy(key)(value) : optimistic setPrivacy → clearTimeout(debounceRef) → setTimeout 500ms → lire settings existants → merger { ...current, privacy: { ...current.privacy, [key]: value } } → upsert.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <done>security.tsx existe, compile, contient updateUser password et 3 toggles de confidentialité.</done>
</task>

<task type="auto">
  <name>Task 2 : Câbler settings.tsx — row Mot de passe → security screen</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>
  <action>
Dans settings.tsx, localiser le groupe STGroup title="Compte" (ligne ~434). Trouver la STRow "Mot de passe" :

```
<STRow icon="lock-closed-outline" tint="#1C1A17" label="Mot de passe" sub="Modifier" onPress={() => router.push('/(auth)/forgot' as any)} />
```

Remplacer onPress par router.push('/(app)/profile/security' as any).

Également mettre à jour la STRow "Confidentialité" dans le même groupe :
```
<STRow icon="shield-checkmark-outline" tint="#3B82F6" label="Confidentialité" sub="Profil public · Données partagées" onPress={() => showAlert(...)} />
```
Remplacer onPress par router.push('/(app)/profile/security' as any) (même écran, scroll vers la section Confidentialité ou simplement ouvrir l'écran — l'utilisateur trouvera la section au scroll).

Aucune autre modification dans settings.tsx pour ce plan.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "router.push.*security" apps/mobile/app/\(app\)/profile/settings.tsx</automated>
  </verify>
  <done>settings.tsx contient au moins 1 occurrence de router.push vers security ; TypeScript 0 erreurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → supabase.auth.updateUser | Changement de mot de passe via session JWT active |
| Mobile → user_profiles JSONB | Écriture préférences confidentialité |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07-01 | Spoofing | updateUser password | mitigate | Supabase valide le JWT Bearer de la session active avant d'autoriser updateUser — pas de changement possible sans session valide |
| T-35-07-02 | Tampering | Privacy JSONB keys | mitigate | RLS users_own_profile : auth.uid() = id — seul le propriétaire peut écrire ses préférences |
| T-35-07-03 | Information Disclosure | Mot de passe en clair | mitigate | TextInput secureTextEntry:true sur les deux champs — masqué à l'écran |
| T-35-07-04 | Denial of Service | Spam updateUser | accept | Supabase Auth rate-limite les appels d'API par token — risque faible côté utilisateur légitime |
</threat_model>

<verification>
```bash
# 1. Fichier créé
ls C:/ziko-platform/apps/mobile/app/\(app\)/profile/security.tsx

# 2. updateUser password présent
grep -c "updateUser.*password\|password.*updateUser" C:/ziko-platform/apps/mobile/app/\(app\)/profile/security.tsx

# 3. 3 toggles confidentialité
grep -c "is_public\|show_stats\|show_activities" C:/ziko-platform/apps/mobile/app/\(app\)/profile/security.tsx

# 4. Navigation câblée dans settings
grep -c "router.push.*security" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 5. TypeScript propre
cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep "error TS" | wc -l
```
</verification>

<success_criteria>
- security.tsx existe avec changement de mot de passe (supabase.auth.updateUser) et validation inline
- 3 toggles de confidentialité avec persistance JSONB merge-safe
- settings.tsx row "Mot de passe" (et "Confidentialité") naviguent vers security
- TypeScript 0 erreurs
</success_criteria>

<output>
Créer `.planning/phases/35-profile-settings-redesign/35-07-SUMMARY.md` quand terminé.
</output>
