---
phase: 35-profile-settings-redesign
plan: 14
type: execute
wave: 2
depends_on: ["35-12"]
files_modified:
  - apps/mobile/app/(app)/profile/index.tsx
autonomous: true
requirements: [PROF-05]
gap_closure: true

must_haves:
  truths:
    - "PRBadgesTab affiche les badges réels depuis user_badges JOIN badge_definitions"
    - "Badges gagnés sont affichés en couleur, badges non-gagnés en gris (opacity 0.4)"
    - "check_and_award_badges(userId) est appelée après le chargement du profil"
    - "La query badges utilise queryKey ['badges', userId]"
  artifacts:
    - path: apps/mobile/app/(app)/profile/index.tsx
      provides: "PRBadgesTab connectée aux tables user_badges + badge_definitions"
      contains: "user_badges"
  key_links:
    - from: apps/mobile/app/(app)/profile/index.tsx
      to: supabase user_badges + badge_definitions
      via: "supabase.from('user_badges').select('badge_slug, earned_at, badge_definitions(slug, name, icon, tier)')"
      pattern: "user_badges"
    - from: apps/mobile/app/(app)/profile/index.tsx
      to: supabase RPC check_and_award_badges
      via: "supabase.rpc('check_and_award_badges', { p_user_id: userId })"
      pattern: "check_and_award_badges"
---

<objective>
Connecter PRBadgesTab dans apps/mobile/app/(app)/profile/index.tsx aux vraies tables user_badges et badge_definitions créées par la migration 051. Remplacer la query user_gamification (incorrecte) par une query sur user_badges. Afficher badges earned en couleur, unearned en gris 0.4. Appeler check_and_award_badges après chargement du profil.

Purpose: Actuellement badgesData vient de user_gamification.badges (JSON) qui n'a rien à voir avec les badges d'accomplissement. La migration 051 (plan 35-12) crée les vraies tables — ce plan les câble.
Output: Système de badges réel dans index.tsx.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-profile-settings-redesign/35-CONTEXT.md
@.planning/phases/35-profile-settings-redesign/35-12-SUMMARY.md

<interfaces>
<!-- Badge tables après migration 051 -->
-- badge_definitions : id UUID, slug TEXT UNIQUE, name TEXT, description TEXT, icon TEXT (emoji), condition_type TEXT, condition_value INTEGER, tier INTEGER
-- user_badges : id UUID, user_id UUID, badge_slug TEXT REFERENCES badge_definitions(slug), earned_at TIMESTAMPTZ

<!-- Query badges à utiliser -->
supabase
  .from('user_badges')
  .select('badge_slug, earned_at, badge_definitions(slug, name, icon, tier)')
  .eq('user_id', userId)
-- Retourne : Array<{ badge_slug: string, earned_at: string, badge_definitions: { slug, name, icon, tier } | null }>

<!-- All badge definitions (pour afficher aussi les non-gagnés) -->
supabase.from('badge_definitions').select('slug, name, icon, tier')
-- Retourne tous les 11 badges définis

<!-- RPC check_and_award_badges -->
supabase.rpc('check_and_award_badges', { p_user_id: userId })
-- Retourne void — appeler sans await bloquant (fire-and-forget après refetch)

<!-- Badges actuellement dans index.tsx (lignes 617-630 — à remplacer) -->
const { data: badgesData } = useQuery({
  queryKey: ['badges', userId],
  queryFn: async () => {
    const { data: gdata, error } = await supabase.from('user_gamification').select('badges')...
    // À REMPLACER ENTIÈREMENT
  }
})

<!-- PRBadgesTab props actuels (ligne 356-359) -->
function PRBadgesTab({ badges }: { badges: Array<{ id: string|number, name?, icon?, tint?, unlocked_at? }> })
-- À étendre avec type BadgeItem qui contient earned: boolean

<!-- STATIC_BADGES (lignes 34-41) -->
-- Garder en fallback local uniquement si la query retourne une erreur réseau
-- Ne plus les afficher si la query réussit avec 0 badges (afficher les 11 badges non-gagnés en gris)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Remplacer la query badges par user_badges JOIN badge_definitions</name>
  <files>apps/mobile/app/(app)/profile/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/index.tsx (lire lignes 34-46 STATIC_BADGES, lignes 355-476 PRBadgesTab entier, lignes 616-630 query badges actuelle — lire AVANT toute modification)
  </read_first>
  <action>
Dans apps/mobile/app/(app)/profile/index.tsx, effectuer ces modifications :

1. Ajouter un type BadgeItem en haut du fichier (après les imports) :
```
type BadgeItem = {
  slug: string;
  name: string;
  icon: string;  // emoji
  tier: number;
  earned: boolean;
  earned_at: string | null;
};
```

2. Remplacer la query badgesData (lignes ~616-630) par deux queries parallèles :
   a. Query allBadges : `supabase.from('badge_definitions').select('slug, name, icon, tier')` — retourne tous les 11 badges définis
   b. Query earnedBadgeSlugs : `supabase.from('user_badges').select('badge_slug, earned_at').eq('user_id', userId!)` — retourne les badges gagnés par l'user

   Merger dans un seul useQuery avec queryKey ['badges', userId] :
   - Exécuter Promise.all([allBadgesQuery, earnedQuery])
   - Si erreur sur allBadges → retourner [] (fallback gracieux)
   - Construire un Set<string> des badge_slugs gagnés
   - Retourner `allBadges.map(b => ({ slug: b.slug, name: b.name, icon: b.icon, tier: b.tier, earned: earnedSlugs.has(b.slug), earned_at: earnedMap.get(b.slug) ?? null }))` trié : earned en premier, puis par tier DESC

3. Après le useQuery du profil principal (lignes ~487-597), ajouter un appel fire-and-forget à check_and_award_badges :
   Dans le useEffect ou directement après que la query profil réussit (via `useEffect(() => { if (data && userId) { supabase.rpc('check_and_award_badges', { p_user_id: userId }).then(() => queryClient.invalidateQueries({ queryKey: ['badges', userId] })); } }, [data?.stats?.sessions, userId]);`).
   La dépendance est data?.stats?.sessions pour ne pas appeler à chaque re-render — uniquement quand le compte change.

4. Mettre à jour PRBadgesTab signature :
   - Props : `{ badges: BadgeItem[] }` (plus de fallback STATIC_BADGES dans PRBadgesTab)
   - `earnedBadges` = badges.filter(b => b.earned)  
   - `unearnedBadges` = badges.filter(b => !b.earned)
   - Supprimer la ligne `const earnedBadges = badges.length === 0 ? STATIC_BADGES : badges;`

5. Dans le JSX de PRBadgesTab, mettre à jour le rendu :
   - Badges earned : afficher normalement (opacité 1.0), icon = emoji dans un Text (pas Ionicons car les icons badge_definitions sont des emojis), tint = couleurs par tier (tier 1: '#E8A33A' bronze, tier 2: '#9CA3AF' argent, tier 3: '#FFD700' or)
   - Badges non-earned : même structure mais `opacity: 0.4`, icon lock 'lock-closed-outline' (Ionicons) à la place
   - En-tête : `{earnedBadges.length} obtenus · {unearnedBadges.length} à débloquer`

6. Mettre à jour l'appel PRBadgesTab (ligne ~1052) :
   `<PRBadgesTab badges={badgesData ?? []} />`
   (inchangé en surface, mais badgesData est maintenant BadgeItem[])

7. STATIC_BADGES (lignes 34-41) : garder la const mais ne plus l'utiliser dans PRBadgesTab. Elle peut rester comme fallback pour d'autres usages futurs.
  </action>
  <verify>
    <automated>grep -c "user_badges" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx</automated>
  </verify>
  <done>La query badges interroge user_badges et badge_definitions (pas user_gamification). PRBadgesTab accepte BadgeItem[] avec earned boolean. TypeScript 0 erreurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → user_badges | Lecture RLS-protégée : seul auth.uid() = user_id voit ses badges |
| Mobile → check_and_award_badges RPC | SECURITY DEFINER RPC — input = UUID utilisateur authentifié |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-14-01 | Spoofing | user_badges query | mitigate | RLS policy user_badges_own (migration 051) — auth.uid() = user_id filtre au niveau DB |
| T-35-14-02 | Information Disclosure | badge_definitions SELECT | accept | badge_definitions est une table de référence publique (pas de données privées) |
| T-35-14-03 | Tampering | check_and_award_badges RPC | mitigate | SECURITY DEFINER + p_user_id forcé = userId authentifié extrait du JWT |
| T-35-14-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan |
</threat_model>

<verification>
```bash
# 1. user_badges présent dans index.tsx
grep -c "user_badges" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 2. badge_definitions présent
grep -c "badge_definitions" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 3. check_and_award_badges RPC présent
grep -c "check_and_award_badges" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 4. user_gamification supprimé de la query badges
grep -c "user_gamification.*badges" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx || echo "0"

# 5. TypeScript propre
cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -v "^#" | grep -c "error TS" || echo "0"
```
</verification>

<success_criteria>
- [ ] Query badges interroge user_badges et badge_definitions (pas user_gamification)
- [ ] PRBadgesTab reçoit BadgeItem[] avec earned boolean
- [ ] Badges earned affichés en couleur, unearned en gris opacity 0.4
- [ ] check_and_award_badges RPC appelée (fire-and-forget) après chargement profil
- [ ] TypeScript 0 erreurs
</success_criteria>

<output>
Créer `.planning/phases/35-profile-settings-redesign/35-14-SUMMARY.md` quand terminé.
</output>
