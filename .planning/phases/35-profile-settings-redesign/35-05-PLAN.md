---
phase: 35-profile-settings-redesign
plan: 05
type: execute
wave: 3
depends_on:
  - 35-01
  - 35-02
  - 35-03
  - 35-04
files_modified: []
autonomous: false
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - PROF-06
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - SET-05

must_haves:
  truths:
    - "Profile hero renders at exactly 160px height with gradient from avatarColor to #1C1A17"
    - "Avatar renders at 84px with -44px marginTop overlap and 4px solid #F7F6F3 border"
    - "Stats tab shows real data: sessions count, streak days, PR count, active weeks"
    - "Progrès tab shows photo gallery from body_measurements"
    - "Badges tab shows 3-column earned + locked grid"
    - "Settings STGroup/STRow system imported from @ziko/ui — no inline redefinitions"
    - "NotifSubScreen: 9 toggles save to user_profiles.settings.notif_prefs with 500ms debounce"
    - "AppearanceSubScreen: units save to user_profiles.settings.appearance.units_preference"
    - "IntegrationsSubScreen shows real connected state from health_sync_log"
  artifacts:
    - path: "apps/mobile/app/(app)/profile/index.tsx"
      provides: "ProfileScreen — all 3 tabs functional with real data"
      contains: "ProfileHero.*PRStatsTab.*PRProgressTab.*PRBadgesTab"
    - path: "apps/mobile/app/(app)/profile/settings.tsx"
      provides: "SettingsScreen + all 3 sub-screens with @ziko/ui components and real data"
      contains: "from '@ziko/ui'.*STGroup.*STRow"
    - path: "packages/ui/src/components/STRow.tsx"
      provides: "Shared STRow component"
      exports: ["STRow"]
    - path: "packages/ui/src/components/ProfileHero.tsx"
      provides: "Shared ProfileHero component"
      exports: ["ProfileHero"]
  key_links:
    - from: "apps/mobile/app/(app)/profile/index.tsx"
      to: "packages/ui/src/components/ProfileHero.tsx"
      via: "import { ProfileHero } from '@ziko/ui'"
      pattern: "ProfileHero"
    - from: "apps/mobile/app/(app)/profile/settings.tsx"
      to: "packages/ui/src/components/STGroup.tsx"
      via: "import { STGroup, STRow } from '@ziko/ui'"
      pattern: "from '@ziko/ui'"
---

<objective>
Verify Phase 35 is complete: all success criteria from the ROADMAP are met, TypeScript compiles clean, automated grep assertions pass, and the user confirms visual correctness via a smoke test on device/simulator.

Purpose: Catch any gaps before closing the phase. The verification plan runs automated checks first, identifies any failing assertions, triggers gap closure if needed, then presents the human smoke test checklist.

Output: Phase 35 VERIFICATION.md with PASS/FAIL status per success criterion.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/PROJECT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/ROADMAP-v1.7.md
@C:/ziko-platform/.planning/phases/35-profile-settings-redesign/35-UI-SPEC.md
@C:/ziko-platform/.planning/phases/35-profile-settings-redesign/35-01-SUMMARY.md
@C:/ziko-platform/.planning/phases/35-profile-settings-redesign/35-02-SUMMARY.md
@C:/ziko-platform/.planning/phases/35-profile-settings-redesign/35-03-SUMMARY.md
@C:/ziko-platform/.planning/phases/35-profile-settings-redesign/35-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <read_first>
    - .planning/phases/35-profile-settings-redesign/35-UI-SPEC.md (sections: Component Inventory, Data Wiring, Implementation Delta)
    - .planning/phases/35-profile-settings-redesign/35-01-SUMMARY.md
    - .planning/phases/35-profile-settings-redesign/35-02-SUMMARY.md
    - .planning/phases/35-profile-settings-redesign/35-03-SUMMARY.md
    - .planning/phases/35-profile-settings-redesign/35-04-SUMMARY.md
  </read_first>
  <action>
    Run the following automated checks in sequence. Record PASS or FAIL + evidence for each. After all checks, produce a summary table.

    **A. TypeScript compile**
    ```bash
    cd /c/ziko-platform && rtk tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | grep -c "error TS"
    cd /c/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -c "error TS"
    ```
    PASS: both outputs = 0.

    **B. Shared component exports**
    ```bash
    grep -c "export { STRow }" packages/ui/src/index.ts
    grep -c "export { STGroup }" packages/ui/src/index.ts
    grep -c "export { STToggle }" packages/ui/src/index.ts
    grep -c "export { PRStatCard }" packages/ui/src/index.ts
    grep -c "export { ProfileHero }" packages/ui/src/index.ts
    ```
    PASS: each = 1.

    **C. ProfileHero dimensions**
    ```bash
    grep -c "height.*160\|160.*height" packages/ui/src/components/ProfileHero.tsx
    ```
    PASS: >= 1.

    **D. Avatar overlap**
    ```bash
    grep -c "marginTop.*-44\|-44.*marginTop" apps/mobile/app/\(app\)/profile/index.tsx
    ```
    PASS: >= 1.

    **E. Followers row labels**
    ```bash
    grep -c "ABONNÉS\|ABONNEMENTS\|SEMAINES" apps/mobile/app/\(app\)/profile/index.tsx
    ```
    PASS: 3.

    **F. PRStatCard labels (all 4)**
    ```bash
    grep -c "Séances totales" apps/mobile/app/\(app\)/profile/index.tsx
    grep -c "Jours d'affilée" apps/mobile/app/\(app\)/profile/index.tsx
    grep -c "PR battus" apps/mobile/app/\(app\)/profile/index.tsx
    grep -c "Semaines actives" apps/mobile/app/\(app\)/profile/index.tsx
    ```
    PASS: each >= 1.

    **G. Tab labels**
    ```bash
    grep -c '"Stats"\|"Progrès"\|"Badges"' apps/mobile/app/\(app\)/profile/index.tsx
    ```
    PASS: 3.

    **H. Settings uses @ziko/ui imports (not inline)**
    ```bash
    grep -c "from '@ziko/ui'" apps/mobile/app/\(app\)/profile/settings.tsx
    # Verify no local redefinition:
    grep -v "^import\|^//" apps/mobile/app/\(app\)/profile/settings.tsx | grep -c "^function STGroup\b\|^function STRow\b\|^function STToggle\b"
    ```
    PASS: first >= 1; second = 0.

    **I. NotifSubScreen debounce**
    ```bash
    grep -c "setTimeout" apps/mobile/app/\(app\)/profile/settings.tsx
    grep -c "notif_prefs" apps/mobile/app/\(app\)/profile/settings.tsx
    ```
    PASS: each >= 1.

    **J. IntegrationsSubScreen — real data**
    ```bash
    grep -c "health_sync_log" apps/mobile/app/\(app\)/profile/settings.tsx
    grep -c "queryKey.*integrations\|integrations.*queryKey" apps/mobile/app/\(app\)/profile/settings.tsx
    ```
    PASS: each >= 1.

    **K. Wrong icons removed**
    ```bash
    grep -c "bicycle-outline\|location-outline" apps/mobile/app/\(app\)/profile/settings.tsx
    ```
    PASS: = 0.

    **L. Version footer copy**
    ```bash
    grep -c "v2.4.1" apps/mobile/app/\(app\)/profile/settings.tsx
    ```
    PASS: >= 1.

    **M. No StyleSheet.create in new UI files**
    ```bash
    grep -l "StyleSheet.create" packages/ui/src/components/STRow.tsx packages/ui/src/components/STGroup.tsx packages/ui/src/components/STToggle.tsx packages/ui/src/components/ProfileHero.tsx packages/ui/src/components/PRStatCard.tsx 2>/dev/null | wc -l
    ```
    PASS: = 0.

    **N. No Alert.alert usage**
    ```bash
    grep -rn "Alert\.alert" apps/mobile/app/\(app\)/profile/ packages/ui/src/components/ 2>/dev/null | grep -v "showAlert" | wc -l
    ```
    PASS: = 0.

    **O. TanStack Query keys in profile/index.tsx**
    ```bash
    grep -c "queryKey.*'profile'" apps/mobile/app/\(app\)/profile/index.tsx
    grep -c "queryKey.*'measurements'" apps/mobile/app/\(app\)/profile/index.tsx
    grep -c "queryKey.*'badges'" apps/mobile/app/\(app\)/profile/index.tsx
    ```
    PASS: each >= 1.

    **P. Locked badges in Badges tab**
    ```bash
    grep -c "lock-closed-outline" apps/mobile/app/\(app\)/profile/index.tsx
    grep -c "Verrouillé" apps/mobile/app/\(app\)/profile/index.tsx
    ```
    PASS: each >= 1.

    For any FAIL: note the issue and what needs fixing. If more than 2 checks fail, output:
    `## GAPS DETECTED — Run /gsd:plan-phase --gaps 35 to create gap closure plans.`

    Otherwise list each fix inline as an action item.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - All 16 automated checks (A through P) output their PASS condition
    - Summary table shows ≥ 14/16 PASS (2 FAIL tolerance for minor issues that can be fixed inline)
    - Any FAIL is documented with exact failing command output
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — profile and settings on device</name>
  <what-built>
    Phase 35 rebuilt the Profile and Settings screens:
    - ProfileScreen: 160px gradient hero cover, 84px avatar overlapping at -44px, followers row, 3 tabs (Stats/Progrès/Badges) with real data from Supabase
    - SettingsScreen: STGroup/STRow system from @ziko/ui, 4 groups, account card with PREMIUM badge
    - NotifSubScreen: 9 toggles that save to user_profiles.settings.notif_prefs
    - AppearanceSubScreen: theme cards (Sombre/Auto locked), language, units persistence
    - IntegrationsSubScreen: real connection state from health_sync_log, 6 integration cards
  </what-built>
  <how-to-verify>
    Start the app and verify the following on your device or simulator:

    **Profile screen (own mode):**
    1. Navigate to the Profil tab. The hero cover should be a gradient from your avatar color to #1C1A17 and be approximately 160px tall.
    2. The avatar circle (84px, rounded square, initials) should visually overlap the hero bottom edge by about half its height.
    3. Your name, @handle, and bio appear below the avatar. The goal pill card (orange tinted, "OBJECTIF" eyebrow) is visible.
    4. Three numbers appear: Abonnés / Abonnements / Semaines (followers, following, active weeks from real data).
    5. Tap "Stats" tab → 4 stat cards appear (Séances totales, Jours d'affilée, PR battus, Semaines actives). Tap "Progrès" → photo gallery or "Ajouter" dashed card appears. Tap "Badges" → badge grid with some earned + 3 locked badges.
    6. If you have 0 sessions, the empty state should appear: "Aucune activité pour l'instant" with CTA.
    7. The floating settings gear button (top-right, glass bg) should be visible.

    **Settings screen:**
    8. Tap the gear icon → Settings opens. Account card shows your name, email, and PREMIUM badge.
    9. Scroll through all 4 groups: Compte, Abonnement, Préférences, Aide & infos. All rows should have icon tiles (colored squares), labels, and right elements.
    10. Tap "Notifications" → NotifSubScreen opens with 3 groups and 9 toggles. Toggle any switch and wait 1 second — the toggle state should persist (reopen the sub-screen to verify).
    11. Tap "Apparence" → 3 theme cards (Clair active, Sombre/Auto visually dimmed). Units selector shows Métrique/Impérial. Select Impérial → it should persist on next open.
    12. Tap "Intégrations" → Info banner "Tes données restent à toi" appears. 6 integration cards listed. Cards connected via health_sync_log should show a green dot.
    13. Tap "Se déconnecter" → alert appears with title "Se déconnecter ?" and buttons "Annuler" / "Se déconnecter". Tap Annuler to dismiss.
    14. Footer shows "Ziko · v2.4.1 · build 8842".
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 14 checks pass, or describe what failed (e.g. "hero not visible", "toggle not persisting", "wrong tab content").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification only | No new code written in this plan — verification and human review only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05-01 | — | Verification plan | accept | No code changes in this plan; no new attack surface introduced |
</threat_model>

<verification>
Phase 35 is complete when:

1. All automated grep checks (A–P above) PASS
2. TypeScript compiles clean in both packages/ui and apps/mobile (0 error TS)
3. Human smoke test returns "approved" from the user

Write `.planning/phases/35-profile-settings-redesign/35-VERIFICATION.md` with:
- Pass/Fail table for all automated checks
- Human smoke test result
- Phase 35 status: COMPLETE or GAPS DETECTED
</verification>

<success_criteria>
**PROF-01**: Profile has 160px hero and 84px avatar (grep C + D PASS)
**PROF-02**: Stats row shows séances, streak, weeks with real data from TanStack Query (grep O PASS)
**PROF-03**: Three tabs functional — Stats/Progrès/Badges (grep G PASS + human check step 5)
**PROF-04**: Followers row present with ABONNÉS/ABONNEMENTS/SEMAINES (grep E PASS)
**PROF-05**: Data from TanStack Query ['profile', userId] — no hardcoded fixtures (grep O PASS)
**PROF-06**: Own mode shows "Modifier" CTA; confirmed by human check step 7
**SET-01**: STGroup/STRow from @ziko/ui used throughout settings (grep H PASS)
**SET-02**: NotifSubScreen has 9 toggles saved to user_profiles.settings.notif_prefs (grep I PASS + human check step 10)
**SET-03**: AppearanceSubScreen: theme cards, language, units persisted (human check step 11)
**SET-04**: IntegrationsSubScreen: 6 integrations with real connection state from health_sync_log (grep J PASS + human check step 12)
**SET-05**: "Supprimer le compte" row present; Mon coach section renders when linked (grep checks + human check)
</success_criteria>

<output>
Create `.planning/phases/35-profile-settings-redesign/35-05-SUMMARY.md` when done.
Write `.planning/phases/35-profile-settings-redesign/35-VERIFICATION.md` with full check results.
</output>
