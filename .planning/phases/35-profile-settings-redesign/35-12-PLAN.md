---
phase: 35-profile-settings-redesign
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/051_profile_settings_gap.sql
  - apps/mobile/app/(app)/profile/edit.tsx
autonomous: false
requirements: [PROF-01, PROF-04, PROF-06]
gap_closure: true

must_haves:
  truths:
    - "Les colonnes bio, handle, is_public, subscription_tier existent dans user_profiles en production"
    - "Les tables badge_definitions et user_badges existent avec RLS"
    - "11 badges sont seeded dans badge_definitions"
    - "La fonction check_and_award_badges(UUID) existe dans la DB"
    - "Les buckets avatars et profile-photos sont présents (INSERT ON CONFLICT DO NOTHING)"
    - "edit.tsx sauvegarde bio et handle comme colonnes directes (pas settings JSONB)"
  artifacts:
    - path: supabase/migrations/051_profile_settings_gap.sql
      provides: "Migration complète: colonnes + badge tables + seed + RPC + buckets"
      contains: "ADD COLUMN IF NOT EXISTS bio"
    - path: apps/mobile/app/(app)/profile/edit.tsx
      provides: "handleSave upsert avec bio et handle colonnes directes"
      contains: "bio: bio.trim()"
  key_links:
    - from: apps/mobile/app/(app)/profile/edit.tsx
      to: supabase user_profiles
      via: "supabase.from('user_profiles').upsert({ id, name, goal, bio, handle })"
      pattern: "upsert.*bio"
---

<objective>
Créer la migration 051_profile_settings_gap.sql qui ajoute les colonnes manquantes à user_profiles, crée les tables de badges, seed 11 badge_definitions, crée la RPC check_and_award_badges, et sécurise les buckets storage. Puis mettre à jour edit.tsx pour sauvegarder bio et handle comme colonnes directes (au lieu de settings JSONB).

Purpose: Les colonnes bio/handle/is_public/subscription_tier n'existent pas dans user_profiles — c'est la cause racine de tous les bugs du smoke test. La migration 051 résout toutes les dépendances de schéma.
Output: Migration SQL appliquée en prod + edit.tsx mise à jour.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone-mobile/ROADMAP-v1.7.md
@.planning/phases/35-profile-settings-redesign/35-CONTEXT.md

<interfaces>
<!-- user_profiles colonnes existantes (migration 001 + extensions) -->
-- id UUID PRIMARY KEY REFERENCES auth.users(id)
-- name TEXT
-- age INTEGER
-- weight_kg DECIMAL
-- height_cm DECIMAL
-- goal TEXT CHECK (IN 'muscle_gain','fat_loss','maintenance','endurance','strength','health')
-- avatar_url TEXT
-- avatar_color TEXT
-- settings JSONB DEFAULT '{}'
-- fitness_level TEXT
-- workout_frequency TEXT
-- equipment TEXT
-- onboarding_done BOOLEAN

<!-- Colonnes MANQUANTES à ajouter (Decision 1) -->
-- bio TEXT
-- handle TEXT UNIQUE
-- is_public BOOLEAN DEFAULT true
-- subscription_tier TEXT DEFAULT 'free' CHECK (IN 'free','premium','coach')

<!-- edit.tsx handleSave actuel (lignes 207-240) — à modifier -->
-- Actuellement: upsert { id, name, goal, settings: { ...existingSettings, bio: bio.trim() } }
-- Cible: upsert { id, name, goal, bio: bio.trim(), handle: handle.trim() } sans fetch settings préalable

<!-- Bucket storage : migrations 017 (avatars) + 025 (profile-photos) -->
-- Safeguard: INSERT INTO storage.buckets (id, name, public) VALUES ('avatars', 'avatars', true) ON CONFLICT (id) DO NOTHING;
-- Idem pour 'profile-photos'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Créer supabase/migrations/051_profile_settings_gap.sql</name>
  <files>supabase/migrations/051_profile_settings_gap.sql</files>
  <read_first>
    - supabase/migrations/001_initial_schema.sql (colonnes existantes user_profiles)
    - supabase/migrations/017_avatars_storage.sql (bucket avatars existant)
    - supabase/migrations/025_storage_buckets.sql (bucket profile-photos existant)
    - supabase/migrations/050_coach_ai_schema.sql (pattern migration récente)
    - .planning/phases/35-profile-settings-redesign/35-CONTEXT.md (Decisions 1, 2, 3)
  </read_first>
  <action>
Créer le fichier supabase/migrations/051_profile_settings_gap.sql avec le contenu suivant, dans cet ordre exact :

SECTION 1 — ADD COLUMNS sur user_profiles :
ALTER TABLE public.user_profiles
  ADD COLUMN IF NOT EXISTS bio TEXT,
  ADD COLUMN IF NOT EXISTS handle TEXT UNIQUE,
  ADD COLUMN IF NOT EXISTS is_public BOOLEAN DEFAULT true,
  ADD COLUMN IF NOT EXISTS subscription_tier TEXT DEFAULT 'free' CHECK (subscription_tier IN ('free', 'premium', 'coach'));

SECTION 2 — CREATE TABLE badge_definitions :
CREATE TABLE IF NOT EXISTS public.badge_definitions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  slug TEXT UNIQUE NOT NULL,
  name TEXT NOT NULL,
  description TEXT NOT NULL,
  icon TEXT NOT NULL,
  condition_type TEXT NOT NULL CHECK (condition_type IN ('sessions', 'streak', 'prs', 'profile_complete', 'friends')),
  condition_value INTEGER NOT NULL,
  tier INTEGER DEFAULT 1
);

SECTION 3 — SEED 11 badge_definitions (INSERT ... ON CONFLICT DO NOTHING) :
INSERT INTO public.badge_definitions (slug, name, description, icon, condition_type, condition_value, tier) VALUES
  ('first_session',    'Première séance',   'Complète ta 1ère séance',     '🏋️', 'sessions',         1,   1),
  ('ten_sessions',     '10 séances',        'Complète 10 séances',         '💪', 'sessions',         10,  1),
  ('fifty_sessions',   '50 séances',        'Complète 50 séances',         '🔥', 'sessions',         50,  2),
  ('hundred_sessions', '100 séances',       'Complète 100 séances',        '🏆', 'sessions',         100, 3),
  ('streak_7',         'Streak 7 jours',    'Garde ta streak 7 jours',     '⚡', 'streak',           7,   1),
  ('streak_30',        'Streak 30 jours',   'Garde ta streak 30 jours',    '🌟', 'streak',           30,  2),
  ('streak_100',       'Streak 100 jours',  'Garde ta streak 100 jours',   '👑', 'streak',           100, 3),
  ('first_pr',         'Premier PR',        'Bats ton 1er record perso',   '🎯', 'prs',              1,   1),
  ('ten_prs',          '10 PRs battus',     'Bats 10 records perso',       '💎', 'prs',              10,  2),
  ('profile_complete', 'Profil complet',    'Complète ton profil',         '✅', 'profile_complete', 1,   1),
  ('first_friend',     'Premier ami',       'Ajoute ton 1er ami',          '🤝', 'friends',          1,   1)
ON CONFLICT (slug) DO NOTHING;

SECTION 4 — CREATE TABLE user_badges + RLS :
CREATE TABLE IF NOT EXISTS public.user_badges (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  badge_slug TEXT REFERENCES public.badge_definitions(slug),
  earned_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(user_id, badge_slug)
);
ALTER TABLE public.user_badges ENABLE ROW LEVEL SECURITY;
CREATE POLICY IF NOT EXISTS "user_badges_own" ON public.user_badges
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);

SECTION 5 — CREATE OR REPLACE FUNCTION check_and_award_badges :
La fonction vérifie les conditions pour un utilisateur et insère dans user_badges les badges mérités non encore obtenus. Elle doit :
- Compter les séances depuis workout_sessions WHERE user_id = p_user_id
- Compter les prs depuis session_sets WHERE user_id = p_user_id AND is_pr = true (avec gestion d'erreur si colonne manquante via un BEGIN/EXCEPTION)
- Compter le streak actuel depuis habit_logs (consécutif jusqu'à aujourd'hui)
- Vérifier profile_complete (avatar_url IS NOT NULL AND bio IS NOT NULL AND goal IS NOT NULL)
- Compter les amis depuis friendships WHERE user_id = p_user_id
- Pour chaque badge_definition non encore dans user_badges de l'utilisateur : comparer la valeur calculée à condition_value selon condition_type et insérer si méritée

Signature : CREATE OR REPLACE FUNCTION public.check_and_award_badges(p_user_id UUID) RETURNS void LANGUAGE plpgsql SECURITY DEFINER AS $$ ... $$;

SECTION 6 — Bucket safeguard (INSERT ON CONFLICT DO NOTHING) :
INSERT INTO storage.buckets (id, name, public) VALUES
  ('avatars', 'avatars', true),
  ('profile-photos', 'profile-photos', true)
ON CONFLICT (id) DO NOTHING;
  </action>
  <verify>
    <automated>ls C:/ziko-platform/supabase/migrations/051_profile_settings_gap.sql && grep -c "ADD COLUMN IF NOT EXISTS bio" C:/ziko-platform/supabase/migrations/051_profile_settings_gap.sql</automated>
  </verify>
  <done>Le fichier 051_profile_settings_gap.sql existe et contient les 6 sections : ADD COLUMN bio/handle/is_public/subscription_tier, CREATE TABLE badge_definitions, seed 11 badges, CREATE TABLE user_badges + RLS, check_and_award_badges function, bucket safeguard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (Checkpoint) : Appliquer migration 051 en production</name>
  <what-built>Fichier 051_profile_settings_gap.sql créé et validé localement.</what-built>
  <how-to-verify>
Exécute depuis le répertoire supabase/ :

```
cd C:/ziko-platform/supabase && supabase db push
```

Vérifie que la migration 051 apparaît dans la liste des migrations appliquées sans erreur.

Si supabase CLI n'est pas disponible, appliquer via le Dashboard Supabase → SQL Editor → coller le contenu de 051_profile_settings_gap.sql et exécuter.

Après application, vérifier :
1. Table user_profiles a bien les colonnes bio, handle, is_public, subscription_tier
2. Table badge_definitions existe avec 11 lignes
3. Table user_badges existe
4. Fonction check_and_award_badges existe
5. Aucune erreur de migration
  </how-to-verify>
  <resume-signal>Type "migration-applied" when done, or describe errors</resume-signal>
</task>

<task type="auto">
  <name>Task 3 : Mettre à jour edit.tsx — sauvegarder bio et handle comme colonnes directes</name>
  <files>apps/mobile/app/(app)/profile/edit.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/edit.tsx (handleSave actuel, lignes 207-240 — lire entièrement avant de modifier)
  </read_first>
  <action>
Dans apps/mobile/app/(app)/profile/edit.tsx, modifier deux éléments :

1. Ajouter le state handle : après la déclaration `const [bio, setBio] = useState('');` (ligne ~98), ajouter :
   `const [handle, setHandle] = useState('');`

2. Dans le useEffect de pré-remplissage (lignes ~106-114), ajouter après setBio :
   `setHandle((profile as any).handle ?? '');`

3. Remplacer handleSave (lignes ~207-240) pour supprimer le fetch settings préalable et upsert directement bio/handle comme colonnes :
   - Supprimer le bloc `const { data: existing } = await supabase.from('user_profiles').select('settings')...`
   - Supprimer `const existingSettings = ...`
   - Remplacer l'upsert par : `supabase.from('user_profiles').upsert({ id: userId, name: name.trim(), goal: goal || null, bio: bio.trim(), handle: handle.trim() || null })`
   - Le reste de handleSave (invalidateQueries, router.back()) reste inchangé

4. Ajouter un champ TextInput "Pseudo" dans le formulaire, entre Bio et Objectif :
   - Même style card row que Bio
   - Label "PSEUDO" (uppercase muted)
   - TextInput value={handle} onChangeText={setHandle} placeholder="@tonpseudo" autoCapitalize="none"

Aucune autre modification. Ne pas toucher uploadAvatar, handleAvatarPress, STHeader, ou les styles.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "bio: bio.trim()" apps/mobile/app/\(app\)/profile/edit.tsx</automated>
  </verify>
  <done>edit.tsx upsert user_profiles avec { id, name, goal, bio, handle } sans fetch settings préalable. TypeScript 0 erreurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase prod | DDL exécuté avec privilèges superuser via supabase CLI |
| Mobile → user_profiles | Upsert bio/handle par utilisateur authentifié |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-12-01 | Tampering | handle UNIQUE constraint | mitigate | UNIQUE constraint DB-side empêche deux users avec même handle |
| T-35-12-02 | Tampering | subscription_tier CHECK constraint | mitigate | CHECK constraint DB rejette toute valeur hors ('free','premium','coach') |
| T-35-12-03 | Tampering | user_badges RLS | mitigate | RLS policy auth.uid() = user_id sur user_badges — seul l'owner peut lire/écrire ses badges |
| T-35-12-04 | Elevation | check_and_award_badges SECURITY DEFINER | mitigate | Fonction SECURITY DEFINER avec p_user_id param — ne peut agir que sur l'user passé en arg |
| T-35-12-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan |
</threat_model>

<verification>
```bash
# 1. Migration créée
ls C:/ziko-platform/supabase/migrations/051_profile_settings_gap.sql

# 2. Colonnes présentes dans la migration
grep -c "ADD COLUMN IF NOT EXISTS" C:/ziko-platform/supabase/migrations/051_profile_settings_gap.sql

# 3. Seed 11 badges
grep -c "first_session\|ten_sessions\|fifty_sessions\|hundred_sessions\|streak_7\|streak_30\|streak_100\|first_pr\|ten_prs\|profile_complete\|first_friend" C:/ziko-platform/supabase/migrations/051_profile_settings_gap.sql

# 4. edit.tsx sauvegarde bio colonne directe
grep -c "bio: bio.trim()" C:/ziko-platform/apps/mobile/app/\(app\)/profile/edit.tsx

# 5. TypeScript propre
cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -v "^#" | grep -c "error TS" || echo "0"
```
</verification>

<success_criteria>
- [ ] 051_profile_settings_gap.sql existe avec 6 sections (colonnes + badge_definitions + seed + user_badges + RPC + buckets)
- [ ] Migration appliquée en production sans erreur (checkpoint validé)
- [ ] edit.tsx upsert `{ id, name, goal, bio, handle }` sans fetch settings préalable
- [ ] Champ handle présent dans le formulaire edit.tsx
- [ ] TypeScript 0 erreurs
</success_criteria>

<output>
Créer `.planning/phases/35-profile-settings-redesign/35-12-SUMMARY.md` quand terminé.
</output>
