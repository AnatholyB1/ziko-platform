---
phase: 32-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/design-system.ts
  - packages/ui/src/components/FormRing.tsx
  - packages/ui/src/components/AISuggestion.tsx
  - packages/ui/src/components/SubTabs.tsx
  - packages/ui/src/components/PluginHeader.tsx
  - packages/ui/src/components/WeekStrip.tsx
  - packages/ui/src/index.ts
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
  - DS-05
  - DS-06

must_haves:
  truths:
    - "packages/ui/src/design-system.ts exports shadow.card, shadow.float, and the full colors object"
    - "FormRing accepts score + parts props and renders a 4-segment SVG ring with a center number"
    - "AISuggestion renders with sparkles icon, left tint strip, body text, and optional action button"
    - "SubTabs renders tabs with an orange underline active indicator and fires onChange with the tab string"
    - "PluginHeader renders a back-chevron button (calls onBack), title, and optional right slot"
    - "WeekStrip renders 7 day cells for the current week; today is highlighted with an orange pill"
    - "All 6 new components are exported from packages/ui/src/index.ts"
    - "npm run type-check passes with zero errors after all files are written"
  artifacts:
    - path: "packages/ui/src/design-system.ts"
      provides: "shadow and colors token constants"
      exports: ["shadow", "colors"]
    - path: "packages/ui/src/components/FormRing.tsx"
      provides: "4-segment SVG wellness ring"
      exports: ["FormRing"]
    - path: "packages/ui/src/components/AISuggestion.tsx"
      provides: "inline AI tip card"
      exports: ["AISuggestion"]
    - path: "packages/ui/src/components/SubTabs.tsx"
      provides: "segmented tab bar with orange underline"
      exports: ["SubTabs"]
    - path: "packages/ui/src/components/PluginHeader.tsx"
      provides: "back-chevron plugin header"
      exports: ["PluginHeader"]
    - path: "packages/ui/src/components/WeekStrip.tsx"
      provides: "7-day date strip with dot indicators"
      exports: ["WeekStrip"]
  key_links:
    - from: "packages/ui/src/index.ts"
      to: "packages/ui/src/components/FormRing.tsx"
      via: "named export"
      pattern: "export.*FormRing.*from"
    - from: "packages/ui/src/components/WeekStrip.tsx"
      to: "date-fns"
      via: "format, startOfDay, addDays, differenceInCalendarDays, getDay"
      pattern: "from 'date-fns'"
---

<objective>
Create the static design token file and 5 new shared UI primitive components in packages/ui/. These components are the visual building blocks that every phase-33-through-41 plan will consume.

Purpose: Establish the shared component foundation so all subsequent phases can import FormRing, AISuggestion, SubTabs, PluginHeader, and WeekStrip from @ziko/ui without duplicating logic per screen.
Output: 6 new files + updated index.ts (7 file changes total). TypeScript compile is clean after this plan completes.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/PROJECT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/ROADMAP-v1.7.md
@C:/ziko-platform/.planning/phases/32-design-system-foundation/32-RESEARCH.md
@C:/ziko-platform/.planning/phases/32-design-system-foundation/32-PATTERNS.md

<interfaces>
<!-- Existing exports in packages/ui/src/index.ts (current 15-line file): -->
export { Button, Card, Badge, Tag, Input, ScreenHeader, StatCard, ProgressBar, Divider, Skeleton,
         spacing, radius, typography } from './components';

<!-- Existing tokens in packages/ui/src/components.tsx lines 17–42: -->
export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32, xxl: 48 } as const;
export const radius  = { sm: 8, md: 12, lg: 16, xl: 24, full: 9999 } as const;
export const typography = {
  h1: { fontSize: 28, fontWeight: '700' as const },
  h2: { fontSize: 22, fontWeight: '700' as const },
  h3: { fontSize: 18, fontWeight: '600' as const },
  body: { fontSize: 15, fontWeight: '400' as const },
  bodySmall: { fontSize: 13, fontWeight: '400' as const },
  caption: { fontSize: 11, fontWeight: '400' as const },
  button: { fontSize: 15, fontWeight: '600' as const },
} as const;

<!-- ThemePalette fields used by components (from @ziko/plugin-sdk): -->
theme.primary    // '#FF5C1A'
theme.surface    // '#FFFFFF'
theme.border     // '#E2E0DA'
theme.text       // '#1C1A17'
theme.muted      // '#6B6963'
theme.cardDark   // '#1C1A17'
theme.background // '#F7F6F3'

<!-- date-fns functions used in WeekStrip (already in package.json): -->
import { format, startOfDay, differenceInCalendarDays, addDays, getDay } from 'date-fns';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create design-system.ts token file + new component files scaffold</name>
  <files>
    packages/ui/src/design-system.ts
    packages/ui/src/components/FormRing.tsx
    packages/ui/src/components/AISuggestion.tsx
    packages/ui/src/components/SubTabs.tsx
    packages/ui/src/components/PluginHeader.tsx
    packages/ui/src/components/WeekStrip.tsx
  </files>
  <read_first>
    - packages/ui/src/components.tsx — read lines 17–63 for spacing/radius/typography/resolveCardStyle patterns; these define the token values and shadow style that design-system.ts consolidates
    - packages/plugin-sdk/src/theme.ts — read the DEFAULT_THEME and ThemePalette interface for the canonical color values (bg, surface, border, primary, text, muted, cardDark, etc.)
    - apps/mobile/app/(app)/index.tsx — read lines 44–77 for the exact FormRing SVG formula; read lines 317–373 for the exact WeekStrip day-cell logic; copy these formulas verbatim (do not rewrite)
    - apps/mobile/src/components/BugReportModal.tsx — read lines 1–14 for the import pattern used across all components in this project (inline styles, useThemeStore, showAlert)
    - packages/ui/src/index.ts — read full file (15 lines) to understand the current export shape before extending it
  </read_first>
  <action>
Create `packages/ui/src/design-system.ts` with the following exact content. This file consolidates shadow and color tokens. It must NOT duplicate spacing/radius/typography — those already exist in components.tsx and are re-exported below.

design-system.ts contents:
- Export `shadow` object with two keys:
  - `card`: { shadowColor: '#1C1A17', shadowOffset: { width: 0, height: 4 }, shadowOpacity: 0.08, shadowRadius: 12, elevation: 3 }
  - `float`: { shadowColor: '#000000', shadowOffset: { width: 0, height: 4 }, shadowOpacity: 0.25, shadowRadius: 8, elevation: 8 }
- Export `colors` object (static aliases — do NOT use useThemeStore here, this is a static constants file):
  { bg: '#F7F6F3', surface: '#FFFFFF', border: '#E2E0DA', primary: '#FF5C1A', text: '#1C1A17', muted: '#6B6963', cardDark: '#1C1A17', cardDarkText: '#FFFAF6', success: '#2E9E5B', info: '#2E7BF6', violet: '#7B5BD0', warn: '#E8A33A', danger: '#EF4444' }
- Re-export spacing, radius, typography from '../components' so callers only need one import: `export { spacing, radius, typography } from '../components';`

---

Create `packages/ui/src/components/FormRing.tsx`. This is an extraction of the existing inline FormRing from apps/mobile/app/(app)/index.tsx lines 44–77. Copy the SVG formula exactly — do not rewrite the offset accumulation.

Imports:
```
import React from 'react';
import { View, Text } from 'react-native';
import Svg, { Circle } from 'react-native-svg';
```

Props interface:
```
interface FormRingProps {
  score: number;
  parts: { value: number; max: number; color: string }[];
  size?: number;
}
```

Core render: STROKE=11, r=(size-STROKE)/2, C=2*Math.PI*r, let offset=0. Track circle: stroke "rgba(28,26,23,0.06)". Per segment: portion = (Math.min(p.value/p.max, 1) / parts.length) * 0.92, len = C*portion, dashOffset = -(offset*C) - (C*0.25), then offset += portion + 0.02. Center: absolute-positioned Text with fontSize=28, fontWeight='800', color='#1C1A17'. Default export as named export: `export function FormRing(...)`.

---

Create `packages/ui/src/components/AISuggestion.tsx`.

Imports: React, View, Text, TouchableOpacity from react-native; Ionicons from @expo/vector-icons; useThemeStore from @ziko/plugin-sdk.

Props:
```
interface AISuggestionProps {
  text: string;
  actionLabel?: string;
  onAction?: () => void;
  tintColor?: string;
}
```

Render: outer View with theme.surface background, borderRadius=16, borderLeftWidth=3, borderLeftColor=(tintColor ?? theme.primary), padding=14, shadow card style (shadowColor=theme.cardDark, shadowOffset={width:0,height:4}, shadowOpacity=0.08, shadowRadius=12, elevation=3). Row with Ionicons name="sparkles" size=16 color=tint, then Text flex=1. If actionLabel+onAction, render TouchableOpacity below row with fontWeight='700' text in tint color.

---

Create `packages/ui/src/components/SubTabs.tsx`.

Imports: React, View, Text, TouchableOpacity from react-native; useThemeStore from @ziko/plugin-sdk.

Props:
```
interface SubTabsProps {
  tabs: string[];
  active: string;
  onChange: (tab: string) => void;
}
```

Render: outer View flexDirection='row', borderBottomWidth=1, borderBottomColor=theme.border. Each tab is a TouchableOpacity with paddingVertical=12, paddingHorizontal=16, alignItems='center', position='relative'. Tab label: fontSize=13, fontWeight active='700' inactive='500', color active=theme.text inactive=theme.muted. Active indicator: position='absolute', bottom=0, height=2, width='100%', backgroundColor=theme.primary, borderRadius=1.

---

Create `packages/ui/src/components/PluginHeader.tsx`.

Imports: React, View, Text, TouchableOpacity from react-native; Ionicons from @expo/vector-icons; useThemeStore from @ziko/plugin-sdk.

Props:
```
interface PluginHeaderProps {
  title: string;
  onBack: () => void;
  right?: React.ReactNode;
}
```

Render: outer View flexDirection='row', alignItems='center', gap=10, padding=14, paddingTop=10. Back button: TouchableOpacity width=34, height=34, borderRadius=11, backgroundColor=theme.text+'10', alignItems='center', justifyContent='center'; Ionicons name="chevron-back" size=16 color=theme.text. Title: Text fontSize=20, fontWeight='800', color=theme.text, letterSpacing=-0.4, flex=1. Right slot: {right && <View>{right}</View>}.

---

Create `packages/ui/src/components/WeekStrip.tsx`.

Imports: React, View, Text, TouchableOpacity from react-native; { format, startOfDay, differenceInCalendarDays, addDays, getDay } from 'date-fns'; useThemeStore from @ziko/plugin-sdk.

Props:
```
interface WeekStripProps {
  selectedDate: Date;
  onSelect: (date: Date) => void;
  dotDates?: Set<string>;
}
```

Day computation (copy from index.tsx lines 324–332 exactly):
```
const today = startOfDay(new Date());
const jsToday = getDay(today);
const mondayOffset = jsToday === 0 ? -6 : 1 - jsToday;
const monday = addDays(today, mondayOffset);
const DAY_LABELS = ['L', 'M', 'M', 'J', 'V', 'S', 'D'];
const days = Array.from({ length: 7 }, (_, i) => {
  const date = addDays(monday, i);
  const key = format(date, 'yyyy-MM-dd');
  const isToday = differenceInCalendarDays(date, today) === 0;
  const hasDot = dotDates?.has(key) ?? false;
  return { label: DAY_LABELS[i], num: format(date, 'd'), date, isToday, hasDot };
});
```

Render: outer View flexDirection='row', paddingHorizontal=8. Each day cell: TouchableOpacity flex=1, alignItems='center', paddingVertical=8, borderRadius=10, backgroundColor=isToday?theme.primary:'transparent'. Day label text: fontSize=9, fontWeight='700', color=isToday?'#fff':theme.muted. Date number: fontSize=13, fontWeight='800', color=isToday?'#fff':theme.text, marginTop=2. Dot: if hasDot, View width=4, height=4, borderRadius=2, backgroundColor=isToday?'#fff':theme.primary, marginTop=3.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - packages/ui/src/design-system.ts exists and exports shadow (card + float) and colors objects
    - packages/ui/src/components/FormRing.tsx, AISuggestion.tsx, SubTabs.tsx, PluginHeader.tsx, WeekStrip.tsx all exist
    - Each file exports its named component
    - No TypeScript errors in packages/ui/
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Export all new components from packages/ui/src/index.ts</name>
  <files>
    packages/ui/src/index.ts
  </files>
  <read_first>
    - packages/ui/src/index.ts — read full file; the current 15-line export must be preserved and 6 new lines added
    - packages/ui/src/design-system.ts — confirm it exists and exports shadow, colors, spacing, radius, typography before referencing in index.ts
  </read_first>
  <action>
Read the current packages/ui/src/index.ts (15 lines). Append the following named exports after the existing export block. Do not remove or modify the existing exports.

Add these lines:
```
export { FormRing } from './components/FormRing';
export { AISuggestion } from './components/AISuggestion';
export { SubTabs } from './components/SubTabs';
export { PluginHeader } from './components/PluginHeader';
export { WeekStrip } from './components/WeekStrip';
export * from './design-system';
```

The final file must still contain all 13 original exports (Button, Card, Badge, Tag, Input, ScreenHeader, StatCard, ProgressBar, Divider, Skeleton, spacing, radius, typography) plus the 6 new lines above.

Note: The `export * from './design-system'` re-exports shadow, colors, AND spacing/radius/typography from design-system.ts (which itself re-exports them from ./components). TypeScript will flag a duplicate export if spacing/radius/typography are exported twice. If that conflict occurs, change the design-system.ts re-export to only export shadow and colors, and keep spacing/radius/typography in the existing './components' export line.
  </action>
  <verify>
    <automated>cd /c/ziko-platform && npm run type-check 2>&1 | tail -10</automated>
  </verify>
  <done>
    - packages/ui/src/index.ts contains export lines for FormRing, AISuggestion, SubTabs, PluginHeader, WeekStrip, and design-system tokens
    - npm run type-check exits 0 (no duplicate export errors)
    - All 13 original exports are still present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packages/ui → apps/mobile | One-way dependency only; UI pkg must not import from apps/mobile/src/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | packages/ui imports | mitigate | Enforce package boundary: packages/ui/ may only import from react, react-native, @ziko/plugin-sdk, moti, react-native-svg, @expo/vector-icons, expo-linear-gradient, react-native-safe-area-context, zustand, date-fns |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — all libraries pre-installed and verified in RESEARCH.md |
</threat_model>

<verification>
After both tasks complete:
1. All 6 new files exist under packages/ui/src/components/ and packages/ui/src/design-system.ts
2. packages/ui/src/index.ts exports all new components
3. npm run type-check passes with zero errors
4. Grep: `grep -c "export.*FormRing" packages/ui/src/index.ts` returns ≥ 1
5. Grep: `grep -c "shadow" packages/ui/src/design-system.ts` returns ≥ 1
6. No file in packages/ui/src/ contains `className=` or `StyleSheet.create`
7. No file in packages/ui/src/ imports from `apps/mobile/src/`
</verification>

<success_criteria>
- packages/ui/src/design-system.ts exports shadow.card, shadow.float, and the full colors object with 13 named color values
- FormRing renders a 4-segment SVG ring using strokeDasharray; center score is a Text overlay
- AISuggestion has borderLeftWidth=3 tint strip + sparkles Ionicon + optional action button
- SubTabs shows orange 2px underline on active tab; inactive tabs use theme.muted color
- PluginHeader has 34×34 borderRadius=11 back button with chevron-back icon
- WeekStrip builds a Monday-anchored 7-day grid; today cell has orange pill background
- npm run type-check passes
</success_criteria>

<output>
Create `.planning/phases/32-design-system-foundation/32-01-SUMMARY.md` when done.
</output>
