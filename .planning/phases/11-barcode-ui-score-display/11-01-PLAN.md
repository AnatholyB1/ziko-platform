---
phase: 11-barcode-ui-score-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/nutrition/src/components/ScoreBadge.tsx
  - plugins/nutrition/src/store.ts
  - packages/plugin-sdk/src/i18n.ts
  - apps/mobile/app.json
autonomous: true
requirements: [SCAN-02, SCORE-01, SCORE-02, SCORE-03]

must_haves:
  truths:
    - "ScoreBadge renders colored pill for grades a through e and a-plus"
    - "ScoreBadge renders nothing for null, undefined, not-applicable, or unknown grades"
    - "NutritionEntry type includes optional food_product_id, nutriscore_grade, ecoscore_grade fields"
    - "All 12 new i18n keys exist in both FR and EN locales"
    - "Android CAMERA permission declared in app.json"
  artifacts:
    - path: "plugins/nutrition/src/components/ScoreBadge.tsx"
      provides: "Shared score badge component"
      exports: ["default"]
    - path: "plugins/nutrition/src/store.ts"
      provides: "Extended NutritionEntry type with grade fields"
      contains: "nutriscore_grade"
    - path: "packages/plugin-sdk/src/i18n.ts"
      provides: "i18n keys for barcode tab, scanner, product card, dashboard widget"
      contains: "nutrition.barcode"
    - path: "apps/mobile/app.json"
      provides: "Android CAMERA permission"
      contains: "android.permission.CAMERA"
  key_links:
    - from: "plugins/nutrition/src/components/ScoreBadge.tsx"
      to: "GRADE_COLORS map"
      via: "grade-to-color lookup"
      pattern: "'a-plus'.*#1A7F37"
    - from: "plugins/nutrition/src/store.ts"
      to: "NutritionEntry interface"
      via: "optional grade fields"
      pattern: "nutriscore_grade\\?.*string.*null"
---

<objective>
Create the shared ScoreBadge component, extend the NutritionEntry store type with grade fields, add all Phase 11 i18n keys, and declare Android CAMERA permission.

Purpose: These are the foundation artifacts that Plans 02 and 03 both depend on. ScoreBadge is shared between LogMealScreen and NutritionDashboard. The store type must include grade fields before either screen can read them. i18n keys are needed by both screens. Android CAMERA permission is needed before the barcode scanner can work on Android devices.

Output: ScoreBadge.tsx component, extended store.ts type, 12 new i18n keys (FR+EN), app.json with CAMERA permission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-barcode-ui-score-display/11-CONTEXT.md
@.planning/phases/11-barcode-ui-score-display/11-RESEARCH.md
@.planning/phases/11-barcode-ui-score-display/11-UI-SPEC.md

<interfaces>
<!-- FoodProduct interface from offApi.ts — ScoreBadge grade values come from this -->
From plugins/nutrition/src/utils/offApi.ts:
```typescript
export interface FoodProduct {
  id: string;
  barcode: string;
  name: string;
  brand: string | null;
  energy_kcal: number;
  proteins_g: number;
  carbs_g: number;
  fat_g: number;
  nutriscore_grade: string | null;
  ecoscore_grade: string | null;
  image_url: string | null;
  serving_size_g: number;
  created_at: string;
}
```

From plugins/nutrition/src/store.ts (current — to be extended):
```typescript
interface NutritionEntry {
  id: string;
  date: string;
  meal_type: 'breakfast' | 'lunch' | 'dinner' | 'snack';
  food_name: string;
  calories: number;
  protein_g: number;
  carbs_g: number;
  fat_g: number;
  serving_g?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScoreBadge component + extend NutritionEntry type</name>
  <files>plugins/nutrition/src/components/ScoreBadge.tsx, plugins/nutrition/src/store.ts</files>
  <read_first>
    - plugins/nutrition/src/store.ts (current NutritionEntry interface — lines 3-13)
    - plugins/nutrition/src/utils/offApi.ts (FoodProduct interface — grade field types)
    - .planning/phases/11-barcode-ui-score-display/11-UI-SPEC.md (ScoreBadge size specs, color table)
    - .planning/phases/11-barcode-ui-score-display/11-CONTEXT.md (D-14 through D-16)
  </read_first>
  <action>
**1. Create `plugins/nutrition/src/components/ScoreBadge.tsx`:**

Import `View`, `Text` from `react-native`. This is a stateless component — no hooks, no theme dependency (badge colors are semantic, not theme-derived).

Define these constants at module level:

```typescript
const GRADE_COLORS: Record<string, string> = {
  'a': '#1A7F37', 'a-plus': '#1A7F37',
  'b': '#78B346', 'c': '#F5A623',
  'd': '#E3692B', 'e': '#CC1F24',
};

const GRADE_LABELS: Record<string, string> = {
  'a-plus': 'A+', 'a': 'A', 'b': 'B', 'c': 'C', 'd': 'D', 'e': 'E',
};

const PREFIX: Record<'nutriscore' | 'ecoscore', string> = {
  nutriscore: 'NS', ecoscore: 'ES',
};

const SIZE_STYLES = {
  sm: { height: 20, paddingHorizontal: 4, fontSize: 12 },
  md: { height: 24, paddingHorizontal: 8, fontSize: 12 },
  lg: { height: 28, paddingHorizontal: 8, fontSize: 12 },
};
```

Props interface: `{ grade: string | null; type: 'nutriscore' | 'ecoscore'; size: 'sm' | 'md' | 'lg' }`

Component logic:
- If `!grade` return `null`
- `const normalized = grade.toLowerCase()`
- `const color = GRADE_COLORS[normalized]`
- If `!color` return `null` (handles 'not-applicable', unknown values)
- Get label from `GRADE_LABELS[normalized]` (fallback: uppercase of grade)
- Get size styles from `SIZE_STYLES[size]`
- Render a `View` pill: `{ height, paddingHorizontal, backgroundColor: color, borderRadius: height / 2, alignItems: 'center', justifyContent: 'center', flexDirection: 'row' }`
- Inside: `Text` with `{ color: '#fff', fontSize, fontWeight: '700' }` displaying `{PREFIX[type]} {label}`

Export as `export default function ScoreBadge(...)`.

**2. Extend `NutritionEntry` in `plugins/nutrition/src/store.ts`:**

Add three optional fields to the `NutritionEntry` interface after `serving_g`:
```typescript
  food_product_id?: string | null;
  nutriscore_grade?: string | null;
  ecoscore_grade?: string | null;
```

No other changes to store.ts. The Zustand store actions and state shape remain the same — these fields are read-only display data passed through from Supabase `select('*')`.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - plugins/nutrition/src/components/ScoreBadge.tsx exists and contains `export default function ScoreBadge`
    - ScoreBadge.tsx contains `GRADE_COLORS` with keys 'a', 'a-plus', 'b', 'c', 'd', 'e'
    - ScoreBadge.tsx contains `'a-plus': '#1A7F37'`
    - ScoreBadge.tsx contains `'a-plus': 'A+'` in GRADE_LABELS
    - ScoreBadge.tsx contains `if (!grade) return null` or equivalent null guard
    - ScoreBadge.tsx contains `if (!color) return null` for unknown grades
    - ScoreBadge.tsx contains SIZE_STYLES with sm (height: 20), md (height: 24), lg (height: 28)
    - ScoreBadge.tsx does NOT contain `StyleSheet.create`
    - store.ts NutritionEntry interface contains `food_product_id?: string | null`
    - store.ts NutritionEntry interface contains `nutriscore_grade?: string | null`
    - store.ts NutritionEntry interface contains `ecoscore_grade?: string | null`
    - `cd apps/mobile && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>ScoreBadge renders colored pill for valid grades (a, a-plus, b, c, d, e), renders nothing for null/unknown/not-applicable. NutritionEntry type includes three new optional fields. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n keys + Android CAMERA permission</name>
  <files>packages/plugin-sdk/src/i18n.ts, apps/mobile/app.json</files>
  <read_first>
    - packages/plugin-sdk/src/i18n.ts (existing nutrition.* keys — find the section to extend)
    - apps/mobile/app.json (android.permissions array — currently missing CAMERA)
    - .planning/phases/11-barcode-ui-score-display/11-UI-SPEC.md (Copywriting Contract table — exact key names and values)
  </read_first>
  <action>
**1. Add i18n keys to `packages/plugin-sdk/src/i18n.ts`:**

In the `fr` dictionary, add these keys in the `nutrition.*` section (after existing nutrition keys):

```
'nutrition.barcode': 'Code-barres',
'nutrition.barcodeAlign': 'Alignez le code-barres pour scanner',
'nutrition.barcodeNotFound': 'Produit introuvable',
'nutrition.barcodeNotFoundHint': 'Code scanné : {barcode}',
'nutrition.logThisMeal': 'Enregistrer ce repas',
'nutrition.enterManually': 'Saisir manuellement',
'nutrition.scanAgain': 'Scanner à nouveau',
'nutrition.cameraPermDenied': 'Accès à la caméra refusé. Activez-le dans Réglages > Ziko.',
'nutrition.avgNutriscore': 'Nutri-Score moyen',
'nutrition.avgNutriscoreCount': 'sur {count} repas scannés',
'nutrition.per100g': 'Pour 100g',
'nutrition.servingSize': 'Taille de la portion',
'nutrition.grams': 'g',
```

In the `en` dictionary, add the same keys with English values:

```
'nutrition.barcode': 'Barcode',
'nutrition.barcodeAlign': 'Align barcode to scan',
'nutrition.barcodeNotFound': 'Product not found',
'nutrition.barcodeNotFoundHint': 'Scanned code: {barcode}',
'nutrition.logThisMeal': 'Log this meal',
'nutrition.enterManually': 'Enter manually',
'nutrition.scanAgain': 'Scan again',
'nutrition.cameraPermDenied': 'Camera access denied. Enable it in Settings > Ziko.',
'nutrition.avgNutriscore': 'Average Nutri-Score',
'nutrition.avgNutriscoreCount': 'from {count} scanned meals',
'nutrition.per100g': 'Per 100g',
'nutrition.servingSize': 'Serving size',
'nutrition.grams': 'g',
```

**2. Add Android CAMERA permission to `apps/mobile/app.json`:**

In the `expo.android.permissions` array, add `"android.permission.CAMERA"` as the first entry (before the health permissions). This is required for expo-camera CameraView to request camera access on Android production builds.

The existing array starts with health.READ_STEPS permissions. Insert `"android.permission.CAMERA"` before those entries.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - i18n.ts FR section contains `'nutrition.barcode': 'Code-barres'`
    - i18n.ts FR section contains `'nutrition.barcodeAlign': 'Alignez le code-barres pour scanner'`
    - i18n.ts FR section contains `'nutrition.barcodeNotFound': 'Produit introuvable'`
    - i18n.ts FR section contains `'nutrition.avgNutriscore': 'Nutri-Score moyen'`
    - i18n.ts FR section contains `'nutrition.avgNutriscoreCount': 'sur {count} repas scannés'`
    - i18n.ts FR section contains `'nutrition.per100g': 'Pour 100g'`
    - i18n.ts EN section contains `'nutrition.barcode': 'Barcode'`
    - i18n.ts EN section contains `'nutrition.barcodeNotFound': 'Product not found'`
    - i18n.ts EN section contains `'nutrition.avgNutriscore': 'Average Nutri-Score'`
    - i18n.ts EN section contains all 13 nutrition.* keys in both FR and EN
    - app.json contains `"android.permission.CAMERA"` in the android.permissions array
    - `cd apps/mobile && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All 13 i18n keys present in both FR and EN locales. Android CAMERA permission declared. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit` passes with zero errors
- ScoreBadge.tsx exports a default function component
- store.ts NutritionEntry has 3 new optional fields
- i18n.ts has 13 new nutrition.* keys in both locales
- app.json has android.permission.CAMERA
</verification>

<success_criteria>
- ScoreBadge component renders grade pills with correct colors per D-14, returns null for invalid grades
- NutritionEntry type extended with food_product_id, nutriscore_grade, ecoscore_grade
- All i18n keys from UI-SPEC Copywriting Contract present in both FR and EN
- Android CAMERA permission declared
- Full type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-barcode-ui-score-display/11-01-SUMMARY.md`
</output>
