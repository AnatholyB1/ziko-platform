---
phase: 23-web-turborepo-onboarding-auth-bootstrap
plan: 07
type: execute
wave: 6
depends_on: [23-03, 23-04, 23-05]
files_modified:
  - apps/web/vercel.json
  - backend/api/vercel.json
  - apps/web/src/app/api/_debug/limits/route.ts
  - backend/api/src/routes/_debug.ts
  - .github/workflows/ci.yml
  - .github/workflows/publish-coach-sdk.yml
autonomous: true
requirements: [ARCH-02, ARCH-08]
requirements_addressed: [ARCH-02, ARCH-08]
tags: [phase-23, wave-6, vercel-topology, pro-tier-probe, ci-guardrails, gha-release, deletable-phase-24]
must_haves:
  truths:
    - "apps/web/vercel.json declares framework nextjs, ignoreCommand, turbo build command"
    - "backend/api/vercel.json gains ignoreCommand (existing crons + rewrites preserved)"
    - "/api/_debug/limits on web is gated by DEBUG_LIMITS=on, sleeps 30s, declares maxDuration=60"
    - "Equivalent _debug probe on backend/api"
    - "Both _debug files carry 'PHASE 23 PRO PROBE — DELETE IN PHASE 24' marker"
    - ".github/workflows/ci.yml runs type-check + lint + test + no-SERVICE_ROLE grep + bundle-hygiene + zod-drift jobs"
    - ".github/workflows/publish-coach-sdk.yml ships gated on vars.PUBLISH_COACH_SDK == 'true' (insurance)"
  artifacts:
    - path: "apps/web/vercel.json"
      provides: "Web project Vercel config — framework, buildCommand, ignoreCommand"
      contains: "ignoreCommand"
    - path: "backend/api/vercel.json"
      provides: "Backend project Vercel config — preserves crons + rewrites, adds ignoreCommand"
      contains: "ignoreCommand"
    - path: "apps/web/src/app/api/_debug/limits/route.ts"
      provides: "Pro-tier evidence probe — 30s sleep, maxDuration=60, DEBUG_LIMITS gate"
      contains: "export const maxDuration = 60"
    - path: "backend/api/src/routes/_debug.ts"
      provides: "Backend Pro-tier evidence probe"
      contains: "DEBUG_LIMITS"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline: type-check/lint/test + ARCH-02 grep + bundle hygiene + zod drift"
      contains: "Verify no SERVICE_ROLE under coach/"
    - path: ".github/workflows/publish-coach-sdk.yml"
      provides: "Conditional GHA release workflow for @ziko/coach-sdk (D-04 insurance)"
      contains: "PUBLISH_COACH_SDK"
  key_links:
    - from: ".github/workflows/ci.yml job no-service-role-in-coach"
      to: "backend/api/src/coach/ (does not exist yet)"
      via: "grep -r 'SERVICE_ROLE' guarded by `[ -d backend/api/src/coach ]` directory existence check"
      pattern: "grep -r 'SERVICE_ROLE'"
    - from: ".github/workflows/ci.yml job bundle-hygiene"
      to: "apps/web/.next/analyze/stats.json"
      via: "ANALYZE=true turbo run build --filter=web then regex grep"
      pattern: "react-native"
    - from: "apps/web/vercel.json"
      to: "turbo run build --filter=web"
      via: "buildCommand reaches repo root and filters web"
      pattern: "turbo run build --filter=web"
---

<objective>
Wire the platform-tier infrastructure: Vercel topology (one project per app, both with `ignoreCommand`), Pro-tier evidence probes (`/api/_debug/limits` on both projects), and the CI workflow that enforces ARCH-02 + bundle hygiene + zod drift detection. Also ship the conditional GHA release workflow for `@ziko/coach-sdk` (D-04 insurance — activates only if `vars.PUBLISH_COACH_SDK=true`).

Purpose: ARCH-02 (CI grep for SERVICE_ROLE under coach/), ARCH-08 (Pro tier evidence via `/api/_debug/limits`), D-14 (two-project Vercel topology), D-15 (Pro-tier probe), D-04 (GHA release insurance). All "_debug" files tagged for deletion in Phase 24.

Output: 2 vercel.json files, 2 _debug route files (both tagged DELETE IN PHASE 24), 2 GitHub Actions workflows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-web-turborepo-onboarding-auth-bootstrap/23-CONTEXT.md
@.planning/phases/23-web-turborepo-onboarding-auth-bootstrap/23-RESEARCH.md

<interfaces>
- `backend/api/vercel.json` ALREADY EXISTS on disk with `buildCommand`, `rewrites`, `crons` (verified in RESEARCH §6.2 lines 1342–1355). MUST preserve all existing keys; only ADD `ignoreCommand`.
- `apps/web/` came from c:/ziko-web; it may or may not have a `vercel.json`. If present, REPLACE with the locked content; if absent, create fresh.
- The CI workflow file `.github/workflows/ci.yml` may NOT exist on `ziko-platform` (Phase 22 used local Vitest only); this plan creates it fresh.
- The publish-coach-sdk.yml workflow is gated on a GitHub Actions repository variable `PUBLISH_COACH_SDK` — when unset, the job is a no-op. The variable is set ONLY on the dual-repo fallback path (D-04 activation).
- The `_debug` route file path on web is `apps/web/src/app/api/_debug/limits/route.ts` (Next.js App Router Route Handler).
- The backend `_debug` route lives at `backend/api/src/routes/_debug.ts` and is registered into the Hono app. Backend mounting convention: read an existing route registration in `backend/api/src/index.ts` or `backend/api/src/app.ts` and follow the pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vercel configs (apps/web new + backend/api ignoreCommand addition) and both _debug probes</name>
  <files>
    apps/web/vercel.json
    backend/api/vercel.json
    apps/web/src/app/api/_debug/limits/route.ts
    backend/api/src/routes/_debug.ts
  </files>
  <read_first>
    - backend/api/vercel.json (current content — preserve crons + rewrites + buildCommand)
    - apps/web/vercel.json if exists (else create fresh)
    - backend/api/src/index.ts or src/app.ts (find where routes mount — pattern to follow when registering _debug)
    - 23-RESEARCH.md §6.1 lines 1323–1331 (apps/web/vercel.json verbatim)
    - 23-RESEARCH.md §6.2 lines 1344–1371 (backend/api/vercel.json before/after)
    - 23-RESEARCH.md §"Code Examples" item 7 lines 559–575 (apps/web _debug/limits route.ts verbatim)
    - 23-CONTEXT.md D-15 (probe gated by DEBUG_LIMITS=on, returns 404 otherwise; 30s sleep; maxDuration=60)
  </read_first>
  <action>
**A) Web Vercel config** — Write `apps/web/vercel.json` (verbatim, RESEARCH §6.1):

```json
{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && turbo run build --filter=web",
  "installCommand": "cd ../.. && npm install",
  "outputDirectory": ".next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ."
}
```

**B) Backend Vercel config** — Read current `backend/api/vercel.json`, ADD only the `ignoreCommand` field, preserve everything else. Target shape (from RESEARCH §6.2 lines 1360–1370):

```json
{
  "buildCommand": "",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/app" }
  ],
  "crons": [
    { "path": "/supplements/cron/scrape", "schedule": "0 3 * * 1" },
    { "path": "/storage/cron/cleanup", "schedule": "0 4 * * *" }
  ],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ."
}
```

If the on-disk `backend/api/vercel.json` differs from the "before" snapshot in RESEARCH §6.2, preserve whatever keys exist and merely ADD `"ignoreCommand": "git diff --quiet HEAD^ HEAD -- ."` to the top-level object.

**C) Web Pro-tier probe** — Create directory and write the route handler.

```bash
mkdir -p apps/web/src/app/api/_debug/limits
```

`apps/web/src/app/api/_debug/limits/route.ts` (based on RESEARCH §"Code Examples" item 7, deletion marker added):

```typescript
// PHASE 23 PRO PROBE — DELETE IN PHASE 24 (ARCH-08 evidence captured in 23-VERIFICATION.md; route has no production purpose).
// D-15: 30s sleep + maxDuration=60 — if response is 504 at 10s, project is on Hobby; 200 after 30s confirms Pro.
// Gated by process.env.DEBUG_LIMITS === 'on' — set on preview deploys ONLY, never production.
export const dynamic = 'force-dynamic';
export const maxDuration = 60;

export async function GET() {
  if (process.env.DEBUG_LIMITS !== 'on') {
    return new Response('Not Found', { status: 404 });
  }
  await new Promise((r) => setTimeout(r, 30_000));
  return Response.json({ ok: true, tier: 'pro-confirmed', durationSec: 30 });
}
```

**D) Backend Pro-tier probe** — Inspect `backend/api/src/index.ts` (or `src/app.ts`) to find the route-mounting pattern. Then write `backend/api/src/routes/_debug.ts`:

```typescript
// PHASE 23 PRO PROBE — DELETE IN PHASE 24
// D-15: Hono equivalent of apps/web/src/app/api/_debug/limits/route.ts
// Vercel function uses `export const config = { maxDuration: 60 }` in Hono via @hono/vercel.
import { Hono } from 'hono';

const debugRoute = new Hono();

debugRoute.get('/limits', async (c) => {
  if (process.env.DEBUG_LIMITS !== 'on') {
    return c.notFound();
  }
  await new Promise((r) => setTimeout(r, 30_000));
  return c.json({ ok: true, tier: 'pro-confirmed', durationSec: 30 });
});

export default debugRoute;

// Vercel function config for @hono/vercel (Pro tier required for maxDuration > 10s)
export const config = {
  maxDuration: 60,
};
```

Then MOUNT the route in the backend Hono app entry. Read `backend/api/src/index.ts` (or `src/app.ts`) and follow its existing route-mount pattern. Typically:

```typescript
// In backend/api/src/index.ts or src/app.ts, near other route registrations:
import debugRoute from './routes/_debug';
app.route('/_debug', debugRoute);
```

If the backend has a single-file `app.ts` exporting the Hono instance, add the import + mount call. Do NOT modify existing route mounts.
  </action>
  <verify>
    <automated>test -f apps/web/vercel.json &amp;&amp; grep -q '"framework": "nextjs"' apps/web/vercel.json &amp;&amp; grep -q "ignoreCommand" apps/web/vercel.json &amp;&amp; grep -q "ignoreCommand" backend/api/vercel.json &amp;&amp; grep -q "/supplements/cron/scrape" backend/api/vercel.json &amp;&amp; test -f apps/web/src/app/api/_debug/limits/route.ts &amp;&amp; grep -q "maxDuration = 60" apps/web/src/app/api/_debug/limits/route.ts &amp;&amp; grep -q "DEBUG_LIMITS" apps/web/src/app/api/_debug/limits/route.ts &amp;&amp; grep -q "PHASE 23 PRO PROBE" apps/web/src/app/api/_debug/limits/route.ts &amp;&amp; test -f backend/api/src/routes/_debug.ts &amp;&amp; grep -q "DEBUG_LIMITS" backend/api/src/routes/_debug.ts &amp;&amp; grep -q "PHASE 23 PRO PROBE" backend/api/src/routes/_debug.ts &amp;&amp; turbo run type-check --filter=web --filter=api</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vercel.json` contains: `"framework": "nextjs"`, `"buildCommand": "cd ../.. && turbo run build --filter=web"`, `"installCommand": "cd ../.. && npm install"`, `"outputDirectory": ".next"`, `"ignoreCommand": "git diff --quiet HEAD^ HEAD -- ."`
    - `backend/api/vercel.json` contains BOTH the existing keys (`buildCommand`, `rewrites`, `crons` with both `/supplements/cron/scrape` and `/storage/cron/cleanup`) AND the new `"ignoreCommand": "git diff --quiet HEAD^ HEAD -- ."` field
    - `apps/web/src/app/api/_debug/limits/route.ts` contains: `PHASE 23 PRO PROBE — DELETE IN PHASE 24`, `export const dynamic = 'force-dynamic'`, `export const maxDuration = 60`, `DEBUG_LIMITS !== 'on'`, `setTimeout(r, 30_000)`, `tier: 'pro-confirmed'`
    - `backend/api/src/routes/_debug.ts` contains: `PHASE 23 PRO PROBE — DELETE IN PHASE 24`, `DEBUG_LIMITS`, `setTimeout(r, 30_000)`, `tier: 'pro-confirmed'`, `maxDuration: 60`
    - Backend route is mounted in `backend/api/src/index.ts` or `src/app.ts` (grep finds an import for `./routes/_debug` or `_debug`)
    - `turbo run type-check --filter=web --filter=api` exits 0
  </acceptance_criteria>
  <done>
    Vercel topology configured; both _debug probes exist and tagged DELETE; type-check green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CI workflow (.github/workflows/ci.yml) with 4 jobs</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    - .github/workflows/ci.yml — CURRENT content (existing `quality`, `deploy-backend`, `migrate-supabase` jobs MUST be preserved per WARNING 9 revision)
    - 23-RESEARCH.md §9 lines 1560–1658 (CI workflow verbatim, 4 NEW jobs — append to existing file)
    - 23-PATTERNS.md — ci.yml is classified as MODIFY, not CREATE
    - Note: RESEARCH job 2 uses an `if` guarding the grep with `[ -d backend/api/src/coach ]` — this is correct because Phase 23 ships BEFORE Phase 24 creates the `coach/` folder.
    - planning_context: explicit instruction to NOT use deprecated `actions/cache@v3` — verify research uses v4
  </read_first>
  <action>
Read existing `.github/workflows/ci.yml` first — it contains 3 existing jobs that MUST be preserved:
- `quality` (Type-check & Lint with `npm run type-check` + `npm run lint`)
- `deploy-backend` (Deploy API to Vercel, `needs: quality`)
- `migrate-supabase` (Apply Supabase migrations on migration-touching commits, `needs: quality`)

The 4 new jobs from RESEARCH §9 must be APPENDED to the existing file under `jobs:`. Do NOT overwrite the file from scratch.

The resulting `.github/workflows/ci.yml` structure:

```yaml
name: CI

on:
  push:
    branches:
      - main
  pull_request:                    # NEW — RESEARCH §9 (current ci.yml only triggers on push)
    branches: [main]

jobs:
  # ─── Existing job (preserved verbatim) ─────────────────────────────
  quality:
    name: Type-check & Lint
    # ... existing steps unchanged ...

  # ─── Existing job (preserved verbatim) ─────────────────────────────
  deploy-backend:
    # ... existing steps unchanged ...

  # ─── Existing job (preserved verbatim) ─────────────────────────────
  migrate-supabase:
    # ... existing steps unchanged ...

  # ─── NEW: Job 1 — turbo type-check / lint / test (broader than existing quality) ──

**The 4 NEW jobs to append** (DO NOT replace the existing `quality`, `deploy-backend`, `migrate-supabase` jobs). Update the `on:` trigger to add `pull_request` (current file only triggers on push). Then append jobs 1–4 under the existing `jobs:` map:

```yaml
# UPDATE the `on:` block to include pull_request (current ci.yml only has push: branches: [main]):
on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]

# APPEND these 4 jobs UNDER the existing `jobs:` map (after quality / deploy-backend / migrate-supabase).
# The YAML stanza below is the LITERAL text to APPEND — do NOT overwrite the file.
# (Header `name: CI` and existing jobs stay verbatim from the current file.)

jobs:
  # ─── Job 1: type-check + lint + unit tests ───────────────────────
  verify:
    name: type-check / lint / test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2   # for git diff on ignoreBuildStep equivalent
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install
        run: npm install
      - name: Type-check
        run: npx turbo run type-check
      - name: Lint
        run: npx turbo run lint
      - name: Test
        run: npx turbo run test

  # ─── Job 2: no SERVICE_ROLE under coach/ (ARCH-02, D-12 second layer) ─
  no-service-role-in-coach:
    name: Verify no SERVICE_ROLE under coach/
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Grep guard
        run: |
          # Phase 23 ships the rule; Phase 24 creates backend/api/src/coach/
          if [ -d backend/api/src/coach ]; then
            if grep -r 'SERVICE_ROLE' backend/api/src/coach/; then
              echo "FAIL: SERVICE_ROLE reference found under backend/api/src/coach/"
              exit 1
            fi
          fi
          echo "PASS: no SERVICE_ROLE under coach/ (or directory does not exist yet)"

  # ─── Job 3: bundle hygiene (D-02 step 3) ──────────────────────────
  bundle-hygiene:
    name: Verify no react-native in web bundle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install
        run: npm install
      - name: Build with analyzer
        run: ANALYZE=true npx turbo run build --filter=web
      - name: Assert RN-clean bundle
        run: |
          if grep -E '"name":\s*"[^"]*react-native(?!-web)' apps/web/.next/analyze/stats.json; then
            echo "FAIL: react-native leaked into web bundle"
            exit 1
          fi
          echo "PASS: web bundle is RN-clean"

  # ─── Job 4: Zod version drift check (D-08 safety) ─────────────────
  zod-drift:
    name: coach-sdk zod resolves to root zod
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm install
      - name: Verify zod is hoisted, not duplicated
        run: |
          ROOT_ZOD=$(node -e "console.log(require.resolve('zod'))")
          SDK_ZOD=$(node -e "console.log(require.resolve('zod', { paths: ['packages/coach-sdk'] }))")
          if [ "$ROOT_ZOD" != "$SDK_ZOD" ]; then
            echo "FAIL: coach-sdk resolves a different zod than root"
            echo "  ROOT: $ROOT_ZOD"
            echo "  SDK:  $SDK_ZOD"
            exit 1
          fi
          echo "PASS: zod resolves to single instance ($ROOT_ZOD)"
```

Verify YAML syntax:

```bash
# If yamllint is available:
yamllint .github/workflows/ci.yml || true
# At minimum, ensure no obvious tabs/indent errors:
cat .github/workflows/ci.yml | head -20
```
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; grep -q "deploy-backend" .github/workflows/ci.yml &amp;&amp; grep -q "migrate-supabase" .github/workflows/ci.yml &amp;&amp; grep -q "quality" .github/workflows/ci.yml &amp;&amp; grep -q "type-check / lint / test" .github/workflows/ci.yml &amp;&amp; grep -q "Verify no SERVICE_ROLE under coach/" .github/workflows/ci.yml &amp;&amp; grep -q "Verify no react-native in web bundle" .github/workflows/ci.yml &amp;&amp; grep -q "coach-sdk zod resolves to root zod" .github/workflows/ci.yml &amp;&amp; grep -q "actions/checkout@v4" .github/workflows/ci.yml &amp;&amp; grep -q "actions/setup-node@v4" .github/workflows/ci.yml &amp;&amp; ! grep -q "actions/cache@v3" .github/workflows/ci.yml &amp;&amp; grep -q 'ANALYZE=true npx turbo run build --filter=web' .github/workflows/ci.yml &amp;&amp; grep -q "\[ -d backend/api/src/coach \]" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - **Existing jobs preserved** (per WARNING 9 revision):
      - `grep -q "deploy-backend" .github/workflows/ci.yml` exits 0 (existing job preserved)
      - `grep -q "migrate-supabase" .github/workflows/ci.yml` exits 0 (existing job preserved)
      - `grep -q "quality" .github/workflows/ci.yml` exits 0 (existing job preserved)
    - 4 NEW job names appended: `type-check / lint / test`, `Verify no SERVICE_ROLE under coach/`, `Verify no react-native in web bundle`, `coach-sdk zod resolves to root zod`
    - All `uses:` actions are `@v4` (no `@v3` deprecation)
    - Job 2 guards grep with `[ -d backend/api/src/coach ]` directory check (Phase 23 passes before dir exists)
    - Job 3 invokes `ANALYZE=true npx turbo run build --filter=web` and greps `apps/web/.next/analyze/stats.json` for `react-native(?!-web)` regex
    - Job 4 compares `require.resolve('zod')` between root and `packages/coach-sdk`
    - YAML is valid (no tab/indent errors — `cat` produces readable output)
  </acceptance_criteria>
  <done>
    CI workflow live with 4 jobs covering type-check/lint/test, ARCH-02 grep, bundle hygiene, zod drift.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create conditional GHA release workflow for @ziko/coach-sdk (D-04 insurance)</name>
  <files>
    .github/workflows/publish-coach-sdk.yml
  </files>
  <read_first>
    - 23-RESEARCH.md §12.2 lines 1884–1921 (publish-coach-sdk.yml verbatim)
    - 23-CONTEXT.md D-04 (publish workflow ships even on monorepo path; gated by vars.PUBLISH_COACH_SDK)
    - 23-RESEARCH.md §12.4 lines 1962–1963 (why ship D-04 even when D-02 passes: ~30 lines insurance)
  </read_first>
  <action>
Write `.github/workflows/publish-coach-sdk.yml` (verbatim, RESEARCH §12.2):

```yaml
name: Publish @ziko/coach-sdk

on:
  push:
    branches: [main]
    paths:
      - 'packages/coach-sdk/**'
  workflow_dispatch:

jobs:
  publish:
    if: ${{ vars.PUBLISH_COACH_SDK == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@ziko'
      - name: Install
        run: npm install
      - name: Build coach-sdk
        run: npx turbo run build --filter=@ziko/coach-sdk
      - name: Type-check + test
        run: |
          npx turbo run type-check --filter=@ziko/coach-sdk
          npx turbo run test --filter=@ziko/coach-sdk
      - name: Publish
        working-directory: packages/coach-sdk
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

The workflow is a no-op until `vars.PUBLISH_COACH_SDK=true` is set in repository variables. Per RESEARCH §12.1, the consumer-side `.npmrc` is NOT shipped in Phase 23 (deferred per CONTEXT.md "Deferred Ideas"); only activated if dual-repo fallback engages.
  </action>
  <verify>
    <automated>test -f .github/workflows/publish-coach-sdk.yml &amp;&amp; grep -q "Publish @ziko/coach-sdk" .github/workflows/publish-coach-sdk.yml &amp;&amp; grep -q "vars.PUBLISH_COACH_SDK == 'true'" .github/workflows/publish-coach-sdk.yml &amp;&amp; grep -q "packages/coach-sdk" .github/workflows/publish-coach-sdk.yml &amp;&amp; grep -q "registry-url: 'https://npm.pkg.github.com'" .github/workflows/publish-coach-sdk.yml &amp;&amp; grep -q "permissions:" .github/workflows/publish-coach-sdk.yml &amp;&amp; grep -q "packages: write" .github/workflows/publish-coach-sdk.yml &amp;&amp; ! grep -q "actions/cache@v3" .github/workflows/publish-coach-sdk.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/publish-coach-sdk.yml` exists
    - Name: `Publish @ziko/coach-sdk`
    - Triggers: `on.push.branches: [main]` AND `paths: 'packages/coach-sdk/**'` AND `workflow_dispatch`
    - `if: ${{ vars.PUBLISH_COACH_SDK == 'true' }}` gate on the `publish` job
    - `permissions: { contents: read, packages: write }` for GH Packages
    - `registry-url: 'https://npm.pkg.github.com'`, `scope: '@ziko'`
    - `working-directory: packages/coach-sdk` for the publish step
    - `NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` env on publish
    - All actions are `@v4` (no deprecation)
  </acceptance_criteria>
  <done>
    Release workflow shipped as D-04 insurance; no-op on monorepo path; activates only if PUBLISH_COACH_SDK variable set true.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/_debug/limits | Untrusted GET; gated by DEBUG_LIMITS env, returns 404 unless `=on` |
| GitHub Actions runner → npm.pkg.github.com | Auth via GITHUB_TOKEN; package write permission required |
| Vercel build → repo root | `buildCommand` reaches up to root via `cd ../..` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07-01 | I (Info Disclosure) | _debug/limits exposed on production | mitigate | `DEBUG_LIMITS !== 'on'` → 404. Env var set on preview ONLY (per D-15 manual checklist). Phase 24 deletes the route. |
| T-23-07-02 | D (Denial of Service) | Probe sleeps 30s and can be hammered | accept | Gated by env var; only set on preview deploys; no production exposure. Vercel preview rate limits apply. |
| T-23-07-03 | T (Tampering) | Forged push triggers publish workflow | mitigate | `vars.PUBLISH_COACH_SDK == 'true'` gate; variable unset on monorepo path. Repo settings restrict variable writes to admin. |
| T-23-07-04 | E (Elevation) | GITHUB_TOKEN over-permissioned | mitigate | `permissions: { contents: read, packages: write }` — minimum scope for publish. |
| T-23-07-05 | T | Vercel `ignoreCommand` misconfigured triggers cross-project rebuilds | mitigate | `ignoreCommand: "git diff --quiet HEAD^ HEAD -- ."` runs in Project Root Directory; Vercel project must have Root Directory set per dashboard (Wave 7 manual checklist). |

</threat_model>

<verification>
```bash
cd C:/ziko-platform
test -f apps/web/vercel.json && cat apps/web/vercel.json
test -f backend/api/vercel.json && cat backend/api/vercel.json
test -f apps/web/src/app/api/_debug/limits/route.ts
test -f backend/api/src/routes/_debug.ts
test -f .github/workflows/ci.yml
test -f .github/workflows/publish-coach-sdk.yml
turbo run type-check --filter=web --filter=api
```
</verification>

<success_criteria>
- apps/web/vercel.json declares framework, build/install commands, ignoreCommand
- backend/api/vercel.json gains ignoreCommand without losing crons/rewrites
- Both _debug probes exist, gated by DEBUG_LIMITS, marked DELETE IN PHASE 24
- .github/workflows/ci.yml runs 4 jobs (verify, no-SERVICE_ROLE, bundle-hygiene, zod-drift)
- .github/workflows/publish-coach-sdk.yml ships, conditional on PUBLISH_COACH_SDK variable
- type-check green
</success_criteria>

<output>
Create `.planning/phases/23-web-turborepo-onboarding-auth-bootstrap/23-07-SUMMARY.md` listing: 6 files modified, CI job names, _debug deletion markers, GHA workflow activation conditions.
</output>
