---
phase: 28-ai-file-imports
plan: 07
type: execute
wave: 4
depends_on:
  - 28-04
files_modified:
  - plugins/ai-programs/src/screens/ImportFileScreen.tsx
  - plugins/ai-programs/src/manifest.ts
  - apps/mobile/app/(app)/(plugins)/ai-programs/import.tsx
autonomous: true
requirements:
  - IMPORT-01
  - IMPORT-04
  - IMPORT-05
  - IMPORT-06

must_haves:
  truths:
    - "ImportFileScreen.tsx implements all 5 states: idle/uploading/parsing/result/failed"
    - "expo-document-picker used for file selection (D-05)"
    - "Upload flow: pick → signed URL → PUT blob → PUT status='uploaded' → POST parse"
    - "Polling every 2s during 'parsing' state; interval cleared on unmount"
    - "ai-programs manifest has import route with path '/(plugins)/ai-programs/import'"
    - "Thin route wrapper apps/mobile/app/(app)/(plugins)/ai-programs/import.tsx exists"
    - "No Alert.alert — showAlert from @ziko/plugin-sdk used for all alerts"
    - "paddingBottom: 100 on all ScrollViews"
  artifacts:
    - path: "plugins/ai-programs/src/screens/ImportFileScreen.tsx"
      provides: "Full-featured mobile import screen with all states"
      exports: ["ImportFileScreen"]
    - path: "plugins/ai-programs/src/manifest.ts"
      provides: "Import route added to manifest"
      contains: "/(plugins)/ai-programs/import"
    - path: "apps/mobile/app/(app)/(plugins)/ai-programs/import.tsx"
      provides: "Thin Expo Router route wrapper"
      contains: "ImportFileScreen"
  key_links:
    - from: "plugins/ai-programs/src/screens/ImportFileScreen.tsx"
      to: "backend /coach/imports"
      via: "fetch POST + PUT + GET polling with Bearer token"
      pattern: "/coach/imports"
    - from: "apps/mobile/app/(app)/(plugins)/ai-programs/import.tsx"
      to: "plugins/ai-programs/src/screens/ImportFileScreen.tsx"
      via: "import + render"
      pattern: "ImportFileScreen"
---

<objective>
Build the mobile athlete import screen inside the ai-programs plugin: ImportFileScreen.tsx with all 5 UI states, update the manifest with the import route, and create the thin Expo Router wrapper.

Purpose: Athletes can import workout files from their phone using the ai-programs plugin's new "Import from file" flow.
Output: ImportFileScreen.tsx, manifest.ts updated, import.tsx thin wrapper.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ai-file-imports/28-CONTEXT.md
@.planning/phases/28-ai-file-imports/28-UI-SPEC.md
@plugins/ai-programs/src/manifest.ts
@CLAUDE.md
</context>

<interfaces>
<!-- expo-document-picker API: -->
```typescript
import * as DocumentPicker from 'expo-document-picker';
const result = await DocumentPicker.getDocumentAsync({
  type: ['application/pdf', 'image/png', 'image/jpeg',
         'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
         'application/vnd.ms-excel',
         'application/vnd.openxmlformats-officedocument.wordprocessingml.document'],
  copyToCacheDirectory: true,
});
// result.canceled: boolean
// result.assets[0]: { uri, mimeType, size, name }
```

<!-- Upload flow (Phase 14 D-20 pattern): -->
```typescript
// Step 1: POST /coach/imports → { import_id, signed_upload_url }
// Step 2: fetch(asset.uri).then(r => r.blob()) → PUT to signed_upload_url with blob
// Step 3: PUT /coach/imports/:id/status { status: 'uploaded' }
// Step 4: POST /coach/imports/:id/parse → 202
// Step 5: poll GET /coach/imports/:id every 2s
```

<!-- Reanimated v3 imports needed: -->
```typescript
import Animated, { FadeInUp, withTiming, withRepeat, withSequence,
  useSharedValue, useAnimatedStyle } from 'react-native-reanimated';
```

<!-- Current manifest routes array: -->
```typescript
routes: [
  { path: '/(plugins)/ai-programs/dashboard', title: 'Programmes IA', icon: 'sparkles-outline', showInTabBar: true },
  { path: '/(plugins)/ai-programs/generate', title: 'Générer', icon: 'add-circle-outline', showInTabBar: false },
  // ADD: { path: '/(plugins)/ai-programs/import', title: 'Importer un fichier', icon: 'cloud-upload-outline', showInTabBar: false }
]
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update ai-programs manifest with import route</name>
  <files>plugins/ai-programs/src/manifest.ts</files>
  <read_first>
    - plugins/ai-programs/src/manifest.ts — full file (in context); current routes array at lines 83–96
    - CLAUDE.md §Plugin System Conventions — manifest route pattern (path, title, icon as Ionicons string, showInTabBar)
    - .planning/phases/28-ai-file-imports/28-RESEARCH.md §Integration Points #5
  </read_first>
  <action>
    In `plugins/ai-programs/src/manifest.ts`, add a new entry to the `routes` array:
    ```
    {
      path: '/(plugins)/ai-programs/import',
      title: 'Importer un fichier',
      icon: 'cloud-upload-outline',
      showInTabBar: false,
    }
    ```
    Place it after the 'generate' route. No other changes to the manifest.

    `cloud-upload-outline` is a valid Ionicons name (CLAUDE.md: manifest icon must be an Ionicons string name).
  </action>
  <verify>
    <automated>grep -c "cloud-upload-outline" C:/ziko-platform/plugins/ai-programs/src/manifest.ts</automated>
  </verify>
  <done>
    manifest.ts routes array contains the import route with path '/(plugins)/ai-programs/import'.
    icon is 'cloud-upload-outline' (Ionicons name, not emoji).
    showInTabBar is false.
    TypeScript compiles without errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build ImportFileScreen.tsx with all 5 states</name>
  <files>plugins/ai-programs/src/screens/ImportFileScreen.tsx</files>
  <read_first>
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 8 (idle/upload state — full card spec)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 9 (uploading state — progress bar)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 10 (parsing/polling state — rotating icon, slow fill bar)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 11 (result preview — stats row, program name input, action buttons)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 12 (failed state — error card, retry/change buttons)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Motion Design §Mobile Reanimated v3 contracts
    - .planning/phases/28-ai-file-imports/28-RESEARCH.md §Pattern 7 (polling loop), §Pitfall 7 (fetch(uri).blob() pattern for upload)
    - CLAUDE.md §Custom Alert System — use showAlert from @ziko/plugin-sdk, never Alert.alert
    - CLAUDE.md §Design System — paddingBottom: 100 on all ScrollViews
  </read_first>
  <action>
    Create `plugins/ai-programs/src/screens/ImportFileScreen.tsx`.

    Props: `{ supabase: SupabaseClient }` (consistent with other ai-programs screens that receive supabase as prop).

    State:
    - `screenState: 'idle' | 'uploading' | 'parsing' | 'result' | 'failed'`
    - `uploadProgress: number` (0–100 — tracked via XMLHttpRequest for true progress; fetch does not support upload progress)
    - `importId: string | null`
    - `parsedImport: ImportRow | null` (stored when polling returns status='ready')
    - `errorMessage: string | null`
    - `elapsedSeconds: number`

    Get API_URL from `process.env.EXPO_PUBLIC_API_URL` and auth token from `supabase.auth.getSession()`.

    File picker (idle state → uploading):
    - Call `DocumentPicker.getDocumentAsync({ type: [...6 MIME types], copyToCacheDirectory: true })`
    - If result.canceled: return
    - If asset.size > 25 * 1024 * 1024: `showAlert('Fichier trop grand', 'La taille maximale est 25 Mo.')` and return
    - setScreenState('uploading')
    - Step 1: POST `/coach/imports` with `{ filename: asset.name, mime_type: asset.mimeType, size_bytes: asset.size, mode: 'athlete' }` + Authorization header
    - Step 2: Use `XMLHttpRequest` (not fetch) to track upload progress: `xhr.upload.onprogress = (e) => setUploadProgress(Math.round(e.loaded/e.total*100))`. PUT blob to signed_upload_url (fetch(asset.uri) → .blob() first, then XHR.send(blob))
    - Step 3: PUT `/coach/imports/:id/status` with `{ status: 'uploaded' }`
    - Step 4: POST `/coach/imports/:id/parse` → setScreenState('parsing')
    - On any error: setScreenState('failed'), setErrorMessage(err.message)

    Polling (parsing state):
    - `useEffect` pattern from RESEARCH.md Pattern 7: setInterval every 2s, clear on unmount
    - On 'ready': setParsedImport(data.import), setScreenState('result')
    - On 'failed': setErrorMessage(data.import.error_message), setScreenState('failed')

    Elapsed timer (parsing state): `setInterval` increments `elapsedSeconds` every second; clear on state change.

    **Idle/upload state UI (Screen 8):**
    - ScrollView with `contentContainerStyle={{ paddingBottom: 100 }}`
    - Screen wrapped in `<Animated.View entering={FadeInUp.duration(300).springify()}>` (Screen entrance)
    - Upload card: background #FFFFFF, border 1px #E2E0DA, radius 12, padding 32 24, shadow 0 4 8 rgba(28,26,23,0.08)
    - Icon: `<Ionicons name="cloud-upload-outline" size={48} color="#FF5C1A" />`
    - Title: "Importer un programme" 18px weight 600 #1C1A17
    - Subtitle: "PDF, Excel, Word ou image" 14px #6B6963
    - CTA button: full width, 44px height, bg #FF5C1A, radius 12, "Choisir un fichier" 14px 600 white
    - CTA press animation: `withTiming(0.96, { duration: 100 })` on scale + `withTiming(1, { duration: 100 })`
    - Credit cost card (Screen 8 spec): bg #FFFFFF, border #E2E0DA, radius 12, padding 16; rows: PDF 1 crédit/page, Image/Excel/Word 1 crédit, Échec 0 crédit
    - Supported formats chips row: [PDF] [Excel .xlsx] [Word .docx] [PNG/JPEG] with bg #F0EFE9

    **Uploading state UI (Screen 9):**
    - Upload progress card replaces upload card content
    - Filename + file size row
    - Progress bar: `useSharedValue(0)` → animated width via `withTiming(uploadProgress/100, { duration: 200 })`
    - "Upload… {uploadProgress}%" caption
    - Cancel button (text, 14px #6B6963, centered) — calls xhr.abort()

    **Parsing state UI (Screen 10):**
    - Parsing animation card: bg white, radius 12, padding 32 20
    - Animated icon: circle bg #FFF7F4, radius 9999, sparkles-outline 32px #FF5C1A rotating via `withRepeat(withTiming(360, { duration: 3000 }), -1)` on rotation shared value
    - Title: "Analyse en cours…" 18px 600 center
    - Subtitle: "L'IA extrait votre programme" 14px #6B6963 center
    - Progress bar: `withTiming(0.85, { duration: 55000 })` + opacity pulse `withRepeat(withSequence(...), -1)` per UI-SPEC
    - Elapsed timer: "{MM:SS}" formatted
    - Patience note: "Patientez, cela peut prendre jusqu'à 60 secondes." caption center

    **Result state UI (Screen 11):**
    - Parse result card: bg white, border #E2E0DA, radius 12, padding 20, shadow
    - Result card entrance: `<Animated.View entering={FadeInUp.duration(350).springify().damping(16)}>`
    - Header row: filename + format chip
    - Overall confidence banner: left-border colored card (green/amber/red per confidence value)
    - Stats row: Semaines | Séances | Exercices (computed from parsedImport.parsed_data.weeks)
      - Number count-up animation: `useSharedValue(0)` → `withTiming(actualValue, { duration: 800 })`
    - Program name input: 44px height, bg white, border 1px #E2E0DA, radius 8, placeholder "Programme importé"
    - [Valider et utiliser] accent button (44px, bg #FF5C1A, radius 12) — calls commit API then navigates back
    - [Réviser les détails] outlined button — opens a deep-link or pushes a simple web view (out of scope for Phase 28 mobile detail review; this button can navigate to the parse result URL in a WebView or simply be disabled for Phase 28 — see discretion note)

    Note on [Réviser les détails]: Deep structural editing on mobile is Claude's Discretion (see CONTEXT.md). For Phase 28, this button shows a `showAlert('Révision détaillée', 'Utilisez la version web pour réviser les détails du programme.')` and does not navigate further. The quick commit path [Valider et utiliser] is the primary mobile flow.

    **Failed state UI (Screen 12):**
    - Error card: bg #FEE2E2, border 1px #FCA5A5, radius 12, padding 24 20, left border 4px solid #EF4444
    - Icon: alert-circle-outline 32px #EF4444 centered
    - Title: "Analyse échouée" 18px 600 #B91C1C center
    - Error message: errorMessage or fallback generic copy (UI-SPEC Screen 12)
    - Credit note: "Aucun crédit débité." 12px #6B6963 italic center
    - Shake animation on mount: `withSequence(withTiming(-8,{d:50}), withTiming(8,{d:100}), ...)` per UI-SPEC Motion Design
    - [Réessayer l'analyse] accent — re-POSTs parse without new upload; sets screenState back to 'parsing'
    - [Choisir un autre fichier] outlined — resets all state to 'idle'

    Use `showAlert` from `@ziko/plugin-sdk` for all user alerts. Never `Alert.alert`.
    Use `Ionicons` from `@expo/vector-icons`.
    Use NativeWind v4 classes OR inline style objects — no StyleSheet.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit 2>&1 | grep -i "ImportFileScreen" | head -10</automated>
  </verify>
  <done>
    ImportFileScreen.tsx exists in plugins/ai-programs/src/screens/.
    Implements all 5 states as conditional renders.
    Uses expo-document-picker for file selection.
    Uses fetch(asset.uri).blob() pattern for upload (pitfall 7 avoided).
    Polling useEffect clears interval on unmount.
    showAlert used (not Alert.alert).
    paddingBottom: 100 on ScrollView.
    All Reanimated v3 animations per UI-SPEC Motion Design contracts.
    TypeScript compiles without errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create thin Expo Router wrapper import.tsx</name>
  <files>apps/mobile/app/(app)/(plugins)/ai-programs/import.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/(plugins)/ai-programs/ — check existing route wrappers in this directory for the pattern (e.g., dashboard.tsx or generate.tsx)
    - .planning/phases/28-ai-file-imports/28-RESEARCH.md §Integration Points #6 — exact wrapper pattern
    - CLAUDE.md §Plugin System Conventions §Route Files — thin wrapper pattern
  </read_first>
  <action>
    Create `apps/mobile/app/(app)/(plugins)/ai-programs/import.tsx`.

    Pattern (same as all other route wrappers in this directory):
    - Import `ImportFileScreen` from `@ziko/plugin-ai-programs/screens/ImportFileScreen` (or the correct relative/workspace import path matching how other ai-programs screens are imported)
    - Import `supabase` from the mobile lib (check the existing wrapper files for the correct import path — typically `'../../../../src/lib/supabase'` or similar)
    - Default export a function component that renders `<ImportFileScreen supabase={supabase} />`

    Check `apps/mobile/app/(app)/(plugins)/ai-programs/dashboard.tsx` or `generate.tsx` for the exact import path pattern.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit 2>&1 | grep -i "ai-programs/import" | head -5</automated>
  </verify>
  <done>
    apps/mobile/app/(app)/(plugins)/ai-programs/import.tsx exists.
    Imports ImportFileScreen and supabase client.
    Default exports a function component.
    TypeScript compiles without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile → Supabase Storage | Direct PUT upload via signed URL; file content goes directly to Storage |
| mobile → backend /coach/imports | JWT-authenticated requests with file metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07-01 | Denial of Service | Mobile file size check | accept | Client 25MB check is UX only. Server validates in POST /coach/imports. Storage signed URL path is scoped to user |
| T-28-07-02 | Spoofing | expo-document-picker MIME type | accept | MIME type from DocumentPicker is device-reported; DB CHECK constraint provides final validation. Format mismatch causes parse failure (status='failed'), no credit deducted |
| T-28-07-03 | Tampering | showAlert vs Alert.alert | mitigate | CLAUDE.md mandatory: use showAlert from @ziko/plugin-sdk. No native Alert.alert in plugin screens |
</threat_model>

<verification>
- plugins/ai-programs/src/manifest.ts: routes includes '/(plugins)/ai-programs/import' with cloud-upload-outline icon
- plugins/ai-programs/src/screens/ImportFileScreen.tsx: exists, exports default ImportFileScreen
- ImportFileScreen: expo-document-picker used; fetch(uri).blob() upload pattern used
- ImportFileScreen: polling useEffect with 2s interval, cleared on unmount
- ImportFileScreen: showAlert used (not Alert.alert)
- ImportFileScreen: paddingBottom: 100 on ScrollView
- ImportFileScreen: all 5 states rendered conditionally
- apps/mobile/app/(app)/(plugins)/ai-programs/import.tsx: thin wrapper exists and renders ImportFileScreen
- TypeScript compiles all three files without errors
</verification>

<success_criteria>
Athletes can access "Importer un fichier" from the ai-programs plugin, pick a file, upload, and commit a program after parsing.
</success_criteria>

<output>
Create `.planning/phases/28-ai-file-imports/28-07-SUMMARY.md` when done.
</output>
