---
phase: 28-ai-file-imports
plan: 06
type: execute
wave: 4
depends_on:
  - 28-04
files_modified:
  - apps/web/src/app/[locale]/(coach)/coach/imports/[id]/page.tsx
  - apps/web/src/app/[locale]/(coach)/coach/imports/[id]/PreviewClient.tsx
autonomous: true
requirements:
  - IMPORT-03
  - IMPORT-05
  - IMPORT-06
  - IMPORT-09
  - IMPORT-10

must_haves:
  truths:
    - "/coach/imports/[id] loads with force-dynamic + revalidate=0"
    - "Parsing state shows progress bar (0→90% over 55s) and elapsed timer; polls every 2s"
    - "Ready state shows overall confidence banner and weeks accordion editor"
    - "Exercise cells with confidence < 0.70 have yellow background #FEF9C3"
    - "All exercise fields are inline-editable before commit"
    - "Commit button POSTs to /coach/imports/:id/commit and navigates to programs on success"
    - "Failed state shows error card with 'Réessayer l'analyse' and 'Uploader un autre fichier' buttons"
    - "Re-upload diff view shows new/removed/changed rows with correct badge colors and dimmed unchanged rows"
  artifacts:
    - path: "apps/web/src/app/[locale]/(coach)/coach/imports/[id]/page.tsx"
      provides: "Server Component — auth guard + import fetch + PreviewClient render"
      contains: "force-dynamic"
    - path: "apps/web/src/app/[locale]/(coach)/coach/imports/[id]/PreviewClient.tsx"
      provides: "Parse preview editor with all states: parsing/ready/failed/committed + diff view"
      contains: "WeekAccordion"
  key_links:
    - from: "apps/web/src/app/[locale]/(coach)/coach/imports/[id]/PreviewClient.tsx"
      to: "backend /coach/imports/:id"
      via: "polling GET every 2s; PUT /commit on commit action"
      pattern: "/coach/imports/"
---

<objective>
Build the parse preview and structural editor for /coach/imports/[id]. This is the most complex web screen — it covers all 4 states (parsing/ready/failed/committed) and the re-upload diff view.

Purpose: Coaches review AI-extracted program data, edit any field before commit, and commit the program as a template.
Output: page.tsx (SSR) + PreviewClient.tsx with all states, weeks accordion, confidence highlighting, and diff view.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ai-file-imports/28-CONTEXT.md
@.planning/phases/28-ai-file-imports/28-UI-SPEC.md
@apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx
@apps/web/src/app/[locale]/(coach)/coach/programs/ProgramsClient.tsx
</context>

<interfaces>
<!-- Import row shape (plan 03): -->
```typescript
interface ImportRow {
  id: string;
  original_filename: string;
  mime_type: string;
  status: 'pending' | 'uploaded' | 'parsing' | 'ready' | 'failed' | 'committed';
  page_count: number | null;
  mode: 'athlete' | 'coach_template';
  parsed_data: Record<string, unknown> | null;  // ImportedProgramSchema shape
  confidence_scores: Record<string, unknown> | null;
  error_message: string | null;
  credit_transaction_id: string | null;
  committed_program_id: string | null;
  re_upload_source_id: string | null;
  created_at: string;
}
```

<!-- ParsedProgram shape (from ImportedProgramSchema): -->
```typescript
interface ParsedExercise {
  name: string; sets: number; reps: number;
  target_rpe?: number | null; target_rir?: number | null;
  rest_seconds?: number | null; notes?: string | null;
  confidence?: number | null;  // <0.70 → yellow cell
}
interface ParsedSession { name: string; day_of_week?: number | null; exercises: ParsedExercise[]; notes?: string | null; }
interface ParsedWeek { week_number: number; sessions: ParsedSession[]; notes?: string | null; }
interface ParsedProgram { name: string; description?: string | null; goal?: string | null; weeks: ParsedWeek[]; overall_confidence?: number | null; }
```

<!-- Diff status values (client-side diff): -->
```typescript
type DiffStatus = 'new' | 'removed' | 'changed' | 'unchanged';
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create page.tsx Server Component for /coach/imports/[id]</name>
  <files>apps/web/src/app/[locale]/(coach)/coach/imports/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(coach)/coach/programs/page.tsx — Server Component pattern (force-dynamic, auth, session, apiUrl, fetch)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 4 (breadcrumb, H1 filename, subrow with chips)
  </read_first>
  <action>
    Create `apps/web/src/app/[locale]/(coach)/coach/imports/[id]/page.tsx`.

    - Export `dynamic = 'force-dynamic'` and `revalidate = 0`
    - Props: `{ params: { locale: string, id: string } }` — Next.js dynamic route
    - Auth guard: `createServerSupabase()` → redirect if no user
    - Fetch `GET ${apiUrl}/coach/imports/${params.id}` with `cache: 'no-store'` and Bearer JWT
    - On 404/error: redirect to `/${locale}/coach/imports`
    - Render `<PreviewClient import={importRow} locale={locale} accessToken={jwt} apiUrl={apiUrl} />`
    - Import `PreviewClient` from `'./PreviewClient'`
  </action>
  <verify>
    <automated>grep -c "force-dynamic" C:/ziko-platform/apps/web/src/app/[locale]/\(coach\)/coach/imports/\[id\]/page.tsx</automated>
  </verify>
  <done>
    page.tsx exists with force-dynamic and revalidate=0.
    Fetches single import by id.
    Renders PreviewClient.
    TypeScript compiles without errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build PreviewClient.tsx — all states + weeks accordion + diff view</name>
  <files>apps/web/src/app/[locale]/(coach)/coach/imports/[id]/PreviewClient.tsx</files>
  <read_first>
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 4 (ready/editor state — full accordion spec)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 5 (parsing state — progress bar + elapsed timer)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 6 (failed state — error card)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Screen 7 (re-upload diff view — NEW/SUPPRIMÉ/MODIFIÉ badges)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Motion Design §Web GSAP contracts (ParseProgressBar, Accordion expand, Diff rows reveal, Failed shake, CTA press)
    - .planning/phases/28-ai-file-imports/28-UI-SPEC.md §Copywriting Contract — all French strings
    - .planning/phases/28-ai-file-imports/28-RESEARCH.md §Pattern 6 (re-upload diff algorithm — client-side)
    - .planning/phases/28-ai-file-imports/28-CONTEXT.md §D-15 (deep structural editing), §D-16 (confidence highlighting), §D-17 (diff coloring), §D-18 (commit behavior)
  </read_first>
  <action>
    Create `apps/web/src/app/[locale]/(coach)/coach/imports/[id]/PreviewClient.tsx` with `'use client'` at top.

    Props: `{ import: ImportRow, locale: string, accessToken: string, apiUrl: string }`

    State management:
    - `importData: ImportRow` (updated by polling)
    - `editedProgram: ParsedProgram | null` (deep copy of parsed_data on transition to 'ready'; mutations stay in local state until commit)
    - `diffMode: boolean` (true when re-upload file selected and new parsed_data available)
    - `prevParsedData: ParsedProgram | null` (the original parsed_data from re_upload_source_id import)
    - `expandedWeeks: Set<number>` (which week accordion items are open — all closed initially)
    - `commitLoading: boolean`
    - `elapsedSeconds: number` (for parsing state timer, updated by setInterval)

    Rendering logic (switch on importData.status):

    **Parsing state:**
    - Progress bar: use `gsap.to(".parse-bar-fill", { width: "90%", duration: 55, ease: "none" })` in useEffect on mount when status='parsing'
    - After reaching 90%: `gsap.to(".parse-bar-fill", { opacity: 0.7, duration: 0.6, yoyo: true, repeat: -1 })`
    - Elapsed timer: `setInterval` incrementing `elapsedSeconds` every second; format as `MM:SS`
    - Poll `GET /coach/imports/:id` every 2s; on status change to 'ready' or 'failed': clear interval, update importData
    - Text: "Analyse en cours avec l'IA…" (H2) + "Cela peut prendre jusqu'à 60 secondes pour les PDFs multi-pages." + elapsed timer (UI-SPEC Screen 5)
    - Cancel button: stops polling only, does not abort backend

    **Failed state:**
    - Error card: background #FEE2E2, border 1px #FCA5A5, radius 12px, left border 4px solid #EF4444
    - GSAP shake on mount: `gsap.to(".error-card", { x: [-8, 8, -6, 6, -3, 3, 0], duration: 0.4 })`
    - error_message from importData.error_message (or generic fallback if null)
    - Credit note: "Aucun crédit n'a été débité pour cet import." (Caption, italic)
    - Buttons: [Réessayer l'analyse] accent (re-POST to /coach/imports/:id/parse) + [Uploader un autre fichier] outlined (navigate back to /coach/imports)

    **Ready state (editor):**
    - Overall confidence banner: green/amber/red left-border card per UI-SPEC Screen 4 spec
    - Weeks accordion: each WeekSchema item rendered as an accordion item
      - Collapsed by default (`expandedWeeks` starts empty)
      - Chevron rotates on expand: GSAP accordion expand `gsap.from(".accordion-body", { height: 0, opacity: 0, duration: 0.2 })`
      - Week header shows "Semaine N — M séances" + [+ Ajouter une séance] link + trash icon
      - Expanded body: SessionCard components
        - Session header: session name + [+ Ajouter un exercice] + trash icon
        - Exercise table: columns Exercice | Séries | Reps | RPE | Repos | [delete]
          - Row height min 44px (touch target compliance)
          - Each cell is inline-editable: `contentEditable` div or `<input>` with transparent border; on focus: border 2px solid #FF5C1A, radius 4px
          - Cells with exercise.confidence < 0.70 AND user has not edited: background #FEF9C3
          - On cell blur: update editedProgram state + clear yellow highlight for that cell
          - Confidence tooltip on yellow cell hover: "Confiance: X% — vérifiez cette valeur" (UI-SPEC Screen 4 spec)
        - [+ Ajouter un exercice] row at bottom of table: dashed border row
      - [+ Ajouter une semaine] full-width dashed zone below all weeks

    Add/remove operations (all in local editedProgram state only):
    - Delete week: showAlert-equivalent browser confirm (use window.confirm since this is web, not mobile) with message from UI-SPEC copywriting contract
    - Delete session: same
    - Add week: appends WeekSchema with week_number = max + 1, empty sessions
    - Add session: appends SessionSchema with empty exercises
    - Add exercise: appends ExerciseSchema with default values

    **Diff view (when diffMode=true):**
    - Diff banner: shows "+N exercices · -M séances · P modifications" (computed from diff)
    - Diff algorithm (client-side, no library):
      Match weeks by week_number, sessions by name, exercises by name.
      For each exercise: status = 'new' if only in next, 'removed' if only in prev, 'changed' if in both but any field differs, 'unchanged' otherwise.
    - Row rendering per DiffStatus: NEW=green bg + "NEW" badge, REMOVED=red bg + strikethrough, CHANGED=inline old~~→~~new, UNCHANGED=opacity 0.6
    - Unchanged rows collapsed when >3 (show "tout afficher" toggle below)
    - GSAP diff rows reveal: `gsap.from(".diff-row", { x: -8, opacity: 0, duration: 0.25, stagger: 0.03 })`
    - [Confirmer les modifications] accent button: applies diff (sets editedProgram to new parsed data), exits diff mode

    **Committed state:**
    - Show breadcrumb + status chip 'committed' + link to the committed_program_id in /coach/programs

    **Sticky footer CTA (ready state only):**
    - Fixed bottom bar: background #FFFFFF, border-top 1px #E2E0DA, padding 16px 24px
    - [Re-upload nouvelle version] outlined: triggers hidden file input → on select: POST new file → on parse ready: enter diffMode
    - [Créer le template] accent: POSTs to `PUT /coach/imports/:id/commit` with `{ parsed_data: editedProgram, program_name: editedProgram?.name }`; on success: navigate to `/${locale}/coach/programs`

    Import gsap from 'gsap'. All GSAP calls in useEffect. React icon imports from 'react-icons/io5'.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit 2>&1 | grep -i "imports/\[id\]" | head -10</automated>
  </verify>
  <done>
    PreviewClient.tsx exists with 'use client' at top.
    Renders parsing state (progress bar + elapsed timer + polling).
    Renders failed state (error card + credit note + retry/change buttons).
    Renders ready state (confidence banner + weeks accordion + exercise table with inline editing).
    Exercise cells with confidence < 0.70 have yellow background; cleared on user edit.
    Commit button POSTs to PUT /coach/imports/:id/commit.
    Re-upload diff view renders NEW/SUPPRIMÉ/MODIFIÉ badges.
    TypeScript compiles without errors.
    All French copy strings match UI-SPEC copywriting contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PreviewClient → PUT /coach/imports/:id/commit | User-edited parsed_data (client state) sent to backend — backend re-validates via Supabase RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | Tampering | PreviewClient inline editing (editedProgram state) | accept | All user edits are in client-side React state. The commit route inserts into workout_programs via RLS-enforced JWT client — no privilege escalation possible |
| T-28-06-02 | Information Disclosure | page.tsx fetches import by id | mitigate | Fetch uses user Bearer JWT → RLS ai_imports_own enforces owner-only. Redirect on 404 prevents information leakage about non-owned imports |
| T-28-06-03 | Denial of Service | Polling every 2s | accept | 1 req/2s per user per import; stops as soon as status is 'ready' or 'failed'. Backend rate limiting (Phase 12) provides additional protection |
</threat_model>

<verification>
- apps/web/src/app/[locale]/(coach)/coach/imports/[id]/page.tsx: force-dynamic + revalidate=0 present
- PreviewClient.tsx: 'use client' at top
- PreviewClient.tsx: polling stops when status is 'ready' or 'failed'
- PreviewClient.tsx: yellow cell #FEF9C3 for confidence < 0.70
- PreviewClient.tsx: confidence tooltip on hover
- PreviewClient.tsx: all week/session/exercise add/delete operations update editedProgram state
- PreviewClient.tsx: diff view renders NEW (green), SUPPRIMÉ (red), MODIFIÉ (amber) badges
- PreviewClient.tsx: sticky footer CTA [Créer le template]
- TypeScript compiles without errors
</verification>

<success_criteria>
Parse preview page is complete for all 4 states. Deep structural editing works. Confidence highlighting and diff view are implemented per UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/28-ai-file-imports/28-06-SUMMARY.md` when done.
</output>
