---
phase: 28-ai-file-imports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/package.json
  - apps/mobile/package.json
  - supabase/migrations/048_ai_imports_credit_fk.sql
  - backend/api/test/coach/imports.spec.ts
  - backend/api/test/coach/parse/pdf.spec.ts
  - backend/api/test/coach/parse/excel.spec.ts
autonomous: false
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03
  - IMPORT-05
  - IMPORT-06
  - IMPORT-07
  - IMPORT-08
  - IMPORT-09
  - IMPORT-10

must_haves:
  truths:
    - "pdfjs-dist, canvas, xlsx, mammoth are installed in backend/api"
    - "expo-document-picker is installed in apps/mobile"
    - "Migration 048 exists and adds the FK constraint on ai_imports.credit_transaction_id"
    - "Supabase schema push has completed without error"
    - "Test stub files exist for all 10 requirements"
  artifacts:
    - path: "supabase/migrations/048_ai_imports_credit_fk.sql"
      provides: "FK wired: ai_imports.credit_transaction_id → ai_credit_transactions(id)"
      contains: "fk_ai_imports_credit_transaction"
    - path: "backend/api/test/coach/imports.spec.ts"
      provides: "Stubs for IMPORT-01 to IMPORT-06, IMPORT-09, IMPORT-10"
      exports: []
    - path: "backend/api/test/coach/parse/pdf.spec.ts"
      provides: "Stub for IMPORT-07"
      exports: []
    - path: "backend/api/test/coach/parse/excel.spec.ts"
      provides: "Stub for IMPORT-08"
      exports: []
  key_links:
    - from: "supabase/migrations/048_ai_imports_credit_fk.sql"
      to: "public.ai_credit_transactions"
      via: "FOREIGN KEY constraint ON DELETE SET NULL"
      pattern: "fk_ai_imports_credit_transaction"
---

<objective>
Pre-flight for Phase 28: install all new npm dependencies, write migration 048, push schema to Supabase, and create failing test stubs for all 10 requirements.

Purpose: Every downstream plan can rely on packages being present and the DB constraint being live before parse code is written.
Output: Installed packages, migration SQL, schema pushed (BLOCKING gate), three test stub files.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-ai-file-imports/28-CONTEXT.md
@.planning/phases/28-ai-file-imports/28-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install npm dependencies</name>
  <files>backend/api/package.json, apps/mobile/package.json</files>
  <read_first>
    - backend/api/package.json — verify pdfjs-dist, canvas, xlsx, mammoth are NOT already present
    - apps/mobile/package.json — verify expo-document-picker is NOT already present
    - .planning/phases/28-ai-file-imports/28-RESEARCH.md §Standard Stack — exact versions and install commands
  </read_first>
  <action>
    Run the following two commands from the repo root (not in parallel — npm workspaces need separate installs):

    1. Install backend parsing dependencies:
       `cd backend/api && npm install pdfjs-dist@5.7.284 canvas@3.2.3 xlsx@0.18.5 mammoth@1.12.0`

    2. Install mobile file picker:
       `cd apps/mobile && npx expo install expo-document-picker@55.0.13`

    NOTE on canvas@3.2.3: On Windows dev machines, `canvas` may fail with node-gyp errors requiring Visual C++ Build Tools. This is expected — Vercel's Linux runtime uses pre-built binaries. Document in the plan summary. If canvas fails to install locally, add the package to backend/api/package.json manually and verify Vercel deployment separately.

    Do NOT install pdf2pic — it depends on GraphicsMagick binary unavailable on Vercel (pitfall 1 in RESEARCH.md).
  </action>
  <verify>
    <automated>cd C:/ziko-platform/backend/api && node -e "require('xlsx'); console.log('xlsx ok')" && node -e "require('mammoth'); console.log('mammoth ok')"</automated>
  </verify>
  <done>
    backend/api/package.json contains pdfjs-dist, canvas, xlsx, mammoth entries.
    apps/mobile/package.json contains expo-document-picker entry.
    xlsx and mammoth importable from backend/api (canvas may require build tools on Windows — acceptable).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write migration 048 (credit_transaction_id FK)</name>
  <files>supabase/migrations/048_ai_imports_credit_fk.sql</files>
  <read_first>
    - supabase/migrations/036_workout_programs_ai_imports.sql — see line 71: `credit_transaction_id UUID NULL, -- FK wired in Phase 28 to ai_credit_transactions(id)`
    - .planning/phases/28-ai-file-imports/28-RESEARCH.md §Integration Points #1 — exact ALTER TABLE SQL
  </read_first>
  <action>
    Create `supabase/migrations/048_ai_imports_credit_fk.sql` with:

    - `SET LOCAL lock_timeout = '5s';` (same fail-fast guard used in migration 036)
    - `ALTER TABLE public.ai_imports ADD CONSTRAINT fk_ai_imports_credit_transaction FOREIGN KEY (credit_transaction_id) REFERENCES public.ai_credit_transactions(id) ON DELETE SET NULL;`

    The `ai_credit_transactions` table was created in migration 026. The ON DELETE SET NULL is correct — it preserves the import record if a transaction is purged.

    Add a comment at the top: `-- Phase 28: wire credit_transaction_id FK left as comment in migration 036`.
  </action>
  <verify>
    <automated>grep -c "fk_ai_imports_credit_transaction" C:/ziko-platform/supabase/migrations/048_ai_imports_credit_fk.sql</automated>
  </verify>
  <done>
    File supabase/migrations/048_ai_imports_credit_fk.sql exists.
    Contains the constraint name `fk_ai_imports_credit_transaction`.
    References `public.ai_credit_transactions(id)` with `ON DELETE SET NULL`.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Push schema to Supabase [BLOCKING]</name>
  <read_first>
    - supabase/migrations/048_ai_imports_credit_fk.sql — confirm Task 2 is complete before pushing
  </read_first>
  <what-built>
    Migration 048 was created in Task 2. It must be pushed to the live Supabase project before downstream plans can rely on the FK constraint being enforced.
  </what-built>
  <how-to-verify>
    Run from the repo root:
    ```
    supabase db push
    ```
    If the CLI prompts interactively, set SUPABASE_ACCESS_TOKEN env var first:
    ```
    set SUPABASE_ACCESS_TOKEN=your_token && supabase db push
    ```
    Expected output: migration 048 listed as applied without errors.

    Confirm the FK exists:
    ```
    supabase db query "SELECT constraint_name FROM information_schema.table_constraints WHERE table_name = 'ai_imports' AND constraint_type = 'FOREIGN KEY';"
    ```
    Should include `fk_ai_imports_credit_transaction` in results.
  </how-to-verify>
  <resume-signal>Type "pushed" when supabase db push completes successfully, or describe any errors.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create failing test stubs for IMPORT-01 through IMPORT-10</name>
  <files>
    backend/api/test/coach/imports.spec.ts,
    backend/api/test/coach/parse/pdf.spec.ts,
    backend/api/test/coach/parse/excel.spec.ts
  </files>
  <read_first>
    - backend/api/test/coach/programs.spec.ts — existing test file pattern: imports, describe blocks, vi.mock usage
    - backend/api/test/setup.ts — global test setup (if exists)
    - .planning/phases/28-ai-file-imports/28-RESEARCH.md §Validation Architecture — exact test IDs and describe labels
  </read_first>
  <behavior>
    Each stub test must:
    - Use Vitest `describe` / `it` structure matching the RESEARCH.md test map
    - Call `it.todo(...)` or use `expect.fail('not implemented')` so they are in FAILING state
    - Be importable without compile error (TypeScript must pass)

    imports.spec.ts stubs (8 tests):
    - `it('upload-url: POST /coach/imports returns import_id and signed_upload_url', ...)`  — IMPORT-01
    - `it('credit-cost: PDF page_count maps to min(n,10) credits', ...)` — IMPORT-02
    - `it('schema-validation: ImportedProgramSchema passes on valid Claude output', ...)` — IMPORT-03
    - `it('dual-mode: import route accepts athlete JWT and coach JWT', ...)` — IMPORT-04
    - `it('status-machine: pending→uploaded→parsing→ready transitions', ...)` — IMPORT-05
    - `it('202-async: POST /coach/imports/:id/parse returns 202 immediately', ...)` — IMPORT-06
    - `it('diff-algo: new/removed/changed rows identified correctly', ...)` — IMPORT-09
    - `it('commit: PUT /coach/imports/:id/commit creates workout_programs row with correct is_template', ...)` — IMPORT-10

    pdf.spec.ts stubs (1 test):
    - `it('rasterize: rasterizePdf returns array of 30 base64 PNG strings for 30-page PDF', ...)` — IMPORT-07

    excel.spec.ts stubs (1 test):
    - `it('excel-parse: parseExcel returns non-empty CSV string for .xlsx buffer', ...)` — IMPORT-08

    Create the `test/coach/parse/` directory if it does not exist.
  </behavior>
  <action>
    Create three stub files. Each uses `import { describe, it, expect } from 'vitest'`. The imports.spec.ts file should also import `ImportedProgramSchema from '@ziko/coach-sdk'` to verify the schema import path works. Do NOT import the implementation modules yet (they don't exist). Use `it.todo('...')` for all stubs — Vitest marks todo tests as pending, not failing, which is the correct state for pre-implementation stubs.

    For imports.spec.ts, structure as two describe blocks: `describe('imports routes', ...)` and `describe('diff algorithm', ...)`.
    For pdf.spec.ts, use `describe('parse/pdf', ...)`.
    For excel.spec.ts, use `describe('parse/excel', ...)`.
  </action>
  <verify>
    <automated>cd C:/ziko-platform/backend/api && npm test -- --reporter=dot 2>&1 | grep -E "todo|passed|failed" | head -20</automated>
  </verify>
  <done>
    All three test stub files exist and compile.
    `npm test` in backend/api shows the stub `describe` blocks as pending/todo (0 failures from missing implementations).
    TypeScript `import { ImportedProgramSchema } from '@ziko/coach-sdk'` resolves without error.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → Supabase | Migration push requires SUPABASE_ACCESS_TOKEN — human action required |
| Windows dev → npm canvas | canvas native binaries may fail on Windows; Vercel Linux is unaffected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-SC | Tampering | npm installs (pdfjs-dist, canvas, xlsx, mammoth, expo-document-picker) | mitigate | All packages passed slopcheck [OK] in RESEARCH.md — no [ASSUMED]/[SUS] packages |
| T-28-01-01 | Denial of Service | Migration 048 lock acquisition | mitigate | `SET LOCAL lock_timeout = '5s'` fail-fast guard same as migration 036 |
</threat_model>

<verification>
- backend/api/package.json has pdfjs-dist, canvas, xlsx, mammoth
- apps/mobile/package.json has expo-document-picker
- supabase/migrations/048_ai_imports_credit_fk.sql exists with constraint fk_ai_imports_credit_transaction
- supabase db push completed (human confirmed)
- backend/api/test/coach/imports.spec.ts exists with 8 it.todo stubs
- backend/api/test/coach/parse/pdf.spec.ts exists with 1 it.todo stub
- backend/api/test/coach/parse/excel.spec.ts exists with 1 it.todo stub
</verification>

<success_criteria>
npm dependencies installed (backend + mobile), migration 048 written, DB schema pushed, test stubs created and compiling.
</success_criteria>

<output>
Create `.planning/phases/28-ai-file-imports/28-01-SUMMARY.md` when done.
</output>
