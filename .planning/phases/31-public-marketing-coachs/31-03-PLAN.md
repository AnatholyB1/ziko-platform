---
phase: 31-public-marketing-coachs
plan: 03
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - apps/web/src/components/marketing/CoachsComparisonTable.tsx
  - apps/web/src/components/marketing/CoachsFounderSection.tsx
  - apps/web/src/components/marketing/CoachsFAQ.tsx
  - apps/web/src/components/marketing/CoachsCtaFooter.tsx
autonomous: true
requirements:
  - MKT-02
  - MKT-04
  - MKT-05
  - MKT-06

must_haves:
  truths:
    - "Comparison table renders 3 rows: AI-native tools, Client data depth, Integrated athlete mobile app"
    - "Ziko column has 4px orange left border and bg-primary/5 background — visually highlighted"
    - "Trainerize and TrueCoach named explicitly in column headers (per D-06)"
    - "Row 1 Trainerize and TrueCoach cells show IoCloseCircleOutline (✗); Row 3 Trainerize shows IoCheckmarkCircleOutline (✓ Basic)"
    - "Founder section contains the mission tagline from t('founder.quote') and the TODO comment for real founder content"
    - "FAQ has 6 items rendered as accordion; AnimatePresence controls height expansion; chevron rotates 180deg on open"
    - "CoachsCtaFooter CTA links to /{locale}/coach/onboarding with the same shadow style as hero CTA"
    - "All useInView calls use { once: true }"
    - "Comparison table has <caption> for accessibility; checkmark/X icons have aria-label"
  artifacts:
    - path: "apps/web/src/components/marketing/CoachsComparisonTable.tsx"
      provides: "3-column comparison table with Ziko highlight"
      contains: "'use client'"
    - path: "apps/web/src/components/marketing/CoachsFounderSection.tsx"
      provides: "Mission blockquote with founder placeholder"
      contains: "TODO"
    - path: "apps/web/src/components/marketing/CoachsFAQ.tsx"
      provides: "6-item accordion FAQ"
      contains: "AnimatePresence"
    - path: "apps/web/src/components/marketing/CoachsCtaFooter.tsx"
      provides: "Final CTA footer section"
      contains: "'use client'"
  key_links:
    - from: "apps/web/src/components/marketing/CoachsCtaFooter.tsx"
      to: "/{locale}/coach/onboarding"
      via: "Link href prop"
      pattern: "coach/onboarding"
    - from: "apps/web/src/components/marketing/CoachsFAQ.tsx"
      to: "framer-motion AnimatePresence"
      via: "accordion open/close height tween"
      pattern: "AnimatePresence"
---

<objective>
Wave 2 (parallel with 31-02) — Comparison table, Founder section, FAQ, and final CTA footer.

Creates the bottom four sections of the /coachs marketing page:
- CoachsComparisonTable: honest 3-column table highlighting Ziko against Trainerize and TrueCoach (MKT-04)
- CoachsFounderSection: mission blockquote with "Built by athletes, for coaches" tagline (MKT-06)
- CoachsFAQ: 6-item animated accordion (accessibility-compliant with aria-expanded, aria-controls)
- CoachsCtaFooter: repeat CTA driving beta signup conversions (MKT-02)

Purpose: Conversion-driving bottom half of the page; FAQ addresses coach objections; final CTA captures visitors who scrolled past the hero.
Output: 4 fully animated, i18n-wired 'use client' components ready for import by page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-public-marketing-coachs/31-UI-SPEC.md
@.planning/phases/31-public-marketing-coachs/31-RESEARCH.md
@.planning/phases/31-public-marketing-coachs/31-CONTEXT.md

<interfaces>
<!-- Verified from apps/web/src/lib/motion.ts -->
```typescript
import { fadeUp, ctaHover, ctaTap } from '@/lib/motion'
// fadeUp: hidden={opacity:0,y:30} / visible={opacity:1,y:0,transition:{duration:0.5,ease:[0.16,1,0.3,1]}}
// ctaHover: { scale: 1.05 }   ctaTap: { scale: 0.97 }
```

<!-- FAQ accordion pattern from RESEARCH.md §Pattern 2 -->
```typescript
import { motion, AnimatePresence, useInView } from 'framer-motion'
const [openIndex, setOpenIndex] = useState<number | null>(null)
// Per item:
<AnimatePresence>
  {openIndex === i && (
    <motion.div
      key="answer"
      initial={{ height: 0, opacity: 0 }}
      animate={{ height: 'auto', opacity: 1 }}
      exit={{ height: 0, opacity: 0 }}
      transition={{ duration: 0.25, ease: [0.16, 1, 0.3, 1] }}
      style={{ overflow: 'hidden' }}
    >
```

<!-- CTA link pattern — locale prop from server page -->
// CoachsCtaFooter accepts locale: string prop (same as CoachsHero)
// href={`/${locale}/coach/onboarding`}

<!-- Icons needed -->
import { IoCheckmarkCircleOutline, IoCloseCircleOutline, IoChevronDownOutline } from 'react-icons/io5'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CoachsComparisonTable — 3-column honest comparison (MKT-04)</name>
  <files>apps/web/src/components/marketing/CoachsComparisonTable.tsx</files>
  <read_first>
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §4. Comparison Table — exact spec: header row style, Ziko column highlight (4px orange left border + bg-primary/5), 3 feature rows with exact cell content, footnote, accessibility requirements
    - .planning/phases/31-public-marketing-coachs/31-CONTEXT.md §D-06, D-07 — Trainerize and TrueCoach named explicitly; 3 rows: AI-native tools, Client data depth, Integrated athlete mobile app
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §Accessibility Requirements — table needs caption element, scope="col" on headers, aria-label on icons
  </read_first>
  <action>
    Create apps/web/src/components/marketing/CoachsComparisonTable.tsx as a 'use client' component.

    Imports:
    - 'use client'
    - { motion, useInView } from 'framer-motion'
    - { useRef } from 'react'
    - { useTranslations } from 'next-intl'
    - { IoCheckmarkCircleOutline, IoCloseCircleOutline } from 'react-icons/io5'

    No locale prop needed for this section.

    Inside component:
    - const t = useTranslations('coachs')
    - const ref = useRef(null)
    - const inView = useInView(ref, { once: true })

    Section: `<section className="py-24 bg-white">`
    Container: `<div className="max-w-screen-xl mx-auto px-8">`

    Headings:
    `<h2 className="text-3xl md:text-4xl font-bold text-text text-center mb-4">{t('comparison.heading')}</h2>`
    `<p className="text-base text-muted text-center mb-12">{t('comparison.subheading')}</p>`

    Table wrapper (ref here for inView, overflow-x-auto for mobile):
    `<motion.div ref={ref} initial={{ opacity: 0, y: 20 }} animate={inView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.5 }} className="max-w-screen-md mx-auto overflow-x-auto">`

    Table: `<table className="w-full border-collapse">`
    Caption (accessibility): `<caption className="sr-only">Comparison of Ziko vs Trainerize vs TrueCoach</caption>`

    Header row (`<thead><tr>`):
    - Empty first cell: `<th scope="col" className="px-4 py-3 border-b-2 border-border" />`
    - Ziko header: `<th scope="col" className="px-4 py-3 border-b-2 border-border text-xs font-bold text-primary bg-primary/5" style={{ borderLeft: '4px solid #FF5C1A' }}>Ziko</th>`
    - Trainerize: `<th scope="col" className="px-4 py-3 border-b-2 border-border text-xs font-bold text-text">Trainerize</th>`
    - TrueCoach: `<th scope="col" className="px-4 py-3 border-b-2 border-border text-xs font-bold text-text">TrueCoach</th>`

    Note: Column names "Ziko", "Trainerize", "TrueCoach" are product names (proper nouns), NOT from i18n — hardcoding them is correct and expected. Feature labels and cell values come from t().

    3 feature rows (tbody), alternating bg: rows at index 0,2 → bg-white; index 1 → bg-surface-muted (className `bg-[#F0EFE9]`). Ziko cells always override with bg-primary/5:

    Row 1 — AI-native tools:
    - Feature label cell: `<td className="px-4 py-3 border-b border-border text-xs font-bold text-text sticky left-0 bg-white">{t('comparison.feature1Label')}</td>`
    - Ziko cell: `<td className="px-4 py-3 border-b border-border bg-primary/5 text-xs text-text" style={{ borderLeft: '4px solid #FF5C1A' }}>` + `<div className="flex items-center gap-1.5"><IoCheckmarkCircleOutline size={18} className="text-[#22C55E] flex-shrink-0" aria-label="Oui" />{t('comparison.zikoFeature1')}</div>`
    - Trainerize: `<IoCloseCircleOutline size={18} className="text-[#EF4444]" aria-label="Non" />` (no text — just icon per D-07)
    - TrueCoach: `<IoCloseCircleOutline size={18} className="text-[#EF4444]" aria-label="Non" />`

    Row 2 — Client data depth (bg-[#F0EFE9] for label+competitor cells):
    - Feature label cell: `bg-[#F0EFE9]` + sticky left-0
    - Ziko cell: bg-primary/5 with orange left border + checkmark + t('comparison.zikoFeature2')
    - Trainerize: text-only `{t('comparison.trainerizeFeature2')}` ("Saisie manuelle" / "Manual input")
    - TrueCoach: text-only `{t('comparison.truecoachFeature2')}`

    Row 3 — Integrated athlete mobile app (bg-white):
    - Feature label cell: bg-white + sticky left-0
    - Ziko cell: bg-primary/5 + checkmark + t('comparison.zikoFeature3')
    - Trainerize: checkmark + t('comparison.trainerizeFeature3') ("Basique" / "Basic") — uses checkmark because Trainerize does have basic mobile app
    - TrueCoach: text-only t('comparison.truecoachFeature3') ("Limitée" / "Limited") — no icon, just text

    Footnote below table:
    `<p className="text-xs text-muted mt-4 text-center">{t('comparison.footnote')}</p>`
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep "CoachsComparison" | head -5 || echo "CoachsComparisonTable: no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with 'use client'
    - File exports function CoachsComparisonTable
    - File contains `<caption` element (accessibility)
    - File contains scope="col" on header cells
    - File contains aria-label="Oui" and aria-label="Non" on check/X icons
    - File contains border-left: '4px solid #FF5C1A' on Ziko column cells
    - File contains bg-primary/5 class on Ziko column cells
    - File contains IoCheckmarkCircleOutline and IoCloseCircleOutline imports
    - "Trainerize" and "TrueCoach" appear as column header text (hardcoded proper nouns — correct)
    - Feature label strings use t('comparison.featureNLabel') — not hardcoded
    - useInView uses { once: true }
    - TypeScript: no errors
  </acceptance_criteria>
  <done>CoachsComparisonTable renders a 3-column table with Ziko highlighted (orange left border + bg-primary/5), 3 feature rows with accurate check/X cells, sticky first column on mobile, and accessible caption + scope attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Build CoachsFounderSection and CoachsCtaFooter</name>
  <files>
    apps/web/src/components/marketing/CoachsFounderSection.tsx,
    apps/web/src/components/marketing/CoachsCtaFooter.tsx
  </files>
  <read_first>
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §5. Founder / Mission Section — blockquote card spec: border-l-4 border-primary, flex row layout, avatar circle, placeholder content, TODO comment requirement
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §7. Final CTA Footer Section — identical CTA button to hero (same shadow, same hover), heading/subheading/note
    - .planning/phases/31-public-marketing-coachs/31-CONTEXT.md §D-08, D-09 — founder placeholder required; TODO comment; tagline "Built by athletes, for coaches"
    - apps/web/src/lib/motion.ts — ctaHover, ctaTap for CTA button
  </read_first>
  <action>
    Create TWO files in a single step.

    === FILE 1: CoachsFounderSection.tsx ===

    'use client' component. No locale prop needed.

    Imports: motion, useInView, useRef, useTranslations from 'next-intl'

    Inside component:
    - const t = useTranslations('coachs')
    - const ref = useRef(null)
    - const inView = useInView(ref, { once: true })

    Section: `<section className="py-24 bg-background">`
    Heading: `<h2 className="text-3xl md:text-4xl font-bold text-text text-center mb-12">{t('founder.heading')}</h2>`

    Blockquote card (motion.div with ref, slide in from left):
    `<motion.div ref={ref} initial={{ opacity: 0, x: -20 }} animate={inView ? { opacity: 1, x: 0 } : {}} transition={{ duration: 0.5, delay: 0.1, ease: [0.16, 1, 0.3, 1] }} className="max-w-2xl mx-auto bg-white border border-border rounded-2xl p-8 border-l-4" style={{ borderLeftColor: '#FF5C1A', boxShadow: '0 4px 8px rgba(28,26,23,0.08)' }}>`

    Note: Tailwind's `border-l-4 border-primary` may conflict with the full border. Use inline style for the left border color override OR use `[border-left:4px_solid_#FF5C1A]` Tailwind arbitrary value alongside `border border-border rounded-2xl` for the other three sides. Simplest approach: `className="... rounded-2xl p-8"` with `style={{ border: '1px solid #E2E0DA', borderLeft: '4px solid #FF5C1A', boxShadow: '0 4px 8px rgba(28,26,23,0.08)' }}`.

    Inner layout: `<div className="flex flex-col md:flex-row gap-8 items-start">`

    LEFT (flex-1):
    - Decorative quote mark: `<span className="text-5xl text-primary/20 font-bold leading-none select-none" aria-hidden="true">"</span>`
    - Mission tagline: `<p className="text-xl font-bold text-text leading-snug mb-4">{t('founder.quote')}</p>`
    - Founder story:
      `{/* TODO: replace with real founder story before go-live */}`
      `<p className="text-base text-muted leading-relaxed">{t('founder.story')}</p>`

    RIGHT (flex-shrink-0, w-48, flex flex-col items-center gap-2, text-center):
    - Avatar circle:
      `{/* TODO: replace with real founder photo before go-live */}`
      `<div className="w-16 h-16 rounded-full border-2 border-border flex items-center justify-center" style={{ backgroundColor: '#F0EFE9' }}>`
        `<span className="text-muted font-bold text-xs">FD</span>` (placeholder initials)
    - Name: `<p className="text-xs font-bold text-text">{/* TODO: replace with real name */}{'Fondateur, Ziko' /* uses t('founder.name') */}</p>`
      Actually: `<p className="text-xs font-bold text-text">{t('founder.name')}</p>`
      `{/* TODO: replace with real name before go-live */}`
    - Title: `<p className="text-xs text-muted">{t('founder.title')}</p>`

    === FILE 2: CoachsCtaFooter.tsx ===

    'use client' component. Props: `interface CoachsCtaFooterProps { locale: string }`

    Imports: motion, useInView, useRef, useTranslations, Link from 'next/link', ctaHover, ctaTap from '@/lib/motion'

    Inside component:
    - const t = useTranslations('coachs')
    - const ref = useRef(null)
    - const inView = useInView(ref, { once: true })

    Section: `<section className="py-24 bg-background">`
    Inner: `<div ref={ref} className="max-w-screen-xl mx-auto px-8 text-center">`

    Animate the entire inner div:
    `<motion.div initial={{ opacity: 0, y: 20 }} animate={inView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.5 }}>`

    Heading: `<h2 className="text-3xl md:text-4xl font-bold text-text mb-4">{t('cta.heading')}</h2>`
    Subheading: `<p className="text-base text-muted mb-8">{t('cta.subheading')}</p>`

    CTA button (identical treatment to hero):
    `<motion.div whileHover={ctaHover} whileTap={ctaTap} className="inline-block">`
      `<Link href={`/${locale}/coach/onboarding`} className="bg-primary text-white px-8 py-4 rounded-xl font-bold text-xs inline-block" style={{ boxShadow: '0 4px 20px rgba(255,92,26,0.30)' }}>`
        `{t('cta.button')}`

    Beta note: `<p className="text-xs text-muted mt-3">{t('cta.note')}</p>`
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -E "CoachsFounder|CoachsCta" | head -5 || echo "CoachsFounderSection + CoachsCtaFooter: no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - CoachsFounderSection.tsx starts with 'use client'
    - CoachsFounderSection.tsx contains at least 2 TODO comments (founder story + founder name/photo)
    - CoachsFounderSection.tsx contains t('founder.quote') — not hardcoded mission tagline
    - CoachsFounderSection.tsx contains borderLeft '4px solid #FF5C1A' on the blockquote card
    - CoachsFounderSection.tsx contains useInView with { once: true }
    - CoachsFounderSection.tsx contains "FD" as placeholder initials in the avatar circle
    - CoachsCtaFooter.tsx starts with 'use client'
    - CoachsCtaFooter.tsx exports function CoachsCtaFooter with prop locale: string
    - CoachsCtaFooter.tsx contains `/${locale}/coach/onboarding` in the Link href
    - CoachsCtaFooter.tsx uses ctaHover and ctaTap from '@/lib/motion'
    - CoachsCtaFooter.tsx contains boxShadow '0 4px 20px rgba(255,92,26,0.30)' on the Link
    - No hardcoded copy strings in either file
    - TypeScript: no errors on either file
  </acceptance_criteria>
  <done>CoachsFounderSection renders the mission blockquote with orange left accent border, decorative quote mark, placeholder founder content with TODO comments for real content. CoachsCtaFooter renders the repeat CTA linked to /{locale}/coach/onboarding with the same shadow treatment as the hero CTA.</done>
</task>

<task type="auto">
  <name>Task 3: Build CoachsFAQ — 6-item animated accordion with AnimatePresence</name>
  <files>apps/web/src/components/marketing/CoachsFAQ.tsx</files>
  <read_first>
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §6. FAQ Section — 6 items fully specified; accordion behavior: AnimatePresence + height tween; chevron rotate 180; aria attributes required
    - .planning/phases/31-public-marketing-coachs/31-RESEARCH.md §Pattern 2: FAQ accordion with AnimatePresence — exact code pattern to follow
    - .planning/phases/31-public-marketing-coachs/31-RESEARCH.md §Pitfall 3 — useInView must use { once: true }
  </read_first>
  <action>
    Create apps/web/src/components/marketing/CoachsFAQ.tsx as a 'use client' component.

    Imports:
    - 'use client'
    - { motion, AnimatePresence, useInView } from 'framer-motion'
    - { useState, useRef } from 'react'
    - { useTranslations } from 'next-intl'
    - { IoChevronDownOutline } from 'react-icons/io5'

    No locale prop needed.

    FAQ items config (const outside component):
    ```
    const FAQ_KEYS = ['1','2','3','4','5','6'] as const
    ```

    Inside component:
    - const t = useTranslations('coachs')
    - const [openIndex, setOpenIndex] = useState<number | null>(null)
    - const ref = useRef(null)
    - const inView = useInView(ref, { once: true })

    Section: `<section className="py-24 bg-white">`
    Container: `<div className="max-w-screen-md mx-auto px-8">`
    Heading: `<h2 className="text-3xl md:text-4xl font-bold text-text text-center mb-12">{t('faq.heading')}</h2>`

    Items list (ref on the container div for stagger trigger):
    `<div ref={ref} className="divide-y divide-border">` (or use border-b on each item)

    For each item (map over FAQ_KEYS with index i):

    Stagger entrance: wrap each item in:
    `<motion.div key={k} initial={{ opacity: 0, y: 16 }} animate={inView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.4, delay: i * 0.06, ease: [0.16, 1, 0.3, 1] }}>`

    Item structure:
    `<div className="border-b border-border last:border-b-0">`

    Question row (toggle on click):
    `<button onClick={() => setOpenIndex(openIndex === i ? null : i)} className="w-full flex justify-between items-center py-5 text-left cursor-pointer" aria-expanded={openIndex === i} aria-controls={`faq-answer-${i}`}>`
      `<span className="text-xs font-bold text-text">{t(`faq.q${k}`)}</span>`
      `<motion.span animate={{ rotate: openIndex === i ? 180 : 0 }} transition={{ duration: 0.25 }}>`
        `<IoChevronDownOutline size={18} className={openIndex === i ? 'text-primary' : 'text-muted'} />`

    Answer (AnimatePresence controls mount/unmount):
    `<AnimatePresence>`
    `{openIndex === i && (`
    `<motion.div key="answer" id={`faq-answer-${i}`} role="region" initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.25, ease: [0.16, 1, 0.3, 1] }} style={{ overflow: 'hidden' }}>`
    `<p className="text-base text-muted leading-relaxed py-4">{t(`faq.a${k}`)}</p>`
    `</motion.div>)`}
    `</AnimatePresence>`

    Note on template literal in JSX: use t(`faq.q${k}`) and t(`faq.a${k}`) where k is from FAQ_KEYS ('1'..'6'). The i variable is the array index (0..5) for delay and openIndex tracking. The k variable is the string key suffix ('1'..'6').
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep "CoachsFAQ" | head -5 || echo "CoachsFAQ: no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with 'use client'
    - File exports function CoachsFAQ
    - File imports AnimatePresence from 'framer-motion'
    - File contains useState for openIndex
    - File contains aria-expanded on button elements
    - File contains aria-controls on button and id matching on the answer div
    - File contains role="region" on the answer motion.div
    - File contains IoChevronDownOutline with animate={{ rotate: openIndex === i ? 180 : 0 }}
    - File contains { once: true } in useInView call
    - FAQ_KEYS has exactly 6 items ('1' through '6')
    - All q/a content via t(`faq.qN`) and t(`faq.aN`) — no hardcoded question/answer text
    - TypeScript: no errors
  </acceptance_criteria>
  <done>CoachsFAQ renders 6 accordion items that stagger in on scroll. Each item toggles open/close with AnimatePresence height tween. Chevron rotates 180deg on open. Accordion is accessible with aria-expanded, aria-controls, and role="region" on panels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale prop → Link href | locale comes from generateStaticParams ['fr','en']; not user-supplied |
| FAQ state → DOM rendering | openIndex is local React state; no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | CoachsCtaFooter locale prop | accept | locale is statically typed string from generateStaticParams; only 'fr' or 'en' possible; no injection surface |
| T-31-06 | Information Disclosure | Founder section placeholder | accept | Placeholder content is non-sensitive marketing copy; TODO comments are stripped from production build |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
After all 3 tasks complete:

1. `ls apps/web/src/components/marketing/CoachsComparisonTable.tsx apps/web/src/components/marketing/CoachsFounderSection.tsx apps/web/src/components/marketing/CoachsFAQ.tsx apps/web/src/components/marketing/CoachsCtaFooter.tsx` — all 4 files exist
2. `grep -l "'use client'" apps/web/src/components/marketing/CoachsComparisonTable.tsx apps/web/src/components/marketing/CoachsFounderSection.tsx apps/web/src/components/marketing/CoachsFAQ.tsx apps/web/src/components/marketing/CoachsCtaFooter.tsx` — all 4 returned
3. `grep "caption" apps/web/src/components/marketing/CoachsComparisonTable.tsx` — accessibility caption present
4. `grep "AnimatePresence" apps/web/src/components/marketing/CoachsFAQ.tsx` — returns match
5. `grep "aria-expanded" apps/web/src/components/marketing/CoachsFAQ.tsx` — returns match
6. `grep "TODO" apps/web/src/components/marketing/CoachsFounderSection.tsx` — TODO comments present
7. `grep "coach/onboarding" apps/web/src/components/marketing/CoachsCtaFooter.tsx` — CTA link present
8. `cd apps/web && npx tsc --noEmit 2>&1 | tail -5` — no errors
</verification>

<success_criteria>
- CoachsComparisonTable: 3 feature rows, Ziko column highlighted with 4px orange border + bg-primary/5, Trainerize/TrueCoach named, accessible caption + scope attributes
- CoachsFounderSection: blockquote card with orange left border, mission tagline from i18n, two TODO comments for real founder content (MKT-06 satisfied)
- CoachsFAQ: 6 accordion items with AnimatePresence, chevron rotation, aria-expanded + aria-controls (accessibility compliant)
- CoachsCtaFooter: repeat CTA with /{locale}/coach/onboarding href and identical shadow to hero button
- All 4 components compile without TypeScript errors
- All copy via useTranslations('coachs') — no hardcoded strings (except proper nouns Trainerize, TrueCoach in comparison headers)
</success_criteria>

<output>
Create `.planning/phases/31-public-marketing-coachs/31-03-SUMMARY.md` when done
</output>
