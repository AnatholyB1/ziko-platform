---
plan: P02
phase: 03-marketing-content
type: execute
wave: 2
depends_on: [P01]
requirements: [MKTG-01]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/marketing/Hero.tsx
  - src/app/[locale]/layout.tsx
autonomous: true
must_haves:
  truths:
    - "A sticky header is visible at the top of every page with logo, locale switcher, and download CTA"
    - "The hero section displays a headline, subline, two CTA buttons, and a CSS phone frame"
    - "Switching locale via header links navigates between FR and EN versions"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky navigation header with logo, locale switcher, download CTA"
      min_lines: 20
      exports: ["Header"]
    - path: "src/components/marketing/Hero.tsx"
      provides: "Hero section with split layout, copy, CTAs, and CSS phone frame"
      min_lines: 40
      exports: ["Hero"]
    - path: "src/app/[locale]/layout.tsx"
      provides: "Layout updated with Header component above content"
      contains: "Header"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "messages/fr.json"
      via: "getTranslations('Header')"
      pattern: "getTranslations.*Header"
    - from: "src/components/marketing/Hero.tsx"
      to: "messages/fr.json"
      via: "getTranslations('Home')"
      pattern: "getTranslations.*Home"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render"
      pattern: "import.*Header"
---

<objective>
Build the sticky Header and Hero section components, and integrate the Header into the locale layout. After this plan, the site has navigation and the hero marketing section.

Purpose: Delivers the top-of-page experience — navigation + hero call-to-action (MKTG-01).
Output: Header.tsx, Hero.tsx, updated layout.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketing-content/03-CONTEXT.md
@.planning/phases/03-marketing-content/03-UI-SPEC.md

<interfaces>
<!-- Established patterns the executor must follow -->

From src/components/layout/Footer.tsx (Server Component pattern to replicate):
```typescript
import { getTranslations } from 'next-intl/server';
import { Link } from '@/i18n/navigation';

export async function Footer() {
  const t = await getTranslations('Footer');
  return (
    <footer className="bg-white border-t-2 border-t-primary">
      <div className="max-w-screen-xl mx-auto px-8 py-6 flex justify-between items-center">
        ...
      </div>
    </footer>
  );
}
```

From src/i18n/navigation.ts:
```typescript
export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

From src/app/[locale]/layout.tsx (current structure — Header goes above flex-1 div):
```typescript
<div className={`${inter.className} bg-background text-text min-h-screen flex flex-col`}>
  <div className="flex-1">
    {children}
  </div>
  <Footer />
</div>
```
</interfaces>
</context>

<tasks>

<task id="T1" type="auto">
  <name>Task 1: Create Header.tsx and Hero.tsx components</name>
  <files>C:/ziko-web/src/components/layout/Header.tsx, C:/ziko-web/src/components/marketing/Hero.tsx</files>
  <read_first>
    - C:/ziko-web/src/components/layout/Footer.tsx — Server Component pattern to replicate exactly (getTranslations, Link from @/i18n/navigation)
    - C:/ziko-web/messages/fr.json — confirm Header and Home.hero translation keys exist (from P01)
    - C:/ziko-platform/.planning/phases/03-marketing-content/03-UI-SPEC.md — exact CSS classes, dimensions, and specs for Header and Hero
  </read_first>
  <action>
Create `src/components/marketing/` directory (does not exist yet).

**File 1: `C:/ziko-web/src/components/layout/Header.tsx`**

Server Component (async function, NOT 'use client'). Per D-13, mirrors Footer.tsx pattern exactly.

```typescript
import { getTranslations, getLocale } from 'next-intl/server';
import { Link } from '@/i18n/navigation';
```

Structure per UI-SPEC:
- `<header>` with classes: `sticky top-0 z-50 bg-white border-b border-border`
- Inner container: `<div className="max-w-screen-xl mx-auto px-8 h-14 flex items-center justify-between">`
- Left side: `<Link href="/" className="text-xl font-bold text-primary">` with `{t('logo')}` (renders "Ziko" in orange) per D-11
- Right side: `<div className="flex items-center gap-4">`
  - Locale switcher (per D-12): Two `<Link>` components with `locale` prop
    - FR link: `<Link href="/" locale="fr">` with text `{t('localeFR')}` — active state: `font-bold text-text`, inactive: `text-muted hover:text-text`
    - EN link: `<Link href="/" locale="en">` with text `{t('localeEN')}` — active state: `font-bold text-text`, inactive: `text-muted hover:text-text`
    - Separator: `<span className="text-muted">|</span>` between the two links
    - To determine active locale: use `const locale = await getLocale()`; then conditionally apply classes: `locale === 'fr' ? 'font-bold text-text' : 'text-muted hover:text-text'`
  - CTA button: `<Link href="#" className="bg-primary text-white px-4 py-2 rounded-lg text-sm font-bold">` with `{t('cta')}` per D-14
- Translation namespace: `const t = await getTranslations('Header');`
- Export: `export async function Header()`

**File 2: `C:/ziko-web/src/components/marketing/Hero.tsx`**

Server Component (async function). Uses `getTranslations('Home')`.

```typescript
import { getTranslations } from 'next-intl/server';
```

Structure per UI-SPEC Hero Section:
- `<section>` wrapper
- Container: `<div className="max-w-screen-xl mx-auto px-8 pt-16 pb-24">`
- Responsive layout: `<div className="flex flex-col md:flex-row md:items-center gap-8">`
  - Left column: `<div className="flex flex-col gap-6 justify-center md:w-1/2">`
    - Headline: `<h1 className="text-3xl md:text-4xl font-bold text-text leading-tight">` with `{t('hero.headline')}`
    - Subline: `<p className="text-base text-muted leading-relaxed">` with `{t('hero.subline')}`
    - CTA row: `<div className="flex gap-4 mt-2">`
      - App Store: `<a href="#" className="bg-primary text-white px-6 py-3 rounded-xl font-bold text-sm inline-block">` with `{t('hero.ctaAppStore')}` per D-04
      - Play Store: `<a href="#" className="bg-primary text-white px-6 py-3 rounded-xl font-bold text-sm inline-block">` with `{t('hero.ctaPlayStore')}` per D-04
  - Right column: `<div className="flex justify-center items-center md:w-1/2">`
    - Phone device frame (per D-02, CSS-only):
      - Outer: `<div>` with inline style: `{ width: 220, height: 440, borderRadius: 32, border: '3px solid #1C1A17', boxShadow: '0 24px 64px rgba(0,0,0,0.18)', position: 'relative', overflow: 'hidden' }`
      - Inner fill: `<div>` with inline style: `{ width: '100%', height: '100%', borderRadius: 30, background: 'linear-gradient(160deg, #FF5C1A 0%, #FFB199 60%, #F7F6F3 100%)' }`
      - Notch: `<div>` with inline style: `{ position: 'absolute', top: 8, left: '50%', transform: 'translateX(-50%)', width: 80, height: 20, borderRadius: 12, background: '#1C1A17', zIndex: 10 }`
- Translation namespace: `const t = await getTranslations('Home');`
- Export: `export async function Hero()`
  </action>
  <verify>
    <automated>test -f /c/ziko-web/src/components/layout/Header.tsx && test -f /c/ziko-web/src/components/marketing/Hero.tsx && echo "PASS"</automated>
  </verify>
  <done>Header.tsx and Hero.tsx exist as async Server Components with correct translations, styling, and structure per UI-SPEC</done>
  <acceptance_criteria>
    - C:/ziko-web/src/components/layout/Header.tsx exists and contains `export async function Header()`
    - Header.tsx contains `getTranslations('Header')`
    - Header.tsx contains `sticky top-0 z-50`
    - Header.tsx contains `h-14` (56px height)
    - Header.tsx contains `text-primary font-bold text-xl` on the logo Link
    - Header.tsx contains `locale="fr"` and `locale="en"` on Link components (per D-12)
    - Header.tsx contains `bg-primary text-white` on the CTA link
    - Header.tsx imports Link from `@/i18n/navigation`
    - C:/ziko-web/src/components/marketing/Hero.tsx exists and contains `export async function Hero()`
    - Hero.tsx contains `getTranslations('Home')`
    - Hero.tsx contains `t('hero.headline')`
    - Hero.tsx contains `md:flex-row` (responsive split layout per D-01)
    - Hero.tsx contains `width: 220` and `height: 440` (phone frame dimensions)
    - Hero.tsx contains `linear-gradient(160deg, #FF5C1A` (phone frame inner gradient)
    - Hero.tsx contains `borderRadius: 32` (phone frame outer radius)
    - Hero.tsx contains `href="#"` on both CTA buttons (per D-04/D-15)
  </acceptance_criteria>
</task>

<task id="T2" type="auto">
  <name>Task 2: Integrate Header into locale layout</name>
  <files>C:/ziko-web/src/app/[locale]/layout.tsx</files>
  <read_first>
    - C:/ziko-web/src/app/[locale]/layout.tsx — current layout structure (Header goes above the flex-1 div, below the root div start)
    - C:/ziko-web/src/components/layout/Header.tsx — confirm export name is `Header` (just created in T1)
  </read_first>
  <action>
Edit `C:/ziko-web/src/app/[locale]/layout.tsx`:

1. Add import: `import { Header } from '@/components/layout/Header';`
2. Insert `<Header />` between the opening `<div className="...bg-background...">` and the `<div className="flex-1">` wrapper.

The final JSX structure should be:
```tsx
<div className={`${inter.className} bg-background text-text min-h-screen flex flex-col`}>
  <Header />
  <div className="flex-1">
    {children}
  </div>
  <Footer />
</div>
```

Do NOT change any other part of the layout file. Keep the Inter font, generateStaticParams, NextIntlClientProvider, Footer, and all existing imports unchanged.
  </action>
  <verify>
    <automated>grep -c "Header" /c/ziko-web/src/app/\\[locale\\]/layout.tsx</automated>
  </verify>
  <done>layout.tsx imports and renders Header above the flex-1 content area, all other layout elements unchanged</done>
  <acceptance_criteria>
    - C:/ziko-web/src/app/[locale]/layout.tsx contains `import { Header } from '@/components/layout/Header'`
    - layout.tsx contains `<Header />` appearing before the `<div className="flex-1">` line
    - layout.tsx still contains `<Footer />` (unchanged)
    - layout.tsx still contains `NextIntlClientProvider` (unchanged)
    - layout.tsx still contains `generateStaticParams` (unchanged)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `cd /c/ziko-web && npx next build` completes without errors (static build succeeds)
2. Header.tsx file size is >= 20 lines
3. Hero.tsx file size is >= 40 lines
4. `grep -c "sticky" C:/ziko-web/src/components/layout/Header.tsx` returns 1
5. `grep -c "Header" C:/ziko-web/src/app/[locale]/layout.tsx` returns at least 2 (import + JSX usage)
</verification>

<success_criteria>
- Header renders on every page: sticky, 56px tall, white bg, border-bottom, logo in orange, FR|EN locale links, orange CTA button
- Hero renders with split layout: headline + subline + two orange CTA buttons on left, CSS phone frame with gradient on right
- Layout correctly places Header above content and Footer below
- `next build` passes with all routes static
</success_criteria>

<output>
After completion, create `.planning/phases/03-marketing-content/03-02-SUMMARY.md`
</output>
