---
phase: 26-crm-client-management
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/package-lock.json
  - packages/coach-sdk/src/schemas/client-summary.ts
  - packages/coach-sdk/src/schemas/client-tag.ts
  - packages/coach-sdk/src/schemas/client-note.ts
  - packages/coach-sdk/src/schemas/index.ts
  - backend/api/test/coach/clients-roster.spec.ts
  - backend/api/test/coach/clients-signals.spec.ts
  - backend/api/test/coach/clients-tabs.spec.ts
  - backend/api/test/coach/clients-summary.spec.ts
  - backend/api/test/coach/clients-tags.spec.ts
  - backend/api/test/coach/clients-notes.spec.ts
  - backend/api/test/coach/clients-compare.spec.ts
  - backend/api/test/coach/clients-revoke-coach.spec.ts
autonomous: true
requirements: [CLIENT-01, CLIENT-02, CLIENT-03, CLIENT-04, CLIENT-05, CLIENT-06, CLIENT-07, CLIENT-08]

must_haves:
  truths:
    - "@tanstack/react-table@8.21.3 and recharts@3.8.1 are in apps/web/package.json"
    - "8 test stub files exist in backend/api/test/coach/ and exit 0 under vitest (--passWithNoTests)"
    - "3 new Zod schemas are exported from packages/coach-sdk"
  artifacts:
    - path: "apps/web/package.json"
      provides: "@tanstack/react-table and recharts deps"
      contains: "@tanstack/react-table"
    - path: "packages/coach-sdk/src/schemas/client-summary.ts"
      provides: "ClientSummarySchema Zod schema"
      exports: ["ClientSummarySchema"]
    - path: "packages/coach-sdk/src/schemas/client-tag.ts"
      provides: "CoachClientTagSchema Zod schema"
      exports: ["CoachClientTagSchema"]
    - path: "packages/coach-sdk/src/schemas/client-note.ts"
      provides: "CoachClientNoteSchema Zod schema"
      exports: ["CoachClientNoteSchema"]
  key_links:
    - from: "packages/coach-sdk/src/schemas/index.ts"
      to: "client-summary.ts, client-tag.ts, client-note.ts"
      via: "export re-export"
      pattern: "ClientSummarySchema|CoachClientTagSchema|CoachClientNoteSchema"
---

<objective>
Wave 0 pre-flight: install the two new frontend deps, create 8 Vitest test stubs, and add 3 Zod schemas to coach-sdk. Nothing runs against the DB yet — test stubs are skipped scaffolds that unblock Wave 1 Nyquist enforcement.

Purpose: All downstream plans depend on these deps and test files existing. Installing them first avoids context window waste on package resolution during implementation plans.
Output: Updated package.json, 8 test stub files, 3 new coach-sdk schemas.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/PROJECT.md
@C:/ziko-platform/.planning/ROADMAP.md
@C:/ziko-platform/.planning/STATE.md
@C:/ziko-platform/.planning/phases/26-crm-client-management/26-CONTEXT.md
@C:/ziko-platform/.planning/phases/26-crm-client-management/26-VALIDATION.md

<interfaces>
<!-- Existing coach-sdk schema structure — extend, do not replace -->
<!-- From packages/coach-sdk/src/schemas/index.ts (verified): -->
```typescript
export { ImportedProgramSchema } from './imported-program.js';
export { CoachClientLinkSchema } from './coach-client-link.js';
export { CoachProfileSchema } from './coach-profile.js';
export { CoachInvitationSchema, ... } from './coach-invitation.js';
export { CoachPreviewPayloadSchema, ... } from './coach-link-preview.js';
```

<!-- Existing test pattern — from backend/api/test/coach/clients-preview.spec.ts shape -->
<!-- Stubs use describe + it.todo to be valid Vitest stubs that pass with --passWithNoTests -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-table and recharts in apps/web</name>
  <read_first>
    - C:/ziko-platform/apps/web/package.json (current deps — must not already have these)
  </read_first>
  <files>apps/web/package.json, apps/web/package-lock.json</files>
  <action>
From the C:/ziko-platform/apps/web/ directory, install both libraries at exact versions:

```bash
cd C:/ziko-platform/apps/web && npm install @tanstack/react-table@8.21.3 recharts@3.8.1
```

These are new deps — neither is in the current package.json. Per D-01 (CONTEXT.md), `@tanstack/react-table` is locked to exactly `8.21.3`. Per D-16, `recharts` is locked to exactly `3.8.1`.

Do NOT use workspace root install — these are web-app-specific deps.
  </action>
  <verify>
    <automated>cd C:/ziko-platform/apps/web && rtk grep "@tanstack/react-table" package.json && rtk grep "recharts" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains `"@tanstack/react-table": "8.21.3"`
    - `apps/web/package.json` contains `"recharts": "3.8.1"` (or `"^3.8.1"` after npm normalizes)
    - `node_modules/@tanstack/react-table` directory exists in `apps/web/node_modules/`
  </acceptance_criteria>
  <done>Both libraries are resolvable from apps/web TypeScript imports</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 3 Zod schemas to packages/coach-sdk + create 8 Vitest test stubs</name>
  <read_first>
    - C:/ziko-platform/packages/coach-sdk/src/schemas/index.ts (existing exports — append only)
    - C:/ziko-platform/packages/coach-sdk/src/schemas/coach-profile.ts (shape reference for new schemas)
    - C:/ziko-platform/backend/api/test/coach/clients-preview.spec.ts (stub pattern to replicate)
    - C:/ziko-platform/.planning/phases/26-crm-client-management/26-VALIDATION.md (Wave 0 gap list)
    - C:/ziko-platform/.planning/phases/26-crm-client-management/26-CONTEXT.md (D-12: exact table shapes)
  </read_first>
  <files>
    packages/coach-sdk/src/schemas/client-summary.ts,
    packages/coach-sdk/src/schemas/client-tag.ts,
    packages/coach-sdk/src/schemas/client-note.ts,
    packages/coach-sdk/src/schemas/index.ts,
    backend/api/test/coach/clients-roster.spec.ts,
    backend/api/test/coach/clients-signals.spec.ts,
    backend/api/test/coach/clients-tabs.spec.ts,
    backend/api/test/coach/clients-summary.spec.ts,
    backend/api/test/coach/clients-tags.spec.ts,
    backend/api/test/coach/clients-notes.spec.ts,
    backend/api/test/coach/clients-compare.spec.ts,
    backend/api/test/coach/clients-revoke-coach.spec.ts
  </files>
  <behavior>
    - ClientSummarySchema: { sessions_this_week: number, habits_pct: number|null, last_workout_at: string|null, latest_weight_kg: number|null, mood_delta: number|null, mood_prev_avg: number|null, mood_curr_avg: number|null }
    - CoachClientTagSchema: { id: string (uuid), coach_id: string (uuid), client_id: string (uuid), tag: string (max 50 chars), created_at: string }
    - CoachClientNoteSchema: { id: string (uuid), coach_id: string (uuid), client_id: string (uuid), content: string, updated_at: string }
    - Test stubs: each file has describe('[route]') with it.todo('[behavior]') — passes with --passWithNoTests
  </behavior>
  <action>
**Part A — 3 Zod schemas in packages/coach-sdk/src/schemas/:**

Create `client-summary.ts`:
```typescript
import { z } from 'zod';

export const ClientSummarySchema = z.object({
  sessions_this_week: z.number().int().min(0),
  habits_pct: z.number().min(0).max(100).nullable(),
  last_workout_at: z.string().nullable(),
  latest_weight_kg: z.number().positive().nullable(),
  mood_delta: z.number().nullable(),       // curr_avg - prev_avg; null if insufficient data
  mood_prev_avg: z.number().nullable(),
  mood_curr_avg: z.number().nullable(),
});
export type ClientSummary = z.infer<typeof ClientSummarySchema>;
```

Create `client-tag.ts`:
```typescript
import { z } from 'zod';

export const CoachClientTagSchema = z.object({
  id: z.string().uuid(),
  coach_id: z.string().uuid(),
  client_id: z.string().uuid(),
  tag: z.string().min(1).max(50),
  created_at: z.string(),
});
export type CoachClientTag = z.infer<typeof CoachClientTagSchema>;
```

Create `client-note.ts`:
```typescript
import { z } from 'zod';

export const CoachClientNoteSchema = z.object({
  id: z.string().uuid(),
  coach_id: z.string().uuid(),
  client_id: z.string().uuid(),
  content: z.string(),
  updated_at: z.string(),
});
export type CoachClientNote = z.infer<typeof CoachClientNoteSchema>;
```

Append to `packages/coach-sdk/src/schemas/index.ts` (after the last export line):
```typescript
export { ClientSummarySchema } from './client-summary.js';
export type { ClientSummary } from './client-summary.js';
export { CoachClientTagSchema } from './client-tag.js';
export type { CoachClientTag } from './client-tag.js';
export { CoachClientNoteSchema } from './client-note.js';
export type { CoachClientNote } from './client-note.js';
```

**Part B — 8 Vitest test stubs in backend/api/test/coach/:**

Each file follows the pattern below (substitute describe label and it.todo descriptions per the VALIDATION.md test map):

`clients-roster.spec.ts`:
```typescript
import { describe, it } from 'vitest';
describe('GET /coach/clients — roster with signal flags', () => {
  it.todo('returns roster rows for linked coach');
  it.todo('does not return rows for unlinked coach');
  it.todo('includes signal_missed, signal_stale, signal_mood boolean flags');
});
```

`clients-signals.spec.ts`:
```typescript
import { describe, it } from 'vitest';
describe('signal flag computation — threshold unit tests', () => {
  it.todo('signal_missed: true when no workout_sessions in last 14 days');
  it.todo('signal_stale: true when no body_measurements in last 28 days');
  it.todo('signal_mood: true when last-3 avg < prev-3 avg');
  it.todo('all signals false when data is current');
});
```

`clients-tabs.spec.ts`:
```typescript
import { describe, it } from 'vitest';
describe('GET /coach/clients/:id/[tab] — 7 tab routes', () => {
  it.todo('sessions tab returns 200 for linked coach');
  it.todo('measurements tab returns 200 for linked coach');
  it.todo('habits tab returns 200 for linked coach');
  it.todo('nutrition tab returns 200 for linked coach');
  it.todo('sleep tab returns 200 for linked coach');
  it.todo('cardio tab returns 200 for linked coach');
  it.todo('journal tab returns 200 for linked coach');
  it.todo('all tabs return empty rows for unlinked coach');
});
```

`clients-summary.spec.ts`:
```typescript
import { describe, it } from 'vitest';
describe('GET /coach/clients/:id/summary — executive summary aggregates', () => {
  it.todo('returns sessions_this_week count for current week');
  it.todo('returns habits_pct as 7-day average completion rate');
  it.todo('returns last_workout_at relative to now');
  it.todo('returns mood_delta computed from last 14 days');
  it.todo('returns null fields when athlete has no data');
  it.todo('unlinked coach gets empty/403');
});
```

`clients-tags.spec.ts`:
```typescript
import { describe, it } from 'vitest';
describe('GET/POST/DELETE /coach/clients/:id/tags — tag CRUD', () => {
  it.todo('POST creates a tag row with coach_id = auth.uid()');
  it.todo('GET lists only the calling coach\'s tags for this client');
  it.todo('DELETE removes tag by id when coach_id matches');
  it.todo('Coach B cannot read Coach A\'s tags (RLS isolation)');
  it.todo('tag > 50 chars is rejected (DB CHECK constraint)');
  it.todo('duplicate tag (same coach+client+tag) is rejected (UNIQUE)');
});
```

`clients-notes.spec.ts`:
```typescript
import { describe, it } from 'vitest';
describe('GET/PUT /coach/clients/:id/notes — note CRUD', () => {
  it.todo('PUT upserts note row with coach_id = auth.uid()');
  it.todo('GET returns the note for this coach+client pair');
  it.todo('PUT updates content and updated_at on second write');
  it.todo('Coach B cannot read Coach A\'s notes (RLS isolation)');
  it.todo('athlete cannot read coach notes (no athlete-read policy)');
});
```

`clients-compare.spec.ts`:
```typescript
import { describe, it } from 'vitest';
describe('GET /coach/clients/compare — multi-client comparison data', () => {
  it.todo('returns time-series data for requested client IDs (body weight)');
  it.todo('returns aggregate data for sessions metric');
  it.todo('does not include data for unlinked client IDs');
  it.todo('metric=sleep returns sleep_logs duration data');
  it.todo('metric=mood returns journal_entries mood_score data');
});
```

`clients-revoke-coach.spec.ts`:
```typescript
import { describe, it } from 'vitest';
describe('DELETE /coach/clients/links/:clientId — coach-side revoke', () => {
  it.todo('sets revoked_at on the link row when called by the coach');
  it.todo('coach cannot read client data after revocation (RLS blocks)');
  it.todo('athlete-side revoke route still works independently');
  it.todo('revoke by non-coach (wrong auth) returns error');
});
```
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk vitest run --reporter=verbose backend/api/test/coach/clients-roster.spec.ts backend/api/test/coach/clients-tags.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 8 test stub files exist under `backend/api/test/coach/`
    - `rtk vitest run` exits 0 (--passWithNoTests is already configured per Phase 22 D-01)
    - `packages/coach-sdk/src/schemas/index.ts` contains exports for `ClientSummarySchema`, `CoachClientTagSchema`, `CoachClientNoteSchema`
    - `packages/coach-sdk/src/schemas/client-summary.ts` exports `ClientSummarySchema` with fields: sessions_this_week, habits_pct, last_workout_at, latest_weight_kg, mood_delta, mood_prev_avg, mood_curr_avg
    - `packages/coach-sdk/src/schemas/client-tag.ts` exports `CoachClientTagSchema` with id, coach_id, client_id, tag, created_at
    - `packages/coach-sdk/src/schemas/client-note.ts` exports `CoachClientNoteSchema` with id, coach_id, client_id, content, updated_at
  </acceptance_criteria>
  <done>vitest exits 0; 3 schemas importable from @ziko/coach-sdk</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → apps/web/node_modules | Third-party package download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-00-01 | Tampering | npm package supply chain | accept | @tanstack/react-table and recharts are Apache-2.0/MIT well-known packages; pinned to exact version; npm lockfile captures integrity hash |
</threat_model>

<verification>
- `rtk grep "@tanstack/react-table" apps/web/package.json` → matches `8.21.3`
- `rtk grep "recharts" apps/web/package.json` → matches `3.8.1`
- `rtk vitest run` exits 0 from `backend/api/`
- All 8 stub files exist: `ls backend/api/test/coach/clients-*.spec.ts | wc -l` → 14 (6 old + 8 new)
</verification>

<success_criteria>
- @tanstack/react-table@8.21.3 and recharts@3.8.1 are installed in apps/web
- 8 new test stubs exist and the full vitest suite exits 0
- 3 new Zod schemas exported from @ziko/coach-sdk
</success_criteria>

<output>
After completion, create `.planning/phases/26-crm-client-management/26-01-SUMMARY.md`
</output>
