---
phase: 35-profile-settings-redesign
plan: 08
type: execute
wave: 4
depends_on: []
files_modified:
  - apps/mobile/app/(app)/profile/settings.tsx
autonomous: true
requirements: [SET-02, SET-03, SET-04, SET-05]

must_haves:
  truths:
    - "NotifSubScreen charge les préférences depuis la DB au mount (pas false par défaut)"
    - "AppearanceSubScreen pré-sélectionne le thème actif visuellement"
    - "Les boutons 'Connecter' des intégrations déclenchent une vraie action (pas juste un alert)"
    - "Le code parrainage est stable (dérivé du userId, pas aléatoire)"
    - "Le badge PREMIUM est conditionnel : FREE (gris) si null/free, PREMIUM (orange) si premium"
    - "La right value 'Intégrations' compte les plateformes réellement connectées"
  artifacts:
    - path: apps/mobile/app/(app)/profile/settings.tsx
      provides: "settings.tsx avec toutes les données hardcodées remplacées par données réelles"
  key_links:
    - from: settings.tsx NotifSubScreen
      to: user_profiles.settings.notif_prefs
      via: "useEffect + supabase select"
      pattern: "notif_prefs"
    - from: settings.tsx PREMIUM badge
      to: user_profiles.settings or subscription_tier column
      via: useQuery ['profile', userId]
      pattern: "subscription_tier\|subscription"
---

<objective>
Corriger toutes les données hardcodées dans settings.tsx : chargement réel des notif_prefs, thème actif pré-sélectionné, actions réelles pour les intégrations, code parrainage stable depuis userId, badge PREMIUM conditionnel depuis la DB.

Purpose: Le smoke test a révélé que NotifSubScreen charge toujours false au mount (ignore la DB), le badge PREMIUM est hardcodé, le parrainage pointe vers une route inexistante, et les boutons "Connecter" des intégrations affichent juste un alert générique.
Output: settings.tsx mis à jour (1 fichier, modifications ciblées).
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-profile-settings-redesign/35-03-SUMMARY.md
@.planning/phases/35-profile-settings-redesign/35-UI-SPEC.md

<interfaces>
<!-- État actuel de settings.tsx (plan 03 + plan 07 modifications) -->
<!-- NotifSubScreen : useEffect charge notif_prefs depuis DB → setS — CORRECT pour notif_prefs -->
<!-- MAIS : useState initial est hardcodé (sessionsReminder: true, ...) -->
<!-- Le problème : si la DB retourne {}, les défauts codés en dur persistent -->
<!-- Solution : initialiser l'état avec {} et ne pré-remplir qu'après le chargement DB -->

<!-- THEMES dans AppearanceSubScreen -->
const THEMES = [
  { id: 'light', label: 'Clair', bg: '#F6F4EF', fg: '#1C1A17' },
  { id: 'dark',  label: 'Sombre', bg: '#1C1A17', fg: '#FFFAF6' },
  { id: 'auto',  label: 'Auto', bg: '#888', fg: '#fff' },
];
// Actuellement : const active = t.id === 'light' (hardcodé)
// Fix : const [activeTheme, setActiveTheme] = useState<string>('light')
// + useEffect pour charger depuis settings.appearance.theme

<!-- useThemeStore — @ziko/plugin-sdk -->
const theme = useThemeStore((s) => s.theme);         // theme object
const themeId = useThemeStore((s) => s.themeId);    // string id si disponible
// Sinon, charger depuis user_profiles.settings.appearance.theme

<!-- Intégrations — comportement actuel -->
// isConnected = connectedPlatforms.includes(it.id) → from health_sync_log
// Bouton "Connecter" → showAlert générique pour tous
// Fix pour Apple Health (iOS) : Linking.openURL('App-Prefs:Privacy&path=HEALTH')
// Fix pour Strava/Garmin/autres → showAlert 'Bientôt disponible'
import { Linking, Platform } from 'react-native';

<!-- right value "Intégrations" dans le groupe Préférences -->
// Actuellement : right="2 actives" hardcodé
// Fix : dériver depuis connectedPlatforms.length (déjà disponible en state)
// connectedPlatforms est dans IntegrationsSubScreen — pas accessible depuis parent
// Solution : extraire la query au niveau parent (SettingsScreen) ou utiliser un état partagé
// Approche simple : useQuery dans SettingsScreen pour health_sync_log count

<!-- Parrainage — actuellement -->
// STRow onPress → router.push('/(app)/referral' as any) → route inexistante
// Fix inline : onPress → afficher un bottom-sheet ou alert avec le code + bouton partager
// Code stable : userId.slice(0, 8).toUpperCase()
import { Share } from 'react-native';

<!-- PREMIUM badge — actuellement hardcodé -->
// user_profiles n'a pas de colonne subscription_tier dans les migrations visibles
// Utiliser settings JSONB : user_profiles.settings.subscription_tier ('free'|'premium'|null)
// profile?.subscription_tier depuis authStore si disponible, sinon query settings
// Plan : utiliser profile de authStore (profile?.settings?.subscription_tier ?? 'free')
// Si 'premium' → badge orange PREMIUM, sinon → badge gris "FREE"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Corriger NotifSubScreen, AppearanceSubScreen, PREMIUM badge</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>
  <action>
Apporter 3 corrections dans settings.tsx :

**Correction 1 — NotifSubScreen : chargement depuis DB (initialisation propre)**
L'état actuel initialise tous les toggles à true/false en dur dans useState. Cela crée un flash (valeurs par défaut visibles avant le chargement DB).
- Ajouter un state `isLoading: true` dans NotifSubScreen.
- Dans useEffect, après le chargement des notif_prefs depuis la DB, si `prefs` est trouvé → setS avec les valeurs DB (le spread existant est correct) → setIsLoading(false). Si pas de prefs (premier usage) → setIsLoading(false) avec les défauts actuels.
- Afficher un ActivityIndicator centré pendant isLoading === true à la place du contenu.
- Les valeurs par défaut en useState restent (représentent les vraies valeurs par défaut pour les nouveaux utilisateurs).

**Correction 2 — AppearanceSubScreen : thème actif pré-sélectionné**
Remplacer `const active = t.id === 'light'` (hardcodé ligne ~143) par :
- Ajouter `const [activeTheme, setActiveTheme] = useState<string>('light')` dans AppearanceSubScreen.
- Dans useEffect (qui charge déjà les settings), lire `a?.theme` et appeler setActiveTheme(a.theme ?? 'light').
- Dans le rendu des THEMES : `const isActive = t.id === activeTheme`.
- Le handleTheme (Clair seulement car Sombre/Auto sont locked) : si !isLocked → setActiveTheme(t.id) → upsert settings JSONB avec appearance.theme = t.id (merge-safe pattern comme pour units).
- Les thèmes locked gardent opacity:0.5 et pointerEvents:"none".

**Correction 3 — Badge PREMIUM conditionnel**
Dans SettingsScreen, lire `profile` depuis useAuthStore (déjà disponible ligne ~340).
Dériver la tier : `const tier = (profile as any)?.settings?.subscription_tier ?? 'free'`.
Dans le rendu du badge (lignes ~427-431) :
- Si tier === 'premium' : backgroundColor:'#FF5C1A', text:'PREMIUM'
- Sinon : backgroundColor:'rgba(28,26,23,0.12)', color:theme.text, text:'FREE'
Remplacer le View hardcodé du badge par cette logique conditionnelle.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "isLoading\|activeTheme\|subscription_tier\|tier.*premium\|FREE" apps/mobile/app/\(app\)/profile/settings.tsx</automated>
  </verify>
  <done>settings.tsx contient isLoading dans NotifSubScreen, activeTheme dans AppearanceSubScreen, et badge PREMIUM/FREE conditionnel.</done>
</task>

<task type="auto">
  <name>Task 2 : Corriger Intégrations, Parrainage, right value "Intégrations"</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>
  <action>
Apporter 3 autres corrections dans settings.tsx :

**Correction 4 — Intégrations : actions réelles**
Dans IntegrationsSubScreen, le onPress du bouton "Connecter" fait actuellement showAlert générique pour tous.
Remplacer par :
```
onPress={() => {
  if (isConnected) {
    showAlert('Intégration active', `${it.name} est connecté. Gestion avancée bientôt disponible.`);
  } else if (it.id === 'apple_health') {
    if (Platform.OS === 'ios') {
      Linking.openURL('App-Prefs:Privacy&path=HEALTH').catch(() =>
        showAlert('Impossible', "Ouvre Réglages > Confidentialité > Santé pour autoriser Ziko.")
      );
    } else {
      showAlert('Android Health Connect', 'La connexion Health Connect sera disponible dans la prochaine mise à jour.');
    }
  } else {
    showAlert(`${it.name}`, 'Cette intégration sera disponible dans une prochaine mise à jour. Reste connecté !');
  }
}}
```
Ajouter `Platform` dans les imports react-native (déjà potentiellement importé ; vérifier et ajouter si absent). Ajouter `Linking` dans les imports react-native.

**Correction 5 — right value "Intégrations" dynamique**
La STRow Intégrations dans SettingsScreen affiche `right="2 actives"` hardcodé.
Ajouter une query dans SettingsScreen (pas dans IntegrationsSubScreen) pour compter les plateformes connectées :
```ts
const { data: connectedCount = 0 } = useQuery({
  queryKey: ['integrations-count', userId],
  queryFn: async () => {
    const { data } = await supabase.from('health_sync_log')
      .select('platform').eq('user_id', userId!);
    const seen = new Set((data ?? []).map((r: any) => r.platform));
    return seen.size;
  },
  enabled: !!userId,
  staleTime: 5 * 60 * 1000,
});
```
Mettre à jour le STRow : `right={connectedCount > 0 ? `${connectedCount} active${connectedCount > 1 ? 's' : ''}` : 'Aucune'}`.

**Correction 6 — Parrainage inline (remplacer route inexistante)**
La STRow Parrainage a `onPress={() => router.push('/(app)/referral' as any)}` qui plante (route inexistante).
Remplacer par une action inline :
```ts
onPress={() => {
  const code = userId.slice(0, 8).toUpperCase();
  showAlert(
    'Ton code de parrainage',
    `Code : ${code}\n\nPartage ce code avec tes amis pour qu'ils rejoignent Ziko !`,
    [
      { text: 'Partager', onPress: () => {
        Share.share({
          message: `Rejoins-moi sur Ziko avec mon code ${code} et commence ton voyage fitness ! 💪 https://ziko-app.com`,
        });
      }},
      { text: 'Fermer', style: 'cancel' },
    ]
  );
}}
```
Ajouter `Share` dans les imports react-native.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "Linking\|connectedCount\|userId.slice.*toUpperCase\|Share.share" apps/mobile/app/\(app\)/profile/settings.tsx</automated>
  </verify>
  <done>settings.tsx contient Linking (intégrations), connectedCount (right value), Share.share (parrainage) ; TypeScript 0 erreurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → health_sync_log | Lecture des plateformes connectées de l'utilisateur |
| Mobile → Linking.openURL | Redirection vers réglages système iOS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-08-01 | Tampering | Share.share referral code | accept | Le code est userId.slice(0,8) — pas de donnée sensible exposée ; Share natif ne peut pas être falsifié |
| T-35-08-02 | Information Disclosure | userId partiel dans code parrainage | accept | 8 premiers chars du UUID non réversibles vers informations personnelles |
| T-35-08-03 | Tampering | Linking.openURL App-Prefs | accept | URL scheme iOS limité à App-Prefs — ouvre seulement les réglages système, sans écriture |
| T-35-08-04 | Spoofing | connectedCount depuis health_sync_log | mitigate | RLS users_own_profile assure que seules les lignes user_id=auth.uid() sont visibles |
</threat_model>

<verification>
```bash
# 1. NotifSubScreen isLoading
grep -c "isLoading" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 2. AppearanceSubScreen activeTheme
grep -c "activeTheme" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 3. PREMIUM badge conditionnel
grep -c "subscription_tier\|tier.*free\|FREE" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 4. Linking importé et utilisé
grep -c "Linking" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 5. connectedCount dynamique
grep -c "connectedCount\|integrations-count" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 6. Share parrainage
grep -c "Share.share\|userId.slice" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 7. TypeScript propre
cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep "error TS" | wc -l
```
</verification>

<success_criteria>
- NotifSubScreen affiche un loader avant de montrer les toggles chargés depuis la DB
- AppearanceSubScreen pré-sélectionne le thème depuis settings.appearance.theme
- Badge PREMIUM/FREE conditionnel selon settings.subscription_tier
- Bouton "Connecter" Apple Health ouvre les réglages système sur iOS
- Right value "Intégrations" affiche le vrai count depuis health_sync_log
- Parrainage affiche un code stable (userId.slice 8 chars) + Share.share
- TypeScript 0 erreurs
</success_criteria>

<output>
Créer `.planning/phases/35-profile-settings-redesign/35-08-SUMMARY.md` quand terminé.
</output>
