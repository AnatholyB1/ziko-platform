---
phase: 35-profile-settings-redesign
plan: 13
type: execute
wave: 2
depends_on: ["35-12"]
files_modified:
  - apps/mobile/app/(app)/profile/settings.tsx
autonomous: true
requirements: [PROF-02, SET-01]
gap_closure: true

must_haves:
  truths:
    - "Le STRow 'Informations personnelles' dans settings.tsx navigue vers /(app)/profile/edit"
    - "subscription_tier est lu depuis profile.subscription_tier (colonne directe) et non depuis settings JSONB"
  artifacts:
    - path: apps/mobile/app/(app)/profile/settings.tsx
      provides: "Navigation corrigée + subscription_tier depuis colonne directe"
      contains: "router.push.*profile/edit"
  key_links:
    - from: apps/mobile/app/(app)/profile/settings.tsx
      to: apps/mobile/app/(app)/profile/edit.tsx
      via: "router.push('/(app)/profile/edit' as any)"
      pattern: "router.push.*profile/edit"
---

<objective>
Corriger deux bugs dans apps/mobile/app/(app)/profile/settings.tsx :
1. Navigation "Informations personnelles" : remplacer showAlert par router.push vers edit.tsx (Decision 4).
2. subscription_tier : lire depuis la colonne directe user_profiles.subscription_tier (ajoutée par migration 051) plutôt que settings JSONB.

Purpose: Ces deux lignes sont les seuls bugs restants dans settings.tsx après plans 01–11. La migration 051 (plan 35-12) doit être appliquée avant ce plan.
Output: settings.tsx corrigé, TypeScript propre.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-profile-settings-redesign/35-CONTEXT.md
@.planning/phases/35-profile-settings-redesign/35-12-SUMMARY.md

<interfaces>
<!-- settings.tsx lignes clés à modifier — vérifiées par grep avant écriture -->

BUG 1 — ligne ~488 (navigation Informations personnelles) :
AVANT : onPress={() => showAlert('Bientôt', 'Cette section arrive dans la prochaine version.')}
APRÈS : onPress={() => router.push('/(app)/profile/edit' as any)}

BUG 2 — ligne ~387 (subscription_tier) :
AVANT : const tier = (profile as any)?.settings?.subscription_tier ?? 'free';
APRÈS : const tier = (profile as any)?.subscription_tier ?? 'free';

<!-- user_profiles après migration 051 -->
-- subscription_tier TEXT DEFAULT 'free' CHECK (IN 'free','premium','coach')
-- La valeur est maintenant une colonne directe, pas dans settings JSONB

<!-- router est déjà importé dans settings.tsx ligne 6 -->
import { router } from 'expo-router';

<!-- profile est lu depuis useAuthStore dans settings.tsx — pattern existant -->
const profile = useAuthStore((s) => s.profile);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Corriger la navigation "Informations personnelles"</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/settings.tsx (lire les lignes 480-500 pour trouver le STRow exact avant modification)
  </read_first>
  <action>
Dans apps/mobile/app/(app)/profile/settings.tsx, localiser le STRow avec label="Informations personnelles" (actuellement ligne ~488).

Remplacer son prop onPress :
- DE : `onPress={() => showAlert('Bientôt', 'Cette section arrive dans la prochaine version.')}`
- À : `onPress={() => router.push('/(app)/profile/edit' as any)}`

Ne modifier QUE cette prop onPress. Ne pas toucher icon, tint, label, sub, ni aucune autre ligne.

Vérifier que `router` est bien importé en tête de fichier (il l'est déjà — ligne 6 `import { router } from 'expo-router';`).
  </action>
  <verify>
    <automated>grep -c "router.push.*profile/edit" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx</automated>
  </verify>
  <done>Le STRow "Informations personnelles" appelle router.push('/(app)/profile/edit') et non showAlert. Grep retourne >= 1.</done>
</task>

<task type="auto">
  <name>Task 2 : Corriger subscription_tier — lire depuis colonne directe</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/settings.tsx (lire les lignes 380-400 pour confirmer la ligne exacte avant modification)
  </read_first>
  <action>
Dans apps/mobile/app/(app)/profile/settings.tsx, localiser la ligne qui lit subscription_tier (actuellement ligne ~387) :
`const tier = (profile as any)?.settings?.subscription_tier ?? 'free';`

Remplacer par :
`const tier = (profile as any)?.subscription_tier ?? 'free';`

Une seule ligne change : supprimer `.settings` du chemin de lecture.

Vérifier que la variable `tier` est utilisée dans le JSX pour afficher le plan (rechercher `tier` dans le fichier pour confirmer l'usage après la modification).

Vérifier TypeScript : la valeur reste une string, le type ne change pas.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "settings?.subscription_tier" apps/mobile/app/\(app\)/profile/settings.tsx || echo "0"</automated>
  </verify>
  <done>La variable tier est lue depuis `profile?.subscription_tier` (sans `.settings`). L'expression `settings?.subscription_tier` n'apparaît plus dans settings.tsx. TypeScript 0 erreurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → user_profiles | Lecture subscription_tier depuis colonne directe (pas JSONB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-13-01 | Spoofing | subscription_tier display | accept | Valeur affichée uniquement, pas de gate fonctionnel côté mobile en phase 35 — Stripe billing est déféré |
| T-35-13-02 | Tampering | route navigation | accept | router.push vers /(app)/profile/edit navigue vers un écran existant protégé par auth — pas de surface d'attaque nouvelle |
| T-35-13-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan |
</threat_model>

<verification>
```bash
# 1. Navigation corrigée
grep -c "router.push.*profile/edit" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 2. showAlert Bientôt supprimé
grep -c "Bientôt.*Cette section" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx || echo "0"

# 3. subscription_tier lu sans settings JSONB
grep -c "settings?.subscription_tier" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx || echo "0"

# 4. TypeScript propre
cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -v "^#" | grep -c "error TS" || echo "0"
```
</verification>

<success_criteria>
- [ ] STRow "Informations personnelles" navigue vers /(app)/profile/edit (grep retourne >= 1)
- [ ] L'ancienne onPress showAlert 'Bientôt' n'apparaît plus (grep retourne 0)
- [ ] subscription_tier lu depuis `profile?.subscription_tier` sans `.settings` intermédiaire
- [ ] TypeScript 0 erreurs
</success_criteria>

<output>
Créer `.planning/phases/35-profile-settings-redesign/35-13-SUMMARY.md` quand terminé.
</output>
