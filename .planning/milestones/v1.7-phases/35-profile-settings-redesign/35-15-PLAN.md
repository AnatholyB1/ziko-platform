---
phase: 35-profile-settings-redesign
plan: 15
type: execute
wave: 3
depends_on: ["35-13", "35-14"]
files_modified:
  - apps/mobile/app/(app)/profile/security.tsx
autonomous: true
requirements: [PROF-03, SET-01]
gap_closure: true

must_haves:
  truths:
    - "Le toggle is_public dans security.tsx lit et écrit la colonne directe user_profiles.is_public"
    - "Aucun hardcode is_public: true dans le code — la valeur vient de la DB"
    - "L'audit de profile/settings stack ne révèle plus de valeurs hardcodées critiques"
    - "Les pages non-critiques (parrainage, aide) restent en stub propre commenté"
  artifacts:
    - path: apps/mobile/app/(app)/profile/security.tsx
      provides: "is_public lu/écrit sur colonne directe user_profiles.is_public"
      contains: "is_public"
  key_links:
    - from: apps/mobile/app/(app)/profile/security.tsx
      to: supabase user_profiles.is_public
      via: "supabase.from('user_profiles').upsert({ id: userId, is_public: value })"
      pattern: "upsert.*is_public"
---

<objective>
Wirer le toggle is_public dans security.tsx sur la colonne directe user_profiles.is_public (ajoutée par migration 051). Puis auditer l'ensemble du profile/settings stack pour identifier et corriger tout hardcode critique restant. Les pages non-critiques (parrainage, aide) restent en stub commenté.

Purpose: security.tsx écrit actuellement is_public dans settings JSONB (settings.privacy.is_public). La colonne directe is_public ajoutée par migration 051 doit être la source de vérité pour la visibilité du profil côté API et communauté.
Output: security.tsx corrigé + audit complet sans hardcode critiques.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-profile-settings-redesign/35-CONTEXT.md
@.planning/phases/35-profile-settings-redesign/35-13-SUMMARY.md
@.planning/phases/35-profile-settings-redesign/35-14-SUMMARY.md

<interfaces>
<!-- security.tsx état actuel — privacy state (lignes 55-79) -->
interface PrivacyState {
  is_public: boolean;
  show_stats: boolean;
  show_activities: boolean;
}
// Charge depuis : supabase.from('user_profiles').select('settings').eq('id', userId)
// Lit : (data as any)?.settings?.privacy?.is_public ?? true
// Écrit : settings: { ...current, privacy: next }

<!-- Après migration 051 : is_public est une colonne directe -->
-- user_profiles.is_public BOOLEAN DEFAULT true
-- La colonne doit être lue et écrite directement

<!-- Cible : load is_public depuis colonne directe -->
supabase.from('user_profiles').select('settings, is_public').eq('id', userId).single()
-- Lire : prefs.is_public = (data as any)?.is_public ?? true  (colonne directe)
-- Lire show_stats, show_activities depuis settings.privacy (restent JSONB car pas de colonnes dédiées)

<!-- Cible : upsert is_public sur colonne directe -->
// Quand is_public change :
supabase.from('user_profiles').upsert({ id: userId, is_public: next.is_public })
// Le reste (show_stats, show_activities) reste dans settings JSONB via upsert settings

<!-- queryClient.invalidateQueries à appeler après upsert is_public -->
import { useQueryClient } from '@tanstack/react-query';
queryClient.invalidateQueries({ queryKey: ['profile', userId] });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Wirer is_public vers colonne directe dans security.tsx</name>
  <files>apps/mobile/app/(app)/profile/security.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/security.tsx (lire entièrement — le fichier est ~250 lignes — avant modification)
  </read_first>
  <action>
Dans apps/mobile/app/(app)/profile/security.tsx, effectuer ces modifications :

1. Ajouter import useQueryClient : la ligne `import { useQuery } from '@tanstack/react-query'` n'existe probablement pas — ajouter `import { useQueryClient } from '@tanstack/react-query';` avec les autres imports React Native.

2. Ajouter `const queryClient = useQueryClient();` dans le corps du composant SecurityScreen, après les déclarations de state existantes.

3. Modifier le useEffect de chargement de privacy (lignes ~63-80) :
   - Changer le select de `'settings'` vers `'settings, is_public'`
   - Modifier la lecture de is_public : remplacer `prefs.is_public ?? true` par `(data as any)?.is_public ?? prefs?.is_public ?? true` (priorité colonne directe, fallback settings JSONB pour migration douce)
   - show_stats et show_activities continuent de venir depuis settings.privacy (pas de colonnes dédiées)

4. Modifier la fonction updatePrivacy (lignes ~105-122) pour séparer is_public (colonne directe) des autres prefs (settings JSONB) :
   - Si key === 'is_public' : appeler un upsert dédié `supabase.from('user_profiles').upsert({ id: userId, is_public: value })` PUIS `queryClient.invalidateQueries({ queryKey: ['profile', userId] })`
   - Pour show_stats et show_activities : garder le comportement JSONB existant (settings: { ...current, privacy: next })
   - Les deux chemins passent toujours par la même fonction updatePrivacy — distinguer avec `if (key === 'is_public')` avant le clearTimeout/debounce

5. Aucune autre modification. Le reste du fichier (section mot de passe, JSX, styles) reste intact.
  </action>
  <verify>
    <automated>grep -c "upsert.*is_public" C:/ziko-platform/apps/mobile/app/\(app\)/profile/security.tsx</automated>
  </verify>
  <done>security.tsx upsert `{ id: userId, is_public: value }` sur la colonne directe quand le toggle is_public change. TypeScript 0 erreurs.</done>
</task>

<task type="auto">
  <name>Task 2 : Audit profile/settings stack — corriger hardcodes critiques restants</name>
  <files>apps/mobile/app/(app)/profile/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/index.tsx (lire les lignes 630-700 : zone où subscription_tier et is_public pourraient être hardcodés)
    - apps/mobile/app/(app)/profile/settings.tsx (lire les lignes 380-500 pour confirmer les corrections du plan 35-13 sont en place)
  </read_first>
  <action>
Effectuer un audit ciblé sur les fichiers suivants et corriger uniquement les hardcodes critiques (subscription_tier et is_public) :

FICHIERS À AUDITER :
- apps/mobile/app/(app)/profile/index.tsx
- apps/mobile/app/(app)/profile/settings.tsx (déjà corrigé par plan 35-13 — vérifier uniquement)
- apps/mobile/app/(app)/profile/security.tsx (déjà corrigé par task 1 — vérifier uniquement)

PATTERN À RECHERCHER (commandes grep d'audit) :
1. `grep "subscription_tier.*free\|'free'" profile/*.tsx` — chercher les string 'free' hardcodées comme valeur de display (acceptable : 'free' comme valeur de fallback ?? 'free')
2. `grep "is_public.*true\|is_public.*false" profile/*.tsx` — chercher les booléens hardcodés hors upsert
3. `grep "STATIC_BADGES\|staticBadge" profile/index.tsx` — vérifier que STATIC_BADGES n'est plus utilisé dans PRBadgesTab (après plan 35-14)

CORRECTIONS À APPORTER SI TROUVÉES :
- Dans index.tsx : si subscription_tier est affiché comme string hardcodée 'free' ou 'Gratuit' sans provenir de la query profil, remplacer par une lecture depuis `data?.profile?.subscription_tier ?? 'free'`. Ajouter 'subscription_tier' à la select de la query profil principale si absent.
- Dans index.tsx : si is_public est hardcodé comme booléen dans le JSX, le remplacer par `data?.profile?.is_public ?? true`.

PAGES NON-CRITIQUES (NE PAS TOUCHER) :
- apps/mobile/app/(app)/profile/referral.tsx — laisser le stub tel quel
- apps/mobile/app/(app)/profile/help.tsx — laisser le stub tel quel

Si l'audit ne trouve aucun hardcode critique résiduel dans index.tsx : documenter le résultat dans le SUMMARY comme "Audit passed — no critical hardcodes found in index.tsx" et ne faire aucune modification au fichier.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -v "^#" | grep -c "error TS" || echo "0"</automated>
  </verify>
  <done>Audit complet sur les 3 fichiers. Les hardcodes critiques subscription_tier et is_public sont lus depuis la DB (ou absents du JSX). TypeScript 0 erreurs sur l'ensemble du projet mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → user_profiles.is_public | Upsert visibilité profil par l'user authentifié — affecte visibilité publique sur la communauté |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-15-01 | Tampering | user_profiles upsert is_public | mitigate | RLS users_own_profile (migration 001) — auth.uid() = id, seul l'utilisateur modifie son profil |
| T-35-15-02 | Information Disclosure | is_public column | accept | La valeur est un préférence utilisateur volontaire — pas de donnée sensible exposée |
| T-35-15-03 | Tampering | subscription_tier display | accept | Affichage uniquement dans ce plan — pas de gate fonctionnel premium en phase 35 (Stripe différé) |
| T-35-15-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan |
</threat_model>

<verification>
```bash
# 1. is_public upsert direct dans security.tsx
grep -c "upsert.*is_public" C:/ziko-platform/apps/mobile/app/\(app\)/profile/security.tsx

# 2. is_public colonne directe dans select security.tsx
grep -c "select.*is_public\|is_public.*select" C:/ziko-platform/apps/mobile/app/\(app\)/profile/security.tsx

# 3. TypeScript propre (zéro erreurs)
cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -v "^#" | grep -c "error TS" || echo "0"

# 4. Audit subscription_tier pas hardcodé en string pure dans JSX index.tsx
grep -n "subscription_tier" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 5. Referral et help stubs non touchés
ls C:/ziko-platform/apps/mobile/app/\(app\)/profile/referral.tsx C:/ziko-platform/apps/mobile/app/\(app\)/profile/help.tsx 2>/dev/null || echo "stubs absent — ok"
```
</verification>

<success_criteria>
- [ ] security.tsx upsert is_public comme colonne directe (`{ id: userId, is_public: value }`)
- [ ] security.tsx charge is_public depuis `select('settings, is_public')` — colonne directe prioritaire
- [ ] Audit profile/settings stack passé sans hardcode critique résiduel
- [ ] Pages referral.tsx et help.tsx non touchées (stubs intentionnels)
- [ ] TypeScript 0 erreurs sur l'ensemble mobile tsconfig
</success_criteria>

<output>
Créer `.planning/phases/35-profile-settings-redesign/35-15-SUMMARY.md` quand terminé.
</output>
