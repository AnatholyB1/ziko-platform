---
phase: 35-profile-settings-redesign
plan: 09
type: execute
wave: 4
depends_on: []
files_modified:
  - apps/mobile/app/(app)/profile/settings.tsx
  - apps/mobile/app/(app)/profile/help.tsx
autonomous: true
requirements: [SET-01]

must_haves:
  truths:
    - "La row 'Noter l'app' ouvre l'App Store (iOS) ou le Play Store (Android)"
    - "La row 'Centre d'aide' navigue vers l'écran help.tsx"
    - "L'écran help.tsx contient au moins 4 groupes avec des vraies réponses en français"
    - "Chaque question dans help est dépliable (toggle local)"
  artifacts:
    - path: apps/mobile/app/(app)/profile/help.tsx
      provides: "Centre d'aide avec FAQ dépliable en 4 groupes"
      exports: [default HelpScreen]
  key_links:
    - from: apps/mobile/app/(app)/profile/settings.tsx
      to: apps/mobile/app/(app)/profile/help.tsx
      via: router.push('/(app)/profile/help')
      pattern: "router.push.*help"
    - from: settings.tsx "Noter l'app"
      to: App Store / Play Store
      via: Linking.openURL
      pattern: "Linking.openURL.*apps.apple\|play.google"
---

<objective>
Câbler la row "Noter l'app" vers les stores (Linking.openURL par plateforme), créer l'écran Centre d'aide `/(app)/profile/help` avec FAQ en 4 groupes dépliables, et câbler la row "Centre d'aide" vers ce nouvel écran.

Purpose: Ces deux rows affichent actuellement des alerts stub. "Noter l'app" doit ouvrir le store, "Centre d'aide" doit ouvrir un vrai écran de FAQ.
Output: help.tsx (nouveau), settings.tsx (2 onPress mis à jour).
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-profile-settings-redesign/35-UI-SPEC.md

<interfaces>
<!-- Bundle IDs depuis apps/mobile/app.json -->
// iOS bundleIdentifier: "com.ziko.mobile"
// Android package: "com.ziko.mobile"
// App Store ID : pas encore publié — utiliser placeholder XXXXXXXXXX (laisser un commentaire TODO)
// Play Store : https://play.google.com/store/apps/details?id=com.ziko.mobile

<!-- Linking depuis react-native -->
import { Linking, Platform } from 'react-native';
// Platform.OS === 'ios' → App Store URL
// Platform.OS === 'android' → Play Store URL
// Linking.openURL(url).catch(err => showAlert('Erreur', "Impossible d'ouvrir le store."))

<!-- STGroup, STRow depuis @ziko/ui -->
// STRow sans onPress → row non-interactive (affichage seul)
// STRow avec onPress → pressable avec highlight

<!-- Pattern dépliable (local state) -->
const [expanded, setExpanded] = useState<Set<string>>(new Set());
const toggle = (id: string) => setExpanded(prev => {
  const next = new Set(prev);
  next.has(id) ? next.delete(id) : next.add(id);
  return next;
});
// Dans le rendu : onPress={() => toggle(item.id)} + {expanded.has(item.id) && <Text>réponse</Text>}

<!-- Design tokens -->
theme.background, theme.surface, theme.border, theme.text, theme.muted
primary: '#FF5C1A'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Créer apps/mobile/app/(app)/profile/help.tsx</name>
  <files>apps/mobile/app/(app)/profile/help.tsx</files>
  <action>
Créer le Centre d'aide. Imports : React, useState ; View, Text, ScrollView, TouchableOpacity ; SafeAreaView ; router depuis expo-router ; Ionicons ; useThemeStore depuis @ziko/plugin-sdk.

Structure : SafeAreaView + STHeader maison (chevron-back + "Centre d'aide") + ScrollView.

Ne pas utiliser STGroup/STRow pour les FAQ car chaque item est dépliable et contient un texte de réponse — construire des cards custom avec le même style visuel (bg:surface, borderRadius:12, shadow md).

Pattern pour chaque groupe :
- Label de section (uppercase, muted, 12px/700) — identique à STGroup
- Card contenant les items (bg:surface, borderRadius:12, shadow md, padding:4)
- Chaque item : TouchableOpacity avec row (icon + texte + chevron) + réponse dépliable en Text dessous
- Séparateur 1px entre les items (sauf le dernier)

4 groupes avec contenu réel (pas de Lorem ipsum) :

**Groupe "Démarrage"** — 3 questions :
1. "Comment créer mon programme ?"
   Réponse : "Va dans l'onglet Séances, appuie sur 'Nouveau programme' et laisse le Coach IA générer un plan adapté à ton niveau et tes objectifs. Tu peux aussi créer un programme manuellement en ajoutant des séances jour par jour."

2. "Configurer mon profil"
   Réponse : "Dans l'onglet Profil, appuie sur 'Modifier' pour mettre à jour ton nom, ta bio et ton objectif. Renseigne ton âge, ton poids et ta taille dans les paramètres pour que le Coach IA te donne des recommandations précises."

3. "Connecter mes appareils"
   Réponse : "Va dans Paramètres > Intégrations pour connecter Apple Health, Apple Watch ou d'autres appareils. Sur iOS, autorise l'accès à la Santé dans Réglages > Confidentialité > Santé."

**Groupe "Séances"** — 3 questions :
1. "Comment enregistrer une séance ?"
   Réponse : "Appuie sur le bouton + dans l'onglet Séances. Sélectionne ou crée ton programme, puis commence la séance. Enregistre chaque série avec le poids et les répétitions. Termine avec le bouton 'Finir la séance'."

2. "Qu'est-ce que le RPE ?"
   Réponse : "Le RPE (Rate of Perceived Exertion) est une échelle de 1 à 10 qui mesure ton effort perçu. RPE 10 = effort maximal. Le plugin RPE t'aide à calculer ton 1RM estimé et tes zones d'entraînement. Utilise-le pour calibrer tes charges de manière intelligente."

3. "Synchronisation avec ma montre"
   Réponse : "Les montres Apple Watch se synchronisent via Apple Health. Assure-toi que l'intégration Apple Health est active dans Paramètres > Intégrations. Les données de santé (pas, FC, sommeil) sont importées automatiquement."

**Groupe "Abonnement"** — 3 questions :
1. "Gérer mon abonnement"
   Réponse : "Ton abonnement est géré directement par l'App Store (iOS) ou le Play Store (Android). Pour modifier ou annuler, ouvre les Réglages de ton téléphone > [Ton nom] > Abonnements (iOS) ou Play Store > Abonnements (Android)."

2. "Annuler mon abonnement"
   Réponse : "Pour annuler, va dans les Réglages de ton iPhone > [Ton nom] > Abonnements > Ziko > Annuler l'abonnement. Sur Android : Play Store > Menu > Abonnements > Ziko > Annuler. L'accès Premium reste actif jusqu'à la fin de la période payée."

3. "Politique de remboursement"
   Réponse : "Les remboursements sont traités par Apple ou Google selon leurs politiques respectives. Pour iOS, contacte le support Apple via reportaproblem.apple.com. Pour Android, contacte le support Google Play dans les 48h suivant l'achat."

**Groupe "Compte"** — 4 questions :
1. "Supprimer mon compte"
   Réponse : "Va dans Paramètres > Compte > Supprimer le compte. Tes données seront effacées dans les 30 jours suivant la suppression, conformément à notre politique de confidentialité. Cette action est irréversible."

2. "Exporter mes données"
   Réponse : "L'export de données est disponible sur la version web de Ziko (ziko-app.com). Connecte-toi et accède à la section 'Mon compte' pour télécharger une copie de tes données au format JSON."

3. "Confidentialité"
   Réponse : "Ziko respecte le RGPD. Tes données de santé ne sont jamais revendues. Tu peux contrôler la visibilité de ton profil dans Paramètres > Sécurité & Confidentialité. Pour exercer tes droits RGPD (accès, rectification, suppression), contacte contact@ziko-app.com."

4. "Contacter le support"
   Réponse : "Pour toute question, envoie un email à support@ziko.app. Délai de réponse : 48h ouvrées. Pour les bugs, utilise le bouton rapport de bug (icône dans l'interface) qui envoie automatiquement le contexte technique."

Chaque item a un id unique (ex: 'start_1', 'session_2', etc.) pour le toggle.
Le chevron de droite doit se retourner quand déplié (Ionicons 'chevron-down' / 'chevron-up').
Padding bottom 100 dans ScrollView pour clearance tab bar.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <done>help.tsx existe, compile, contient 4 groupes avec au moins 13 questions et un système de toggle dépliable.</done>
</task>

<task type="auto">
  <name>Task 2 : Câbler settings.tsx — "Centre d'aide" + "Noter l'app"</name>
  <files>apps/mobile/app/(app)/profile/settings.tsx</files>
  <action>
2 modifications dans le groupe STGroup title="Aide & infos" de settings.tsx :

**1. "Centre d'aide"** — actuellement `onPress={() => router.push('/(app)/help' as any)}`.
Corriger le chemin : `router.push('/(app)/profile/help' as any)`.

**2. "Noter l'app"** — actuellement `onPress={() => showAlert('Merci !', "...")}`.
Remplacer par :
```ts
onPress={() => {
  const url = Platform.OS === 'ios'
    ? 'https://apps.apple.com/app/id6744155867' // TODO: remplacer par l'ID App Store réel
    : 'https://play.google.com/store/apps/details?id=com.ziko.mobile';
  Linking.openURL(url).catch(() =>
    showAlert('Impossible', "Impossible d'ouvrir le store. Cherche 'Ziko fitness' manuellement.")
  );
}}
```
Vérifier que Platform et Linking sont dans les imports react-native (le plan 35-08 les a peut-être déjà ajoutés — si oui, pas besoin de les ré-importer ; vérifier et ajuster).

Ne modifier rien d'autre dans settings.tsx.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "router.push.*profile/help\|Linking.openURL.*apple\|Linking.openURL.*play.google" apps/mobile/app/\(app\)/profile/settings.tsx</automated>
  </verify>
  <done>settings.tsx contient navigation vers profile/help et Linking.openURL vers les stores ; TypeScript 0 erreurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → Linking.openURL stores | Ouverture d'une URL externe (App Store / Play Store) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09-01 | Tampering | Linking.openURL URL externe | accept | URLs hardcodées (apple.com, google.com) — pas de paramètre utilisateur, pas d'injection possible |
| T-35-09-02 | Information Disclosure | Contenu FAQ | accept | FAQ contient uniquement des informations produit publiques — aucune donnée utilisateur |
</threat_model>

<verification>
```bash
# 1. help.tsx créé
ls C:/ziko-platform/apps/mobile/app/\(app\)/profile/help.tsx

# 2. 4 groupes présents
grep -c "Démarrage\|Séances\|Abonnement\|Compte" C:/ziko-platform/apps/mobile/app/\(app\)/profile/help.tsx

# 3. Système toggle dépliable
grep -c "expanded\|toggle\|Set<" C:/ziko-platform/apps/mobile/app/\(app\)/profile/help.tsx

# 4. Navigation Centre d'aide
grep -c "profile/help" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 5. Linking App Store
grep -c "apps.apple.com\|play.google" C:/ziko-platform/apps/mobile/app/\(app\)/profile/settings.tsx

# 6. TypeScript
cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep "error TS" | wc -l
```
</verification>

<success_criteria>
- help.tsx existe avec 4 groupes (Démarrage, Séances, Abonnement, Compte) et questions dépliables
- Réponses réelles en français (pas de placeholder)
- settings.tsx "Centre d'aide" navigue vers profile/help (chemin corrigé)
- settings.tsx "Noter l'app" ouvre App Store sur iOS, Play Store sur Android via Linking.openURL
- TypeScript 0 erreurs
</success_criteria>

<output>
Créer `.planning/phases/35-profile-settings-redesign/35-09-SUMMARY.md` quand terminé.
</output>
