---
phase: 35-profile-settings-redesign
plan: 10
type: execute
wave: 5
depends_on: [35-06]
files_modified:
  - apps/mobile/app/(app)/profile/index.tsx
autonomous: true
requirements: [PROF-03]

must_haves:
  truths:
    - "Le bouton 'Ajouter' dans PRProgressTab ouvre un image picker"
    - "L'image sélectionnée est uploadée dans le bucket Supabase 'profile-photos' sous {userId}/{timestamp}.jpg"
    - "Après upload, une ligne body_measurements est insérée avec photo_url et la galerie se rafraîchit"
    - "Un long-press sur une photo existante propose 'Supprimer'"
    - "Un ActivityIndicator s'affiche sur le bouton 'Ajouter' pendant l'upload"
  artifacts:
    - path: apps/mobile/app/(app)/profile/index.tsx
      provides: "PRProgressTab avec upload photo réel + suppression"
  key_links:
    - from: PRProgressTab "Ajouter" button
      to: supabase storage bucket 'profile-photos'
      via: expo-image-picker + supabase.storage.from('profile-photos').upload
      pattern: "profile-photos.*upload\|storage.*profile"
    - from: upload success handler
      to: body_measurements table
      via: supabase.from('body_measurements').insert
      pattern: "body_measurements.*insert\|insert.*body_measurements"
---

<objective>
Implémenter l'upload de photos de progrès dans PRProgressTab de profile/index.tsx : image picker → upload bucket `profile-photos` → insert body_measurements → refresh galerie. Également : long-press sur photo existante → option supprimer (Storage + DB).

Purpose: Le bouton "Ajouter" dashed card ne fait rien (render statique). Les photos de progrès sont un critère PROF-03 du redesign.
Output: index.tsx mis à jour avec upload fonctionnel dans PRProgressTab.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-profile-settings-redesign/35-UI-SPEC.md
@.planning/phases/35-profile-settings-redesign/35-06-SUMMARY.md

<interfaces>
<!-- Bucket 'profile-photos' (migration 025) — privé, owner-only write, public read -->
-- path: {userId}/{timestamp}.jpg
-- supabase.storage.from('profile-photos').upload(`${userId}/${Date.now()}.jpg`, blob, { contentType:'image/jpeg', upsert:false })
-- supabase.storage.from('profile-photos').getPublicUrl(`${userId}/{timestamp}.jpg`)
-- supabase.storage.from('profile-photos').remove([`${userId}/{timestamp}.jpg`])
-- Attention : bucket 'profile-photos' est NON public (public:false) mais la policy "profile_photos_public_read" permet SELECT TO public
-- donc getPublicUrl fonctionne pour l'affichage

<!-- body_measurements — migration 012 -->
-- CREATE TABLE public.body_measurements (
--   id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
--   user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
--   weight_kg DECIMAL, body_fat_pct DECIMAL, waist_cm DECIMAL, chest_cm DECIMAL,
--   arm_cm DECIMAL, thigh_cm DECIMAL, hip_cm DECIMAL,
--   photo_url TEXT,
--   measured_at TIMESTAMPTZ DEFAULT NOW(),
--   created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
-- )

<!-- expo-image-picker — déjà installé -->
import * as ImagePicker from 'expo-image-picker';
// launchImageLibraryAsync({ mediaTypes:['images'], allowsEditing:true, aspect:[3,4], quality:0.8 })

<!-- TanStack Query — queryKey ['measurements', userId] — déjà défini dans index.tsx -->
// queryClient.invalidateQueries({ queryKey: ['measurements', userId] })

<!-- PRProgressTab structure actuelle (index.tsx) -->
// Rendu gallery : map sur data.gallery → View cards avec gradient de couleur
// Bouton "Ajouter" dashed card → TouchableOpacity sans onPress fonctionnel
// data.gallery provient de la query ['measurements', userId] qui charge body_measurements WHERE photo_url IS NOT NULL

<!-- Pattern upload — réutiliser la logique du plan 35-06 (avatars) -->
// fetch(uri) → blob → storage.upload → getPublicUrl → insert DB → invalidateQueries

<!-- UI-SPEC PRProgressTab -->
// Photo card : aspectRatio variable, borderRadius:16, bg:gradient ou Image
// Bouton Ajouter : border 1.5px dashed #E2E0DA, borderRadius:16, alignItems:center, justifyContent:center
// "Ajouter" text + camera-outline icon, color:muted
// Long press : showAlert avec option "Supprimer"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Implémenter upload photos + suppression dans PRProgressTab</name>
  <files>apps/mobile/app/(app)/profile/index.tsx</files>
  <action>
Modifier PRProgressTab dans index.tsx pour implémenter l'upload et la suppression.

Ajouter les imports nécessaires en tête de fichier s'ils ne sont pas déjà présents :
- `ActivityIndicator` depuis react-native
- `* as ImagePicker` depuis expo-image-picker
- `useQueryClient` depuis @tanstack/react-query

Dans le composant `PRProgressTab` (qui reçoit déjà `stats`, `prRows`, et potentiellement `gallery` en props — vérifier la signature actuelle) :

1. Ajouter `userId` dans les props de PRProgressTab (string). Le parent profile/index.tsx passe déjà le userId via useAuthStore.

2. Ajouter deux états locaux dans PRProgressTab :
   - `uploading: boolean` (false)
   - `queryClient = useQueryClient()`

3. Fonction `handleAddPhoto` :
```ts
const handleAddPhoto = async () => {
  const perm = await ImagePicker.requestMediaLibraryPermissionsAsync();
  if (!perm.granted) {
    showAlert('Permission refusée', 'Autorise l\'accès à la galerie dans les Réglages.');
    return;
  }
  const result = await ImagePicker.launchImageLibraryAsync({
    mediaTypes: ['images'],
    allowsEditing: true,
    aspect: [3, 4],
    quality: 0.8,
  });
  if (result.canceled) return;
  setUploading(true);
  try {
    const uri = result.assets[0].uri;
    const response = await fetch(uri);
    const blob = await response.blob();
    const fileName = `${userId}/${Date.now()}.jpg`;
    const { error: uploadError } = await supabase.storage
      .from('profile-photos')
      .upload(fileName, blob, { contentType: 'image/jpeg', upsert: false });
    if (uploadError) throw uploadError;
    const { data: urlData } = supabase.storage.from('profile-photos').getPublicUrl(fileName);
    const photoUrl = urlData.publicUrl;
    const { error: insertError } = await supabase.from('body_measurements').insert({
      user_id: userId,
      photo_url: photoUrl,
      measured_at: new Date().toISOString(),
    });
    if (insertError) throw insertError;
    queryClient.invalidateQueries({ queryKey: ['measurements', userId] });
  } catch (err: any) {
    showAlert('Erreur', err.message ?? 'L\'upload a échoué. Réessaie.');
  } finally {
    setUploading(false);
  }
};
```

4. Fonction `handleDeletePhoto(photoUrl: string, id: string)` :
```ts
const handleDeletePhoto = (photoUrl: string, id: string) => {
  showAlert(
    'Supprimer cette photo ?',
    'La photo sera définitivement supprimée.',
    [
      { text: 'Annuler', style: 'cancel' },
      { text: 'Supprimer', style: 'destructive', onPress: async () => {
        // Extraire le path du storage depuis l'URL publique
        // URL format: .../storage/v1/object/public/profile-photos/{userId}/{timestamp}.jpg
        const urlParts = photoUrl.split('/profile-photos/');
        if (urlParts.length > 1) {
          await supabase.storage.from('profile-photos').remove([urlParts[1]]);
        }
        await supabase.from('body_measurements').delete().eq('id', id);
        queryClient.invalidateQueries({ queryKey: ['measurements', userId] });
      }},
    ]
  );
};
```

5. Dans le rendu du bouton "Ajouter" dashed card :
- Remplacer TouchableOpacity sans onPress par `onPress={handleAddPhoto}`.
- Si `uploading` → afficher ActivityIndicator à la place de l'icône + texte.
- activeOpacity:0.7, disabled={uploading}.

6. Dans le rendu de chaque photo card du gallery :
- Envelopper dans TouchableOpacity avec `onLongPress={() => handleDeletePhoto(photo.photo_url, photo.id)}` et `delayLongPress={500}`.
- Si photo.photo_url est défini → utiliser Image source={{ uri: photo.photo_url }} style={{ width:'100%', height:'100%', borderRadius:16 }} resizeMode="cover" au lieu du gradient.
- Conserver le gradient comme fallback si photo_url est vide (cas legacy).

7. Dans useProfileData ou la query ['measurements', userId] existante dans le parent :
- Vérifier que la query sélectionne `id, photo_url, measured_at` depuis body_measurements.
- S'assurer que `id` est exposé dans l'objet gallery pour la suppression.

Passer `userId` en prop depuis le composant parent vers PRProgressTab. Trouver où PRProgressTab est instancié dans index.tsx et ajouter `userId={userId}`.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && grep -c "profile-photos\|body_measurements.*insert\|handleDeletePhoto\|handleAddPhoto" apps/mobile/app/\(app\)/profile/index.tsx</automated>
  </verify>
  <done>index.tsx contient handleAddPhoto (upload bucket profile-photos), insert body_measurements, handleDeletePhoto, et ActivityIndicator pendant l'upload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile → Storage profile-photos | Upload de photos privées de progrès |
| Mobile → body_measurements | Insert/delete de mesures avec photo_url |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-10-01 | Spoofing | Upload profile-photos | mitigate | RLS policy "profile_photos_insert" enforce (storage.foldername(name))[1] = auth.uid() — impossible d'écrire dans le dossier d'un autre utilisateur |
| T-35-10-02 | Tampering | Delete body_measurements | mitigate | RLS body_measurements_own (auth.uid() = user_id) — ne peut supprimer que ses propres lignes |
| T-35-10-03 | Information Disclosure | getPublicUrl profile-photos | accept | policy "profile_photos_public_read" intentionnellement publique — photos de progrès affichées dans le profil public |
| T-35-10-04 | Denial of Service | Upload photos multiples | mitigate | ImagePicker quality:0.8 + aspect[3,4] limite la taille ; un seul upload à la fois (uploading state gate) |
</threat_model>

<verification>
```bash
# 1. Upload pattern
grep -c "profile-photos" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 2. Insert body_measurements
grep -c "body_measurements.*insert\|insert.*body_measurements" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 3. Suppression wired
grep -c "handleDeletePhoto\|\.remove\|\.delete.*body_measurements" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 4. ActivityIndicator upload
grep -c "uploading\|ActivityIndicator" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 5. Invalidation cache measurements
grep -c "invalidateQueries.*measurements" C:/ziko-platform/apps/mobile/app/\(app\)/profile/index.tsx

# 6. TypeScript
cd C:/ziko-platform && rtk tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep "error TS" | wc -l
```
</verification>

<success_criteria>
- Bouton "Ajouter" dans PRProgressTab déclenche handleAddPhoto
- Upload vers bucket `profile-photos` dans le dossier `{userId}/`
- Insert body_measurements avec photo_url après upload réussi
- Long-press photo → confirmation + delete Storage + delete DB
- ActivityIndicator affiché pendant l'upload
- Query ['measurements', userId] invalidée après upload et après suppression
- TypeScript 0 erreurs
</success_criteria>

<output>
Créer `.planning/phases/35-profile-settings-redesign/35-10-SUMMARY.md` quand terminé.
</output>
