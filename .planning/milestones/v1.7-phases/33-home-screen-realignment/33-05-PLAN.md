---
phase: 33-home-screen-realignment
plan: 05
type: execute
wave: 3
depends_on:
  - 33-04
files_modified: []
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - HOME-07
  - HOME-08
  - HOME-09
  - HOME-10

must_haves:
  truths:
    - "No fixture constants remain in index.tsx (PROFILE, STREAK, TODAY, FORME, RECENT, ALL_PLUGINS, AI_TIPS)"
    - "No cross-plugin require() blocks remain in index.tsx"
    - "TypeScript compiles without errors across all new and modified files"
    - "FormRing import comes from @ziko/ui, not defined locally"
    - "PluginsDrawer import comes from @ziko/ui with visible prop and installedPluginIds from user_plugins"
    - "All 3 new hook files exist: useHomeData.ts, useAITips.ts, useSmartActions.ts"
  artifacts:
    - path: "apps/mobile/app/(app)/index.tsx"
      provides: "Fully wired home screen — no fixture data"
    - path: "apps/mobile/src/hooks/useHomeData.ts"
      provides: "8 TanStack Query hooks"
    - path: "apps/mobile/src/hooks/useAITips.ts"
      provides: "Rule-based tips engine"
    - path: "apps/mobile/src/hooks/useSmartActions.ts"
      provides: "Time-of-day action cards"
  key_links:
    - from: "apps/mobile/app/(app)/index.tsx"
      to: "apps/mobile/src/hooks/useHomeData.ts"
      via: "import"
      pattern: "from.*hooks/useHomeData"
    - from: "apps/mobile/app/(app)/index.tsx"
      to: "@ziko/ui"
      via: "FormRing + PluginsDrawer imports"
      pattern: "from '@ziko/ui'"
---

<objective>
Phase verification — run all automated checks and produce a definitive pass/fail report for all 10 requirements. No production file modifications.

Purpose: Confirm that all prior plans completed correctly and the home screen is fully fixture-free with real data connections wired end-to-end. Surfaces any residual issues before the phase is marked complete.

Output: Verification report with pass/fail for each HOME-01 through HOME-10 requirement. Any failures generate a gap list for follow-up.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ziko-platform/.planning/PROJECT.md
@C:/ziko-platform/.planning/workstreams/milestone-mobile/REQUIREMENTS-v1.7.md
</context>

<tasks>

<task type="checkpoint:automated">
  <name>Task 1: Automated verification — fixture purge + TypeScript + structure checks</name>
  <read_first>
    - apps/mobile/app/(app)/index.tsx (read the final file — verify state against all requirements)
    - apps/mobile/src/hooks/useHomeData.ts (verify all 8 exports present)
    - apps/mobile/src/hooks/useAITips.ts (verify exports and appStorage usage)
    - apps/mobile/src/hooks/useSmartActions.ts (verify exports)
    - packages/ui/src/components/PluginsDrawer.tsx (verify installedPluginIds prop present)
  </read_first>
  <action>
    Run the following verification commands in sequence. Report PASS or FAIL for each with the actual command output:

    1. TypeScript compile (all new files must be error-free):
       `cd C:/ziko-platform && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -E "error TS|error:" | grep -v "node_modules" | head -30`
       PASS condition: zero lines output (no TypeScript errors)

    2. Fixture purge — no PROFILE/STREAK/TODAY/FORME/RECENT/ALL_PLUGINS constants:
       `grep -v '^[[:space:]]*//' apps/mobile/app/\(app\)/index.tsx | grep -cE "const (PROFILE|STREAK|TODAY|FORME|RECENT|ALL_PLUGINS|AI_TIPS)\s*="`
       PASS condition: output is 0

    3. Cross-plugin require() purge:
       `grep -cE "require\('@ziko/plugin-(sleep|hydration|journal|measurements)'\)" apps/mobile/app/\(app\)/index.tsx`
       PASS condition: output is 0

    4. @ziko/ui FormRing imported (local FormRing function deleted):
       `grep -c "from '@ziko/ui'" apps/mobile/app/\(app\)/index.tsx`
       PASS condition: >= 1
       `grep -c "^function FormRing" apps/mobile/app/\(app\)/index.tsx`
       PASS condition: 0

    5. @ziko/ui PluginsDrawer with visible prop:
       `grep -c "visible={drawerOpen}" apps/mobile/app/\(app\)/index.tsx`
       PASS condition: >= 1
       `grep -c "open={drawerOpen}" apps/mobile/app/\(app\)/index.tsx`
       PASS condition: 0

    6. useHomeData hooks present (8 exports):
       `grep -c "^export function use" apps/mobile/src/hooks/useHomeData.ts`
       PASS condition: 8

    7. useAITips: appStorage (not MMKV) for dismiss:
       `grep -c "appStorage" apps/mobile/src/hooks/useAITips.ts`
       PASS condition: >= 2
       `grep -v '^[[:space:]]*//' apps/mobile/src/hooks/useAITips.ts | grep -c "mmkv\|MMKV"`
       PASS condition: 0

    8. useSmartActions: max 2 cards:
       `grep -c "slice(0, 2)" apps/mobile/src/hooks/useSmartActions.ts`
       PASS condition: 1

    9. FireAndForget: API endpoint wired:
       `grep -c "ai/tools/execute" apps/mobile/app/\(app\)/index.tsx`
       PASS condition: >= 1

    10. PaddingBottom: 100 for tab bar clearance:
        `grep -c "paddingBottom.*100\|paddingBottom: 100" apps/mobile/app/\(app\)/index.tsx`
        PASS condition: >= 1

    11. maybeSingle() for optional queries (not single()):
        `grep -c "maybeSingle" apps/mobile/src/hooks/useHomeData.ts`
        PASS condition: >= 2

    12. is_active boolean filter:
        `grep -c "is_active.*true" apps/mobile/src/hooks/useHomeData.ts`
        PASS condition: 1

    For each FAIL, output the exact line numbers and content that need to be fixed.
  </action>
  <verify>
    <automated>cd C:/ziko-platform && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep -E "error TS" | grep -v node_modules | head -20</automated>
  </verify>
  <done>
    All 12 checks above return their PASS conditions.
    TypeScript compiles without errors.
    Report written to SUMMARY.md with PASS/FAIL per check.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 33-01 through 33-04 delivered:
    - useHomeData.ts: 8 TanStack Query hooks for all home screen data
    - useAITips.ts: rule-based tips engine with appStorage dismiss (24h for both "Plus tard" and "J'applique")
    - useSmartActions.ts: time-of-day action cards
    - index.tsx: fully wired with FormRing (@ziko/ui), real MissionCard, HomeWeekStrip, Recent, AICoachInline, QuickLog, SmartActions, PluginsDrawer (@ziko/ui)
    - PluginsDrawer.tsx: updated with installedPluginIds prop to show plugins from user_plugins (HOME-08)
    - All fixture constants removed
  </what-built>
  <how-to-verify>
    1. Start the Expo dev server: `cd C:/ziko-platform/apps/mobile && npx expo start`
    2. Open the app on a device/simulator and navigate to the Home tab (Accueil)
    3. Verify FormeDuJour: the ring shows 4 colored segments (violet sleep, blue water, orange nutrition, green load). Center shows a score 0–100. Segments are non-zero if you have data.
    4. Verify MissionCard: if you have an active ai_generated_programs row, the dark card shows a session name and up to 3 exercises. If no program, the empty state appears with "Aucun programme actif".
    5. Verify AICoachInline: the card shows a tip that changes automatically every 6.5 seconds. "Plus tard" dismisses it for 24h. "J'applique" (on hydration tip) should log +250ml and also dismiss for 24h.
    6. Verify QuickLog: tap "+250ml" (water) → border turns blue, confirmed badge appears. Tap "Humeur" → emoji picker opens. Tap "Poids" → numeric input opens. Tap "Repas" → navigates to nutrition.
    7. Verify WeekStrip: current week shows real session dots (green = done, dark = today, transparent = rest).
    8. Verify Recent: if you have workout sessions, up to 3 appear with name, date, and volume. If none, the empty state appears.
    9. Verify PluginsDrawer: tap "Tous mes outils" → drawer slides up showing only your enabled plugins from user_plugins (not all 17 static plugins). Tap a plugin → navigates to it.
    10. Verify header: shows your first name (not "Léo" fixture), shows streak count if > 0.
    11. Check that there is no "AI_TIPS" or fixture data label visible anywhere.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification only | No new trust boundaries introduced in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
All verification is the body of Task 1. The checkpoint in Task 2 provides human confirmation of the visual result.

Final state assertion (run after Task 1 passes):

```bash
cd C:/ziko-platform && rtk git status
```

Expected: `apps/mobile/app/(app)/index.tsx` modified, `apps/mobile/src/hooks/useHomeData.ts` new, `apps/mobile/src/hooks/useAITips.ts` new, `apps/mobile/src/hooks/useSmartActions.ts` new, `packages/ui/src/components/PluginsDrawer.tsx` modified.
</verification>

<success_criteria>
All 12 automated checks in Task 1 return PASS.
Human checkpoint in Task 2 approved.
Phase 33 requirements HOME-01 through HOME-10 all verified:
- HOME-01: FormRing with real 4-source data ✓
- HOME-02: MissionCard from ai_generated_programs ✓
- HOME-03: AICoachInline rule-based tips + CTAs (24h dismiss for both buttons) ✓ — sleep/hydration rules wired; habit tip deferred to Phase 37 (unmetHabitName not wired in Phase 33)
- HOME-04: QuickLog 4 buttons with fire-and-forget ✓
- HOME-05: SmartActions time-of-day cards ✓
- HOME-06: WeekStrip from workout_sessions ✓
- HOME-07: Recent 3 sessions from workout_sessions ✓
- HOME-08: PluginsDrawer from user_plugins (is_enabled=true) via RLS ✓
- HOME-09: Header name + streak from real data ✓
- HOME-10: Zero fixture constants in index.tsx ✓
</success_criteria>

<output>
Create `.planning/phases/33-home-screen-realignment/33-05-SUMMARY.md` when done.
</output>
