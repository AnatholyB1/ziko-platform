---
phase: 23-web-turborepo-onboarding-auth-bootstrap
plan: 01
subsystem: infra
tags: [phase-23, wave-0, pre-flight, rollback-tag, workspace-cleanup, turborepo, hoisting]

# Dependency graph
requires:
  - phase: 22-schema-foundation-rls-keystone
    provides: verified RLS keystone — predecessor confirmed before any branch operation
provides:
  - Root package.json free of react-native-worklets (hoisting eliminated for Wave 1 web spike)
  - Immutable origin tag pre-web-onboarding as rollback anchor
  - 23-ROLLBACK.md with full revert procedure + D-04 dual-repo fallback activation
affects: [23-02, 23-02b, all-wave-1-spike, D-04-dual-repo-fallback]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pre-flight rollback tagging before integration spikes (tag origin BEFORE any merge)"
    - "Workspace-local dep pinning over root hoisting (RN deps confined to apps/mobile)"

key-files:
  created:
    - .planning/phases/23-web-turborepo-onboarding-auth-bootstrap/23-ROLLBACK.md
  modified:
    - package.json
    - package-lock.json

key-decisions:
  - "Remove react-native-worklets from root package.json — it is already pinned in apps/mobile/package.json. Eliminates the most likely hoisting leak path into the future apps/web bundle (RESEARCH Pitfall 2)."
  - "Tag pre-web-onboarding pushed to origin BEFORE Wave 1 spike — one-command rollback (git reset --hard pre-web-onboarding) is now wired."
  - "23-ROLLBACK.md branch reference set to gsd/phase-22-schema-foundation-rls-keystone (the ACTUAL branch) rather than the plan-template assumption gsd/phase-23-... — branching_strategy is 'none' so we stay on the existing branch."

patterns-established:
  - "Wave 0 pre-flight pattern: rollback safety net (tag + doc) MUST land before any Wave 1 integration spike."
  - "Workspace-local RN deps: react-native-* packages live exclusively in apps/mobile/package.json, never at root."

requirements-completed: [ARCH-05]

# Metrics
duration: 4min
completed: 2026-05-14
---

# Phase 23 Plan 01: Pre-flight workspace cleanup + rollback tag Summary

**Removed react-native-worklets from root package.json and tagged pre-web-onboarding on origin so Wave 1 has a one-command revert path before the c:/ziko-web subtree merge.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-14T20:52:25Z
- **Completed:** 2026-05-14T20:56:17Z
- **Tasks:** 2
- **Files modified:** 3 (package.json, package-lock.json, 23-ROLLBACK.md)

## Accomplishments
- Root `package.json` no longer pins `react-native-worklets` — `grep -c` returns 0 at root, 1 at `apps/mobile/`
- `npm install` regenerated `package-lock.json` cleanly; `npm ls react-native-worklets --workspace=apps/mobile --depth=0` resolves to `react-native-worklets@0.5.1` from the mobile workspace
- Tag `pre-web-onboarding` (sha `f02aff5e`) created locally and pushed to origin; `git ls-remote --tags origin` confirms remote presence
- `23-ROLLBACK.md` written with rollback commands, D-04 dual-repo activation procedure, and decision-recorder placeholders ready for the spike executor

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove react-native-worklets from root package.json** — `5009497` (chore)
2. **Task 2: Tag pre-web-onboarding rollback point + write rollback doc** — `2102faf` (docs)

**Plan metadata commit:** _appended at end of plan_

## Files Created/Modified
- `package.json` — Removed `"react-native-worklets": "^0.5.1"` line from `"dependencies"`; all other deps (`@chrisai/seed`, `@supabase/supabase-js`, `react`, `zod`) preserved
- `package-lock.json` — Regenerated by `npm install`; `react-native-worklets` no longer resolved at root, only from `apps/mobile` workspace
- `.planning/phases/23-web-turborepo-onboarding-auth-bootstrap/23-ROLLBACK.md` — New file documenting rollback procedure, D-04 dual-repo fallback activation, and decision-recorder placeholders for Plan 23-02 outcome

## Decisions Made
- **Branch reference in 23-ROLLBACK.md uses actual branch `gsd/phase-22-schema-foundation-rls-keystone`** rather than the plan-template assumption `gsd/phase-23-...`. Per `.planning/config.json`, `branching_strategy: "none"` — we stay on the existing branch through Phase 23. The rollback commands and the `**Branch:**` header now reflect the on-disk reality so `git push --force-with-lease origin <branch>` will not error during an emergency invocation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Branch reference correction in 23-ROLLBACK.md**
- **Found during:** Task 2 (Write 23-ROLLBACK.md)
- **Issue:** The plan's `<action>` block templated `**Branch:** gsd/phase-23-web-turborepo-onboarding-auth-bootstrap` and `git push --force-with-lease origin gsd/phase-23-web-turborepo-onboarding-auth-bootstrap`. Actual current branch is `gsd/phase-22-schema-foundation-rls-keystone` (verified by `git rev-parse --abbrev-ref HEAD`); `branching_strategy: "none"` in `.planning/config.json` means no phase-23 branch was created. If left as-templated, the rollback commands would silently fail during a real incident because the branch literally does not exist on origin.
- **Fix:** Updated both the `**Branch:**` header and the `git push --force-with-lease origin` argument in 23-ROLLBACK.md to `gsd/phase-22-schema-foundation-rls-keystone`. Context notes from the executor objective explicitly authorized this judgement call.
- **Files modified:** `.planning/phases/23-web-turborepo-onboarding-auth-bootstrap/23-ROLLBACK.md`
- **Verification:** `grep -c "gsd/phase-22-schema-foundation-rls-keystone" 23-ROLLBACK.md` returns 2 (header line + force-push command); no remaining references to the templated phase-23 branch name.
- **Committed in:** `2102faf` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Correction prevents silent rollback failure during a real Wave 1 incident. No scope creep — rollback semantics unchanged, only the branch literal updated to match disk reality.

## Issues Encountered
- npm install reported 16 vulnerabilities (9 moderate, 7 high) — pre-existing in the lockfile, not introduced by this plan. Out of scope per the deviation scope-boundary rule; logged for future hardening pass (not added to deferred-items.md because the lockfile audit cleanup is plan-23-N+ scope, not pre-flight).
- CRLF warnings on commit — Windows line-ending defaults; informational only, no behavior change.

## User Setup Required
None — this is a workspace-internal cleanup plan with no external service configuration.

## Self-Check: PASSED
- `package.json` exists, contains 0 `"react-native-worklets"` matches: FOUND
- `apps/mobile/package.json` exists, contains 1 `"react-native-worklets"` match: FOUND
- `package-lock.json` modified by `npm install`: FOUND
- `.planning/phases/23-web-turborepo-onboarding-auth-bootstrap/23-ROLLBACK.md`: FOUND
- Tag `pre-web-onboarding` exists locally: FOUND
- Tag `pre-web-onboarding` exists on origin (sha `f02aff5e`): FOUND
- Commit `5009497` (Task 1): FOUND in `git log`
- Commit `2102faf` (Task 2): FOUND in `git log`
- All 5 strings required by Task 2 acceptance criteria present in 23-ROLLBACK.md: FOUND

## Next Phase Readiness
- Wave 1 (Plan 23-02 spike) can proceed: root hoisting risk eliminated, rollback anchor pushed to origin, rollback doc committed
- No blockers for Plan 23-02
- Decision recorder in 23-ROLLBACK.md awaits the spike outcome (PASS → monorepo path; FAIL → invoke rollback + D-04 dual-repo)

---
*Phase: 23-web-turborepo-onboarding-auth-bootstrap*
*Completed: 2026-05-14*
