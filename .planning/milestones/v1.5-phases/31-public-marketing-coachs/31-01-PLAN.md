---
phase: 31-public-marketing-coachs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/public/og-coachs.png
  - apps/web/messages/fr.json
  - apps/web/messages/en.json
  - apps/web/src/app/[locale]/(marketing)/coachs/page.tsx
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-03
  - MKT-04
  - MKT-05
  - MKT-06

must_haves:
  truths:
    - "/fr/coachs and /en/coachs resolve to a rendered page with <main> wrapping 7 section components"
    - "generateStaticParams returns [{locale:'fr'},{locale:'en'}]; setRequestLocale is called before any await"
    - "generateMetadata emits og:image pointing to /og-coachs.png (1200x630)"
    - "fr.json and en.json both contain a top-level 'coachs' key with all 52 required sub-keys"
    - "og-coachs.png exists at apps/web/public/og-coachs.png"
  artifacts:
    - path: "apps/web/public/og-coachs.png"
      provides: "OG social sharing image (1200x630)"
    - path: "apps/web/messages/fr.json"
      provides: "French i18n keys for coachs.* namespace"
      contains: '"coachs"'
    - path: "apps/web/messages/en.json"
      provides: "English i18n keys for coachs.* namespace"
      contains: '"coachs"'
    - path: "apps/web/src/app/[locale]/(marketing)/coachs/page.tsx"
      provides: "SSG page shell assembling 7 components"
      exports: ["generateStaticParams", "generateMetadata", "default"]
  key_links:
    - from: "apps/web/src/app/[locale]/(marketing)/coachs/page.tsx"
      to: "apps/web/public/og-coachs.png"
      via: "generateMetadata openGraph.images[0].url"
      pattern: "og-coachs\\.png"
    - from: "apps/web/src/app/[locale]/(marketing)/coachs/page.tsx"
      to: "apps/web/messages/fr.json"
      via: "getTranslations namespace 'coachs'"
      pattern: "namespace.*coachs"
---

<objective>
Wave 1 — Blocking prerequisites for all component work.

Creates the three assets that must exist before any component can compile or any metadata can resolve:
1. `/public/og-coachs.png` — static OG image; `generateMetadata` references it; missing = broken social cards
2. `coachs.*` i18n namespace in both `fr.json` and `en.json` — components call `useTranslations('coachs')` at import time; missing = runtime crash
3. `apps/web/src/app/[locale]/(marketing)/coachs/page.tsx` — SSG shell that registers the routes `/fr/coachs` and `/en/coachs` with Next.js and assembles all 7 section components

Purpose: Wave 2 plans (31-02, 31-03) can only run after this plan completes — component files `import { useTranslations } from 'next-intl'` with namespace `'coachs'` and are imported by `page.tsx`.
Output: Routable static page skeleton + complete i18n keys + OG image asset
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-public-marketing-coachs/31-CONTEXT.md
@.planning/phases/31-public-marketing-coachs/31-UI-SPEC.md
@.planning/phases/31-public-marketing-coachs/31-RESEARCH.md

<interfaces>
<!-- Verified from apps/web/src/app/[locale]/(marketing)/page.tsx -->
<!-- Clone this shell exactly for coachs/page.tsx -->

SSG page pattern (apps/web/src/app/[locale]/(marketing)/page.tsx):
```typescript
import type { Metadata } from 'next'
import { getTranslations, setRequestLocale } from 'next-intl/server'

type Props = { params: Promise<{ locale: string }> }

export async function generateStaticParams() {
  return [{ locale: 'fr' }, { locale: 'en' }]
}

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { locale } = await params
  // use getTranslations({ locale, namespace: 'coachs' })
}

export default async function CoachsPage({ params }: Props) {
  const { locale } = await params
  setRequestLocale(locale)   // ← FIRST line after await, before any other await
  return (
    <main>...</main>
  )
}
```

Marketing layout (apps/web/src/app/[locale]/(marketing)/layout.tsx):
- Automatically wraps children with <Header /> + <Footer />
- No new layout.tsx needed in coachs/ directory

i18n file structure (apps/web/messages/fr.json — top-level keys):
- "Header", "Home", "Pricing", "Sidebar" ... add "coachs" at same level

motion.ts exports (apps/web/src/lib/motion.ts):
```typescript
export const fadeUp: Variants   // hidden: {opacity:0,y:30} / visible: {opacity:1,y:0}
export const fadeIn: Variants   // hidden: {opacity:0} / visible: {opacity:1}
export const ctaHover           // { scale: 1.05 }
export const ctaTap             // { scale: 0.97 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /public/og-coachs.png static OG image</name>
  <files>apps/web/public/og-coachs.png</files>
  <read_first>
    - apps/web/public/ — confirm og-image.png exists as the precedent for static OG PNG files (do NOT use a dynamic /api/og route)
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §SEO / Metadata Contract — OG image spec: 1200×630px, background #1C1A17, "ZIKO" white 72px font-bold centered, subtitle "La CRM coaching à l'IA" in #FF5C1A 40px below
    - .planning/phases/31-public-marketing-coachs/31-RESEARCH.md §OG Image Requirements — confirms static PNG approach
  </read_first>
  <action>
    Create a 1200×630 PNG at apps/web/public/og-coachs.png.

    Spec (from UI-SPEC §SEO / Metadata Contract):
    - Canvas size: 1200px × 630px
    - Background fill: #1C1A17 (solid dark)
    - Text line 1: "ZIKO" — white (#FFFFFF), font-weight 700, font-size 72px, horizontally centered, vertically positioned at ~220px from top
    - Text line 2: "La CRM coaching à l'IA" — color #FF5C1A (orange), font-size 40px, horizontally centered, vertically positioned at ~320px from top (approx 30px below the ZIKO baseline)

    Implementation approach: Use a Node.js script inline via the canvas API or a minimal HTML file opened in a headless browser. If neither is available, generate the PNG using sharp or jimp (check if installed in apps/web/node_modules). If no image library is available, create a minimal valid PNG programmatically using raw PNG bytes (IHDR+IDAT+IEND). The file MUST be a valid PNG binary, not an HTML file renamed.

    Simplest reliable approach: write a small Node script `scripts/gen-og-coachs.mjs` that uses the `canvas` package if available, otherwise falls back to generating a minimal placeholder PNG (even a solid dark 1200×630 background with no text is acceptable as a first pass — the metadata link must not 404). Run it with `node scripts/gen-og-coachs.mjs` and verify the output PNG exists.

    If no canvas/image library is available and the fallback minimal PNG approach is used, add a comment at the top of the script: `// TODO: replace with designed PNG before go-live (spec: #1C1A17 bg, ZIKO white 72px, subtitle orange 40px, 1200x630)`
  </action>
  <verify>
    <automated>ls -la apps/web/public/og-coachs.png && file apps/web/public/og-coachs.png | grep PNG</automated>
  </verify>
  <done>apps/web/public/og-coachs.png exists as a valid PNG file (not empty, not HTML), minimum 1KB</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add coachs.* i18n namespace to fr.json and en.json (52 keys each)</name>
  <files>apps/web/messages/fr.json, apps/web/messages/en.json</files>
  <read_first>
    - apps/web/messages/fr.json — read the current file to find the correct insertion point (add "coachs" at top-level, alongside "Header", "Home", etc.)
    - apps/web/messages/en.json — read the current file for the same
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §i18n Keys Contract — exact 52-key structure with copy strings
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §Copywriting Contract — all FR and EN copy strings
  </read_first>
  <action>
    Add the complete "coachs" namespace to BOTH fr.json and en.json simultaneously. The key structure is identical in both files — only the string values differ.

    Add to apps/web/messages/fr.json (insert "coachs" as a new top-level key, after the last existing key before the closing brace):

    ```
    "coachs": {
      "meta": {
        "title": "Ziko — La CRM coaching à l'IA",
        "description": "Gérez vos clients fitness avec l'IA. CRM coach + app athlète intégrée. Bêta privée gratuite."
      },
      "hero": {
        "badge": "Bêta privée · 100% gratuit",
        "headline": "La CRM coaching à l'IA",
        "subtitle": "Gérez vos clients, analysez leurs données en temps réel, et générez des programmes avec l'IA. Gratuit en bêta privée — le futur ERP des coachs fitness.",
        "cta": "Rejoindre la bêta privée",
        "ctaNote": "Aucune carte bancaire requise"
      },
      "video": {
        "heading": "Découvrez Ziko en action",
        "caption": "Démo bientôt disponible"
      },
      "features": {
        "heading": "Tout ce dont un coach a besoin",
        "subheading": "Un CRM IA construit pour les professionnels du fitness.",
        "crm": {
          "title": "CRM Client intelligent",
          "body": "Consultez le roster complet de vos clients. Accédez à leurs données en lecture seule — séances, mesures corporelles, sommeil, nutrition, RPE, habitudes et cardio — tout en un seul endroit.",
          "bullet1": "Historique complet des séances et séries",
          "bullet2": "Données de sommeil, nutrition, et cardio automatiques",
          "bullet3": "Mesures corporelles et progression visuelle"
        },
        "ai": {
          "title": "Analyse IA intégrée",
          "body": "Interrogez l'IA sur n'importe quel client — analysez ses performances, générez un programme personnalisé, ou identifiez les clients à risque de décrochage.",
          "bullet1": "Analyse client en langage naturel",
          "bullet2": "Génération de programme multi-semaines",
          "bullet3": "Alertes clients à risque automatiques"
        },
        "programs": {
          "title": "Programmes de coaching",
          "body": "Créez des templates multi-semaines, assignez-les à vos clients liés, et suivez la compliance hebdomadaire en temps réel depuis votre tableau de bord.",
          "bullet1": "Templates réutilisables à travers tous vos clients",
          "bullet2": "Assignation directe depuis le CRM",
          "bullet3": "Suivi de compliance hebdomadaire"
        },
        "erp": {
          "tag": "Roadmap",
          "title": "Du CRM à l'ERP coaching",
          "body": "Ziko est né CRM IA. La suite : agenda, facturation, gestion d'entreprise. Construit pour évoluer avec votre activité de coaching.",
          "bullet1": "Agenda et planification de séances",
          "bullet2": "Facturation et gestion des paiements",
          "bullet3": "Gestion d'entreprise complète"
        }
      },
      "comparison": {
        "heading": "Pourquoi choisir Ziko ?",
        "subheading": "Une comparaison honnête.",
        "feature1Label": "Outils IA natifs",
        "feature2Label": "Profondeur des données client",
        "feature3Label": "App mobile athlète intégrée",
        "zikoFeature1": "Analyse client + génération de programme",
        "zikoFeature2": "Sommeil, nutrition, RPE, cardio, habitudes — automatiques",
        "zikoFeature3": "Complète, incluse gratuitement",
        "trainerizeFeature1": "",
        "trainerizeFeature2": "Saisie manuelle",
        "trainerizeFeature3": "Basique",
        "truecoachFeature1": "",
        "truecoachFeature2": "Saisie manuelle",
        "truecoachFeature3": "Limitée",
        "footnote": "* Données au 2026-05-22. Comparaison basée sur les fonctionnalités publiquement documentées."
      },
      "founder": {
        "heading": "Notre mission",
        "quote": "«Construit par des athlètes, pour les coachs.»",
        "story": "Nous avons vécu le manque d'outils adaptés — à la fois comme athlètes et comme coachs. Ziko est la plateforme que nous aurions voulu avoir.",
        "name": "Fondateur, Ziko",
        "title": "Athlète & entrepreneur"
      },
      "faq": {
        "heading": "Questions fréquentes",
        "q1": "Quelles données mes clients doivent-ils partager ?",
        "a1": "Vos clients téléchargent l'app Ziko gratuite et créent un compte. En acceptant votre invitation, vous accédez à leurs données en lecture seule : séances, mesures, sommeil, nutrition, RPE, habitudes et cardio. Aucun partage manuel requis — tout se synchronise automatiquement.",
        "q2": "Est-ce vraiment gratuit ?",
        "a2": "Oui. Ziko est 100% gratuit pendant la bêta privée. Aucune carte bancaire requise. Nous vous préviendrons avant tout changement de modèle tarifaire.",
        "q3": "Sur quelles plateformes fonctionne l'app athlète ?",
        "a3": "L'app Ziko pour athlètes est disponible sur iOS (iPhone) et Android. Le CRM coach est une application web qui fonctionne sur tout navigateur moderne.",
        "q4": "Combien de clients puis-je gérer ?",
        "a4": "Sans limite pendant la bêta privée. Nous évaluerons les limites selon les retours des coachs avant le lancement public.",
        "q5": "Mes clients voient-ils mes données de coach ?",
        "a5": "Non. L'accès est unidirectionnel : vous voyez les données de vos clients liés, mais vos clients n'ont pas accès à votre espace coach. Le lien est révocable à tout moment par le client.",
        "q6": "Comment lier un client à mon espace coach ?",
        "a6": "Depuis votre tableau de bord, générez un code d'invitation à 6 caractères. Votre client le saisit dans l'app Ziko > \"Mon coach\". La liaison est instantanée."
      },
      "cta": {
        "heading": "Rejoignez les premiers coachs sur Ziko.",
        "subheading": "Bêta privée ouverte — 100% gratuit, sans engagement.",
        "button": "Rejoindre la bêta privée",
        "note": "Aucune carte bancaire requise"
      }
    }
    ```

    Add to apps/web/messages/en.json the same structure with these English values:

    meta.title: "Ziko — The AI-powered coaching CRM"
    meta.description: "Manage your fitness clients with AI. Coach CRM + integrated athlete app. Free private beta."
    hero.badge: "Private beta · 100% free"
    hero.headline: "The AI-powered coaching CRM"
    hero.subtitle: "Manage your clients, analyze their real-time data, and generate programs with AI. Free in private beta — the future ERP for fitness coaches."
    hero.cta: "Join the private beta"
    hero.ctaNote: "No credit card required"
    video.heading: "See Ziko in action"
    video.caption: "Demo coming soon"
    features.heading: "Everything a coach needs"
    features.subheading: "An AI CRM built for fitness professionals."
    features.crm.title: "Smart client CRM"
    features.crm.body: "View your full client roster. Access their read-only data — sessions, body measurements, sleep, nutrition, RPE, habits, and cardio — all in one place."
    features.crm.bullet1: "Full session and set history"
    features.crm.bullet2: "Automatic sleep, nutrition, and cardio data"
    features.crm.bullet3: "Body measurements and visual progression"
    features.ai.title: "Built-in AI analysis"
    features.ai.body: "Ask the AI about any client — analyze their performance, generate a personalized program, or identify at-risk clients before they drop off."
    features.ai.bullet1: "Natural language client analysis"
    features.ai.bullet2: "Multi-week program generation"
    features.ai.bullet3: "Automatic at-risk client alerts"
    features.programs.title: "Coaching programs"
    features.programs.body: "Build multi-week templates, assign them to linked clients, and track weekly compliance in real time from your dashboard."
    features.programs.bullet1: "Reusable templates across all clients"
    features.programs.bullet2: "Direct assignment from the CRM"
    features.programs.bullet3: "Weekly compliance tracking"
    features.erp.tag: "Roadmap"
    features.erp.title: "From CRM to coaching ERP"
    features.erp.body: "Ziko started as an AI CRM. What's coming: scheduling, billing, business management. Built to grow with your coaching business."
    features.erp.bullet1: "Scheduling and session planning"
    features.erp.bullet2: "Billing and payment management"
    features.erp.bullet3: "Full business management"
    comparison.heading: "Why choose Ziko?"
    comparison.subheading: "An honest comparison."
    comparison.feature1Label: "AI-native tools"
    comparison.feature2Label: "Client data depth"
    comparison.feature3Label: "Integrated athlete mobile app"
    comparison.zikoFeature1: "Client analysis + program generation"
    comparison.zikoFeature2: "Sleep, nutrition, RPE, cardio, habits — automatic"
    comparison.zikoFeature3: "Full-featured, included free"
    comparison.trainerizeFeature1: ""
    comparison.trainerizeFeature2: "Manual input"
    comparison.trainerizeFeature3: "Basic"
    comparison.truecoachFeature1: ""
    comparison.truecoachFeature2: "Manual input"
    comparison.truecoachFeature3: "Limited"
    comparison.footnote: "* Data as of 2026-05-22. Comparison based on publicly documented features."
    founder.heading: "Our mission"
    founder.quote: '"Built by athletes, for coaches."'
    founder.story: "We lived the lack of proper tools — both as athletes and as coaches. Ziko is the platform we wish had existed."
    founder.name: "Founder, Ziko"
    founder.title: "Athlete & entrepreneur"
    faq.heading: "Frequently asked questions"
    faq.q1: "What data do my clients need to share?"
    faq.a1: "Your clients download the free Ziko app and create an account. By accepting your invitation, you get read-only access to their data: sessions, measurements, sleep, nutrition, RPE, habits, and cardio. No manual sharing required — everything syncs automatically."
    faq.q2: "Is it really free?"
    faq.a2: "Yes. Ziko is 100% free during the private beta. No credit card required. We'll notify you before any pricing changes."
    faq.q3: "What platforms does the athlete app run on?"
    faq.a3: "The Ziko athlete app is available on iOS (iPhone) and Android. The coach CRM is a web app that works in any modern browser."
    faq.q4: "How many clients can I manage?"
    faq.a4: "Unlimited during the private beta. We'll evaluate limits based on coach feedback before the public launch."
    faq.q5: "Can my clients see my coach data?"
    faq.a5: "No. Access is one-directional: you see your linked clients' data, but your clients have no access to your coach space. The link is revocable at any time by the client."
    faq.q6: "How do I link a client to my coach space?"
    faq.a6: 'From your dashboard, generate a 6-character invitation code. Your client enters it in the Ziko app > "My coach". The link is instant.'
    cta.heading: "Join the first coaches on Ziko."
    cta.subheading: "Open private beta — 100% free, no commitment."
    cta.button: "Join the private beta"
    cta.note: "No credit card required"

    Critical: both files must be valid JSON after editing. Run `node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/fr.json','utf8'))"` and the same for en.json to confirm no parse errors.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/fr.json','utf8')); console.log('fr.json valid')" && node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/en.json','utf8')); console.log('en.json valid')" && grep -c '"coachs"' apps/web/messages/fr.json && grep -c '"coachs"' apps/web/messages/en.json</automated>
  </verify>
  <acceptance_criteria>
    - fr.json parses as valid JSON with no errors
    - en.json parses as valid JSON with no errors
    - grep '"coachs"' apps/web/messages/fr.json returns count >= 1 (top-level key present)
    - grep '"coachs"' apps/web/messages/en.json returns count >= 1 (top-level key present)
    - grep '"hero"' apps/web/messages/fr.json returns count >= 1 (nested under coachs)
    - grep 'Rejoindre la bêta privée' apps/web/messages/fr.json returns a match
    - grep 'Join the private beta' apps/web/messages/en.json returns a match
    - grep 'Bêta privée · 100% gratuit' apps/web/messages/fr.json returns a match
    - grep 'meta' apps/web/messages/fr.json — coachs.meta.title key present (needed by generateMetadata)
  </acceptance_criteria>
  <done>Both fr.json and en.json contain valid JSON with the complete "coachs" namespace (52 keys each including coachs.meta.title for generateMetadata)</done>
</task>

<task type="auto">
  <name>Task 3: Create SSG page shell apps/web/src/app/[locale]/(marketing)/coachs/page.tsx</name>
  <files>apps/web/src/app/[locale]/(marketing)/coachs/page.tsx</files>
  <read_first>
    - apps/web/src/app/[locale]/(marketing)/page.tsx — SSG shell to clone (exact pattern: type Props, generateStaticParams, generateMetadata with await params, default async function with setRequestLocale first)
    - apps/web/src/app/[locale]/(marketing)/layout.tsx — confirms no new layout.tsx needed
    - .planning/phases/31-public-marketing-coachs/31-RESEARCH.md §SSG Shell Pattern — exact code with pitfall callouts
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §Page Assembly — component order and section background alternation
  </read_first>
  <action>
    Create the directory apps/web/src/app/[locale]/(marketing)/coachs/ and the file page.tsx.

    The file must contain exactly:

    1. Imports from 'next' (Metadata type), 'next-intl/server' (getTranslations, setRequestLocale), and the 7 component imports (paths relative to src root using @/ alias):
       - @/components/marketing/CoachsHero
       - @/components/marketing/CoachsVideoPlaceholder
       - @/components/marketing/CoachsFeatureBlocks
       - @/components/marketing/CoachsComparisonTable
       - @/components/marketing/CoachsFounderSection
       - @/components/marketing/CoachsFAQ
       - @/components/marketing/CoachsCtaFooter

    2. type Props = { params: Promise<{ locale: string }> }  (MUST be Promise — Next.js 15)

    3. export async function generateStaticParams() returning [{ locale: 'fr' }, { locale: 'en' }]

    4. export async function generateMetadata({ params }: Props): Promise<Metadata> — await params first, then getTranslations({ locale, namespace: 'coachs' }), return:
       - title: { absolute: t('meta.title') }
       - description: t('meta.description')
       - alternates: { canonical: `/${locale}/coachs`, languages: { fr: '/fr/coachs', en: '/en/coachs' } }
       - openGraph: { title: t('meta.title'), description: t('meta.description'), url: `/${locale}/coachs`, siteName: 'Ziko', images: [{ url: '/og-coachs.png', width: 1200, height: 630 }], locale: locale === 'fr' ? 'fr_FR' : 'en_US', type: 'website' }

    5. export default async function CoachsPage({ params }: Props):
       - const { locale } = await params
       - setRequestLocale(locale)  ← IMMEDIATELY after await, no other await before this
       - return <main> containing the 7 components in order: CoachsHero, CoachsVideoPlaceholder, CoachsFeatureBlocks, CoachsComparisonTable, CoachsFounderSection, CoachsFAQ, CoachsCtaFooter

    Components that need the locale for CTA href: pass locale as a prop to CoachsHero and CoachsCtaFooter (they need `/${locale}/coach/onboarding` for the CTA Link). Add `locale: string` prop to both those component calls, e.g. `<CoachsHero locale={locale} />` and `<CoachsCtaFooter locale={locale} />`.

    Do NOT add `export const dynamic = 'force-dynamic'` or `export const revalidate = 0` — those are for (coach) authenticated pages only. Marketing pages are SSG.
    Do NOT create a layout.tsx in the coachs/ directory — the parent (marketing) layout already provides Header + Footer.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "coachs/page" | head -5 || echo "no type errors in coachs/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at apps/web/src/app/[locale]/(marketing)/coachs/page.tsx
    - File contains "generateStaticParams" function returning two locale objects
    - File contains "generateMetadata" using getTranslations with namespace 'coachs'
    - File contains "setRequestLocale(locale)" as first statement after await params in CoachsPage
    - File references '/og-coachs.png' in openGraph.images
    - File imports all 7 CoachsXxx components
    - File contains "type Props = { params: Promise" (Promise — not plain object)
    - No "force-dynamic" or "revalidate" exports
    - TypeScript reports no errors on this file
  </acceptance_criteria>
  <done>
    Page file compiles without TypeScript errors. Both /fr/coachs and /en/coachs routes are registered in the Next.js file-based router. generateMetadata emits correct OG image path. Components are assembled in the specified order.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static file → CDN | og-coachs.png is served as a static asset; no user input crosses this boundary |
| Next.js build → HTML output | generateMetadata runs at build time; no runtime user data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | i18n JSON files | accept | JSON files are public marketing copy; no secrets, no PII |
| T-31-02 | Tampering | og-coachs.png static asset | accept | Static CDN asset; no user input; no injection surface |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; no install tasks |
</threat_model>

<verification>
After all 3 tasks complete:

1. `node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/fr.json','utf8'))"` — exits 0
2. `node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/en.json','utf8'))"` — exits 0
3. `ls -la apps/web/public/og-coachs.png` — file exists
4. `grep -l "generateStaticParams" apps/web/src/app/\[locale\]/\(marketing\)/coachs/page.tsx` — file found
5. `grep "og-coachs" apps/web/src/app/\[locale\]/\(marketing\)/coachs/page.tsx` — returns match
</verification>

<success_criteria>
- /public/og-coachs.png exists as a valid PNG binary
- fr.json and en.json are valid JSON with complete "coachs" namespace (52 keys each, including coachs.meta.title)
- coachs/page.tsx exists with generateStaticParams(['fr','en']), generateMetadata using 'coachs' namespace, and 7 component imports
- No TypeScript errors on the new page file
- Wave 2 plans (31-02, 31-03) can now start — they depend on i18n keys existing and page.tsx importing their components
</success_criteria>

<output>
Create `.planning/phases/31-public-marketing-coachs/31-01-SUMMARY.md` when done
</output>
