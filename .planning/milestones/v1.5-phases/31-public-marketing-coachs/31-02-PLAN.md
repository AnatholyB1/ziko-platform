---
phase: 31-public-marketing-coachs
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - apps/web/src/components/marketing/CoachsHero.tsx
  - apps/web/src/components/marketing/CoachsVideoPlaceholder.tsx
  - apps/web/src/components/marketing/CoachsFeatureBlocks.tsx
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-03

must_haves:
  truths:
    - "CoachsHero renders badge 'Bêta privée · 100% gratuit' (FR) as a pill above the headline"
    - "CTA button text is 'Rejoindre la bêta privée' and href is /{locale}/coach/onboarding"
    - "Hero headline is 'La CRM coaching à l'IA' (FR) with word-stagger animation"
    - "Hero right column renders a dark CRM mock card (bg-gray-900 with skeleton rows)"
    - "CoachsVideoPlaceholder renders a video section with aspect-video gradient frame, Ziko logo, play icon, and caption 'Démo bientôt disponible' (FR)"
    - "CoachsFeatureBlocks renders 4 cards in 2x2 grid: CRM, AI, Programs, ERP(Roadmap tag)"
    - "Feature Block 4 has a 'Roadmap' tag pill with orange border in top-right corner"
    - "All copy comes from useTranslations('coachs') — zero hardcoded strings"
    - "All useInView calls use { once: true }"
  artifacts:
    - path: "apps/web/src/components/marketing/CoachsHero.tsx"
      provides: "Hero section component"
      contains: "'use client'"
    - path: "apps/web/src/components/marketing/CoachsVideoPlaceholder.tsx"
      provides: "Video placeholder section"
      contains: "'use client'"
    - path: "apps/web/src/components/marketing/CoachsFeatureBlocks.tsx"
      provides: "4-card feature grid"
      contains: "'use client'"
  key_links:
    - from: "apps/web/src/components/marketing/CoachsHero.tsx"
      to: "/{locale}/coach/onboarding"
      via: "Link href prop"
      pattern: "coach/onboarding"
    - from: "apps/web/src/components/marketing/CoachsFeatureBlocks.tsx"
      to: "apps/web/messages/fr.json"
      via: "useTranslations('coachs') t('features.erp.tag')"
      pattern: "features\\.erp"
---

<objective>
Wave 2 (parallel with 31-03) — Hero, Video, and Feature blocks.

Creates the top three sections of the /coachs marketing page:
- CoachsHero: full hero with badge, staggered headline, subtitle, primary CTA, beta note, and dark CRM mock card on the right
- CoachsVideoPlaceholder: branded video placeholder frame satisfying MKT-03 structurally (section exists in DOM; real video is deferred per D-05)
- CoachsFeatureBlocks: 2×2 grid of 4 animated feature cards with stagger-in scroll trigger

Purpose: Top-of-funnel sections that visitors see first; hero CTA drives MKT-02 (beta signup).
Output: 3 fully animated, i18n-wired 'use client' components ready for import by page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-public-marketing-coachs/31-UI-SPEC.md
@.planning/phases/31-public-marketing-coachs/31-RESEARCH.md
@.planning/phases/31-public-marketing-coachs/31-CONTEXT.md

<interfaces>
<!-- Verified from apps/web/src/lib/motion.ts -->
```typescript
import { fadeUp, fadeIn, ctaHover, ctaTap, easeOut } from '@/lib/motion'
// fadeUp: hidden={opacity:0,y:30} / visible={opacity:1,y:0,transition:easeOut}
// fadeIn: hidden={opacity:0} / visible={opacity:1,transition:{duration:0.4}}
// ctaHover: { scale: 1.05 }
// ctaTap: { scale: 0.97 }
// easeOut: { duration: 0.5, ease: [0.16, 1, 0.3, 1] }
```

<!-- Verified from apps/web/src/components/marketing/Hero.tsx -->
Word-stagger pattern (define locally in CoachsHero — not from shared lib):
```typescript
const wordVariants = {
  hidden: { opacity: 0, y: 40 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] } }
}
const containerVariants = {
  hidden: {},
  visible: { transition: { staggerChildren: 0.14 } }
}
```

useScroll + useTransform pattern (from Hero.tsx line 29-31):
```typescript
import { motion, useScroll, useTransform, useInView } from 'framer-motion'
const { scrollY } = useScroll()
const cardY = useTransform(scrollY, [0, 400], [0, -40])
```

CTA link pattern (confirmed — use next/link with explicit locale prefix):
```typescript
import Link from 'next/link'
// href={`/${locale}/coach/onboarding`}
// locale comes as a prop from CoachsPage: <CoachsHero locale={locale} />
```

Icons (react-icons/io5 — confirmed pre-installed):
```typescript
import { IoPeopleOutline, IoSparklesOutline, IoCalendarOutline, IoRocketOutline, IoPlayOutline } from 'react-icons/io5'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CoachsHero — badge, staggered headline, subtitle, CTA, CRM mock card</name>
  <files>apps/web/src/components/marketing/CoachsHero.tsx</files>
  <read_first>
    - apps/web/src/components/marketing/Hero.tsx — read fully; CoachsHero is a close relative; extract: containerVariants, wordVariants, useScroll/useTransform on the phone mock, CTA structure, badge pill
    - apps/web/src/lib/motion.ts — confirm ctaHover, ctaTap, fadeIn, fadeUp exports
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §1. Hero Section — pixel-exact spec: badge pill style, headline copy, subtitle max-width, CTA shadow, right-column dark card spec, mobile breakpoints
  </read_first>
  <action>
    Create apps/web/src/components/marketing/CoachsHero.tsx as a 'use client' component.

    Props: `interface CoachsHeroProps { locale: string }` — locale is passed from server page to construct `/${locale}/coach/onboarding`.

    Imports required:
    - 'use client'
    - motion, useScroll, useTransform from 'framer-motion'
    - useTranslations from 'next-intl'
    - Link from 'next/link'
    - ctaHover, ctaTap, fadeIn from '@/lib/motion'

    Define locally (NOT from shared lib — mirrors Hero.tsx pattern):
    ```
    const wordVariants = {
      hidden: { opacity: 0, y: 40 },
      visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] } }
    }
    const containerVariants = {
      hidden: {},
      visible: { transition: { staggerChildren: 0.14 } }
    }
    ```

    Inside component function:
    - const t = useTranslations('coachs')
    - const { scrollY } = useScroll()
    - const cardY = useTransform(scrollY, [0, 400], [0, -40])

    Section wrapper: `<section className="min-h-screen flex items-center bg-background py-20">`
    Inner container: `<div className="max-w-screen-xl mx-auto px-8 w-full">`
    Desktop layout: `<div className="grid grid-cols-1 md:grid-cols-[3fr_2fr] gap-16 items-center">`

    LEFT COLUMN content (in order):
    1. Beta pill badge:
       `<motion.div variants={fadeIn} initial="hidden" animate="visible">`
       `<span className="inline-block bg-primary text-white text-xs font-bold px-3 py-1 rounded-full">`
       Content: `{t('hero.badge')}` — renders "Bêta privée · 100% gratuit"

    2. Headline — word stagger:
       `<motion.h1 variants={containerVariants} initial="hidden" animate="visible" className="text-5xl md:text-7xl font-bold leading-none tracking-tight text-text mt-6">`
       Split `t('hero.headline')` on spaces and wrap each word in `<motion.span variants={wordVariants} className="inline-block mr-[0.25em]">`.
       Important: `t('hero.headline')` returns "La CRM coaching à l'IA" (FR) or "The AI-powered coaching CRM" (EN). Split on space, render each as a span inside a flex-wrap container.

    3. Subtitle:
       `<motion.p variants={fadeUp} initial="hidden" animate="visible" transition={{ delay: 0.6 }} className="text-base text-muted leading-relaxed mt-6 max-w-[480px]">`
       Content: `{t('hero.subtitle')}`

    4. CTA group (mt-8):
       `<motion.div variants={fadeUp} initial="hidden" animate="visible" transition={{ delay: 0.75 }} className="flex flex-col items-start gap-3">`
         `<motion.div whileHover={ctaHover} whileTap={ctaTap} className="inline-block">`
           `<Link href={`/${locale}/coach/onboarding`} className="bg-primary text-white px-6 py-3 rounded-xl font-bold text-xs inline-block" style={{ boxShadow: '0 4px 20px rgba(255,92,26,0.30)' }}>`
             `{t('hero.cta')}`
         `<p className="text-xs text-muted">{t('hero.ctaNote')}</p>`

    RIGHT COLUMN — CRM mock card (hidden below md: `<div className="hidden md:block">`):
    `<motion.div style={{ y: cardY }} initial={{ opacity: 0, x: 80 }} animate={{ opacity: 1, x: 0 }} transition={{ duration: 0.8, delay: 0.3, ease: [0.16, 1, 0.3, 1] }} className="relative" style={{ ...cardY, boxShadow: '0 32px 80px rgba(0,0,0,0.22)' }}>`

    Note: style prop can't contain both y from useTransform and a static boxShadow — handle by wrapping: outer div gets style={{ y: cardY }}, inner div gets static styles.

    Card structure:
    - Outer: `<div className="bg-gray-900 rounded-2xl border border-gray-700 overflow-hidden">`
    - Browser chrome bar: `<div className="bg-gray-800 h-9 flex items-center px-3 gap-1.5">` + three colored dots: `<div className="w-3 h-3 rounded-full bg-[#EF4444]" />`, `bg-[#F59E0B]`, `bg-[#22C55E]`
    - Orange accent bar: `<div className="h-1 bg-primary w-full" />`
    - Content area: `<div className="p-6 space-y-3">` — three skeleton client rows:
      Each row: `<div className="bg-gray-800 h-10 rounded-lg flex items-center px-3">` containing a gray pill `<div className="bg-gray-700 h-4 rounded" style={{ width: '60%' }} />` (vary widths: 60%, 45%, 75%)
      On the second row, add a small `<span className="text-primary font-bold text-xs ml-auto">92% →</span>` as the orange accent data point
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep "CoachsHero" | head -5 || echo "CoachsHero: no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with 'use client'
    - File exports function CoachsHero with prop locale: string
    - File contains t('hero.badge') — not a hardcoded string
    - File contains t('hero.cta') — not "Rejoindre la bêta privée" hardcoded
    - File contains `/\${locale}/coach/onboarding` in the Link href
    - File contains containerVariants and wordVariants defined locally
    - File contains useScroll and useTransform for the card parallax
    - File contains className="bg-primary" on the badge span
    - File contains bg-gray-900 on the CRM mock card outer div
    - File contains hidden md:block on the right column wrapper
    - No hardcoded French or English strings (all via t() calls)
    - TypeScript: no errors on this file
  </acceptance_criteria>
  <done>CoachsHero renders the hero badge, staggered headline, subtitle, CTA linked to /{locale}/coach/onboarding, and dark CRM mock card with skeleton rows. All copy via useTranslations('coachs').</done>
</task>

<task type="auto">
  <name>Task 2: Build CoachsVideoPlaceholder — gradient frame with Ziko logo and play button (MKT-03)</name>
  <files>apps/web/src/components/marketing/CoachsVideoPlaceholder.tsx</files>
  <read_first>
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §2. Demo Video Placeholder — exact spec: aspect-video, gradient bg-gray-900 to orange-950, Ziko logo 64px, play button circle, caption
    - .planning/phases/31-public-marketing-coachs/31-RESEARCH.md §MKT-03 Resolution — confirms placeholder satisfies requirement; includes TODO comment requirement
    - apps/web/src/lib/motion.ts — scaleIn variant available for the frame entrance animation
  </read_first>
  <action>
    Create apps/web/src/components/marketing/CoachsVideoPlaceholder.tsx as a 'use client' component.

    Imports:
    - 'use client'
    - { motion, useInView } from 'framer-motion'
    - { useRef } from 'react'
    - { useTranslations } from 'next-intl'
    - { IoPlayOutline } from 'react-icons/io5'

    No locale prop needed — this section has no CTA link.

    Inside component function:
    - const t = useTranslations('coachs')
    - const ref = useRef(null)
    - const inView = useInView(ref, { once: true })

    Section: `<section className="py-24 bg-white">`
    Container: `<div className="max-w-screen-xl mx-auto px-8">`

    Section heading:
    `<h2 className="text-3xl md:text-4xl font-bold text-text text-center mb-10">{t('video.heading')}</h2>`

    Video frame wrapper (add ref for inView trigger):
    `<motion.div ref={ref} initial={{ opacity: 0, scale: 0.96 }} animate={inView ? { opacity: 1, scale: 1 } : {}} transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }} className="aspect-video max-w-3xl mx-auto rounded-2xl overflow-hidden relative" style={{ boxShadow: '0 16px 40px rgba(0,0,0,0.18)', border: '1px solid rgba(255,92,26,0.30)' }}>`

    Inside frame (`className="absolute inset-0 bg-gradient-to-br from-gray-900 to-orange-950 flex flex-col items-center justify-center gap-6"`):
    1. Ziko logo: `<span className="text-white text-5xl font-bold tracking-tight">ZIKO</span>`
    2. Play button circle: `<div className="w-16 h-16 rounded-full bg-white/10 backdrop-blur-sm border border-white/20 flex items-center justify-center">` containing `<IoPlayOutline size={28} className="text-white ml-1" />`
    3. Caption: `<p className="text-xs text-gray-400">{t('video.caption')}</p>`

    Add the TODO comment above the video frame (before the motion.div):
    `{/* TODO: replace with real video when available: <video autoPlay muted loop playsInline className="w-full aspect-video rounded-2xl"><source src="/demo-coachs.mp4" type="video/mp4" /></video> */}`
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep "CoachsVideo" | head -5 || echo "CoachsVideoPlaceholder: no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with 'use client'
    - File exports function CoachsVideoPlaceholder
    - File contains t('video.heading') and t('video.caption') — no hardcoded strings
    - File contains className="aspect-video" on the frame container
    - File contains bg-gradient-to-br from-gray-900 to-orange-950 on the inner frame
    - File contains useInView with { once: true }
    - File contains the TODO comment about replacing with real video
    - File contains IoPlayOutline import from 'react-icons/io5'
    - TypeScript: no errors on this file
  </acceptance_criteria>
  <done>CoachsVideoPlaceholder renders a styled gradient frame with centered ZIKO logo, play button circle, and caption — all copy via t(). Section exists in DOM satisfying MKT-03 structurally. TODO comment marks the video swap location.</done>
</task>

<task type="auto">
  <name>Task 3: Build CoachsFeatureBlocks — 4-card animated grid with Roadmap tag on block 4</name>
  <files>apps/web/src/components/marketing/CoachsFeatureBlocks.tsx</files>
  <read_first>
    - .planning/phases/31-public-marketing-coachs/31-UI-SPEC.md §3. Feature Blocks (4 blocks) — complete spec for all 4 blocks: icons, titles, body, sub-bullets, roadmap tag visual treatment, card hover animation
    - .planning/phases/31-public-marketing-coachs/31-CONTEXT.md §D-04 — 4 blocks in order: Client CRM, AI Analysis, Coaching Programs, CRM-to-ERP
    - apps/web/src/lib/motion.ts — confirm staggerContainer available (staggerChildren: 0.1)
  </read_first>
  <action>
    Create apps/web/src/components/marketing/CoachsFeatureBlocks.tsx as a 'use client' component.

    Imports:
    - 'use client'
    - { motion, useInView } from 'framer-motion'
    - { useRef } from 'react'
    - { useTranslations } from 'next-intl'
    - { IoPeopleOutline, IoSparklesOutline, IoCalendarOutline, IoRocketOutline } from 'react-icons/io5'

    Feature config array (defined as const outside component):
    ```
    const features = [
      { key: 'crm', icon: IoPeopleOutline, hasRoadmapTag: false },
      { key: 'ai', icon: IoSparklesOutline, hasRoadmapTag: false },
      { key: 'programs', icon: IoCalendarOutline, hasRoadmapTag: false },
      { key: 'erp', icon: IoRocketOutline, hasRoadmapTag: true },
    ] as const
    ```

    Inside component function:
    - const t = useTranslations('coachs')
    - const ref = useRef(null)
    - const inView = useInView(ref, { once: true, margin: '-80px' })

    Section: `<section className="py-24 bg-background">`
    Container: `<div className="max-w-screen-xl mx-auto px-8">`

    Section heading: `<h2 className="text-3xl md:text-4xl font-bold text-text text-center mb-4">{t('features.heading')}</h2>`
    Section subheading: `<p className="text-base text-muted text-center mb-16 max-w-lg mx-auto">{t('features.subheading')}</p>`

    Grid (add ref here for inView): `<div ref={ref} className="grid grid-cols-1 md:grid-cols-2 gap-8">`

    Each card (map over features array):
    `<motion.div key={f.key} initial={{ opacity: 0, y: 30 }} animate={inView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.5, delay: index * 0.1, ease: [0.16, 1, 0.3, 1] }} whileHover={{ y: -2, boxShadow: '0 8px 24px rgba(28,26,23,0.10)' }} className="bg-white border border-border rounded-2xl p-8 relative" style={{ boxShadow: '0 4px 8px rgba(28,26,23,0.08)' }}>`

    Card inner content:
    1. Top row (flex justify-between items-start):
       - Icon circle: `<div className="w-12 h-12 rounded-full bg-primary/10 flex items-center justify-center flex-shrink-0">` + `<f.icon size={24} className="text-primary" />`
       - Roadmap tag (only if f.hasRoadmapTag): `<span className="text-[11px] font-bold px-2 py-0.5 rounded" style={{ color: '#FF5C1A', border: '1px solid #FF5C1A', backgroundColor: 'rgba(255,92,26,0.08)' }}>{t('features.erp.tag')}</span>`

    2. Title: `<h3 className="text-xl font-bold text-text mt-4">{t(`features.${f.key}.title`)}</h3>`

    3. Body: `<p className="text-base text-muted leading-relaxed mt-2">{t(`features.${f.key}.body`)}</p>`

    4. Sub-bullets (ul, mt-4):
       For f.key === 'erp': apply `className="text-sm text-muted/60 leading-relaxed"` to the li items (opacity-60 treatment for roadmap items)
       For other keys: `className="text-sm text-muted leading-relaxed"`
       Each li: `<li className="flex items-start gap-2 mt-2"><span className="text-primary mt-0.5">·</span><span>[text]</span></li>`
       Content: `{t(`features.${f.key}.bullet1`)}`, bullet2, bullet3
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep "CoachsFeature" | head -5 || echo "CoachsFeatureBlocks: no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with 'use client'
    - File exports function CoachsFeatureBlocks
    - features array has exactly 4 items (crm, ai, programs, erp) with hasRoadmapTag: true only on erp
    - File contains IoPeopleOutline, IoSparklesOutline, IoCalendarOutline, IoRocketOutline imports
    - File contains t('features.erp.tag') for the roadmap tag — not hardcoded "Roadmap"
    - File contains useInView(ref, { once: true }) — not missing the once:true
    - File contains whileHover={{ y: -2 }} on card motion.div
    - Erp bullets use opacity or text-muted/60 treatment
    - No hardcoded French or English copy strings
    - TypeScript: no errors on this file
  </acceptance_criteria>
  <done>CoachsFeatureBlocks renders a 2×2 grid of 4 animated cards. Cards stagger in on scroll via useInView. Block 4 (ERP) has the Roadmap tag pill and muted sub-bullets. All copy via useTranslations('coachs').</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n keys → rendered HTML | Translation strings from fr.json/en.json render as text content; not user-supplied input |
| locale prop → Link href | locale comes from Next.js generateStaticParams — only 'fr' or 'en' values are possible at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | locale prop in CoachsHero | accept | locale originates from generateStaticParams returning ['fr','en'] only; no user-controlled value |
| T-31-04 | Information Disclosure | i18n translation strings | accept | All strings are public marketing copy; no secrets in translation files |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase; all imports are pre-existing |
</threat_model>

<verification>
After all 3 tasks complete:

1. `ls apps/web/src/components/marketing/CoachsHero.tsx apps/web/src/components/marketing/CoachsVideoPlaceholder.tsx apps/web/src/components/marketing/CoachsFeatureBlocks.tsx` — all 3 files exist
2. `grep -l "'use client'" apps/web/src/components/marketing/CoachsHero.tsx apps/web/src/components/marketing/CoachsVideoPlaceholder.tsx apps/web/src/components/marketing/CoachsFeatureBlocks.tsx` — all 3 files returned
3. `grep "coach/onboarding" apps/web/src/components/marketing/CoachsHero.tsx` — CTA link present
4. `grep "aspect-video" apps/web/src/components/marketing/CoachsVideoPlaceholder.tsx` — frame class present
5. `grep "hasRoadmapTag" apps/web/src/components/marketing/CoachsFeatureBlocks.tsx` — roadmap config present
6. `grep -v "^//" apps/web/src/components/marketing/CoachsHero.tsx | grep -c "Rejoindre\|Join the"` — returns 0 (no hardcoded copy)
7. `cd apps/web && npx tsc --noEmit 2>&1 | tail -5` — no errors
</verification>

<success_criteria>
- CoachsHero: badge renders from t('hero.badge'), headline uses word-stagger animation, CTA links to /{locale}/coach/onboarding, CRM mock card has bg-gray-900 with skeleton rows
- CoachsVideoPlaceholder: aspect-video gradient frame present with play button and TODO comment for future video swap (MKT-03 structurally satisfied per D-05)
- CoachsFeatureBlocks: 4-card grid with stagger-in animations, ERP block has Roadmap tag, erp bullets are visually muted
- All 3 components compile without TypeScript errors
- All copy flows through useTranslations('coachs') — zero hardcoded strings
</success_criteria>

<output>
Create `.planning/phases/31-public-marketing-coachs/31-02-SUMMARY.md` when done
</output>
