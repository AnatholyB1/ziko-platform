---
phase: 27
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/api/test/coach/programs.spec.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-06
  - PROG-08

must_haves:
  truths:
    - "backend/api already has vitest installed (vitest ^3.2.4) and runnable with `npm test` — no new packages needed"
    - "test/coach/programs.spec.ts exists with it.todo() stubs for POST /coach/programs (PROG-01), POST /coach/programs/:id/assign (PROG-06), GET /coach/programs seed templates visible (PROG-08)"
    - "Stubs are it.todo() — they are skipped by vitest (not red-failing); they become the target for test bodies in Wave 2 after routes exist"
    - "`npm test` in backend/api/ exits 0 with the new stub file present (--passWithNoTests is already in the test script)"
  artifacts:
    - path: backend/api/test/coach/programs.spec.ts
      provides: stub spec file with 3 it.todo() entries mapping to PROG-01, PROG-06, PROG-08
      contains: it.todo
  key_links:
    - from: backend/api/test/coach/programs.spec.ts
      to: backend/api/src/coach/programs/service.ts
      via: will import and test programsRouter routes once Wave 2 creates them
      pattern: "programs.spec.*programsRouter"
---

<objective>
Establish the test stub file for the coaching programs API routes. Vitest is already installed and configured in backend/api — this plan only creates the programs spec file with it.todo() placeholders that document which behaviors must be tested when the routes are implemented in Wave 2.

Purpose: Provide a red-phase baseline that names the three highest-priority behaviors (PROG-01 template creation, PROG-06 fork-on-assign, PROG-08 seed visibility) before any implementation begins. Executors of 27-04 and 27-05 will implement the test bodies.
Output: backend/api/test/coach/programs.spec.ts with 3 it.todo() stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-CONTEXT.md
@.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-RESEARCH.md

<interfaces>
backend/api/vitest.config.ts already exists and includes:
- test.include: ['test/**/*.{spec,test}.ts']
- test.setupFiles: ['./test/setup.ts']
- test.environment: 'node'
- --passWithNoTests already set in package.json "test" script: "vitest run --passWithNoTests"

Existing coach test files in test/coach/ follow this pattern:
- Import from vitest: afterAll, beforeAll, describe, expect, it
- Import fixtures from '../rls/fixtures': cleanupTestUsers, createTestUser, getAdminClient
- Import DB functions from '../../src/coach/*/db.js'

backend/api/test/setup.ts already exists — no new setup file needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create programs.spec.ts with it.todo() stubs</name>
  <files>backend/api/test/coach/programs.spec.ts</files>
  <read_first>
    - backend/api/test/coach/clients-roster.spec.ts — import pattern, describe/it structure to follow exactly
    - backend/api/vitest.config.ts — confirm include glob (test/**/*.{spec,test}.ts) so the new file is picked up
    - backend/api/package.json — confirm "test" script uses --passWithNoTests (already set)
  </read_first>
  <action>
    Create backend/api/test/coach/programs.spec.ts.

    The file must import from vitest (describe, it) and contain a top-level describe block "coaching programs API" with three it.todo() entries. Each entry label must reference its requirement ID and the behavior being stubbed:

    - it.todo('PROG-01: POST /coach/programs creates a program template with is_template=TRUE and returns 201 with the new program row')
    - it.todo('PROG-06: POST /coach/programs/:id/assign forks the template per client_id with is_template=FALSE, assigned_to_user_id set, and start_date=CURRENT_DATE')
    - it.todo('PROG-08: GET /coach/programs returns at least one seed template (created_by_coach_id=NULL, is_template=TRUE) visible to an authenticated coach')

    Add a file-level comment at line 1:
    // Phase 27 plan 00 — programs route smoke tests (stubs; implement bodies in Wave 2 after 27-04 routes exist)

    Do NOT import any DB functions or fixtures yet — the routes do not exist until Wave 2. The stubs are todo-only.

    Do NOT create a new setup.ts — backend/api/test/setup.ts already exists and is referenced by vitest.config.ts.
  </action>
  <verify>
    <automated>cd /c/ziko-platform/backend/api && npm test 2>&1 | tail -15</automated>
  </verify>
  <done>backend/api/test/coach/programs.spec.ts exists; `npm test` in backend/api exits 0; output shows the new spec file is picked up (0 tests run, 3 todos skipped)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → vitest | Test infrastructure only; no external data or auth boundaries crossed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-00-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — vitest already in devDependencies at ^3.2.4 |
</threat_model>

<verification>
Verify test file exists and is picked up:

```bash
cd /c/ziko-platform/backend/api && npm test 2>&1 | tail -10
```

Expected: exits 0; output shows programs.spec.ts in the run (0 pass, 0 fail, 3 todo or similar).

Verify it.todo stubs present:

```bash
grep -c "it.todo" /c/ziko-platform/backend/api/test/coach/programs.spec.ts
```

Expected: 3
</verification>

<success_criteria>
- backend/api/test/coach/programs.spec.ts exists in test/coach/ (matching vitest include glob)
- File contains exactly 3 it.todo() entries covering PROG-01, PROG-06, PROG-08
- No new packages added to backend/api/package.json (vitest already installed)
- `npm test` in backend/api exits 0
</success_criteria>

<output>
Create `.planning/phases/27-coaching-programs-mobile-mon-coach-full/27-00-SUMMARY.md` when done
</output>
