---
phase: 28-ai-file-imports
plan: 03
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - backend/api/src/coach/imports/types.ts
  - backend/api/src/coach/imports/db.ts
autonomous: true
requirements:
  - IMPORT-01
  - IMPORT-04
  - IMPORT-05
  - IMPORT-06
  - IMPORT-09
  - IMPORT-10

must_haves:
  truths:
    - "types.ts declares all TypeScript interfaces for the imports module"
    - "db.ts exports all 6 Supabase query functions used by the route layer"
    - "All queries use a JWT-scoped Supabase client (not service key)"
    - "Status machine transitions are enforced in db.ts update functions"
  artifacts:
    - path: "backend/api/src/coach/imports/types.ts"
      provides: "TypeScript interfaces: ImportRow, CreateImportBody, CommitImportBody"
      exports: ["ImportRow", "CreateImportBody", "CommitImportBody", "ImportStatus"]
    - path: "backend/api/src/coach/imports/db.ts"
      provides: "Supabase query functions for ai_imports table"
      exports: ["createImport", "getImport", "listImports", "updateImportStatus", "updateImportParsed", "commitImport"]
  key_links:
    - from: "backend/api/src/coach/imports/db.ts"
      to: "public.ai_imports"
      via: "JWT-scoped supabase.from('ai_imports')"
      pattern: "ai_imports"
    - from: "backend/api/src/coach/imports/db.ts"
      to: "public.workout_programs"
      via: "supabase.from('workout_programs').insert"
      pattern: "workout_programs"
---

<objective>
Build the internal contract layer for the imports bounded module: types.ts (all TypeScript interfaces) and db.ts (all Supabase query functions). The route layer (plan 04) imports exclusively from these two files.

Purpose: Interface-first ordering prevents the route executor from discovering the DB schema at implementation time. The executor reads types.ts and db.ts signatures — not the migration SQL.
Output: types.ts and db.ts with all interfaces and query functions ready for the route layer.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ai-file-imports/28-CONTEXT.md
@.planning/phases/28-ai-file-imports/28-RESEARCH.md
@supabase/migrations/036_workout_programs_ai_imports.sql
@backend/api/src/coach/programs/service.ts
</context>

<interfaces>
<!-- ai_imports table columns (from migration 036): -->
<!-- id, user_id, file_url, original_filename, mime_type, size_bytes, page_count, mode,
     status, parsed_data, confidence_scores, error_message, credit_transaction_id,
     committed_program_id, re_upload_source_id, created_at, updated_at, parsed_at, committed_at -->

<!-- status machine: 'pending' → 'uploaded' → 'parsing' → 'ready' | 'failed' → 'committed' -->

<!-- mode values: 'athlete' | 'coach_template' (DB CHECK) -->

<!-- workout_programs columns used at commit (from migration 036 line 16–24):
     created_by_coach_id UUID, assigned_to_user_id UUID, is_template BOOLEAN, weeks_data JSONB -->

From backend/api/src/coach/programs/service.ts:
- JWT pattern: `const jwt = c.req.header('Authorization')!.slice(7)` then passed to db functions
- db functions receive `jwt: string` as first param and use it to create a user-scoped client
- UUID validation: `UUID_REGEX.test(id)` pattern
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create types.ts — all TypeScript interfaces</name>
  <files>backend/api/src/coach/imports/types.ts</files>
  <read_first>
    - supabase/migrations/036_workout_programs_ai_imports.sql — full column list and CHECK constraints (all 19 columns)
    - backend/api/src/coach/programs/service.ts — types.ts import pattern (CreateProgramBody, UpdateProgramBody, etc.)
    - packages/coach-sdk/src/schemas/imported-program.ts — ImportedProgramSchema shape for parsed_data typing
    - .planning/phases/28-ai-file-imports/28-CONTEXT.md §D-18 (commit behavior — is_template, created_by_coach_id)
  </read_first>
  <action>
    Create `backend/api/src/coach/imports/types.ts` with these exported interfaces:

    `ImportStatus`: string union `'pending' | 'uploaded' | 'parsing' | 'ready' | 'failed' | 'committed'`

    `ImportMode`: string union `'athlete' | 'coach_template'`

    `ImportRow`: maps all 19 columns of ai_imports table. Use `string` for TEXT, `number` for BIGINT/INTEGER, `boolean` (not needed here), nullable fields use `T | null`. `parsed_data` typed as `Record<string, unknown> | null`, `confidence_scores` typed as `Record<string, unknown> | null`. Include `page_count: number | null`.

    `CreateImportBody`: what the client POSTs to create an import record. Fields: `filename: string`, `mime_type: string`, `size_bytes: number`, `mode: ImportMode`, `re_upload_source_id?: string | null`.

    `CommitImportBody`: what the client PUTs to commit. Fields: `parsed_data: Record<string, unknown>` (user-edited program data), `program_name?: string` (optional override for the name field).

    `UpdateStatusBody`: `{ status: 'uploaded' }` (the only valid client-triggered status update).

    All interfaces are internal (not exported from the module's public API — only db.ts and service.ts import them).
  </action>
  <verify>
    <automated>cd C:/ziko-platform/backend/api && rtk tsc --noEmit 2>&1 | grep -i "imports/types" | head -5</automated>
  </verify>
  <done>
    types.ts exists in backend/api/src/coach/imports/.
    Exports ImportStatus, ImportMode, ImportRow, CreateImportBody, CommitImportBody, UpdateStatusBody.
    TypeScript compiles without errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create db.ts — all Supabase query functions</name>
  <files>backend/api/src/coach/imports/db.ts</files>
  <read_first>
    - backend/api/src/coach/imports/types.ts (just created in Task 1)
    - backend/api/src/coach/programs/service.ts — JWT-scoped client pattern (how jwt is passed, how createClient is called with user JWT)
    - supabase/migrations/036_workout_programs_ai_imports.sql — status transitions and column names
    - .planning/phases/28-ai-file-imports/28-CONTEXT.md §D-18 (commit: is_template per mode, created_by_coach_id for coach)
    - .planning/phases/28-ai-file-imports/28-RESEARCH.md §Code Examples §ai_imports Row States Flow
  </read_first>
  <action>
    Create `backend/api/src/coach/imports/db.ts`. Import `createClient` from `'@supabase/supabase-js'`. Import types from `'./types.js'`. Use environment variables `process.env.SUPABASE_URL!` and `process.env.SUPABASE_PUBLISHABLE_KEY!` to build the user-scoped client per JWT (same pattern as programs/service.ts).

    Export these 6 functions (all internal — used only by service.ts):

    `createImport(jwt: string, userId: string, body: CreateImportBody): Promise<ImportRow>`
    - Inserts into ai_imports with status='pending', file_url set to placeholder `''` (real URL built after signed URL is created in service.ts), mode, mime_type, size_bytes, original_filename, re_upload_source_id
    - Returns the inserted row

    `getImport(jwt: string, userId: string, importId: string): Promise<ImportRow | null>`
    - Selects all columns from ai_imports where id = importId
    - RLS enforces owner-only; returns null if not found

    `listImports(jwt: string, userId: string): Promise<ImportRow[]>`
    - Selects all columns ordered by created_at DESC, limit 50
    - Returns array (empty if none)

    `updateImportStatus(jwt: string, importId: string, status: ImportStatus, extra?: Partial<Pick<ImportRow, 'error_message' | 'parsed_data' | 'confidence_scores' | 'page_count' | 'credit_transaction_id' | 'parsed_at'>>): Promise<void>`
    - Updates status + updated_at + any extra fields
    - Used for: uploaded, parsing, ready, failed transitions

    `updateImportFileUrl(jwt: string, importId: string, fileUrl: string): Promise<void>`
    - Updates file_url after Storage upload path is confirmed

    `commitImport(jwt: string, userId: string, importId: string, parsedData: Record<string, unknown>, programName: string | undefined, mode: ImportMode): Promise<{ program_id: string }>`
    - In a logical sequence (not a DB transaction — Supabase JS client doesn't support multi-statement transactions):
      1. INSERT into workout_programs: name = parsedData.name or programName, weeks_data = parsedData, is_template = (mode === 'coach_template'), created_by_coach_id = (mode === 'coach_template' ? userId : null), assigned_to_user_id = null. Set user_id = userId, weeks_count = (parsedData.weeks as any[])?.length ?? 1.
      2. UPDATE ai_imports SET committed_program_id = new_program_id, status = 'committed', committed_at = NOW()
    - Returns `{ program_id }` of the newly created program
  </action>
  <verify>
    <automated>cd C:/ziko-platform/backend/api && rtk tsc --noEmit 2>&1 | grep -i "imports/db" | head -5</automated>
  </verify>
  <done>
    db.ts exists in backend/api/src/coach/imports/.
    Exports createImport, getImport, listImports, updateImportStatus, updateImportFileUrl, commitImport.
    All functions use JWT-scoped Supabase client (not service key).
    TypeScript compiles without errors.
    commitImport sets is_template correctly based on mode per D-18.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db.ts → Supabase | All queries use user JWT (not service key) — RLS ai_imports_own enforces owner-only access |
| commitImport → workout_programs | INSERT uses userId as owner; is_template is server-determined from mode field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Information Disclosure | getImport / listImports | mitigate | JWT-scoped client enforces RLS ai_imports_own — users see only their own rows. No service key in db.ts |
| T-28-03-02 | Elevation of Privilege | commitImport is_template field | mitigate | is_template is computed server-side from mode field (stored at create time, owner-only writable via RLS). Client cannot elevate athlete mode to coach_template |
| T-28-03-03 | Tampering | commitImport INSERT into workout_programs | mitigate | created_by_coach_id is set only when mode='coach_template'; athlete commits set it null — no cross-user ownership possible |
</threat_model>

<verification>
- backend/api/src/coach/imports/types.ts exports ImportStatus, ImportMode, ImportRow, CreateImportBody, CommitImportBody
- backend/api/src/coach/imports/db.ts exports all 6 functions
- All functions accept jwt: string as first parameter (not service key pattern)
- commitImport sets is_template = (mode === 'coach_template') per D-18
- TypeScript compiles both files without errors
</verification>

<success_criteria>
types.ts and db.ts compile, export correct functions, and implement the full data access layer for the imports module. The route layer (plan 04) can import from these files immediately.
</success_criteria>

<output>
Create `.planning/phases/28-ai-file-imports/28-03-SUMMARY.md` when done.
</output>
