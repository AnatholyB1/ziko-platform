---
phase: 07-ai-recipe-suggestions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/plugin-sdk/src/i18n.ts
  - plugins/pantry/src/i18n/fr.ts
  - plugins/pantry/src/i18n/en.ts
autonomous: true
gap_closure: true
requirements:
  - RECIPE-01
  - RECIPE-02
  - RECIPE-03
  - RECIPE-04

must_haves:
  truths:
    - "t('pantry.recipes_retry_btn') resolves to 'Réessayer' in FR and 'Try again' in EN"
    - "t('pantry.recipe_detail_back') resolves to 'Retour' in FR and 'Back' in EN"
  artifacts:
    - path: "packages/plugin-sdk/src/i18n.ts"
      provides: "Central runtime i18n dictionary with both missing keys"
      contains: "pantry.recipes_retry_btn"
    - path: "plugins/pantry/src/i18n/fr.ts"
      provides: "FR reference copy with both missing keys"
      contains: "pantry.recipes_retry_btn"
    - path: "plugins/pantry/src/i18n/en.ts"
      provides: "EN reference copy with both missing keys"
      contains: "pantry.recipes_retry_btn"
  key_links:
    - from: "plugins/pantry/src/screens/PantryRecipes.tsx"
      to: "packages/plugin-sdk/src/i18n.ts"
      via: "t('pantry.recipes_retry_btn') lookup at line 302"
      pattern: "pantry\\.recipes_retry_btn"
    - from: "plugins/pantry/src/screens/RecipeDetail.tsx"
      to: "packages/plugin-sdk/src/i18n.ts"
      via: "t('pantry.recipe_detail_back') lookup at line 60"
      pattern: "pantry\\.recipe_detail_back"
---

<objective>
Close the single verification gap from Phase 07: two i18n translation keys used in PantryRecipes.tsx and RecipeDetail.tsx are missing from the central i18n dictionary and both reference copies. Without these keys, the retry button and back button display raw key strings at runtime.

Purpose: Eliminate visible UX defect where buttons show `pantry.recipes_retry_btn` and `pantry.recipe_detail_back` as literal text instead of translated labels.
Output: Three files updated with two new translation keys each (FR + EN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-recipe-suggestions/07-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing i18n keys to central dictionary and reference copies</name>
  <files>packages/plugin-sdk/src/i18n.ts, plugins/pantry/src/i18n/fr.ts, plugins/pantry/src/i18n/en.ts</files>
  <read_first>
    - packages/plugin-sdk/src/i18n.ts (central runtime dictionary — find existing pantry.recipes_retry on ~line 738 for FR insertion point, ~line 1483 for EN insertion point)
    - plugins/pantry/src/i18n/fr.ts (FR reference copy — insert after pantry.recipes_retry on line 70)
    - plugins/pantry/src/i18n/en.ts (EN reference copy — insert after pantry.recipes_retry on line 70)
    - plugins/pantry/src/screens/PantryRecipes.tsx (confirm line 302 uses t('pantry.recipes_retry_btn'))
    - plugins/pantry/src/screens/RecipeDetail.tsx (confirm line 60 uses t('pantry.recipe_detail_back'))
  </read_first>
  <action>
    Add two missing i18n keys to three files:

    **1. packages/plugin-sdk/src/i18n.ts — FR dict (~line 738, after 'pantry.recipes_retry'):**
    Add this line after the existing `'pantry.recipes_retry': 'Réessayer',` entry:
    ```
    'pantry.recipes_retry_btn': 'Réessayer',
    ```

    In the same FR dict, after `'pantry.recipe_detail_prep_time': 'Préparation',` (~line 751), add:
    ```
    'pantry.recipe_detail_back': 'Retour',
    ```

    **packages/plugin-sdk/src/i18n.ts — EN dict (~line 1483, after 'pantry.recipes_retry'):**
    Add this line after the existing `'pantry.recipes_retry': 'Try again',` entry:
    ```
    'pantry.recipes_retry_btn': 'Try again',
    ```

    In the same EN dict, after `'pantry.recipe_detail_prep_time': 'Preparation',` (~line 1496), add:
    ```
    'pantry.recipe_detail_back': 'Back',
    ```

    **2. plugins/pantry/src/i18n/fr.ts — after line 70 ('pantry.recipes_retry'):**
    Add:
    ```
    'pantry.recipes_retry_btn': 'Réessayer',
    ```
    After line 83 ('pantry.recipe_detail_prep_time'), add:
    ```
    'pantry.recipe_detail_back': 'Retour',
    ```

    **3. plugins/pantry/src/i18n/en.ts — after line 70 ('pantry.recipes_retry'):**
    Add:
    ```
    'pantry.recipes_retry_btn': 'Try again',
    ```
    After line 83 ('pantry.recipe_detail_prep_time'), add:
    ```
    'pantry.recipe_detail_back': 'Back',
    ```
  </action>
  <verify>
    <automated>rtk grep "recipes_retry_btn" packages/plugin-sdk/src/i18n.ts plugins/pantry/src/i18n/fr.ts plugins/pantry/src/i18n/en.ts && rtk grep "recipe_detail_back" packages/plugin-sdk/src/i18n.ts plugins/pantry/src/i18n/fr.ts plugins/pantry/src/i18n/en.ts && rtk npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - packages/plugin-sdk/src/i18n.ts contains `'pantry.recipes_retry_btn': 'Réessayer'` in the FR dict
    - packages/plugin-sdk/src/i18n.ts contains `'pantry.recipes_retry_btn': 'Try again'` in the EN dict
    - packages/plugin-sdk/src/i18n.ts contains `'pantry.recipe_detail_back': 'Retour'` in the FR dict
    - packages/plugin-sdk/src/i18n.ts contains `'pantry.recipe_detail_back': 'Back'` in the EN dict
    - plugins/pantry/src/i18n/fr.ts contains `'pantry.recipes_retry_btn': 'Réessayer'`
    - plugins/pantry/src/i18n/fr.ts contains `'pantry.recipe_detail_back': 'Retour'`
    - plugins/pantry/src/i18n/en.ts contains `'pantry.recipes_retry_btn': 'Try again'`
    - plugins/pantry/src/i18n/en.ts contains `'pantry.recipe_detail_back': 'Back'`
    - `npm run type-check` passes with 0 errors
  </acceptance_criteria>
  <done>Both missing i18n keys exist in all three files (central + FR ref + EN ref), and the monorepo type-checks clean. The retry button in PantryRecipes and back button in RecipeDetail will render translated text instead of raw key strings.</done>
</task>

</tasks>

<verification>
1. `grep "recipes_retry_btn" packages/plugin-sdk/src/i18n.ts` returns 2 matches (FR + EN)
2. `grep "recipe_detail_back" packages/plugin-sdk/src/i18n.ts` returns 2 matches (FR + EN)
3. `grep "recipes_retry_btn" plugins/pantry/src/i18n/fr.ts` returns 1 match
4. `grep "recipe_detail_back" plugins/pantry/src/i18n/fr.ts` returns 1 match
5. `grep "recipes_retry_btn" plugins/pantry/src/i18n/en.ts` returns 1 match
6. `grep "recipe_detail_back" plugins/pantry/src/i18n/en.ts` returns 1 match
7. `npm run type-check` exits 0
</verification>

<success_criteria>
- All 6 key insertions present across 3 files
- No type-check regressions
- Phase 07 VERIFICATION gap #15 ("All pantry.recipes_* and pantry.recipe_detail_* i18n keys resolve") is now closeable
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-recipe-suggestions/07-04-SUMMARY.md`
</output>
