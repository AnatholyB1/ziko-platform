---
phase: 11
slug: barcode-ui-score-display
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-02
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for Phase 11: Barcode UI + Score Display.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (React Native — shadcn not applicable) | CLAUDE.md |
| Preset | not applicable | CLAUDE.md |
| Component library | NativeWind v4 (inline style objects, no StyleSheet) | CLAUDE.md |
| Icon library | Ionicons (`@expo/vector-icons`) | CLAUDE.md |
| Font | System default (React Native) | codebase |

**Design principle:** No dark mode. Light sport theme only. No StyleSheet — use inline style objects or NativeWind classes.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge internal padding (small size) |
| sm | 8px | Badge internal padding (medium size), chip gap |
| md | 16px | Default element spacing, screen horizontal padding |
| lg | 24px | Section spacing |
| xl | 32px | Major layout gaps |
| 2xl | 48px | N/A for this phase |

Exceptions:
- Tab bar clearance: `paddingBottom: 100` (existing convention — all screens)
- Touch targets: minimum 44px height for all interactive elements
- Product card section padding: `padding: 16, borderRadius: 14` (matches NutritionDashboard surface card style from CONTEXT.md — borderRadius kept at 14 per existing codebase pattern)
- Macro card borderRadius: `14` (matches existing NutritionDashboard pattern)
- Header borderRadius: `20` (matches existing calorie summary card in NutritionDashboard)

---

## Typography

Exactly 4 sizes, exactly 2 weights declared.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Label | 12px | 700 (bold) | 1.4 | Badge text (all ScoreBadge size variants); chip text; entry row food name |
| Body | 14px | 400 (regular) | 1.5 | Muted labels, scan instruction text, sub-labels |
| Display | 16px | 700 (bold) | 1.3 | Product name on card; macro values; stepper TextInput grams |
| Heading | 22px | 700 (bold) | 1.2 | Screen header ("Log Meal") — existing |

Typography size map for `ScoreBadge` component (source: CONTEXT.md D-15):
- All size variants use `fontSize: 12` (Label role). Size differentiation is achieved via height and padding only — NOT different font sizes.
- `size='lg'` (product card): `fontSize: 12, height: 28, paddingHorizontal: 8`
- `size='md'` (dashboard widget): `fontSize: 12, height: 24, paddingHorizontal: 8`
- `size='sm'` (journal entry rows): `fontSize: 12, height: 20, paddingHorizontal: 4`

---

## Color

| Role | Value | Usage | Source |
|------|-------|-------|--------|
| Dominant (60%) | `#F7F6F3` | Screen background (`theme.background`) | CLAUDE.md |
| Secondary (30%) | `#FFFFFF` | Surface cards, product card, dashboard widget (`theme.surface`) | CLAUDE.md |
| Accent (10%) | `#FF5C1A` | Primary tab active state, "Log this meal" CTA button, ActivityIndicator, macro bar | CLAUDE.md |
| Destructive | `#F44336` | Delete log confirmation (existing — no new destructive actions in this phase) | codebase |
| Text primary | `#1C1A17` | All primary text (`theme.text`) | CLAUDE.md |
| Text muted | `#6B6963` | Sub-labels, serving size hint, scan instruction (`theme.muted`) | CLAUDE.md |
| Border | `#E2E0DA` | Card borders, separator (`theme.border`) | CLAUDE.md |

Accent (`#FF5C1A`) reserved for:
- Active tab highlight (pill background in tab toggle)
- "Log this meal" primary button background
- Loading `ActivityIndicator` color
- Quick-select chip selected state border

Score badge grade colors (source: CONTEXT.md D-14) — these are semantic, not accent:

| Grade | Color | Note |
|-------|-------|-------|
| A / A+ | `#1A7F37` | Dark green; 'a-plus' displays label "A+" with this color |
| B | `#78B346` | Light green |
| C | `#F5A623` | Yellow |
| D | `#E3692B` | Orange |
| E | `#CC1F24` | Red |
| null / unknown / 'not-applicable' | — | Badge not rendered (omit entirely) |

Both NutriScore and EcoScore use the same A–E color palette.

---

## Component Inventory

### New: `ScoreBadge` component

**File:** `plugins/nutrition/src/components/ScoreBadge.tsx`

Props interface:
```
grade: string | null
type: 'nutriscore' | 'ecoscore'
size: 'sm' | 'md' | 'lg'
```

Behavior:
- If `grade` is null, undefined, 'not-applicable', or any unrecognized value → renders `null` (nothing).
- If `grade === 'a-plus'` → display label "A+" with `#1A7F37` background.
- Otherwise map grade letter (a–e, case-insensitive) to color and display uppercase letter.
- Prefix label: "NS" for nutriscore, "ES" for ecoscore.
- Visual structure: `[NS A]` — pill with white text, colored background, borderRadius = height / 2.

Size values (all share `fontSize: 12` — size differentiation via height and padding only):
- `sm`: height 20, paddingHorizontal 4, fontSize 12
- `md`: height 24, paddingHorizontal 8, fontSize 12
- `lg`: height 28, paddingHorizontal 8, fontSize 12

### Modified: `LogMealScreen` — 4th tab addition

Tab toggle order (source: CONTEXT.md D-01, D-02):
1. `search` — label: i18n key `nutrition.search` — icon not required in tab label (existing)
2. `scan` — label: i18n key `nutrition.scan` — existing AI scan tab (unchanged)
3. `barcode` — label: i18n key `nutrition.barcode` — icon: `barcode-outline`
4. `custom` — label: i18n key `nutrition.custom` — existing (unchanged)

Tab pill style (matches existing pattern from codebase):
- Active: `backgroundColor: theme.primary, borderColor: theme.primary`
- Inactive: `backgroundColor: 'transparent', borderColor: theme.border`
- Text active: `color: '#fff'`
- Text inactive: `color: theme.muted`
- `fontSize: 12, fontWeight: '700', paddingVertical: 8, borderRadius: 10`

### Modified: `LogMealScreen` — Barcode tab content flow

**State 1 — Camera live (initial):**
- `CameraView` fills the tab area (flex: 1)
- Translucent overlay: semi-transparent black (`rgba(0,0,0,0.45)`) with a transparent rectangular cutout in the center
- Reticle: white border rectangle, `width: 260, height: 120, borderRadius: 10, borderWidth: 2, borderColor: '#fff'`
- Instruction text below reticle: i18n key `nutrition.barcodeAlign` — `color: '#fff', fontSize: 14, marginTop: 12, textAlign: 'center'`
- `barcodeTypes={['ean13']}` — EAN-13 only (source: CONTEXT.md D-05)
- Scanning is automatic (`onBarcodeScanned`) — no tap required (source: CONTEXT.md D-04)

**State 2 — Loading (after scan detected):**
- Camera hides (or freezes — set `scanned: true` to prevent re-fire)
- Full-width `ActivityIndicator` with `color={theme.primary}` centered in the tab area
- No text overlay during load (spinner is sufficient per CONTEXT.md discretion)

**State 3 — Product card (product found):**
- Camera hidden; product card scrolls up inline (source: CONTEXT.md D-08)
- Card: `backgroundColor: theme.surface, borderRadius: 14, padding: 16, borderWidth: 1, borderColor: theme.border`
- Layout top-to-bottom (source: CONTEXT.md D-09):
  1. Product photo: `width: '100%', height: 180, borderRadius: 10, resizeMode: 'cover'`; fallback: `Ionicons name="nutrition-outline" size={64} color={theme.muted}` centered
  2. Name (`fontSize: 16, fontWeight: '700', color: theme.text`) + Brand (`fontSize: 12, color: theme.muted`) — stacked
  3. Score badges row: `[ScoreBadge grade={nutriscore} type='nutriscore' size='lg'] [ScoreBadge grade={ecoscore} type='ecoscore' size='lg']` — `flexDirection: 'row', gap: 8, marginTop: 8`
  4. Macros grid per 100g — reuse existing 3-cell layout pattern from NutritionDashboard (`flex: 1` columns for protein/carbs/fat + energy kcal label above)
  5. Serving size adjuster (see below)
  6. Scaled macros row — `fontSize: 14, fontWeight: '700', color: theme.text`; updates in real time
  7. "Log this meal" button (primary) — full-width, `backgroundColor: theme.primary, borderRadius: 12, padding: 16, marginTop: 12`
  8. "Enter manually" button (secondary) — full-width, `backgroundColor: 'transparent', borderRadius: 12, padding: 16, borderWidth: 1, borderColor: theme.border, marginTop: 8`
  9. "Scan again" button (tertiary text) — `color: theme.muted, fontSize: 14, textAlign: 'center', paddingTop: 8`

**State 4 — Product not found:**
- Camera hidden; inline message in tab area (source: CONTEXT.md D-13):
  - Icon: `Ionicons name="barcode-outline" size={48} color={theme.muted}` centered
  - Heading: i18n key `nutrition.barcodeNotFound` — `fontSize: 16, fontWeight: '700', color: theme.text`
  - Sub-label: scanned barcode string — `fontSize: 12, color: theme.muted`
  - "Enter manually" button — primary style (only CTA here since no product data)
  - "Scan again" button — secondary/text link style

**State 5 — Permission denied:**
- Full-screen message in tab area (source: CONTEXT.md D-07):
  - Icon: `Ionicons name="camera-off-outline" size={48} color={theme.muted}` centered
  - Text: i18n key `nutrition.cameraPermDenied` — instructions to open settings
  - No buttons (user must go to OS settings)

### Serving Size Adjuster (source: CONTEXT.md D-11, D-12)

Layout (top to bottom):
1. Quick-select chips row: `[50g] [100g] [150g] [200g]`
   - Each chip: `paddingHorizontal: 12, paddingVertical: 8, borderRadius: 16, borderWidth: 1`
   - Selected: `borderColor: theme.primary, backgroundColor: theme.primary + '18'`
   - Unselected: `borderColor: theme.border, backgroundColor: theme.surface`
   - `fontSize: 12, fontWeight: '700'`
2. Stepper row: `[-5]` | `TextInput (grams)` | `[+5]`
   - Stepper buttons: `width: 40, height: 40, borderRadius: 10, backgroundColor: theme.surface, borderWidth: 1, borderColor: theme.border`
   - TextInput: `flex: 1, textAlign: 'center', fontSize: 16, fontWeight: '700', color: theme.text`
   - `keyboardType: 'numeric'`
3. Clamp: minimum 1g, maximum 1000g — silent clamp, no error shown (source: CONTEXT.md D-12)
4. Pre-filled with `product.serving_size_g` (default 100 if null)
5. Macro rescale formula: `(servingGrams / 100) * macro_per_100g`

### Modified: `NutritionDashboard` — Journal entry badge injection

Row layout (source: CONTEXT.md D-17):
```
[food_name (flex:1)]  [NS badge][ES badge]  [460 kcal]
```
- Existing `flexDirection: 'row', alignItems: 'center'` — insert badge container between name and calorie text
- Badge container: `flexDirection: 'row', gap: 4, marginHorizontal: 6`
- Only renders badges when `nutriscore_grade !== null` or `ecoscore_grade !== null`
- Manual log entries (null grades): no visual change (source: CONTEXT.md D-19)

### New: Dashboard Score Widget (source: CONTEXT.md D-20, D-21, D-22)

Position: after macros row, before TDEE Calculator link.
Visibility: hidden (not rendered) when `scoredMeals.length === 0` — `scoredMeals` = `todayLogs.filter(l => ['a','b','c','d','e','a-plus'].includes(l.nutriscore_grade))`.

Widget card structure:
- `backgroundColor: theme.surface, borderRadius: 14, padding: 16, marginBottom: 16, borderWidth: 1, borderColor: theme.border`
- Left: `ScoreBadge` at `size='md'` showing computed average grade
- Right of badge: label `nutrition.avgNutriscore` (i18n) + sub-label count string `nutrition.avgNutriscoreCount` (e.g., "sur 3 repas scannés")
- Average computation: map grades to numeric (a-plus=1, a=1, b=2, c=3, d=4, e=5), mean, round, map back to letter (1=A, 2=B, 3=C, 4=D, 5=E)
- Widget layout: `flexDirection: 'row', alignItems: 'center', gap: 12`

---

## Copywriting Contract

All strings follow `nutrition.*` i18n key namespace (source: CONTEXT.md discretion note).

| Element | i18n Key | FR Copy | EN Copy |
|---------|----------|---------|---------|
| 4th tab label | `nutrition.barcode` | "Code-barres" | "Barcode" |
| Scanner instruction | `nutrition.barcodeAlign` | "Alignez le code-barres pour scanner" | "Align barcode to scan" |
| Product not found heading | `nutrition.barcodeNotFound` | "Produit introuvable" | "Product not found" |
| Not found sub-label | `nutrition.barcodeNotFoundHint` | "Code scanné : {barcode}" | "Scanned code: {barcode}" |
| Primary CTA (product card) | `nutrition.logThisMeal` | "Enregistrer ce repas" | "Log this meal" |
| Secondary CTA (manual fallback) | `nutrition.enterManually` | "Saisir manuellement" | "Enter manually" |
| Scan again button | `nutrition.scanAgain` | "Scanner à nouveau" | "Scan again" |
| Camera permission denied | `nutrition.cameraPermDenied` | "Accès à la caméra refusé. Activez-le dans Réglages > Ziko." | "Camera access denied. Enable it in Settings > Ziko." |
| Dashboard widget label | `nutrition.avgNutriscore` | "Nutri-Score moyen" | "Average Nutri-Score" |
| Dashboard widget count sub-label | `nutrition.avgNutriscoreCount` | "sur {count} repas scannés" | "from {count} scanned meals" |
| Macros per 100g section label | `nutrition.per100g` | "Pour 100g" | "Per 100g" |
| Serving size label | `nutrition.servingSize` | "Taille de la portion" | "Serving size" |
| Serving unit | `nutrition.grams` | "g" | "g" |

Empty state (barcode tab before scan):
- Not applicable — camera is live immediately on tab focus (no empty state needed).

Error state (scan fails — network/API error):
- Use `showAlert(t('general.error'), errorMessage)` — existing pattern (source: CLAUDE.md; never use `Alert.alert`).
- No inline error state for network failures; modal alert is sufficient.

Destructive actions this phase:
- None new. Delete log entry already handled in `NutritionDashboard` with `showAlert` confirmation (existing, unchanged).

---

## Interaction Contracts

### Camera permission request
- Trigger: on first focus of Barcode tab
- Pattern: `useCameraPermissions()` hook from `expo-camera` v17
- If denied: render permission-denied state (State 5) instead of camera view
- Do not re-request on subsequent focuses if already denied

### Scan guard (prevent re-fire)
- Use `scannedRef = useRef(false)` (not `useState`) — matches pantry BarcodeScanner pattern from STATE.md accumulated decisions
- Set `scannedRef.current = true` on `onBarcodeScanned` fire before async call
- "Scan again" resets `scannedRef.current = false` and clears product card state

### Serving size real-time rescaling
- `onChangeText` on TextInput + chip `onPress` both call the same `setServingGrams(clamp(value, 1, 1000))` handler
- Macro display values are derived (not stored in state) — computed inline from `servingGrams` in render

### "Enter manually" from product card (source: CONTEXT.md D-10)
- Calls `editScanResult()` — existing function in `LogMealScreen`
- Prefills Custom tab with product data (name, macros scaled to serving size)
- Switches to `custom` tab

### "Enter manually" from not-found state (source: CONTEXT.md D-13)
- Calls `editScanResult()` with empty/default values — switches to `custom` tab with no prefill

### saveLog extension for barcode entries
- Extend `saveLog()` to accept optional fields: `food_product_id`, `nutriscore_grade`, `ecoscore_grade`
- These fields are included in the insert payload when logging from the barcode tab
- Manual log entries (non-barcode tabs) continue to call `saveLog()` without these fields — `null` is the default

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none (React Native project — not applicable) | not required |
| Third-party | none | not required |

No third-party component registries. All UI built from NativeWind inline styles + Ionicons + expo-camera (already installed per CONTEXT.md canonical refs).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Pre-Population Audit

| Decision | Source |
|----------|--------|
| Tab order, tab labels, icons | CONTEXT.md D-01, D-02 |
| Scanner UX (auto-scan, reticle, EAN-13 only) | CONTEXT.md D-03, D-04, D-05 |
| Scan guard pattern (scannedRef) | STATE.md accumulated decisions |
| Product card layout and inline display | CONTEXT.md D-08, D-09 |
| "Enter manually" reuses editScanResult() | CONTEXT.md D-10 |
| Serving size adjuster (chips + stepper, clamp 1–1000) | CONTEXT.md D-11, D-12 |
| Product not found state | CONTEXT.md D-13 |
| ScoreBadge color palette (A–E) | CONTEXT.md D-14 |
| ScoreBadge size variants (sm/md/lg) | CONTEXT.md D-15 |
| ScoreBadge component interface | CONTEXT.md D-16 |
| Journal row badge position | CONTEXT.md D-17 |
| Dashboard query unchanged (select *) | CONTEXT.md D-18 |
| Manual entries = null grades, no visual change | CONTEXT.md D-19 |
| Dashboard widget position (after macros, before TDEE link) | CONTEXT.md D-20 |
| scoredMeals definition (nutriscore_grade IS NOT NULL) | CONTEXT.md D-21 |
| Average grade computation (numeric mean → letter) | CONTEXT.md D-22 |
| 'a-plus' display as "A+" with dark green | CONTEXT.md specifics section |
| Design tokens (colors, fonts, spacing) | CLAUDE.md |
| Inline styles (no StyleSheet), Ionicons, showAlert | CLAUDE.md |
| surface card style (borderRadius: 14, padding: 16) | CONTEXT.md discretion + checker fix |
| paddingBottom: 100 for tab bar | CLAUDE.md known bugs/conventions |
| expo-camera v17 CameraView API | CONTEXT.md specifics section |
| Typography collapsed to 4 sizes, 2 weights | checker revision 2026-04-02 |
| Spacing scale lg token changed from 20px to 24px | checker revision 2026-04-02 |
| Non-multiple-of-4 padding values corrected | checker revision 2026-04-02 |
