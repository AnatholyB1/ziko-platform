---
phase: "42"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/src/coach/clients/db.ts
autonomous: true
requirements:
  - AUDIT-01
must_haves:
  truths:
    - "Coach opens a client's Programs tab and sees programs assigned to that athlete regardless of which coach created them"
    - "The program with the most recent start_date <= today appears as the active program; all others appear in history"
    - "If no program has a start_date <= today, active is null and all programs appear in history"
    - "Programs where assigned_to_user_id = clientId but created_by_coach_id differs from the requesting coach are still returned"
    - "compliance_pct is computed only for the active program; history entries have compliance_pct null"
    - "coach_display_name is populated on the active program if a profile join exists"
  artifacts:
    - path: "backend/api/src/coach/clients/db.ts"
      provides: "getProgramsForClient returns { active: Program | null, history: Program[] }"
      contains: "return { active, history }"
  key_links:
    - from: "backend/api/src/coach/clients/db.ts"
      to: "GET /coach/clients/:id/programs"
      via: "service.ts line 396 — result passed through c.json(result) unchanged"
      pattern: "return \\{ active, history \\}"
    - from: "apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/page.tsx"
      to: "ClientProgramsContent programsData prop"
      via: "programsData = json at line 69 — already correct once API shape matches"
      pattern: "programsData = json"
---

<objective>
Fix the Programs tab in the coach client detail view. Two bugs: (1) getProgramsForClient returns { programs: Program[] } but the UI expects { active: Program | null, history: Program[] }, causing the tab to always appear empty; (2) the query filters .eq('created_by_coach_id', coachId), which hides programs created by other coaches but assigned to the same athlete.

Purpose: Coach must be able to see all programs assigned to an athlete, not just self-created ones. Active/history split enables the UI to render the current program card vs the history list.
Output: Fixed db.ts function returning the correct shape; no other file changes required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/custom-coach/ROADMAP.md
@.planning/phases/42-audit-client-programs-visibility/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix getProgramsForClient — remove coach filter, return { active, history }</name>

  <read_first>
    backend/api/src/coach/clients/db.ts — lines 720-796 (the full getProgramsForClient function): read before touching anything to see current query, enrichment loop, and return shape.
    apps/web/src/app/[locale]/(coach)/coach/clients/[id]/programs/ClientProgramsContent.tsx — lines 1-27: read the ActiveProgram and HistoryProgram interface definitions; these are the exact contracts the API must satisfy.
  </read_first>

  <action>
    Modify getProgramsForClient in backend/api/src/coach/clients/db.ts (starting at line 721).

    Change the return type signature from Promise&lt;{ programs: any[] }&gt; to Promise&lt;{ active: any | null; history: any[] }&gt;.

    In the Supabase query (lines 728-733): remove the .eq('created_by_coach_id', coachId) filter. Keep .eq('assigned_to_user_id', clientId) and .order('start_date', { ascending: false, nullsFirst: false }). The select list must include all fields currently selected (id, name, description, goal, weeks_count, is_template, created_by_coach_id, assigned_to_user_id, template_source_id, start_date, weeks_data) plus also select coach_display_name if a profile join can be added — check whether a foreign key join on created_by_coach_id to user_profiles.display_name is feasible within the existing pattern; if the existing code does not join, leave coach_display_name absent from the select rather than breaking the query.

    Update the early-return guard on line 736: change return { programs: [] } to return { active: null, history: [] }.

    After the enrichment loop completes and enriched is populated, replace the final return on line 795:

    Determine today's date string as todayStr using new Date().toISOString().split('T')[0].

    Find the active program: iterate enriched (already sorted start_date DESC NULLS LAST) and find the first entry where prog.start_date !== null and prog.start_date <= todayStr. Assign to activeProgram (or null if none found). Per D-07/D-08: only one active at a time; null start_date = history.

    compliance_pct should remain computed only for the program at index i === 0 in the enrichment loop (existing logic). After the active/history split, verify that the program assigned as active is also the one at i === 0 — this is guaranteed because programs are sorted start_date DESC NULLS LAST and the first started program is always i === 0 among those with a start_date.

    Build history as all entries in enriched that are not the activeProgram. History entries must set compliance_pct to null (they are not the active program, so the existing loop already sets compliancePct null for i > 0 — this is preserved automatically).

    Return return { active: activeProgram ?? null, history }.

    The coachId parameter remains in the function signature (it is still passed from service.ts and may be used for future IDOR checks); do not remove it from the signature.
  </action>

  <verify>
    <automated>cd C:/ziko-platform/backend/api && rtk tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - db.ts line ~725: function signature reads Promise&lt;{ active: any | null; history: any[] }&gt;
    - db.ts query block: no .eq('created_by_coach_id', coachId) line present
    - db.ts query block: .eq('assigned_to_user_id', clientId) is present
    - db.ts: variable todayStr is declared using new Date().toISOString().split('T')[0]
    - db.ts: final return statement is return { active: activeProgram ?? null, history } (no programs key)
    - db.ts: early-return guard returns { active: null, history: [] } (not { programs: [] })
    - TypeScript check passes with no errors in db.ts or service.ts
    - GET /coach/clients/:id/programs response shape is { active: {...} | null, history: [...] } — verifiable by reading the return statement in db.ts, since the service returns c.json(result) unchanged (service.ts line 397)
  </acceptance_criteria>

  <done>getProgramsForClient returns { active: Program | null, history: Program[] }. The .eq('created_by_coach_id', coachId) filter is removed. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coach JWT → RLS | All queries use createUserClient(jwt) — Supabase RLS enforces coach_read policy |
| clientId param → DB query | clientId is validated against UUID_REGEX in service.ts before reaching db.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information Disclosure | GET /:id/programs — removal of created_by_coach_id filter | mitigate | RLS policy workout_programs_coach_read (migration 045) enforces assigned_to_user_id IS NOT NULL AND is_coach_of(coach, athlete); removing the app-layer filter does not bypass RLS — verified per D-05 |
| T-42-02 | Elevation of Privilege | Coach querying programs for a client they are not linked to | accept | Handled by RLS: createUserClient(jwt) scopes all reads to coach's own rows via is_coach_of() check in the policy |
</threat_model>

<verification>
After task completes:
1. Read db.ts lines 720-800 and confirm: no created_by_coach_id filter, todayStr computation present, return { active, history } at the end.
2. Run TypeScript check: cd C:/ziko-platform/backend/api && rtk tsc --noEmit
3. Inspect service.ts line 397 to confirm it still does c.json(result) unchanged — no service-layer changes needed.
4. Inspect page.tsx line 69: programsData = json — once API returns { active, history }, this assignment makes programsData.active and programsData.history available to ClientProgramsContent correctly.
</verification>

<success_criteria>
- GET /coach/clients/:id/programs returns { active: ActiveProgram | null, history: HistoryProgram[] }
- Programs created by any coach but assigned to the athlete are included (created_by_coach_id filter removed)
- Only one active program (latest start_date <= today); null if none started
- compliance_pct is non-null only on the active program
- TypeScript compiles cleanly across backend/api
- No changes to page.tsx, ClientProgramsContent.tsx, service.ts, or supabase/migrations/
</success_criteria>

<output>
Create `.planning/phases/42-audit-client-programs-visibility/42-01-SUMMARY.md` when done.
</output>
